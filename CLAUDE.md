@AGENTS.md

# Assistant session journal & self-review (running)

> Naldo asked me (the assistant) to keep this: a running log of what ships each
> session, an honest tally of what I get right vs. wrong, and a short retro so the
> work compounds instead of repeating mistakes. It loads every session via
> CLAUDE.md — **read the Scorecard first; it's the actionable part.** Newest
> session on top. Keep per-session detail short (the full narrative lives in
> `docs/context/session_log_naldo.md`); older sessions move to
> `docs/context/assistant_journal_archive.md` once this grows.
>
> Stable cross-session rules get PROMOTED to AGENTS.md "Pitfalls" (single source;
> AGENTS.md loads with this file). Promoted 2026-07-06: grep-count reconcile ·
> positive seam gates · migration order · browser-E2E-or-say-so + rollback lever ·
> flag-delete names its replacement. Promoted 2026-07-20: blocked-tool-call three-category
> fallback (transient outage / content false-positive / legit policy block) · concurrent-session
> re-scan before build · named-consent prod writes (folded into the blocked-tool bullet) ·
> combined-tree gate · inert-fix caller wiring · state-from-origin-master ·
> task-text-is-hypothesis · OneDrive-scoped subagent commit-per-piece · chain-after-fallible
> script-file surgery · session-number under concurrency. (Evidence numbers cite Naldo-side
> records.) Don't re-add them here.

## Running scorecard (cumulative — skim before starting work)

**Keep doing (these worked):**
- **[S32] Evidence-first debugging beat every guess this session.** The dead #157 exclusion was "working" per code+tests for 10 days — an instrumented replay of the EXACT runtime call against prod data exposed the swallowed 22P02 in one run. The #175 build started by PROVING Valor webhooks declines (Vercel logs, 60s latency) before writing code. The 60s-page mystery fell to a 5-angle measurement workflow that RULED OUT DB/payload/region/limits with live numbers, leaving the auth gate as the only structural difference. Reproduce → instrument → then build.
- **[S32] The wrap/lens pipeline scales: 13 PRs in one session, every finding back to a worker with specific corrections.** Builders repeatedly caught wrong claims in MY briefs (swapped line labels, a wrong "same pattern" premise, a nonexistent column claim) because every brief says distrust-the-brief; honest builder reports (self-flagged residuals, deviations explained) made dispositions cheap. Reward that shape in briefs.
- **[S30] Read-before-write on any recovery that trusts an id from an error payload.** GHL's dup-400 hands back `meta.existingId` — which can point at an ACTIVE cross-pipeline deal. Fetching the card first and REFUSING open ones turned a silent deal-hijack into a clean 409. Three review lenses independently converged on exactly that guard — convergence like that means it's a class, not a nit.
- **[S29] Trap-first probe ladder for undocumented vendor channels.** Seven $1 tests solved a 5-week blocker because each test answered exactly ONE question via a names-only diagnostic first (log the shape → prove auth → persist guarded → fix the clobber → prove reuse). Never build the wiring before the channel's real shape/signing/behavior is observed — the vendor's dashboard and docs both lied (schema, save_card, VT-only scare).
- **[S46] Ground a greenfield "how would we build X" in the codebase BEFORE designing it.** A grep for the feature turned a from-scratch design into "extend what exists" — the dormant #82 text-bot (webhooks/allowlist/dispatcher/`notifyTelegram`) + the inbox's existing GHL call-channel ingest meant the hard 70% was already built. Recon first; the plan gets smaller and truer.
- **[S45] Answer a "why does this keep happening / is it my fault" question by CLASSIFYING the real incidents from the logs, not asserting.** Pulling the actual classifier-block incidents (S40/S42 transient fail-closed Bash outages · S44 content false-positives · S31/S34 legit policy blocks) turned a vague frustration into a 3-category answer with per-category fixes, and separated the classifier gate from the already-solved S43 MCP-transport drops. The recurring fallback itself is now codified in AGENTS.md Pitfalls (don't re-derive it).
- **[S44] Before deleting a shared UI block, ENUMERATE what lives inside it** -- the "delete the sticky bar" brief would have silently deleted our own lead-form's placement marker, which was nested in the doomed container. Same class as the S19 sceneLinks lesson: when you remove or widen a shared structure, list its occupants/consumers FIRST. Also: trace a site-wide element to its real owner (a template, not the page you were pointed at) before editing.
- **[S31] Third-party UI writes need a POSITIVE persistence check, not a click** -- PostHog insight saves are real only when the URL flips off /insights/new; Elementor code-UPDATEs silently revert (create a new entry). Verify the artifact, then move on.
- Branch off **FRESH `origin/master`**, never the worktree's stale base — first confirm the spec/plan commits are actually present (mine weren't until I rebased).
- Re-ground the exact lines before planning, **then independently verify the recon** — a fast read-agent missed a real data leak and cited a stale file; my own grep caught both.
- TDD + run all three gates (`tsc · lint · vitest`) **after every phase**, not just at the end.
- Run the adversarial review before merge — and **disposition** each finding (fix / accept-as-parity / process). Don't blindly "fix" a pattern that matches existing prod code.
- Verify safety before an irreversible prod op (grepped for bare-anon data paths before enabling RLS; smoke-tested the live app after).
- Surface the high-risk / council-worthy / shared-file calls and confirm outward-facing actions instead of rubber-stamping.
- **[S18] Verify UI in the ACTUAL browser** — tsc-green ≠ correct. A client/server import-boundary break (#52 dragged the server-only `sharp` chain into the client bundle) and a HIGH `/training/new` railing ship-blocker BOTH passed tsc (an `as` cast laundered the type) and surfaced only in-browser + in the adversarial review. **[S19 reinforced 3×]** — Jason's device rounds caught a tag-expansion leak, a garland-trace gap, and a strip-placement miss, all invisible to tsc/tests.
- **[S19] Checkpoint cadence for epics** — slice a big feature into 2-PR chunks → dev device-verifies → merge bottom-up → next chunk. Beat both "one giant PR" and "gate every PR" on #13 (6 PRs, 3 checkpoints, feedback reshaped each next slice; stacks never grew past 2 branches; relays only on approved editor-core).
- **[S19] Scope a big epic with a recon workflow BEFORE the plan** — the 6-agent #13 recon + completeness critic caught the `maybeSingle()` silent-empty-materials trap (killed the N-designs architecture before any code) and 5 wrong claims via cross-verification.
- **[S19] Mechanical prompt/text moves → prove byte-identity** — extracting shared blocks out of the quoting SYSTEM_PROMPT via a substring-move script + an eval-and-compare check made "the prod prompt didn't change" a fact, not a hope.
- **[S20] Model-routing policy pays — Sonnet builds, Opus reviews/orchestrates (the seat), Fable only-if-asked.** The whole #110 fix marathon (~19 PRs) ran Sonnet-build/Opus-review — dramatically cheaper than S19's Opus builders, same quality, because the seat reviews every diff + integrates. Route deliberately, don't default everything to the top tier.
- **[S20] Write a workflow script to a FILE + `node --check` before launching.** A W4 workflow died on a missing `)` in a `.map()`; a W5 workflow was fine. The habit: Write the script, `node --check` (top-level `return` is the only expected "error"), then `Workflow({scriptPath})`. Cheaper than a failed launch.
- **[S20] Audit-workflow shape that worked 6×:** dimension finders (per sub-area × lens) → semantic dedupe → **adversarial verify every finding** (refute-lens for bugs, real-at-this-scale for perf, payoff-lens for refactors) → completeness critic. Pre-load intentional-design context so finders don't "discover" deliberate behavior as bugs. Dedupe against a seeded known-findings ledger so waves don't re-report #80.

- **[S23] For a customer-facing go-live you can't drive live (no secrets/node_modules), an in-process E2E of the REAL modules + a seeded `is_test` prod quote substitutes.** Drove pricing→packages→$2,500-gate on a real house via `tsx` (front $2,480 · sides $2,765 · back $1,925 → $7,797.38, D===total no drift), then seeded a permanent `is_test` quote for the human's live-portal eyeball. Validated P4/P5/P6 without a browser.
- **[S23] The adversarial hunt finds the money bugs the happy path hides.** The 7-dim hunt's HIGH was a tax-override under-bill that only triggers on a PARTIAL approved selection (permanent per-side packages A/B/C make partial the DEFAULT) — invisible to the go-live E2E; two-lens verify (reachability + money) killed 6 false positives; the completeness critic flagged the BOM engine as dead code (= the P7 build). Seed the hunt with confirmed-intentional design so finders don't re-flag it.
- **[S24] Adversarially review YOUR OWN just-built stack before merge — the review's 5 confirmed findings were ALL in MY new #134 filter** (headliner: holiday's empty 'D' recommendation slot filtered out before the loader populated it → the whole #12 staff-recommendation flow + Build-Your-Own would have silently died on every ≥$1k holiday quote). My browser checks had "verified" the feature on quotes that never exercised the interplay. Fixed + regression-tested pre-merge.
- **[S24] Domain-rule Q&A rounds BEFORE the plan (AskUserQuestion with concrete hypotheses; ask for the dev's images).** Jason's "every track cut needs an extension" answer collapsed most of the AI-detection problem into pure geometry (interior vertices of drawn lines) — the analyzer's job shrank to drawing lines + jumps/splitters, and the card works with zero AI. The plan got fundamentally simpler because the questions came first.
- **[S24] Seed AI output as DRAWN EDITABLE GEOMETRY, never as numbers.** The rebuilt permanent analyzer seeds the same satellite channels/strands the operator draws by hand; footage/corners/extension counts all derive from visible lines. That's the trust-model difference from the removed S22 analyzer — and it made the live E2E one click: address → lines → all numbers.
- **[S25] Deterministic geometry beats AI wherever geodata exists.** The Street View pano's camera coords (already fetched, previously discarded) turned satellite orientation from the #1 AI failure mode into pure math — the relabel corrected the model on its FIRST live run. Look for discarded metadata before reaching for prompts or training.
- **[S25] Reconcile the engine against the dev's REAL spreadsheet on identical inputs, and itemize every cent before re-anchoring goldens.** The #144 pass found 5 rule gaps (incl. ordering T-taps with ZERO wire); both golden-job deltas decomposed exactly (+$104.55/+$84.40), so the re-anchor was evidence, not fudge.
- **[S25] The dev's device round IS the domain spec.** The written S24 "road-viewer's left" rule was simply wrong; Jason's real-quote check set the true convention (homeowner-left). Written rules are hypotheses — supersede them on operator evidence, and mark the supersession in the notes.

**Fix going forward (mistakes I've made):**
- **[S47] A test identifier that collides with a live record MUTATES real data — check before any prod-writing test.** Used Naldo's real phone in a partial-capture probe; GHL upsert matched his real contact by phone and overwrote its email/name. Before putting a real-shaped phone/email into a test that writes to prod (Supabase or a CRM upsert), query whether it already matches a live row/contact; a "test" value is not inert just because I labeled it one. Same family as task-text-is-hypothesis.
- **[S47] Read what a "test mode" actually suppresses before calling an outward action safe.** `?yll_test=1` only silenced a Telegram ping; the `new lead` + real-SMS enrollment ran regardless. Verify the flag's real gate in the code BEFORE telling the dev a send/charge/enroll is safe to drive.
- **[S32] A flagged-deferred residual on a CUSTOMER path is a ledger row, not a PR footnote.** #653 explicitly flagged the multipart analyze upload as still-413-exposed and deferred it — it bit Jason live the NEXT DAY as a cryptic JSON parse error. If a review names a reachable customer-facing residual, open the ledger row (or fix it) in the same breath as the merge.
- **[S32] Cross-surface parity is STILL my blind spot (4th session running).** All four wrap-review fix findings were parity gaps: staff-approve missed the TOCTOU re-check its 5 siblings got; the LIST page's actions menu never learned the flag the DETAIL page hides on; two 409-copy sites missed the special-case their siblings got. At feature end, enumerate EVERY surface offering the guarded action + EVERY sibling of a hardened idiom and reconcile the list to zero — grep the idiom (`.eq('view_only'`, `isViewOnlyCode`), not just the symbol.
- **[S32] Shared-tree parallel builders cost real incidents (~5 in one day)** — promoted to AGENTS.md Pitfalls (worktree isolation, own npm ci, git-show-only reviewers). Don't re-derive; follow it.
- **[S30] Half-wired features, three flavors in one session (all wrap-caught):** a severity system that classified only 2 of the 4 money-moved reasons as critical; a wrapper migration (queueAttach) that left ONE direct call site (the promoted grep-count pitfall, live again); an API visibility field (`resurrected`) the UI never rendered (the promoted inert-fix pitfall, in miniature). Lesson: at the END of any cross-cutting change, grep the OLD symbol / enumerate the FULL reason list / wire the consumer — reconcile to zero in the same diff.
- **[S30] A label rename is cross-cutting:** renamed a job-status label on my surface without checking the OTHER renderer (Naldo's dashboard) — created a cross-surface terminology collision AND an ambiguity (his board already used the new wording for a different entity). Grep the display string across BOTH owners' surfaces at rename time; flag collisions then, not at wrap.
- **[S29] Wiring a previously-guarded path MUST update its safety comment in the SAME diff.** The match script's header still said `--live # (guarded — refuses until wired)` after I wired it — a misleading safety claim on a prod-writing script, caught only by the wrap review. Grep the file's own docs when a stub becomes real.
- **[S44] A CSS `display:none` hide is NOT a removal -- and a past session's "fixed" is a hypothesis.** S34 hid the old sticky with `.elementor-element-a0fa6d1{display:none!important}`; that rule was STILL live in the Customizer, yet the bar was rendering on a fresh logged-out NitroPack-purged page. Re-verify a CSS-level workaround is actually holding before trusting the note that says it was handled, and prefer deleting the element over hiding it.
- **[S40] Vercel's MASKED (Sensitive) env-value fields eat blind coordinate typing** -- the financing flag saved EMPTY on the first flip (caught only because flag-off renders nothing). Set masked fields with form_input on a DOM ref (it echoes the value back) and verify by the FEATURE, not the save toast.
- **[S25] Vercel deploys: verify the SHA by API, never trust "a deploy went READY".** One master webhook silently never spawned a deployment, and a dashboard Redeploy rebuilt the OLD commit — it looked live while prod served stale code. Dashboard Create-Deployment (from ref master) is the recovery lever; env-var changes need a fresh deploy to apply.
- **[S24] `gh pr merge --delete-branch` on a STACK BASE hard-closes the stacked PR — unrecoverable** (GitHub can't retarget or reopen once the base ref is gone; #451 had to be recreated as #456). Retarget the remaining stacked PRs to master BEFORE merging their base, or merge without `--delete-branch` until the stack is flat.
- **[S24] A derive effect that writes form state must respect REOPEN semantics.** My accessories-derive keyed on session state (`permanentSatLines`) that is NOT rehydrated on quote reopen — it would have clobbered saved counts with zeros/street-only. Gate such effects on the live session (mirror the footage effect's `satellitePreview != null` / hadRef pattern). Caught mid-build by asking "what does this do on a reopened quote?"
- **[S23] Supabase MCP does DML fine; only DDL (schema/migrations) needs the browser SQL editor.** Used `execute_sql` to read `app_settings`, DELETE the inert flag row, and INSERT a seeded test quote — all worked. Don't route data reads/writes through the browser SQL editor. (Supersedes the retired S13 read-only note.)
- **[S23] A post-merge restarted branch needs `--force-with-lease` to push, and fast-moving master will stale a PR mid-review — re-merge + re-gate the combined tree before landing.** #378 went 23 commits stale (Jason's #379 event-flag removal, mirroring mine) between green and merge; merged master in, resolved a `serviceType`/`isPermanent` prop collision by consolidating onto the shared prop, re-gated (vitest 1607) before merging. "Never merge stale" bites even same-day.
- **[S20] A big parallel fan-out WILL occasionally hit a transient server-side rate-limit (not your quota) — resume from the journal, don't re-run.** W5's first run lost 10/14 finders to rate-limiting; a resume replayed the 4 cached + re-ran the 10, and because their findings feed dedupe/verify/critic those re-cascaded correctly (the partial 3-HIGH re-adjudicated to the accurate 1-HIGH on the full set). Don't trust a partial workflow result; resume for full coverage.
- **[S19] Expanding shared link data → audit every downstream consumer first.** The sceneLinks twin-expansion (right, for portal toggling) silently fed twin ids into the photo-tag chip lookup → canonical rows mis-tagged. When a shared structure gains members, grep its readers before shipping.
- **[S18] Widening a shared/analyzer type → grep EVERY consumer + stale narrow duplicate up front.** The railing detection type had two parallel narrow copies (`/training/new`, `QuoteBuilder`) hidden behind `as` casts that tsc didn't flag; only the review caught them. Before shipping a type widen, list every UI that displays/edits it.
- **[S13] Map PR dependencies FIRST.** Given a batch to merge, check base branches (`gh pr view <n> --json baseRefName`) before touching anything — I began merging a *stacked* PR (#229) out of order and had to abort + re-plan bottom-up (#227→#228→#229).
- **[S13] Browser-tool quirks:** the Chrome `javascript_tool` REPL doesn't reliably await a long async loop (a Monaco poll returned `{}`) — use synchronous checks + explicit `wait`s. The Supabase SQL editor takes ~12–18s to mount Monaco; wait up front. And **batch browser actions** (`browser_batch`) instead of one screenshot/click/wait per call.

## Sessions (newest first)

### S47 (Naldo, review-followups sibling) -- 2026-07-20→08-04 -- VERIFIED partial/abandoned-form lead capture (#584) LIVE end-to-end; found + fixed 4 customer-facing bugs of two classes (auth-allowlist-lag + E.164 phone dedup); 6 PRs merged (#604/#605/#614/#617/#622/#625), #637 OPEN -- close PR naldo/s47-reviewfollowups-close (touches CLAUDE.md → NOT auto-merged, needs Naldo's go)

- Invariant PROVEN live through the real forms (driven in Naldo's Chrome): partial+no-consent → `partial-lead` tag ONLY, no drip; consented submit → `new lead` + card. All 3 form variants fire. The prod blocker was the auth PERIMETER (`operatorGate` default-deny), not the embed/cache: `/api/leads/partial` was missing from `isPublicPath` so it 401'd every anonymous visitor (my browser probes "passed" only on Naldo's session cookie; anonymous curl proved the 401). Same class 401'd customer PDF downloads + the #163 colour-change (#614). Plus an E.164-vs-10-digit phone-compare that silently renamed real GHL contacts (#604) + the same phone class in dashboard `customerKey` (#617), and the adjacent email case/trim normalization there (#622).
- MISTAKE: used Naldo's REAL phone in a prod-writing test without checking it first → the upsert matched his real contact by phone and OVERWROTE its email/name (still owed: his GHL merge to fix it). Lesson: a test identifier that collides with a live record mutates real data; check before writing.
- MISTAKE: told Naldo step 5 was safe "with the test flag" — `?yll_test=1` only silences a Telegram ping, `syncLeadToGhl` (real `new lead` + SMS) runs regardless. Caught by reading the route before executing, but the false-safety framing was already out. Lesson: read what a "test mode" actually gates before calling an outward action safe.
- Did right: took over the browser leg myself when pushed ("why aren't you testing this yourself"); verified before asserting on prod reads (control-tested `get-opportunity` on a known-good id before concluding a card vanished); named-consent on every prod write; red-green proved the new test by reverting the line; the 4-agent session review confirmed the work sound + surfaced 4 leftovers (3 fixed in #637, 1 email-overwrite vector ticketed for Jason); the CI merge rule (#625) enforced itself (caught its own branch stale).

### S32 (Jason) -- 2026-07-28→30 -- THE 13-TASK SHIP DAY: #175-#186 all shipped (PRs #645-#656, every one lens-reviewed + CI-read + combined-tree-gated + SHA-verified) · Kristie decline→browse-twin→paid arc · the dead-#157-exclusion forensics · the 60s-operator-page diagnosis -- close PR jason/s32-close + jason/s32-wrap-fixes

- MISTAKE: #653's explicitly-flagged multipart-upload residual bit Jason live the next day as a cryptic parse error. Lesson: a deferred customer-path residual gets a ledger row at merge time.
- MISTAKE (4th session running, wrap-caught ×4): cross-surface parity — staff-approve missed the TOCTOU re-check its 5 siblings got; the list-page actions menu never learned view_only; 2 of 4 stale-tab 409 copy sites missed the special-case. Lesson: at feature end, grep the hardened IDIOM across every sibling and reconcile to zero.
- MISTAKE (promoted to Pitfalls): concurrent builders shared the main worktree — branch flips under readers, a stash that took S31's untracked scripts off disk, node_modules eaten 3× by junction teardowns.
- Did right: evidence-first everywhere (instrumented prod replay exposed the 22P02-poisoned exclusion that code+tests swore was fine; Valor decline webhook PROVEN in logs before #175 was built; the 60s mystery measured from 5 angles until only the auth gate survived); read-before-delete caught that "TEST Walkthrough" was actually naldoven's live demo quote; 13 PRs of builder→lens→disposition→worker-fix cadence with zero seat redos; every deploy SHA-verified; prod data ops named-consented + reverted-after-test.

### S30 (Jason) -- 2026-07-24→26 -- AUTO-CHARGE ARMED + PROVEN ($1.09 live button test; #170 a–e + payment preference; **#83 COMPLETE**) · #172 GHL abandoned-card resurrect (diana unblocked + sent) · portal custom-label fix · #169 aftercare · Rony #1167 revert -- close PR jason/s30-close (+ wrap-fixes PR #643)

- MISTAKE (x3, one family): shipped three half-wired pieces the wrap lenses caught — the critical-tone set missing 2 of its 4 members, one attach call site left off the queueAttach migration, the `resurrected` response field never rendered by the UI. All fixed in #643. Lesson: finish cross-cutting changes by reconciling to zero (grep the old symbol, enumerate the whole reason class, wire the consumer) before calling the diff done.
- MISTAKE: the "Awaiting payment" rename collided with Naldo's dashboard terminology (his board says "Requires invoicing" for the same status and already used "Awaiting payment" for invoices). Checked his file enough to NOT touch it, not enough to flag the collision at rename time.
- Did right: probe-first on GHL (read diana's real card + a multi-pipeline probe BEFORE designing the resurrect); the read-before-write open-card refusal; migration-first; CAS discipline end to end (settle claim, txn record, appendRetiredTxn, the #1167 revert guard); every deploy SHA-verified via the Vercel API; the classifier's block on my prod-write E2E honored (offered the paste-able script; Jason's own button test replaced it); four-lens on both money PRs caught 3+10 real findings pre-merge including a cross-pipeline active-deal hijack.

*(Journal archive holds S19-S20, S23-S26, S28-S29, S31, S40, and S44-S46. S21-S22, S27, S30, S32-S39, and S41-S43 have no journal blocks anywhere; their lessons live in the session logs and the local self-assessment.)*
