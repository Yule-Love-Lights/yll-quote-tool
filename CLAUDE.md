@AGENTS.md

# Assistant session journal & self-review (running)

> Naldo asked me (the assistant) to keep this: a running log of what ships each
> session, an honest tally of what I get right vs. wrong, and a short retro so the
> work compounds instead of repeating mistakes. It loads every session via
> CLAUDE.md — **read the Scorecard first; it's the actionable part.** Newest
> session on top. Keep per-session detail short (the full narrative lives in
> `docs/context/session_log_naldo.md`); older sessions move to
> `docs/context/assistant_journal_archive.md` once this grows.
>
> Stable cross-session rules get PROMOTED to AGENTS.md "Pitfalls" (single source;
> AGENTS.md loads with this file). Promoted 2026-07-06: grep-count reconcile ·
> positive seam gates · migration order · browser-E2E-or-say-so + rollback lever ·
> flag-delete names its replacement. Promoted 2026-07-20: blocked-tool-call three-category
> fallback (transient outage / content false-positive / legit policy block) · concurrent-session
> re-scan before build · named-consent prod writes (folded into the blocked-tool bullet) ·
> combined-tree gate · inert-fix caller wiring · state-from-origin-master ·
> task-text-is-hypothesis · OneDrive-scoped subagent commit-per-piece · chain-after-fallible
> script-file surgery · session-number under concurrency. Promoted 2026-08-05: sibling-guard parity (S32/S33/S51). Promoted 2026-08-25: mark-a-row-off-when-you-ship-it (S49). (Evidence numbers cite Naldo-side
> records.) Don't re-add them here.

## Running scorecard (cumulative — skim before starting work)

**Keep doing (these worked):**
- **[S87] Three review lenses converged on one HIGH, and the proof was sitting in our OWN ledger the whole time.** The business-card QR pointed at `/get-a-quote/`; row 272, open on master, already recorded that a Rank Math redirect created 2026-06-26 makes that URL 301 to the homepage and had counted 48 hits. I had the S50 memory of this and still sent Naldo off to check the page by hand instead of grepping the repo. The S55 lesson recurring: when a URL or a literal string is in play, grep our own records BEFORE asking a human to go look, and before writing code that depends on it being fine.
- **[S87] When a concurrent session's answer is better, reset onto it — do not merge over it.** A desktop session worked the same branch and reached the same four findings independently. On the allowlist it was RIGHT and I was WRONG: I narrowed the public prefix to a single segment, which would have sent `/qr/a/b` to the operator LOGIN page; it made the route an optional catch-all so every path under `/qr` lands on the marketing site, and pinned the namespace with a test that fails if a second route ever appears. My push was rejected twice; reading instead of forcing is the only reason nothing was clobbered.
- **[S87] A mutation probe on an auth allowlist is the difference between a guard and a hypothesis.** Removing the one allowlist line served the operator login shell to both real printed codes — the exact outage being fixed, invisible to 10,000+ green tests because nothing pinned that constant. Same session, the prototype-key bug (`/qr/constructor` emitting a fabricated `utm_campaign`) was found by RUNNING the function over hostile keys, not by reading it.
- **[S59] Measure the thing the design depends on BEFORE offering the design, and let the number delete an option.** Phase 3 could have computed pay from hours x rate. One query killed it: Naldo logged **50h 55m in seven days**, and overtime has no formula here (row 285, parked pending an accountant), so a flat-rate figure would have been wrong for a real week already sitting in the data. That turned a money-math risk into a PRODUCT question with three options and a recommendation, and Jason's answer (the tool records payments, it does not calculate them) is now a structural property: `total_cents` is typed, the reference is stamped, and nothing asserts the two agree. A tool that refuses to have an opinion about unruled payroll cannot be wrong about it.
- **[S59] A new guard that BREAKS other tests is a guard that fires — read the breakage as evidence, not as a chore.** Adding `assertNotSettled` broke 10 existing tests with `unexpected table shift_settlement_lines` before the mock knew that table existed. That is free proof it is genuinely reached on BOTH write paths, which is exactly the question the inert-fix pitfall says an author cannot ask themselves. Same session, the same signal one layer up: three review lenses each found something the other two structurally could not, and the admin HIGH (the sweep-closed warning dropped in the one panel where paying locks those hours) was invisible to the technical lens reading the same diff.
- **[S59] The adversarial delta-verify proved a gap by MUTATION that four lenses and I had all read past.** It removed the crew's "your payment was undone" notice and found that NO test failed, while the matching notice on the record path was pinned. Nobody reading the file would have noticed the asymmetry; the probe found it in one run. This is the fourth consecutive session where the delta-verify caught something the fix round itself introduced or left, and it cost one agent.
- **[S59] Separate the POLICY change from the build it unblocks, even when the same person authorises both.** The AGENTS.md ownership row moved in its own PR (#1177) with Naldo's ack quoted verbatim, not paraphrased, and not bundled into the feature PR that needed it. The process lens's own finding was to quote rather than paraphrase — which the inbox row already did — so the precedent was there to follow and I had not followed it.
- **[S58] When two independent readers disagree on 18 of 123 rows, the FILE is ambiguous — build the tool to say "unclear", not to pick a side.** The ledger reader's first draft called 44 rows finished; a human read said ~25; the honest third state ("looks finished, the row does not say so plainly") is what made `lint` trustworthy, and the eight rows it structurally cannot see are named in its own header. Same session, the check-the-check class hit the instrument three times (cell-head vs whole-row, an unanchored proximity match, emoji classes without the `u` flag) and each was found by measuring the tool against a known set, never by reading it.
- **[S58] Read the vendor's OWN record before theorising about your code.** The missing "booked" emails had five plausible code/infra causes and every one died on a measurement; the answer was one field on the GHL contact (`dndSettings.Email.status = active`, code 105) with a timestamp that bracketed the last accepted send to the second. The customer sends in the same batch succeeding was the tell that the code was fine — a failure that is recipient-specific is a recipient problem.
- **[S58] Measure the schedule before making it authoritative.** Jason's rule was "the last job on the schedule"; 0 of 44 jobs have an install date, so the rule as stated computes nothing on every real day. One query turned "schedule is authoritative" into "GPS now, schedule later" before a line was written — the inert-feature class caught at plan time.
- **[S85] A second review pass over the same file can catch a bug the first pass's own fix introduced, and a "cosmetic" label from the agent is a hypothesis, not a verdict.** A post-close integration lens flagged a stale session-number string in a code comment as cosmetic, but the same bug (a blind find/replace during an S84 to S85 renumber) had also turned a factual sentence in the journal fragment's own NUMBER paragraph into nonsense: "a claim on S85... took S85 anyway after confirming no S85 collision exists" describes no real event, since a numberless collision explains no "anyway." Reading the agent's note as a prompt to re-check the surrounding prose, not just apply the one-word fix it suggested, is what caught the real defect.
- **[S85] Verify a computed style, not a screenshot, when a fix is "make it bigger."** `getComputedStyle` on the live-rendered button confirmed padding 12px/8px to 20px/12px and font 12px to 14px, a fact a screenshot only suggests. Caught nothing wrong here, but it is the difference between a claim and a measurement for a one-line CSS change that would otherwise ship on eyeballing alone.
- **[S77] When an instrument and a count disagree about WHEN a problem started, check when the instrument was switched on.** The daily robot reported the marketing site's JavaScript error as "0 to 201 in 24 hours" and suggested chasing recent WordPress and Elementor updates. One query killed that framing: pageviews on that host go back to 2026-07-30, the first `$exception` is 2026-08-28, the day capture was enabled. The spike was the measurement starting, not the bug starting, so the whole "which update broke it" search was a dead end before it began. The real cause was our own dead code, live for an unknown but long time.
- **[S77] A monitoring feature can be ON, configured, and structurally incapable of firing: check its numbers against the population before calling it armed.** PostHog's spike detection shipped with a minimum threshold of 500 exceptions in a 5-minute window; the largest bucket this project has EVER recorded is 23. Turning the alert on without that one query would have produced a permanently silent alert that everyone believed was watching. The same session then found the message body was a static string with no tokens at all, so even a firing alert would have named neither the error nor the issue. Both were invisible from the "alert enabled" state.
- **[S82] A control that FAILS is the instrument confessing, and reading it that way is what turned a wrong root cause into the real one.** A prod cron 401'd on the HighLevel user lookup and I published a scope-gap theory; Naldo said the Vercel and local keys are the SAME, which killed it. My own probe then 401'd on all three calls INCLUDING the control I knew worked, so the instrument was wrong, not the token: I had used `rest.gohighlevel.com/v1` instead of the `services.leadconnectorhq.com` plus Version header the app actually uses. Corrected, every call died at the network layer, because this machine's router resolves that host to an ISP filter address while public DNS returns Cloudflare (the S75 poisoning, new host). With the address pinned it returns 200. The token was always fine and the 401 was transient. The durable finding was one layer down and worse than the theory: `getGhlUser` never retries, so a one-second blip permanently unassigns a task (row 489).
- **[S82] Measure the row before believing the layout fits, and assert nothing WRAPPED before trusting an overflow number.** Adding a 12th nav item with both badges showing measured 22px of page overflow at 1024px and 6px at 1280px, neither visible by reading. The file's own comments record a previous false pass where a squeezed multi-word button wrapped to two lines, shrinking its measured width and hiding a real shortfall, so every measurement asserted uniform element heights first and only then read scrollWidth. Final: 0 overflow at 1024, 1120, 1280 and 390.
- **[S82] When a review finds two problems that share one cause, fix the cause.** Two staff-lens MEDs (the form sits under the whole list below 1024px; a keyboard user tabs through every row to reach it) were the same fact seen twice, because getting the tasks first on a phone is what puts the form last. One control answered both: a visible button below the breakpoint, a focus-only skip link above it. Same session, the same move on the URL and tab desync, where two findings were one duplicated piece of state.
- **[S57] The browser drive found four defects that tsc, lint and 8,448 tests all passed — and the negative control caught a fifth that the fix itself introduced.** Opening the migrated quote showed a missing badge, a banner inviting an action the server refuses, futile lock copy, and a false subtitle; every one invisible to static checks because they are all COPY, and copy compiles. Then driving a NON-migrated quote as the control proved the ordinary path was untouched AND surfaced a spacing regression my own edit had just made. One instrument note worth keeping: my first programmatic probe reported all three fixes missing because `document.body.innerText` returned 122 characters (just the nav) — `textContent` and the screenshot both showed them present. The check was broken, not the code; check the check before believing a red result.
- **[S57] Reconcile a migration against the SOURCE system's own reports, not your own arithmetic — three independent exports, three matching totals, and the only two differences were facts already known.** `payments.csv` → collected $29,890.70, the A/R aging report → outstanding $11,272.18, revenue-by-customer → contracted $41,162.88, each to the cent. The two adjustments (one payment collected after the export; five estimate-sourced customers who never had an invoice and so appear in neither report) were explanations, not fudges. That is what turned "the migration is done" from an opinion into a finding. It also needed a rule that is not obvious and is now written down: money in = invoice payments + `added to credit` − `decreased from credit`, and an invoice row paid from `Customer Credit Balance` is a credit being APPLIED, not new money — without it one customer reads $6,811.89 instead of $4,093.14.
- **[S57] Suspecting data is wrong is not evidence it is wrong: check before "correcting" it, or you write errors into correct records.** Six of Mary's and Raymond's installment dates sat exactly on their due dates, which is the classic signature of assumed data — and Jane's real dates never did. The export showed Raymond genuinely paid on the due date three times running; only ONE date of the six was wrong. Correcting all six on the pattern would have corrupted five true records. Same session, the mirror case: an assertion stopped me deleting another dev's stray conflict marker because my claim about the preceding line was wrong, and I only removed it after confirming no content had been lost.
- **[S79] Check the invoice before the code: a paid dependency that silently stopped working had stopped for BILLING, and two confident technical root causes were published before anyone looked at the account.** NitroPack was serving zero optimized pages on the marketing site. I diagnosed the Meta pixel's server-side `Set-Cookie: _fbp` (refuted by a request that already carried the cookie: no Set-Cookie in the response, still a MISS), then SiteGround's WAF 403-blocking their crawler, and **pushed that second theory into a live support ticket before testing it**. Support answered in one message: the Business plan's renewal had failed. The evidence was visible the whole time (renewal cancellation emails in the info@ inbox, "Next billing: N/A" on the dashboard) and I filed it as a footnote. Worse, the wrong diagnosis produced a fake decision I handed the owner: "fixing this costs you Facebook ad attribution" was a tradeoff that never existed. Lesson, now a habit: when a paid third-party service stops working, read the account and billing state BEFORE building any mechanism theory, and say which diagnosis a tradeoff rests on before asking the owner to weigh it.
- **[S79] A fix verified with an optimizer switched OFF is a hypothesis, and turning the optimizer on silently killed FOUR snippets, three of which I never looked at.** Accessibility was verified at 100 while NitroPack served nothing. The moment it started serving, NitroPack rewrote every inline script to `type="nitropack/inlinescript"` and deferred it until user interaction, so both accessibility snippets went inert and the score fell to 94. I fixed the two I had authored with `nitro-exclude` — and did not sweep the siblings. The wrap admin lens then found **Mobile Header Clearance (4400) broken on the live site right then**, the logo printing straight across the page title on mobile, which is the exact defect a previous session had already fixed once. The wrap staff lens found the lead form's **embed auto-height script (4323)** deferred too, on the page where customers actually submit. Both fixed and re-verified. Lesson: when an optimization or caching layer changes state, every prior verification is void, and the sweep is over the whole POPULATION of scripts, not the ones you happened to write.
- **[S79] Three lenses converging on the same weakness is a class, not three nits — and the fix was structural rather than three patches.** The customer lens found the honeypot fix never reaches the real quote form (it is an Elementor popup injected on click, and the script only ran at load, 2.5s and 7s); the staff lens found a late-mounting carousel would miss its labels; the technical lens had the timing model on its plan. One debounced MutationObserver closed all three. The same round also caught that my `!important` background had killed the hover state on the lead-form submit button, the site's primary conversion CTA — found by a real hover in a headless browser, fixed inside the close by splitting the rule. Proven with a control: before injection the page carries ZERO honeypots (so the old fixed-timing fix provably could not have reached them), and a form injected after load got `tabindex="-1"` within 600ms. Performance stayed at 85 and TBT at 0ms afterwards, so the observer cost nothing measurable.
- **[S79] I told the owner the job was complete while the single thing he opened with was still failing.** He asked for the Agentic Browsing audit fixed "ASAP"; I fixed the video element, watched that specific failure disappear, and reported done — while the category still read 2/3 because a second element (unnamed carousel buttons) was failing behind it, and I already had that finding in hand from a Lighthouse run. His own PageSpeed link surfaced it, not me. Lesson: close against the user's stated goal in their words, not against my task list.

- **[S78] When one write destroys data and another write is its only record, the ORDER is a correctness property.** The void deleted a payroll row and then wrote the sole surviving copy of it, best-effort, while the confirm dialog promised the record was kept. Two wrap lenses found it independently; re-deriving it instead of accepting the report found the part neither had, that supabase-js reports a failed insert as `{ error }` and never throws, so the error was not read at all and an RLS refusal read as success. The fix is not a bigger try/catch, it is writing the record FIRST and refusing the destruction if the record cannot be written. Of the two ways such a pair can lie, the recoverable lie is the one where the thing still exists on screen.
- **[S76] Hold even an explicitly pre-authorized merge when the landscape has moved, and let an anti-mangle sweep prove the re-sync.** The one-merge bundle absorbed #1061 at a snapshot; that session then merged a NEWER copy to master plus a changed duplicate rule. Re-syncing produced add/add two-way merges (squash vs branch history) — the class that silently mangles — and the proof was one sweep: diff HEAD against master over every absorbed path and assert the ONLY differences are your intended edits. It listed exactly three area props and one comment. The merge fired only after that, and the smoke was re-run because the absorbed rule change had made one of its assertions wrong.
- **[S76] Test a feature by running its REAL life: generated sample photos through the live bucket and data layer caught what no unit test owned.** The capture-to-pay smoke (is_test, full teardown, before/after counts asserted) surfaced the audit-table cleanup gap, proved the money stamps to the cent, and independently hit the same test-data blind spot another session found hours earlier — two sessions converging on one design gap is the S38 convergence signal at system scale.
- **[S76] When a lens contradicts the DESIGN you suggested, the fix can be a synthesis, not a retreat.** My dup-noise collapse hid the one fraud shape carrying only the worker-day reason; the admin lens wanted the photos back. The toggle keeps both properties: noise collapsed, evidence one tap away — and the split function's test pins that nothing is ever dropped.
- **[S75] A live-database proof caught what 9,050 green tests could not: the feature was dead on arrival, twice.** The technical lens stood up a disposable postgres:16 container and reproduced both HIGHs empirically: every ON CONFLICT upsert targeted a PARTIAL unique index (42P10), so the call sync could never insert a row and the commitment producer could never create a task. Third repo hit of this exact class. The fix round and both later rounds then had to prove themselves in the same container before merging, and the backfill ran clean on the first try because of it. For schema work, the container run is the only gate that catches this class.
- **[S75] Drive the REAL route handlers in-process for a prod backfill.** The backfill invoked the actual cron route GET handlers with a synthetic request (env set locally, DNS pinned per-process), so the exact production code path processed all 79 calls. Zero drift between what was tested and what runs hourly, and the first automated tick had already been rehearsed end to end.
- **[S75] One measured query killed a whole build request.** Naldo asked to connect HighLevel accounts to Cool Tool accounts; the query showed email matching already worked 100% for every named rep, and the unmatched 6 calls were inbound ring-all calls GHL itself never attributed. The fix was a product decision (shared pool, his option 2), not code. Same family as S67's schema-shape-is-not-capability, pointed the other way: the capability already existed.
- **[S75] DNS poisoning diagnosed by comparing resolvers, then routed around with zero system changes.** Router DNS returned an ISP filter IP for exactly one host while public resolvers returned Cloudflare; curl --resolve and a per-process dns.lookup pin fixed probe and backfill without touching the hosts file or the machine.
- **[S74] When an instrument and a count disagree, pull the SERVER log before touching code.** The geocoding fix-list said "nothing needs fixing"; prod counted 25 unschedulable properties; one Supabase API log line (a 400 on the exact request) turned a vague "page looks wrong" into a one-column fix. The page had been dead since it shipped, behind three green gates, because its error path returned [] and rendered the all-clear. The fix included making the failure VISIBLE (an error card), not just correcting the column.
- **[S74] One real day of data beat any amount of further building: assign one real job on the surface that already exists, then just watch.** Job #1046 on the existing schedule page armed the whole GPS pipeline with zero new code, and the tracker recorded the full day unattended: two visits at one job (the double-back captured separately, as designed), depot bookends, 47-minute and 4.4-hour durations, the first real inputs rows 431/432 have been waiting on. The urge was to build scheduling first; the measurement needed none of it.
- **[S74] The lying-instrument class, through a NEW door: every automated browser probe ran in a HIDDEN tab, where React defers island hydration indefinitely, so "Loading map" forever was indistinguishable from broken code in dev AND in a production build.** An hour of ghost-chasing (interop rewrites, cache clears, full rebuilds) ended with one line: document.visibilityState === 'hidden'. The code had probably worked the whole time. Before debugging a page a probe says is broken, prove the probe can see a working page.
- **[S56] Measure before designing, and let the measurement kill the plan — it decided the shape three times in one migration.** (a) The pricing engine **cannot reproduce the charged tax on 8 of 14 Homeworks invoices** (per-line rounding, two rates on one invoice, one rate absent from the document), which killed "recompute from a rate" before a line of code existed and made "store the charged figures verbatim" the founding rule. (b) Card-on-file vaulting turned out to be **live and populated — 18 of 22 paid quotes carry a Valor vault token** — collapsing "build recurring billing" into "build a schedule", and the pay-link path (the card vaults as a side effect of the first payment) retired the vault-transfer question entirely. (c) **Zero prod quotes had ever had deposit = total**, so the dev's "what happens to a paid-in-full customer who still owes an install" was genuinely unprecedented — and the completion route already handled it by name. None of these three was knowable by reading the code; each needed a count.
- **[S56] Write the gate BEFORE the write, assert the invariant you believe, and expect it to fail — every gate I wrote failed first, on something real.** The invoice parser asserted the three Homeworks totals and caught itself reading the **"due" column as the total** (a plausible table of wrong numbers). The line-item gate caught **four invoices running onto a second page**, then **a continuation page carrying no invoice number and $3,600 of items**. The record-set gate caught **one customer's figures being the pre-export ones** because a payment landed the same day. Not one of those was visible by reading the output; each was caught by an assertion about a number I already knew.
- **[S56] Run the standing review even when the dev has already said merge — and HOLD the merge to show them what it found.** Jason gave a merge-go on a money PR; the four-lens round then returned three HIGHs, and **none was in the code** — they were in the data I had already written to prod: a homepage KPI turned negative by my own backdating, a portal about to tell a customer who paid in full that we held a 50% deposit, and 11 quotes still flagged as parked inventory. His "merge" predated the findings, so it was not consent to them. Surfacing them and re-asking cost one message and changed what he was agreeing to.
- **[S73] Build the reviewer, then point it at your own work the same day: it found real defects in BOTH pull requests it has ever seen, and one was invisible behind three green gates.** On #1039 it caught that the staff-device property read `document.cookie` for a cookie set `httpOnly`, so it could never fire in a real browser; the tests passed only because they stubbed a state the platform forbids. The whole feature was dead on arrival with tsc, lint and 8400+ tests green. On #1044 it caught a coaching reply that broke the bot's group contract AND diagnosed the deeper cause better than my own fix (a broad prefix regex where the sibling matcher anchors the whole string). The transferable part is not "use a bot": it is that a reader who did not write the code asks "does this actually fire?", and that question is the one an author structurally cannot ask themselves.
- **[S73] When a review reasons its way to a privacy finding, turn it into a NUMBER before deciding anything — the number changed both the severity and the owner.** The customer lens argued from code that dead-click capture writes element text outside replay masking. One prod query: 1004 events in 180 days carry a `$` in `$el_text`, samples are real quote figures, and the bulk is `$autocapture` which has been on for months, while this session's change added SEVEN. So it was pre-existing and wider than reported, not newly introduced, which turned a "revert your change" reflex into a measured ledger row and a decision that belongs to Naldo. Same family as the S47 sweep-the-domain lesson, applied to a privacy claim.
- **[S68] A zero reading proves nothing until you prove the probe can read non-zero - run the CONTROL before reporting an interaction bug fixed.** The keystroke-flash check produced zero canvas remounts, which is exactly what a broken observer also produces. Deliberately triggering a real remount (switching the sample home) made the counters fire, which is the only reason the zero means anything. The same move proved the canonical test: removing the fix fails 3 of its 4 assertions.
- **[S68] Land a close PR fast, because one that sits open across other sessions' merges renumbers every time.** S68's findings were minted three times (404-406, 409-411, 415-417) and the PR was still closed unmerged with nothing landing. Every renumber was caught at the pre-merge re-check, so the rule worked, but the cheaper fix is to not leave the window open.
- **[S54] On a money PR, each of the three review passes caught what the previous one STRUCTURALLY could not — budget all three, do not economise.** Row 388: the four-lens round's admin lens found the HIGH (a resync rewriting invoice money with no audit trail, able to silently reopen a PAID invoice). The fix round answered it — and the DELTA-VERIFY then caught the fix was HALF-WIRED: the route computed `audited: false` on three failure paths **and unit-tested it**, but no consumer rendered it, so staff saw an identical "Resynced" whether the trail landed or vanished (a returned field nothing reads — the inert-fix class, killing the fix's own promise on its failure path). Then the technical lens and that same delta-verify CONVERGED on a residual TOCTOU: the paid guard tested a read from the top of the request while the resync re-reads later, so an invoice settling in the gap could still be reopened — the row-356 lesson (gate at the STATE CHANGE, not a prior read) recurring one file away, in the same session that shipped it.
- **[S54] A completeness ASSERT stopped my own wrong number twice in one session, before a single byte was written.** The shipped-row script asserted 10 stamps and found 11 (row 335 already carried one I had added hours earlier); the archive script's anchor had the emoji and bold markers in the wrong order. Both failed loudly and cost nothing, because the encode-first → temp → `os.replace` pattern means a wrong assumption never reaches the file. The habit that makes this free: assert the INVARIANT you believe (a count, an ordering, a presence in exactly one place), not just that the write succeeded.
- **[S53] Paste the number from the instrument; never re-type it — and read a just-pushed head from local git, not the API.** Two self-caught instances in one session: a commit message claimed 7794 tests when the measured run printed 7791 (third occurrence of the class), and a CI poll watched the WRONG SHA for seven minutes because `gh pr view` was read immediately after a push and returned the pre-push head — the passing run was on the true head all along. `git rev-parse` after a push cannot be stale; the API can.
- **[S53] The write-findings-early rule promoted in #971 paid the SAME SESSION it shipped.** The wrap's own two lens agents were killed mid-run by the usage limit — a NEW door into the S50/S51 process-exit class (user quota, not process death) — and the early-created findings files are what made the cut legible and the respawn cheap.
- **[S53] A dev device round is a review lens with its own findings.** Jason's checks confirmed every shipped surface same-day AND found a real bug none of nine lens agents saw: the quotes list displays #1262 but search only matched the UUID. Fixed same-day (#977, sibling parity with jobs/invoices). His two improvement notes became rows 418/419 — treat device feedback as ledger input, not chat.
- **[S51] When a fix targets a CLASS of defect, check whether the fix reintroduces that class — three times in one session it did, and lenses caught all three, not me.** Row 405's guard read a baseline that is seeded LAZILY (first line edit, or Recount), so on a reopened untouched quote it was EMPTY and the guard was INERT for the exact scenario the row describes. Row 362's label helper took an optional scheme list NO caller passed, so permanent quotes resolved against the holiday list, and `getColorScheme` silently degrades an unknown id to "Staff's pick" — a CONFIDENTLY WRONG colour on the crew's build screen, which is the Kristie failure reintroduced for a different segment. Row 406's own fix round added a sticky banner whose containing block is the whole ~8000-line component, pinning it over the Send Quote button. Identical pattern each time: correct about the mechanism, wrong about a precondition, and only an outside reader asking "does this actually fire?" found it.
- **[S51] Fix by DELETING the trap, not by patching the call sites.** Row 362's real fix was not "pass the scheme list at three call sites" — it was making callers pass a SERVICE TYPE, resolving the list internally from `app_settings` (the same source the approve and apply-colour routes use, so staff-customised swatches work too), and DELETING the convenience wrapper that made the wrong call easy. An optional correctness-critical parameter is a trap whether or not today's callers happen to get it right. Same shape on row 405: moving the confirm PRE-FLIGHT killed four findings at once — a portal photo/lines mismatch and two admin findings, one of which would have fed one house's photo paired with another house's geometry into the AI training corpus — because a decline then changes nothing at all, instead of patching each downstream consequence one by one.
- **[S51] Measure the blast radius instead of arguing about severity — one query ended it.** A lens called the colour bug a HIGH; rather than reason about likelihood I queried prod: of 22 live non-test approved quotes carrying a colour, exactly ONE renders wrong — booked permanent quote #1303, approved "Orange". One is enough, because that is a real house whose crew would read "Staff's pick". The number also sized the fix honestly in the PR body instead of hand-waving "could affect permanent quotes".
- **[S51] A hostile browser check is worth more than a passing one — mine tried to lie twice, and both lies looked exactly like MY code failing.** Synthetic DOM events never reached React, so the row-406 guard looked broken; I was one step from rewriting a working fix, and what saved it was noticing the FORM STATE had not moved either — which meant the instrument was broken, not the code (the promoted check-the-check pitfall, third occurrence). Then a `location.reload()` silently did nothing and I read the unchanged DOM as failure: that was `beforeunload` cancelling the navigation, i.e. the feature working. Real clicks then verified all five legs, including a genuine leave-site dialog.
- **[S50] When a fix "addresses" a finding, check whether it actually FIXES it - the highest-value catch of the session was a fix that MOVED a problem instead of solving it.** Row 395's complaint was an amber caution firing on 100% of settled invoices; the fix relocated it to the amend panel, which renders unconditionally - so the noise moved to a screen staff open MORE often, with the firing condition untouched. I then tried to rescue it with "the new site has a smaller population" and MEASURED prod instead of arguing: 4 invoices, all 4 fire, 0 with an outstanding balance. My own counter-argument died on the number. Relocating is not gating.
- **[S50] When two agents contradict each other, COMPUTE - do not average, and do not pick the more confident one.** One said clearing a baseline ref "doesn't change today's behaviour"; the other had reproduced a bug and a fix at the pure-function level. Both were right about DIFFERENT branches: the clear is inert for the pre-existing redraw-wins rule, and load-bearing for the new suppression branch. Same lesson as S58's paid/break arithmetic, and here it decided whether a money PR could merge.
- **[S50] Hold a merge when the module has a live undiagnosed defect, even against a standing merge-go.** Jason had given "merge anything reviewed and green", and #938 was reviewed and green - but it changes permanent-side derive behaviour, and he had just confirmed a live money defect on that exact surface. Held it, said why, merged once an investigation cleared it on evidence. A standing authorisation covers the routine case; it does not transfer judgement about a case the dev has not seen.
- **[S67] A schema's SHAPE is not a capability — measure the population before you design on top of it, and before you tell the dev it exists.** I reported "the quote tool already stores geocoded lat/lng per property" as good news; the columns were real and **0 of 211 rows were populated**, so the feature I was leaning on did not exist. One `count(*)` corrected it a message later and turned a two-line assumption into phase 1 of the plan. The same query pass also found `job_segments` at **0** ever written, which reframed the whole build from "add GPS to the working crew-tracking flow" to "this is the first real use of it" — a materially different risk profile that no amount of reading the schema would have surfaced. Same family as the S47 sweep-the-domain lesson, one layer lower: not "is the row's premise true in the world" but "does this column have anything in it".
- **[S67] On a docs conflict, diff the conflicting LINE character-by-character before choosing a side — "mine is a superset" is a hypothesis.** The `task_ledger.md` counter line conflicted; my side looked like master's text with my prefix added, and taking HEAD would have been the obvious move. Diffing first showed master had **appended** a whole 367-377 note at the END of the same line while I changed the beginning, so either side taken wholesale silently drops the other's edit. Resolved as master's line WITH my prefix applied, then asserted master's tail text survived. This is the S61 archival-move lesson generalised: the danger in a docs conflict is never the half you can see, it is the half git is quietly offering to discard.
- **[S49] Verify a BUILDER's claims against the code, exactly as you would a reviewer's — the highest-value catch of the post-close run came from reading a diff I had already been told was fine.** A builder's write-time brightness clamp mapped every per-photo entry through a function returning 50 for non-numerics. Correct for the scene-level field, wrong per photo, where the lookup is `?? baseBrightness` — so a null entry INHERITS the scene brightness and coercing it to 50 would have silently changed what a homeowner sees on their portal, introduced by a guard built to prevent that exact class. The builder's report was accurate about what it did and silent about what that meant. Same session, the same habit settled three other claims: row 191's "already removed" (grep-zero, true), row 393's "master supersedes it" (grep-zero, true), and row 379's guard worry (refuted by a lens tracing every call site). Read the diff, not the summary.
- **[S49] A mutation probe is the only thing that proves a REGRESSION TEST is real — and mine wasn't.** After the delta-verify caught a never-expiring cooldown, I wrote a test for it, watched it pass, and would have shipped it: three POSTs complete inside the same millisecond, so the timestamps the route generated compared EQUAL and the test passed happily against the reintroduced bug. Reverting the fix and watching the test NOT fail is what exposed it; rewritten against a seeded 30-minutes-ago anchor it fails exactly once. The promoted "a guard nobody has watched fail is a hypothesis" rule applies to the test you write to prove the guard, not just to the guard — and time-derived assertions are the flakiest form of it, because sub-millisecond execution silently makes two different values equal.
- **[S49] Measure the population BEFORE building, and let the number pick the design — it changed three separate decisions in one session.** (a) Running row 378's real functions over prod first (4 invoices, all paid, 0 payable, 0 drift) is what made a HARD customer-facing refusal defensible: a false positive there means a customer who wants to pay us cannot, and the number showed the blast radius was zero today with 22 booked jobs arriving this season — so pre-season was the cheapest window the guard will ever get. (b) A review flagged an unquantified deploy-day surge; measuring it returned **0** and retired the finding on a fact rather than a marker. (c) An orphaned worktree held a real unmerged staff-HIGH fix — 20 exposed rows, but **0 still open**, so it was dormant and correctly did not jump the queue. Same family as S47's sweep-the-domain lesson, applied three times in one day.
- **[S48] The adversarial delta-verify on every fix round is the highest-yield step in this process — 13 of the session's 27 pre-merge catches were defects the FIX ROUNDS introduced.** Headline: #927's retry produced a **~$500 double-charge** (`newTotal − depositPaid` where depositPaid is the DEPOSIT ONLY, so a settled balance payment was subtracted nowhere), the `charge-balance` guard added in the same commit used the identical formula so it validated the wrong number against itself, and a shipped test asserted `900` as correct. The author didn't catch it; I didn't catch it; the delta-verify did. Others in the same session: an ambiguous-null stock state that reopened the very bug it fixed through an ordinary transient write failure, a permanently stuck "Refreshing…" from a no-op date change, a pill that stopped firing for booked orders because two commits an hour apart drifted, and a pre-check that silently skipped its own fix. Budget it into every money/identity fix round.
- **[S48] When a shared contract CHANGES SHAPE, "I already audited this consumer" is not evidence about the new contract.** #928's builder diagnosed its own miss precisely: in round 1 it examined both `ReplyComposer` call sites under a two-state boolean `onSent`, where `InboxList`'s unconditional removal was genuinely correct; in round 2 it widened the contract to three states, re-derived one consumer from scratch, and CARRIED FORWARD the old conclusion for the other. The sentence was true of the shape it had just replaced and went silently false the moment `'error'` became a distinct state. Re-derive every consumer against the NEW contract; a prior audit is about the old one.
- **[S48] Verify a money reading against the PERSISTED data before escalating it.** A device-check grouping read **6** where the rule says 4 — a live over-billing bug on a pricing path, one message away from being reported. Querying the scene showed both members were actually `3` (an earlier edit had applied to both), so 3+3=6 was correct. The screen was right and my model of the inputs was wrong. Same family as the S47 sweep-the-domain lesson, at device-check scale.
- **[S48] A failed command returns NO information, not good news.** Four transient DNS failures in one session silently no-op'd a `fetch`, a `pull` and four branch deletions — and one of them left the gates running on a STALE tree, printing a green 7407-test result I nearly read as confirming freshness. The test count only moved (7425) once the fetch actually succeeded. Check the outcome of any git/gh command whose success you are about to depend on, especially one whose failure is invisible in the next command's output.
- **[S47] Answer an owner's "how bad is it" question with a NUMBER, not a marker.** An admin lens flagged that historical unattributed writes were forensically indistinguishable and proposed a backfill flag; it had been correctly blocked from querying prod itself. Running it took one query: `dashboard_activity` holds 1396 'system' + 858 real ids + **exactly 1 NULL** - one row, already identified. A measured exposure closes the question; a marker defers it forever.
- **[S47] When a row's premise is a claim about the REAL WORLD, sweep the domain before building on it - and be willing to hold a finished build.** Row 334 said grouping seeds the billed count from `members[0]`, so "sum them" looked obviously right; a builder built it and four lenses passed it. One prod query then showed 275 of ~327 grouped members (84%) sit at the untouched default of 1, because staff trace N segments and type the TRUE count on the GROUP - so summing would have seeded 176 where staff typed 16. The fix was not wrong in code, it was wrong about the world, and NO amount of code review would have caught it.
- **[S66] Re-verify the session number at WRAP and on every master re-sync, not only at start — a compaction summary's number is a hypothesis, and a collision can be born AFTER you start.** The summary said S64; two concurrent sessions already held S64 (open close PR #876, eight draft PRs) and S65 (a MERGED close #904, the light-size slider) — the S65 one landed on master during my own re-sync. Caught both before stamping a single artifact by reading `gh pr list` + the fragments, and yielded to S66. The promoted "one conversation = one session" collision arriving through a compaction resume: two sessions that both read the same prior close both pick the same next number, and a merged claim can appear mid-wrap, so the check has to run again at close AND after every fetch.
- **[S66] Diagnose a CI outage by the run-history DURATION pattern, and give the dev the actual fix.** A repo-wide Actions failure was free-plan minutes/capacity exhaustion, not a transient blip: runs 846-850 passed in minutes, 851+ died in ~5s with runner_id 0. Reading completion duration (not the transient queued/in_progress states, which I twice misread as recovery) settled it and produced the real fix — raise the org Actions spending limit — plus a ledger row for the durable ci.yml savings (concurrency-cancel + docs-only skip, row 355).
- **[S65] Separate a presentation knob from a money number as STRUCTURE, then prove the separation by grep, not by comment.** The light-size fix could have ridden `pxPerFoot` (which staff were already dragging) — but `pxPerFoot` also sets bulb spacing and divides strand length into billed footage, so scaling it corrupts the quote. Giving the visual multiplier its own `scene.lightScale` field that never enters a priced expression made the money-safety claim verifiable: a repo-wide grep for the symbol returns only render arguments and slider UI, zero hits in `projectScene.ts`/`materialsProjection.ts`/pricing. All four wrap lenses independently re-confirmed the isolation the same way. When a visual tweak is tempting to bolt onto a money variable, the cheaper-looking path is the trap.
- **[S65] Negative-control every new guard AT WRITE TIME, and the multiply-after-floor decision is exactly the kind that needs it.** Both floor guards (bulbs, spritzers) were verified by moving the `* scale` inside the `Math.max(floor, …)` and watching exactly one test fail — the "multiplies AFTER the floor, so the slider actually moves them" test, which is the whole feature on a whole-house photo (a wide shot pins to the floor, so scaling before it does nothing). A guard nobody has watched fail is a hypothesis.
- **[S62] A four-lens adversarial round on a RULE change earns its keep exactly as much as one on code.** The second round on my own AGENTS.md draft found 4 HIGHs in my wording — including a gate that would have deadlocked Naldo's own nightly work (it swept the daily journal into a blocking review) and a trigger verb ("editing") that didn't cover creation, so the rule missed the very PR that motivated it. Rules are executable process; review them adversarially, and re-derive each finding against the LIVE text before accepting (that is how all 3 wrap-review HIGHs were confirmed real rather than taken on faith).
- **[S61] Record a number/name collision in the shared doc instead of silently taking the number — it is what makes the NEXT collision legible.** The ledger-301 clash looked two-way when it was written down, turned out three-way, then four across seventeen hours. Because the counter carried the history, each new claimant could be adjudicated in one edit instead of surfacing as duplicate rows after merge. Corollary learned the hard way: a number cemented in the SOURCE of an UNMERGED branch does NOT outrank a merged row — I reasoned the opposite at first, and it was wrong.
- **[S61] Verify a payroll claim by sweeping the WHOLE domain against the real algorithm, not by trusting the PR body or one spot-check.** The DST fix was re-derived by running old and new `etMidnightAfter` over all 365 days of 2026 against an independently computed ground truth: old wrong on exactly 2 days (Mar 8 an hour early = underpay, Nov 1 an hour late = overpay), new correct on all 365, converging in 2 iterations against a cap of 4 — which also answered "can it oscillate and exit wrong?" without hand-waving. A wrap lens re-derived it independently and agreed. Cheap, total, and it turns a claim into a fact.
- **[S61] Negative-control every guard you add: remove it and watch the RIGHT test fail, alone.** Done twice this session (the DELETE crew guard, the telegram unique-violation handler). Both times exactly one test failed and it was the intended one. A test that has never been seen to fail is a hypothesis about a guard, not evidence of one — this is the S58 lesson applied at write time instead of at review time.
- **[S61] An untriaged lint warning can be a dead FEATURE, not a style nit.** `'CrewLogins' is defined but never used` sat among 31 accumulated warnings while it was, in fact, reporting that an entire admin panel had no reachable surface — S58's crew-login feature shipped dead and nobody noticed for a session. Fixing it moved the count 20 → 19, which is also how the cause was confirmed rather than assumed. Read the warning list when it grows; it is a defect queue.
- **[S43] Encode a review's HIGH as a BUILD-TIME hard constraint in the ledger row, before any code exists.** A customer lens proved the draft terminal-quote rule would hide a live unanswered colour request; writing "NEVER auto-complete a row holding an unanswered inbound" into row 317 as a POSITIVE structural exclusion (eligibility = `status='handled'`, so needs_reply is ineligible by construction) is why the live post-deploy check passed with the real customer's request still on screen. A constraint in the row survives the builder, the fix round, and the merge; a constraint in a chat message does not.
- **[S43] Gate a destructive prod op on MEASURING that the leak is actually fixed first.** The 1.13M-row audit purge was only run after the post-fix write rate was measured (584 rows in the 2h before the deploy → 3 in the 20min after, later 0 in 4h). Deleting first and hoping the fix held would have re-accumulated silently; the measurement is also what made the recommendation honest rather than confident.
- **[S43] Apply a delta-verify's merge-conflict instruction verbatim, and let the gates be the proof.** `git merge-tree` warned that a naive resolution would silently revert an earlier PR's prod-verified fix; resolving keep-both exactly as instructed preserved it, and the wrap technical lens later re-verified every symbol survived at the final SHA. Related: a `head -5` on the marker sweep hid two more conflicted files — git's own unmerged flag caught it, so sweep with an unbounded grep AND check `git status` for U flags.
- **[S42] The adversarial delta-verify on EVERY fix round is now empirically load-bearing: in one session it caught a fix-introduced HIGH in EVERY feature it ran on** (consent-overwrite via amend-consent's pending-check, the second-CAS-write silent loss, the stale-basis reconstruction, the retry-CAS two-failure data loss, the bare-void `after()` teardown inertness, the deposit-clamp approval-freeze bypass). Budget it into every fix round; skipping it is exactly how these would have shipped.
- **[S42] Mutation-probe a guard test before trusting it** — revert the guarded line, watch the test fail, restore. Three probes this session; two exposed fixtures that couldn't reach the guard at all (a 409 fired first; a taxAmount:0 fixture made the two money bases trivially agree).
- **[S42] When two surfaces must agree on money, make them agree BY CONSTRUCTION** — one basis, computed once, stamped in the same atomic write, read by every surface. Three rounds of "make the displays match" kept failing until the reconstruction was deleted outright.
- **[S41] The dev's own device check is a review lens.** Jason's offline test on /inbox found the silent thrown-fetch catch that FOUR lens agents had read past in the same file (-> #806); his "am I doing something wrong?" was the bug report. Hand the dev a concrete adversarial test and treat a confused result as a finding, never as user error.
- **[S41] Measure the FIRING RATE, not the population, before accepting a magnitude claim.** Three lenses converged that #796 "kills write-backs for ~95% of Gmail traffic" -- population TRUE (470/474 null message ids), magnitude FALSE (the retired call fired ~2x/month because gmail rows resolve by dismiss/complete, which never reach it). One prod aggregate settled the disposition and kept the simpler design.
- **[S41] The write-time ledger-number re-check dodged a FIFTH collision, and merged-source anchoring resolved it PRE-collision for the first time** -- the counter said 299 free at draft time, Naldo took 299-301 overnight, but #806/#814's code comments had already cemented 302/307, so the remap was mechanical.
- **[S58] When a review finds a bug in code you shipped, re-derive it yourself before accepting — and before fixing.** The DST payout finding was real, and I confirmed it by running the real function against both transition days rather than trusting the report. A previous lens in the same session was WRONG about a test being sound, so neither trust nor distrust is the answer: compute.
- **[S58] A near-miss is worth as much as a catch, if you write down why.** Copying the sibling project's "secret must be 16+ chars" rule into a shared cron guard would have 503'd all eleven live crons at once if the real prod secret were shorter. Twenty-two failing tests caught it. The reasoning is now in the helper so nobody re-adds it.
- **[S40] Run the reachability probe at BRIEF time for any gate/coupling whose value depends on a live population** — #793's coupling was proven dead code by ONE prod SELECT paired with an independent code-trace lens converging on the same verdict; pre-merge this time, but the same query at brief time would have saved the whole build.
- **[S40] Delta-verify-every-fix-round caught a fix-introduced HIGH for the third session running** (#787 round 2's reopen starvation) — and pulling the CI log for the exact final SHA turned the gate claims from reports into facts.
- **[S39] The worker-refutation loop closed FIVE stale ledger rows in one batch** — every "build row N" brief said distrust-the-row, and five builders came back with shipped-already evidence chains instead of duplicate code. A truthful "already done" is a fully successful build. Also: when master moved 37 commits mid-merge-go, re-running the combined gate + REBUILDING the conflicted 2-line docs branch on fresh master beat hand-resolving (zero conflict-resolution risk).
- **[S38] When two lenses converge on the same finding from different questions, treat it as a CLASS immediately** — the PipelineActionsMenu send-honesty gap was found independently by the customer lens (MED) and staff lens (HIGH); convergence, not severity, is the signal (same lesson as S30's read-before-write convergence).
- **[S38] A worker-fix-loop beats seat redos at scale: six briefs refuted by builders with evidence, six correct refusals/corrections, zero seat rewrites.** The brief's job is to be refutable: name premises as premises and reward the refutation.
- **[S38] Assert-the-count BEFORE any doc write** — two atomic-script asserts (17≠18 rows; a stale marker) each stopped a wrong ledger surgery with zero bytes touched. The pattern: encode payload → assert invariants → temp → os.replace.
- **[S58] When two review lenses disagree, settle it with arithmetic, not by picking the more confident one.** The technical lens called the paid/break identity test sound; the crew lens called the identity false. Re-deriving it by hand (10,700 ms envelope, 400 ms break -> paid 11 + break 1 = 12 against an 11-second envelope) settled it in one minute and the crew lens was right. A disagreement between reviewers is a signal to compute, not to average.
- **[S58] Verify a guardrail FIRES before claiming it guards anything.** Wrote a throwaway probe file that violated the new lint rule, watched the gate fail, deleted the probe. Then the wrap review found the rule still missed bracket notation - so the same probe technique, applied more thoroughly the second time, is what proved the widened rule actually covers all five forms. A rule nobody has seen fail is a hypothesis.
- **[S58] Check unmerged branches before claiming a ledger number, not just the counter on master.** Three near-collisions in one session: the counter was mid-flight inside an open PR, a concurrent session then claimed a 17-row block, and an unmerged branch already held the next two numbers while master's counter still offered them. Declining to claim a number when the counter was in flight is the only reason this session did not add a fourth collision to the pile.
- **[S35] The wrap review earns its keep when a lens asks a DIFFERENT question, not when more eyes ask the same one.** Three lenses (technical/money, staff, admin) all traced the #199 deposit-rate code and returned CONFIRMED-SAFE; the customer lens found TWO HIGHs in that same code — because it asked "what happens when NCE turns OFF?" while the others asked "is the ON path wired correctly?". Both HIGHs shared one root cause (nothing treats false as a reset signal). When briefing lenses, give each a genuinely different question, not a different file list.
- **[S35] Reward reviewers and builders who refute the brief — it happened FOUR times in one session and every one was right.** #202's delta-verify caught that my diff range was contaminated by a master merge and corrected it (then proved the corrected range arithmetically); #203's reviewer refuted my type-drift worry by compiling isolated repros; #215's builder proved the admin siblings do NOT gate their propagation caveat the way my brief claimed; #717's reviewer settled a disputed mechanism by reading Konva's actual `Stage.js` instead of reasoning about it. Every brief says distrust-the-brief; this is what it buys.
- **[S35] A device check is the only thing that closes a canvas/pointer leg — and it can overturn a HIGH.** The wrap staff lens raised a HIGH claiming #203's fix was inert whenever something was selected. Refuted twice: by a library-source read (Konva's destroy-and-rebuild breaks its own click-identity check, so no click fires) and by Jason's real browser. Surfaced the contradiction to him rather than picking a side or burying it.
- **[S55] When a user reports a broken link, grep the literal string from their screenshot first — the answer was one grep away and already documented.** "Open the inbox" found the exact builder in one call, and the same grep surfaced the July audit row (W7-026) that had already diagnosed it and gone unticketed. Reading the report's own words as a search key beats reasoning about where the code probably lives.
- **[S55] Stop and re-check the client/server boundary before importing an env-reading helper into a lib module.** The obvious 2-line fix (import `appBaseUrl()` into `notify.ts`) would have put a `process.env` read into a module three `'use client'` components import. Switching to a passed-in `baseUrl` cost one extra signature change and removed the question entirely — the S18 lesson applied BEFORE the bug instead of after.
- **[S53] Before declaring a data source unavailable/unconfirmed, enumerate the org's sibling Supabase projects.** The #168 plan sat 3 weeks on "confirm GHL transcription exists" while 1,210 transcripts sat in the `yll-call-copilot` project — one `list_projects` call away. Same family as the S34 second-channel lesson: the answer often lives in the system ADJACENT to the one you're staring at.
- **[S34] The wrap review found the seam BETWEEN six individually-reviewed PRs.** Every PR took a four-lens round at merge; the wrap's combined-tree pass still surfaced 3 HIGHs sharing one root cause (identity-resolution callers bypassing the #213 gate) that NO per-PR review could see, because each PR's reviewers scoped to that PR's diff. The standing wrap-review order pays for itself precisely here.
- **[S34] Delta-verify every fix round on a money/identity seam — twice it caught HIGHs the fixes themselves introduced.** #213's round-1 fixes introduced 2 new HIGHs (the coordinator's own rule-4 design reopened the household merge rich-into-sparse; the hl-folded dup key split a returning customer); a second adversarial pass caught both pre-merge. Fixes introduce bugs; verify the delta, not just the original findings.
- **[S34] Answer a "works for staff, fails for customer" payment mystery from the SECOND channel.** Jane's hourglass reproduced on Valor's own emailed invoice link — a path with zero of our code — which localized the fault to her environment × Valor's hosted page in one observation, before any code was read. Find the leg that bypasses your system entirely; it halves the search space instantly.
- **[S52] Run the device-check's stated adversarial test literally, don't eyeball the UI and call it fine.** The handoff named the exact test (draw a gutterline, type an Override, click Auto-Analyze, confirm nothing else moves) — running it exactly as specified failed on the first try, surfacing a real HIGH (Auto-Analyze silently zeroing an archive trace) that a casual "does it look right" pass would have missed entirely. The specificity of the test is what made it useful.
- **[S52] A "master moved" check is not one-time — re-verify immediately before every push, not just the first fetch of the session.** Hit this 3 times in one wrap: before #672's push, before #673's push, and again mid-review when the wrap branch itself went 2 commits stale from concurrent activity on the same continuity docs. Re-fetched and re-merged each time rather than trusting an earlier check still held.
- **[S47] A whole-session "review ALL your code" adversarial pass over the FINAL, merged state earns its keep — it catches what per-PR reviews + the build miss.** After captureLead + the digest hardening each got a 4-lens at merge, a 5-agent pass across the entire session's code still surfaced 6 real MEDIUMs: a consent gate a stray group "ok" could satisfy (an SMS-compliance hole), a FIX-INTRODUCED household address-clobber, a silently-lost mid-confirm photo, and the digest's silent-crash + 500-row count-truncation. A per-PR review sees one diff; the final-state pass sees composition + fix-regressions. Run it before a batch of related PRs merges, and re-check any code a fix touched (fixes introduce bugs).
- **[S51] Recon that kills a plan premise BEFORE code is the cheapest review there is.** The slice-3 plan sold "the existing few-shot retrieval picks it up automatically — no change to inference code" as a benefit; reading the retrieval showed it was the RISK (overhead-satellite exemplars served to ground-level customer photos as imitate-this ground truth + 80 rows flooding a recency pool). One grep of the consumer changed the design (source column + positive filter) with Naldo's sign-off before any build. Grep the consumer before believing a plan's "automatic".
- **[S47] Answer a "why didn't X happen?" prod question with the LOG + a direct query, never a guess.** "No morning digest today" got a real answer in one look: the Vercel cron log showed `11:30:50 GET /api/ops/digest 200` with no errors, and a direct prod query showed an empty board (0 installs/drafts/deposits), proving the by-design quiet-day null-send, not a failure. I'd earlier guessed CRON_SECRET timing and been wrong; evidence settled it and drove the right fix (the always-send heartbeat).
- **[S32] Evidence-first debugging beat every guess this session.** The dead #157 exclusion was "working" per code+tests for 10 days — an instrumented replay of the EXACT runtime call against prod data exposed the swallowed 22P02 in one run. The #175 build started by PROVING Valor webhooks declines (Vercel logs, 60s latency) before writing code. The 60s-page mystery fell to a 5-angle measurement workflow that RULED OUT DB/payload/region/limits with live numbers, leaving the auth gate as the only structural difference. Reproduce → instrument → then build.
- **[S32] The wrap/lens pipeline scales: 13 PRs in one session, every finding back to a worker with specific corrections.** Builders repeatedly caught wrong claims in MY briefs (swapped line labels, a wrong "same pattern" premise, a nonexistent column claim) because every brief says distrust-the-brief; honest builder reports (self-flagged residuals, deviations explained) made dispositions cheap. Reward that shape in briefs.
- **[S30] Read-before-write on any recovery that trusts an id from an error payload.** GHL's dup-400 hands back `meta.existingId` — which can point at an ACTIVE cross-pipeline deal. Fetching the card first and REFUSING open ones turned a silent deal-hijack into a clean 409. Three review lenses independently converged on exactly that guard — convergence like that means it's a class, not a nit.
- **[S29] Trap-first probe ladder for undocumented vendor channels.** Seven $1 tests solved a 5-week blocker because each test answered exactly ONE question via a names-only diagnostic first (log the shape → prove auth → persist guarded → fix the clobber → prove reuse). Never build the wiring before the channel's real shape/signing/behavior is observed — the vendor's dashboard and docs both lied (schema, save_card, VT-only scare).
- **[S46] Ground a greenfield "how would we build X" in the codebase BEFORE designing it.** A grep for the feature turned a from-scratch design into "extend what exists" — the dormant #82 text-bot (webhooks/allowlist/dispatcher/`notifyTelegram`) + the inbox's existing GHL call-channel ingest meant the hard 70% was already built. Recon first; the plan gets smaller and truer.
- **[S45] Answer a "why does this keep happening / is it my fault" question by CLASSIFYING the real incidents from the logs, not asserting.** Pulling the actual classifier-block incidents (S40/S42 transient fail-closed Bash outages · S44 content false-positives · S31/S34 legit policy blocks) turned a vague frustration into a 3-category answer with per-category fixes, and separated the classifier gate from the already-solved S43 MCP-transport drops. The recurring fallback itself is now codified in AGENTS.md Pitfalls (don't re-derive it).
- **[S44] Before deleting a shared UI block, ENUMERATE what lives inside it** -- the "delete the sticky bar" brief would have silently deleted our own lead-form's placement marker, which was nested in the doomed container. Same class as the S19 sceneLinks lesson: when you remove or widen a shared structure, list its occupants/consumers FIRST. Also: trace a site-wide element to its real owner (a template, not the page you were pointed at) before editing.
- **[S31] Third-party UI writes need a POSITIVE persistence check, not a click** -- PostHog insight saves are real only when the URL flips off /insights/new; Elementor code-UPDATEs silently revert (create a new entry). Verify the artifact, then move on.
- Branch off **FRESH `origin/master`**, never the worktree's stale base — first confirm the spec/plan commits are actually present (mine weren't until I rebased).
- Re-ground the exact lines before planning, **then independently verify the recon** — a fast read-agent missed a real data leak and cited a stale file; my own grep caught both.
- TDD + run all three gates (`tsc · lint · vitest`) **after every phase**, not just at the end.
- Run the adversarial review before merge — and **disposition** each finding (fix / accept-as-parity / process). Don't blindly "fix" a pattern that matches existing prod code.
- Verify safety before an irreversible prod op (grepped for bare-anon data paths before enabling RLS; smoke-tested the live app after).
- Surface the high-risk / council-worthy / shared-file calls and confirm outward-facing actions instead of rubber-stamping.
- **[S18] Verify UI in the ACTUAL browser** — tsc-green ≠ correct. A client/server import-boundary break (#52 dragged the server-only `sharp` chain into the client bundle) and a HIGH `/training/new` railing ship-blocker BOTH passed tsc (an `as` cast laundered the type) and surfaced only in-browser + in the adversarial review. **[S19 reinforced 3×]** — Jason's device rounds caught a tag-expansion leak, a garland-trace gap, and a strip-placement miss, all invisible to tsc/tests.
- **[S19] Checkpoint cadence for epics** — slice a big feature into 2-PR chunks → dev device-verifies → merge bottom-up → next chunk. Beat both "one giant PR" and "gate every PR" on #13 (6 PRs, 3 checkpoints, feedback reshaped each next slice; stacks never grew past 2 branches; relays only on approved editor-core).
- **[S19] Scope a big epic with a recon workflow BEFORE the plan** — the 6-agent #13 recon + completeness critic caught the `maybeSingle()` silent-empty-materials trap (killed the N-designs architecture before any code) and 5 wrong claims via cross-verification.
- **[S19] Mechanical prompt/text moves → prove byte-identity** — extracting shared blocks out of the quoting SYSTEM_PROMPT via a substring-move script + an eval-and-compare check made "the prod prompt didn't change" a fact, not a hope.
- **[S20] Model-routing policy pays — Sonnet builds, Opus reviews/orchestrates (the seat), Fable only-if-asked.** The whole #110 fix marathon (~19 PRs) ran Sonnet-build/Opus-review — dramatically cheaper than S19's Opus builders, same quality, because the seat reviews every diff + integrates. Route deliberately, don't default everything to the top tier.
- **[S20] Write a workflow script to a FILE + `node --check` before launching.** A W4 workflow died on a missing `)` in a `.map()`; a W5 workflow was fine. The habit: Write the script, `node --check` (top-level `return` is the only expected "error"), then `Workflow({scriptPath})`. Cheaper than a failed launch.
- **[S20] Audit-workflow shape that worked 6×:** dimension finders (per sub-area × lens) → semantic dedupe → **adversarial verify every finding** (refute-lens for bugs, real-at-this-scale for perf, payoff-lens for refactors) → completeness critic. Pre-load intentional-design context so finders don't "discover" deliberate behavior as bugs. Dedupe against a seeded known-findings ledger so waves don't re-report #80.

- **[S23] For a customer-facing go-live you can't drive live (no secrets/node_modules), an in-process E2E of the REAL modules + a seeded `is_test` prod quote substitutes.** Drove pricing→packages→$2,500-gate on a real house via `tsx` (front $2,480 · sides $2,765 · back $1,925 → $7,797.38, D===total no drift), then seeded a permanent `is_test` quote for the human's live-portal eyeball. Validated P4/P5/P6 without a browser.
- **[S23] The adversarial hunt finds the money bugs the happy path hides.** The 7-dim hunt's HIGH was a tax-override under-bill that only triggers on a PARTIAL approved selection (permanent per-side packages A/B/C make partial the DEFAULT) — invisible to the go-live E2E; two-lens verify (reachability + money) killed 6 false positives; the completeness critic flagged the BOM engine as dead code (= the P7 build). Seed the hunt with confirmed-intentional design so finders don't re-flag it.
- **[S24] Adversarially review YOUR OWN just-built stack before merge — the review's 5 confirmed findings were ALL in MY new #134 filter** (headliner: holiday's empty 'D' recommendation slot filtered out before the loader populated it → the whole #12 staff-recommendation flow + Build-Your-Own would have silently died on every ≥$1k holiday quote). My browser checks had "verified" the feature on quotes that never exercised the interplay. Fixed + regression-tested pre-merge.
- **[S24] Domain-rule Q&A rounds BEFORE the plan (AskUserQuestion with concrete hypotheses; ask for the dev's images).** Jason's "every track cut needs an extension" answer collapsed most of the AI-detection problem into pure geometry (interior vertices of drawn lines) — the analyzer's job shrank to drawing lines + jumps/splitters, and the card works with zero AI. The plan got fundamentally simpler because the questions came first.
- **[S24] Seed AI output as DRAWN EDITABLE GEOMETRY, never as numbers.** The rebuilt permanent analyzer seeds the same satellite channels/strands the operator draws by hand; footage/corners/extension counts all derive from visible lines. That's the trust-model difference from the removed S22 analyzer — and it made the live E2E one click: address → lines → all numbers.
- **[S25] Deterministic geometry beats AI wherever geodata exists.** The Street View pano's camera coords (already fetched, previously discarded) turned satellite orientation from the #1 AI failure mode into pure math — the relabel corrected the model on its FIRST live run. Look for discarded metadata before reaching for prompts or training.
- **[S25] Reconcile the engine against the dev's REAL spreadsheet on identical inputs, and itemize every cent before re-anchoring goldens.** The #144 pass found 5 rule gaps (incl. ordering T-taps with ZERO wire); both golden-job deltas decomposed exactly (+$104.55/+$84.40), so the re-anchor was evidence, not fudge.
- **[S25] The dev's device round IS the domain spec.** The written S24 "road-viewer's left" rule was simply wrong; Jason's real-quote check set the true convention (homeowner-left). Written rules are hypotheses — supersede them on operator evidence, and mark the supersession in the notes.

- **[S50] Prove a break predates your change before accepting blame, then keep auditing.** Naldo's About and Blog pages looked broken right after my banner shipped. The page's last-modified date (10 weeks earlier) plus reproducing the break with the banner bypassed settled it in two checks: an Elementor 7.0.2 casualty, not my snippet. The same sweep then found a REAL 40-page header break nobody had reported. Answer "did I break this" with evidence, then keep looking.

- **[S50] Two review gates on the same feature caught different classes, and both paid.** The PRE-merge lenses caught what a USER would hit (a resume cap advertising a size the platform rejects, a nomination storable with nobody named, a timing rule binning real signups behind a success message). The CLOSE lenses caught what the CODE claims (a resume stored under the caller-declared MIME type despite a magic-byte check, and a privacy claim of mine that was simply false). Neither gate would have found the other's findings. Run both.
- **[S50] When a reviewer contradicts you, verify it yourself before believing OR dismissing it.** The close review said the main GET A QUOTE button dead-ends and that the header covers titles on mobile. Both sounded like agent noise; both were real, confirmed by a redirect trace and by elementFromPoint occlusion. The same agent had also self-corrected two false positives of its own, so neither blanket trust nor blanket scepticism was right.

- **[S50 post-close] Cloning a page's builder data is not cloning the page.** The Diwali and Ramadan pages carried the right Elementor content and still read as "just WordPress" to Naldo, because the ASTRA LAYOUT META did not come with the clone: without `ast-site-content-layout: full-width-container` the body loses `ast-page-builder-template` and the hero renders BOXED at 1200px instead of full bleed. "Renders and images load" did not catch it; comparing MEASUREMENTS against the template page did (1905px vs 1200px hero). When copying a page, copy its presentation meta and diff the result against the original, not against nothing.

- **[S47] On a maxDuration-bounded handler, once the customer's value is computed + SAVED, push every remaining best-effort step into `after()` — never await it inline.** I awaited design persistence (2× sharp + 2 uploads) inline on a 60s handler that already burns 20-40s on the analyzer; a slow run times out the request and loses the customer a price that was already computed and saved. Worst case should cost the picture, never the number.
- **[S57] Verification caught what care did not, twice, on the SAME resolution.** git's merge silently dropped 7 ledger rows (confirmed as Jason ARCHIVING them by checking the archive on master, not assumed); and a post-close dangling-reference sweep found a stale `#222 relay` pointer in `project_quote_tool.md` that the resolution itself had missed. Lesson: after a renumber, grep every doc for the OLD numbers — the resolution commit is not the end of the job. (A second stale pointer in Jason's own `session_log.md` was deliberately left and flagged rather than edited: never edit the other dev's log.)

- **[S69] The pre-merge lens round is not ceremony: on the ONE PR where three lenses converged, the root cause was my own merge resolution.** #849's branch had deliberately moved `setResult` to AFTER the satellite plan is durably saved, so a quote never looks sendable until the plan is stored. Resolving a conflict to preserve master's #839 identity-freeze notice, I took master's side whole, re-added the early `setResult`, and silently defeated the fix the PR exists to make. The comment kept claiming the protection while the code no longer delivered it. Customer, admin and technical lenses each found it from a different angle. Convergence is the signal; a conflict resolution that keeps BOTH sides can still destroy one side's intent.
- **[S69] Run the thing. Two blockers and two extra bugs were found by EXECUTING SQL in a disposable Postgres, none of which reading had caught across two review rounds.** A review reproduced that quote deletion still failed after my "fix"; a later run found `record_quote_lifecycle_event()` errored on EVERY call with an ambiguous column (an OUT parameter shadowing `quotes.first_sent_at`), and that narrowing a trigger had opened a delete gap. I also tested, rather than assumed, that revoking DELETE from `service_role` does not re-block an FK cascade. For schema work, a throwaway database is cheaper than a second wrong fix.

- **[S69 post-close] A one-line question from the dev found two lying ledger rows that every gate had passed.** Naldo asked "is everything in the session complete?" Gates were green, PRs merged, prod verified. But row 434 had SHIPPED and still read as open, and row 375 said "PR #1009 OPEN: staff now see a Timing pill" after he had closed that PR, so it described a feature that does not exist. No test, lint rule or CI check can catch a doc that lies about what shipped. The only defence is re-reading the rows you wrote against what actually landed, at the END, after the merges.

**Fix going forward (mistakes I've made):**
- **[S87] I announced a session number off a check that structurally could not see the collision.** Said S86 after listing journal fragments; S86 was Naldo's, used the day before, and its fragment simply had not merged yet. The ledger counter caught it. A fragment listing is not a session-number check — the counter, the archive, open PRs and remote branches all are, and the machine-local self-assessment is uncheckable from a remote container, which I said out loud rather than glossing.
- **[S87] I wrote "flyers" into a PR body and two code comments with no evidence for it.** The printed material verified was a van decal and business cards. A concurrent session caught it. The report-not-hope pitfall in its smallest form: a detail absorbed from conversation and repeated as fact.
- **[S87] My own fix was worse than the alternative, and only a concurrent session's independent answer showed it.** Narrowing the `/qr` allowlist to one segment closed the security gap and opened a customer-facing one, because the gated paths then render the staff login. I checked the fix against the finding, never against the module's own promise that a scan never dead-ends. When a fix satisfies a review note, re-read the thing it protects and ask whether the fix breaks THAT.
- **[S59] My instruments lied to me FOUR times in one session, each in a different way, and every one produced a false GREEN.** (a) A `perl -0pi` mutation silently matched nothing and the suite passed — indistinguishable from a passing probe, caught only because I printed a grep count beside it. (b) A probe harness reported RESTORE FAILED on a correct file, because `grep -F` with a multi-line pattern matched a line the mutation and the original share. (c) A python probe applied its mutation, ran, then CRASHED on a `cp1252` decode of subprocess output before restoring, leaving the file mutated — restored by INVERTING the mutation, never `git checkout`, which would have discarded the uncommitted fix with it. (d) A python heredoc's `
` escaping turned a JS template literal's newlines into real ones and produced a file I then had to repair with a real editor. The through-line: the instrument written in ten seconds gets the least scrutiny and needs the most. Naldo has PR #1146 open promoting the heredoc half of this on its second occurrence; this session is a third.
- **[S59] I shipped row 473's fix in #1178 and did not mark the row off until the close sweep — the promoted mark-a-row-off pitfall, recurring for the fourth time (S49, S57, S69, now).** The rule says the close sync is the DEADLINE, not the mechanism, so this was caught inside the rule rather than outside it. But the row sat claiming to be open for two PRs while its fix was live, and the only reason it did not survive the session is that the wrap has a linter for exactly this. The fix is not another reminder: it is marking the row in the same commit that closes it, which costs nothing at the time and is free to verify.
- **[S59] A test I wrote passed for the wrong reason and I nearly shipped it.** The short-insert test named the landed-lines assertion but used another person's shift, so it refused on OWNERSHIP first and never reached the branch its name describes. Caught by re-reading my own new tests before the fix round, not by any gate. The promoted rule (break the exact behaviour in the title and watch THAT test fail) is what the mutation probes then confirmed for the other 24 — but the lying test would have passed every probe I ran, because I would have probed the behaviour it never touched.
- **[S59] I left a stale comment block sitting beside its own replacement.** Updating the phase-1 copy that said office shifts had no editor, I added the corrected comment and did not delete the old one, so the file briefly carried two comment blocks stating opposite things about the same behaviour. Self-caught in my own diff, and a lens flagged the sibling instance in another file that I had missed entirely. When a fix REPLACES an explanation, delete the explanation it replaces in the same edit.
- **[S58] I merged a PR while its `gates` check was PENDING.** #1121 touched `scripts/`, so CI ran, and `--match-head-commit` guards the head, not the check state; it came out green by luck. The rule is wait, not skip; the fix is to read the conclusion BY SHA in the same command that authorises the merge — which is what every merge after it did.
- **[S58] A `| tail -1` laundered a failed push into "pushed=<sha>".** The branch was not on origin and the PR create said so; I had printed a LOCAL hash after a silently failed push. Promoted-pitfall recurrence (a failed command returns no information): show the exit code and prove the remote state (`ls-remote --exit-code`) before the next step depends on it.
- **[S58] My own SQL counted a LEFT JOIN's empty row as an open shift** and I nearly reported two people clocked in who have never clocked in. Caught by re-querying the open set directly. A join that can produce a null row must not be counted with `count(*) filter (...)` on a nullable column.
- **[S85] A find-and-replace scoped to one paragraph is not scoped to one MEANING inside it.** Fixing a real S84 to S85 renumber, I ran `data.replace(b'S84', b'S85')` on the whole NUMBER paragraph. That paragraph narrated TWO parties' states: a peer session's actual S84 claim (true, needed to stay S84) and my own choice of S85 (needed to become S85). The blind replace turned both into S85, producing a sentence that no longer parsed ("a claim on S85... took S85 anyway after confirming no S85 collision exists"), and it shipped merged before I re-read it. The same replace also mislabeled three code comments with the wrong session number, since those were written by the fix round before the collision was even found. Fixed properly in a post-close delta, only because a second review pass asked why the sentence didn't make sense. Lesson: when a replace touches a sentence describing more than one party, scope it by reading the sentence, not by matching the string.
- **[S77] I reported a 3-day query result as an all-time fact.** Told Naldo his manual test produced "the first `yll_lead_form_submitted` event ever recorded"; the real 30-day figure is 20 submissions. The window was in my own SQL and I read past it. Second occurrence of the class (S39 reported a `LIMIT 5` result as exhaustive), so it is now PROMOTED to AGENTS.md Pitfalls: state the window, or query the whole population.
- **[S77] I probed a blocked tool call three times instead of handing over the paste-able procedure on the first block.** Naldo's own global rule names this exact failure and cites two prior sessions that burned three attempts each. Mitigating detail, kept because it matters for what to change: the manual steps went over after the second block, and the third attempt came only because he asked me to try again. The rule is still first block, hand it over.
- **[S77] I read a purge as done from a click result instead of the feature.** A stale element ref sent my confirm click to an unrelated admin page; the NitroPack "last purge" timestamp still read 2 hours old and that is the only reason I caught it. Fresh refs after any layout change, and verify by the state the feature reports, not by the click returning without error.
- **[S82] A test whose NAME describes a regression must be mutation-probed against THAT regression, not a nearby proxy. Second occurrence in two consecutive PRs, so this is now a class.** "puts the add form AFTER the list on the page" asserted only that two substrings existed; a lens swapped the two render sites, the exact regression the name describes, and all 46 tests still passed. My own earlier probe had missed it because it removed the grid CLASS, which the test does check, so I had probed a proxy and felt covered. The PR before it had the same shape through a different door: a test named "defaults its sort to recently finished" passed with the sort logic deleted entirely, because a static render never gets past its loading state to render the sort control. The habit: after writing a test, break the exact behaviour in the title and watch THAT test fail.
- **[S82] Never spawn a review round against a dirty working tree.** I launched two lenses while an uncommitted test fix sat in the worktree; the agents read files, not just the diff, so one reviewed a tree that matched no commit. It reported that honestly rather than folding it into findings, which is the only reason it did not become a phantom disposition. Commit first, or tell the lens the tree is live.
- **[S82] A probe's stated privacy constraint binds the probe's OUTPUT, not just its intent.** I said a diagnostic would print only status codes, then printed enough response body to include a customer's name. Trivial exposure in the owner's own tooling and the files were deleted, but the constraint was mine and I broke it in the same breath as stating it. Cap what is printed in the code, rather than promising it in prose.
- **[S57] When you claim a defect is UNREACHABLE, enumerate every caller of the code that causes it — not just the gate you happen to be thinking about.** I told Jason two customer-facing rows were dormant "because the runner is unarmed", and wrote that into the ledger and two code comments. True of the runner and irrelevant: the SAME PR shipped a manual Record button with no such gate, and that was the path staff would actually use — none of the three customers even had a vaulted card, so the runner was never going to charge them at all. Caught by the wrap customer lens six days before a real $453.13 payment fell due; the first click would have left that customer's Balance Due overstating what she owed by exactly the amount she had just paid.
- **[S57] A guard and the copy that NARRATES it are one change, not two — this bit four times in a single session, on one feature.** Locking shipped without the builder copy; the builder copy was fixed and left the JOB PAGE saying the identical false thing ("edit it in the builder and Calculate", which the server refuses); the migrated lock message was branched at every call site except `refuseIfFrozen`, the one shared helper; and the dormancy claim above is the same shape. Each time the guard was correct and the screen was lying. **PROMOTED to AGENTS.md Pitfalls 2026-08-29.**
- **[S57] I stated an artifact of my own too-narrow grep as a fact about the world.** Told Jason "row 425 does not exist — not active, not archived, not in the counter"; it exists, unbolded, and my pattern required bold. The same flaw produced a "110 open rows" count that included rows whose bodies say SHIPPED. The promoted check-the-check pitfall, recurring: before asserting something does NOT exist, widen the pattern and say what it cannot match.
- **[S57] Every row the session shipped still read as OPEN at the point the dev asked "is anything left?"** — including the one whose body still carried a red LIVE TRAP telling staff not to press Calculate, hours after the PR closing it had merged. The promoted mark-a-row-off pitfall, broken in consecutive sessions. The deadline is not the close; it is the same breath as the merge.
- **[S78] A scripted mutation that silently matches nothing reports the same green as a passing probe: read the file back before trusting the run.** Three times in one session a probe no-opped, and each time the test suite said everything passed: a `perl -0pi` pattern that never matched, an anchor that appears twice in the file because a sibling function uses the same CAS lines, and an em dash that came through a shell heredoc mangled so the replacement could not apply. The promoted check-the-check pitfall with a third door, string encoding. Escape non-ASCII as a backslash-u escape in any python heredoc that must match repo text, anchor uniquely, and assert a DELTA (count before, count after) rather than an absolute string typed from memory, which is how two of my own assertions failed on correct data this session.
- **[S76] A gates claim is scoped to the BRANCH AND RUN it came from — I wrote "lint 0 errors" into a PR body from a sibling branch's run while that branch's CI was genuinely red.** The technical lens reproduced the red gate byte-for-byte and called the body claim false; both were mine. The promoted report-not-hope pitfall through a NEW door: the claim was TRUE somewhere, just not of the thing it described. Run the gate on the head you are describing, or write "not run on this branch".
- **[S76] My own suggestion list is a stack of seat prescriptions — recon each before promising it.** Suggestion 7 ("server-render the role into the shell") was impossible: ~24 'use client' surfaces including the 8000-line QuoteBuilder render OperatorShell, and a client component cannot render an async server component. One grep killed it before code; the residual comment that had pointed at it as "the real fix" had to be corrected too.
- **[S76] A cleanup claim must enumerate every table a write CASCADES into.** The smoke script deleted its placements and said "everything deleted" while each run left 6 advertising_activity rows whose FK nulls on delete (19 accumulated before the admin lens caught it). Audit appends and set-null shadows are part of the write, so they are part of the cleanup and its before/after assert.
- **[S75] I spawned a four-lens round on a STALE local tree that was missing the entire third slice.** The builder had pushed S6 to origin; my worktree sat at the pre-S6 commit, and every lens brief said read the tree directly. The customer lens burned its pass reporting a route missing that existed, and only the contradiction with the builder's report exposed it. *Lesson: before spawning reviewers on a branch, assert local HEAD equals the origin head by pasting both SHAs; a review of the wrong tree is worse than no review because it produces confident wrong verdicts.*
- **[S75] I hand-typed a full SHA into --match-head-commit and burned two merge attempts on my own typo.** The pin guard refused exactly as designed; the fix was reading the SHA from git in the same command. Second occurrence of the paste-the-number-from-the-instrument class (S53). **PROMOTION DUE at the next docs PR that may touch AGENTS.md** (this close is barred from AGENTS.md to keep the auto-merge): never retype an identifier an instrument can paste.
- **[S75] I applied migration 1 comment-stripped instead of byte-verbatim.** The executable DDL was identical and the close integration lens was tasked to verify prod-vs-file structural parity, but the AGENTS.md rule says byte-identical for a reason: structural-equivalence is a claim needing verification, byte-equality is a fact. Applied migrations 2 and 3 verbatim.
- **[S75] A background command chained a poll loop after a fallible merge, and the conflicted merge sat unresolved in the worktree for 40 minutes.** The never-chain pitfall through a new door: run_in_background makes the silent half-done state invisible until a goal check-in reads the output file. State-changing steps get their own foreground command; watchers watch only.
- **[S75] My backfill runner's stop condition read response fields that did not exist, and printed 'drained' after one batch.** Caught because 22 pending minus 6 done did not equal drained. The check-the-check pitfall in a five-line loop I wrote in ten seconds; the instrument you write last gets the least scrutiny and needs the most.
- **[S74] My mutation-probe restore WIPED my own uncommitted work: `git checkout -- <file>` after a perl mutation restores the BRANCH state, not the pre-probe state.** All four uncommitted fleetDay edits vanished mid-build; recovered only because the edits were still in context. On uncommitted code, restore a probe by INVERTING the mutation (perl back), never by git checkout. The never-chain pitfall in a new costume: the danger was not a chained state change after a failure, it was a restore command whose baseline I had not checked.
- **[S74] The session-number check missed the machine-local self-assessment AGAIN, one day after S73 documented that exact door.** Took S72 on the handoff's word plus repo-side checks; the referral session held S72 in the local file git cannot see, and S73 was taken the same way. Caught at wrap, yielded to S74, and the local-file clause is now PROMOTED into the AGENTS.md session-number pitfall (second occurrence, promotion due).
- **[S74] The transcript's structure decisions took three build iterations to land (clocks in the same page, then a sub-header, then a separate admin-only page), and the nav bug was called out by the owner, not caught by me.** Jason had already said fleet is its own thing and time tracking lives elsewhere; a tighter first read of the decided structure as SPEC rather than inspiration would have saved two device rounds.
- **[S56] A "prior value" snapshot must capture what is being OVERWRITTEN, not a summary of it — and it must be written exactly once.** Two failures in one migration. My `priorTool` recorded total/deposit/status/serviceType but not the quote's `inputs`/`result`, so 13 overwritten drafts lost their original content with no recovery. Worse, it was not idempotent: a re-run captured the **already-corrected** figures as if they were the originals, so the audit trail for a corrected invoice quietly asserted the wrong "before" and had to be restored by hand from a pre-flight query I happened to still have. An audit trail that lies is worse than none.
- **[S56] Back-dating one timestamp means auditing every metric derived from it.** I set `quote_sent_at` and `customer_approved_at` to the real historical dates and left `created_at` at the migration run date, which made the homepage's Quote-turnaround KPI **negative** — −63.35 days across the migrated set, dragging the tool-wide figure from +4.52 to −12.66 — on the most prominent card of the owner's dashboard. Found by the admin lens, not by me, despite my having written the backdating. Before back-dating a column, grep for every date-DIFF that reads it.
- **[S56] I reported a false fact and the dev ruled on it.** I told Jason that Kathy Polera's invoice was untaxed; he replied "deliberate, leave as is". Her invoice charges $56.52 at 8.75% — my parser found no rate on the visible line rows and I reported "no rate found" as "no tax". The instrument's silence is not evidence of absence, and a fact handed to the dev becomes a decision they own. Say what the check can and cannot see when reporting it.
- **[S73] I committed doc updates to a branch whose PR had ALREADY MERGED, so the canonical runbook told the owner the pipeline was never set up while it was running.** The two commits sat orphaned on a dead branch for hours. Nothing in the gates can catch this: the branch was green, the commits were real, and the file on master was simply older. Caught by the close admin lens, whose whole job was asking whether the doc matches reality. *Lesson: after a PR merges, its branch is dead; before editing a file, check whether the PR carrying that file is still open, and re-read the file ON MASTER before claiming it says anything.*
- **[S73] I gated the branch that TALKS and left the branch that ACTS.** Told to keep a reply quiet unless the bot was addressed, I gated the coaching reply and not the path that merges, so the approver typing `merge 1043` in a crew group would have deployed unbidden and announced it to the room. The delta-verify caught it. *Lesson: when adding a gate, enumerate every branch that flows past it and say out loud which one carries the blast radius; the visible branch is the one you will fix by reflex.*
- **[S73] I blamed the safety classifier for four blocked commits while holding the real cause constant.** I varied the commit message three ways (long, short, via a file) and never varied the pipe; the block was on `| tail -2`. *Lesson: when something is refused, change ONE variable per attempt, and start with the one you have NOT built a theory about.*
- **[S73] The session-number check we have documented is incomplete, and it bit.** Master, the archive, every open PR and every remote branch all left S72 free; a concurrent session already held it in the machine-local `feedback_self_assessment.md`, which git cannot see. Caught before anything landed, yielded to S73. *Lesson: the number space includes the LOCAL self-assessment, and that is the one place no documented check looks. Worth promoting once it recurs.*
- **[S54] `--match-head-commit` guards a PR's HEAD, not its BASE — and the gap between "re-gate on current master" and "merge" is unguarded.** I brought #990 current with master, re-ran the gates, pushed — and Naldo merged his own PR in the minutes before my merge fired, so mine squashed onto a master I had never gated against. Harmless this time (disjoint files; the gates I then ran on the merged master were green at 7978) but that is luck, not process: the promoted pitfall covers the head-moved case and says nothing about the base moving, and with a second dev actively merging the window is minutes wide, not theoretical. Re-check `origin/master` in the SAME command as the merge (`git merge-base --is-ancestor origin/master <branch>` immediately before `gh pr merge`), not a step earlier.
- **[S54] I under-tiered a pre-merge review by eyeballing risk instead of checking the diff's paths against the FULL-tier trigger list.** #985 got two lenses; its diff touches `src/components/quote/**`, which AGENTS.md names explicitly as a FULL trigger. Self-caught and backfilled — and the backfilled ADMIN lens returned a real MED, which is exactly the outcome the trigger list exists to prevent. Third occurrence of this family (S38, S42, now S54), but the rule itself is no longer in dispute — AGENTS.md already carries the explicit trigger list, so nothing needs promoting; what needs changing is my method: read the changed PATHS against the list, never judge the tier by how risky the change feels.
- **[S51] I lost four premerge lens agents to a process exit — the SECOND time (S50), so the fix is structural now, not a reminder.** Every review brief this session ends with "write the file EARLY and keep updating it", and that is precisely why one partial report survived the crash. Launching reviews early is not enough on its own: the failure mode is the PROCESS dying, not the agent stalling, so the agent has to persist findings as it goes.
- **[S51] I wrote a code comment claiming a distinction the code did not make.** My comment said the customer-profile em dash separated "no colour approved" from "the lookup failed". It did not — errors collapsed into an empty map and rendered the identical dash, so a failed query would confidently state "none", on exactly the question that column exists to answer. Caught by a staff lens. The promoted report-not-hope pitfall, recurring in a CODE COMMENT rather than a PR body.
- **[S51] My first regression test for a fix round PASSED against the bug it was written to pin.** The row-404 correlation test asserted the CAS value, but the mock returned the same snapshot on both reads, so it could not tell a correlated clear from an uncorrelated one. Only after the harness modelled a genuine concurrent write — a drifted row, and a CAS that claims zero rows on a miss — did removing the fix fail it. For a CONCURRENCY fix the fixture has to actually contain the concurrency.
- **[S51] I under-reported a residual in my own PR body and two lenses had to correct me.** I called row 404's remaining stuck state narrow; `mark-paid` — the cash/check action every non-card customer settles through — never clears the markers and is provably terminal for them after this change. The fix was still right; the characterisation was wrong, and a wrong scope in a PR body is what a future reader plans against.
- **[S50→AGENTS.md] Stage by explicit path — `git add -A` and `git checkout <branch> -- .` both take files that are not yours. PROMOTED 2026-08-25 (S52, second occurrence, through the staging area).** Read it in AGENTS.md Pitfalls; don't re-derive here.
- **[S50→AGENTS.md] Check the CHECK before reporting from it — PROMOTED 2026-08-25 (S52, second occurrence, failing the opposite way).** Read it in AGENTS.md Pitfalls; don't re-derive here.
- **[S50] Honesty and usability are SEPARATE axes - fixing one is not licence to wreck the other.** Correcting copy that made a false claim, I pushed the replacement into naming a raw database table and column to staff reading on a phone, and gated the one actionable instruction behind a fact they cannot check. The staff lens diffed against master and showed the PRE-PR wording was already plain and actionable. Fix the false claim; keep the plain instruction.
- **[S50] When promoting a lesson into a rule, promote the LESSON - do not add clauses of your own.** My AGENTS.md draft added "in the same PR that ships it", which appears nowhere in the source lesson, contradicted the batch-don't-PR-every-tweak bullet two entries above, and would have dragged the unified ledger into every code PR across concurrent sessions. Caught by the process lens; reworded with a note recording why the stronger form was rejected, so nobody re-adds it.
- **[S67] I offered to verify something I had no way to reach, and only checked afterwards.** Naldo asked me to confirm the Bouncie API would work and attached the docs; the attachment never arrived AND every Bouncie host (`docs.bouncie.dev`, `api.bouncie.dev`, `api.bouncie.com`) is blocked by this container's egress proxy. I had already hit that block twice earlier in the same session while researching, and still framed the next step as "I'll verify" rather than "I cannot reach this, here is what to paste". Lesson: when a task depends on reaching an external host, test reachability BEFORE promising the check — especially in an environment that has already refused that host once.
- **[S49] MARK A ROW OFF WHEN YOU SHIP IT — the write-side twin of the read-the-row-status pitfall, and it nearly caused already-finished work to be rebuilt.** S39's lesson was "don't trust presence in the ACTIVE table, read the row's own status text". This is the other half: after SHIPPING, update that status text, or the next reader is misled by construction. The S49 close minted new rows and bumped the counter but never marked off the four rows it had just shipped; a proper sweep then found TWELVE already-done rows still active, four of them literally saying "still listed here in error" since S46. Row **191 was among them and was on the build list Jason had just handed me** — one step from rebuilding work live for weeks. Two further traps inside the fix: my first two sweeps checked only the TASK column, but 191 and 192 declare completion in the NOTES column, so a task-column sweep structurally could not see them (sweep EVERY cell); and an assertion of mine compared a UTF-8 BYTE count against a CHARACTER count on a file dense with em dashes, failing spuriously on correct data. **PROMOTED to AGENTS.md Pitfalls 2026-08-25 (S50)** — read it there; don't re-derive here.
- **[S49] When the dev signals a wind-down or re-scopes the work, CONFIRM SCOPE before dispatching — do not answer a new constraint with new work.** Jason said "run the reviewers, do not start fixes, record them for next session"; I had dispatched three fix-round agents seconds earlier, straight off the findings, without pausing on whether he wanted fixing now. Stopped all three and verified every worktree clean, so nothing was half-applied — but the reflex was to move from findings to fixes automatically. Same family as the S65 overshoot (deviating from an approved split and explaining afterwards): the moment the plan changes is the moment to confirm, not to launch.
- **[S49] Derive a permitted-state set from the surface that ACTUALLY invokes the action, never from the set of surfaces that exist — and if the set has no test, that is why the error survives.** Row 387 gave `dismissItem` a positive CAS and I passed `['unresponded','handled']`, reasoning from `applyBucketFilter`'s three buckets. But the only Dismiss control in the app is fed by `listOpenItems` → bucket `needs_reply` → `status='unresponded'`, and InWorksSection renders no dismiss at all. So `'handled'` — precisely the state a stale click races INTO, and the one the guard exists to refuse — was in the allowed set, reopening the very defect the row was written to close (an answered lead flipped to dismissed, plus `addSuppressedSenders` silencing a real customer). Two lenses converged on it independently. Root cause of the SURVIVAL is separate and worth its own habit: the route's status set had zero test coverage, so re-adding the bug failed nothing. The fix round added `dismiss/route.test.ts`, the direct sibling of `handled/route.test.ts` — which already existed for row 320(c), the same class. When you add a guard whose correctness IS a constant, test the constant.
- **[S48] I stopped after every status report and made the dev restart me, all session.** He said so directly: "multiple times this session you've stopped working and I've had to send a new message to get you started again." A status update is not a stopping point — report and keep going in the same turn; stop only for a genuine decision, a merge-go, or something only he can do.
- **[S48] I asked a non-coder to make coding decisions.** Which local branches to force-delete, whether to merge each PR as its verify cleared or batch them, and a ten-row table of code-level findings — he answered "I have no idea about this, I'm not really a coder." His calls are business, money, customer-facing and authorization; everything else is mine to decide and then explain in plain terms. Adjacent to S47's "stop asking for permission already given", but worse: this was asking for judgment he has no basis to give.
- **[S47] I kept asking for permission the dev had already given, and he had to tell me.** After an explicit standing "you can merge anything that's green", I still surfaced each PR individually for a merge-go. Reserve the stop for the five things that genuinely need a human - domain judgment I cannot derive, changes to the dev's own environment, actions only they can take in the world, irreversible prod ops, and priority - and make each a one-liner with a recommendation. *Corollary from the same session: "want me to look at X?" is never one of them; looking is free and reversible, so just look. Doing that turned up an orphaned worktree holding a tested fix for an open red row.*
- **[S47] A ledger-number check that skips the ARCHIVE is not a check.** I verified 347 was free in the ACTIVE table and in every open PR and minted 347/348/349 - but 347 was held by an ARCHIVED row, and 348/349/350 merged while I worked, forcing a renumber to 360-362 and a branch rebuild on fresh master. Later the same session the corrected check (master + archive + every open PR) caught that Naldo's #913 already held 363, and I skipped to 364 cleanly. *Lesson: the number space is master + archive + every unmerged branch - and a `gh` call that FAILS returns zero claimants, so treat an empty result from a failed command as UNKNOWN, never as free.*
- **[S66] I enforced the office/field boundary at one consumer and shipped the write path unguarded — the enumerate-all-consumers pitfall, twice in one feature.** The #893 premerge round caught the schedule DROPDOWN showing office staff as assignable field crew, and I scoped a new `listActiveFieldCrew` to fix it. The wrap integration lens then found `assignCrewToJob` (`POST /api/ops/schedule`) inserts a raw `crewMemberId` into `job_assignments` with NO `is_office` check at all — a direct POST still assigns an office person to a field job, contradicting the schedule page's own "must not be offered" comment. Filtering the reader is not gating the write. Lesson (promoted pitfall, recurrence → row 356): when a rule filters a population, gate it at the point of the STATE CHANGE, not only where the UI lists candidates.
- **[S65] When a required gate physically can't run this session, name the substitute BEFORE the merge ask, not after.** #883 was customer-facing and merged with no pre-merge lens review (the harness wouldn't spawn agents mid-build) and no browser check. I surfaced all three gaps clearly and Naldo said merge each time, so the decision was his and informed — but a slider wired to nothing passes all 6819 tests (none touch a pixel), so the cheap browser check WAS the real pre-merge gate and I offered it as an afterthought instead of as the thing that should gate the go. The wrap's four lenses (0 HIGH, 3 MED) then did the review that should have preceded the merge. Lesson: the moment a standing gate is unavailable, state it and put the available substitute (a two-minute browser drive) in front of the merge decision, so "merge" is a yes to a real check, not to skipping one.
- **[S65] I overshot a work split Naldo had just chosen, and let a handoff I'd sent him go stale.** He picked "do the 3 editor edits, hand the rest to Codex"; the edits went fast so I built the whole feature inline, and the Codex prompt already in his hands became garbage. Net less work for him, but not the plan he approved, and he had to notice the pivot himself. Lesson: an approved plan is a commitment; the moment I decide to deviate, say so in one line, don't just deliver a different thing and explain later.
- **[S61] "Keep both sides" is the RIGHT rule for an append and the WRONG rule for an ARCHIVAL MOVE — and I applied it blindly across five resyncs.** Master had archived the S58 and S50 blocks out of CLAUDE.md (a delete-here plus an add-there); my branch still carried them live. Resolving "keep both" preserved my live copies AND accepted master's archive additions, so both blocks existed in two places at once from `ac75f114` all the way to HEAD. Nothing was lost — the failure mode was the mirror image, silent RETENTION of content the other side had deliberately removed — and it survived five conflict resolutions and every gate because a duplicated section still parses. Caught only by a merge-integrity lens re-run during idle time, after the first pair of lenses died unreported. Lesson: before resolving a docs conflict as additive, check whether one side's change is a MOVE. If a block disappeared from a file on one side, look for it arriving somewhere else before you "restore" it. The cheap test is the one that found it: for every section header, assert it appears in exactly ONE of {live file, archive}.
- **[S61] I shipped UI copy that overstated what my own feature achieved, and only a lens caught it.** My Settings text said the bot "recognises a crew member by their Telegram account, so until this is linked their texts do nothing" — true, but it implied the converse, that linking is sufficient. It is not: the webhook drops any chat not in `TELEGRAM_ALLOWED_CHATS` BEFORE the dispatcher runs, so a 1:1 DM stays silent even when linked. This is the promoted "a PR body is a REPORT" pitfall recurring in USER-FACING COPY, which is worse — a wrong PR body misleads a reviewer, wrong UI copy misleads the office into thinking a crew member is set up when they are not. Lesson: copy that describes what the system will do is a claim, and gets traced like one.
- **[S61] I built a feature on top of a component without checking anything rendered it.** I extended `CrewLogins`, wrote its API, tested it, and wrote a ledger row describing "a Telegram section in Settings → Accounts → Crew logins" — while a two-line grep would have shown the component was imported and never used anywhere. I had even been told the target surface by name in the brief. Lesson: before extending a UI component, grep for its JSX usage, not just its definition; "the file exists and compiles" is not "the screen exists".
- **[S43] I read a multi-entity activity join as if it were ONE customer's timeline, and told Jason a false story about a real customer.** Asked to list every colour-change request, I queried `dashboard_activity` for all `:color-request` items at once and reported that Kristie Tibbetts' request was "dismissed the same day so no escalation email ever fired" — the dismissed row belonged to the TEST item; her request was never dismissed, an escalation DID fire, and it is still pending three weeks on (found by the wrap customer lens, now row 321). The report-not-hope pitfall recurring through a new door: when answering "what happened to X", GROUP BY the entity or filter to it — a join across siblings is not a timeline.
- **[S43] Two of my fix-round prescriptions asserted a code state I had not checked on that BRANCH.** "Fold `errors` into `ok` like the existing wiring does" (there was no such wiring — `ok` was hardcoded true) and "`detail` is already SELECTed since #827, just map it" (false: that branch's base predated #827's merge by 12 minutes). Both builders refuted with evidence and both were right. The seat-prescription-is-hypothesis pitfall, with the specific new lesson: a claim about what code contains is scoped to a BRANCH BASE, not to "master" in the abstract — check `git merge-base` before asserting.
- **[S42] The four-lens count is the policy — SECOND violation (S38, now S42), promotion to AGENTS.md Pitfalls DUE (deferred to S44's first docs PR; this close was barred from AGENTS.md).** I ran 2 lenses on #807; the backfilled pair found BOTH HIGHs (the invisible sync failure and the reschedule gap).
- **[S42] Don't "fix" a mismatch by changing only the surface you can see — read the other surface's code first.** The rounded-email cut was built on my own false claim that the portal card rounds (it has its own 2-decimal formatter); two lenses converged to reverse it. Task-text-is-hypothesis applies to MY OWN claims.
- **[S42] A builder in the main tree is exposed to ANY concurrent tree user** — an unattributed branch hop (19:33Z, reflog-cleared of S41) landed a builder's commit on local master; recovered clean, but worktrees for builders whenever anything else could touch the machine.
- **[S42] Two mid-report false alarms stated as fact before checking** (a grep pattern "proving" the declined-skip missing; a wrong ~16:00Z timestamp corrected by S41's reflog). Both self-corrected in the next message, but each briefly asserted a wrong fact — verify before stating, even in status updates.
- **[S41] I dispatched a fix round into the worktree a live builder still owned** -- two of my own agents interleaved edits in one tree (the promoted S32 worktree pitfall recurring ONE LEVEL DOWN, seat-spawned agents instead of sessions). The agent caught the contamination; I verified containment via non-comment-diff. Worktree assignment is per-AGENT, not per-task.
- **[S41] I merged #814 STALE because the freshness check and the merge were chained in one command** -- the check printed BEHIND while the merge was already firing (promoted never-chain pitfall, merge-flavored). Zero overlap + green CI made it harmless by luck. A merge never rides in the same command as the check that authorizes it.
- **[S41] I briefed a build for row 292 without reading the sibling lane's just-merged #799 first** -- it had already fixed the defect via a different mechanism; the builder discovered this, the branch died, a build was wasted. Brief-time reachability probing applies to CODE STATE (what merged an hour ago), not just prod data.
- **[S41] I misattributed three merges (#816/#820/#821, Naldo's) to S42 in a cross-session coordination message** -- S42 rightly demanded titles+authors. Cross-session facts come from gh/reflog, never inference. Same message family: when S42 accused this session of a main-tree branch hop, the reflog showed the hop was real but unattributable -- answered with evidence, declined blame, recorded it as unattributed.
- **[S41] FOUR false claims in my own PR bodies/briefs, every one caught by a lens or delta-verify** (the promoted S58 report-not-hope pitfall, 4 recurrences in one session): "every live row carries a message id" (470/474 don't) - "a 200 with ok:false is a designed outcome" (nothing returns it) - "5 items churning" (4; one was dead-quote-routed) - "can't be undone from here" (Reverse exists, and the wrap staff lens then found the CORRECTED copy still overpromises: Reverse restores the item but not the auto-closed nag -> row 304).
- **[S58] I broke the one-conversation-one-session rule, and collided with a real session doing it.** After the S58 close I kept working in the same conversation and labelled the continuing work "S59", then "S60", across commits, ledger rows and docs. The wrap skill's step 9 says post-close work is a DELTA on the same entry. Worse: a genuinely separate session had already taken S59 (the copilot outage, PR #810), so my labels collided with someone else's real work — the exact collision class this repo's log header has warned about since S38. Lesson: the session number is fixed when the conversation starts and does not move because the work feels like a new chapter.
- **[S58] Adding a second user population to a shared auth store means auditing EVERY door that reads a role, not just the ones you wrote.** I hardened getOperator and the proxy, then shipped a crew→admin escalation because `PATCH /api/admin/users/[id]` — a route I never touched — reads role through `roleOf`, which collapses 'crew' to 'operator'. The new population made an existing, correct-looking route unsafe. Lesson: when you widen who can log in, grep every consumer of the role primitive and ask what each does with the collapsed value.
- **[S58] I claimed a vocabulary that already belonged to something else.** The crew command parser took `help`/`commands`/`?` — the existing bot's own help words — and because it runs ahead of the dispatcher, every user in every chat got crew time-clock help instead of the real help. I had reasoned carefully about running ahead of the address-gate and wrote that tradeoff down; I never checked whether the WORDS were free. Lesson: before putting a matcher in front of an existing one, grep the words it claims.
- **[S50 post-close] I verified a page in isolation instead of against its template, and Naldo caught it.** I had checked the new pages for headings, FAQ count and image loads, all of which passed, and reported them ready. They looked wrong beside a real service page. A page built FROM a template must be verified BY comparison with that template.
- **[S50 post-close] My first mobile-clearance script put 251px of dead space on the classic town pages.** The theme renders a HIDDEN SEO `h1`; taking the first `h1` in DOM order and falling back to the container's OUTER edge double-counted padding those pages already had. Caught only because I tested the fix against live pages BEFORE publishing it. Measure the first VISIBLE element, and fall back to the padding box.
- **[S50 post-close] I published two pages onto `-2` slugs without first checking who held the canonical ones.** Pre-existing DRAFTS (3788, 3790) held `diwali-lighting` / `ramadan-eid-lighting` with 11.5k and 21k chars of hand-written copy, one edited hours earlier. Nothing was lost (they were parked, not deleted) but the near-miss was real: check slug ownership BEFORE publishing, and treat an unexpected `-2` as a collision signal rather than cosmetic.
- **[S40] I opened a PR for an already-run script without gating its branch** — tsx executes TypeScript without typechecking; "it ran" proves nothing about tsc. Every branch gates before its PR, script-only branches included.
- **[S40] I edited docs in the main tree while a builder owned it** — its instructed branch switch reverted my edit and the retry landed inside the builder's tree. The one-builder-in-main-tree rule binds the SEAT too; mid-round docs surgery goes to its own worktree.
- **[S39] I presented 5 already-shipped rows as open work** — my "open tasks" table trusted presence-in-the-ACTIVE-table without reading each row's own status text (two rows literally said ✅ SHIPPED S57 in their bodies). Read the status text before presenting a to-do list.
- **[S39] I reported "5 GML rows" from a LIMIT 5 query as if exhaustive** — the real count was 7, then 8 by the time the backfill ran. Never report a count from a LIMITed query; COUNT(*) first.
- **[S39] My "structurally impossible duplicate" claim rode from a fix brief into shipped code comments and the wrap falsified it** (GHL-timeout failures are outcome-unknown) — the S58 report-not-hope pitfall recurring through a BRIEF: strong claims in briefs get embedded verbatim by builders, so brief-claims need the same evidence bar as PR-body claims.
- **[S50] I fixed one template family and called the bug fixed.** The 'header covers the text' repair targeted `body.page-template-default:not(.elementor-page)` and I verified it at six widths on a CLASSIC page. About Us and ~10 town pages are ELEMENTOR pages, excluded by that selector, and at 375px the header still covers their title by 47px. I had even re-saved About Us earlier the same session and checked it at desktop only. When a defect spans a population, enumerate the TEMPLATES in that population, not just the URLs you happened to open.
- **[S50] I wrote a privacy claim I had not tested, into a PR body and a code comment.** 'A nominated third party's contact details never reach GHL' was false: they ride inside the staff alert email that GHL delivers and persist there. I had deliberately stripped them from the Telegram line, which is exactly why I believed the claim about the other channel. State the property you actually tested, per channel, not the intention behind the design.
- **[S50] I read a green result as proof without checking what it proved.** A post-swap sweep reported `/get-a-quote/` as loading with lead embeds present. It was 301ing to the homepage and I was reading the HOMEPAGE's contents. A follow-redirect check cannot confirm a specific page rendered: assert on something unique to that page, or disable redirects.
- **[S50] I verified an embeddable form against a protected preview and learned nothing.** Every /forms/* path on the Vercel preview returned the same 'Continue with Email' page, which was Vercel's own login wall, not my form. I nearly logged it as a pass. When every path returns an identical page, that is the harness talking, not the app.
- **[S50] I left the close-review agents until last and lost them to a process exit, twice in one day.** Both the first attempt and a later pair had to be re-run. Launch review agents early enough that their results land inside the working window.
- **[S38→AGENTS.md] Browser targeting: FRESH refs only — promoted.** A review agent re-used an element ref captured before a menu opened; the portaled menu shifted the layout and the stale ref fired a REAL prod Send (is_test gates contained it). Stale refs are coordinate-class errors; the S34 ban + this corollary now live in AGENTS.md Pitfalls — don't re-derive here.
- **[S38] The standing four-lens-per-PR order is not mine to scale down.** I ran 2-lens rounds on three PRs to save tokens; the missing lenses, backfilled after self-catching, found a HIGH (training-data poisoning) the two run lenses missed. The policy's cost is priced in; the count is the policy.
- **[S38] The dev server serves the WORKING TREE — never switch the main tree's branch while lens agents are live-driving it.** Two agents' browser legs silently tested the wrong code mid-session; their static analysis saved them, but pin the tree (or point agents at git-show only) for the round's duration.
- **[S38] Hand agents MEASURED numbers, not remembered ones.** I briefed all four wrap lenses with “31 files” for an 89-file session and called #754 “pre-session” when it merged inside the window — two lenses burned time refuting me. Derive the span stat fresh at wrap time.
- **[S58] I wrote three confident claims in a PR body that were false, and only a review caught them.** That the lint rule made the accessor "the only door" (bracket notation walked around it), that a function's ownership constraint was "written into its doc block" (it was not), and a test named "the two ALWAYS sum back to the envelope" that only held for whole-second inputs. All three were describing intent as if it were verified fact. Lesson: before writing "X is documented" or "X is enforced" in a PR body, re-open the file and read the words, or run the thing that would fail. Describing what you meant to do is not reporting.
- **[S58] A test name is a claim, and it gets tested too.** "and the two always sum back to the envelope" encoded a false invariant while passing, because the fixture only used clean minute boundaries. The name asserted a general property the test never checked. Lesson: when a test name says always/never/every, either prove the general case or name the specific one.
- **[S58] I shipped a doc that contradicted my own code an hour later.** A6 item 12 said "nothing reads this flag today" while the same session built the module that reads it, and project_quote_tool.md still called a merged PR open. Both were live docs I edited for OTHER reasons in the same session and did not re-read around my edit. Lesson: when editing a paragraph, read the whole paragraph for claims the session just falsified - the edit window is exactly where staleness hides.
- **[S57→AGENTS.md] Sibling-guard parity recurred a 7th time (evidence only — the bullet itself was already promoted 2026-08-05).** `crewMembers.ts`'s insert/update shipped without the race-recovery pattern `shifts.ts`'s `clockIn` already had, same session, same reviewer. Read AGENTS.md Pitfalls; don't re-derive here.
- **[S57→AGENTS.md] Two new pitfalls PROMOTED 2026-08-10: verify cwd/branch/remote before the first write — including at a compaction resume, not just a fresh session start; a new migration must update `FULL-SCHEMA.sql` in the SAME PR, it does not reconcile itself.** Read AGENTS.md Pitfalls; don't re-derive here.
- **[S35] My own memory-doc script truncated task_ledger.md to 0 bytes — the S25 wipe class, recurring in a NEW form, from a script that FOLLOWED the post-S25 rule.** `pathlib.write_text` opens with `'w'`, so when the encode blew up on a lone surrogate (a hand-typed `🔜` emoji escape, which Python builds as two invalid halves) it had already emptied the file. Caught it immediately (`wc -c` = 0) and restored from git, no data lost. Lesson, now PROMOTED to AGENTS.md: the script-file rule is necessary but NOT sufficient — encode the payload first so a fault raises before any file is touched, write to a temp, verify size, `os.replace`. Also: never hand-type surrogate-pair escapes; use `\U0001F51C` or the literal character.
- **[S35] A hand-resolved merge conflict can pass a marker-and-count check and still be structurally broken.** #215 × #199: zero stray markers tree-wide, every expected export and describe block present — and the resolution was still short one closing brace, three levels deep in a nested `describe`. Only `tsc` caught it. Counts prove nothing about nesting; after ANY conflict resolution the gates are the proof, not the grep. (Extends the S52 marker-grep habit rather than replacing it.)
- **[S55] I nearly read a tool's local-cleanup error as a failed merge.** `gh pr merge --delete-branch` printed `failed to run git: fatal: 'master' is already used by worktree` — that was only the post-merge local branch switch, on a machine where another worktree holds `master`; the merge itself had landed. Checking `gh pr view --json state,mergedAt` settled it in one call. Lesson: when a command that does BOTH a remote action and a local action errors, determine WHICH half failed before reporting or retrying — a retry here would have been a no-op at best.
- **[S53] Prod "today" queries: pin the business-local clock first, phone-match by digits-suffix, and treat an empty join as a bug hypothesis.** My first day-window was the wrong calendar day (UTC vs ET — exposed only by sync-cursor timestamps looking "stale"), and an exact-string phone `IN` against E.164 values silently returned empty and nearly became a wrong "no SMS thread" claim. `select now() at time zone 'America/New_York'` first; `regexp_replace(phone,'\D','','g') like '%<10digits>'`; verify any surprising empty join with a known-hit probe.
- **[S34] A seat-authored fix PRESCRIPTION is a hypothesis — trace it like code before dispatch (now PROMOTED to AGENTS.md Pitfalls).** Three instances in one session: my uniform touched-ref formula silently broke tag inheritance on first save (builder caught); my rule-4 zero-disagreement design reopened the household merge (delta-verify caught); my googleAddress compare suggestion would have failed the exact test scenario I named (builder caught). S33's #660 brief bug was the same class. Trace the consumer's exact behavior first, or mark the snippet intent-not-prescription — and delta-verify the fix round on money seams.
- **[S34] A review that accepts a "guarded/linked-only" claim must query the DATA RATE the guard excludes.** The #200 lenses accepted "push fires for linked contacts only" without asking how many rows were linked — the answer was 2 of 168, so the feature shipped ~inert until Jason's device round caught it. When a guard filters a population, count the population before accepting.
- **[S34] The never-chain pitfall applies to PIPED commands too — a pipe launders the exit code.** `git checkout ... | tail -1 && git merge` ran the merge after a FAILED checkout because tail's exit code masked it (harmless here — ff-only + wrong-branch aborted — but the same shape that wiped a ledger in S25). A state-changing command never chains after ANYTHING whose exit code isn't the real operation's.
- **[S52] Twice in one session, a merge-conflict `Edit` silently dropped a whole entry: `old_string` spanned an opening `<<<<<<<` marker plus a full body, `new_string` only restored the short pointer-note half.** Caught both times by my own follow-up grep before pushing, but I made the SAME mistake a second time in the same session right after fixing the first instance — didn't generalize the check into a habit fast enough. After ANY hand-resolved conflict: grep for stray markers AND verify entry/header counts, unconditionally, before treating the edit as done.
- **[S52] Started the session in the wrong working directory (an unrelated repo's worktree) — caught it in my own first message before acting, but it's a recurring surprise from how this machine's desktop app resolves a session's cwd.** Check cwd against what the task is obviously about before the first real action, every session.
- **[S51] I declared a slow background review agent dead on elapsed time alone and spawned a duplicate.** The original landed fine at 83 min (siblings took 5–8) and read the POST-fix tree, accidentally covering the delta. Probe the agent (SendMessage) before declaring it dead; elapsed-vs-siblings is not evidence.
- **[S51] I shipped a link to a route that has never existed** (traced-card → /training/<id>; only the wrap sweep caught the 404). Every href a new surface mints gets checked against the real route tree (find src/app/<path>) before commit — mandatory when no browser leg runs.
- **[S51] A safety CLAIM in a comment needs the same adversarial pass as code.** My seed-effect comment said the calibration effect couldn't clobber the seeded scale — true only on the first tick; the manual-override field invited exactly the edit that broke it (HIGH). Reason over the state machine's whole life, not its initial state.
- **[S48] A "show our work" / public-exposure query must gate on a POSITIVE committed status, never just exclude the bad ones.** My sample-home gallery filtered `non-test && non-dead`, which let raw `draft` quotes through — a random visitor's auto-created self-serve estimate could land on the PUBLIC marketing gallery (caught by the four-lens admin lens, pre-merge). Same class as the positive-seam-gate pitfall: negative filters silently leak the next thing that isn't explicitly excluded. Require `status ∈ {sent,viewed,approved,booked,…}`, not `status ∉ {dead}`.
- **[S48] For a TRUST/quality-facing visual ("show our real work"), reach for REAL data first — a fabricated stand-in undersells and burns round-trips.** My first sample-homes cut drew fake SVG-dot lights on stock photos; Naldo: "THIS IS TERRIBLE, reference the jobs completed." Pivoting to real completed-job designs through the real render engine landed immediately. Don't approximate the thing whose whole value is authenticity.
- **[S47] A test identifier that collides with a live record MUTATES real data — check before any prod-writing test.** Used Naldo's real phone in a partial-capture probe; GHL upsert matched his real contact by phone and overwrote its email/name. Before putting a real-shaped phone/email into a test that writes to prod (Supabase or a CRM upsert), query whether it already matches a live row/contact; a "test" value is not inert just because I labeled it one. Same family as task-text-is-hypothesis.
- **[S47] Read what a "test mode" actually suppresses before calling an outward action safe.** `?yll_test=1` only silenced a Telegram ping; the `new lead` + real-SMS enrollment ran regardless. Verify the flag's real gate in the code BEFORE telling the dev a send/charge/enroll is safe to drive.
- **[S47] A fix on a filtered/derived field is not done until an END-TO-END test runs the real load path.** The crew-photo portal-leak filter (`portalPhotos` dropping `source==='crew'`) passed an isolated unit test but was DEAD in prod because the loader (`toDesignWithPhoto`) stripped `source` before the filter ever saw it. A customer-facing leak that tsc + the unit test both missed; only the 21-agent audit caught it. Test the whole chain (loader → shape → filter → surface), not just the pure function.
- **[S50] I shipped a targeted fix off an incomplete enumeration and had to redo it.** I built the classic-page header fix as a hard-coded list of 12 page IDs harvested from the SITEMAP; the real population was ~40, because the Rank Math sitemap omits published pages (`/service-areas/glen-cove-ny/landscape-lighting/` was missing and was broken). Enumerate the full population from the AUTHORITATIVE source (the WordPress REST API, 97 pages) BEFORE writing the fix, and prefer a class-based rule over an ID list so pages added later stay covered.
- **[S50] I did not pin the session number at start, and it nearly collided.** By wrap, S47/S48/S49 were all held by concurrent sessions with open close PRs, ledger #192/#193 were already claimed, and the branch name I had created (`naldo/s48-close`) already existed on the remote as another session's PR head. Read BOTH session logs AND `gh pr list --limit 200` at session START, then pin the session number and ledger range.
- **[S50] I left the close-review lenses as the last background step and lost them.** Both review agents were still running when the process exited, so the standing close review produced no output and I verified the site myself instead. Run close-review agents early enough to land, or synchronously.
- **[S32] A flagged-deferred residual on a CUSTOMER path is a ledger row, not a PR footnote.** #653 explicitly flagged the multipart analyze upload as still-413-exposed and deferred it — it bit Jason live the NEXT DAY as a cryptic JSON parse error. If a review names a reachable customer-facing residual, open the ledger row (or fix it) in the same breath as the merge.
- **[S32→AGENTS.md] Sibling/cross-surface parity — PROMOTED to AGENTS.md Pitfalls 2026-08-05** (S32 TOCTOU siblings · S33 vault-notice states · S51 promote shipped without the CAS its sibling identify had in the same file). Read it there; don't re-derive here.
- **[S32] Shared-tree parallel builders cost real incidents (~5 in one day)** — promoted to AGENTS.md Pitfalls (worktree isolation, own npm ci, git-show-only reviewers). Don't re-derive; follow it.
- **[S30] Half-wired features, three flavors in one session (all wrap-caught):** a severity system that classified only 2 of the 4 money-moved reasons as critical; a wrapper migration (queueAttach) that left ONE direct call site (the promoted grep-count pitfall, live again); an API visibility field (`resurrected`) the UI never rendered (the promoted inert-fix pitfall, in miniature). Lesson: at the END of any cross-cutting change, grep the OLD symbol / enumerate the FULL reason list / wire the consumer — reconcile to zero in the same diff.
- **[S30] A label rename is cross-cutting:** renamed a job-status label on my surface without checking the OTHER renderer (Naldo's dashboard) — created a cross-surface terminology collision AND an ambiguity (his board already used the new wording for a different entity). Grep the display string across BOTH owners' surfaces at rename time; flag collisions then, not at wrap.
- **[S29] Wiring a previously-guarded path MUST update its safety comment in the SAME diff.** The match script's header still said `--live # (guarded — refuses until wired)` after I wired it — a misleading safety claim on a prod-writing script, caught only by the wrap review. Grep the file's own docs when a stub becomes real.
- **[S44] A CSS `display:none` hide is NOT a removal -- and a past session's "fixed" is a hypothesis.** S34 hid the old sticky with `.elementor-element-a0fa6d1{display:none!important}`; that rule was STILL live in the Customizer, yet the bar was rendering on a fresh logged-out NitroPack-purged page. Re-verify a CSS-level workaround is actually holding before trusting the note that says it was handled, and prefer deleting the element over hiding it.
- **[S40] Vercel's MASKED (Sensitive) env-value fields eat blind coordinate typing** -- the financing flag saved EMPTY on the first flip (caught only because flag-off renders nothing). Set masked fields with form_input on a DOM ref (it echoes the value back) and verify by the FEATURE, not the save toast.
- **[S25] Vercel deploys: verify the SHA by API, never trust "a deploy went READY".** One master webhook silently never spawned a deployment, and a dashboard Redeploy rebuilt the OLD commit — it looked live while prod served stale code. Dashboard Create-Deployment (from ref master) is the recovery lever; env-var changes need a fresh deploy to apply.
- **[S24] `gh pr merge --delete-branch` on a STACK BASE hard-closes the stacked PR — unrecoverable** (GitHub can't retarget or reopen once the base ref is gone; #451 had to be recreated as #456). Retarget the remaining stacked PRs to master BEFORE merging their base, or merge without `--delete-branch` until the stack is flat.
- **[S24] A derive effect that writes form state must respect REOPEN semantics.** My accessories-derive keyed on session state (`permanentSatLines`) that is NOT rehydrated on quote reopen — it would have clobbered saved counts with zeros/street-only. Gate such effects on the live session (mirror the footage effect's `satellitePreview != null` / hadRef pattern). Caught mid-build by asking "what does this do on a reopened quote?"
- **[S23] Supabase MCP does DML fine; only DDL (schema/migrations) needs the browser SQL editor.** Used `execute_sql` to read `app_settings`, DELETE the inert flag row, and INSERT a seeded test quote — all worked. Don't route data reads/writes through the browser SQL editor. (Supersedes the retired S13 read-only note.)
- **[S23] A post-merge restarted branch needs `--force-with-lease` to push, and fast-moving master will stale a PR mid-review — re-merge + re-gate the combined tree before landing.** #378 went 23 commits stale (Jason's #379 event-flag removal, mirroring mine) between green and merge; merged master in, resolved a `serviceType`/`isPermanent` prop collision by consolidating onto the shared prop, re-gated (vitest 1607) before merging. "Never merge stale" bites even same-day.
- **[S20] A big parallel fan-out WILL occasionally hit a transient server-side rate-limit (not your quota) — resume from the journal, don't re-run.** W5's first run lost 10/14 finders to rate-limiting; a resume replayed the 4 cached + re-ran the 10, and because their findings feed dedupe/verify/critic those re-cascaded correctly (the partial 3-HIGH re-adjudicated to the accurate 1-HIGH on the full set). Don't trust a partial workflow result; resume for full coverage.
- **[S19] Expanding shared link data → audit every downstream consumer first.** The sceneLinks twin-expansion (right, for portal toggling) silently fed twin ids into the photo-tag chip lookup → canonical rows mis-tagged. When a shared structure gains members, grep its readers before shipping.
- **[S18] Widening a shared/analyzer type → grep EVERY consumer + stale narrow duplicate up front.** The railing detection type had two parallel narrow copies (`/training/new`, `QuoteBuilder`) hidden behind `as` casts that tsc didn't flag; only the review caught them. Before shipping a type widen, list every UI that displays/edits it.
- **[S13] Map PR dependencies FIRST.** Given a batch to merge, check base branches (`gh pr view <n> --json baseRefName`) before touching anything — I began merging a *stacked* PR (#229) out of order and had to abort + re-plan bottom-up (#227→#228→#229).
- **[S13] Browser-tool quirks:** the Chrome `javascript_tool` REPL doesn't reliably await a long async loop (a Monaco poll returned `{}`) — use synchronous checks + explicit `wait`s. The Supabase SQL editor takes ~12–18s to mount Monaco; wait up front. And **batch browser actions** (`browser_batch`) instead of one screenshot/click/wait per call.

- **[S47] A plausible number is not a verified one — smoke money code on a REAL input, not a synthetic one.** The estimator's first live smoke used a real (but slightly-wrong) address; Google silently returned the TOWN centroid with a valid county, so we'd have quoted a house we never located — MASKED by the $1k floor as a tidy $900-1100. Every synthetic test passed. A geocode must resolve to a HOUSE (ROOFTOP/RANGE_INTERPOLATED + street number, non-partial) before you spend or quote. Also reinforces [S24]: every confirmed finding across all 6 self-serve PRs was in MY OWN new code, caught pre-merge.
- **[S47] Read the measured geometry the engine PRICES from; never sum the analyzer's free-text LABELS for a money decision.** I reported "satellite 110 ft vs street 105 ft" by summing AI text labels; both paths recompute to 105 from the polylines, and the bogus 110 briefly propped up a proposed +10% pricing buffer before I caught it.
- **[S47] Before trusting an accuracy/telemetry metric, ask "is the thing I'm measuring derived from the thing I'm comparing it to?"** My first self-serve accuracy metric bracketed the shown range around the same saved total, so every unedited send read as trivially in-range (~100% forever). Fixed by anchoring "verified" on an INDEPENDENT event (staff actually re-priced it).
- **[S57] MISTAKE, unresolved:** used em dashes throughout commit messages and docs despite the standing global voice rule forbidding them, rationalized by the surrounding docs already being full of them. Needs Naldo's ruling on whether the rule covers internal repo docs; the current inconsistency is worse than either answer.

- **[S69] I declared a schema blocker fixed after changing ONE of the three things that caused it.** `quote_lifecycle_events.quote_id` was `on delete restrict`, so I cascaded it and said done. Two other things on the same chain still refused the delete: the immutability trigger was `before update or delete` and a cascade delete fires the child table's row triggers, and `quote_event_outbox.event_id` had its own `restrict` behind it. All four lenses returned BLOCK and one reproduced it live. Lesson: a delete/cascade fix is not verified until the whole chain is traced or, better, run. Same family as the promoted grep-count reconcile rule, applied to FK chains and triggers.
- **[S69] I hand-wrote a schema constraint from memory instead of copying the authoritative source, and shipped a fabricated enum.** Repairing `FULL-SCHEMA.sql` I typed the `event_type` CHECK as seven values including `QuoteApproved` and `QuoteRevived`, neither of which exists; the real constraint has TWENTY-FOUR including `QuoteAccepted` and `QuoteCancelled`. On a database rebuilt from that file a customer clicking Approve would have hit a check violation. It landed inside the commit meant to fix drift in that very file, under a message claiming I had restored it faithfully. The fix was to EXTRACT the list programmatically and assert parity. Never retype a list you can copy; when a doc claims parity with a source, prove it by diffing, not by reading.
- **[S69] I chained `git add` after a fallible assertion and briefly staged a file containing conflict markers.** The resolution script asserted every row line started with `| 3`, which fired because the other side's block included rows 400-402. The assert correctly stopped the write, but the `&&`-chained `git add` in the same command ran on the unresolved file. Caught immediately, unstaged, redone. This is the promoted never-chain-after-a-fallible-step pitfall, recurring in a new place: the danger is not only a state-changing command after a failed cast, it is any command that CAPTURES state after a step that may not have produced it.
- **[S69] I proposed a fix that would have reintroduced the exact bug the code was written to prevent, and Naldo approved it before I caught it.** I offered reordering `deleteQuote()` so the quote row goes before the design cleanup. `designs.quote_id` is `on delete set null` and `deleteDesignsForQuote()` looks designs up BY `quote_id`, so quote-first orphans the row and strands the customer's house photo in storage forever, which is what the existing designs-first order and its comment exist to prevent. I read past that comment when proposing. Reverted and reported. A seat-authored prescription is a hypothesis even after the dev has approved it.

- **[S69 post-close] I broke the mark-a-row-off pitfall in the same session I wrote it into my own scorecard.** Row 434's guard merged in #1020 and the row stayed open; row 433 was marked shipped but never moved to the archive. The promoted rule says the close sync is the deadline, and I had quoted it hours earlier. Writing a lesson down is not the same as applying it: the fix is a mechanical end-of-session sweep of every row the session minted, comparing each row's own status text against the PR that closed it.
- **[S69 post-close] A ledger row I wrote while a PR was OPEN went false the moment the dev closed that PR, and I never revisited it.** Row 375 claimed staff now see a Timing pill. Naldo reversed the decision, #1009 closed unmerged, and the row still announced the feature. This is the report-not-intent pitfall with a time dimension: a row describing an in-flight PR is a PREDICTION, and predictions need re-checking when the PR's fate changes. Any row referencing an open PR gets re-read when that PR closes, merged or not.

## Sessions (newest first)

### S87 (Naldo), 2026-09-03: every printed QR code the company owns was dead, and the routing lived inside a former agency's unpaid GoHighLevel account. Rebuilt on infrastructure we own. PR #1186 open, worked concurrently with a desktop session that superseded half of it

- **NUMBER:** announced S86 off an insufficient check, corrected to **S87** via the ledger counter (S86 is Naldo's, used 2026-09-02). The machine-local self-assessment check could not be run in a remote container; that gap is stated rather than glossed.
- **THE DIAGNOSIS:** `link.yulelovelights.com` resolves to the SiteGround marketing site (`35.212.83.65`, same IP as apex and www), which has no `/qr/` route. It had been served by a GoHighLevel account opened by a FORMER AGENCY, now showing "Your payment method is missing". Van decals and business cards already in circulation, unrecallable. Two codes total, recovered from `Sites → QR Codes` before that account is purged.
- **#1186** serves `/qr/<slug>` from our own app, allowlisted signed-out in the same commit, verified by RUNNING it with the gate on plus three controls (the gate blocking `/inventory`; the allowlist line removed to prove it load-bearing; mutation probes on every guard).
- **THE WRAP REVIEW FOUND A HIGH IN MY OWN WORK** — three lenses converged on the card pointing at `/get-a-quote/`, which ledger row 272 already records as 301ing to the homepage since June. Also a prototype-key bug, an over-broad allowlist prefix (`#611` had removed exactly that pattern from `/estimate`), and zero coverage on the allowlist entry.
- **A DESKTOP SESSION OWNED THE SAME BRANCH AND WON ON THE MERITS.** It verified the old codes still resolve on `link.surgecrm.ai` BY REQUEST rather than screenshot, corrected three of my claims, and carried Naldo's product call: both codes land on the homepage, tagged apart by campaign, because reproducing the card's dead-end destination would have reproduced the outage. Its optional-catch-all beat my allowlist narrowing. I reset onto it and kept only `Cache-Control: no-store` + `X-Robots-Tag: noindex` (the route comment had claimed `force-dynamic` meant "never cached", measurably false).
- **OPEN:** rows **499** (the live cutover, gated on deciding what other legacy paths on that subdomain should do — today they serve a homeowner the operator login) and **500** (capture the GHL scan analytics before the unpaid account is purged; unrecoverable after).
- Gates at close: tsc 0 · lint 0 errors (23 warnings) · vitest **10337 / 591 files**, measured at `ba993926`. The branch kept moving after close (the desktop session pinned this session's two headers with a test in `7b9a6c9a`), so that SHA is where the numbers were taken, not the branch head — #1186 is a live lane owned by that session. Full detail: `docs/context/journal/S87-naldo.md`.


### S59 (Jason), 2026-09-02 — time tracking phases 1 to 3 built and live in one day: everyone's hours, each person's own record with the first editor office shifts have ever had, and recording what was paid against which shifts. 4 PRs merged. Close PR jason/s59-close

- **NUMBER:** S59, checked against Jason's fragments (S58 was the latest), the machine-local `feedback_self_assessment.md`, `gh pr list --limit 200` and every remote branch. Naldo ran S82→S86 on the same calendar date; numbers are PER DEV, which a wrap lens later misread, so the ledger counter now says whose S59 minted row 498.

- **OWNERSHIP FIRST, BUILD SECOND (#1177).** The plan's section 7 recorded Naldo's 2026-09-01 handoff of the P4P labor surface but deliberately left the AGENTS.md table alone, because an ownership change is a policy change. Jason relayed Naldo's written ack on 2026-09-02 — *"yes jason will be taking over time tracking and setting up the p4p system"* — and the row moved then, in its own PR so the policy change was reviewable apart from the build it unblocked. The process lens asked for the ack to be QUOTED rather than paraphrased, matching the inbox row's own precedent; it is.

- **PHASE 1 (#1176):** `hoursSummary.ts` + a table on `/admin/time-tracking`, and a `Time tracking` row in the account menu. First real caller of `paidSecondsForShift`, which had shipped with 2,849 lines of tests and zero consumers. Hours only, no money. Midnight auto-closes are counted AND flagged, because 5 of 27 prod shifts are sweep-closed averaging 14h against a normal 4h 40m.
- **PHASE 2 (#1178):** `personHours.ts` + `/admin/time-tracking/[crewMemberId]`: shifts by ET day over rolling ranges, the edit and void controls, and the manual-changes trail. **The first editor office shifts have ever had** — the two clocks page lists field shifts only, so an office clock-out nobody made could not be corrected anywhere. No guard was relaxed to do it: `adminUpdateShiftTimes` and `adminVoidShift` never carried an `is_office` condition, the rows simply had no screen. Closes row **473**.
- **PHASE 3 (#1179):** `shiftSettlements.ts`, a pay panel, a new admin route, a migration, and the paid-shift guard. Closes row **459**, parked since #1062 waiting for a paid marker to exist.

- **THE DECISION THAT SHAPED PHASE 3, and the measurement behind it.** Three options were put to Jason. He chose: **the tool records payments, it does not calculate them.** That is not a shortcut — overtime has no formula here (row 285, parked pending an accountant) and the case is already live: Naldo logged **50h 55m in the seven days to 2026-09-02**, so any flat-rate figure would be wrong for a real week sitting in the data. The typed amount is the money record; hours × rate is a stamped reference, and **nothing asserts the two agree**. That gap is the feature: overtime, a rounded-up cash payment, an advance, a deduction agreed off-system.

- **REVIEW, scaled by tier.** #1176 two lenses (1 HIGH, 3 MED) · #1177 one process lens (2 MED, 1 LOW) · #1178 two lenses + delta-verify (0 HIGH, 3 MED, 7 LOW) · #1179 the FULL four lenses + delta-verify (1 HIGH, 5 MED, 4 LOW). Every finding fixed or dispositioned; **23 mutation probes** across the session, each failing exactly the test that names it, every restore verified.
  - **The #1179 admin HIGH is the one worth remembering.** The Hours list flags a sweep-closed shift in amber; the Pay panel below re-listed the SAME shifts with that warning DROPPED — in the one place where ticking a box locks those hours into a payment. Live, not hypothetical: 5 of 27 real shifts.
  - **The delta-verify proved a gap by MUTATION that reading had missed:** removing the crew's "your payment was undone" notice broke no test at all, while the matching notice on the record path was pinned.
  - **A guard that breaks other tests is a guard that fires.** Adding `assertNotSettled` broke 10 existing tests with `unexpected table shift_settlement_lines` before the mock knew that table — free proof it is reached on both write paths.

- **THE GAP JASON CLOSED HIMSELF.** Phase 2's office write path shipped unproven: the control rendered and no code refused it, but no save had been watched land, because that writes real payroll. Jason then corrected two office shifts through the page. Prod confirms both: his own 21 Aug shift went from 14h 10m (a midnight-sweep placeholder) to 8h 09m, and Khaye's 31 Aug from 21:05 to 19:00, each stamped `manual_by` with his name and email and each writing a `shift-manual-edit` row carrying the exact before and after times. The dev's device round as a review lens, again — and it proved the row-473 trail on real data at the same time.

- **STILL UNPROVEN, on record:** no settlement has been recorded end to end. Every guard and both write paths are covered against a mocked database that models the real partial unique index, but the live wiring has not been watched work. A $1.00 record-and-remove was offered and not taken up.

- **Ledger:** 459 and 473 marked SHIPPED and archived. **498** minted (a cross-person "who is owed" view, recorded not built — and note the constraint that makes it non-trivial: while overtime is unruled the tool can honestly show unpaid HOURS, never an unpaid dollar figure). Counter 499.

- **Migration** `2026-09-02-shift-settlements.sql` applied directly per the safe/additive allowlist and verified by query — columns, and the partial unique index really is `WHERE (voided_at IS NULL)`. `FULL-SCHEMA.sql` updated in the same commit and asserted line-for-line against the migration.

- **THE WRAP REVIEW EARNED ITS KEEP, on a file no per-PR lens could have seen.** Two lenses ran (integration + customer, the latter because the combined diff touches `AGENTS.md`, a SHARED path). Customer: PASS, 0 findings — no homeowner surface touched, `/crew` still money-free, both new routes correctly OFF the public allowlist, RLS with zero policies correct for service-role-only tables. Integration: **1 MED, and a real one** — `/admin/fleet/clocks` was in NONE of the four PRs' diffs, so a paid shift still showed live Edit and Remove links there and the lock was discovered by clicking into a 409. The promoted guard-and-copy pitfall, on a sibling surface. Not reachable today (zero settlements, measured) but live from the first payment, so fixed rather than deferred: **PR #1183**, open at close.
  - That fix surfaced a second thing the tests had been hiding: the fleetDay mock's chain had no `range`, which `settledShiftIds` pages with, so the new read threw into its own fail-safe on EVERY fleetDay test while the suite stayed green. Adding `range` is what let the new tests assert `errors` is empty at all.

- Gates at close: tsc 0 · lint 0 errors (23 warnings) · vitest **10174 / 581 files** on `91d57e87`; **10263 / 586** on the #1183 branch, which is based on the newer master.
- **Master:** `91d57e87` when phase 3 merged and prod was verified READY on that exact SHA (`/admin/time-tracking` 307s to login signed out). Naldo then merged #1167, #1172 and #1083 DURING this wrap, so master is `00f7035d` at close and the close PR is based on that.

### S58 (Jason), 2026-08-29 → 09-02: two of three device asks shipped (the third already existed), the ledger cut from 123 to 90 rows behind a reader/linter, a 15-section time-tracking plan, and a two-day silent staff-alert outage found in GHL, not code. 15 PRs merged. Close PR jason/s58-close

- **NUMBER:** S58, checked against Jason's fragments, the local self-assessment and `gh pr list`; Naldo ran S81→S85 alongside.
- **SHIPPED:** #1115 grouped railings re-place across photos + numbered/thumbnailed twin picker with persisted ordinals · #1116 `scripts/ledger.mjs` · #1117-#1123 the ledger sweep, counter cut, epic close/split, two duplicate numbers recorded · #1118 one-session-per-lane activated · #1120 rows 259/415 ruled + a Pitfalls bullet · #1156-#1165 the time-tracking plan · #1174 the alert fallback, marker and DND health check.
- **THE ADVERSARIAL DELTA-VERIFY EARNED ITS KEEP TWICE.** On #1115 it found that merely OPENING an approved design popped "Changes here are NOT saved" — the new ordinal backfill scheduled a write on a locked scene — after four lenses had passed the fix round. On #1174 it found a never-cleared race timer on the common path. Both fixed before merge.
- **THE INCIDENT:** GHL's own record settled it — `dndSettings.Email = active`, code 105, "User clicked on the unsubscribe link", contact modified 2026-08-31 11:11:56Z, last accepted app email 02:29:51 that morning. Five hypotheses died on measurements first (see the S58 fragment). Alerts re-sent internal-only; row 497 holds the ~13 siblings.
- MISTAKES: see the scorecard.
- Full detail: `docs/context/journal/S58-jason.md`. Master at close: `fbd9c7a2`; gates in the close PR body.

### S85 (Naldo), 2026-08-31 to 09-01, every call gets an automatic HighLevel note and internal comment, shows up on the customer profile and the quote builder, and posts within 20-25 minutes instead of an hour. 8 PRs merged and live, 1 fix-round PR open. Close PR naldo/s85-close, post-close delta naldo/s85-comment-renumber

- **NUMBER:** the local worktree list surfaced a live peer session with a branch literally named `claude/wrap-s84-fixes`, a real concurrent claim on S84 invisible to `gh pr list`/master/archive/remote branches, since it was local-only and unpushed. Took S85 instead.
- **SHIPPED:** #1109 the core note worker, #1112 fixed 6 real prod notes that posted with a null id, #1131 the internal comment plus a customer-profile call-notes panel, #1132 a one-off comment backfill, #1138 a call-notes drawer on the quote builder, #1140 the pipeline cadence cut from hourly to 15 minutes, #1147 the wrap's own fix round, #1152 the drawer button sized up on Naldo's ask.
- **THE WRAP REVIEW FOUND A REAL HIGH IN ITS OWN SESSION'S MERGED WORK.** The customer profile's call-history panel claimed to show every HighLevel id a customer ever carried, but structurally collapsed to one id for a customer merged across two different HL ids, showing less history than the quote-builder drawer for the same person. Fixed in #1147 with a shared resolution function, alongside a MED (a comment-marker-write failure could let a later backfill double-post) and 3 stale cadence references. Every fix mutation-probed.
- **A SECOND POST-CLOSE REVIEW FOUND A BUG IN THE FIRST CLOSE'S OWN FIX.** The S84 to S85 renumber used a blind find-and-replace that corrupted the very paragraph documenting the collision, turning a factual reference to the OTHER session's S84 claim into a self-contradictory sentence about S85, plus mislabeling three code comments. Fixed properly, not just patched: see the scorecard and Mistakes entries above.
- MISTAKES: the blind renumber replace, both entries above.
- Full detail: `docs/context/journal/S85-naldo.md`. Gates at close: tsc 0 · lint 0 errors (22 warnings) · vitest **9750 / 564 files**. Gates after the post-close delta: tsc 0 · lint 0 errors (22 warnings) · vitest **9804 / 567 files**. Master at post-close delta: `66303e50`.
