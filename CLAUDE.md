@AGENTS.md

# Assistant session journal & self-review (running)

> Naldo asked me (the assistant) to keep this: a running log of what ships each
> session, an honest tally of what I get right vs. wrong, and a short retro so the
> work compounds instead of repeating mistakes. It loads every session via
> CLAUDE.md — **read the Scorecard first; it's the actionable part.** Newest
> session on top. Keep per-session detail short (the full narrative lives in
> `docs/context/session_log_naldo.md`); archive old sessions here once it grows.

## Running scorecard (cumulative — skim before starting work)

**Keep doing (these worked):**
- Branch off **FRESH `origin/master`**, never the worktree's stale base — first confirm the spec/plan commits are actually present (mine weren't until I rebased).
- Re-ground the exact lines before planning, **then independently verify the recon** — a fast read-agent missed a real data leak and cited a stale file; my own grep caught both.
- TDD + run all three gates (`tsc · lint · vitest`) **after every phase**, not just at the end.
- Run the adversarial review before merge — and **disposition** each finding (fix / accept-as-parity / process). Don't blindly "fix" a pattern that matches existing prod code.
- Apply migrations to prod **before** merge, and reason about ORDER per migration: a column-add is **migration-first** (column must exist before the code writes/reads it); enabling RLS is **code-first** (ship the service-role switch first so RLS can't break the old anon code).
- "**Never merge stale**": re-merge master + re-gate **every** PR before merging; check the cross-cutting logical interaction, not just text conflicts.
- Verify safety before an irreversible prod op (grepped for bare-anon data paths before enabling RLS; smoke-tested the live app after).
- Surface the high-risk / council-worthy / shared-file calls and confirm outward-facing actions instead of rubber-stamping.
- **[S18] Verify UI in the ACTUAL browser** — tsc-green ≠ correct. A client/server import-boundary break (#52 dragged the server-only `sharp` chain into the client bundle) and a HIGH `/training/new` railing ship-blocker BOTH passed tsc (an `as` cast laundered the type) and surfaced only in-browser + in the adversarial review. **[S19 reinforced 3×]** — Jason's device rounds caught a tag-expansion leak, a garland-trace gap, and a strip-placement miss, all invisible to tsc/tests.
- **[S19] Checkpoint cadence for epics** — slice a big feature into 2-PR chunks → dev device-verifies → merge bottom-up → next chunk. Beat both "one giant PR" and "gate every PR" on #13 (6 PRs, 3 checkpoints, feedback reshaped each next slice; stacks never grew past 2 branches; relays only on approved editor-core).
- **[S19] Scope a big epic with a recon workflow BEFORE the plan** — the 6-agent #13 recon + completeness critic caught the `maybeSingle()` silent-empty-materials trap (killed the N-designs architecture before any code) and 5 wrong claims via cross-verification.
- **[S19] Mechanical prompt/text moves → prove byte-identity** — extracting shared blocks out of the quoting SYSTEM_PROMPT via a substring-move script + an eval-and-compare check made "the prod prompt didn't change" a fact, not a hope.
- **[S20] Model-routing policy pays — Sonnet builds, Opus reviews/orchestrates (the seat), Fable only-if-asked.** The whole #110 fix marathon (~19 PRs) ran Sonnet-build/Opus-review — dramatically cheaper than S19's Opus builders, same quality, because the seat reviews every diff + integrates. Route deliberately, don't default everything to the top tier.
- **[S20] For a fix BATCH, integrate locally + gate the COMBINED tree BEFORE any merge — never-stale for parallel PRs.** Every wave's fixes were built by parallel worktree agents on disjoint files, then merged into one int branch, gated (tsc·eslint·vitest) as a set, and only then landed PR-by-PR with a real-master final gate. Caught composition issues a per-PR gate can't; the recurring `googleMaps.test.ts` two-agent conflict was resolved once, cleanly.
- **[S20] Write a workflow script to a FILE + `node --check` before launching.** A W4 workflow died on a missing `)` in a `.map()`; a W5 workflow was fine. The habit: Write the script, `node --check` (top-level `return` is the only expected "error"), then `Workflow({scriptPath})`. Cheaper than a failed launch.
- **[S20] Audit-workflow shape that worked 6×:** dimension finders (per sub-area × lens) → semantic dedupe → **adversarial verify every finding** (refute-lens for bugs, real-at-this-scale for perf, payoff-lens for refactors) → completeness critic. Pre-load intentional-design context so finders don't "discover" deliberate behavior as bugs. Dedupe against a seeded known-findings ledger so waves don't re-report #80.

**Fix going forward (mistakes I've made):**
- **[S23] A logic-layer E2E is NOT a substitute for the in-browser pass on a customer-facing money vertical — and the LLM council will call it out.** Event lighting shipped to prod (merged + flag removed) verified only by driving `calculateEventQuote → portal adapter` in-process, because the remote container has no backend creds. The council (unprompted) flagged this as the #1 risk: unit-math correctness says nothing about portal render / e-signature / email-send / approve. When the browser leg genuinely can't run in-container, say so loudly, stage a rollback lever (a revert PR / keep the flag) BEFORE the go, and get a human to run create→send→portal→approve before the vertical is relied on. Don't let "gates green + logic E2E" read as "verified."
- **[S23] Removing a feature flag removes your instant rollback lever — replace it, don't just delete it.** Deleting `eventEnabled` (Naldo's "always live") was correct product-wise, but it also deleted the one-toggle kill switch hours after go-live. When you delete a launch flag, name the replacement rollback path (revert-the-PR) in the same breath.
- **[S20] A big parallel fan-out WILL occasionally hit a transient server-side rate-limit (not your quota) — resume from the journal, don't re-run.** W5's first run lost 10/14 finders to rate-limiting; a resume replayed the 4 cached + re-ran the 10, and because their findings feed dedupe/verify/critic those re-cascaded correctly (the partial 3-HIGH re-adjudicated to the accurate 1-HIGH on the full set). Don't trust a partial workflow result; resume for full coverage.
- **[S19] A cross-cutting stamp/behavior across N similar code sites → enumerate EVERY path, then grep-verify the count.** The photoId stamp covered 13 of 14 item-creation sites; the missed one (garland TRACE mode's own commit fn) shipped a real bug Jason caught on device. The recon listed "12 sites"; trust the grep, not the list.
- **[S19] Expanding shared link data → audit every downstream consumer first.** The sceneLinks twin-expansion (right, for portal toggling) silently fed twin ids into the photo-tag chip lookup → canonical rows mis-tagged. When a shared structure gains members, grep its readers before shipping.
- **[S18] Widening a shared/analyzer type → grep EVERY consumer + stale narrow duplicate up front.** The railing detection type had two parallel narrow copies (`/training/new`, `QuoteBuilder`) hidden behind `as` casts that tsc didn't flag; only the review caught them. Before shipping a type widen, list every UI that displays/edits it.
- **[S13] Map PR dependencies FIRST.** Given a batch to merge, check base branches (`gh pr view <n> --json baseRefName`) before touching anything — I began merging a *stacked* PR (#229) out of order and had to abort + re-plan bottom-up (#227→#228→#229).
- **[S13] Trust the documented gotcha immediately.** The Supabase MCP is **read-only** (no DDL) — go straight to the Chrome SQL editor for migrations instead of trying `apply_migration` first and eating the error. (It's in `project_apply_migrations_via_browser.md`.)
- **[S13] Browser-tool quirks:** the Chrome `javascript_tool` REPL doesn't reliably await a long async loop (a Monaco poll returned `{}`) — use synchronous checks + explicit `wait`s. The Supabase SQL editor takes ~12–18s to mount Monaco; wait up front. And **batch browser actions** (`browser_batch`) instead of one screenshot/click/wait per call.

## Sessions (newest first)

### S23 (Naldo) — 2026-07-04 — EVENT LIGHTING went LIVE (#344 merged + #379 removed `eventEnabled`) · #58 inbox rows actionable (#380) · #116/#117 recon + council review (master `746fbcf`→`e91b678`)
**Event go-live session.** Re-synced the stale event PR #344 onto fresh master through TWO advances (resolved event↔permanent portal/settings seam conflicts + the #377 `permanentEnabled`-removal conflicts), hand-verified the money seams, **merged #344**, then per Naldo **removed the `eventEnabled` flag (#379)** so event is always-on. Verified event via a **logic-layer E2E** (pricing→adapter: rates/accessories/bistro/3-dates/holiday-regression all green) — **NO in-browser E2E (no container creds → the #1 open risk)**. Built #58 In-the-works actionable rows (#380 draft — `ReplyComposer` extraction + inline reply + Followed snooze; optimistic, no refresh endpoint). Parallel Explore recon on #116/#117 → ledger updated. **Ran the LLM council** to review the session's work: verdict → browser-E2E the event flow before relying on it, keep a revert-#379 rollback lever, ship **#116 rebook-only first** (defer re-send), **defer #117** (consider extracting a vertical abstraction before a 4th copy-paste vertical), the cross-sell "matrix" is the north-star not this-week.
- **Did right:** never-stale re-sync + re-gate before every merge (through 2 master advances); hand-verified auto-merged money seams (3-way dispatch, approve fee-forcing) not just the text-merge; stopped for Naldo's explicit go on both prod merges; mirrored #377's exact flag-removal pattern for #379; recon-before-build on all 3 follow-ups (didn't build the FUTURE #117 epic blind); ran the council on the shipped work and let it change the #116 call (rebook-only).
- **Friction / open:** couldn't run the in-browser event E2E or flip/inspect prod (Supabase MCP + `send_later` + `AskUserQuestion` all hit approval/permission-stream errors in this remote session) → surfaced ready-to-run SQL + options instead of stalling. **#380 (#58) still a draft awaiting merge-go; event browser-E2E outstanding.**

### S20 — 2026-07-03/04 — #110 FULL-TOOL QUALITY AUDIT: planned + W0/W1/W2/W4/W5 audited & FIXED, W7→Naldo handoff (master `d2c8027`→`746fbcf`)
**One conversation = a whole audit-and-fix epic.** Scoped the 8-wave #110 quality audit (plan-critic-lite'd → 2 BLOCKERs caught pre-wave-0), then audited AND fixed **5 of 8 waves in-session**, handed 1 to Naldo. Gates **1695 → 1973** vitest (tsc 0 · eslint 0 throughout). Model-routing policy (Naldo's PR #335) adopted + followed: Sonnet-build / Opus-review / Fable-untouched.
- **Shipped LIVE (~19 fix PRs + 6 audit/docs PRs):** **W0** scoping (manifest 648 files, charter, dedupe ledger) · **W1 money** 79 findings, CRITICAL W1-001 (invoice billed full `result.total` not the approved selection) + 4 HIGH + 22 more fixed · **W2 data** 36 findings, HIGH W2-001 (rebook dropped `extra_photos`) + W2-002 (rebook dropped `is_test`) all fixed · **W4 portal** 34 findings, W4-003 HIGH (blank e-signature on approve) + gallery/a11y all fixed, browser-verified · **W5 AI/training** 30 findings, HIGH axis-aware ppf + **token-cost lens** (prompt caching, image downscale, max_tokens) all fixed, **live-analyze verified** · **W7 Naldo** 96 findings → handoff doc (3 trial-critical HIGHs; no PRs in his lane).
- **Did right:** plan-critic caught the coverage-manifest + Naldo-lane-CRITICAL BLOCKERs before any wave; adversarial-verify + dedupe kept findings real; **model policy followed to the letter** (cheap fix marathon); integrated + gated the COMBINED tree before every merge (never-stale for parallel batches); caught the S18 `sharp` client/server boundary risk (was `import type` → safe); browser + live-analyze verified the risky changes; every open item needing Jason captured in `docs/audit/NEEDS-JASON-2026-07.md`.
- **Friction:** a W4 workflow parse error (fixed → write-to-file + `node --check` habit); W5's first run lost 10/14 finders to a transient server rate-limit (resumed from journal); two dev-server restarts for browser/live checks.
- **Open for Jason (→ NEEDS-JASON):** decisions (W2-026 · package savings · W5 downscale quality) · device-checks (W4-003/013/002 + W5 downscale/cache + railing AI + gallery) · loop Naldo. **NEXT (S21): W3 (dense files) + W6 (routes) audits — both heavy, fresh budget** — then the W1 fix-later backlog.

### S19 — 2026-07-02 — #53 training revamp · #54 completed-install analyzer (council'd) · #13 MULTI-IMAGE QUOTING COMPLETE (12 PRs, all LIVE)
**Shipped to prod (master `d2c8027`, 12 PRs merged + device-verified):** **#53** /training/new revamp — ft-reset bug + field/slot cleanup + `tree_bush` tag (PR #307) · **#54** completed-install analyzer mode — dedicated night prompt + shared ROOFLINE_TRACING_RULES/OUTPUT_JSON_SCHEMA consts, quoting prompt byte-identical (PR #308, council-reviewed → #109 raised) · **#13 multi-image quoting END-TO-END** — 6 PRs (#318/#319/#321/#322/#323/#324) in 3 checkpoints: `extra_photos`+`photo_title` prod migrations, editor photo tab strip (ported design-tool UX), staff photo-tag chips, **linked twins** (stamp + billing-link; skipped in projection/materials/fulfillability; sceneLinks twin-expansion), SV-vantage grab, portal hero-strip-below-price + reprise arrows + "Every angle" gallery (🧪 trial kept), admin thumbnails; vocabulary-only relays `593c115`+`a90b69a`. Gates **1445 → 1695** vitest.
- **Did right:** scoped #13 with a 6-agent recon workflow + completeness critic BEFORE planning (killed the N-designs architecture on the `maybeSingle()` materials trap); locked every fork with Jason via 2 Q&A rounds before code; **checkpoint cadence** (2-PR chunks → device-verify → merge bottom-up) kept feedback cheap — his 3 catches (tag leak, garland trace, strip placement) each cost one small fix round, not a rework; proved the #54 prompt refactor byte-identical mechanically; council on #54 produced 2 real refinements + a principle ("AI drafts, humans label") now in the ledger; restored every staged test mutation (SENT quotes left pristine); never-stale re-gates against Naldo's ~44 mid-session commits, zero conflicts.
- **Mistakes / friction:** the garland-TRACE commit path was the 1-of-14 stamp site I missed (recon said "12 sites" — trusted the list over a grep-count); the sceneLinks twin-expansion leaked into the tag-chip lookup (didn't audit the new data's readers); Chrome-extension automation can't reach localhost on this machine (pivoted to curl + the preview browser); the Supabase SQL editor ate two blind typing attempts before the click→settle→type→screenshot-verify sequence stuck.
- **Do better:** for any cross-cutting change over N similar sites, `grep -c` the pattern and reconcile the count against the edit list before gating; when a shared structure gains members (link expansion, id threading), grep its consumers the same hour; keep proving UI in the dev's hands early — all three S19 bugs were sub-30-minute fixes because checkpoints caught them fresh.
