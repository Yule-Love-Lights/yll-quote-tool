<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Default coding practice — the Karpathy guidelines

Adopt these four principles by default when writing, reviewing, or refactoring **non-trivial** code (use judgment on trivial one-liners / typo fixes — don't over-apply rigor). Full detail in the `karpathy-guidelines` skill (`.claude/skills/karpathy-guidelines/SKILL.md`).

1. **Think before coding** — state assumptions; if multiple interpretations exist, surface them instead of picking silently; push back when a simpler approach exists; stop and ask when something's unclear.
2. **Simplicity first** — minimum code that solves the problem; no speculative features / abstractions / config / error-handling that wasn't asked for. If 200 lines could be 50, rewrite it.
3. **Surgical changes** — touch only what the request requires; match existing style; don't refactor or reformat unrelated code; flag unrelated dead code rather than deleting it; clean up only orphans your own change created.
4. **Goal-driven execution** — turn tasks into verifiable success criteria (e.g. a failing test → make it pass) and loop until they're met; state a brief plan for multi-step work.

**Verification before calling it "done" (scale it to the change).** After shipping a feature/fix: run the full gates (`tsc · lint · test`) green, then a self-review **sized to the risk**. Small change → a critical re-read of your own diff (watch for unreachable branches, a stale doc/route reference, a half-updated registry). **Customer-facing or risky** change → a full adversarial review (multi-agent), then disposition each finding. Don't fire a heavy review on a typo; don't ship a customer-facing change on a glance.

**Confirm a feature variant's exact scope before editing.** Adding a variant (a light pattern, a mini-light / spritzer type, a surface) → pin down *which* bulb types / surfaces / cases it applies to first; don't apply it broadly across all types unless asked. This is principle #1 in our domain — the #92 light-patterns build went smoothly precisely because the scope (minis vs spritzers vs C9, whole-house vs per-item) was nailed before any code.

**Pitfalls we've hit — don't repeat:**
- **The session number is fixed when the CONVERSATION starts and never moves, however much the work changes shape. Post-close work in the same conversation is a DELTA on the same log entry, never a new session block.** Before claiming any number, check `gh pr list --limit 200` AND every unmerged `*-close` branch — another session may already hold it. Reason: promoted 2026-08-19 after S58 continued past its own close and relabelled the ongoing work "S59" and then "S60" across commits, ledger rows and docs — while a genuinely separate session already held S59 (the 8-day copilot outage, PR #810). That collided two sessions' work under one number and took a full ledger sweep to unpick. This is the same class `session_log_naldo.md`'s header has warned about since S38 (S39/S40, S41/S42, S47's three siblings, S53's near-miss); the new part is that it can happen from INSIDE a single conversation, not just between concurrent ones.
- **Adding a new user population to a shared auth store means auditing EVERY consumer of the role primitive, not just the code you wrote.** Reason: 2026-08-19, crew logins were added to the operator auth store with `getOperator` and the perimeter both correctly hardened — and a crew→admin privilege escalation shipped anyway through `PATCH /api/admin/users/[id]`, an untouched route that reads role via `roleOf`, which collapses every non-admin role (including 'crew') to 'operator'. The new population made an existing, correct-looking route unsafe. Grep every consumer of the role helper and ask what each does with the collapsed value.
- **Before putting a command matcher in FRONT of an existing one, grep the words it claims.** Reason: 2026-08-19, a crew time-clock parser claimed `help` / `commands` / `?` — the existing bot's own help words — and because it ran ahead of the dispatcher, every user in every chat got crew help instead of the real help. The author had reasoned carefully about running ahead of the address-gate and documented that tradeoff, but never checked whether the vocabulary was free.
- **A PR body / commit message is a REPORT, not a statement of intent: before writing "X is documented", "X is enforced", "X is covered", or "the tests prove X", re-open the file and read the words, or run the thing that would fail if the claim were false.** Also: a test NAME is a claim that ships as documentation — if it says always/never/every, either prove the general case or rename it to the specific one; and when you edit a paragraph in a living doc, re-read the WHOLE paragraph for claims this session just falsified (the edit window is exactly where staleness hides, because touching a line creates false confidence you have read it). Reason: promoted 2026-08-11 (S58) after THREE instances in a single session, every one caught by the wrap review rather than the author — a lint rule described as "the only door" that bracket notation walked around, an ownership constraint described as "written into its doc block" that was not written anywhere, and a test named "the two ALWAYS sum back to the envelope" that held only for whole-second inputs while the real millisecond case was broken. Same session also shipped two live docs contradicting code written an hour earlier in that same session (`nothing reads this flag today` about a flag it had just built a reader for; a merged PR still described as awaiting a merge decision). Adjacent to the "a seat-authored fix prescription is a HYPOTHESIS" pitfall below, but distinct: that one is about prescribing without tracing, this one is about REPORTING without checking. **RECURRED 2026-08-19** on the test-name half: a DST test named "handles the spring-forward day" computed the midnight AFTER that day — an ordinary day — so it passed while the real transition-day case was broken and underpaid/overpaid by an hour twice a year.
- **Ledger numbers: the "Next free task #" counter on master is necessary and NOT sufficient — check open PRs and UNMERGED branches for numbers already claimed before taking one.** Reason: S58 hit three near-collisions in one session (the counter was mid-flight inside an open PR; a concurrent session then claimed a 17-row block; an unmerged branch `naldo/257-slice1-followups` already held the next two numbers while master's counter still offered them). Declining to claim a number while the counter was in flight is the only thing that prevented a fourth. This is the same collision class already recorded for SESSION numbers in `session_log_naldo.md`'s header.
- **Branch BEFORE you edit.** Create your `jason/`/`naldo/` feature branch *first* — never start editing on `master`'s working tree (even uncommitted), then scramble to move the changes onto a branch later.
- **Any NEW customer-reachable route or Vercel cron must be added to `operatorGate`'s allowlist in the SAME PR, and verified LOGGED OUT (or, for a cron, with its `CRON_SECRET`) — never as a logged-in operator.** With `AUTH_GATE_ENABLED` on in prod, `src/proxy.ts` default-denies every path not in the allowlist BEFORE the route runs: a missing cron 401s before it ever reaches its own `CRON_SECRET` check, and a missing customer page silently serves the operator LOGIN SHELL instead of the page. It hides in every test where you are signed in, because an operator's own session sails through the gate. Add the path to `PUBLIC_API_EXACT` (exact match, method-scoped if it mutates) or the page allowlist, and confirm on a signed-out request. Reason: recurred with real prod incidents — partial/abandoned-lead capture 401'd (S42), the morning digest cron silent (S44/S47, fixed #675), and the #195 non-lead form pages served the login shell on yulelovelights.com (#682).
- **Trace the full side-effect chain before presenting an approach** — especially for subtle gesture / shared-editor-core changes. (e.g. a `dragstart→stopDrag` plan had to be reversed mid-build once it was found to trigger `dragend→bake→mid-draw redraw`; verifying the whole chain first avoids approving-then-reversing.)
- **After a `git checkout` / branch switch, `Read` a file before you `Edit` it** — the harness requires a fresh read post-switch; doing it proactively avoids failed-edit retries.
- **Read the giant `task_ledger.md` surgically.** Its rows are enormous single lines — grab a narrow line-range or `grep -o` just the bit you need; don't pull whole sections into context (it's the biggest avoidable context drain in a long session).
- **Don't open a separate PR for every tiny docs/ledger tweak.** Batch the bundle-able ones into one PR or fold them into your session-close sync; only land a docs change on its own when something downstream needs it first (e.g. a rule before the work it governs).
- **Cross-cutting change across N similar sites: `grep -c` the pattern and reconcile the count against your edit list before gating.** Reason: the photoId stamp covered 13 of 14 item-creation sites; the missed one (the garland TRACE commit path) shipped a bug only a device check caught (S19).
- **Service-type / vertical seam gates are positive-match (`=== 'holiday'`), never negative (`!== 'permanent'`).** Reason: a negative gate silently hands every FUTURE vertical the old behavior; event inherited holiday's confirmation page, rush/takedown add-ons, and scheduling windows across 5 seams (S23/S25).
- **Migration ORDER is a per-migration decision.** A column-add ships migration-first (the column must exist before code reads or writes it); enabling RLS ships code-first (the service-role switch lands before the lock, so RLS can't break the old anon paths). Reason: "additive + nullable tolerates pre-apply" proved false the moment code SELECTed the column (S16).
- **Migration APPLICATION default (Naldo, 2026-08-07, both devs' assistants): apply a safe/additive migration to Supabase directly (MCP `apply_migration`) as soon as it's ready — don't wait for the PR to merge, don't ask each time.** This is an ALLOWLIST, not a denylist — only these are "safe/additive": `create table if not exists`; `add column ... nullable/with a default` (population-agnostic — an existing table with real rows is exactly the normal case for this one, that's the whole point of "nullable/with a default"; a `NOT NULL DEFAULT` add-column backfilling every existing row is still on this list, that's what the default is for); new indexes on a table with no rows yet, or a partial/unique index that can't collide with existing data; RLS-enable-with-zero-policies **on a brand-new table only** (never on an existing table — that's still the code-first sequencing in the bullet above, because RLS-enable on a table with live anon reads can break them); and idempotent seed inserts guarded by `where not exists` **for adding new rows** (the guard does NOT make re-running a migration a way to correct an already-seeded value — if a seeded value is wrong, fix it with a direct UPDATE, not by editing the seed file and re-applying it; say so in the migration's own comment). Anything not on this list — a CHECK-constraint change, a column type change, a column drop, an RLS-policy change — defaults to asking first, full stop, REGARDLESS of whether the table has rows; the list is exhaustive, not illustrative. **"DDL touching a table with real rows" below means structural/destructive DDL specifically (type changes, drops, constraint tightening on an existing column) — it does NOT retroactively un-allow the ADD COLUMN case named safe two sentences up. A S57 review caught this rule contradicting itself on the exact migration it was written to describe (`jobs.rates_are_placeholder ... not null default true` on the live, populated `jobs` table) — this parenthetical exists so it never reads ambiguous again.** Verify the applied SQL is byte-identical to the reviewed migration file first (a live branch can drift from disk), then confirm the result with a real query, not the tool's own success message. **Still asks first, every time, no exception:** anything that drops a column/table, alters or deletes existing populated data (not merely adds a defaulted column to it), or changes RLS policy on a table with real rows. That boundary isn't a repo convention Naldo waived — it's the standing floor for irreversible prod ops and no instruction lifts it. Reason: migration-ships-before-code-reads-it was already this repo's own convention (the bullet above); the manual paste-into-SQL-editor step was pure friction on top of a decision already made, and the assistant already has a Supabase MCP session anyway. (Two review lenses independently flagged that a rule this consequential — it grants standing write authority to BOTH devs' assistants — rode along inside an otherwise-routine schema PR (#712) without Jason's eyes on it; if you're reading this and haven't seen it, that's the gap they meant. A THIRD S57 lens flagged it again on the same PR's follow-up work — Jason, if you haven't seen this rule yet, this note is for you specifically.)
- **A customer-facing money vertical is not "verified" until a human or a browser E2E runs create→send→portal→approve.** When that leg can't run (no creds, no browser), say so loudly, stage a rollback lever BEFORE the go, and hand the human the exact steps. Reason: event went live on a logic-layer E2E alone and the council flagged it as the session's #1 risk (S23).
- **Deleting a launch flag deletes your instant rollback lever: name the replacement (usually revert-the-PR) in the same breath.** Reason: `eventEnabled` was deleted hours after go-live; right product call, suddenly missing kill switch (S23).
- **Two devs + parallel sessions move the repo under you: re-scan `origin/master` + `gh pr list --limit 200` immediately before building any ledgered task, and between waves of a long campaign.** Verify-before-build every ledgered task. Reason: duplicate PRs and one whole duplicated fix campaign shipped this way (S25 3x, S26, S35, S36).
- **Answer any current-state question about master or the shared docs from `git show origin/master:<path>` after a fetch, never a stale worktree copy.** Reason: stale answers cited deleted ledger rows and missed merged work (S32, S38).
- **A fix that adds a prop, param, or setting is inert until a caller passes it: grep the caller and prove behavior flips end-to-end.** Reason: two "fixed" claims shipped doing nothing until review caught them (S29, S35).
- **On an OneDrive-synced checkout (Naldo's machine today; Jason's is not), long subagent builds run in a persistent worktree OUTSIDE the synced tree (convention: `~/.config/superpowers/worktrees/<repo>/`), and every builder brief says commit per completed piece.** Reason: OneDrive sync killed processes and emptied worktrees mid-build, losing whole builds twice (S17 2x, S20, S27). Moot for a checkout that leaves OneDrive.
- **Any brief, handoff, ledger row, or past "fixed" note is a hypothesis: verify the live system before acting on it (for a feature flag: read prod config AND grep the flag's consumers).** Reason: wrong premises in S23, S26, S38, S44; one would have deleted our own live lead form.
- **Parallel PRs land only after the combined tree gates green as a set** (merge the branches + current master into one integration tree, run the gates, then land PR-by-PR). Reason: per-PR green missed composition breaks that the combined gate caught across six sessions (S17, S22, S24, S36, S37, S41).
- **Never chain a state-changing command after one that can fail (a checkout, a cast, a cd): verify the intermediate state first, and memory-doc surgery goes through script files (python), never inline chains. The script itself must write ATOMICALLY: encode the whole payload FIRST (so a fault raises before any file is touched), write to a temp file, verify its size, then `os.replace` — never `write_text`/`open(...,'w')` straight onto a memory doc.** Reason: a chained write after a failed cast wiped task_ledger.md (S25), a merge chained after a failed checkout landed on the wrong branch (unnumbered 2026-06-29 session), and in S35 a *compliant* script file still truncated task_ledger.md to 0 bytes — `pathlib.write_text` opens with `'w'`, so an encoding fault mid-write (a lone surrogate from a hand-typed emoji escape) emptied the file before the exception surfaced. Restored from git, but the script-file rule alone is necessary and NOT sufficient; the atomic-write half is what actually protects the doc.
- **Parallel subagent builders NEVER share the main working tree.** One builder in the main tree at a time; every CONCURRENT builder or gate-running reviewer gets its own `git worktree` + its OWN `npm ci` (never junction/link node_modules into the main tree — teardowns ate the main checkout's packages 3×), stages by explicit path only, and read-only reviewers read via `git show <branch>:<path>`, never the checked-out tree. Reason: S32 ran concurrent builders in one tree — branch flips corrupted reads mid-edit, one builder's stash briefly took ANOTHER session's untracked campaign scripts off disk, and a commit nearly bundled a different agent's staged edits (~5 incidents in one day).
- **A blocked tool call is one of THREE things — diagnose which before reacting, and never silently stall.** The Anthropic safety classifier gates tool calls (Bash most of all) and **fails CLOSED** when it can't complete, so a block is usually the safe default kicking in, not a verdict on your command. (1) **Transient outage** — a routine command (git, npm, a gate) blocks for no content reason. This is Anthropic-side and NOT the dev's fault; it just scales with how much shell a session runs. Fall back DOWN the ladder — a **script file** run via a different path → the **equivalent MCP tool** (Supabase/GitHub/psql) → a **ready-to-paste command sequence** for the dev's own terminal — then continue from the verified result. Never leave a git/gate/commit step half-done in silence. (2) **Content false-positive** — the block is on what the *output* looks like, not the command. Strip `?query=...` off URLs before returning them, and don't echo base64 / token- / secret-shaped blobs back through tool results. (3) **Legit policy block** — a prod-data write or a config edit the dev never explicitly named. Do NOT route around it: get the dev's explicitly-named authorization ("seed the test quote", not a generic "merge it"), then script the write. Reason: repeated Bash outages stalled whole wrap/gate sequences (S40/S42); query-string output tripped false blocks 3× (S44); the prod-write/config blocks were the gate working as intended (S14b/S31/S32/S34; a generic "merge it" never authorizes a prod-data write). Related but separate: MCP transport drops (the old Supabase OAuth server) are NOT classifier blocks — the PAT-stdio swap + psql backup (S43) already cover those.

- **A seat-authored fix prescription (in a builder brief or review batch) is a HYPOTHESIS: trace the consumer's exact behavior before dispatch, or mark it intent-not-prescription — and a FIX ROUND on a money/identity seam gets its own adversarial delta-verify before merge.** Reason: prescriptive briefs shipped wrong fixes four times across two sessions (S33 #660's literal fix code read the wrong shape; S34's uniform touched-ref formula broke tag inheritance, its rule-4 design reopened the household merge, and its address-compare suggestion failed its own named test) — every one caught only because the builder distrusted the brief or a delta-verify pass re-attacked the fixes. Fixes introduce bugs; the delta needs the same adversarial eyes as the original. Promoted 2026-08-07 (S34).

- **A new mutating action ships only after a sibling-guard parity check: grep the same file/module for the guard idiom its NEIGHBORING actions carry (a CAS, an `.is(null)` re-check, a TOCTOU re-read, a 409-on-lost-race) and reconcile to parity before commit.** Reason: `promote` shipped without the compare-and-swap its sibling `identify` had in the same file (S51) — the 6th recurrence of the cross-surface/sibling parity class (S32 staff-approve TOCTOU, S33 vault-notice states, …). Two independent review lenses converging on the same gap is the class's signature. Promoted 2026-08-05. **S57 recurred a 7th time** (`crewMembers.ts`'s `insertCrewMember`/`updateCrewMember` shipped without the race-recovery `shifts.ts`'s `clockIn` already had, written the SAME session) — the check still isn't happening at write time, only at review time; keep doing the review, but the goal is catching it BEFORE the review has to.
- **Verify cwd, branch, and remote before the first write of a session — including a session resumed from a compaction summary, not just a fresh start.** A compaction summary describing work at one path (e.g. `C:\dev\yll-quote-tool`) does not mean the current harness invocation's own worktree/branch is that path; a resumed conversation can be sitting in an unrelated leftover worktree on an unrelated branch. Reason: S52 hit this at a fresh session start; S57 hit the same class at a COMPACTION RESUME specifically (self-caught before writing anything, via `git remote`/`git log`/`gh pr view`, but only because the branch name looked wrong on inspection — it isn't always that obvious). Promoted 2026-08-10 (S57).
- **Browser-automation targeting: coordinate clicks on small controls are BANNED, and element refs must be FRESH — re-read the page after ANY layout change (a menu opening, a portal render, an HMR reload) before clicking; a stale ref is a coordinate-class error.** Review/lens agents may never click mutating controls at all — read-only means refs are for reading. Reason: S34 hit two coordinate mis-clicks (one untagged a real customer's NCE); S38 hit the same class through a NEW door — a review agent re-used a ref captured before a portaled menu opened, the layout shifted, and the click fired a REAL prod quote-send (is_test gates contained it to a 2-field revert). Promoted 2026-08-13 (S38).
- **A new migration must update `FULL-SCHEMA.sql` in the SAME PR — it does not reconcile itself.** Reason: recurred twice — S33's #188 was a full regen forced by accumulated drift (4 canon indexes missing on prod vs the file), and S57 shipped two more migrations (`crew_members`, `shifts`) without touching the file at all, caught only because all three review lenses independently flagged the same gap. A fresh onboarding DB built from this file is wrong until someone remembers to fix it later; don't defer the fix to "later." Promoted 2026-08-10 (S57).

# Codebase navigation — prefer the graphify graph for big-picture questions

A `graphify-out/` knowledge graph of `src/` may exist locally. It's **gitignored — per-machine, never committed**, so a fresh clone / another machine won't have one until it's built: `/graphify src` (free for code, ~seconds). The optional post-commit auto-rebuild hook (`graphify hook install`, also per-machine) then keeps it fresh on every commit.

- **When a graph exists** — for **architecture / cross-cutting** questions ("how does X flow", "what touches Y", "trace A → B") query it first with `graphify query "..."` instead of reading lots of files; it's cheaper and faster.
- **For targeted lookups** (one specific function / prop / line) — just grep/read; that's already the cheapest route and the graph isn't worth the overhead.
- **If no graph exists** (fresh clone / another machine) — grep/read, or build one first with `/graphify src`.

Staleness guards (the graph is a point-in-time snapshot and drifts):

- Treat it as a **map for orientation, not ground truth** — verify any file / function / line it cites against the live code before acting on it.
- If it seems unaware of recent work, fall back to grep/read.

# Token-efficiency defaults

Default habits to keep sessions cheap (S16 task #94):

- **graphify-first** for architecture / cross-cutting questions ("how does X flow", "what touches Y"); grep/read for targeted one-function/one-line lookups.
- **Read code surgically** — line-ranges / `Grep`, not whole 1800–4000-line files into context.
- **On long sessions, delegate broad searches** to compressed subagents (`cavecrew` / `Explore`) so tool-results stay small.
- **Batch independent tool calls** in one message.
- **Don't re-read** a file the harness already tracks as edited.
- **Keep continuity docs lean — archive on cadence, don't wait to be asked.** Tight ledger Notes; completed tasks → `task_ledger_archive.md`; session logs keep only the latest ~3 (older → `session_log_archive.md`); `project_quote_tool` history in its archive. **This happens automatically at every session close (a `/wrap` step), and a fresh session self-checks at start and archives if they've grown** — so the docs never balloon between manual cleanups and the dev never has to remember to ask.
- **The `caveman` skill compresses OUTPUT** — per-machine opt-in via a SessionStart hook.

**Skills placement.** Repo-shared skills live in `.claude/skills/` (git-synced to both devs); per-machine / global skills in `~/.claude/skills/`. Choose **repo** for team skills, **global** for personal. Don't keep the same skill in both (drift) — the **`llm-council` canonical copy is the repo one**. Known exception: `wrap` exists in both ON PURPOSE (per-dev merge behavior; see "Review / merge" below); don't "fix" it as drift.

# Model routing & production guardrails (STANDING POLICY — applies to ALL plans/builds, both devs; Naldo 2026-07-02)

**1. Work auto-routes to the right model tier (silent, automatic — the dev is not asked):**

| Tier | Model | Does |
|---|---|---|
| DOWN | **Haiku 4.5** | reads: recon, file location, doc lookups, log scans |
| DOWN | **Sonnet 5** | builds: routine implementation, tests, UI components, docs |
| SEAT (default) | **Opus 4.8** | plans, judges, reviews: orchestration, adversarial review passes, PR review, finding dispositions |
| UP | **Fable 5** | top-tier — **always asks first**, never used silently |

**Subagent spawns (added 2026-07-06, both devs):**
- **Never spawn a subagent on Fable, for any reason.** Subagents inherit the session model by default, so in a Fable-seat session pass an explicit model on EVERY spawn (the Agent tool, Workflow `agent()` calls, council advisors), picked from the table above. Reason: Fable burns usage roughly twice as fast as Opus, and one forgotten spawn bills all its grunt work at the top rate. In non-Fable sessions this rule costs nothing.
- **Do NOT enforce this with the `CLAUDE_CODE_SUBAGENT_MODEL` env var.** Per the official Claude Code docs it sits at the TOP of model resolution: it overrides agent frontmatter AND the explicit per-call `model` parameter, so any single value flattens this whole table (either Haiku reads bill at Opus, or Opus reviewers silently drop to Sonnet). Instruction-level routing keeps the tiers.
- **Write every spawn brief self-contained:** goal, files or area involved, constraints, what done looks like, report format. Reason: the worker has zero context from the conversation, and a vague brief wastes the whole agent run.
- **When a worker's output falls short, send the fix back to a worker with specific corrections** instead of the seat quietly redoing it. Reason: the seat redoing labor pays top rate twice for one piece of work.
- **Default to parallel fan-out for independent work (added 2026-07-11, Naldo).** When a task splits into 2+ independent pieces (recon of separate areas, builds on disjoint files, review lenses, batch fixes), spawn one agent per piece in a SINGLE message so they run concurrently, instead of doing pieces one at a time inline. Do NOT fan out when pieces depend on each other's output, or when the whole task is one small edit (spawn overhead loses). Every spawn still follows the routing table above and carries an explicit model in Fable sessions. Reason: serial completion of independent work wastes wall-clock time, and independent perspectives improve review quality.

**2. Only 2 interruptions ever:** *"use the expensive model?"* and *"ship to production?"*. Everything else proceeds without stopping the dev.

**3. Top-tier (Fable 5) = design, danger, or money ONLY** — architecture calls, production debugging, security review, migrations, money-math verdicts. **Never routine coding. ~20% of the work, max.**

**Named Fable-eligible cases (added 2026-07-10, Naldo). All still ask-first; naming them stops them running casually on lower tiers:**
- **Quoting/analyzer prompt changes count as MONEY.** The quoting SYSTEM_PROMPT and per-vertical analyzer prompts directly shape customer prices and detected footage; a prompt rewrite is a money change unless byte-identity is proven (the S19 substring-move technique). Reason: wrong prompt = silent money drift, same failure class as pricing-engine math.
- **Golden-number re-anchoring counts as a money-math verdict.** Moving golden-job totals redefines what "correct" means for every future run. Itemize every cent of the delta first (the S25 #144 standard); the go/no-go verdict on the re-anchor is Fable-eligible.
- **Council chairman verdict may run on Fable.** The council is already ask-first; when it runs, advisors stay on lower tiers per the routing table, but the chairman synthesis is the single highest-leverage output of the run and may take the top tier (one call).
- **Stuck-escalation (the UP path).** Fallback in rule 5 only covers a tier being DOWN; this covers a tier being BEATEN. When the same problem defeats the seat twice (a fix loop bounces twice, a review keeps missing the root cause, a worker returns garbage twice on a corrected brief), offer Fable for that ONE call, ask first, then drop back to normal tiers. Scoped to the single stuck problem, never the whole session.

**4. Every production change is guarded:** `branch → PR → automated checks (tsc · lint · vitest) → merge → deploy → verify`. **The AI never merges itself** — before any merge it shows the dev a **plain-English summary derived from the ACTUAL code diff** (not from intent) and waits for an explicit "go". (Strengthens the human-merge rule below; Jason-area PRs still carry his review flag.) Post-merge, the deploy is **verified in-browser**, never assumed.

**5. Model fallback:** if a tier's model is down/unavailable, drop **exactly one tier** (Fable → Opus → Sonnet → Haiku) and **say so** in the output. For anything risky (money math, prod migrations, approve/amend paths, security), do NOT silently substitute — **stop and ask** first.

# Review gates: pre-merge lenses + wrap review (STANDING POLICY, both devs; Naldo 2026-07-20)

**1. Pre-merge lens review (every PR; the one carve-out is named below).** Before asking the dev for a merge-go on ANY PR (code, docs, skill, or config), spawn four review agents in parallel, in a single message. Each spawn carries an explicit model: lens finders run Sonnet 5 (the S20 finder pattern: cheap hunters, the seat verifies), and the seat's finding dispositions stay on Opus per the routing table. Never let a spawn inherit the session model; this matters most in Fable sessions. Each brief is self-contained: it names the PR or branch, states that the app is a quoting and portal tool for a residential lighting company, points the agent at the real surfaces (the Area-ownership paths below: portal, quote builder, pricing engine, dashboard, API routes), and gives the lens. For changes touching portal, quote, or checkout UI, the customer and staff briefs tell the agent to drive the actual pages (preview deploy or dev server) at desktop and mobile widths, not just read the diff (S18/S41: gates-green pages still crashed in-browser). On a docs, skill, or config PR the lenses review process impact instead: who could this rule change hurt. The four lenses:

- **Customer lens:** what a homeowner hits. The portal, the quote view, emails, the approval and payment flow. Hunt for broken links, wrong or confusing prices shown, dead-end states, mobile rendering breaks.
- **Staff lens:** what a crew member or office staffer hits. The quote builder, the dashboard, the day-to-day workflow. Hunt for extra clicks, lost work on reload or reopen, broken tools, workflow dead ends.
- **Admin lens:** what the owner hits. Settings, pricing rules, reporting, permissions, data integrity. Hunt for silent misconfiguration, wrong totals in reports, access gaps, rows that drift out of sync.
- **Technical lens:** correctness. Money math (integer cents, rounding direction, partial selections), idempotency and retry, duplicate submission, auth and permission gaps, migration order, service-type seam gates (positive-match, per Pitfalls), client/server import boundaries.

The seat dispositions every finding: fix it, accept it with a stated reason, or defer it to a ledger row. HIGH findings get fixed BEFORE the merge-go is asked, not after. The merge-go message shows the dev the dispositioned findings list (never raw agent output) alongside the plain-English diff summary from rule 4 above. This gate supersedes the "don't fire a heavy review on a typo" guidance above for PR merges specifically: Naldo's explicit call (2026-07-20), cost accepted. Depth still scales inside each lens; a tiny diff means a short review, not a skipped one.

One carve-out: the wrap skill's own close PR is pure continuity docs (memory, a prod behavior no-op), so it takes no lens review; it gets the wrap skill's collision verify instead. On Naldo's machine that PR auto-merges under his standing go; on Jason's machine it still stops for his explicit merge-go. If a close branch touches AGENTS.md or CLAUDE.md (the shared rule files), auto-merge is OFF for that PR: stop for an explicit go.

**2. Wrap review (every /wrap).** Session close runs a multi-agent review of EVERYTHING the session shipped: the diff from the session's starting master SHA to current master, plus any branch still open at close. It runs even when every PR was lens-reviewed at merge time, because the combined tree can break in ways per-PR reviews miss. A session that changed a live non-repo customer surface (WordPress, Elementor, GHL, Vercel config) with zero git diff still gets a review: run at least the customer lens against a written description of what changed (S44 shipped a customer-facing change with no repo diff at all). Findings go into the session log entry and the self-assessment; findings against the assistant's own work go in its Mistakes list. Step-by-step detail lives in the wrap skill (`.claude/skills/wrap/SKILL.md`).

# Multi-dev collaboration (Jason + Naldo)

Two devs work in this repo on **different machines**. **Naldo owns the dashboard** (the `/` homepage, task #58); **Jason owns everything else** (portal, quote builder, pricing engine, design editor, training, settings). Both **PR into `master` — never commit to `master` directly** — and run the gates (`npx tsc --noEmit` · `npm run lint` · `npm test`) green before committing. New-machine setup → `docs/context/ONBOARDING_NALDO.md`.

## Branches
- Prefix every branch with your name: **`jason/<desc>`** or **`naldo/<desc>`** (e.g. `naldo/dashboard-shell`). Instant attribution, no name clashes.
- **Bot-initiated sessions are the accepted third prefix: `claude/<desc>-<id>`** (Claude Code on the web, GitHub Actions, and similar). The harness *assigns* that branch name and instructs the session to develop on it — the assistant cannot choose a `naldo/`/`jason/` name, so this is not a violation to correct. It is also not rare: 21 such branches existed as of 2026-08-20, against 189 `naldo/` and 104 `jason/`. **But `claude/` alone says nothing about WHOSE work it is, which is the whole point of the prefix — so a `claude/*` PR must name its operating dev in the PR body** ("run for Naldo", "run for Jason"). That restores the attribution the prefix would otherwise lose. Reason: added 2026-08-20 after a review flagged a `claude/*` branch as breaking the naming rule, when the rule had simply never addressed sessions that don't pick their own branch name.
- Branch off fresh `master` (pull first); keep PRs small; merge `master` back in if a branch lives more than a day.

## Area ownership
| Owner | Files |
|---|---|
| **Naldo** | `src/app/page.tsx`, `src/components/dashboard/**`, `src/app/api/dashboard/**`, `src/lib/dashboard/**` |
| **Jason** | `src/app/portal/**`, `src/components/portal/**`, `src/app/quote/**`, `src/components/quote/**`, pricing (`pricingEngine` / `BUSINESS_RULES`), `src/components/design/**` + `editor-core/**`, training, settings |
| **Operations Hub contract** | `docs/context/OPERATIONS_HUB_CONTRACT.md`, `crew_members` + future shared-labor tables/migrations, `/api/ops/v1/**` — Claude/quote-tool-assistant-owned only; the Operations Hub Codex reads this contract from its repo and never edits this repo's schema/routes |
| **SHARED — claim it first** | `src/app/layout.tsx`, `globals.css`, `package.json` + lockfile, the data layer (`src/lib/quotes.ts`, `designs.ts`, `supabase*`), shared types (`sceneTypes.ts`), tsconfig / eslint / next config, **`AGENTS.md` + `CLAUDE.md`** (the rule files), **`.claude/skills/**`** (both devs' assistants read and act on these) |

*Reading* a shared file (e.g. importing from `src/lib/quotes.ts`) is always fine; only **editing** a SHARED file needs a heads-up to the other owner first.

**Why the rule files and `.claude/skills/**` are SHARED (added 2026-08-20):** they are
executable process. A skill or a Pitfalls bullet changes how the *other* dev's
assistant behaves on their next session, with no code review of the behavior it
produces — a wider blast radius than most `src/` files, not a narrower one. A
four-lens review of the `video` skill flagged that the table never named them, so
a new skill could land with no cross-review at all; that was the gap, and this row
closes it. Two carve-outs stay as they are: the per-machine copies under
`~/.claude/skills/` are personal and need no review, and the two `wrap` copies
differ **on purpose** (per-dev merge behavior — see "Skills placement" above);
don't "fix" that as drift.

## Review / merge
- **An AI assistant never merges on its own — a human says go.** Assistants may create, push, and open PRs, but must **not** merge to `master` without their operating dev's explicit go-ahead (Jason's assistant ← Jason; Naldo's assistant ← Naldo). `master` auto-deploys to prod, so a human approves every merge. One standing exception (Naldo, 2026-07-02, per-machine): on Naldo's machine the `wrap` skill may auto-merge its OWN docs-only session-notes PR after a re-sync onto fresh master and a collision check; every code/feature PR still needs the dev's explicit go. Reason: wrap notes PRs once piled up six deep waiting for manual gos. (Jason's machine keeps the human-gated wrap; the two wrap skill copies differ on purpose.)
- **Own-area PRs: no cross-review needed** — a PR touching only your own area doesn't need the *other* owner's review (your own dev's go to merge still applies, per above).
- **SHARED-file PRs: the other owner reviews first** before merge.
- **Always merge current, never stale.** Before merging ANY PR, `git fetch`. If `master` has advanced past the branch's base, bring the branch up to date with `master` (merge `master` in or rebase, resolve any conflicts) and **re-run the gates (`npx tsc --noEmit` · `npm run lint` · `npm test`) on the updated branch** before merging. Never merge a branch whose green gates predate the current `master` — a clean text-merge can still be a logical regression when `master` changed underneath (e.g. a renamed export, a changed type, new business rules). This applies even to same-day branches, and strengthens the "merge `master` back in if a branch lives more than a day" guidance above into a hard pre-merge step. (Merging on GitHub combines the branch with origin's *current* `master` regardless of your local state, so this is about updating the **branch**, not just pulling local `master`.)
- **The CI `gates` check must be GREEN before any merge — verify it, never assume it.** `.github/workflows/ci.yml` runs the same three gates (`npx tsc --noEmit` · `npm run lint` · `npm test`) on every PR to `master` and on `master` itself. Read its conclusion before merging: `gh pr checks <n>`, or `gh pr view <n> --json statusCheckRollup`. A `failure` blocks the merge; a still-`pending` run gets waited on, not skipped. If the run is missing entirely (workflow disabled, fork PR, Actions minutes exhausted), fall back to running the three gates locally on the updated branch and SAY that CI did not run. This is a CONVENTION, not an enforced gate: making `gates` a required status check needs branch protection, which GitHub restricts to Pro/Team on private repos and the `Yule-Love-Lights` org is on the free plan (verified 2026-07-21, HTTP 403 on both the branch-protection and rulesets APIs). So nothing server-side stops a red merge — only this rule does. If the org ever upgrades, set `gates` required and this bullet becomes a backstop instead of the primary control. Reason: `master` auto-deploys to prod, and until CI existed a green local suite protected nothing when someone forgot to run it; four customer-facing bugs reached prod that way (S47).
- **If the Bash tool is blocked mid-flow** (transient safety-classifier outage, as first hit in S12 and recurring through S40/S42) don't silently stall on a git/gate step — fall back per the "blocked tool call is one of THREE things" Pitfall above (script file → MCP tool → ready-to-paste sequence for the dev's own terminal), then continue from the verified result.

## Shared memory (docs/context ⇄ local)

**Session numbering — one conversation = one session.** Never auto-increment or relabel the session number mid-conversation: an overnight pause + "good morning" resume does NOT advance it; only a *fresh conversation* does. Read the current number from the session logs at start and keep it fixed unless the dev explicitly says otherwise. (Hard-won — the premature "S13 CLOSE" mislabel in S12.) Verify the number against BOTH logs on FRESH master at start AND again at wrap; a concurrent session holding a higher number never bumps yours (local self-assessment record: mislabels S18, S30, S34, near-misses S29, S32, plus a fresh near-miss the day this rule landed: S46 work stamped as S45 while a concurrent session owned S45).

Each machine's local `~/.claude/.../memory` seeds from + syncs back to the repo's `docs/context/` (the canonical mirror). To stop the two machines clobbering each other:
- **Session logs are per-dev** — Jason writes `session_log.md`, Naldo writes `session_log_naldo.md`. **Only ever edit your OWN log.** Read both at session start.
- **`task_ledger.md` + `project_quote_tool.md` stay unified** (one source of truth). Before copying local → `docs/context`, **branch your sync off a fresh `git pull origin master`** so your PR is only your delta on top of the latest shared state — git auto-merges non-overlapping edits; you only hand-resolve a literal same-line clash.
- **Sync = a PR off fresh master at your own session close** (PR-not-master applies to docs too). Seed a machine (pull master → copy `docs/context/*` to local) only when local has nothing unsynced.

# Big decisions — OFFER the LLM Council, never auto-run it

A project skill, **`llm-council`** (`.claude/skills/llm-council/SKILL.md`), runs a question through 5 independent advisors → anonymized peer review → a chairman verdict. It's valuable for **high-stakes, genuinely-uncertain calls** — task sequencing, architecture, customer-facing tradeoffs, "should we do X or Y."

**An AI assistant must NEVER run the council automatically.** Three reasons: each run spawns ~11 sub-agents (real token cost); "big decision" is a judgment call (auto-running guesses wrong and fires on the wrong things); and an unprompted council is a heavy mid-work interrupt. Instead, when a genuinely council-worthy decision comes up:

1. **Prompt the dev** (Jason or Naldo) — flag that this looks like a council-worthy decision.
2. **Give your recommendation** on whether it's actually worth running here, and why or why not.
3. **Wait for an explicit yes/no.** Run the council ONLY on a "yes."

Don't raise it for small / reversible / obvious calls — that's noise. The dev can always trigger it themselves any time ("council this", "pressure-test this", "war room this").
