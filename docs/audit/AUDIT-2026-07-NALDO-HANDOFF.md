# #110 audit — Naldo handoff

> Append-target for every wave's `hand-to-Naldo` findings (created wave 0, per
> `AUDIT-PLAN-2026-07.md`). Naldo: these are findings in YOUR lane (dashboard / inbox /
> inventory / console pages / insights / customers) — we audited read-only and open no PRs
> here. Disposition them your way; ping Jason's thread if one turns out to be ours.
>
> **CRITICAL protocol** (pre-agreed): if a wave finds a CRITICAL in your lane you get an
> immediate ping — you fix same-day, or Jason's assistant opens the PR with you as required
> reviewer. Nothing merges in your lane without your eyes.

**Wave 7 delivered 2026-07-04** — 96 findings (9 HIGH / 38 MEDIUM / 49 LOW), all Naldo-lane, read-only. See below.

---

## Wave 7 findings (2026-07-04 @ `650beb2`, read-only)

Method: 15 finders (5 sub-areas × correctness/efficiency/quality) → 155 raw → 95 deduped → adversarial verify (1 refuted) → completeness critic → **96 findings**. Resumed on Opus after a Fable usage-limit stop (journal replay, no loss). ALL in Naldo's lane — this is a handoff, no PRs from Jason's side.

| Severity | Count |
|---|---|
| HIGH | 9 |
| MEDIUM | 38 |
| LOW | 49 |

**Trial-critical cluster (the 9 HIGHs decide whether the Aug–Oct parallel run produces trustworthy signal):**

### W7-001 · bug · `src/app/admin/jobs/[id]/page.tsx:147` · effort S

**Cancel-order success handler drops the route's refundedDeposit/refundNeeded flags — cancelling a booked-but-not-yet-invoiced order (deposit already charged, the common pre-install case) shows bare 'Order cancelled.' with no refund cue, so a real 50% deposit refund can be silently forgotten.**

- **Evidence:** cancelOrder (page.tsx:144-149) builds the message from only body.alreadyCancelled and body.refundedInvoice. POST /api/jobs/[id]/cancel returns { ok, cancelled, refundedInvoice, refundedDeposit, refundNeeded, note } (cancel/route.ts:86-97), and the route's own comment says the deposit read exists precisely 'so we can flag a deposit refund even when no invoice exists yet — a booked-but-not-completed order still took a 50% deposit (review MEDIUM)' (cancel/route.ts:68-71). refundedInvoice is true only when a PAID invoice existed; most cancellations happen pre-complete when no invoice row exists at all, so the page shows no refund note for exactly the case the flag was added for. Compounds wave-1 W1-008 (route persists no durable refund record and sends no staff notification — fix-later): the ONE surface that receives the ephemeral flags discards them, making the refund obligation silent end-to-end. Only the generic pre-action confirm text ('Any refund is handled manually in Valor') remains. Trial impact: booked order cancelled → customer's ~$1-3k deposit refund never issued, nothing anywhere says it is owed.
- **Suggested fix:** Use the route's richer response: show the refund warning when body.refundNeeded (or refundedDeposit || refundedInvoice), e.g. append body.note — 'Order cancelled. A payment was already taken — issue the refund manually in Valor.' — and style it as a warning rather than plain gray actionMsg text.
- **Verifier:** Verified against live code at baseline. src/app/admin/jobs/[id]/page.tsx:144-149 builds actionMsg from ONLY body.alreadyCancelled and body.refundedInvoice, discarding body.refundedDeposit, body.refundNeeded and body.note. The route (src/app/api/jobs/[id]/cancel/route.ts:82-93) deliberately computes refundedInvoice = invoice.status==='paid' (:82) AND separately refundedDeposit from quotes.deposit_paid_at (:69-77), with its own comment (:66-68) stating the deposit read exists so a refund can be flagged 'even when no invoice exists yet'. cancel/route.test.ts:117-126 locks in exactly the finding's scenario — deposit paid, getInvoiceByJob→null → {refundedDeposit:true, refundNeeded:true}. For a booked-but-not-invoiced order (the common pre-install cancel), refundedInvoice is false, so the page appends nothing and shows bare 'Order cancelled.' while a real 50% deposit refund is owed and the route's refundNeeded/note signal is thrown away. Both endpoint consumers drop the flags (PipelineActionsMenu.tsx:78-81 doesn't even parse the body), so the obligation is silent end-to-end, compounding wave-1 W1-008 (no durable refund record). No dedupe match in KNOWN-FINDINGS (the cancel FIXED rows #83-002/003/005/006 are unrelated /approve and webhook fixes). Sole mitigation — the unconditional pre-click confirm text (:133-135) mentioning manual Valor refunds — fires identically on no-deposit cancels and precedes the action, so it never confirms that THIS order owes a refund; it softens but does not remove the gap. Wrong-money-adjacent behavior on a plausible, common trial-surface path with $1-3k impact = HIGH (not CRITICAL: a human operator is in-loop and generically warned; no automatic money movement).

### W7-002 · bug · `src/app/api/inventory/purchase-order/auto-send/route.ts:13` · effort L

**Self-acknowledged but trial-critical: with no on-order/received tracking, ANY shortfall change between auto-sends re-emails the full CUMULATIVE shortfall, not the delta — near-certain double-ordering during busy season since the 3-day cron + the ≥5-jobs webhook trigger both outpace supplier delivery.**

- **Evidence:** The file header (lines 13-15) documents the limitation. Concretely: cron emails an order for 100× SKU 40056 on day 1 and records sig('40056:100'); day 3 a new deposit-paid job adds 10 more; stock hasn't arrived so on-hand is unchanged; the new PO reads 'order 110' → different signature → passes dedup → Thunder receives a second email for 110 units (the email is a plain order sheet with no 'supersedes previous order' language in supplierOrderEmailHtml), for a true remaining need of 10. The Valor-webhook trigger (purchaseOrder.ts:197-217) fires on every deposit once ≥5 active jobs, making signature churn the common case exactly when the trial is busiest.
- **Suggested fix:** Before enabling PO_AUTO_SEND_ENABLED for the trial, add a minimal on-order ledger (record the sent lines; subtract outstanding on-order qty from the shortfall until marked received), or make the email explicitly cumulative ('replaces our previous order dated X').
- **Verifier:** Every mechanical claim checks out against the code. There is no on-order ledger: listOnHand() (src/lib/inventory/onHand.ts:23-35) reads only the manually-curated inventory_on_hand.on_hand_qty, and a codebase-wide search finds no on-order/pending/outstanding column in the inventory system. buildSupplierPurchaseOrder() (src/lib/inventory/purchaseOrder.ts:55-58) aggregates demand from every active job with stock_decremented_at IS NULL, so a newly deposit-paid job adds its full material need while on-hand stays flat (stock hasn't arrived); computePurchaseOrder() (line 29-38) then emits order = needed − onHand = the entire cumulative shortfall (100 → 110), not the 10-unit delta. The signature dedup (purchaseOrderSignature line 157-162, checked at auto-send/route.ts:49-52) does NOT catch this because order changed, so it passes and re-sends. supplierOrderEmailHtml (src/lib/integrations/quoteMessages.ts:314-327) is a plain "please prepare the following order... Qty 110" sheet with zero supersedes/replaces language, so Thunder reads it as an additive second order. Both triggers realistically outpace supplier delivery: the 3-day Vercel cron and triggerAutoPOIfBusy({ minJobCount: 5 }) called on every deposit-paid event (src/app/api/integrations/valor/webhook/route.ts:407) make signature churn the common case during the busy Aug-Oct trial. Upgraded from the finder's MEDIUM to HIGH because the outcome is money-wrong (real double-ordering / over-purchase) on a plausible path during the exact trial window; not CRITICAL because it is gated OFF by default behind PO_AUTO_SEND_ENABLED='true' + THUNDER_ORDER_CONTACT_ID and cannot fire in prod as currently configured, and a human-gated manual Send remains the safe fallback. The finding's proposed pre-activation fix (an on-order ledger, or explicit cumulative/supersedes email language) is the correct disposition before enabling the flag for the trial.

### W7-003 · bug · `src/lib/dashboard/inbox/store.ts:295` · effort M

**Response-time analytics measure reconcile-cron lag, not customer wait: ingest overwrites last_message_at with the outbound reply's timestamp and stamps handled_at=now on auto-resolve, so handled_at - last_message_at collapses to ~1 minute for every reply made outside the inbox UI (the primary flow).**

- **Evidence:** store.ts:133-148 always writes item.last_message_at = touch.lastMessageAt; store.ts:293-296 sets handled_at = now when plan.autoResolved. responseMetrics.ts:91-92 computes responseFor = handledAt - lastMessageAt as 'how long a customer waited'. Scenario: customer texts 9am; Naldo replies 5pm from the GHL app; the next reconcile ingests the conversation (lastMessageDirection=outbound, lastMessageDate=5:00pm) -> last_message_at=5:00pm, handled_at=5:01pm -> recorded response time ~1min instead of 8h. Even in-tool replies get retroactively zeroed: markItemHandledLocal stamps handled_at at click time, then the next reconcile overwrites last_message_at with our outbound's timestamp -> ~0. medianResponseMs/withinOneHourPct/byRep trend toward perfect regardless of real responsiveness, defeating the '#1 pain is replying too late' KPI during the Jobber-parallel trial.
- **Suggested fix:** Track the customer's last inbound separately (e.g. a last_inbound_at column, or don't overwrite last_message_at on outbound touches) and compute response time as outbound/handled time minus the prior inbound time captured at resolve-time.
- **Verifier:** Verified end-to-end: ghl.ts:45,58 normalizes each conversation into one touch whose lastMessageAt is the LAST message's timestamp (our outbound once we reply); reducer.ts:51-55 auto-resolves any outbound-latest touch; store.ts:139 unconditionally overwrites last_message_at with that outbound timestamp and store.ts:293-296 stamps handled_at=now (reconcile time) on auto-resolve. responseMetrics.ts:87-93 then computes response time as handledAt−lastMessageAt (documented as "how long a customer waited") with no exclusion of auto-resolved rows (handled_by=null becomes rep 'system'), and vercel.json:12-13 runs the GHL reconcile every minute — so any reply sent from the GHL app (the primary flow) records ~≤1 min regardless of the real wait, and even in-tool handled items get retroactively zeroed when the next reconcile overwrites last_message_at with the outbound's time (the Math.max(0,…) clamp at responseMetrics.ts:90 tacitly acknowledges this state). No guard refutes it: the skip path only covers no-existing-item, the webhook just triggers the same reconcile, no test encodes cron-lag semantics, no KNOWN-FINDINGS match, and the intentional-design notes cover auto-resolve behavior but not the analytics reusing the overwritten timestamp. HIGH (not CRITICAL): the Jobber-trial "replying too late" KPI (median/within-1h/byRep/trend) is systematically wrong on the dominant path, but there is no data-loss/PII/money impact.

### W7-004 · perf · `src/lib/dashboard/inbox/store.ts:311` · effort M

**ingestTouch unconditionally upserts the item row and inserts an 'ingested' dashboard_activity row for every non-skipped touch on every reconcile run — with the GHL cron at * * * * * (limit 50), Gmail at */2 (25 threads), quotetool at */5 (500 quotes), plus a webhook-triggered reconcile per inbound GHL message, this is tens of thousands of no-change writes and dead audit rows per day into an append-only table with no retention and no action index.**

- **Evidence:** store.ts:288-317 — the upsert (with a fresh updated_at each run) and `sb.from('dashboard_activity').insert({actor:'system',action,...})` run even when decideInboxState returned 'no change' (same message re-ingested; reducer.ts:61-67 explicitly handles this case, but the store writes anyway). store.ts:1040-1044's own comment admits it: "'ingested' (one row per reconcile touch — thousands)". vercel.json schedules the GHL reconcile EVERY MINUTE; migrations/2026-06-28-dashboard-tables.sql:131 declares dashboard_activity append-only with only inbox_item_id/created_at indexes, so listActivity's `.not('action','in',...)` and getReopenCounts' `.eq('action','handled')` must filter past an ever-growing 'ingested' majority. Over the Aug–Oct trial this is millions of rows: DB bloat, WAL/autovacuum churn, and progressively slower activity/analytics reads.
- **Suggested fix:** In ingestTouch, compare the plan against the existing item (same status, same last_message_at, no contact change, no flag clears) and return early without the upsert or activity insert; log 'ingested' only on first insert and on real state transitions (reopen/auto-resolve already have their own actions). Optionally add a retention sweep or partial index on dashboard_activity(action) WHERE action NOT IN ('ingested','escalated').
- **Verifier:** All evidence verified against live code. store.ts:288-317 unconditionally upserts inbox_items (with a fresh updated_at every run) AND inserts a 'system'/'ingested' dashboard_activity row on every non-skipped touch, even when reducer.ts:61-67 explicitly returned a no-change plan for a re-ingested same message. vercel.json schedules the GHL reconcile at * * * * * (every minute), quotetool at */5, gmail at */2, so this fires tens of thousands of no-change writes/day. migrations/2026-06-28-dashboard-tables.sql:131-144 declares dashboard_activity append-only with indexes only on inbox_item_id (143) and created_at (144) — no action index — so listActivity's .not('action','in','(ingested,escalated)') (store.ts:1043) and getReopenCounts' .eq('action','handled') (store.ts:770) must scan past an ever-growing 'ingested' majority; getReopenCounts even caps at limit(5000) (772), which can undercount real reopens once bloat sets in. Over the Aug-Oct trial this is millions of dead rows: DB/WAL/autovacuum churn plus progressively slower activity/analytics reads. HIGH (not CRITICAL) is correct — real waste on a plausible path that degrades over weeks, not immediate data-loss/PII/money-wrong; and well above a micro-optimization given the every-minute cron scale. Not present in KNOWN-FINDINGS-2026-07.md, so no knownId.

### W7-005 · bug · `src/lib/dashboard/inbox/store.ts:579` · effort S

**The Followed snooze hides an item from the open queue but does NOT stop escalation: listEscalatableItems filters only status='unresponded' with no followed_up_at check, so a manually-Followed item keeps firing amber/red team emails and appears in the end-of-day digest every single day until an inbound arrives.**

- **Evidence:** store.ts:573-581 selects .eq('status','unresponded') with no .is('followed_up_at', null); markItemFollowed (store.ts:505-516) stamps followed_up_at without changing status. sync.ts runEscalation feeds those items to newlyCrossedLevel and isDueForEodDigest (no dedupe on the EOD digest by design, sync.ts:381-387). Scenario: operator handles a call outside the tool and clicks 'Followed' 30 minutes in -> item disappears from the queue, but at >1h the team gets a '🟡 Heads up: customer message still unanswered' email, at >4h a '🔴 URGENT', and it shows up in EVERY 5pm digest until the customer happens to reply. Contrast: listOpenItems (store.ts:344) DOES filter .is('followed_up_at', null). Also reachable via Reverse of a 'handled' action (inverseOf leaves followed_up_at set, lifecycle.ts:31-32). Daily false urgencies train the team to ignore the safety net.
- **Suggested fix:** Exclude followed items in listEscalatableItems (.is('followed_up_at', null)), or if snoozed items should still digest, label them 'followed' in the email instead of 'unanswered'.
- **Verifier:** Confirmed with one precision fix. listEscalatableItems (src/lib/dashboard/inbox/store.ts:576-580) selects .eq('status','unresponded') + a lead_kind .or() with NO followed_up_at filter, while markItemFollowed (store.ts:509-512) stamps followed_up_at without changing status — so a manually-Followed item (via /api/dashboard/followed/route.ts:31) stays in the escalation feed. Contrast proves it's a bug, not design: listOpenItems filters .is('followed_up_at', null) (store.ts:344), the reply route escapes escalation only because it ALSO calls markItemHandledLocal which sets status='handled' (reply/route.ts:78, store.ts:459), and runEscalation's own doc comment says it scores "every open item" (sync.ts:324) yet its query is broader than the open list. The store.test.ts listEscalatableItems suite (line 348) tests only the lead_kind filter — no test blesses followed items escalating. Escalation's stated purpose is "REPLYING TOO LATE" (escalation.ts:6), so alerting on an already-followed item is wrong per its own spec, and the "customer message still unanswered" emails are factually false. One overstatement in the finding: amber/red do NOT fire daily — newlyCrossedLevel dedupes via notified_levels (escalation.ts:35-43), so each fires at most once after the Followed click (in the stated scenario: Followed at 30min → one false amber at >1h, one false red at >4h). The daily repetition is real but only via the EOD digest, which by design has no dedupe (escalation.ts:49-50, "rolls into the next day until handled") — the item appears in every 5pm digest until an inbound clears followed_up_at (store.ts:300) or outbound auto-resolve happens to see a tracked reply; a phone-call follow-up (the exact case the Followed button exists for) never auto-clears. HIGH stands: plausible everyday path during the Aug-Oct trial, false 🟡/🔴 alerts plus a permanent digest entry train the team to ignore the safety net.

### W7-006 · bug · `src/lib/dashboard/insights.ts:78` · effort S

**Insights page still counts cancelled/declined/lost orders as booked revenue — the #83 B7 fix (PR #300 / commit 20e0967) covered metrics.ts, workflowBoard.ts and serviceMetrics.ts but never touched insights.ts, so all three aggregations there (computeInsightStats, monthlyRevenue, revenueByService) key on customer_approved_at alone.**

- **Evidence:** computeInsightStats (lines 78-96) sums totalBooked/avgJobValue/closeRatio from `!!q.customer_approved_at` with no deriveStatus/terminal check; monthlyRevenue line 37 (`if (!q.customer_approved_at) continue`) and revenueByService line 51 do the same. Scenario: a $8,000 quote is approved, then cancelled (status='cancelled', customer_approved_at still set — exactly the case the B7 tests pin in metrics.test.ts:78-104). The home-dashboard KPI correctly excludes it, but /insights shows Total booked +$8,000, the monthly-revenue bar for that month +$8,000, the service donut +$8,000, avgJobValue inflated, and closeRatio counts it as a win. The two dashboards disagree during the Aug–Oct Jobber-parallel trial, on the exact bug class Naldo already flagged as trial-blocking (#83-006, FIXED elsewhere). insights.test.ts has zero terminal-status cases, confirming the gap.
- **Suggested fix:** Mirror serviceMetrics.ts: add an isTerminalStatus(q) guard (deriveStatus ∈ {cancelled, declined, lost}) to the approved branches of computeInsightStats, monthlyRevenue, and revenueByService; add the same red-first terminal-state tests the other three modules got in 20e0967.
- **Verifier:** Confirmed. src/lib/dashboard/insights.ts:37, 51, 78-85 aggregate booked revenue keyed on `!!q.customer_approved_at` alone with no terminal-status guard, while its three siblings (metrics.ts, serviceMetrics.ts, workflowBoard.ts) all got an isTerminalStatus/deriveStatus exclusion in PR #300/commit 20e0967 — whose `--stat` shows insights.ts was never touched (git log confirms its last edit is the fe0679c Phase-4 creation, no B7 fix). The data to fix it is present: DASHBOARD_QUOTES_SELECT (queries.ts:32) selects `status`, and /insights/page.tsx:57-63 feeds the full result.rows set (same source as the home dashboard) into all three functions, so a $8,000 approved-then-cancelled quote (status='cancelled', customer_approved_at retained — the exact case pinned in metrics.test.ts) inflates computeInsightStats totalBooked/avgJobValue/closeRatio, the monthlyRevenue bar (line 37), and the revenueByService donut (line 51), making /insights disagree with the correctly-excluding home KPI. insights.test.ts (26-115) has zero terminal-status cases, confirming the coverage gap. This is the un-fixed remainder of the #83-006 class (that row is FIXED but scoped to dashboard queries only, so no dedupe applies). HIGH not CRITICAL: it is a read-only mis-count on an internal operator reporting page during the Aug–Oct Jobber-parallel trial, not customer-facing money movement or data loss.

### W7-007 · bug · `src/lib/dashboard/worklist.ts:18` · effort S

**computeWorklist never checks the persisted status, so declined/cancelled/lost/changes_requested quotes nag in the homepage worklist forever — the B7 terminal-status fix (#83-006 class) reached metrics/workflowBoard/needsAction/serviceMetrics but missed this module.**

- **Evidence:** worklist.ts imports only DASHBOARD_CONFIG + types (no deriveStatus); the only exits are `if (q.customer_approved_at) continue` (line 18) and the sent/draft age gates. Sibling modules needsAction.ts:70-75, workflowBoard.ts:61-66, metrics.ts:45-49 and serviceMetrics.ts:17-20 all derive status and skip terminal states, and `status` IS selected by DASHBOARD_QUOTES_SELECT (queries.ts:32) so the data is present. worklist.test.ts has zero terminal-status cases. Scenario: during the Aug–Oct trial a customer declines via the portal (status='declined', quote_sent_at 10 days ago, no approval) → the row renders as 'Sent 10 days ago — no reply' with a /portal deep-link, never leaves the list, and because the list sorts oldest-first with a 25-row cap (worklist.ts:51-52), dead quotes occupy the top and bury genuinely stale drafts; a cancelled never-sent quote likewise nags as 'draft-stale' forever.
- **Suggested fix:** Mirror needsAction.ts: derive status via deriveStatus(q) at the top of the loop and `continue` on a TERMINAL_STATUSES set (declined/cancelled/lost/changes_requested — or surface changes_requested as its own actionable kind if Naldo wants it visible). Add worklist.test.ts cases for each terminal status (TDD per charter §6 — this is a state-transition path).
- **Verifier:** Verified end-to-end. computeWorklist (src/lib/dashboard/worklist.ts:14-48) only inspects customer_approved_at (line 18), quote_sent_at (line 20), and age — it imports neither deriveStatus nor any terminal-status set, so persisted status is ignored. The rows arrive unfiltered for status: listQuotesForDashboard(500) → listQuotesForDashboardResult filters ONLY .eq('is_test', false) (queries.ts:56), and DASHBOARD_QUOTES_SELECT does select 'status' (queries.ts:32), so terminal rows are present in the data but never excluded. page.tsx:41 passes those exact rows into computeWorklist and renders <Worklist> on the homepage (page.tsx:58). Sibling modules do guard: needsAction.ts:130-133 derives status and continues on a TERMINAL_STATUSES set (declined/cancelled/lost/changes_requested), matching workflowBoard/metrics/serviceMetrics — worklist is the one that missed the B7 terminal-status class. Scenario is reachable and routine in the Aug-Oct trial: a declined quote carries status='declined', quote_sent_at set, customer_approved_at null → falls through line 18, past the draft branch (has sent_at), into the sent-no-reply branch and renders 'Sent N days ago — no reply' with a /portal deep-link, never clearing; with oldest-first sort + 25-row cap (worklist.ts:51-52) dead quotes bury actionable stale drafts. A cancelled never-sent quote likewise nags as draft-stale. worklist.test.ts has zero status/terminal cases. Not CRITICAL (no money/PII/data-loss — deriveStatus already fixed the revenue path in #83-006/PR#300, a separate module), but wrong behavior on a plausible path polluting the operator's primary daily queue = HIGH. No dedupe match: KNOWN-FINDINGS #83-006 is the FIXED revenue-inflation fix, not the worklist.

### W7-008 · bug · `src/lib/inventory/jobs.ts:262` · effort M

**prepareJobMaterials stamps the claim (stock_decremented_at + stage) BEFORE reading the materials; every downstream read failure is swallowed to empty data, so a transient failure permanently marks the job prepped with zero stock deducted — and a retry returns alreadyDone.**

- **Evidence:** jobs.ts:242-248 atomically claims the job first. Then jobs.ts:262 `getJobWorkOrder(id)` depends on getJob (returns null on error), the designs/quotes queries (error ignored → empty scene), getInventoryBindings (bindings.ts:72-75 returns EMPTY on error → every line unbound), and listOnHand (onHand.ts:30-33 returns [] on error → every SKU 'untracked', computeStockDeductions skips onHand===null). Any of these blips → deductions=[] → route returns 200 {ok:true, alreadyDone:false, deductions:[]}, the Kanban modal shows '✓ Stock deducted — job prepped' (jobs/page.tsx:269-270), and because the claim is already stamped, re-clicking prepare returns alreadyDone forever. On-hand stays overstated → the low-stock alarm never fires for those SKUs and the demand-driven PO under-orders. No test covers a post-claim read failure (jobsPrepare.test.ts only covers claim/test-quote paths).
- **Suggested fix:** Read + compute the deductions BEFORE claiming (or unwind the claim by nulling stock_decremented_at when the work-order read yields no materials view / wo===null), and make the empty-data-on-error reads distinguishable (propagate errors from getInventoryBindings/listOnHand into prepareJobMaterials instead of silently proceeding). At minimum return a distinct result when wo is null so the UI doesn't show success.
- **Verifier:** Every link in the chain verified. prepareJobMaterials (src/lib/inventory/jobs.ts:242-248) atomically stamps stock_decremented_at + fulfillment_stage='ready_for_install' BEFORE reading materials at line 262. getJobWorkOrder depends entirely on error-swallowing reads: getJob returns null on error (src/lib/jobs.ts:67) → getJobWorkOrder returns null (jobs.ts:150); getInventoryBindings returns EMPTY on error (src/lib/inventory/bindings.ts:74); listOnHand returns [] on error (src/lib/inventory/onHand.ts:32) so every SKU is untracked (onHand null) and computeStockDeductions skips it (jobs.ts:218); the designs/quotes queries in getJobWorkOrder ignore their errors. Any transient post-claim read blip therefore yields wo===null or empty materials → deductions=[] (jobs.ts:263), the route returns 200 {ok:true,alreadyDone:false,deductions:[]} (src/app/api/inventory/jobs/[id]/prepare/route.ts:20), and the Kanban shows "✓ Stock deducted — job prepped" keyed only on stockDecrementedAt (src/app/inventory/jobs/page.tsx:270). Because the claim is already stamped, any retry hits the !claimed branch and returns alreadyDone:true forever (jobs.ts:250-254) — no unwind exists. jobsPrepare.test.ts covers only test-quote, baseline-deduct, and claim-taken paths; no test exercises a post-claim read failure. On-hand stays overstated → low-stock alarm never fires and the demand-driven PO under-orders, silently, during the Aug-Oct inventory trial. Not CRITICAL (requires a transient failure, no direct customer-money error), but HIGH: wrong behavior on a plausible path (transient Supabase read failure) with a permanent, silent, non-recoverable inventory-integrity impact and a UI that falsely reports success.

### W7-009 · bug · `src/lib/inventory/purchaseOrder.ts:96` · effort S

**Supplier PO, auto-PO, job work orders, and prep stock deductions all drop clipRules (pass {} to projectMaterials) — clips are never ordered, never on pick lists, and never deducted from stock; only the per-quote /api/inventory/materials view includes them.**

- **Evidence:** purchaseOrder.ts:91-96 `const { bindings } = await getInventoryBindings(); ... projectMaterials(scene, bindings, {}, colorChoice)` and jobs.ts:177-178 identically destructure clipRules away and pass `{}`, while materials/route.ts:41-42 passes the real clipRules. projectClips() with empty rules returns qty from DEFAULT_CLIP_PER_FT but sku only from the rule — with `{}` the whole clip line is generated with rule=undefined → sku null → dropped by aggregateMaterials. So a booked job with 150 ft of gutterline shows '~150 Gutterline clips' on the Materials tab, but the emailed supplier PO (manual POST and the unattended auto-send cron), the Kanban work-order modal, the print page, the email-order digest, and prepareJobMaterials' deduction all contain zero clips. Clip SKUs (14147/14145/14159/14343/14144) are configured on the Bindings→Clips tab and seeded, so staff reasonably believe they're covered.
- **Suggested fix:** Destructure clipRules from getInventoryBindings() in both call sites and pass it through: `const { bindings, clipRules } = await getInventoryBindings(); projectMaterials(scene, bindings, clipRules, colorChoice)` in buildSupplierPurchaseOrder (purchaseOrder.ts:96) and getJobWorkOrder (jobs.ts:178). Add a test asserting a roofline run's clips appear in the PO and the work order.
- **Verifier:** Verified end-to-end. purchaseOrder.ts:91-96 destructures only `{ bindings }` from getInventoryBindings() and calls `projectMaterials(scene, bindings, {}, colorChoice)`; jobs.ts:177-178 does the same for getJobWorkOrder. With an empty clipRules `{}`, clipRules.ts:32-38 finds `rule = rules[feature]` undefined, so qty is still computed via DEFAULT_CLIP_PER_FT but `sku` resolves to null (line 38). materialsProjection.ts:202-213 pushes that clip line with `sku: null`, and aggregateMaterials (materialsProjection.ts:257 `if (!l.sku) continue;`) drops every null-sku line — so clips are stripped from every aggregated consumer. By contrast materials/route.ts:41-42 passes the real `clipRules`, so its per-quote Materials-tab view keeps the clip line (real SKU). Confirmed reachability: buildSupplierPurchaseOrder feeds the manual PO route, the auto-send route, triggerAutoPOIfBusy on the Valor deposit webhook, and inventory/page.tsx; getJobWorkOrder feeds the job-detail API, the print page, the email-order route, and prepareJobMaterials' stock deduction (jobs.ts:262-263). So for any non-metal roofline C9 job, staff see "~N Gutterline clips" on the Materials tab but the emitted supplier PO, work order, print sheet, order email, and prep deduction all contain zero clips — clips are never ordered nor deducted despite configured/seeded SKUs. Not a dedupe match in KNOWN-FINDINGS. HIGH (wrong behavior on a plausible normal path during the Aug-Oct trial; not PII/money-wrong, so not CRITICAL).

### MEDIUM (38)

| id | area | type | file:line | summary | fix |
|---|---|---|---|---|---|
| W7-010 | Console pages | bug | `src/app/admin/invoices/[id]/page.tsx:281` | Both detail pages render the reconciliation's INFORMATIONAL 'balance-outstanding' flag inside the red 'Reconciliation issues' alarm box — every normal unpaid invoice (the standard state between compl… | Render 'balance-outstanding' in the existing neutral gray footnote (it already shows the balance) and reserve the red box for overpaid / short-deposi… |
| W7-011 | Console pages | bug | `src/app/admin/jobs/[id]/page.tsx:318` | The 'Amend order' panel (and the 'Mark installed & create invoice' button) render regardless of job status — on a CANCELLED job the operator gets a live 'Record amendment' affordance that, at baselin… | Gate the Amend panel and the complete button on non-terminal job status, mirroring the Cancel panel's condition (hide or disable with a 'cancelled/do… |
| W7-012 | Inbox / ingest | bug | `src/app/api/dashboard/reply/route.ts:79` | Reply route sends the customer message before any item-state check and silently swallows a failed handled-stamp (returns ok:true) — two operators in the shared queue, or a retry after a stamp failure… | Re-read item status (and optionally claim state) immediately before sending and reject if already handled/followed; if the post-send local stamp fail… |
| W7-013 | Inventory | consistency | `src/app/api/inventory/bindings/route.ts:17` | None of the 11 operator inventory API routes call requireOperator() — the entire /api/inventory surface (stock writes, catalog import, supplier-order email triggers, customer-PII job feeds) relies so… | Add the standard `const denied = await requireOperator(); if (denied) return denied;` to the 11 non-cron routes (bindings, catalog, categories, jobs,… |
| W7-014 | Inventory | bug | `src/app/api/inventory/jobs/[id]/route.ts:29` | The Kanban 'Move to: Ready For Install' dropdown reaches the prepped-looking end state without ever running the stock deduction — the job sits in the Ready column with stock_decremented_at NULL, so i… | When a stage move lands on ready_for_install with stock_decremented_at still NULL, either run/require prepare (confirm dialog → call the prepare endp… |
| W7-015 | Inventory | bug | `src/app/api/inventory/purchase-order/route.ts:35` | The manual 'Send to supplier' neither checks nor records the auto-send dedup signature — with PO_AUTO_SEND_ENABLED, the cron (or the deposit-webhook trigger) re-emails the identical order staff just … | Record the signature on the manual send too (and check it, with a staff override), so all three send paths share one dedup ledger; optionally make th… |
| W7-016 | Other | perf | `src/app/customers/[contactId]/page.tsx:61` | Customer detail page fetches the newest 500 quotes and filters in memory to find one customer's ~1-5 quotes; past 500 total quotes, older customers get incomplete history or a misleading 404 | Query the customer's quotes directly: `.or('highlevel_contact_id.eq.<id>,customer_id.eq.<id>')` with the same is_test filter (a small helper next to … |
| W7-017 | Dashboard / insights | bug | `src/app/insights/page.tsx:57` | The `capped` flag on DashboardQuotesResult is computed and tested but no consumer ever renders it — at >500 quotes every lifetime money KPI silently truncates with the promised 'based on newest N quo… | Render the caveat where result.capped is true on /insights (one line under the headline cards), and thread the result (not the wrapper) into the dash… |
| W7-018 | Inventory | bug | `src/app/inventory/bindings/page.tsx:75` | If the bindings GET fails, the editor silently renders the SEED defaults with no error banner; clicking 'Save bindings' then replaces the operator's saved custom bindings/clip rules wholesale with th… | Track the GET failure, show a 'couldn't load saved bindings — Save disabled' banner, and disable Save until a successful load (mirror how the overrid… |
| W7-019 | Inventory | bug | `src/app/inventory/overrides/page.tsx:4` | The 'locked' (sold-out) flag and hiddenCategories overrides are inert in the materials/ordering engine — the page promises 'lock sold-out items so the engine won't order them' and 'hide categories th… | Decide the intended semantics and wire them: at PO-build time, drop (or flag) lines whose SKU is locked / whose effective category is hidden, and sur… |
| W7-020 | Inventory | perf | `src/app/inventory/page.tsx:42` | Every /inventory overview render runs buildSupplierPurchaseOrder alongside the page's own fetches, duplicating jobs, quotes, on-hand, and catalog queries and pulling every active job's FULL design sc… | Let buildSupplierPurchaseOrder accept optional prefetched deps (active job/quote rows, onHand, catalog, bindings) and have the overview page pass its… |
| W7-021 | Other | bug | `src/app/page.tsx:33` | Home dashboard and Customers pages still use the error-swallowing listQuotesForDashboard, rendering an all-zero KPI strip / '0 customers' as if real when the quotes query fails; the `capped` 500-row … | Switch page.tsx and both customers pages to listQuotesForDashboardResult with the same error-banner pattern as /insights, and render the capped cavea… |
| W7-022 | Other | bug | `src/components/admin/PipelineActionsMenu.tsx:74` | 'Collect payment' in the pipeline menu fires POST mark-paid with NO confirmation — one misclick in the dropdown marks a real invoice paid (balance→0, paid_at stamped) and closes the job to done, and … | Add a window.confirm to 'collect-payment' stating the amount and irreversibility ('Record the full remaining balance as paid offline? This marks the … |
| W7-023 | Dashboard / insights | bug | `src/lib/dashboard/customers.ts:71` | Customers list 'Booked' lifetime-spend column and the customer-detail header count cancelled/declined/lost orders as booked spend, and statusOf shows a cancelled-approved quote as an 'Approved' badge. | Exclude deriveStatus ∈ {cancelled, declined, lost} from bookedSpend in both aggregateCustomers and the detail page; either extend CustomerStatus with… |
| W7-024 | Inbox / ingest | bug | `src/lib/dashboard/inbox/classify.ts:48` | Layer-2 substring phrase matching over subject+preview can classify a REAL customer message as 'automated': any inbound containing 'opt out', 'unsubscribe', or 'reply stop' as ordinary words is hidde… | Require stronger signals for the phrase layer (e.g. only apply phrases to email channel plus List-Unsubscribe/no-reply corroboration, or match whole … |
| W7-025 | Inbox / ingest | bug | `src/lib/dashboard/inbox/notify.ts:36` | Escalation/EOD emails interpolate customer-controlled name and message preview into HTML without escaping — HTML injection into team-wide emails | Add a small escapeHtml() (mirror the reply route's replace chain) and run i.name and i.preview through it in itemList before interpolation. Pure func… |
| W7-026 | Inbox / ingest | bug | `src/lib/dashboard/inbox/notify.ts:52` | The 'Open the inbox →' link in every escalation/EOD email is a relative href="/inbox", which is broken in email clients | Pass a baseUrl into escalationEmailHtml/eodDigestHtml (or reuse appBaseUrl() from telegramNotify — consider hoisting it) and emit `${baseUrl}/inbox`.… |
| W7-027 | Inbox / ingest | bug | `src/lib/dashboard/inbox/quotetool.ts:52` | The quote-tool adapter ignores terminal quote statuses (declined/cancelled added in PR #300): a declined quote still spawns a 'quote_sent_no_reply' follow-up that surfaces in the due-today strip unti… | In normalizeQuoteTouch treat terminal statuses (declined/cancelled/lost) as outbound/answered, and in quoteFollowUpDecision return kind:'close' for t… |
| W7-028 | Inbox / ingest | bug | `src/lib/dashboard/inbox/store.ts:201` | findExistingItem and findCandidates swallow query errors and return 'not found', so a transient DB error during a reconcile falsely reopens handled items and creates duplicate contacts | Make both reads distinguish error from not-found (return a sentinel / throw), and have ingestTouch skip that touch and count it in `errors` (the Reco… |
| W7-029 | Inbox / ingest | bug | `src/lib/dashboard/inbox/store.ts:616` | Persistent ingest failure of a source is recorded in sync_cursors (last_status='error') but surfaced nowhere — no UI reads it and no alert fires, so a revoked Gmail refresh token or expired GHL token… | Extend the escalation run to check the other sources' last_run_at/last_status and email the team when a source has been failing/stale for >N minutes;… |
| W7-030 | Inbox / ingest | bug | `src/lib/dashboard/inbox/store.ts:659` | The configurable 'Remind me to follow up after (days)' setting is never used where follow-ups are created: ensureFollowUp calls quoteSentNoReplyFollowUp without afterDays, so quote-sent reminders alw… | Pass `afterDays: await getFollowUpDays()` through runQuoteToolReconcile/ensureFollowUp when building the follow-up (or document the setting as stalen… |
| W7-031 | Inbox / ingest | perf | `src/lib/dashboard/inbox/store.ts:776` | getReopenCounts issues 6 sequential queries (3 windows x 2 actions), each transferring up to 5,000 raw inbox_item_id rows for a client-side distinct count — up to 30,000 rows moved on every /inbox pa… | Fetch inbox_item_id + created_at once per action (2 parallel queries) and compute the 3 windows in JS; or use head:true counts / an RPC with count(di… |
| W7-032 | Inbox / ingest | test-gap | `src/lib/dashboard/inbox/store.ts:1070` | reverseItemState — the audit-log Reverse state machine — is 70 lines of branching logic living in the 'intentionally untested thin I/O glue' half of store.ts, with zero test coverage of its guards | Extract the pure decision into lifecycle.ts — e.g. planReverse(activity: {action, detail}, current: {status, followedUpAt}) → { ok, target } \| { ok:… |
| W7-033 | Inbox / ingest | perf | `src/lib/dashboard/inbox/sync.ts:127` | runQuoteToolReconcile processes 500 quotes strictly sequentially with 2–5 DB round-trips each (~1,000–2,000 serial queries per run, every 5 minutes, mostly at steady state where nothing changed) — 5–… | Batch: one `.in('external_id', ids)` read for all 500 existing items, one batched contact-candidate fetch for the new ones, then write only changed r… |
| W7-034 | Inbox / ingest | bug | `src/lib/dashboard/inbox/sync.ts:183` | Gmail poll scans only the newest 25 in:inbox threads per run with no pagination or cursor — an email burst or a cron outage pushes an unanswered customer thread past position 25 before it is ever ing… | Paginate threads.list until reaching already-known unchanged threads (or use q=newer_than based on the sync cursor / history.list incremental sync), … |
| W7-035 | Inbox / ingest | perf | `src/lib/dashboard/inbox/sync.ts:192` | Gmail poll is a serial N+1 against the Gmail API: threads.list then one awaited threads.get per thread (26 sequential HTTP calls per run, every 2 minutes ≈ 18k calls/day) re-fetching the same mostly-… | Cheapest: Promise.all the getThread calls in chunks of 5–10. Better: persist each thread's historyId in the sync cursor and skip threads whose histor… |
| W7-036 | Dashboard / insights | bug | `src/lib/dashboard/metrics.ts:70` | KPI 'Active quotes' / active-customers count includes terminal-status quotes: the B7 fix excluded terminals from booked revenue but the active-quote branch still counts a declined/cancelled/changes_r… | Add `!isTerminal` (and decide on changes_requested) to the active-quote condition: `if (!approvedAt && !isTerminal && sentMs >= activeCutoff)`; exten… |
| W7-037 | Dashboard / insights | refactor | `src/lib/dashboard/needsAction.ts:61` | The needs-action 'nudge' bucket duplicates the worklist's 'sent-no-reply' kind — the same sent quote renders twice on the homepage (once under 'Drafts & unsent', once under 'Overdue / follow-up'), th… | Make the worklist drafts-only (drop the 'sent-no-reply' kind — the Needs-Action nudge bucket owns overdue sent quotes now), or explicitly own the spl… |
| W7-038 | Dashboard / insights | perf | `src/lib/dashboard/queries.ts:123` | listJobsForWorkflowBoard and listInvoicesForWorkflowBoard each run their own quotes is_test lookup (2 near-identical queries per dashboard load), and both lookups silently discard errors — a failed l… | Extract one shared lookupTestQuoteIds(quoteIds) helper called once with the union of ids (or fold is_test into the row read via a foreign-table selec… |
| W7-039 | Inventory | perf | `src/lib/inventory/jobs.ts:86` | listFulfillmentCards fetches ALL jobs (≤500, every status, including each row's line_items jsonb) and filters the two active statuses in JS — every Kanban render, /inventory overview, and WhatsApp co… | Query with the filter pushed to SQL — e.g. a dedicated fetch `.from('jobs').select(JOB_SELECT).in('status', ['to_schedule','scheduled'])` (no limit n… |
| W7-040 | Inventory | bug | `src/lib/inventory/jobs.ts:266` | Stock deduction is a non-atomic read-modify-write of absolute quantities — two jobs prepped near-simultaneously (or a prep concurrent with a staff Stock-tab edit) lose one of the deductions (last wri… | Make the deduction atomic: a Postgres RPC (`update inventory_on_hand set on_hand_qty = greatest(0, on_hand_qty - $need) where sku = $sku`) or an opti… |
| W7-041 | Inventory | bug | `src/lib/inventory/materialsProjection.ts:231` | A mini-group (grouped railing) always projects WARM-WHITE mini strands regardless of the color its member strands are drawn in — staff order the wrong-color lights for any non-warm-white railing appr… | Derive the group's effective color from its member strands (e.g. the first member's colorPattern, or the deduped member color set fed through matchPa… |
| W7-042 | Inventory | bug | `src/lib/inventory/purchaseOrder.ts:55` | buildSupplierPurchaseOrder treats every failed Supabase query as empty data; in the unattended auto-send path this (a) resets the dedup signature on a phantom-empty PO → the SAME unchanged order is r… | Check `error` on each query in buildSupplierPurchaseOrder and throw (the auto-send route will then 500 instead of acting on phantom data); only call … |
| W7-043 | Inventory | bug | `src/lib/inventory/purchaseOrder.ts:148` | A failed auto-send purchase order is invisible to staff: success fires a Telegram ping, failure only console.errors — during the trial a persistent GHL/send failure means materials silently never get… | In the failure paths of emailSupplierPurchaseOrder (or in triggerAutoPOIfBusy / the cron on `!res.ok`), send a best-effort notifyTelegram alert ('Aut… |
| W7-044 | Inventory | bug | `src/lib/inventory/purchaseOrder.ts:177` | recordAutoSentSignature ignores the upsert result (supabase-js returns { error }, it does not throw) — if the app_settings write fails after a successful supplier email, auto-send reports { ok: true,… | Check `error` from the upsert and throw (or at minimum console.error and surface `recorded: false` in the route response) so a failed dedup write is … |
| W7-045 | Inventory | test-gap | `src/lib/inventory/purchaseOrder.ts:197` | triggerAutoPOIfBusy — the path that auto-emails real supplier orders from the deposit-paid webhook — has zero tests: the flag gate, the ≥N job threshold, the signature dedup, and the 'send failure mu… | Add a triggerAutoPOIfBusy describe block (mock buildSupplierPurchaseOrder + the email/signature seams like the existing tests do): disabled flag → no… |
| W7-046 | Inventory | refactor | `src/lib/inventory/purchaseOrder.ts:205` | The auto-send flow exists twice — triggerAutoPOIfBusy and an inline re-implementation in the auto-send cron route — and they have already drifted: the cron resets the dedup signature when the shortfa… | Make the cron route call triggerAutoPOIfBusy (e.g. `{minJobCount: 0}`) and map the structured result to HTTP, moving the shortfall-cleared signature … |
| W7-047 | Inventory | bug | `src/lib/inventory/purchaseOrder.ts:209` | Auto-PO dedup is check-then-act (TOCTOU): the deposit-webhook trigger and the 3-day cron (or two concurrent deposit webhooks) can both pass the signature check and both email the supplier the same or… | Serialize via a conditional write: record the signature with a compare-and-swap (e.g. update app_settings where value->>signature = <old>) BEFORE sen… |

### LOW (49)

| id | area | type | file:line | summary | fix |
|---|---|---|---|---|---|
| W7-048 | Console pages | refactor | `src/app/admin/invoices/[id]/page.tsx:9` | Both 'use client' detail pages value-import reconcileInvoice from the server data layer (@/lib/invoices), dragging @supabase/supabase-js + the service-role client module + the displayId RPC allocator… | Move reconcileInvoice + InvoiceReconciliation (pure, no IO) into a client-safe module (e.g. src/lib/invoiceStatus.ts, already imported client-side, o… |
| W7-049 | Console pages | bug | `src/app/admin/invoices/[id]/page.tsx:139` | chargeBalance's confirm dialog quotes the page's STALE loaded invoice.balance while the route charges the fresh server-side balance with no expected-amount check — the saved card can be charged a dif… | Send the displayed balance in the POST body (expectedBalanceUsd) and have the route 409 on mismatch (>1¢) so the operator re-loads and re-confirms; a… |
| W7-050 | Console pages | bug | `src/app/admin/invoices/[id]/page.tsx:244` | The derived 'Rush / takedown fees' breakdown line labels ANY positive residual (total − subtotal + discount − tax) as fees — on an over-discounted invoice the engine's subtotal clamp breaks the ident… | Clamp the stored discount to the subtotal at invoice-build time (computeInvoiceTotals) so the identity always holds, or label the derived line 'Fees … |
| W7-051 | Console pages | test-gap | `src/app/admin/jobs/[id]/page.tsx:64` | Zero tests cover the money-action handlers' route-response mapping on either detail page (complete → settled messaging, cancel → refund flags, amend → delta/new_balance/notify messaging, charge-balan… | Add a colocated component test per detail page (vi.mock fetch per charter house style) asserting the operator message for the key money outcomes: can… |
| W7-052 | Console pages | consistency | `src/app/admin/jobs/[id]/page.tsx:70` | The three money-action handlers (markComplete, recordAmendment, cancelOrder) omit the 401 → /login bounce that load() in the same file and EVERY handler in the sibling invoices/[id] page apply — an e… | Add the same `if (res.status === 401) { window.location.href = ... }` block to the three handlers (or extract the tiny fetch-with-401-bounce helper a… |
| W7-053 | Console pages | bug | `src/app/admin/jobs/[id]/page.tsx:159` | Both detail pages pass active="quotes" to OperatorShell, so the top nav highlights 'Quotes' while the operator is on a Jobs or Invoices detail page — contradicting their own BillingSubNav and the lis… | Change to active="jobs" in jobs/[id]/page.tsx and active="invoices" in invoices/[id]/page.tsx. |
| W7-054 | Console pages | refactor | `src/app/admin/jobs/[id]/page.tsx:268` | The reconciliation-issues block (flagLabels map + red-box markup + summary line) plus the money()/fmtDate helpers are duplicated verbatim between the job and invoice detail pages — a flag-label or wo… | Extract a shared client component (e.g. src/components/admin/InvoiceReconciliationPanel.tsx taking the invoice row) owning the flagLabels map + marku… |
| W7-055 | Console pages | docs-drift | `src/app/admin/jobs/[id]/page.tsx:330` | The Amend section's copy tells the operator 'A price change sends it back for the customer to re-approve' — but nothing is sent back and no re-approve surface exists anywhere, so the operator may bel… | Reword to the true manual process, e.g. 'A price change should be OK'd by the customer before you collect the new balance — use the Notify checkbox (… |
| W7-056 | Inbox / ingest | perf | `src/app/api/dashboard/reply/route.ts:80` | After the GHL send succeeds, the reply route makes the operator wait on a fully sequential post-send chain — markItemHandledLocal (2 reads + write + insert), then up to 4 serial external GHL/Gmail wr… | Run the independent write-back steps with Promise.allSettled, and/or move the whole write-back + recordWriteback after the response via waitUntil (th… |
| W7-057 | Inventory | bug | `src/app/api/inventory/low-stock-alert/route.ts:32` | A failed on-hand read makes the daily low-stock cron report success with lowCount 0 — a DB blip silently skips the day's alarm with no error signal or retry. | Propagate the query error (strict listOnHand variant or an error flag) and return non-200 from the cron so the failure is visible in Vercel cron logs… |
| W7-058 | Inventory | consistency | `src/app/api/inventory/on-hand/route.ts:41` | PUT /api/inventory/on-hand blindly casts on_hand_qty/reorder_point (`as number \| undefined`) instead of validating per charter §2 — a malformed body silently ZEROES a stock count (toQty coerces garb… | Guard in the route: if on_hand_qty/reorder_point are present and not finite non-negative numbers → 400; keep toQty as the last-line floor. Same for s… |
| W7-059 | Inventory | test-gap | `src/app/api/inventory/purchase-order/auto-send/rout…:30` | The unattended real-money supplier-ordering cron has no colocated route test — its ordering-sensitive dedup transitions (401 gate → enabled-flag no-op → reset-signature-on-empty → skip-on-unchanged →… | Add auto-send/route.test.ts modeled on low-stock-alert/route.test.ts: mock buildSupplierPurchaseOrder/emailSupplierPurchaseOrder/signature storage, a… |
| W7-060 | Other | perf | `src/app/customers/[contactId]/page.tsx:74` | Customer detail serializes two independent network calls: the quote_view_events read and the HighLevel getContact fetch run back-to-back though neither depends on the other | Promise.all the view-events read and the (conditional) getContact call after the quotes load — e.g. `const [viewEvents, contactResult] = await Promis… |
| W7-061 | Inbox / ingest | dead-code | `src/app/inbox/activity/page.tsx:11` | ActivityPage awaits getOperator() in Promise.all and discards the result | Drop getOperator() from the Promise.all (page access is already gated by the middleware perimeter): `const res = await listActivity();` |
| W7-062 | Dashboard / insights | perf | `src/app/insights/page.tsx:59` | Sequential independent awaits: the quotes read completes before the inbox metrics/reopen batch starts, serializing two independent round-trip batches | Start all three in one Promise.all (or fire listQuotesForDashboardResult unawaited before the error-banner branch and await it alongside the inbox ba… |
| W7-063 | Inventory | consistency | `src/app/inventory/jobs/[id]/print/page.tsx:19` | The printable work order — the only other server-rendered inventory page, and one that renders customer name + address — lacks the dormant getOperator() defense-in-depth re-check that the overview pa… | Add the same two-line dormant check as the overview page: `if (process.env.AUTH_GATE_ENABLED === 'true' && !(await getOperator())) redirect('/login?f… |
| W7-064 | Inventory | refactor | `src/app/inventory/jobs/page.tsx:21` | The Kanban page hand-copies the WorkOrder/MaterialRow/UnboundConcept shapes (lines 19-24) and casts res.json() to them, and materials/page.tsx:14-16 does the same for MaterialsView — the exact S18 's… | Replace the local type declarations with `import type { WorkOrder } from '@/lib/inventory/jobs'` (jobs/page.tsx) and `import type { MaterialRow, Unbo… |
| W7-065 | Inventory | bug | `src/app/inventory/jobs/page.tsx:57` | Kanban move() captures a whole-board snapshot (`const prev = cards`) for its failure revert — two quick stage moves where the FIRST PATCH fails reverts BOTH optimistic updates, visually undoing the s… | Revert per-card functionally: capture the moved card's previous stage and `setCards((cs) => cs.map((c) => c.id === id ? { ...c, stage: prevStage } : … |
| W7-066 | Inventory | perf | `src/app/inventory/materials/page.tsx:30` | The Materials page fetches GET /api/quotes - 500 quote rows with 17 columns including customer phone/email and full lifecycle timestamps - to power a picker that renders 50 rows and uses only id, cus… | Add a lightweight quote-picker endpoint (or a ?fields= / ?q= param on /api/quotes) returning id, customer_name, total, created_at with server-side se… |
| W7-067 | Inventory | docs-drift | `src/app/inventory/materials/page.tsx:77` | Staff-visible copy still says roofline materials aren't included — 'Roofline bulbs/wire/clips come later (Slice 2b)' (line 77) and the empty state 'roofline-only — bulbs/wire/clips arrive in Slice 2b… | Update the two on-page strings (a roofline-only design with a yardstick DOES project materials now — the empty state mainly means 'no yardstick/geome… |
| W7-068 | Inventory | bug | `src/app/inventory/orders/page.tsx:44` | The human-gated 'Send to supplier' POST rebuilds the purchase order server-side at send time, so the order actually emailed can differ from the lines staff just reviewed on screen (no signature/confi… | Have the client send purchaseOrderSignature(po.lines) (already exported from purchaseOrder.ts) with the POST; the server recomputes, compares, and 40… |
| W7-069 | Other | perf | `src/app/page.tsx:48` | Dashboard page fetches the jobs and invoices tables TWICE per load (workflow board + Needs-Action) and pays an extra sequential round-trip pair for the second fetch | Fetch jobs and invoices once with the union of columns (jobs: id, quote_id, status, created_at, line_items; invoices: id, job_id, quote_id, status, b… |
| W7-070 | Inbox / ingest | bug | `src/components/dashboard/inbox/FollowUpStrip.tsx:17` | Follow-up 'Done' and InboxList Handled/Dismiss/Followed/Completed actions optimistically remove the row but never check res.ok, so an HTTP 4xx/5xx failure is silently swallowed — for FollowUpStrip th… | Check res.ok in both handlers and restore the removed item (plus a small inline error) on failure, matching DuplicatesList's restore-on-reject patter… |
| W7-071 | Dashboard / insights | docs-drift | `src/components/dashboard/KpiStrip.tsx:31` | The Conversion KPI's sub-label reads 'approved / sent' but the metric is deliberately approved / (sent OR approved) — the label misstates the denominator that types.ts and the regression test careful… | Change the sub to 'approved / reached' to match the Insights page's wording. |
| W7-072 | Inventory | refactor | `src/components/inventory/OnHandStock.tsx:16` | The 'effective category' rule (yll_category ?? category) is duplicated as three identical named consts (OnHandStock.tsx:16, bindings/page.tsx:39, overrides/page.tsx:17) plus inline in SkuPicker.tsx:1… | Export one `export const effectiveCategory = (i: CatalogItem) => i.yll_category ?? i.category` from lib/inventory/catalog.ts (or skuSearch.ts) and im… |
| W7-073 | Inbox / ingest | docs-drift | `src/lib/dashboard/inbox/followups.ts:5` | followups.ts header still says '⚠️ NOT YET WIRED (#58 gap): nothing creates follow-ups yet and the /inbox UI has no due today strip' — both halves are now false | Replace the stale warning block (followups.ts:5-9) with a one-liner pointing at the live wiring (sync.ts runQuoteToolReconcile + FollowUpStrip). |
| W7-074 | Inbox / ingest | bug | `src/lib/dashboard/inbox/store.ts:303` | The item upsert has no monotonic guard: a concurrent or stale reconcile (webhook-triggered run racing the 60s cron) can overwrite a newer message's last_message_at/preview/direction with an older sna… | Make the upsert conditional on newer data (e.g. a WHERE last_message_at <= excluded.last_message_at via RPC, or read-compare-write keyed on the times… |
| W7-075 | Inbox / ingest | perf | `src/lib/dashboard/inbox/store.ts:356` | listOpenItems' "returning" tally fetches one raw row per inbox item for every open contact with no .limit — row transfer grows with per-contact history, silently truncates at PostgREST's default 1,00… | Replace with a grouped count (RPC `select contact_id from inbox_items where contact_id = any($1) group by contact_id having count(*) > 1`) or per-con… |
| W7-076 | Inbox / ingest | dead-code | `src/lib/dashboard/inbox/store.ts:534` | listFollowedItems (plus its FollowedItem/FollowedItemsResult types) has zero importers — superseded by listInWorks in the v3 lifecycle work | Delete listFollowedItems, FollowedItem, and FollowedItemsResult from store.ts. |
| W7-077 | Inbox / ingest | bug | `src/lib/dashboard/inbox/store.ts:772` | getReopenCounts fetches dashboard_activity with .limit(5000) and no ORDER BY — once handled/reopened event rows exceed 5000, Postgres returns an arbitrary subset and the reopen-rate KPI (all-time and… | Add .order('created_at', { ascending: false }) so the cap keeps the newest events, and surface a truncated flag like listItemsForMetrics does. |
| W7-078 | Inbox / ingest | perf | `src/lib/dashboard/inbox/store.ts:906` | listInWorks awaits its two independent inbox_items queries (awaiting + handled groups) sequentially on the /inbox page's critical render path — Promise.all would halve this helper's latency for free. | const [aw, hd] = await Promise.all([...]) — no behavior change. |
| W7-079 | Inbox / ingest | bug | `src/lib/dashboard/inbox/suppression.ts:47` | Suppression list updates are read-modify-write over one app_settings row with no concurrency control: concurrent dismisses/reverses lose each other's updates — worst case a Reverse's un-suppress is c… | Move suppression to its own table with one row per sender (insert/delete are naturally concurrent), or mutate the JSON array server-side via an RPC. |
| W7-080 | Inbox / ingest | bug | `src/lib/dashboard/inbox/sync.ts:121` | A quote that leaves the reconcile scan set after its inbox item was created (marked is_test later, deleted, or aged past the newest-500 window while still a draft) orphans a permanently-'unresponded'… | Either include is_test rows in the scan and skip/dismiss their touches explicitly, or add a reconcile pass that dismisses quotetool items whose exter… |
| W7-081 | Inbox / ingest | consistency | `src/lib/dashboard/inbox/sync.ts:121` | runQuoteToolReconcile uses the error-swallowing listQuotesForDashboard, so a failed quotes query records an 'ok' heartbeat with scanned:0 — the lead-ingestion cron can silently stop working | Switch to listQuotesForDashboardResult; on !ok, recordSyncRun('quotetool','error', result.error) and return {ok:false,...} so the cron 500s and the s… |
| W7-082 | Dashboard / insights | bug | `src/lib/dashboard/metrics.ts:13` | customerKey uses raw email/phone/name with no normalization (case, whitespace, phone formatting), so the same non-CRM customer splits into multiple 'customers' with divided quote counts and booked sp… | Normalize in customerKey: lowercase+trim email, strip non-digits from phone, trim/casefold name — one function change covers the KPI, customers list,… |
| W7-083 | Dashboard / insights | dead-code | `src/lib/dashboard/metrics.ts:81` | Kpis.activeCustomers is computed on every dashboard render and covered by four tests, but no component ever displays it — KpiStrip renders only 5 of the 6 Kpis fields. | Either add the 'Active customers' card to KpiStrip (the 6-column grid already has room since the prominent card spans 2 → currently exactly full; add… |
| W7-084 | Dashboard / insights | cost | `src/lib/dashboard/queries.ts:107` | Workflow board selects the full line_items jsonb for up to 1000 jobs only to sum per-item amounts client-side | Either store/maintain a total column on jobs (it's written by the tool at job creation), or compute the sum server-side (RPC / generated column), and… |
| W7-085 | Dashboard / insights | bug | `src/lib/dashboard/serviceMetrics.ts:51` | HolidayBreakdown ignores its documented 'current season (Sep–Feb window)' — bookedTotal, the season-goal bar, and byMonth aggregate LIFETIME approved holiday quotes, so from season 2 onward the '47/5… | Derive the current season window from `now` (e.g. Jul 1 → Jun 30, or the documented Sep–Feb) and filter quotes by the attribution month (homeworks_si… |
| W7-086 | Inbox / ingest | bug | `src/lib/integrations/whatsapp.ts:111` | The WhatsApp/Telegram bot 'move <job#> ready' command reaches Ready-For-Install via setJobFulfillmentStage — a bare fulfillment_stage write with NO stock deduction — a second, typo-easy entrypoint to… | Route the bot's move-to-ready through the same prep path as 'prep' (call prepareJobMaterials when the target stage is ready_for_install), or reject a… |
| W7-087 | Inbox / ingest | bug | `src/lib/integrations/whatsapp.ts:137` | The bot's 'set <sku> <qty>' command upserts any arbitrary string as a new inventory_on_hand row: a typo'd SKU silently creates a phantom row and reports success while the real SKU's stock stays wrong. | Have 'set' verify the SKU exists in listOnHand()/catalog first and reply "<sku> isn't tracked — check the SKU" instead of creating a row (mirror the … |
| W7-088 | Inbox / ingest | bug | `src/lib/integrations/whatsapp.ts:137` | The bot 'set <sku> <qty>' command writes an absolute on-hand value with the same non-atomic last-writer-wins race the sweep flagged for jobs.ts:266 — a bot `set` concurrent with a prep deduction (or … | Treat the bot as a first-class writer in whatever concurrency fix jobs.ts:266 gets (atomic delta/RPC for deductions; for `set`, an explicit absolute-… |
| W7-089 | Inventory | refactor | `src/lib/inventory/detectUnfulfillable.ts:22` | The 'colorable mini/spritzer item' classification is triplicated (resolveInstalls.colorables, detectUnfulfillable.targets, materialsProjection's inline branch) along with MINI_SURFACES ×3 and colorLa… | Extract one shared helper (e.g. inventory/colorables.ts exporting MINI_SURFACES, colorLabel, and a classifyColorables(items) walker) consumed by all … |
| W7-090 | Inventory | dead-code | `src/lib/inventory/jobs.ts:51` | groupByStage is exported but has zero non-test importers — the Kanban page it was written for buckets cards inline with cards.filter() per column instead. | Either use groupByStage in the Kanban page (one call instead of N filters) or delete the export + its describe block. Keeping a tested-but-unused exp… |
| W7-091 | Inventory | perf | `src/lib/inventory/jobs.ts:146` | getJobWorkOrder makes 7 Supabase round-trips in 5 sequential stages, including a fully redundant second query to the SAME jobs row just for stock_decremented_at; most stages are independent and could… | Add stock_decremented_at to the job select (or select it in getJob's query via a second select string) and run the design/quote pair, getInventoryBin… |
| W7-092 | Inventory | perf | `src/lib/inventory/jobs.ts:264` | prepareJobMaterials writes the stock deductions one SKU at a time in a sequential await loop - N round-trips (typically 10-30 SKUs per job) where one batched upsert would do. | Batch into a single sb.from('inventory_on_hand').upsert(rows, { onConflict: 'sku' }) call (the per-SKU try/catch granularity moves to one all-or-noth… |
| W7-093 | Inventory | bug | `src/lib/inventory/materialsProjection.ts:185` | Multi-color roofline bulb split rounds each color independently, so the per-color quantities can sum to fewer (or more) bulbs than the run needs | Use largest-remainder allocation: floor each share, then distribute the remaining (bulbCount − sum) bulbs to the largest fractional parts so totals a… |
| W7-094 | Inventory | perf | `src/lib/inventory/purchaseOrder.ts:55` | buildSupplierPurchaseOrder scans the jobs table unbounded with only .is('stock_decremented_at', null) and filters status in memory - cancelled/done jobs (never prepped, so the column stays NULL) accu… | Add .in('status', ['to_schedule','scheduled']) to the jobs query (the status filter already exists in code as isActiveFulfillment), and Promise.all t… |
| W7-095 | Inventory | perf | `src/lib/inventory/purchaseOrder.ts:91` | buildSupplierPurchaseOrder awaits its 6 queries fully sequentially even though bindings, on-hand, and catalog are independent of the jobs→quotes→designs chain | Promise.all the independent reads: kick off getInventoryBindings/listOnHand/listCatalog alongside the jobs→quotes→designs chain and await them where … |
| W7-096 | Inventory | perf | `src/lib/inventory/purchaseOrder.ts:201` | triggerAutoPOIfBusy runs the full PO build (6 queries + all scene projections) BEFORE checking the minJobCount threshold, so every below-threshold deposit webhook pays the heavy build for nothing | Check the active-non-test job count first with a cheap head/count query (jobs where stock_decremented_at is null + active statuses) and bail before c… |


---

## Wave 6 hand-off — event/permanent money engines (audit-coverage gap, 2026-07-04)

> Surfaced by W6-007: the #110 coverage manifest had drifted — **6 money-domain modules from your
> #88 permanent-install + event-lighting work (~1007 lines) were never assigned to any audit wave,
> so no wave money-audited them.** They price real quotes, so they want a money-lens pass before the
> Aug–Oct trial. The manifest generator is now fixed (assigns them to W1/money) so it stops drifting;
> **the actual audit is handed to you** (they're your code, your lane). Jason's call, 2026-07-04.

**Files to money-lens** (each has a unit-test sibling, so this is audit-triage, not zero-coverage):
- `src/lib/event/pricing.ts` · `src/lib/event/packages.ts` · `src/lib/event/rates.ts` · `src/lib/event/types.ts`
- `src/lib/permanent/pricing.ts` · `src/lib/permanent/bom.ts` · `src/lib/permanent/projectPermanent.ts` · `src/lib/permanent/derivePackagesPermanent.ts` · `src/lib/permanent/types.ts`
- shared money helpers `src/lib/agreedTotal.ts` · `src/lib/money.ts` (these DID get touched by W1 fixes; a re-confirm is cheap)

**Lens to apply** (same as #110 W1 money-core): does the billed total match the approved selection · rounding
(use the shared `round2`/`money.ts`, no ad-hoc float math) · deposit/gate math · BOM projection quantity
correctness (largest-remainder for any multi-way split — cf. W7-093) · no silent 0/NaN price on a missing field ·
override/discount interaction · idempotency on any money-mutating path. Run it as a small finder→verify pass like
the other waves; dedupe against W1 (the holiday engine) since these mirror its shape.
