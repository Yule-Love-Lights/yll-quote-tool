# Yule Love Lights Quote Tool — Quality Audit (#110)

**Started:** 2026-07-02 (Jason S20) · **Baseline:** `c04447c` (re-frozen per wave; each wave
section records its own SHA) · **Plan:** `AUDIT-PLAN-2026-07.md` · **Coverage:**
`COVERAGE-MANIFEST-2026-07.md` (648 files, 100% assigned) · **Dedupe:**
`KNOWN-FINDINGS-2026-07.md` · **Fix conventions:** `CONVENTIONS-CHARTER-2026-07.md`

**Method per wave:** dimension finders (bug / perf / refactor / dead-code / consistency +
wave-specific lenses) → adversarial verify every finding → dedupe vs known-findings →
completeness critic → append here → disposition with Jason (CRIT/HIGH/MED individually, LOWs
batched default-accept) → fix PRs → close wave.

**Finding schema:** `id (W<wave>-NNN)` · area · type (bug | security | perf | cost |
refactor | dead-code | consistency | test-gap | a11y | docs-drift) · severity · confidence ·
`file:line@SHA` · summary · evidence · suggested fix · effort (S/M/L) · disposition
(fix-now / fix-later / accept / feed-#29 / hand-to-Naldo). LOWs may use the slimmed schema.

## Wave status

| Wave | Area | Audit | Disposition | Fixes | Closed |
|---|---|---|---|---|---|
| 0 | Scoping | ✅ 2026-07-02 | — | — | ✅ |
| 1 | Money core | ✅ 2026-07-04 | ✅ 2026-07-04 | ✅ PRs A–E merged (master `1e65602`, vitest 1756) | ✅ |
| 7 | Naldo's areas (early, read-only) | ✅ 2026-07-04 (96 findings) | n/a (Naldo) | n/a — handoff `AUDIT-2026-07-NALDO-HANDOFF.md` | ✅ audit |
| 2 | Data layer | ✅ 2026-07-04 (36 findings) | 🔵 notes-only (recommended) | ⏸️ deferred to a fix wave | audit ✅ |
| 3 | Dense files | — | — | — | — |
| 4 | Portal | ✅ 2026-07-04 (34 findings) | 🔵 notes-only | ⏸️ deferred to a fix wave | audit ✅ |
| 5 | AI / training | — | — | — | — |
| 6 | Routes + cross-cutting | — | — | — | — |

## Wave 0 — scoping (2026-07-02 @ `c04447c`)

No findings produced (by design). Artifacts:
- **Coverage manifest** — 648 files → exactly one wave each (W1:100 · W2:76 · W3:35 · W4:62 ·
  W5:43 · W6:137 · W7:195). Generator `scripts/audit/coverage-manifest.mjs` exits nonzero on
  any unassigned file; re-run at each wave start.
- **Conventions charter** — the 6 house patterns fix-PRs must follow (auth gate · validation ·
  error shape · client/server boundary · data access · tests) + epic-wide rules.
- **Known-findings dedupe ledger** — 135 seeded rows: all 109 #80 findings status-mapped
  through the real fix trail (PRs #152/#157/#173-g\*/#222/#227–#230/#236/#290 — **80 FIXED ·
  3 PARTIAL · 4 UNKNOWN · 22 OPEN**, + 3 Supabase-advisor WARNs open) · 8 PR #300 fixes ·
  18 S17–S19 review dispositions. The 22 OPEN #80 findings are live audit input — waves
  covering their files re-verify rather than re-discover.
- **Naldo handoff doc** — created as the append-target for all waves' hand-to-Naldo findings.
- **Test-coverage proxy** (test files per area — no coverage provider installed;
  characterization-test gate applies to the thin ones):

| Area | src : test | Signal |
|---|---|---|
| `lib/pricing` | 2 : 1 | pricingEngine.test.ts is 860L — **well covered** |
| `lib/portal` | 7 : 5 | good |
| `lib/design` | 8 : 7 | good |
| `lib/dashboard` | 34 : 30 | good (Naldo) |
| `lib/inventory` | 17 : 19 | good (Naldo) |
| `lib/integrations` | 14 : 11 | good |
| `app/api/quotes` | 17 : 14 | good |
| `components/quote` | 2 : 0 | **THIN — QuoteBuilder.tsx 3,276L has zero direct tests** |
| `components/design` | 23 : 3 | **THIN — editor.ts 5,291L covered only via keymap/yardstick tests** |
| `components/portal` | 36 : 2 | **THIN** |
| `app/training` | 4 : 0 | **THIN — training/new/page.tsx 1,459L untested** |

Pattern: the data/logic layer is well-tested; the big UI components are not — any wave-3/4
refactor hits the characterization-test gate first.

---

_Wave sections append below as they complete._
## Wave 1 — money core (2026-07-04 @ `c04447c`)

**Method run:** 15 finders (5 sub-areas × correctness/efficiency/quality) → 100 raw → 85 after
dedupe (15 merged) → adversarial verify: **74 confirmed, 11 refuted** → completeness critic
raised 5 gap candidates, all verified in → **79 findings**. (Run interrupted once by a usage
limit and resumed from the journal — 27 verifies + the critic re-ran; no loss.)

| Severity | Count |
|---|---|
| CRITICAL | 1 |
| HIGH | 4 |
| MEDIUM | 19 |
| LOW | 55 |

**Disposition status: recorded 2026-07-04 (Jason).** Fix-nows -> PRs A-E (in flight); fix-laters convert to ledger rows at wave close; LOWs accepted except 6 pulled into PRs A/E.

**✅ FIX OUTCOME (2026-07-04, all merged to master `1e65602`, gates tsc 0 · eslint 0 · vitest 1756, +61 tests):**
- **PR-A [#331]** — W1-001 (CRITICAL), W1-004, W1-043 fixed via a shared `resolveAgreedTotal`/`amendedAgreedTotal` helper (invoice + amend + convert bill the approved SELECTION total, not `result.total`). **W1-068 degraded to fix-later** (portal selection ids don't map to `result.lineItems[].id`; cosmetic).
- **PR-B [#336]** — W1-007/011/014/016/017/018/022/023 (8 status-guard/race) fixed via terminal-status exclusions + atomic CAS predicates.
- **PR-C [#330]** — W1-005 fixed (adapter drops billed roofline by stable id, not label regex).
- **PR-D [#329]** — W1-002 (canonical `MINI_LIGHT_TYPES`, no narrow drift) + W1-003 (booked re-price → 409) fixed.
- **PR-E [#334]** — W1-009/015/021/024 + pulled LOWs W1-027/050/063/064 fixed; W1-012 recorded as an auto-charge blocker in `docs/jobber-flow/VALOR-AUTOCHARGE-FOR-JASON.md`.
- **23 of 24 fix-nows landed** (W1-068 → fix-later). ⚠️ **SHARED**: A/B touched `invoices.ts`/`jobs.ts` (Naldo's #83 modules) — Jason authorized the merge; Naldo to be looped post-merge.
- **⏳ OUTSTANDING (policy step):** post-merge in-browser prod smoke-test of the approve → invoice → pay-balance money flow (the +61 unit tests are the substantive verification in hand; browser confirmation deferred to a focused session to conserve usage).

**FIX-LATER BACKLOG (→ ledger, #110 wave-1 tail):** W1-006 (2nd-deposit durable record + alert) · W1-008 (cancel-of-paid refund record — needs design) · W1-010 (design-projection route tests) · W1-013 (approve discount/waive/rush snapshot tests) · W1-019 (portal-open sequential DB round-trips) · W1-020 (GHL stage-config knob drift) · W1-068 (job `line_items` selection filter). Plus **known-open carried:** W1-073 (#80-076 autoRoofline tie) · W1-074 (multi-spritzer single line).

### CRITICAL (1)

#### W1-001 · bug · `src/lib/invoices.ts:435` · effort M

**createInvoiceFromJob prices the invoice from the FULL quote.result total, ignoring the selection-based total the customer actually approved (approval_snapshot.customerSelection.currentTotalUsd) — over/under-bills any order whose portal selection diverged from the staff-built quote, and the wrong balance is really charged via the pay-balance Valor link.**

- **Evidence:** createInvoiceFromJob selects only 'id, result, deposit_amount_usd, deposit_paid_at' (invoices.ts:415) and computes totals from `quote.result ?? { total: 0 }` (line 435→442). Nothing ever writes the approved selection back to quotes.result: the approve route persists only customer_approved_at + approval_snapshot + status (approve/route.ts:540-553) after server-recomputing the SELECTION price (serverSubtotal over selectedItemIds, route.ts:404-487). Billed total being selection-based is the locked design, and the Valor webhook itself acknowledges result/total ≠ agreed total by preferring snapshot.currentTotalUsd for the GHL card value (webhook/route.ts:375-379, the #107 fix). result.total = engine-chosen roofline + ALL items + fees + tax (pricingEngine.ts:727-837). Failure scenario: quote result.total $5,437.50 (Santa's Village $4,000 auto-chosen, garland $600, wreath $400, 8.75% tax); customer picks the cheaper Gingerbread tier ($2,800) and deselects the wreath → approves $3,697.50, pays $1,848.75 deposit (webhook records it). Job completed → invoice total $5,437.50, balance $3,588.75; /api/quotes/[id]/pay-balance charges invoice.balance on Valor's hosted page (pay-balance/route.ts:87-96) → customer over-charged $1,740 vs the $1,848.75 they owe. Under-billing direction too: rush/takedown toggled ON by the customer (in the snapshot) but absent from staff inputs never reach result.total. reconcileInvoice's short-deposit flag (deposit < 40% of total) only catches divergences > ~20%, so smaller deselections settle silently. invoices.test.ts fixtures never include an approval_snapshot — the mismatch is untested. setInvoiceTaxOverride re-prices from the same wrong source (invoices.ts:592-599).
- **Fix:** Price the invoice from the agreed total: last amendment's new_total ?? approval_snapshot.customerSelection.currentTotalUsd, falling back to result.total only when no snapshot exists (mirroring amend/route.ts:113's precedence) — or re-price the frozen selectedItemIds server-side via the same buildPortalLineItems + priceSelection path approve uses, so the breakdown (subtotal/discount/fees/tax) also matches the selection. Apply the same source to setInvoiceTaxOverride's re-price.
- **Verifier:** createInvoiceFromJob prices the invoice entirely from quote.result (src/lib/invoices.ts:415, 435→442), but the customer's approved total is selection-based and lives only in approval_snapshot: the approve route server-recomputes the selection price and persists only customer_approved_at + approval_snapshot + status (approve/route.ts:404-488, 540-553), and the sole writer of quotes.result anywhere in src/ is staff updateQuote (quotes.ts:239-242) — no step reconciles the selection into result. The "INTENTIONAL" comment (invoices.ts:427-434) only defends live result vs the job's booking-time line_items snapshot (amendments), not this divergence. The codebase elsewhere already treats the snapshot as the agreed total — amend's prior-total precedence is new_total ?? snapshot.currentTotalUsd ?? result.total (amend/route.ts:110-113) and the Valor deposit webhook prefers snapshot.currentTotalUsd over result.total (valor/webhook/route.ts:375-379, the #107 fix) — making the invoice creator the one money consumer on the wrong source. Any customer who deselects an item, picks the non-recommended roofline tier, or toggles rush/takedown (all first-class portal features) pays a selection-based 50% deposit yet gets an invoice whose total is the full result.total, and pay-balance charges that incoherent invoice.balance on a real Valor hosted page (pay-balance/route.ts:87-96). setInvoiceTaxOverride re-prices from the same wrong source (invoices.ts:592-599); no guard, UI surface, or test (invoices.test.ts fixtures have no approval_snapshot) catches the mismatch. Wrong money on the plain happy path in prod = CRITICAL as stated. Not in KNOWN-FINDINGS (the nearest rows, #80-013/#80-032, are the approve route's client-vs-server totals, FIXED; PR #300's invoice fixes are unrelated).
- **Disposition:** **fix-now → PR-A** (CRITICAL escape hatch)

### HIGH (4)

#### W1-002 · bug · `src/app/api/quote/route.ts:38` · effort S

**VALID_MINILIGHT_TYPES is a stale narrow duplicate of the engine's MiniLightItem.type — it is missing 'curtain', so any design-linked quote containing a curtain group becomes un-re-priceable after reopen (400 before the projection can replace the arrays).**

- **Evidence:** pricingEngine.ts:96 types MiniLightItem.type as 'tree'|'bush'|'column'|'railing'|'curtain' (curtain billed at the railing rate, #100; NO_WRAP_STYLE_TYPES:498 includes it), and projectScene.ts:96-99,120-126 emits {type:'curtain', wrapStyle:'canopy', stringCount} mini inputs. The route validates the BODY's miniLightItems at lines 185-191 (VALID_MINILIGHT_TYPES.has fails for 'curtain') BEFORE the design projection replaces the arrays at 226-231. Concrete failure: staff draws a curtain group on a design-linked quote → first Calculate passes (form arrays have no curtain yet) → the route saves the PROJECTED inputs (with type:'curtain') into the inputs jsonb (saveQuote at 260) → staff reopens the quote (quoteForm.ts inputsToFormData:263 rehydrates form.miniLightItems from the stored jsonb) → every subsequent Calculate / recommend-roofline POST (QuoteBuilder.tsx:1450,1526 via buildQuoteInputs → form.miniLightItems passthrough, quoteForm.ts:163) returns 400 'Invalid miniLightItems element'. The quote can no longer be re-priced, edited-and-recalculated, or roofline-recommended from the builder. Same failure class as #R18-006 (railing type-widen left stale narrow consumers — that fix added 'railing' here but 'curtain' was never added); not in the dedupe ledger.
- **Fix:** Add 'curtain' to VALID_MINILIGHT_TYPES (line 38), landing TDD per the charter: a failing route test first — POST a valid-inputs body whose miniLightItems contains {type:'curtain', wrapStyle:'canopy', stringCount:1} and assert 200. Consider deriving the set from a single exported constant next to MiniLightItem in pricingEngine.ts so the route can never drift from the engine union again.
- **Verifier:** Every link of the chain checks out in the live code. The editor offers Curtain as a first-class mini surface (src/components/design/editor-core/editor.ts:2486, 3496, and grouping at 2726), and projectScene emits {type:'curtain', wrapStyle:'canopy', stringCount} mini inputs (src/lib/design/projectScene.ts:96-99, 120-126). The route validates the BODY's miniLightItems at src/app/api/quote/route.ts:185-191 against VALID_MINILIGHT_TYPES (line 38: only tree/bush/column/railing — 'curtain' missing, though the engine union at src/lib/pricing/pricingEngine.ts:96 includes it), and the design projection that would replace the arrays runs only later (route.ts:226-231). The first Calculate passes (form has no curtain minis), but the route persists the POST-projection quoteInputs — containing type:'curtain' — into the quote row (route.ts:229 reassigns, 252-267 saves quoteInputs). On reopen, QuoteBuilder.tsx:250 hydrates the form via inputsToFormData (quoteForm.ts:263 passes miniLightItems straight through), and buildQuoteInputs (quoteForm.ts:163) sends them back verbatim, so every subsequent Calculate/recommend POST 400s ('Invalid miniLightItems element') before projection can rescue it — the quote can no longer be re-priced from the builder. No filter, guard, or test covers this: the only route-adjacent curtain reference is QuoteBuilder.tsx:2959 (recommendable kinds), and the dedupe ledger's only curtain row is #R18-004 (training-label ACCEPTED, unrelated); this exact set was patched for 'railing' in the R18 type-widen fix but 'curtain' (#100) was never added. HIGH is the honest severity — a plausible staff path bricks re-pricing of a real quote, but no wrong money is computed and recovery (delete the curtain row in the form, or fix the one-line set) is possible; it is not a prod money/data-loss CRITICAL.
- **Disposition:** **fix-now → PR-D**

#### W1-003 · bug · `src/app/api/quote/route.ts:251` · effort M

**The update branch re-prices ANY existing quote — including a BOOKED (deposit-paid) order — with no status gate, no amendment trail, and no invoice re-sync; the close-time invoice then bills a total the customer never consented to.**

- **Evidence:** POST /api/quote with a valid quoteId calls updateQuote() (src/lib/quotes.ts:224-262) which blindly overwrites inputs, result, and total for any status. Nothing checks deriveStatus/deposit_paid_at. The designed amend flow (src/app/api/quotes/[id]/amend/route.ts) exists precisely to append the trail + re-sync the invoice + trigger re-consent, but it is a SEPARATE, skippable second step. createInvoiceFromJob (src/lib/invoices.ts:427-435) intentionally prices off the LIVE quote.result and its own comment admits the hole: "Pre-existing caveat: updateQuote can re-price a booked quote with no booked-state guard — that guard belongs with the amend route". The edit page (src/app/quote/[id]/page.tsx:38-39) passes only sentAt/approvedAt — not deposit_paid_at — so the builder shows a booked order as merely "Approved" with a soft warning about the portal, not billing. Concrete failure: booked $5,000 order, $2,500 deposit paid; staff opens /quote/[id] for a what-if ("what would premium takedown cost?"), hits Calculate → quote.result/total silently become $5,600 with no amendments[] entry and no re-consent; job completes → createInvoiceFromJob bills balance = 5,600 − 2,500 = $3,100 instead of the agreed $2,500. Not in the KNOWN-FINDINGS ledger (only acknowledged in a code comment).
- **Fix:** In the /api/quote update branch, load the row's status/deposit_paid_at and refuse (409) the in-place re-price when deriveStatus is booked or terminal (declined/cancelled/lost) unless the caller is explicitly in the amend flow (e.g. an ?amending flag that the amend UI sets and that requires a follow-up POST /amend), so a booked order's totals can only change through the trail+re-sync path.
- **Verifier:** Confirmed as stated. The update branch (src/app/api/quote/route.ts:243-259) routes any valid quoteId to updateQuote (src/lib/quotes.ts:237-255), which unconditionally overwrites inputs/result/total with no status or deposit_paid_at check; the edit page (src/app/quote/[id]/page.tsx:38-39) omits deposit_paid_at so QuoteBuilder (QuoteBuilder.tsx:1745-1748) shows a booked order as merely "Approved" with a portal-display warning, not a billing one. createInvoiceFromJob (src/lib/invoices.ts:427-435) intentionally bills off the live quote.result and its own comment admits the missing booked-state guard ("that guard belongs with the amend route"), while the amend route (src/app/api/quotes/[id]/amend/route.ts) is a separate, unenforced second POST — so a what-if Calculate on a booked order silently changes the close-time invoice balance with no amendments[] entry and no re-consent. Not in KNOWN-FINDINGS-2026-07.md (#80-029 was the auth fix, a different defect). HIGH not CRITICAL: the path is operator-only (requireOperator) and requires a staff mistake, and the in-place re-price of booked quotes is the designed step 1 of the amend flow — the defect is that step 2 (trail + invoice re-sync + re-consent) is skippable, not that the write is attacker-reachable.
- **Disposition:** **fix-now → PR-D** (status gate; invoice-sync interplay noted in PR-A)

#### W1-004 · bug · `src/app/api/quotes/[id]/amend/route.ts:117` · effort M

**Amend computes its delta from two different bases — previousTotal from the approval snapshot's SELECTION total but newTotal from the full quote.result.total — so on any selection-diverging booked order an amend records a phantom price increase, re-syncs the invoice UP to the full-quote total, and can text the customer the inflated balance.**

- **Evidence:** previousTotal = last amendment ?? snap.customerSelection.currentTotalUsd ?? result.total (route.ts:113), while newTotal = quote.result?.total (route.ts:117). For a customer who approved a partial selection (e.g. approved $5,000 of a $5,600 full quote — cheaper roofline tier or a deselected item), the bases differ from day one: an operator recording ANY amendment — even without touching the builder — computes delta = 5,600 − 5,000 = +$600, passes the no-change 409 gate (route.ts:144), appends a wrong trail entry ("customer owes $600 more" they never agreed to), re-syncs the linked invoice to total $5,600 (route.ts:207-259), reopening a paid invoice to awaiting_payment, and with notifyCustomer sends SMS/email quoting the inflated new_balance (route.ts:285-301). Even when staff DO edit the builder (the documented flow), the recorded delta = (edited full total) − (selection total), overstating the change by the original divergence. The route tests only model result.total == snapshot total at booking (amend/route.test.ts:82-92), so this path is untested. Same root cause as the invoice-pricing finding but a distinct failure (corrupted immutable financial trail + wrong re-consent ask).
- **Fix:** Derive newTotal from the same selection basis as previousTotal — re-price the approved selectedItemIds (plus staff edits) via priceSelection rather than taking the whole-quote result.total — or at minimum block the amend (409 with guidance) when result.total ≠ the snapshot's agreed total at read time, i.e. when the builder was never re-based to the customer's selection.
- **Verifier:** Verified as stated: amend/route.ts:112-117 computes previousTotal from the approval snapshot's SELECTION total (approve/route.ts:486-504 server-recomputes currentTotalUsd via priceSelection over only selectedItemIds + customer toggles/install discount) but newTotal from the full-quote quote.result.total, and nothing ever re-bases quote.result to the approved selection (the only result-column write in src is charge-balance's txn-id patch). Divergence is a first-class path, not an edge — derivePackages.ts:258-272 makes packages A/B strict subsets of the quote — so on any partial-selection booking the operator's very first "Record amendment" (admin/jobs/[id]/page.tsx:86-102 posts only reason+notify, no builder edit required) yields a phantom delta equal to the divergence, which sails past the no-change 409 (route.ts:144), appends a wrong entry to the immutable amendments trail, flags re-consent for a change the customer never made, re-syncs the invoice to the result.total basis (route.ts:207-259), and with notifyCustomer texts/emails the inflated new_balance (route.ts:285-301); even the documented edit-builder-first flow overstates every delta by the original divergence. The route's own header (lines 10-12) and happy-path test (route.test.ts:82-92, asserting delta 600 from exactly a 5000-selection/5600-result fixture) show the design wrongly assumed result.total equals the agreed total, while the rest of the system (valor webhook route.ts:376 GHL monetaryValue) treats the selection total as authoritative — so this is a genuine bases mismatch, not intentional design. Not in KNOWN-FINDINGS (only #83-008 paid_at, a different amend defect, FIXED). HIGH not CRITICAL: no money moves in the route itself (balance collection is separate and auto-charge is gated off), operator-only, notice opt-in, re-consent flagged before charging — but it corrupts the financial trail and sends wrong customer-facing balances on a plausible everyday path.
- **Disposition:** **fix-now → PR-A**

#### W1-005 · bug · `src/lib/portal/adapter.ts:247` · effort M

**buildPortalLineItems drops ANY line whose label contains 'Roofline' or 'Gingerbread' — a staff-typed custom line item with those words silently vanishes from the portal, tiers, gate, and the approve-route recompute**

- **Evidence:** isBilledRoofline (adapter.ts:198-200) matches /Roofline/i or /Gingerbread/i against the raw label, and line 247 filters ALL matching rows out of the portal list (`rest = all.filter((li) => !isBilledRoofline(li.label))`), intended only for the ONE engine-billed roofline row. Custom line items (#27) have freeform staff-typed labels the engine passes through verbatim (pricingEngine.ts:626-647). Scenario: a quote with a normal Santa's/Gingerbread roofline plus a custom item 'Gingerbread house display – $250' (or 'Extra roofline strip, back of house – $300') → the custom row is deleted from portal lineItems, so derivePackages tiers exclude it, minimumOrderSubtotal excludes it, AND the approve route's authoritative server recompute (approve/route.ts:405-413 uses the same buildPortalLineItems, unknown ids dropped at :408) prices the approval WITHOUT it. Staff's result.total/email includes the $250; the customer sees, approves, and pays a total that silently excludes it — undercharge vs. staff intent with no signal anywhere. The engine itself documents label-sensitivity as a known hazard for Stake Lighting (pricingEngine.ts:463-464) but custom labels are uncontrolled. Not in the dedupe ledger.
- **Fix:** Filter by identity instead of label: the billed roofline row now carries stable ids 'roofline-santas'/'roofline-gingerbread' (#104, pricingEngine.ts:428/437) — drop rows where raw.id/stableId matches those ids, keeping the label regex only as the legacy fallback for pre-#104 results (which had no custom-label ambiguity in practice, but at minimum never drop rows whose parsed kind isn't roofline/ridge with empty custom-id).
- **Verifier:** Real as stated. adapter.ts:247 drops every row whose label matches isBilledRoofline (adapter.ts:198-200, /Roofline/i or /Gingerbread/i) whenever the quote has rooflineOptions + a santas/gingerbread choice (adapter.ts:222 — the normal phase-1+ path), and custom line items are freeform staff text (QuoteBuilder.tsx:2652-2654) passed through verbatim by the engine (pricingEngine.ts:626-647). No id guard saves it: the filter never checks raw.id/stableId even though the billed roofline now carries 'roofline-santas'/'roofline-gingerbread' (pricingEngine.ts:428/437). The approve route's authoritative recompute (approve/route.ts:405-413) uses the same buildPortalLineItems, so the custom item's id fails the realIds intersection at :408 and serverSubtotal excludes its price — while the engine's result.total (staff view/email) includes it, producing a silent undercharge on the customer-approved total with no divergence warning (client and server both derive from the same filtered list). Not in KNOWN-FINDINGS-2026-07.md; no test covers a roofline-worded custom label; the adapter comment (192-197) justifies label-matching only against unparseable-kind fallback and never considers #27 custom labels. HIGH (not CRITICAL) is the honest tier: prod-reachable wrong money, but gated on staff typing one of two specific words into a custom label.
- **Disposition:** **fix-now → PR-C**

### MEDIUM (19)

#### W1-006 · bug · `src/app/api/integrations/valor/webhook/route.ts:239` · effort S

**A genuinely DISTINCT second approved deposit charge (different txn_id, same order ref) is silently swallowed as 'alreadyPaid' — the customer's double payment leaves no durable record and no staff alert.**

- **Evidence:** webhook/route.ts:239-241 and 313-314 short-circuit `alreadyPaid` whenever deposit_paid_at is set, without comparing event.txnId to the stored quote.valor_txn_id. That's correct for Valor's up-to-3x retries of the SAME transaction, but /pay reuses the same valor_order_ref across re-opened checkouts (pay/route.ts:112) while minting a NEW hosted page each time — so two live hosted pages can exist for one quote. Concrete: customer opens checkout (page A), thinks it failed, re-opens (page B), pays on B → booked; later finds tab A still open and pays there too → Valor fires an approved webhook with a different txn_id → the handler 200-acks {alreadyPaid:true}, writes nothing, alerts no one. The only trace is the transient sigOk console.log (line 177-185). The double charge is invisible everywhere in the app (deposit_amount_usd keeps the first amount; the invoice applies a single deposit) until someone manually reconciles the Valor portal. convert-to-job/route.ts:105-109 explicitly names this 'DROP the real payment' failure mode for the force path but the customer-reachable double-hosted-page path has no mitigation.
- **Fix:** In the alreadyPaid branches, compare event.txnId with the stored valor_txn_id; when they differ on an approved event, write a durable marker (e.g. duplicate_payment_txn_id / notify row) and log at error level so staff can refund, instead of a bare {alreadyPaid:true} ack.
- **Disposition:** fix-later (durable record + alert — wave-close ledger row)

#### W1-007 · consistency · `src/app/api/integrations/valor/webhook/route.ts:283` · effort S

**The deposit webhook's atomic booking claim (and /pay's gates) lack the terminal-status exclusion /approve's B2 claim has — a cancelled quote holding a live pay link would be silently resurrected to 'booked'.**

- **Evidence:** approve/route.ts:540-553 guards its claim with .not('status','in','("declined","cancelled","lost")'), and the balance branch claims .in('status',['draft','awaiting_payment']) (webhook/route.ts:563) — but the deposit claim at webhook/route.ts:283-301 is conditional ONLY on deposit_paid_at IS NULL and then writes status:'booked', and /pay (pay/route.ts:91-107) checks only deposit_paid_at + customer_approved_at + snapshot. cancelled→booked is an illegal transition (quoteStatus.ts:126, cancelled is terminal), and the data model explicitly contemplates approved-then-cancelled rows with customer_approved_at still set (serviceMetrics.test.ts:140). Mitigating reachability: today no API route writes status='cancelled' on an approved-UNBOOKED quote (jobs/[id]/cancel is post-booking, where deposit_paid_at blocks re-pay), so this needs a direct DB edit or the future pre-booking cancel the dashboard already models — the exact gap class as the FIXED #83-005 (cancelled-invoice resurrection), left open on the deposit path.
- **Fix:** Mirror B2 on the deposit path: add .not('status','in','("declined","cancelled","lost")') to the webhook's atomic claim (plus a fast-path status check that acks 200 ignored with a loud log, since real money moved), and a matching status gate in /pay so a dead quote can't mint a hosted page at all.
- **Disposition:** **fix-now → PR-B**

#### W1-008 · bug · `src/app/api/jobs/[id]/cancel/route.ts:84` · effort S _(gap pass)_

**Cancelling a deposit-paid order surfaces the refund obligation ONLY in the ephemeral API response — no durable record (no flag/credit_note on the quote, job, or invoice) and no staff notification, so a real 50% deposit refund can be silently forgotten.**

- **Evidence:** cancel/route.ts:69-93 computes refundedDeposit/refundedInvoice/refundNeeded and returns them with a note ('issue the refund manually in Valor'), but persists nothing: the quote gets only status='cancelled' (line 78), the invoice only status='cancelled', and no internal email/Telegram fires (unlike the deposit webhook which emails staff on every money event). Contrast the amend path, which durably records credit_note in both the invoice row and the approval_snapshot.amendments[] trail. Scenario: operator cancels a booked order (deposit $1,467 already charged) from the admin UI; the response note is missed or the tab closes; nothing anywhere in the system afterwards says a refund is owed — reconcileInvoice can't flag it either because most cancellations happen pre-install, when NO invoice row exists at all.
- **Fix:** On cancel with deposit_paid_at set, persist the obligation (e.g. stamp a refund_due marker/credit note into approval_snapshot or the invoice row) and/or fire the internal staff email, mirroring the deposit webhook's staff notification.
- **Disposition:** fix-later (needs refund-record design)

#### W1-009 · bug · `src/app/api/jobs/[id]/close/route.ts:48` · effort S

**Close finalizes a job to 'done' without ever creating an invoice: called on a to_schedule/scheduled/installed job (API allows it; the ladder advances all the way) the entire remaining 50% balance is silently forfeited with no invoice row, no flag, and no reconciliation trail.**

- **Evidence:** The route settles an invoice only `if (invoice && …)` (route.ts:48-59) and never calls createInvoiceFromJob; the ladder then advances any pre-terminal status to done (route.ts:62-72). The test suite encodes the mechanism ('advances from to_schedule through all legal steps to done' with getInvoiceByJob → null, close/route.test.ts:107-116). The operator UI only offers Close at requires_invoicing (pipelineActions.ts:84-89), where complete has normally created the invoice — but a direct API call (same operator perimeter), a future UI, or the requires_invoicing-with-no-invoice state left behind when complete's createInvoiceFromJob 409s (complete advances the status BEFORE creating the invoice, complete/route.ts:68-98) all reach it. Failure scenario: deposit-paid $5,000 order, job at scheduled; POST /api/jobs/[id]/close → job done, no invoice ever exists, the $2,500 balance is never tracked anywhere (listInvoicesForAdmin/reconcileInvoice can't flag an invoice that doesn't exist).
- **Fix:** In the close route, when no invoice exists and the job has a linked quote, create it first via createInvoiceFromJob (then settle), or reject close for jobs that haven't reached requires_invoicing with an invoice — keeping 'close = finalize + settle' truthful.
- **Disposition:** **fix-now → PR-E**

#### W1-010 · test-gap · `src/app/api/quote/route.ts:226` · effort S

**The design-projection branch of POST /api/quote — the #27 'design is the master item list' money path — has zero route-level test coverage; every test mocks isValidDesignId() to false.**

- **Evidence:** route.test.ts:22-25 stubs '@/lib/designs' with isValidDesignId: () => false and an untouched getDesign, so lines 226-231 (getDesign → applyProjectionToInputs replacing the per-unit inputs before calculateQuote AND before saveQuote/updateQuote persists them) are never exercised at the route level. The pure projectScene/applyProjectionToInputs lib tests cannot catch route-level ordering bugs — the curtain 400 (validation-before-projection, finding above) is exactly the class of regression this gap already let ship. A future regression here (e.g. projection silently skipped on a getDesign shape change, or stale manual inputs priced instead of the design) would mis-price and persist wrong money with all gates green.
- **Fix:** Add a route-test block with isValidDesignId → true and getDesign returning a design whose scene projects known per-unit items; assert (1) the response result prices the PROJECTED items, not the body's manual arrays, (2) saveQuote/updateQuote receives the projected inputs, and (3) a body whose arrays contain projection-only shapes (curtain) still 200s after the F1 fix.
- **Disposition:** fix-later (tests batch)

#### W1-011 · bug · `src/app/api/quotes/[id]/amend/route.ts:100` · effort S

**Amend gates only on deposit_paid_at, not on the quote's lifecycle status — a CANCELLED booked order (cancel never clears deposit_paid_at) can still be amended, appending trail entries and optionally texting the customer a new balance on a dead order.**

- **Evidence:** The only state gate is `if (!quote.deposit_paid_at) → 409 not-booked` (route.ts:100-105). POST /api/jobs/[id]/cancel sets quote.status='cancelled' but leaves deposit_paid_at intact (cancel/route.ts:69-80). Failure scenario: order booked then cancelled (refund handled manually); weeks later staff open the old builder tab /quote/[id], edit + Calculate, and POST /amend — the route records an amendment on the cancelled order, returns status 'cancelled' as if amendable, and with notifyCustomer=true sends the customer an SMS/email demanding a new balance for a cancelled job. The invoice re-sync is correctly skipped (cancelled invoice), but the quote-level trail + customer notice fire. The PipelineActionsMenu hides Amend for cancelled quotes, so this needs a stale tab/direct call — hence LOW.
- **Fix:** Reject the amend when deriveStatus(quote) is 'cancelled' (or any terminal state) with a 409, mirroring the cancelled-invoice skip already present.
- **Disposition:** **fix-now → PR-B**

#### W1-012 · bug · `src/app/api/quotes/[id]/amend/route.ts:317` · effort M _(gap pass)_

**Amend computes requiresReconsent and its header promises 'the customer re-approves the new total before any balance is charged', but NO collection surface enforces or records re-consent — once VALOR_AUTO_CHARGE_ENABLED flips on, staff can amend an order UP and immediately MIT-charge the saved card for a total the customer never saw.**

- **Evidence:** amend/route.ts:17-19 and amend.ts:224-226 encode the SPEC §9 re-consent default, and the route returns requiresReconsent — but the quote status deliberately stays 'booked' (line 177-182), nothing persists a 'consent pending/granted' state, the portal has no re-approve surface for a booked quote, and none of charge-balance/route.ts, pay-balance/route.ts, or mark-paid check the amendments trail before collecting invoice.balance (which the amend just re-synced upward at amend/route.ts:246-259). Today the live path is the pay-link (paying ≈ consent), so this is latent; the moment the auto-charge stub is wired + flag-flipped, charge-balance/route.ts:120-126 charges the un-consented amended balance with zero guard. The consent mechanism the amend design depends on simply does not exist anywhere.
- **Fix:** Either persist a reconsent_pending marker on amend (delta >= 1¢) and have charge-balance refuse to auto-charge while it is set (pay-link/mark-paid may stay allowed — customer/operator action implies consent), or explicitly document that re-consent is an operator-judgment process, removing the unenforced promise from the route header before auto-charge ships.
- **Disposition:** fix-later — note appended to VALOR-AUTOCHARGE-FOR-JASON.md (blocker on auto-charge)

#### W1-013 · test-gap · `src/app/api/quotes/[id]/approve/route.ts:419` · effort S

**The approve route's manual-discount, waive-minimum, early-install recompute, and rush/takedown branches freeze authoritative snapshot money with zero test coverage.**

- **Evidence:** approve/route.test.ts always passes inputs:null and never sets installTiming/rushSelected/takedownSelected in a money assertion. Untested branches that shape the frozen approval_snapshot total/deposit: (1) route.ts:419-426 — inputs.discount precedence (hasManual = d.amount>0; percentage passes d.amount straight through as discountRate — I verified against pricingEngine.ts:805-806 that amount is a fraction, so it's correct today, but nothing locks that unit contract); (2) route.ts:426 discountFlat for type 'flat'; (3) route.ts:454 inputs.waiveMinimum zeroing the $1,000 gate; (4) route.ts:427-428 server-derived snapshotInstallDiscountUsd for september/october; (5) route.ts:433 effectiveCharges(rushSelected, takedownSelected). Charter §6 requires money paths TDD'd; priceSelection primitives are tested in derivePackages.test.ts but the route's MAPPING from quote.inputs into them is not — a regression there (e.g. treating a percentage amount as whole-percent 15 instead of 0.15) would freeze a wildly wrong total/deposit into the snapshot the /pay charge is taken from, and no test would fail.
- **Fix:** Add approve/route.test.ts cases: quote.inputs with {discount:{type:'percentage',amount:0.2}} and {type:'flat',amount:150} asserting snapshot total/deposit; waiveMinimum:true approving a sub-$1,000 selection; installTiming:'september' asserting the server-derived snapshotInstallDiscountUsd; rushSelected/takedownSelected asserting the fee lands in the recomputed total.
- **Disposition:** fix-later (tests batch)

#### W1-014 · bug · `src/app/api/quotes/[id]/approve/route.ts:552` · effort S

**Guarded approval UPDATE's .not('status','in',...) filter excludes NULL-status rows (SQL three-valued logic), so a legacy/NULL-status quote can never be approved — the customer gets a false 409 'already approved' and the approval is silently lost.**

- **Evidence:** The write at approve/route.ts:540-553 filters `.is('customer_approved_at', null).not('status','in','("declined","cancelled","lost")')`. In Postgres, `NOT (status IN (...))` evaluates to NULL when status IS NULL, so the row is excluded and the update matches 0 rows → the route returns 409 'already-approved' (line 565-570) with NO snapshot written and NO notification. The route explicitly intends to support NULL-status rows: QuoteRow's comment ('timestamps are the fallback for legacy rows'), deriveStatus (quoteStatus.ts:90-97) handles NULL, and the route's own happy-path test (approve/route.test.ts:352-362) uses `status: null` and asserts 200 — it only passes because the mock builder ignores filters. The sibling decline route knew about this trap and handles it explicitly (`status.in.(...),status.is.null` at decline/route.ts:136). Customer-visible failure: StickyBottomBar.tsx:148-156 treats 409 as already-approved and routes to the checkout/celebration page, and DepositCheckout.tsx:49-53 treats /pay's resulting 409 (approve-first, since no snapshot exists) as already-paid and routes to /approved — so the customer sees success while nothing was recorded and staff never learn. Reachability today is narrowed (the 2026-06-27-quote-status migration backfilled NULL rows and saveQuote stamps 'draft'), which is why MEDIUM not HIGH — but any restored backup, manual insert, or future insert path without status re-opens it, and the code+test contract claims NULL is supported.
- **Fix:** Replace the .not filter with an OR that admits NULL, mirroring the decline route: `.or('status.not.in.("declined","cancelled","lost"),status.is.null')` (or normalize status to NOT NULL with a DB default + backfill and drop NULL support from the types/tests).
- **Disposition:** **fix-now → PR-B**

#### W1-015 · bug · `src/app/api/quotes/[id]/pay/route.ts:117` · effort S

**The valor_order_ref + deposit_amount_usd stamp is an unguarded read-then-write: concurrent first-time checkout opens can orphan a hosted page's order ref (customer pays but the webhook can't map the payment → charged-but-never-booked), and the stamp can overwrite the webhook's recorded ACTUAL deposit with the intended amount.**

- **Evidence:** pay/route.ts:112-120: `orderRef = quote.valor_order_ref ?? q<random>` then `.update({ valor_order_ref: orderRef, deposit_amount_usd: depositUsd }).eq('id', id)` — no `.is('valor_order_ref', null)` and no `.is('deposit_paid_at', null)`. Scenario A (orphaned ref): the same portal link is shared; two devices (or two tabs) hit the pendingPayment state and DepositCheckout auto-POSTs /pay on mount (DepositCheckout.tsx:34-47). Both requests read valor_order_ref=null, mint different refs, and the second stamp overwrites the first. The customer pays on the hosted page carrying the overwritten ref → webhook lookup `.eq('valor_order_ref', event.orderRef)` (webhook/route.ts:217) finds no row → 200-ack 'no-matching-quote' with only a console.warn (webhook/route.ts:220-227). Real money charged; quote stays unbooked; no receipt, no CRM move, no job. Scenario B (amount clobber): webhook records a partial-auth ACTUAL amount into deposit_amount_usd (webhook/route.ts:283-301, e.g. 1000 vs intended 1350); a /pay POST that passed its `deposit_paid_at` read just before the webhook landed then overwrites deposit_amount_usd back to 1350 → the later invoice applies a 1350 deposit credit that was never collected (invoices.ts: deposit_applied = quotes.deposit_amount_usd) → balance under-billed by 350. convert-to-job/route.ts:102-115 documents this exact drop-the-payment failure class for its own path but /pay itself has no guard.
- **Fix:** Make the ref deterministic like the balance path (`dep_<quoteId>` mirrors `bal_<quoteId>`), eliminating the race entirely; or guard the stamp with `.is('valor_order_ref', null).is('deposit_paid_at', null)` and on 0 rows re-read and reuse the winner's ref (409 already-paid if deposit_paid_at got set).
- **Disposition:** **fix-now → PR-E**

#### W1-016 · bug · `src/app/api/quotes/[id]/route.ts:52` · effort S

**DELETE /api/quotes/[id] has no status guard or confirmation second factor — one call on a BOOKED (paid) quote irreversibly cascades away its job, invoice, and deposit records.**

- **Evidence:** The handler validates operator + config then calls deleteQuote(id) unconditionally (lines 50-53); deleteQuote (src/lib/quotes.ts:62-74) deletes the row with no status check, and the FK CASCADE on jobs.quote_id / invoices.quote_id (documented at src/lib/quotes.ts:103-105) removes the derived job and invoice automatically. Contrast the bulk wipe in src/app/api/quotes/route.ts, which requires the explicit x-confirm-delete-all phrase (g29) because it is PII/money destructive — the per-row delete of a paid order has zero server-side friction. Concrete: operator means to delete a junk draft on the admin list and clicks Delete on the adjacent row — a booked $5,000 order with a $2,500 deposit recorded; the quote, its job, its invoice, its approval_snapshot (signature + amendment trail), and the valor txn linkage vanish permanently; the only remaining payment record is inside Valor's dashboard, and dashboards/reports silently lose booked revenue.
- **Fix:** In the DELETE handler, load the row first and refuse (409, or require a distinct explicit confirmation header) when deposit_paid_at is set or customer_approved_at is set on a non-test quote; keep the frictionless path for draft/test rows.
- **Disposition:** **fix-now → PR-B**

#### W1-017 · bug · `src/app/api/quotes/[id]/send/route.ts:175` · effort S

**?retryGhl has no current-status check, so a reconcile on a quote that has since been approved/booked yanks its GHL card back to "Bid Sent" — the exact regression the alreadySent short-circuit documents avoiding.**

- **Evidence:** isGhlRetry = quote_sent_at set && retryGhl && ghl_stage_synced_at == null (line 175) — nothing checks currentStatus. ghl_stage_synced_at only tracks the SEND-stage sync, so if the original send's card move failed (GHL hiccup → ghl_sync_error persisted, ghl_stage_synced_at stays NULL forever), the retry affordance (QuoteBuilder line 1400 posts ?retryGhl=1) remains live even after the customer approves and pays. The deposit webhook / #107 card lifecycle has by then advanced the card past Bid Sent. Concrete: send on Monday → HL 500 → sync error recorded; customer approves + pays Wednesday (card now at the deposit-paid stage, monetaryValue set); Thursday staff housekeeping clicks "retry GHL sync" → updateOpportunity(pipelineStageId: HIGHLEVEL_STAGE_QUOTE_SENT) moves a PAID job's card back to Bid Sent and stamps ghl_stage_synced_at, so ops lose the booked deal in the pipeline. The route's own idempotency comment (lines 149-152) names this yank-back as the thing to prevent.
- **Fix:** Gate the retry on the derived status: only run the ?retryGhl reconcile when currentStatus is 'sent' or 'viewed' (and consider marking ghl_stage_synced_at reconciled-by-later-stage when the deposit webhook moves the card, so the stale retry affordance disappears).
- **Disposition:** **fix-now → PR-B**

#### W1-018 · bug · `src/app/api/quotes/[id]/staff-approve/route.ts:128` · effort S

**staff-approve's atomic write guards only customer_approved_at IS NULL — not the branch/terminal statuses — so a concurrent customer decline (or request-changes) is silently overwritten to 'approved', resurrecting a terminal quote.**

- **Evidence:** The customer /approve route's B2 fix added `.not('status', 'in', '("declined","cancelled","lost")')` to its guarded write (approve/route.ts:552) precisely because the fast-path canTransition check is a TOCTOU; staff-approve's write (lines 120-129) has only `.is('customer_approved_at', null)`, despite its header claiming "Same atomic guard". Cross-actor failure scenario: customer clicks Decline on the portal while the operator clicks "Mark approved" in the pipeline menu; both fast-paths read status='viewed' and pass canTransition; decline's guarded write wins first and sets status='declined' (legal from viewed); staff-approve's UPDATE still matches (customer_approved_at is untouched by decline) and overwrites status='approved' + customer_approved_at + snapshot — declined is terminal (canTransition('declined', *) = []) yet the quote is now approvable→bookable→chargeable, and the customer's decline survives only as a stale decline_reason string. Same hole for a concurrent request-changes (changes_requested→approved is illegal).
- **Fix:** Mirror the sibling routes' guard on the write: add `.or('status.in.(draft,sent,viewed),status.is.null')` (the exact set canTransition allows to 'approved' pre-booking) alongside `.is('customer_approved_at', null)`, and treat 0 rows as a 409/alreadyMoved rather than alreadyApproved.
- **Disposition:** **fix-now → PR-B**

#### W1-019 · perf · `src/lib/designs.ts:278` · effort S

**getDesignByQuote makes 2 sequential Supabase queries for one row, and the portal loader serializes the whole design fetch behind the quote fetch — ~2 avoidable sequential DB round trips on every customer portal open.**

- **Evidence:** src/lib/designs.ts:278-288: getDesignByQuote runs `.from('designs').select('id').eq('quote_id', quoteId).maybeSingle()` and then calls getDesignWithPhoto(data.id) -> getDesign(id) which re-fetches the SAME row with `.select('*').eq('id', id).maybeSingle()` (designs.ts:230) — two dependent round trips to locate + load one row. Its hottest caller is the customer portal loader (src/lib/portal/loader.ts:69): `const design = await getDesignByQuote(id)` runs only AFTER the quotes select at loader.ts:47-55 resolves, even though it depends solely on the quote id the caller already has. Concrete scenario: a customer opens their money page /portal/[quoteId] (approve + ValorPay deposit): server waits quotes-select RTT, then designs-id-select RTT, then designs-row-select RTT, then the (already parallelized) signed-URL calls — 3 serialized Supabase round trips where 2 parallel ones suffice, adding roughly 1 full DB RTT + 1 serialized query (~60-200ms region-dependent) to TTFB on every portal view. The same double-query also runs on /quote/[id] (page.tsx:22) and /admin/quotes/[id] (page.tsx:63) on every builder/admin open.
- **Fix:** In getDesignByQuote, select the full column set directly by quote_id (one query), then do the signing — i.e. share the row-shaping of getDesignWithPhoto but skip the redundant id->row hop. Optionally, in loadPortalQuote start getDesignByQuote(id) concurrently with the quotes select (Promise.all) and discard the design result when the quote row is missing/null.
- **Disposition:** fix-later (perf batch)

#### W1-020 · docs-drift · `src/lib/integrations/highlevelPipelines.ts:51` · effort S

**HIGHLEVEL_STAGE_QUOTE_INTERESTED is a dead config knob promoted by four setup surfaces (guessAssignments hints, /settings/highlevel page, .env.local.example, ONBOARDING.md) while HIGHLEVEL_STAGE_QUOTE_APPROVED — the var the deposit-paid money path actually prefers — is absent from all of them.**

- **Evidence:** Grep of src/ shows NO production code reads process.env.HIGHLEVEL_STAGE_QUOTE_INTERESTED — the portal approve route (src/app/api/quotes/[id]/approve/route.ts) performs no CRM stage move at all (per #107 the move happens at deposit-paid in the Valor webhook, which reads HIGHLEVEL_STAGE_QUOTE_APPROVED || HIGHLEVEL_STAGE_QUOTE_SIGNED at valor/webhook/route.ts:389). Yet highlevelPipelines.ts:51 suggests the INTERESTED var, src/app/settings/highlevel/page.tsx:23 describes it as 'Stage: customer approves on the portal', .env.local.example:58 says 'stage when the customer APPROVES on the portal (approve route)', and docs/ONBOARDING.md:110 warns 'Make sure your real .env.local sets it, or customer approval will silently fail to advance the CRM stage' — all factually wrong today. Concrete scenario: Naldo (or any fresh-machine setup) follows the settings page, sets the useless INTERESTED var, never learns APPROVED exists, and expects a card move on portal approval that never comes — while the webhook silently falls back to the SIGNED stage.
- **Fix:** Remove the HIGHLEVEL_STAGE_QUOTE_INTERESTED row from STAGE_HINTS, the settings ENV_VARS list, and .env.local.example; add HIGHLEVEL_STAGE_QUOTE_APPROVED (keywords ['approved']) with a desc like 'Stage moved to when the deposit is PAID (Valor webhook); falls back to SIGNED'; correct ONBOARDING.md:93/110.
- **Disposition:** fix-later (config/docs)

#### W1-021 · perf · `src/lib/integrations/valor.ts:265` · effort S

**Valor payment fetches (createHostedPageSale, getClientToken) have no timeout/AbortController — a hung Valor gateway pins the customer's Pay click and bills serverless duration until the platform kills it**

- **Evidence:** Both fetch calls (valor.ts:143 getClientToken, valor.ts:265 createHostedPageSale) are bare fetch with no AbortSignal.timeout. createHostedPageSale is awaited inline in the customer-facing POST /api/quotes/[id]/pay (:136) and /pay-balance (:87). Concrete scenario: Valor's securelink endpoint accepts the TCP connection but stalls (their staging host runs on a nonstandard :4430 Apache front, which the code comments note has already behaved oddly) — the customer's deposit-pay click spins for the full Vercel function limit (up to 300s of billed duration per stuck request), with no error surfaced until then; the same class was already fixed for Google Maps (#80-018, PR #152) and remains open for embeddings (#80-082), but no ledger row covers valor.ts.
- **Fix:** Pass AbortSignal.timeout(~15_000) to both fetches and map the abort to a ValorError so the existing catch in /pay//pay-balance returns the friendly 502 quickly.
- **Disposition:** **fix-now → PR-E**

#### W1-022 · bug · `src/lib/invoices.ts:542` · effort S

**markInvoicePaidManually's atomic claim is only `.neq('status','paid')` — it does not exclude 'cancelled', so a mark-paid/close racing a concurrent order-cancel resurrects a just-cancelled (terminal) invoice to paid with balance 0.**

- **Evidence:** The cancelled pre-check (invoices.ts:533-535) is read-then-write; the UPDATE itself (invoices.ts:538-543) matches any row whose status ≠ 'paid' — including 'cancelled'. Failure scenario: operator A opens /admin/invoices/[id] on an awaiting_payment invoice and clicks 'Mark paid' (or Pipeline 'Collect payment' → /api/invoices/[id]/mark-paid); operator B simultaneously cancels the order via POST /api/jobs/[id]/cancel, which flips the invoice to cancelled (cancel/route.ts:56-63). A's read saw awaiting_payment, so the throw is skipped; A's UPDATE then matches the now-cancelled row → status 'paid', balance 0, paid_at stamped. A cancelled order now carries a PAID invoice: the cancel response's refund flags were already sent, invoiceStatus.ts declares 'cancelled' terminal (TRANSITIONS.cancelled = []), and downstream revenue/AR views treat the invoice as collected. The same helper is called from charge-balance and close.
- **Fix:** Tighten the claim to `.neq('status','paid').neq('status','cancelled')` (and treat 0 rows whose re-read shows 'cancelled' as the existing thrown error), mirroring the guarded-write pattern the approve route uses for terminal quote states.
- **Disposition:** **fix-now → PR-B**

#### W1-023 · bug · `src/lib/jobs.ts:348` · effort S

**setJobStatus (and setInvoiceStatus, invoices.ts:507-512) enforce the legal-transition table via read-then-write only — the UPDATE has no `.eq('status', current.status)` predicate, so concurrent transitions can write an illegal state, e.g. 'done' over a just-cancelled job.**

- **Evidence:** setJobStatus reads the row, checks canTransition in memory (jobs.ts:338-343), then updates unconditionally by id (jobs.ts:348). Failure scenario: a requires_invoicing job; operator A fires POST /api/jobs/[id]/close while operator B fires POST /api/jobs/[id]/cancel. Interleaving: A's close settles the invoice paid (markInvoicePaidManually), B's cancel lands (job → cancelled, invoice cancel attempt), then A's setJobStatus(id,'done') — its read predates B — writes status 'done' + completed_at over 'cancelled'. Net state: a cancelled order whose job reads DONE with a settled invoice; jobStatus.ts declares both states terminal, and the cancel's manual-refund note is contradicted by the record. The codebase already uses atomic claims for the money-critical writes (deposit_paid_at .is(null), markInvoicePaidManually .neq) but not for the status machines themselves.
- **Fix:** Add `.eq('status', current.status)` to the UPDATE in setJobStatus and setInvoiceStatus and treat 0 updated rows as a lost race (re-read and return/throw), making the canTransition check compare-and-swap instead of advisory.
- **Disposition:** **fix-now → PR-B**

#### W1-024 · bug · `src/lib/pricing/pricingEngine.ts:807` · effort S

**A negative manual discount inflates the engine total (stored/emailed/CRM figure) while the portal and approve recompute silently ignore it — divergent money on a plausible typo path**

- **Evidence:** computeTotalsTail uses inputs.discount.amount raw: flat → discountAmount = amount (line 807), percentage → Math.round(subtotal × amount) (line 806); only the LOWER bound is clamped (Math.max(0,…) line 827). A negative amount makes postDiscount = subtotal + |amount| → subtotalAfterDiscount, tax, total, deposit all INFLATED, and the builder hides it (breakdown renders discount only when > 0, QuoteBuilder.tsx:2828). Reachability: the builder's discount input has min="0" (QuoteBuilder.tsx:2750) but HTML min doesn't block typing '-50' → Number('-50') = -50; /api/quote validates footage/rates/arrays/overrides but never validates `discount` at all (route.ts:100-217 — no discount check). Downstream divergence: SelectionContext requires rate/flat > 0 (SelectionContext.tsx:380) and the approve route requires d.amount > 0 (approve/route.ts:420), so the customer sees and approves the UN-inflated price while the saved result.total, quote row total, staff email, and GHL monetaryValue carry the inflated one. Scenario: staff fat-fingers '-50' flat discount on a $1,000 quote → stored total $1,141.87 vs portal/approved $1,087.50; books/CRM disagree with the snapshot. Engine's stated invariant ('never negative, NaN, or Infinity no matter what the inputs') doesn't cover inflation; tests only cover over-LARGE positive discounts.
- **Fix:** Guard in computeTotalsTail: treat a non-finite or negative discount.amount as 0 (mirroring the units()/overrideAmount guards), and add discount validation ({type in ['percentage','flat'], amount finite, >= 0, percentage <= 1, flat capped}) to /api/quote alongside the other input checks.
- **Disposition:** **fix-now → PR-E**

### LOW (55) — slimmed schema, batch-dispositioned (default accept)

| id | type | file:line | summary | fix (one-line) | disposition |
|---|---|---|---|---|---|
| W1-025 | bug | `src/app/api/integrations/highlevel/attach/route.ts:116` | Attach write-back that matches zero quote rows reports linked:true and never logs the orphaned GHL card, defeating the #80-054 fix's discoverability contract | Chain `.select('id')` onto the update and treat `data.length === 0` the same as updateErr: return linked:false and emit the orphan console.error. | accept |
| W1-026 | perf | `src/app/api/integrations/valor/webhook/route.ts:336` | Every booked deposit pays the full ~6-round-trip work-order projection (getJobWorkOrder) just to build a Telegram message that notifyTelegram throws away whenever the bot is dormant (its documented default) | Gate the work-order fetch: only call getJobWorkOrder/prepJobMessage when the notifier is active — e.g. export an isTelegramNotifyActive() from telegramNotify.ts (isTelegramBotEnab… | accept |
| W1-027 | perf | `src/app/api/integrations/valor/webhook/route.ts:416` | Deposit webhook awaits 4+ independent external HTTP calls (GHL stage move, customer SMS, customer email, internal email) strictly sequentially after the atomic claim, inflating webhook response time by seconds | Run the four GHL side effects with Promise.allSettled (keep the claim -> createJobFromQuote ordering; telegram/auto-PO can join the same parallel batch), collecting the per-channe… | **pulled → PR-E** |
| W1-028 | bug | `src/app/api/integrations/valor/webhook/route.ts:538` | Balance settle never persists the ACTUAL paid amount: an overpayment against a stale (amended-down) balance vanishes with no credit_note, and an underpaid real charge is recorded nowhere durable (console.error only). | Persist the actual event.amountUsd on settle (e.g. paid_amount column) and compute credit_note = max(0, paid − balance); on the underpaid branch, write a durable reconciliation ma… | accept |
| W1-029 | refactor | `src/app/api/invoices/[id]/charge-balance/route.ts:183` | The 'close the linked job after settle' block is copy-pasted across 3 routes (+ the Valor webhook), and the legal-transition ladder to requires_invoicing is duplicated between complete and close — a job-lifecycle change… | Move two small helpers into src/lib/jobs.ts: closeJobIfInvoiced(jobId) (the best-effort requires_invoicing→done settle-close) and advanceToRequiresInvoicing(job) (lift the one in … | accept |
| W1-030 | bug | `src/app/api/invoices/[id]/mark-paid/route.ts:47` | mark-paid maps a failed DB update to 404 'Invoice not found' and any thrown error to code 'cancelled' — an operator whose offline-payment record silently failed is told the invoice doesn't exist (or was cancelled) inste… | Distinguish the three outcomes: missing invoice → 404; update error → 500 'Could not record the payment — retry'; cancelled → 409 code 'cancelled' (match on the specific error mes… | accept |
| W1-031 † | consistency | `src/app/api/jobs/[id]/cancel/route.ts:45` | Cancelling a booked order never updates the HighLevel opportunity — the CRM card stays at the deposit-paid/Approved stage with its full monetaryValue, so the sales pipeline permanently shows a live booked deal for a can… | Best-effort move the linked opportunity to a lost/abandoned stage (or at least zero its monetaryValue) after the quote-cancel write, mirroring the webhook's best-effort GHL patter… | accept |
| W1-032 | perf | `src/app/api/jobs/[id]/close/route.ts:62` | Status-helper read-before-write pattern makes POST /close ~10 and POST /complete ~12 sequential Supabase round-trips per operator click, mostly re-reading rows the caller already holds | Let the helpers accept an optional already-fetched row (e.g. setJobStatus(id, to, { current?: JobRow }) / createInvoiceFromJob(jobId, { job?: JobForInvoice })) and skip the intern… | accept |
| W1-033 | consistency | `src/app/api/jobs/[id]/complete/route.ts:94` | createInvoiceFromJob's null collapses five distinct failure causes, and the complete route hardcodes them all to 409 'no-quote' ("job has no linked quote") — a transient DB failure is misreported as a data-linkage probl… | Have createInvoiceFromJob distinguish 'no quote link' from operational failure (e.g. return a small discriminated result or throw on DB errors), and map only the genuine missing-l… | accept |
| W1-034 | perf | `src/app/api/pipeline/[quoteId]/route.ts:35` | PipelineActionsMenu status read reuses getQuoteRaw, pulling the full inputs/result/approval_snapshot jsonb (up to ~200KB drawn-signature data-URL) to compute 6 scalar fields, and serializes two independent queries. | Use a narrow status-only select for this route (id, status, quote_sent_at, customer_approved_at, deposit_paid_at, viewed_at, is_test) instead of getQuoteRaw, and run it in Promise… | accept |
| W1-035 | refactor | `src/app/api/quote/route.ts:16` | The canonical UUID regex is copy-pasted into 31 files (grep-verified) while a canonical export (isValidQuoteId in lib/portal/loader.ts) already exists and is used by only 2 of them — the exact drift pattern that produce… | Promote one canonical export (move isValidQuoteId/UUID_RE from lib/portal/loader.ts into a tiny lib/uuid.ts to avoid pulling the loader chain into every route) and mechanically re… | accept |
| W1-036 | perf | `src/app/api/quote/route.ts:247` | requireOperator() + a second getOperator() call means two sequential supabase.auth.getUser() network validations per request once AUTH_GATE_ENABLED=true — in /api/quote (every Calculate), staff-approve, and staff-declin… | Call getOperator() once at the top and gate on it locally (if (process.env.AUTH_GATE_ENABLED === 'true' && !operator) return 401), or change requireOperator to return {operator} \… | accept |
| W1-037 | test-gap | `src/app/api/quotes/[id]/amend/route.ts:113` | No route-level test exercises a SECOND amendment — the previousTotal fallback chain (last.new_total ?? snapshot currentTotalUsd ?? result.total ?? 0) is only ever tested with an empty amendments trail. | Add one route test seeding approval_snapshot.amendments with a prior entry (both initial read and fresh re-read) and assert previous_total === the prior entry's new_total, plus th… | accept |
| W1-038 † | consistency | `src/app/api/quotes/[id]/amend/route.ts:214` | The amend-DOWN settle path flips the invoice to 'paid' (deposit now covers the total) but — unlike every sibling settle path (charge-balance, mark-paid flow, balance webhook) — never closes the linked requires_invoicing… | After a settle-to-paid re-sync in the amend route, close the linked job the same way charge-balance does (or extract the shared close-after-settle helper the existing duplication … | accept |
| W1-039 | consistency | `src/app/api/quotes/[id]/approve/route.ts:341` | Public (customer-reachable) money routes interpolate raw Supabase error messages into API responses, deviating from charter §3 and from their sibling routes' clean shapes. | Match pay-balance: return the static strings ('Quote not found', 'Failed to record approval', 'Failed to start checkout') and keep the raw err in the existing console.error only. | accept |
| W1-040 | docs-drift | `src/app/api/quotes/[id]/approve/route.ts:373` | Resolves ledger UNKNOWN #80-094: verified /approve does NOT require SENT — draft→approved is deliberately legal (offline-close), so a never-sent quote is customer-approvable by UUID; ledger row can be dispositioned as a… | No code change required if the shared draft→approved allowance is confirmed intentional for the CUSTOMER route (not just staff-approve); otherwise, gate the customer route specifi… | accept |
| W1-041 | bug | `src/app/api/quotes/[id]/approve/route.ts:427` | When a staff manual discount wins, the snapshot still records a nonzero installDiscountUsd (and installTiming) for an early-install discount that was NOT applied to the price — the authoritative approval record misstate… | Set snapshotInstallDiscountUsd = 0 (and consider forcing installTiming='none') when hasManual is true, so the snapshot reflects the discount actually applied. | accept |
| W1-042 | refactor | `src/app/api/quotes/[id]/approve/route.ts:449` | The selection-pricing composition (minimum-gate tierLineItems filter + manual-vs-install discount precedence) is copy-mirrored across three files, held in sync only by comments — and this exact duplication has already b… | Extract two pure helpers into lib/portal/derivePackages.ts (or adapter.ts): minimumGateLineItems(lineItems, roofline) and discountCharges(inputs.discount, installTiming) → {rate, … | accept |
| W1-043 † | bug | `src/app/api/quotes/[id]/convert-to-job/route.ts:131` | The manual-booking deposit clamp uses the FULL quote.total, not the selection total the customer actually approved (approval_snapshot.customerSelection.currentTotalUsd), so the 'can't over-record a deposit' invariant is… | Clamp against approval_snapshot.customerSelection.currentTotalUsd when present (falling back to quote.total), matching the base /pay and the approve snapshot use. | **pulled → PR-A** (agreed-total cousin) |
| W1-044 | refactor | `src/app/api/quotes/[id]/decline/route.ts:136` | The guarded status-transition write idiom (or('status.in.(FROM…),status.is.null') + .is('deposit_paid_at', null) + zero-rows→409) is copy-pasted across three routes; it is the race guard that stops a decline/change land… | Extract one shared helper, e.g. guardedQuoteTransition(sb, id, to, extraPayload) in src/lib/quoteStatus-db.ts (or quotes.ts) that derives the FROM set from canTransition, builds t… | accept |
| W1-045 | dead-code | `src/app/api/quotes/[id]/route.ts:20` | GET /api/quotes/[id] has no consumer anywhere in the app, and its header claims it is 'public … for the customer portal' while the auth-perimeter allowlist (isPublicPath) does NOT include it — the comment and the perime… | Delete the GET handler (keep DELETE) and the loader-comment's '/api/quotes/[id]' reference (lib/portal/loader.ts:2); or, if it is deliberately kept as an external/debug surface, s… | accept |
| W1-046 | consistency | `src/app/api/quotes/[id]/route.ts:50` | DELETE /api/quotes/[id] is the only per-quote handler in the family that skips the UUID_RE → 400 check; a malformed id reaches Postgres and surfaces as an opaque 500 with a raw 22P02 message. | Add `if (!isValidQuoteId(id)) return NextResponse.json({ error: 'Invalid quote id' }, { status: 400 });` before deleteQuote — the helper is already imported for GET. | accept |
| W1-047 | docs-drift | `src/app/api/quotes/[id]/send/route.ts:24` | The /send header comment still documents the pre-#81 auth model ('Auth model: same as /approve — the quote UUID is the capability token … we don't enforce admin auth here because admin auth is not yet wired'), contradic… | Rewrite the header's auth paragraph: operator-only via requireOperator() (dormancy-aware), quote UUID is NOT sufficient; drop the 'admin auth is not yet wired' rationale. | accept |
| W1-048 | bug | `src/app/api/quotes/[id]/send/route.ts:213` | The send stamp is an unguarded read-then-write: concurrent sends double-message the customer / can create duplicate GHL cards, and the unconditional status='sent' write can overwrite a concurrent terminal 'declined'. | Make the stamp a guarded conditional write mirroring the sibling routes: add `.or('status.in.(draft,sent,viewed,changes_requested),status.is.null')` plus, for a fresh (non-resend)… | accept |
| W1-049 | consistency | `src/app/api/quotes/[id]/send/route.ts:245` | Send route pushes monetaryValue to GHL without the >0 guard that both sibling call sites (Valor webhook, attach route) explicitly apply — a $0 total blanks a live card's deal value | Apply the same guard: `monetaryValue: value > 0 ? value : undefined` before the three updateOpportunity/createOpportunity calls (or inside the monetaryValue computation at line 24… | accept |
| W1-050 | perf | `src/app/api/quotes/[id]/send/route.ts:261` | The send route runs the GHL stage move, sync-state write, SMS, and email strictly sequentially even though the stage-move chain and the two customer messages are mutually independent — the operator's Send click waits on… | Run the stage-move-then-sync-write chain and the two messaging calls concurrently: Promise.all([ghlStageChain(), doSms ? sms() : null, doEmail ? email() : null]) with the existing… | **pulled → PR-E** |
| W1-051 | consistency | `src/app/api/quotes/[id]/view/route.ts:146` | /view and /interested ignore is_test — every portal open of a test quote fires a real internal warm-lead email and pollutes view analytics, unlike every sibling route which special-cases test quotes. | Select is_test in both routes and short-circuit the internal email (and optionally the event-log insert) for test quotes, mirroring the send route's #93 handling — keep the viewed… | accept |
| W1-052 | dead-code | `src/lib/amend.ts:241` | amend.ts exports three dead symbols — repriceAmend (zero consumers anywhere, not even tests) and amendedQuoteStatus/AMEND_RECONSENT_STATUS (test-only) — and the latter two encode a status policy the shipped amend route … | Delete repriceAmend, amendedQuoteStatus, and AMEND_RECONSENT_STATUS (plus their amend.test.ts describe block and the priceSelection/SelectionCharges imports); if the re-consent-st… | accept |
| W1-053 | dead-code | `src/lib/design/projectScene.ts:74` | Projection's five per-category arrays (miniLightItems/spritzers/wreaths/garland/bows) have no production consumer — applyProjectionToInputs deliberately re-derives id-carrying inputs from `items`, leaving an id-less par… | Remove the five derived arrays from Projection (retarget the tests at `items` or at applyProjectionToInputs output), or at minimum rewrite the comment to say 'test/debug convenien… | accept |
| W1-054 | bug | `src/lib/integrations/highlevel.ts:55` | ghlFetch has no timeout/AbortController — a hung GHL API stalls the Valor deposit webhook (and send/approve) until the platform kills the function, provoking webhook replays | Add `signal: AbortSignal.timeout(10_000)` (overridable per-call) inside ghlFetch and surface the abort as a HighLevelError so existing catch blocks handle it. | accept |
| W1-055 | docs-drift | `src/lib/integrations/highlevel.ts:177` | findOrCreateOpportunityForContact's block comment says the fallback creates a card 'at "Make Quote" stage' — directly contradicting the never-Make-Quote invariant enforced four lines later and everywhere else. | Fix the stale block comment to say the fallback creates at the ENTRY stage (HIGHLEVEL_STAGE_QUOTE_CREATED, e.g. 📭Open), never Make Quote. | accept |
| W1-056 | refactor | `src/lib/integrations/highlevel.ts:201` | updateOpportunityStage is an exact functional subset of updateOpportunity — two independent builders for the same GHL PUT endpoint that guard-fixes only ever land on one of. | Make updateOpportunityStage a one-line delegate — return updateOpportunity(opportunityId, { pipelineStageId }) — or replace its one call site and delete it. | accept |
| W1-057 | docs-drift | `src/lib/integrations/highlevel.ts:247` | listPipelines' comment claims 'Not called from production code — used manually via a dev script', but the Settings -> HighLevel setup page calls it in production | Update the comment to name the real consumer: 'Called by the Settings -> HighLevel setup page (src/app/settings/highlevel/page.tsx) via parsePipelines(); also usable from dev scri… | accept |
| W1-058 | dead-code | `src/lib/integrations/highlevel.ts:407` | The { includeRaw: true } opt-in branch of toCrmContact and its CrmContactInternal return type have zero callers — the `{ ...base, raw: hl }` arm is unreachable in production and untested. | Delete the includeRaw overload/branch and the CrmContactInternal type (keeping the plain toCrmContact), or if the team wants the extension point kept, this is an accept-with-comme… | accept |
| W1-059 | consistency | `src/lib/integrations/quoteMessages.ts:22` | URL interpolation into href attributes is inconsistent within the module: the newer templates escape URLs (escapeHtml) while the eight older ones interpolate them raw — safe today because all are internally constructed,… | Escape every interpolated URL uniformly (escapeHtml on all href/link insertions), or add a single link(url, label) helper next to row()/td() so the safe pattern is the only one av… | accept |
| W1-060 | refactor | `src/lib/integrations/quoteMessages.ts:101` | The row() table-row helper is copy-pasted verbatim 6 times and td() 3 times inside the same module, and the balance derivation Math.max(0, totalUsd - depositUsd) is duplicated across two money templates — a 6-way sync b… | Hoist row() and td() to module scope next to escapeHtml/usd, and compute balance once (or accept balanceUsd as an explicit input) shared by receiptEmailHtml and internalPaidEmailH… | accept |
| W1-061 | bug | `src/lib/integrations/quoteMessages.ts:404` | Deposit receipt SMS (and amendment SMS) assert a whole-dollar-rounded amount as the exact transacted figure — 'We received your $1,468 deposit' when Valor charged $1,467.72 | Use usdExact() in receiptSmsBody and amendmentSmsBody (matching balanceLinkSmsBody and the email bodies), or add the 'about' qualifier used in approvalSmsBody. | accept |
| W1-062 | test-gap | `src/lib/integrations/quoteMessages.ts:464` | The three deposit-paid/amendment money templates — internalPaidEmailHtml (the balance staff are told to MANUALLY charge in the Valor portal), receiptEmailHtml (customer 'Balance due at install'), and amendmentEmailHtml … | Add quoteMessages.test.ts cases asserting receiptEmailHtml/internalPaidEmailHtml render the exact deposit, total, and balance (incl. the Math.max(0,...) clamp when deposit > total… | accept |
| W1-063 | dead-code | `src/lib/integrations/valor.ts:122` | The entire Passage.js GetClientToken path (~140 lines: getClientToken, extractClientToken, requireConfig, formatAmount, ValorConfig, ClientTokenInput/Result, STAGING_BASE/PROD_BASE) is dead — the flow moved to hosted-pa… | Delete the GetClientToken section (lines ~46-49, 58-96, 98-193) and update the module header to describe the hosted-page + webhook surface only; keep the historical Passage.js not… | **pulled → PR-E** |
| W1-064 | refactor | `src/lib/invoices.ts:60` | Money rounding (round2) is copy-pasted 6× across the codebase in TWO different variants — the #83 modules promise cross-file identity in comments that a shared util would make structural. | Extract one round2 (the guarded variant) into a shared pure module (e.g. src/lib/money.ts) and import it from all six sites; delete the per-file copies and the keep-in-sync commen… | **pulled → PR-E** |
| W1-065 | test-gap | `src/lib/invoices.ts:586` | setInvoiceTaxOverride's quote-missing fallback path has zero test coverage, and on that path toggling the override OFF can never restore the tax line — the invoice silently keeps tax dropped while tax_overridden flips t… | Add a failing test for the fallback path, then either refuse toggle-OFF when the source quote/result is missing AND the invoice is currently overridden (409-style error telling th… | accept |
| W1-066 | perf | `src/lib/jobs.ts:111` | listJobsForAdmin fetches the full line_items jsonb for up to 500 jobs only to compute itemCount = line_items.length, discarding the rest | Give listJobsForAdmin its own slim select (JOB_SELECT minus line_items, e.g. a JOB_LIST_SELECT const) and derive itemCount via a lighter mechanism — simplest: keep a `jsonb_array_… | accept |
| W1-067 | refactor | `src/lib/jobs.ts:118` | The quote→customer/is_test join is copy-pasted 4× across the billing data layer (listJobsForAdmin vs listInvoicesForAdmin bulk join; getJobDetail vs getInvoiceDetail single join) — ~120 duplicated lines whose #93 test-b… | Extract two helpers (e.g. in a small src/lib/billing/quoteJoin.ts or on quotes.ts): fetchQuoteCustomerSummaries(db, quoteIds) → Map and fetchQuoteCustomerIdentity(db, quoteId); us… | accept |
| W1-068 | consistency | `src/lib/jobs.ts:307` | createJobFromQuote snapshots line_items from the FULL quote.result.lineItems with no selection filter — the job's fulfillment reference (admin job page, itemCount) includes items the customer deselected and the engine's… | Filter result.lineItems by approval_snapshot.customerSelection.selectedItemIds (when present) before snapshotting, or store the snapshot's selection alongside line_items so downst… | **pulled → PR-A** (agreed-total cousin; may degrade to fix-later if the id mapping is gnarly — LOW) |
| W1-069 | consistency | `src/lib/pipeline/pipelineActions.ts:68` | The legal draft/approved→cancelled and *→lost transitions have NO write path anywhere — an approved-but-never-pays quote is permanently unclosable and 'lost' is dead vocabulary. | Add an operator quote-cancel (or mark-lost) route gated by canTransition (approved/draft/sent/viewed/changes_requested → cancelled\|lost) with the same guarded-write pattern as st… | accept |
| W1-070 | consistency | `src/lib/pipeline/pipelineActions.ts:77` | A booked quote with NO job (auto job-create failed) gets only create-job + details — the console cannot express booked → cancelled even though the transition table allows it and the deposit is already paid. | Either offer 'cancel' in the booked-no-job branch (wiring it to create-then-cancel or a quote-level cancel), or document in pipelineActions.ts that create-job-then-cancel is the i… | accept |
| W1-071 | consistency | `src/lib/portal/adapter.ts:272` | buildApproval's legacy deposit fallback hardcodes `* 0.5` with whole-dollar rounding instead of BUSINESS_RULES.depositPercentage with cent rounding, duplicating a money constant that every other consumer reads from the … | Replace with `Math.round(totalUsd * BUSINESS_RULES.depositPercentage * 100) / 100` (adapter already transitively depends on pricingEngine via its type imports, so the value import… | accept |
| W1-072 | test-gap | `src/lib/portal/adapter.ts:361` | The charges.manualDiscount mapping (percentage→rate fraction vs flat→dollars) that drives the customer's live portal price has zero test coverage in adapter.test.ts. | Add two adapter.test.ts cases: percentage discount → charges.manualDiscount { rate: 0.2, flat: 0 }, flat discount → { rate: 0, flat: 150 }, plus the no-discount default { rate: 0,… | accept |
| W1-073 | bug | `src/lib/pricing/pricingEngine.ts:402` | autoRooflineChoice's below-minimum branch keeps the FIRST candidate on an exact amount tie, contradicting the 'pick the larger' comment (known-open #80-076, still present at c04447c). | Use `>=` in the below-minimum reduce (or explicitly prefer gingerbread on ties) and add a one-line test; alternatively re-disposition the ledger row as ACCEPTED given the degenera… | accept |
| W1-074 | consistency | `src/lib/pricing/pricingEngine.ts:533` | A manual spritzer entry with quantity N still collapses into ONE line item / one portal toggle ('24" Spritzers × 3'), un-splittable by the customer (known-open #80-067, still present at c04447c). | Either split a quantity-N manual entry into N per-instance line items at projection/form-mapping time (matching the design path's granularity), or disposition the row as ACCEPTED … | accept |
| W1-075 | docs-drift | `src/lib/pricing/pricingEngine.ts:595` | Four comments still claim the standalone bow price is '$0 / TBD by Naldo' when BUSINESS_RULES.standaloneBowPrice has been $35 since #17 — misleading anyone auditing billed money. | Update the four comments to state the flat $35 rule (or just point at BUSINESS_RULES.standaloneBowPrice without naming a number): pricingEngine.ts:595-596, quoteForm.ts:57, projec… | accept |
| W1-076 | docs-drift | `src/lib/pricing/test.ts:168` | The manual dev pricing script's expected-value comments have drifted from BUSINESS_RULES again (garland/wreath prices this time), repeating the exact failure mode of FIXED #80-078 — the script is redundant with the vite… | Delete src/lib/pricing/test.ts (its value is fully superseded by pricingEngine.test.ts), or if kept as a demo script, replace hardcoded expected-value comments with values derived… | accept |
| W1-077 | perf | `src/lib/quotes.ts:110` | deleteTestQuotes / deleteAllQuotes delete linked designs one-at-a-time in a sequential N+1 loop (each design = storage.list + storage.remove + row delete), risking a Vercel timeout mid-wipe that leaves designs erased bu… | Batch the per-design work: chunked Promise.all (e.g. 5-10 concurrent deleteDesign calls), and/or collect all storage paths first and issue one storage.remove(paths) per chunk befo… | accept |
| W1-078 | dead-code | `src/lib/quoteStatus.ts:66` | The exported QuoteLifecycleTimestamps alias has zero importers — the comment 'Kept so existing imports keep resolving' no longer describes reality. | Delete the alias and its two comment lines; tsc will confirm nothing breaks. | accept |
| W1-079 | docs-drift | `src/lib/serviceType.ts:7` | The '⚠️ POST-MERGE DEDUPE' banner instructs pointing dashboard/types.ts at this module — that dedupe already happened, so the warning now sends readers hunting for a duplicate that no longer exists. | Delete the ⚠️ POST-MERGE DEDUPE paragraph (serviceType.ts:7-10); optionally leave one line noting dashboard/types.ts re-exports from here. | accept |

_† = gap-pass finding._
## Wave 2 — data layer (2026-07-04 @ `8b00ab2`)

**Method:** 11 finders (3 code groups × correctness/efficiency/quality + 2 schema-drift) → 44 raw → 36 deduped → adversarial verify (3 refuted) → completeness critic +4 → **36 findings**. Ran clean on Opus, no interruption.

| Severity | Count |
|---|---|
| HIGH | 2 |
| MEDIUM | 14 |
| LOW | 20 |

**Disposition: pending (CRIT/HIGH/MED individually; LOWs batched default-accept).**

### HIGH (2)

#### W2-001 · bug · `src/lib/designs.ts:345` · effort M

**cloneDesignToNewQuote drops the extra_photos jsonb array (and its titles), so a rebooked multi-photo design loses every extra street photo and its scene items become orphaned references.**

- **Evidence:** The insert (lines 345-349) and the follow-up row update (lines 383-396) copy scene, photo_path, satellite_*, satellite_lines, and seed_analysis — but NEVER copy src.extra_photos. Meanwhile the scene jsonb IS copied verbatim (line 347: scene: src.scene, asserted in designClone.test.ts:133). Scene items carry photoId references to extras (sceneTypes.ts:105, matchesPhoto at :330). So after a rebook clone of a design with N extra photos: the new row has extra_photos = NULL, but scene items still contain photoId=<extraUuid>. getDesignWithPhoto returns extraPhotos:[] (designs.ts:245, extras=row.extra_photos ?? []), so those items reference a photo tab that does not exist — they are invisible/unreachable in the editor yet still present in the scene that projects to pricing. The storage-copy loop (lines 360-374) DOES copy the extra-<id>.<ext> blobs to the new prefix (it copies every object, only pattern-matching photo./satellite. for path assignment), so the bytes are duplicated but nothing references them: the customer's booked-again design silently loses all secondary angles.
- **Fix:** In cloneDesignToNewQuote, carry the extras: after copying storage objects, rebuild extra_photos by remapping each src.extra_photos[i].path from the src.id prefix to the newId prefix (the extra-<uuid>.<ext> objects are already copied by the list/copy loop), and include extra_photos + photo_title in the row update. Only keep entries whose copy succeeded, mirroring the null-on-failed-copy discipline used for photo_path.
- **Verifier:** Verified in live src/lib/designs.ts: cloneDesignToNewQuote inserts only {quote_id, scene} (line 347) and the follow-up update (lines 385-395) carries photo_path, satellite_*, satellite_lines, seed_analysis — but never src.extra_photos or src.photo_title. The scene jsonb is copied verbatim (line 347), and scene items reference extras via photoId (sceneTypes.ts:105, isItemOnPhoto :329-333). The storage loop (lines 364-374) copies every object under the source prefix, including the extra-<uuid>.<ext> blobs (path format confirmed at :675), but only assigns paths for names starting photo./satellite., so the extra bytes are duplicated with nothing pointing at them. Because the new row's extra_photos is NULL, getDesignWithPhoto returns extraPhotos:[] (line 245, 262), leaving every photoId-tagged scene item referencing a nonexistent photo tab: unreachable/invisible in the editor yet still present in the projecting scene, and the base photo_title is lost too. Concrete path: rebooking (clone) a repeat customer's multi-photo design during the Aug-Oct ops trial silently drops all secondary street angles and orphans their scene items. Confirmed as a real persistence-loss bug; HIGH (not CRITICAL) since it is per-design data loss / orphaned state, not cross-customer bleed or money corruption.
- **Disposition:** _pending_

#### W2-002 · bug · `src/lib/rebook.ts:38` · effort S _(gap pass)_

**rebookLastSeason clones an approved quote WITHOUT carrying is_test, so rebooking a TEST quote produces a REAL quote that leaks test data into the dashboard, jobs, invoices and purchase-order surfaces during the trial.**

- **Evidence:** SOURCE_COLUMNS (rebook.ts:57) does not select is_test, and buildRebookInsert (rebook.ts:38) never sets it, so the insert falls to the DB default is_test=false. Nothing in the approve flow prevents a test quote from getting customer_approved_at set (grep of is_test shows it is only written once in saveQuote). is_test is the sole isolation boundary the whole downstream layer trusts via the quote link — dashboard/queries.ts:56 filters is_test=false, needsAction.ts:128 skips is_test, inventory/jobs.ts:172 and purchaseOrder.ts:70 derive is_test from the quote. Scenario: during the Aug-Oct ops trial, staff seed a test quote (is_test=true), approve it, then click Rebook Last Season. rebookLastSeason clones it into a fresh quote with is_test=false → it now counts as booked revenue on the dashboard, appears in needs-action, and once converted flows into a REAL job/invoice/supplier PO. Test data silently becomes production/billing data.
- **Fix:** Add is_test to SOURCE_COLUMNS and carry src.is_test through buildRebookInsert (RebookSource + the insert row) so a rebooked clone inherits the source quote's test flag; add a test covering rebook of an is_test=true source producing an is_test=true clone.
- **Verifier:** Verified all load-bearing claims against live code at baseline. (1) `is_test` is a real quotes column, `NOT NULL DEFAULT false` (migrations/2026-06-28-quotes-add-is-test.sql:24, db/schema.sql:50) — an insert that omits it lands as REAL (false). (2) rebook.ts SOURCE_COLUMNS (line 58) does NOT select is_test, and buildRebookInsert (lines 38-53) never sets it — the clone insert (line 97 via buildRebookInsert) therefore falls to the false default. (3) Reachability confirmed: rebookLastSeason (line 79) selects the last quote where customer_approved_at IS NOT NULL, and staff-approve/route.ts:131-141 sets customer_approved_at with NO is_test exclusion in the WHERE clause (its own comment even says "is_test quotes are safe: no external calls" — i.e. test quotes ARE approvable, just skip notifications). So an approved test quote is a valid rebook source. (4) is_test is the sole downstream isolation boundary, derived via the quote link everywhere: dashboard/queries.ts:56 (.eq('is_test', false)), needsAction.ts:128 (skips is_test), jobs.ts:137, invoices.ts:272, inventory/jobs.ts:172, purchaseOrder.ts:73 — a clone with is_test=false leaks into all of them. (5) Route is user-reachable (src/app/api/customers/[customerId]/rebook/route.ts has no is_test gate; RebookButton.tsx exists; requireOperator dormant by design per #90). Concrete scenario: during the Aug-Oct trial, staff seed a test quote (is_test=true), staff-approve it, then click Rebook Last Season → rebookLastSeason clones it into a fresh quote defaulting is_test=false → the clone now counts as a real draft on the dashboard and in needs-action, and once sent/approved/converted flows into a REAL job, invoice, and supplier PO. This is exactly the cross-contamination #93's isolation was built to prevent. No dedupe match in KNOWN-FINDINGS-2026-07.md; rebook.test.ts has no is_test coverage. Not CRITICAL: no automatic cross-customer bleed and the clone is deletable (recoverable), and the immediate effect is dashboard/reporting pollution rather than an instant billing charge — but it is wrong data on a plausible operator path, so HIGH. Fix as described: add is_test to SOURCE_COLUMNS, carry src.is_test through RebookSource + buildRebookInsert, and add a test that a rebook of an is_test=true source yields an is_test=true clone. NOTE: since is_test is immutable-by-convention (only written in saveQuote and buildRebookInsert would be a second write site), the fix's comment/invariant docs in quotes.ts:144 ("is_test is written once at insert") should be updated to reflect the second legitimate write site.
- **Disposition:** _pending_

### MEDIUM (14)

#### W2-003 · consistency · `db/schema.sql:19` · effort S

**db/schema.sql declares quotes.created_by as `text`, but migration 2026-06-28-add-created-by.sql adds it as `uuid REFERENCES auth.users(id)` — a type disagreement between the bootstrap file and the migration the code+prod actually use; the surrounding comment block is also stale/false.**

- **Evidence:** db/schema.sql line 19 `created_by text,` and line 35 `add column if not exists created_by text;`. migrations/2026-06-28-add-created-by.sql line 17-18 adds `created_by uuid REFERENCES auth.users(id) ON DELETE SET NULL`. The db/schema.sql comment (lines 11-18) asserts created_by is 'NOT written by any code yet', there is 'NO standalone migrations/*.sql applying it', and 'PROD does NOT have this column' — all three are now false: src/lib/quotes.ts saveQuote writes `created_by: createdBy` (line 185), the migration exists and adds it, and designs.ts createDesign writes designs.created_by too (line 187, backed by the same migration). A fresh bootstrap from db/schema.sql creates the column as text (no FK), diverging from prod's uuid.
- **Fix:** Change db/schema.sql to `created_by uuid references auth.users(id) on delete set null` and delete the stale comment block (lines 11-18); it now contradicts saveQuote, createDesign, and migration 2026-06-28-add-created-by.sql.
- **Disposition:** _pending_

#### W2-004 · consistency · `db/schema.sql:53` · effort S

**db/schema.sql disables RLS on every table (`disable row level security`), contradicting the #90 model that FULL-SCHEMA.sql and migration 2026-06-28-enable-rls-all-tables.sql enforce (RLS ENABLED, service-role-only) — a fresh bootstrap from db/schema.sql ships the pre-#90 security posture.**

- **Evidence:** db/schema.sql lines 53, 89, 127, 171-172, 204, 249 all `alter table ... disable row level security`. FULL-SCHEMA.sql enables RLS on every table (lines 100, 126, 174, 199, 239, 303) and its header (lines 11-12) documents 'RLS ENABLED on all — #90 defense in depth'; migrations/2026-06-28-enable-rls-all-tables.sql is the authoritative enable. The task brief confirms RLS-enabled-no-policy is the intended service-role model (#90). db/schema.sql is the only source still disabling it.
- **Fix:** Flip db/schema.sql's `disable row level security` statements to `enable`, matching FULL-SCHEMA.sql + the enable-rls migration, or annotate db/schema.sql as superseded by FULL-SCHEMA.sql for provisioning.
- **Disposition:** _pending_

#### W2-005 · consistency · `db/schema.sql:184` · effort S

**db/schema.sql still defines the photo_corrections table that migration 2026-06-25-drop-photo-corrections.sql DROPPED and FULL-SCHEMA.sql marks REMOVED — the two schema sources contradict each other and a later migration.**

- **Evidence:** db/schema.sql lines 184-206 `create table if not exists photo_corrections (...)` (+ index line 206). This table was dropped by migrations/2026-06-25-drop-photo-corrections.sql (`drop table if exists photo_corrections`) and FULL-SCHEMA.sql lines 136-141 document it as REMOVED (S13). No code reads/writes it (grep of src/ for `photo_corrections` yields no live table access — the corrections flow, route, and lib/corrections.ts were all removed). A fresh DB bootstrapped from db/schema.sql re-creates a dropped, unused table, and the two canonical schema files disagree on whether it exists.
- **Fix:** Remove the photo_corrections block from db/schema.sql (lines 184-206) to match FULL-SCHEMA.sql + the drop migration; or add a REMOVED tombstone comment like FULL-SCHEMA.sql has.
- **Disposition:** _pending_

#### W2-006 · security · `migrations/2026-06-27-customers-properties.sql:69` · effort S

**RLS protection on PII/business tables is split across two un-ordered migrations: 12 older files END with `DISABLE ROW LEVEL SECURITY` and are all documented "idempotent, safe to re-run," while protection is restored only by the separate, later 2026-06-28-enable-rls-all-tables.sql. Migrations are applied MANUALLY (paste-into-SQL-editor, no runner/ordering), so re-pasting any older file after the enable-rls one silently strips RLS and re-opens the #90 anon-leak class for that table.**

- **Evidence:** customers-properties.sql:69-70 `ALTER TABLE public.customers DISABLE ROW LEVEL SECURITY; ALTER TABLE public.properties DISABLE ...` (customers/properties hold names+emails+phones, the highest-PII rows). Same DISABLE tail in invoices.sql:100, jobs.sql:104, inventory-catalog.sql:30, inventory-on-hand.sql:16, app-settings.sql:21, custom-uploads.sql:25 (all labeled "idempotent; safe to re-run"). enable-rls-all-tables.sql:30-45 re-enables them in a separate BEGIN/COMMIT with no ordering guard. inventory-catalog.sql:9-11 explicitly documents a "re-seeded on every import" workflow that INVITES re-running that DISABLE file. Verified live (project chhntsbnbofyqrpivuog): all 20 public tables currently rls_enabled=true, and anon/authenticated have rolbypassrls=false — so prod is presently correct and this is a LATENT footgun, not a live leak. Scenario: operator re-imports the Thunder catalog by re-pasting inventory-catalog.sql (as its header instructs) OR re-runs customers-properties.sql to add a future column; RLS on that table flips back to disabled; a subsequently-leaked anon key (or an accidental NEXT_PUBLIC_ Supabase var) can then read every customer PII / invoice row on that table.
- **Fix:** Remove the `DISABLE ROW LEVEL SECURITY` line from the create-table migrations (default is disabled anyway on create; enabling belongs only in the enable-rls migration), OR append `ENABLE ROW LEVEL SECURITY` to each create-table migration so re-running one is self-securing, OR add a bold apply-order banner + fold enable-rls into a single canonical rebuild file. Also verify RLS state after any re-import.
- **Disposition:** _pending_

#### W2-007 · docs-drift · `migrations/FULL-SCHEMA.sql:20` · effort M

**FULL-SCHEMA.sql (labeled 'full canonical schema … rebuilds the ENTIRE database') is self-admittedly STALE: it omits 8 tables and the entire post-2026-06-15 quotes column set that the invoices/jobs/customers data modules depend on, so it is not a true source of truth despite its name.**

- **Evidence:** FULL-SCHEMA.sql lines 20-23 self-declare it STALE ('app_settings, custom_uploads, inventory_catalog, inventory_on_hand, customers, properties, jobs, invoices are NOT defined here'). Beyond those tables, its quotes table (lines 46-113) also lacks status/decline_reason/quote_number (2026-06-27-quote-status.sql), is_test (2026-06-28-quotes-add-is-test.sql), created_by (2026-06-28-add-created-by.sql), all valor_* / deposit_amount_usd / deposit_paid_at (2026-06-24-quotes-add-valor-payment.sql), ghl_stage_synced_at/ghl_sync_error, and approval_notify_* columns. src/lib/invoices.ts, jobs.ts, and customers.ts select/insert columns on tables FULL-SCHEMA.sql never creates (e.g. INVOICE_SELECT lines 55-58; JOB_SELECT lines 37-39; customers/properties). Running FULL-SCHEMA.sql alone does NOT reproduce the DB the code assumes.
- **Fix:** Either regenerate FULL-SCHEMA.sql to fold in every migration through 2026-07-02 (it is meant to be the single-file rebuild) or demote its header from 'full canonical schema' to 'partial — run the dated migrations after this' so nobody bootstraps from it and gets a DB the data layer errors against.
- **Disposition:** _pending_

#### W2-008 · bug · `src/app/api/designs/[id]/photo/route.ts:42` · effort S

**POST /photo uploads the storage blob then updates the row by id with no existence check — a valid-format but nonexistent design id orphans a blob and returns 200 as if it succeeded.**

- **Evidence:** The route calls uploadDesignPhoto(id, ...) directly (line 42) without first getDesign(id) — unlike seed-analysis/route.ts (line 56) and seed-roofline/route.ts (line 58) which both load the row and 404 if missing. Inside designs.ts uploadDesignPhoto() the storage upload runs first (line 622), then `sb.from('designs').update({photo_path,...}).eq('id', id)` (line 628-631). A PostgREST/Supabase UPDATE whose .eq() matches zero rows returns error:null (it is not an error to update nothing), so rowErr is falsy and the function returns {path,width,height}; the route then signs the path and responds 200 with a photoUrl. isValidDesignId only checks the UUID *shape*, not existence. Net: an operator (or replayed request) hitting a stale/wrong design UUID writes `{id}/photo.jpg` into the private designs bucket with no owning row. deleteDesign() cleans storage by listing the design's own prefix, but there is no row to ever trigger a delete, so the blob is a permanent orphan (customer photo PII at rest with no lifecycle).
- **Fix:** Load the row first (const row = await getDesign(id); if (!row) return 404) before uploadDesignPhoto, mirroring seed-analysis/seed-roofline. Same guard applies to /photos (addDesignExtraPhoto) and analysis-context's uploadDesignSatellite. Alternatively make the row UPDATE assert a matching row (.select() and check length) inside the lib helpers and roll back the uploaded object on zero-match.
- **Disposition:** _pending_

#### W2-009 · bug · `src/lib/customers.ts:71` · effort M

**match_key precedence (hl > email > phone > name) splits one real customer into two rows when their quote history mixes an HL-linked quote with an email/phone-only quote — the #306 backfill's exact input.**

- **Evidence:** customerMatchKey() returns 'hl:<id>' when highlevel_contact_id is present, else 'email:'/'phone:'/'name:'. saveQuote()'s attachQuoteToCustomer call (src/lib/quotes.ts:204-210) OMITS highlevel_contact_id, and the HL attach route (src/app/api/integrations/highlevel/attach/route.ts:112) writes highlevel_contact_id onto the quote AFTER save without re-attaching — so live rows are keyed on email/phone/name. But backfillCustomersFromQuotes (customers.ts:236) SELECTs highlevel_contact_id and passes it through attachQuoteToCustomer→findOrCreateCustomer, so backfill keys HL-bearing historic quotes on 'hl:<id>'. Concrete: Jane's quote A (email jane@x.com, no HL id) and quote B (same person, highlevel_contact_id='hl9') → backfill creates customer 'email:jane@x.com' (A) AND 'hl:hl9' (B): two customers, one person. Each holds only part of her history, so 'rebook last season' (rebook.ts filters by a single customer_id) clones from whichever fragment was chosen and misses the other property/most-recent approved quote.
- **Fix:** Dedup on ALL available identity keys, not just the highest-precedence one: when creating/looking up a customer, also match on secondary keys (email/phone) so an HL-only row and an email-only row for the same person merge. At minimum, have the HL-attach route re-run attachQuoteToCustomer after stamping highlevel_contact_id, and de-duplicate customers whose email/phone collide with an existing hl-keyed row.
- **Disposition:** _pending_

#### W2-010 · test-gap · `src/lib/customers.ts:140` · effort S

**The 23505 unique-violation race-recovery re-select in findOrCreateCustomer (and the identical branch in findOrCreateProperty) is never exercised by any test — the entire raison d'etre of these helpers (concurrent create → correct dedup, no duplicate customer/property row) is unverified.**

- **Evidence:** customers.ts:127-144 (customer) and 166-188 (property) do select→insert→re-select: on a lost insert race the insert errors and the retry re-select at line 141-142 (customer) / 179-185 (property) recovers the winner's id. But the in-memory fake in customers.test.ts:119-123 makes .single()/.insert() ALWAYS succeed (never returns an error / null data), so the `if (!ins.error && ins.data)` happy path always fires and the retry re-select + the `console.error(...); return null` tail are dead in test. The module doc (lines 15-17) and the migration UNIQUE(match_key)/UNIQUE(customer_id,address_key) indexes exist specifically for this race, yet the recovery is untested. A regression that (e.g.) drops the retry re-select, or returns null instead of the existing id on a unique-violation, would ship green — producing duplicate-customer failures or a silently-null identity under concurrent saveQuote calls (two quotes saved for the same new customer near-simultaneously, the real prod trigger via quotes.ts:204 attachQuoteToCustomer).
- **Fix:** Add a fake-Supabase mode that returns a unique-violation error (code 23505 / {error:{code:'23505'}}, data null) from insert on the second writer, seed a pre-existing matching row, and assert findOrCreateCustomer / findOrCreateProperty return the EXISTING row's id (not null, no second row). Cover both the recover-via-re-select branch and the genuine-hard-error branch (insert error + retry finds nothing → null).
- **Disposition:** _pending_

#### W2-011 · perf · `src/lib/customers.ts:251` · effort S · known: W1-077

**backfillCustomersFromQuotes processes every unlinked quote strictly sequentially, and each quote costs ~5 serial DB round-trips (find-or-create customer = 1 select + maybe 1 insert, find-or-create property = 1 select + maybe 1 insert, then 1 quote UPDATE), so the total is ~5N wholly-serial awaits with zero concurrency.**

- **Evidence:** The loop `for (const q of rows) { const res = await attachQuoteToCustomer(q); ... }` (customers.ts:251-255) awaits one quote at a time. attachQuoteToCustomer (customers.ts:196-217) itself chains findOrCreateCustomer (select at 124, insert at 127) → findOrCreateProperty (select at 158, insert at 166) → quotes UPDATE (209). Quotes are mutually independent (each links its own customer/property), so nothing forces serialization across rows. On the FIRST backfill over an unlinked history that is limit=5000 rows × ~5 round-trips = up to ~25k sequential network hops in one request — at ~20ms each that is minutes of wall-clock, exposing it to a Vercel function timeout that would leave the scan half-linked (idempotent re-run recovers, but the operator sees a 500). Today's table is small (#306 backfilled 9 quotes) so N is low, but the pattern is the same class as ACCEPTED W1-077 (quotes.ts:110 delete-designs N+1).
- **Fix:** Process the scan in bounded-concurrency chunks (e.g. `for` over slices of ~5-10 quotes, `await Promise.all(chunk.map(attachQuoteToCustomer))`) so independent quotes' round-trips overlap. The find-or-create UNIQUE-index race-recovery already makes concurrent same-key creates safe. Optionally batch the final quote-link UPDATEs. Keeps the one-shot request well under the function timeout as the unlinked backlog grows.
- **Disposition:** _pending_

#### W2-012 · test-gap · `src/lib/designs.ts:326` · effort S

**cloneDesignToNewQuote has no test covering a source design that has extra_photos — the one data-integrity-critical clone path (multi-image rebook) is entirely unexercised.**

- **Evidence:** designClone.test.ts is the only suite for the clone primitive; its SOURCE fixture (lines 103-116) covers photo_path, satellite_path, scene, satellite_lines, seed_analysis but never sets extra_photos or photo_title. Because cloneDesignToNewQuote is the sole primitive behind rebook.ts's 'rebook last season' (a data-preserving copy of what a customer agreed to), the absence of any extras case is why the drop described in the primary finding shipped undetected. A clone is a data-integrity-critical path (silent loss = wrong bill), which is exactly the test-gap class this lens targets.
- **Fix:** Add a test whose source row includes extra_photos (with a scene item referencing one via photoId) and assert the cloned row has extra_photos populated, repointed to the new prefix, and the extra-* storage object is copied. This test is what makes the primary fix verifiable failing-then-passing.
- **Disposition:** _pending_

#### W2-013 · bug · `src/lib/designs.ts:372` · effort M

**cloneDesignToNewQuote's photo/satellite path detection uses startsWith('photo.')/'satellite.', which also matches the base photo but silently ignores copied extra-*.ext objects, duplicating storage bytes with no row reference (dangling blobs).**

- **Evidence:** Lines 364-374 copy EVERY object under src.id to newId, but only assign photoPath when o.name.startsWith('photo.') and satellitePath when o.name.startsWith('satellite.'). An extra photo stored as extra-<uuid>.jpg (see addDesignExtraPhoto line 675) is copied to {newId}/extra-<uuid>.jpg yet never recorded on the new row (compounds the finding above). These orphaned copies are only reclaimable via deleteDesign's blanket prefix removal — until then they are dangling duplicated PII blobs. Note the copied extra blob keeps the OLD photoId in its filename, so even if extra_photos were later reconstructed the id/path/scene-photoId three-way mapping is fragile.
- **Fix:** Enumerate + remap extras explicitly rather than relying on the blanket copy: iterate src.extra_photos, copy each old path to a new path under newId, and record the remapped entry. Avoids both the orphan and the id-mismatch. This should be part of the same fix as the extra_photos-drop finding.
- **Disposition:** _pending_

#### W2-014 · bug · `src/lib/designs.ts:621` · effort S _(gap pass)_

**uploadDesignPhoto/uploadDesignSatellite orphan the previous base-photo/satellite blob when the replacement has a different file extension, because upsert only overwrites the identical storage key.**

- **Evidence:** uploadDesignPhoto writes to `${id}/photo.${ext}` with upsert:true (designs.ts:621-625); uploadDesignSatellite writes `${id}/satellite.${ext}` the same way (designs.ts:465-469). The only .remove() calls in the file are in deleteDesign (562) and removeDesignExtraPhoto (714) — neither upload path removes the prior object first. Scenario: a design's base photo is stored as photo.png (Street-View WEBP normalized, or a PNG upload). The operator later replaces it via POST /api/designs/[id]/photo with a JPEG → new object photo.jpg is written and the row's photo_path is updated to photo.jpg, but photo.png is a different key so it is neither overwritten nor removed. photo.png remains a dangling PII-bearing blob under the design prefix until the whole design is deleted (deleteDesign's prefix-list is the only backstop). This is distinct from the clone-path extension issue (:372) and the extras concurrency findings.
- **Fix:** Before writing the new photo/satellite object (or right after a successful row update), remove any prior object whose name starts with `photo.`/`satellite.` but differs from the new path — or read the old photo_path/satellite_path and remove it when the extension changed.
- **Disposition:** _pending_

#### W2-015 · bug · `src/lib/designs.ts:683` · effort M

**addDesignExtraPhoto is a non-atomic read-modify-write on the extra_photos jsonb array: two concurrent uploads on the same design last-writer-wins, silently losing one photo (and orphaning its storage blob).**

- **Evidence:** getExtraPhotos (line 683) reads the current array, then the update at 684-688 writes [...extras, entry]. Between the read and the write another addDesignExtraPhoto (or updateDesignExtraPhotoTitle / removeDesignExtraPhoto, all same read-modify-write on the same column) can interleave. Both compute their new array from the same stale snapshot; the second .update() overwrites the first, so one uploaded photo's entry vanishes from extra_photos while its blob stays in storage (dangling). Staff double-clicking 'add photo', or the editor firing two uploads, triggers it. There is no optimistic-concurrency guard (no updated_at/version check in the .eq('id', id)).
- **Fix:** Serialize the append server-side — either a Postgres RPC that does jsonb array_append atomically (extra_photos = extra_photos || $entry) in one statement, or a conditional update guarded by the row's updated_at read alongside extras with a retry on mismatch. Same pattern applies to updateDesignExtraPhotoTitle and removeDesignExtraPhoto.
- **Disposition:** _pending_

#### W2-016 · bug · `src/lib/designs.ts:720` · effort M

**removeDesignExtraPhoto reads the extras array once at the top but re-fetches the scene in a SECOND getDesign call after the array update; a concurrent scene autosave between the two reads means the scene-item prune operates on and rewrites a stale scene, dropping legitimate edits.**

- **Evidence:** Line 703 reads extras; line 720-723 writes the filtered extras array; then line 734 getDesign(id) re-reads the WHOLE row (including scene) and line 737 updateDesignScene overwrites scene with a pruned copy. The editor autosaves the scene independently via updateDesignScene (line 292). If an autosave lands between line 734's read and line 737's write, the prune's write clobbers it (last-writer-wins on the scene jsonb) — the just-saved billable geometry edit is lost. The prune reads a fresh scene rather than reusing a snapshot, but still does a read-modify-write with no concurrency guard.
- **Fix:** Fold the array update and the scene prune into a single row update (compute both new extra_photos and new items from one getDesign snapshot, write once), or guard the scene write with an updated_at precondition + retry. Reduces the read-modify-write window and the double-write.
- **Disposition:** _pending_

### LOW (20) — batch-dispositioned (default accept)

| id | type | file:line | summary | fix |
|---|---|---|---|---|
| W2-017 | docs-drift | `db/schema.sql:1` | Neither db/schema.sql nor FULL-SCHEMA.sql is the actual source of truth — the 45 dated migrations are; both files diverge from the migration-summed schema the code assumes, with no note pointing a re… | Add a header to both files stating the migrations/ directory is authoritative and these files are convenience bootstraps that may lag; ideally auto-g… |
| W2-018 † | docs-drift | `db/schema.sql:1` | db/schema.sql omits the entire `designs` table (and thus extra_photos, photo_title, photo_path and every design column) — a fresh-DB bootstrap from this file 500s every design read/write, the core st… | Either add the current designs table DDL (all columns incl. extra_photos jsonb, photo_title, photo_path/_w/_h, satellite_*) to db/schema.sql, or add … |
| W2-019 | consistency | `db/schema.sql:214` | db/schema.sql's training_houses table is missing five columns the data layer reads/writes (garland_detections, c9_lines, stake_lighting_footage, stake_lighting_difficulty, stake_lines) — a fresh DB b… | Add the five columns to db/schema.sql's training_houses definition (mirror FULL-SCHEMA.sql's garland_detections/c9_lines alter + the stake-lighting m… |
| W2-020 | consistency | `src/app/api/admin/customers/backfill/route.ts:23` | Under /api/admin/* this route gates with requireOperator() while the other admin-namespaced routes (admin/users) gate with the stricter requireAdmin(); the conventions charter §1 pairs the admin name… | Either switch to requireAdmin() to match the /api/admin namespace and the charter's admin pairing, or (if operator-level is intended) move the route … |
| W2-021 | perf | `src/app/api/designs/[id]/analysis-context/route.ts:66` | analysis-context route calls getDesign(id) — a select('*') pulling scene, seed_analysis and extra_photos jsonb — purely for an existence check; the fetched row is never read. | Use a lightweight existence check (select('id')...maybeSingle) instead of getDesign's select('*'), or drop the pre-check and rely on the update's row… |
| W2-022 | bug | `src/app/api/designs/[id]/analysis-context/route.ts:71` | The `analysis` jsonb is persisted verbatim with no size or shape bound — only satelliteBase64 is capped — so an arbitrarily large operator-supplied analysis object bloats the row and every downstream… | Bound the serialized analysis size (e.g. JSON.stringify(analysis).length <= a few hundred KB → 400) and/or validate it against the expected PhotoAnal… |
| W2-023 | refactor | `src/app/api/designs/[id]/photos/[photoId]/route.ts:17` | PHOTO_ID_RE reimplements the same weak /^(base|[0-9a-f-]{36})$/i UUID fragment as designs.ts isValidDesignId — a second, independently-maintained copy of the loose id pattern that can drift and share… | Tighten the UUID portion of PHOTO_ID_RE to the strict dashed-UUID pattern (share the same hoisted UUID_RE constant proposed for designs.ts, e.g. `new… |
| W2-024 | consistency | `src/app/api/uploads/route.ts:51` | POST /api/uploads accepts a file whose filename ends in .svg (route.ts:51 fallback regex) even though the lib deliberately rejects SVG — the request 500s ('Failed to upload') instead of returning a c… | Drop 'svg' from the route's filename fallback regex so it matches the lib's allowed extensions, giving a clean 400 for SVG and other unsupported type… |
| W2-025 | perf | `src/lib/appSettings.ts:271` | putAppSettings re-reads the ENTIRE app_settings table once per patched key plus a final read — a multi-key Settings save fires 4+ redundant full-table reads instead of 1. | Read current settings ONCE at the top of putAppSettings, build every merged row against that snapshot, upsert, then return the snapshot spread with t… |
| W2-026 | consistency | `src/lib/customers.ts:124` | findOrCreateCustomer/findOrCreateProperty never update the representative contact/address fields when the row already exists, so customers/properties permanently store whatever the FIRST-seen quote h… | On a find hit, optionally upsert non-null incoming fields onto the existing customer/property row (COALESCE-style: only overwrite when the existing v… |
| W2-027 | bug | `src/lib/customers.ts:138` | attachQuoteToCustomer's three writes (customer, property, quote-link) are non-atomic; a failure of the final quotes.update leaves an orphaned customer+property with no quote pointing at it. | Either move the customer+property find-or-create AFTER a successful quote link is guaranteed, or reconcile orphans by filtering the customer/property… |
| W2-028 | bug | `src/lib/customUploads.ts:106` | deleteCustomUpload treats a .single() lookup error as 'already gone' and returns success, but .single() also errors on transient DB faults / multi-row — so a real failure to find the path silently no… | Distinguish the not-found case (PGRST116 / no-rows) from other errors: use maybeSingle() and only treat data===null as already-gone; on a real error,… |
| W2-029 | consistency | `src/lib/designs.ts:135` | isValidDesignId uses a weak UUID regex /^[0-9a-f-]{36}$/i that accepts malformed ids (e.g. 36 dashes), diverging from the strict UUID_RE used elsewhere in the codebase and gating every designs route. | Replace the designs.ts UUID_RE with the strict dashed-UUID pattern already used in portal/loader.ts and dashboard/inbox/validate.ts (ideally hoist a … |
| W2-030 | bug | `src/lib/designs.ts:199` | createDesign with a seed photo does a create-row → upload-photo → seed-scene multi-step write with no rollback: if uploadDesignPhoto or the seed update throws after the row insert, the row survives b… | This is a deliberate best-effort design per the comment, but the swallow hides a real partial state. At minimum surface a seedFailed flag in the retu… |
| W2-031 | perf | `src/lib/designs.ts:275` | getDesignByQuote issues two sequential queries for one row: select('id') by quote_id, then getDesignWithPhoto re-selects the FULL row by id. | Add a getDesignRowByQuote that does `select('*').eq('quote_id', quoteId).maybeSingle()` and feed its row into a getDesignWithPhoto variant that takes… |
| W2-032 | consistency | `src/lib/designs.ts:345` | cloneDesignToNewQuote omits the created_by actor stamp that createDesign (and saveQuote) set on every other design-creation path — the rebooked clone lands with a NULL creator, a gap in the #90 audit… | Thread an optional createdBy through cloneDesignToNewQuote (and rebookLastSeason) and add `created_by: createdBy ?? null` to the clone insert, matchi… |
| W2-033 | perf | `src/lib/designs.ts:733` | removeDesignExtraPhoto reads the same design row twice — getExtraPhotos (select extra_photos) then getDesign (select *) for the scene. | In getExtraPhotos (or a small helper) select `extra_photos, scene` once at the top of removeDesignExtraPhoto, then use the already-loaded scene for t… |
| W2-034 | perf | `src/lib/quotes.ts:86` | deleteAllQuotes erases every linked design in a sequential one-at-a-time await loop (same accepted N+1 family as deleteTestQuotes/W1-077). | Chunked Promise.all (5-10 concurrent deleteDesign calls) across deleteAllQuotes/deleteTestQuotes/deleteDesignsForQuote; optionally collect all storag… |
| W2-035 | perf | `src/lib/trainingExamples.ts:243` | getCorpusBiasNote pulls the full final_scene + original_analysis jsonb for up to 200 rows and re-projects every scene via sceneToFewShotPieces on EVERY analyze call — the corpus bias note is recomput… | Cache the computed bias note (it is corpus-wide, not query-specific) — memoize with a short TTL or invalidate on training_examples write, so it is co… |
| W2-036 | perf | `src/lib/trainingExamples.ts:303` | listTrainingExamples fires a SECOND full-table query solely to derive the has_analysis boolean, doubling the round-trips per training-review list render. | Add a boolean generated/computed column (e.g. has_analysis = original_analysis IS NOT NULL) or a small SQL view/RPC so the single list query returns … |

_† = gap-pass._
## Wave 4 — customer portal (2026-07-04 @ `b3f4230`)

**Method:** 11 finders (3 code groups × correctness/efficiency/quality + 2 a11y) → 41 raw → 36 deduped → adversarial verify (4 refuted) → critic +2 → **34 findings**. Ran clean on Opus.

| Severity | Count |
|---|---|
| HIGH | 3 |
| MEDIUM | 13 |
| LOW | 18 |

**Disposition: pending (notes-only per Jason; recommended before-trial priorities in chat).**

### HIGH (3)

#### W4-001 · perf · `src/components/portal/dark/PhotoGallery.tsx:88` · effort S

**The per-tile Konva scene object is rebuilt inline on every render instead of being memoized, so every color/toggle change tears down and fully remounts every gallery Konva stage.**

- **Evidence:** Line 88 builds `const scene = { ...design.scene, items: design.scene.items.filter(...) }` inline inside `photos.map()` on every render. PhotoGallery subscribes to `useSelection()` (line 34: hiddenSceneItemIds, colorOverride), so any item toggle or color-scheme pick re-renders it. Each render produces a brand-new `scene` reference per tile, which is passed to DesignCanvas. DesignCanvas's mount effect keys on `[scene, photoUrl, photoW, photoH]` (DesignCanvas.tsx:114) and on a new `scene` identity runs the full teardown+remount path: `ctrlRef.current?.destroy()` then re-`import('./editor-core/render-readonly')` and re-render the whole stage, reloading the photo. This defeats the dedicated live-update effects (`setHidden` line 118, `setColorOverride` line 125) that exist precisely to avoid a remount. The sibling DesignReprise does this correctly — its `activeScene` is `useMemo`'d on `[design.scene, active.id]` (DesignReprise.tsx:65) so a toggle updates in place. On an N-photo job (the 'Every angle' gallery is N tiles), one tap re-imports and re-rasterizes N Konva stages at once.
- **Fix:** Memoize the per-photo scenes so their identity is stable across selection changes, mirroring DesignReprise: compute `const scenes = useMemo(() => photos.map(p => ({ ...design.scene, items: design.scene.items.filter(i => isItemOnPhoto(i, p.id)) })), [design.scene, photos])` and index it in the map, so toggles/recolors flow through DesignCanvas's in-place setHidden/setColorOverride effects instead of remounting.
- **Verifier:** Confirmed at PhotoGallery.tsx:88. The per-tile `scene` object is built inline inside `photos.map()` on every render, yielding a fresh object reference each time. PhotoGallery subscribes to `hiddenSceneItemIds` + `colorOverride` via useSelection() (line 34), so every item toggle or color-scheme pick re-renders it. DesignCanvas's mount/teardown effect keys on `[scene, photoUrl, photoW, photoH]` (DesignCanvas.tsx:114) — a new `scene` identity runs the full path: `ctrlRef.current?.destroy()` (line 110), re-`import('./editor-core/render-readonly')` (line 76), and re-render the whole Konva stage reloading the photo. This defeats the dedicated in-place live-update effects `setHidden` (line 118) and `setColorOverride` (line 125) that exist precisely to avoid a remount.

Verified the cost is real, not a micro-op: `design`/`design.scene` keep stable identity across selection changes (selection lives in SelectionContext, separate from the `design` prop), so the sibling DesignReprise's `useMemo`'d activeScene (DesignReprise.tsx:65-68, keyed on `[design.scene, active.id]`) genuinely updates in place — while PhotoGallery genuinely churns its scene identity every render. On an N-photo 'Every angle' gallery (a mobile-visible section, deliberately not skipped on phones per the header comment), one tap re-imports and re-rasterizes all N Konva stages at once, producing visible flicker/photo-reload. This is a heavy, customer-visible cost on a core portal interaction (toggling an item or picking a color scheme), not a micro-optimization.

Severity HIGH: degraded/wrong behavior on a plausible customer path (any customer who scrolls to the gallery then toggles/recolors). Not CRITICAL — no wrong money, no approval dead-end. The proposed fix (memoize per-photo scenes on `[design.scene, photos]`, mirroring DesignReprise) is correct and directly routes toggles/recolors through the in-place setHidden/setColorOverride effects.
- **Disposition:** _pending_

#### W4-002 · bug · `src/components/portal/snowglobe/DepositCheckout.tsx:95` · effort S

**The deposit-checkout "Try again" button is a dead button — after a transient failure the customer is stuck on a spinner and can never re-attempt the deposit payment without a full page reload.**

- **Evidence:** The POST to /pay (or /simulate-deposit) fires from the useEffect at line 34, whose deps are [quoteId, router, isTest] and which is one-shot-guarded by startedRef (line 35-37). retry() (line 95-100) resets startedRef.current=false, sets phase back to 'starting', and calls router.refresh(). But none of the effect's dependencies change, so the effect does NOT re-run — router.refresh() re-fetches the route's server components and preserves client state; it does not remount DepositCheckout (the parent StickyBottomBar keeps showCheckout=true, so the component instance persists). The startedRef reset is therefore never read again and no new POST is issued. The UI is set to phase 'starting', which renders the "Taking you to our secure checkout…" spinner (line 122-132) permanently.
- **Fix:** Make the retry actually re-run the request. Simplest: extract the POST into a callable (e.g. `runCheckout`) and have retry() call it directly after resetting state, instead of relying on router.refresh() to re-trigger the effect. Alternatively bump a state counter that is in the effect's dependency array (e.g. `const [attempt,setAttempt]=useState(0)`; effect deps include `attempt`; retry does `setAttempt(a=>a+1)` after resetting startedRef). Drop the ineffective router.refresh().
- **Verifier:** Confirmed the mechanism. DepositCheckout.tsx:34-93 fires the POST from a one-shot effect guarded by startedRef (lines 35-36) with deps [quoteId, router, isTest]. retry() (lines 95-100) sets startedRef.current=false, setPhase('starting'), and calls router.refresh(). In Next.js App Router, router.refresh() refetches server components but preserves the client React tree — it neither changes any effect dependency nor remounts DepositCheckout. The parent StickyBottomBar (lines 193-194, 248-249) renders DepositCheckout with no key and keeps showCheckout=true across the error/retry cycle, so the same instance persists. Therefore the effect body never re-runs after retry, the startedRef reset at line 96 is never re-read, and no new POST is issued — the UI is pinned on the 'starting' spinner (lines 122-132) with only the friendly copy and a spinner shown. No DepositCheckout component test exists to catch this (only an API-route test). Real customer scenario: a 40+ homeowner on a phone hits a transient network blip during deposit checkout, clicks the "Try again" button offered for exactly this, and lands on a spinner that never resolves. Downgraded from CRITICAL to HIGH (not a hard dead-end): the header "Cancel" button (line 114) still works from the stuck spinner and unmounts the component, so reopening via "Complete deposit" (line 207) remounts it fresh and re-runs the effect — an escape exists, though a non-technical user is unlikely to discover that the labeled recovery button is dead. Fix is sound: extract the POST into a callable that retry() invokes directly, or bump an attempt counter in the effect deps, and drop the ineffective router.refresh().
- **Disposition:** _pending_

#### W4-003 · bug · `src/components/portal/snowglobe/SignaturePad.tsx:142` · effort S _(gap pass)_

**Toggling drawn→typed→drawn leaves hasDrawn=true while the remounted canvas is blank, so a blank canvas PNG is emitted and frozen as the customer's 'drawn' e-signature on the approve/money path.**

- **Evidence:** The canvas is conditionally rendered only when `mode === 'drawn'` (line 142), so toggling to 'typed' unmounts it and toggling back to 'drawn' mounts a fresh BLANK canvas. But nothing resets `hasDrawnRef`/`hasDrawn` on a mode change — no effect keys off `mode` to clear them (clearCanvas is the only reset, line 103-110). After draw→'Type my name instead'→'Or draw your signature instead', `hasDrawn` is still true, so the emit effect (line 53-56) calls `onChange({kind:'drawn', value: canvas.toDataURL('image/png')})` on the empty canvas. That non-empty blank-PNG data-URL passes the server's parseSignature validation (approve/route.ts:161-183 only checks name+value present), freezing a blank image as approval_snapshot.signature — a degraded legal e-signature record.
- **Fix:** Reset `hasDrawnRef.current=false` and `setHasDrawn(false)` whenever `mode` changes (e.g. in the toggle onClick at line 135 or a `mode`-keyed effect), so re-entering draw mode requires an actual new stroke before a drawn signature is emitted.
- **Verifier:** Verified end-to-end. In SignaturePad.tsx the canvas is conditionally rendered only when mode==='drawn' (line 142), so a typed→drawn toggle mounts a fresh BLANK canvas. hasDrawnRef/hasDrawn are reset ONLY in clearCanvas (103-110) — no mode-keyed effect clears them. After draw→'Type my name instead'→'Or draw your signature instead', hasDrawn is still true. The emit effect (53-56) re-runs on the mode change with drawn=true; because React assigns refs during commit before useEffect fires, canvasRef.current is the freshly-mounted blank canvas, so emit (37-48) calls onChange({kind:'drawn', value: blankCanvas.toDataURL('image/png')}). That blank-PNG data-URL is a non-empty string. The parent StickyBottomBar stores this non-null signature (line 70) and enables Approve (disabled = submitting || !signature, line 421), then POSTs sig.value (line 142). Server parseSignature (approve/route.ts:168-185) only requires name/kind/value be present, so the blank drawn PNG passes validation and is frozen into approval_snapshot.signature — a degraded legal e-signature record. The fix (reset hasDrawnRef/hasDrawn on mode change) is correct. Severity assessed HIGH not MEDIUM: it corrupts the signed approval artifact on a plausible customer path (draw, switch to type, switch back to draw, approve without re-drawing) where the visible canvas is blank but the typed name still displays above so nothing signals breakage; not CRITICAL because no money is shown/charged wrong — only the stored signature image is degraded.
- **Disposition:** _pending_

### MEDIUM (13)

#### W4-004 · consistency · `src/app/portal/[quoteId]/approved/page.tsx:275` · effort S _(gap pass)_

**The approved-page footer always tells the customer 'No payment is due right now — we'll reach out to collect your deposit', contradicting the whole page when the deposit was already paid online (isPaid).**

- **Evidence:** The page branches every headline, intro line, and Step 1 on `isPaid = !!quote.approval?.depositPaidAt` (lines 71, 119, 124-138, 78-87) — a paid customer sees 'You're booked!', 'Deposit received', and 'Your deposit is in'. But the footer at lines 272-278 is rendered UNCONDITIONALLY: 'No payment is due right now — we'll reach out to collect your deposit and confirm your install date.' With online checkout ON, a customer who just completed the Valor hosted-page deposit is redirected here (DepositCheckout redirects to /portal/[id]/approved on success), lands on the booked variant, and reads a footer saying no payment is due and staff will collect a deposit they already paid — a wrong money-state message on the real post-payment path.
- **Fix:** Gate the footer copy on `isPaid`: when paid, show a 'deposit received / balance collected after install' line (mirroring Step 1); keep the 'no payment due / we'll reach out' copy only for the unpaid (checkout-off or approved-not-yet-paid) variant.
- **Disposition:** _pending_

#### W4-005 · perf · `src/app/portal/[quoteId]/page.tsx:185` · effort S

**PortalPage awaits three independent data sources strictly sequentially (quote+design chain → Google Places HTTP → app-settings DB read), serializing latency the customer feels on first paint.**

- **Evidence:** In the async server component, `resolveQuote(quoteId)` (line 109) runs the full loader chain, THEN `await fetchGoogleReviews()` (185) makes a Google Places HTTP call, THEN `await getAppSettings()` (206) makes a fresh Supabase `app_settings` read. None of the three depend on each other's results, yet each awaits the previous. On a 3G customer open the wall-clock time is the SUM: (loader: quote query + design-id query + design select('*') + N+2 signed URLs, per W1-019) + a full Google Places round-trip + a second Supabase round-trip — all before any HTML streams to the phone. getAppSettings/googleReviews are uncached (no React cache/unstable_cache; getAppSettings hits Supabase every render — appSettings.ts:216). This is the page-level orchestration layered on top of, and distinct from, the loader-internal chain W1-019 flags.
- **Fix:** Run the three independent loads concurrently: `const [quote, reviews, settings] = await Promise.all([resolveQuote(quoteId), fetchGoogleReviews(), getAppSettings()])`. (resolveQuote can throw notFound()/PortalConfigError — keep its try/notFound structure by resolving to a sentinel and calling notFound() after the Promise.all, or wrap the reviews/settings loads so a Promise.all rejection doesn't mask the quote's notFound path.) Collapses ~2 extra sequential round-trips into the critical-path max instead of the sum.
- **Disposition:** _pending_

#### W4-006 · perf · `src/components/design/DesignCanvas.tsx:49` · effort M

**Every DesignCanvas instance independently fetches /api/inventory/offered-colors on mount, so a portal with hero + reprise + N-photo gallery fires 2+N identical uncached DB-backed requests.**

- **Evidence:** DesignCanvas's mount effect (lines 47-54) does `fetch('/api/inventory/offered-colors')` with no shared cache/dedupe. The portal renders DesignCanvas in InteractiveHero (hero), DesignReprise (reprise card), and once per tile in PhotoGallery — so a customer with a 4-extra-photo design triggers 1 (hero) + 1 (reprise) + 5 (gallery) = 7 concurrent identical requests. The endpoint is not static: `GET` calls `getInventoryBindings()` (offered-colors/route.ts:15) which reads the `app_settings` row from Supabase on every call (bindings.ts, no memo). On a phone/3G these are 7 parallel round-trips returning identical bytes, and each canvas holds at as-designed until ITS OWN fetch resolves (line 59-62), so tiles light up staggered. The response is the same for the whole page — it belongs at page/loader level, fetched once.
- **Fix:** Fetch offered-colors once at the portal level (in loader.ts alongside the design, or a single top-of-page effect / context provider) and pass the resolved OfferedColorLists down as a prop to every DesignCanvas, instead of each instance fetching independently. Alternatively dedupe with a module-level in-flight promise cache.
- **Disposition:** _pending_

#### W4-007 · a11y · `src/components/portal/dark/GoogleReviews.tsx:123` · effort S · known: #80-093

**Review-carousel pagination dots are 24x24px tap targets, below the 44px minimum, on the primary touch path.**

- **Evidence:** The pagination buttons use `w-6 h-6` (24px) around a 10px inner dot (line 123). Commit 8e11fd0 already bumped these from the original 10px to 24px (partial fix of ledger #80-093), but 24px is still well under WCAG 2.5.5/2.5.8's 44px comfortable-touch minimum. For a 40+ homeowner on a phone tapping a specific review dot in a tight row (`-mx-0.5` shrinks the effective gap), mis-taps are likely. (The prev/next arrows at `w-11 h-11` = 44px are compliant and provide an alternative, which caps the severity.)
- **Fix:** Enlarge the button hit area to at least 44x44px (e.g. `w-11 h-11` or `min-w-[44px] min-h-[44px] p-3`) while keeping the 10px visual dot centered via the existing grid/place-items pattern.
- **Disposition:** _pending_

#### W4-008 · a11y · `src/components/portal/dark/Hero.tsx:73` · effort S

**The before/after hero toggle is a ~32px-tall tap target in a corner, below the 44px minimum.**

- **Evidence:** The toggle button uses `px-3.5 py-2` with 13px text and a 16px icon (line 68-77), computing to roughly 32px tall — under the 44px touch minimum. It is the sole affordance to compare the daytime 'before' against the lit 'after' render (a key trust/decision moment for a 40+ buyer) and sits in the top-right corner, the hardest thumb zone on a phone. A 40+ user on a phone will find it fiddly to hit.
- **Fix:** Increase vertical padding to reach a 44px hit height (e.g. `py-2.5`/`py-3` plus a `min-h-[44px]`), or add an invisible expanded hit area, without changing the visual pill size much.
- **Disposition:** _pending_

#### W4-009 · a11y · `src/components/portal/dark/WhatsIncluded.tsx:218` · effort S

**The #7B7361 (cream-muted) color used for small labels — "Prices shown are before tax", "Off" badge, add-on price/blurb — is ~3.5-3.7:1 at 11-13px, under AA for normal text even at full opacity.**

- **Evidence:** Line 218 ("Prices shown are before tax", 13px #7B7361 on #121B16) is ~3.7:1; the 11px "Off" badge (line 273) is ~3.5:1 on the card bg; the AddOnToggle +amount/blurb use #7B7361 at 13-16px. All fall below the 4.5:1 AA threshold for normal-size text. Scenario: the pre-tax caveat and the per-item "Off" status are exactly the small print a 40+ homeowner squints at on a phone; low contrast makes them easy to miss.
- **Fix:** Use #7B7361 only for genuinely large/bold text; for the small captions and badges here, step up to a color that clears 4.5:1 (e.g. #A89F87 already passes at ~6:1).
- **Disposition:** _pending_

#### W4-010 · a11y · `src/components/portal/dark/WhatsIncluded.tsx:237` · effort S

**Deselected line-item cards apply opacity-60 to the whole button, dropping the label/price/"Off" text below WCAG-AA contrast — hard to read on a phone.**

- **Evidence:** Line 237 gives a deselected item's <button> the class `opacity-60`, which composites BOTH the text and the card background over the section bg (#121B16). The label/price (#A89F87) then measures ~3.1:1 and the 11px "Off" badge (#7B7361) ~2.1:1 against the effective card background (computed via alpha-composite) — well under the 4.5:1 AA floor and below even the 3:1 large-text floor for the small text. Scenario: a 40+ homeowner on a phone in daylight toggles an item OFF to compare prices, then can barely read the now-dimmed line they want to toggle back ON. The label of what's excluded is the information they most need.
- **Fix:** Don't dim the actual text/price of a deselected-but-still-interactive card via a blanket opacity-60. Keep the label/price at a full-opacity accessible color (e.g. #A89F87 with no wrapper opacity) and signal deselection with the border/checkbox state only, or raise the dimmed text to a color that clears 4.5:1 after the opacity multiply.
- **Disposition:** _pending_

#### W4-011 · test-gap · `src/components/portal/SelectionContext.tsx:319` · effort M

**The mutually-exclusive roofline toggle (santas XOR gingerbread), selectPackage, the locked no-op wiring, and the approve-payload assembly in SelectionProvider have ZERO test coverage — the only colocated test (SelectionContext.test.ts) exercises the pure computeInitialSelection seed and never renders the provider.**

- **Evidence:** toggleItem (319-337) enforces the either/or roofline invariant: adding a roofline id deletes every sibling in rooflineGroup so the customer can never select both Santa's and Gingerbread. This is the single most money-sensitive selection rule (double roofline = wrong subtotal/approve total). SelectionContext.test.ts only imports computeInitialSelection; grep for SelectionProvider/toggleItem/rooflineGroup across *.test.* returns no files. A regression that broke the XOR (e.g. dropping the sibling-delete loop, or a rooflineGroup memo keyed wrong) would ship green: derivePackages.test.ts tests tier COMPOSITION, not the live toggle. The approve POST body in StickyBottomBar reads currentTotal/selectedItemIds straight from this untested state.
- **Fix:** Add a provider-level test (renderHook/@testing-library-react with SelectionProvider) covering: (1) adding roofline-gingerbread while roofline-santas is selected removes santas and keeps exactly one; (2) toggling any item flips packageId to 'D'; (3) locked=true makes toggleItem/selectPackage/toggleRush no-ops; (4) breakdown.total/currentDeposit equal priceSelection for a known selection so the approve payload is pinned.
- **Disposition:** _pending_

#### W4-012 · a11y · `src/components/portal/snowglobe/DepositCheckout.tsx:168` · effort S

**11-12px micro-copy in the #6E6553 color ("Secured by Valor", test-mode note, signature hint, char counter) sits at ~3.2:1 — fails AA for normal-size text.**

- **Evidence:** The reassurance line at line 168-172 (#6E6553 at text-[11px]) measures ~3.2:1 on the #0D1519 modal bg. The same #6E6553 recurs at 11-12px in SignaturePad ("Sign above with your finger…", line 156) and QuoteResponseModal's char counter (line 169) at ~3.2:1. Placeholder text (#6E6553 on #060B0F, ~3.4:1) is likewise sub-AA. Scenario: a 40+ customer can't comfortably read the "your card never touches our servers" trust copy — a small but real legibility loss on the reassurance that helps them commit to paying.
- **Fix:** Bump #6E6553 to at least the #7B7361→#8a8168 range (or larger, per AA large-text) so 11-12px informational copy clears 4.5:1 on the dark modal backgrounds. It's a token tweak, not a layout change.
- **Disposition:** _pending_

#### W4-013 · bug · `src/components/portal/snowglobe/StickyBottomBar.tsx:189` · effort M

**With online checkout ON, a quote that was approved-but-not-yet-paid and is then cancelled/declined by staff still shows an actionable "Complete deposit" button; clicking it hits the /pay 409 guard and the 409 handler routes the customer to the celebration/approved page for a dead order.**

- **Evidence:** The page-level dead-quote gate (page.tsx:154) is `if (!quote.approval && !isPortalActionable(quote.quoteStatus))` — it is intentionally skipped once `quote.approval` is truthy (so a booked-then-cancelled quote shows its confirmation). But for checkoutEnabled + approved + NOT paid + then cancelled: isApproved=true, isPaid=false, isBooked = checkoutEnabled?isPaid:isApproved = false. In StickyBottomBar, pendingPayment = approved(true) && !booked(true) && depositFlow(true) = true (line 189), rendering the actionable "Complete deposit" button (line 205-213). The server /pay route correctly 409s a cancelled/declined/lost quote (W1-007, pay/route.ts:114), so no wrong charge occurs — but DepositCheckout's 409 handler treats ALL 409s as already-paid and does `router.push('/portal/${quoteId}/approved')` (DepositCheckout.tsx:49-53). The approved page only requires quote.approval (truthy here), so the customer of a cancelled order lands on the "You're booked/approved!" celebration.
- **Fix:** Extend the page-level dead-quote gate to also cover the approved-unpaid case when the status is terminal: gate on `isPortalActionable(quote.quoteStatus)` for approved-but-unpaid quotes (i.e. only skip the gate when the quote is actually booked/paid, not merely approved), OR pass the derived status into StickyBottomBar and suppress the pendingPayment bar for declined/cancelled/lost. Also make DepositCheckout distinguish the not-payable 409 (code:'not-payable') from already-paid so it doesn't route a dead order to the celebration page.
- **Disposition:** _pending_

#### W4-014 · a11y · `src/components/portal/snowglobe/StickyBottomBar.tsx:294` · effort S

**The "Add $X more to reach the $1,000 minimum" nudge isn't announced (no aria-live) and isn't tied to the disabled Approve button, which is unfocusable — a screen-reader user can't discover why they can't approve.**

- **Evidence:** The nudge <p> at lines 294-300 has no role="status"/aria-live and no id referenced by an aria-describedby on the Approve button. The Approve button (line 338) is `disabled` when under the minimum, so it can't receive focus and offers no accessible reason. Scenario: a 40+ customer using a screen reader deselects items below $1,000; visually a nudge appears, but AT users hear nothing new and, finding the Approve button unfocusable/silent, hit a soft dead-end with no explanation of what to do.
- **Fix:** Give the nudge role="status" aria-live="polite" so it's announced when it appears, and add an id + aria-describedby linking it to the Approve button; consider keeping the button focusable (aria-disabled instead of the disabled attribute) so its reason is reachable.
- **Disposition:** _pending_

#### W4-015 · a11y · `src/components/portal/snowglobe/StickyBottomBar.tsx:362` · effort M

**The Approve/sign, deposit, and response modals never move keyboard/AT focus into the dialog on open, don't trap focus, and don't restore it on close — a barrier on the money (approve) path.**

- **Evidence:** SignModal (line 362), DepositCheckout, QuoteResponseModal, and the Gallery lightbox each render `role="dialog" aria-modal="true"` but none call autofocus / move focus into the dialog, none trap Tab within it, and none return focus to the trigger on close. Scenario: a customer using VoiceOver/TalkBack or a keyboard taps Approve; the sign-and-pay dialog opens but the reading cursor stays on the sticky bar behind it, so they can't find the signature field or the Confirm button without hunting. On the highest-stakes flow (approving a quote and paying a deposit) this can strand an AT user mid-approval.
- **Fix:** On open, focus the first interactive element (or the dialog heading with tabIndex=-1); trap Tab inside the dialog while aria-modal is true; on close, restore focus to the element that opened it. A small shared modal-focus hook covers all four dialogs.
- **Disposition:** _pending_

#### W4-016 · perf · `src/lib/portal/loader.ts:68` · effort M

**The linked-design lookup runs strictly AFTER the quote row fetch even though it only needs the URL quote id, and getDesignByQuote itself issues two serial queries (id-by-quote_id, then full-row-by-id) — three sequential DB round-trips where one-and-a-parallel would do.**

- **Evidence:** loadPortalQuote awaits the quotes row fetch (line 47-55), then INSIDE `if (portal)` awaits getDesignByQuote(id) (line 68). getDesignByQuote only takes the quote id (the URL param, already known before the quote fetch), so the design lookup could start in parallel with the quote fetch instead of after it. Worse, getDesignByQuote (designs.ts:275) does `designs.select('id').eq('quote_id', id)` and THEN calls getDesignWithPhoto(id) → getDesign(id) which re-SELECTs the SAME row by its id — a redundant round-trip purely to translate quote_id→id. Net: for every design-linked portal open (the common case) the customer waits on quote-fetch → designs-id-fetch → design-row-fetch in series before the page renders. On 3G each hop's RTT (100–300ms) stacks.
- **Fix:** Two independent wins: (1) start the design lookup in parallel with the quote fetch since it only needs `id` — `const [quoteRow, design] = await Promise.all([...quote select..., getDesignByQuote(id)])`, then adapt/attach after both settle. (2) In getDesignByQuote, select the full design row filtered on `quote_id` in one query (or have it return the row so getDesignWithPhoto skips the second getDesign round-trip), eliminating the redundant id→row hop.
- **Disposition:** _pending_

### LOW (18) — batch-dispositioned (default accept)

| id | type | file:line | summary | fix |
|---|---|---|---|---|
| W4-017 | bug | `src/app/api/quotes/[id]/approve/route.ts:426` | The frozen approval snapshot can record a non-zero installDiscountUsd that was never actually applied to the total, when a staff manual discount is present but the client still sends an ear… | Guard the early-install portion the same way the rate is guarded: `snapshotInstallDiscountUsd = (!hasManual && installTiming !== 'none') ? … |
| W4-018 | consistency | `src/app/portal/[quoteId]/pay-balance/page.tsx:28` | Three customer error paths inline their own friendly copy instead of the shared friendlyPortalError() helper — only DepositCheckout uses it, so error wording/phone can drift between the flo… | Route these three failure paths through friendlyPortalError('start your payment' / 'record your approval' / 'send that') so all customer er… |
| W4-019 | refactor | `src/components/portal/dark/DesignReprise.tsx:56` | The '[base, ...extraPhotos] + per-photo scene filter' construction is duplicated verbatim across InteractiveHero, DesignReprise, and PhotoGallery (drift risk — the numbering bug above is th… | Add a pure `portalPhotos(design): { id: string|null; url: string|null; w: number|null; h: number|null; title: string }[]` helper in src/lib… |
| W4-020 | dead-code | `src/components/portal/dark/Hero.tsx:20` | dark/Hero (the before/after crossfade hero) is never imported — the live portal renders snowglobe/InteractiveHero exclusively. | Delete src/components/portal/dark/Hero.tsx. It is a superseded v2-dark hero kept alongside the shipping snowglobe hero; leaving it invites … |
| W4-021 | a11y | `src/components/portal/dark/LightColorPicker.tsx:184` | Custom-pattern chips (28px) and add-color swatches (32px) fall below the 44px comfortable tap target for 40+ fingers on a phone. | Wrap the small chips/swatches in a 44px min touch area (padding or an invisible hit box) while keeping the visible dot small, matching the … |
| W4-022 | consistency | `src/components/portal/dark/PhotoGallery.tsx:43` | Portal photo fallback numbering filters url-less extras BEFORE numbering, so 'Photo N' can disagree with the canonical staff-facing numbering in photoLabels.ts. | Number the fallback label over the raw extras index (compute the label before filtering, e.g. map raw extras to {..., title: p.title || `Ph… |
| W4-023 | refactor | `src/components/portal/dark/PhotoGallery.tsx:50` | The IntersectionObserver lazy-mount effect is copy-pasted near-identically between PhotoGallery and DesignReprise. | Extract a `useLazyMountOnVisible(): [ref, mounted]` hook (or `useInViewportOnce`) in src/components/portal and reuse it in both cards. Remo… |
| W4-024 | perf | `src/components/portal/dark/PhotoGallery.tsx:97` | The whole 'Every angle' gallery mounts all N Konva stages simultaneously the instant one shared IntersectionObserver fires, spiking main-thread work on a phone. | Give each tile its own IntersectionObserver/lazy-mount (or mount them sequentially, e.g. reveal the next tile after the previous canvas rep… |
| W4-025 | dead-code | `src/components/portal/dark/ReferralCard.tsx:22` | ReferralCard is a fully-built portal component that is exported but never mounted anywhere — dead code carrying a live '$150 off next season' referral promise that no customer can see. | Either mount ReferralCard on the approved page (if the referral band is intended) or delete the component. Confirm the '$150 off' terms wit… |
| W4-026 | consistency | `src/components/portal/dark/SatelliteRoofView.tsx:67` | Satellite <img> has no onError fallback, unlike the hero's images — a broken/expired satellite URL shows the browser broken-image icon inside 'Where the lights go'. | Add an onError handler that hides the section (or shows the same neutral poster the hero uses) when satelliteUrl fails, so the roofline ove… |
| W4-027 | a11y | `src/components/portal/dark/TrustBar.tsx:16` | Dimmed duplicate press wordmarks render at ~3.28:1 contrast, below the 4.5:1 AA threshold for normal text. | Raise the dimmed variant to meet 4.5:1 (e.g. use full `#A89F87` at 7.1:1, or `text-[#A89F87]/80`), or drop the visual duplicates entirely a… |
| W4-028 | dead-code | `src/components/portal/format.ts:27` | depositFor(total) is an unused export AND a divergent second deposit formula — a landmine that would silently reintroduce a parallel 50%-deposit calc if a future dev reaches for it instead … | Delete depositFor from format.ts; it has no callers. If a deposit helper is ever wanted, have it delegate to the single source (BUSINESS_RU… |
| W4-029 | consistency | `src/components/portal/snowglobe/InteractiveHero.t…:278` | Customer-facing photo numbers are computed from the URL-FILTERED extras array, so a broken signed URL on an earlier extra shifts every later photo's number down by one vs the canonical (unf… | Number extras by their index in the UNFILTERED `design.extraPhotos` array (matching photoLabels.extraPhotoLabels) before filtering out url-… |
| W4-030 | perf | `src/components/portal/snowglobe/InteractiveHero.t…:294` | Hero thumbnail strip renders full-resolution extra-photo source images into 76x50 chips with no width constraint, downloading each extra photo at full size just for a thumbnail. | Serve a downsized image for the strip — use next/image with a small `sizes`/`width`, or generate/store a thumbnail path for extras — so the… |
| W4-031 | test-gap | `src/components/portal/snowglobe/StickyBottomBar.t…:121` | The approve POST payload assembly and the meetsMinimum gate in StickyBottomBar have no test — the client field-mapping into /approve is unverified even though the underlying gate math is co… | Add a focused unit/component test asserting (a) Approve is disabled and the POST is not fired when meetsMinimum is false, and (b) a valid s… |
| W4-032 | consistency | `src/components/portal/snowglobe/StickyBottomBar.t…:298` | Customer-facing minimum-gate nudge hardcodes the literal '$1,000' while the parallel nudge in WhatsIncluded.tsx derives it from minimumOrderSubtotal via formatUsd — a drift risk if the seas… | Destructure minimumOrderSubtotal from useSelection() and render `reach the ${formatUsd(minimumOrderSubtotal)} minimum` so the sticky bar an… |
| W4-033 | perf | `src/lib/designs.ts:230` | The portal design load does `select('*')`, pulling the full raw AI-analysis jsonb (seed_analysis) that the portal never uses — wasted DB→server payload on every portal open. | Narrow the portal-facing read to the columns getDesignWithPhoto actually returns: `select('id, quote_id, scene, photo_path, photo_w, photo_… |
| W4-034 | bug | `src/lib/portal/derivePackages.ts:287` | Package C's "Save $X vs à la carte" savings compares a PRE-TAX à-la-carte subtotal against the TAX-INCLUSIVE bundle total — mismatched bases that would show wrong/negative savings if a bund… | Make aLaCarteTotal and the compared total the same basis: either compute aLaCarteTotal as the tax-inclusive price of the items priced indiv… |

_† = gap-pass._
