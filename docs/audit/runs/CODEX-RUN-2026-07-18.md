# CODEX RUN 2026-07-18

## Verifier

Production login reachability: PASS. A read-only request to `https://quote.yulelovelights.com/login` returned HTTP 200.

Team member flow: PASS after credential remediation.

Steps tried:

1. The first preflight found all three named secrets missing, so no browser was launched.
2. After the secrets were supplied through macOS Keychain, checked only their presence and email shape. No secret value was printed or written to the repository.
3. Installed Playwright Chromium 149.0.7827.55 because the local browser binary was absent.
4. Ran the production verifier twice (the configured initial attempt and one retry).
5. Both attempts filled the login form, but browser-native validation rejected `TEST_LOGIN_EMAIL` as an incomplete email and the page stayed on `/login` until the three-minute test timeout.
6. Updated the verifier preflight to reject malformed login or customer emails immediately without printing their values. The new preflight confirmed that both `TEST_LOGIN_EMAIL` and `TEST_CUSTOMER_EMAIL` currently have invalid email shapes.
7. After both email entries were corrected, validated their shapes without printing their values and ran one production attempt with automatic retries disabled.
8. Production returned the visible error `Incorrect email or password`; the page stayed on `/login` and no quote was created.
9. A length-only check showed that `TEST_LOGIN_PASSWORD` contains one character. Added a minimum-length preflight and set the production suite to zero retries so malformed credentials fail immediately and partial flows cannot create duplicate quotes.
10. After the complete password was stored, the credential preflight passed and one zero-retry production run completed the team flow in 20.9 seconds.
11. Created and sent test quote `e3eefe4e-3403-4512-9551-e947d8482943` with title `TEST-CODEX-2026-07-18`.
12. The first anonymous portal attempt exposed a zero-line-item send defect: the customer saw `Your quote is being finalized` instead of the approval experience.
13. Reopened the same test quote through the operator UI, added one $1,200 test-only manual lighting line, recalculated it, and confirmed the public portal now had one line item.
14. Reused the same quote for the anonymous customer flow. Selected an alternate package, entered the typed signature, confirmed approval, simulated the deposit, and reached the booked confirmation.

Customer flow: PASS after repairing the same test artifact.

Final production state: the test quote is `booked`, remains `is_test=true`, has one line item, contains an approval snapshot, and has a simulated paid-deposit marker. Test mode suppressed real customer messaging and payment processing.

## New confirmed findings

### 1. Booked amendments have no customer re-consent path

Severity: High

Files: `src/app/api/quotes/[id]/amend/route.ts:231`, `src/app/api/invoices/[id]/charge-balance/route.ts:30`, `src/lib/quoteStatus.ts:124`

Failure scenario:

- Start with a booked quote totaling $2,000.00 and a paid $1,000.00 deposit.
- Re-price it to $2,400.00 and record the amendment.
- The invoice balance becomes $1,400.00.
- Settlement returns `reconsent-required`.
- The quote remains booked. The portal is read-only. The approval state machine does not allow booked to approved.

Wrong output: The customer cannot review or sign the $400.00 increase. Staff can proceed only with an operator override.

Suggested fix: Add a pending-amendment consent record and portal comparison screen. Capture a new signature for the changed total. Let settlement consume that consent marker without changing the paid booking state.

### 2. The public approval endpoint accepts no signature

Severity: Medium

File: `src/app/api/quotes/[id]/approve/route.ts:180`

Failure scenario:

- Use a sent quote with a valid package and item selection.
- POST the normal approval body to `/api/quotes/{id}/approve`.
- Omit the `signature` field.

Wrong output: The route returns success, writes `approval_snapshot.signature = null`, and advances the quote to approved.

Suggested fix: Require a valid signature on the public customer route. Keep any legacy or staff approval exception behind a separate authenticated path.

### 3. Customer delivery failures still show as sent

Severity: Medium

Files: `src/app/api/quotes/[id]/send/route.ts:256`, `src/app/api/quotes/[id]/send/route.ts:491`, `src/components/quote/QuoteBuilder.tsx:2148`

Failure scenario:

- Send a real quote through both SMS and email.
- Make both HighLevel message calls fail.
- Let the pipeline update succeed or fail independently.

Wrong output: The quote is stamped sent. The API returns HTTP 200 with `ok: true`. The builder shows a sent state and ignores `smsSent`, `emailSent`, and `messageError`. The customer receives no link.

Suggested fix: Track local send state separately from delivery state. When every requested channel fails, return a delivery failure and show channel-specific retry controls.

### 4. Half-cent tax boundaries can round down

Severity: Low

Files: `src/lib/pricing/pricingEngine.ts:900`, `src/lib/portal/derivePackages.ts:148`

Failure scenario:

- Use a taxable subtotal of $1,002.80.
- Use the current 8.75% tax rate.
- Use no discount or fee.

Wrong output: The exact tax is $87.745. Binary floating-point represents it just below the half-cent boundary. The code returns $87.74 instead of $87.75. It returns a $1,090.54 total and a $545.27 deposit instead of $1,090.55 and $545.28.

Suggested fix: Calculate in integer cents with an explicit half-up rule, or use one decimal-money helper for the pricing engine and portal.

### 5. Quote-builder customer labels are not associated with inputs

Severity: Low

File: `src/components/quote/QuoteBuilder.tsx:2688`

Failure scenario:

- Open `/quote/new?test=1`.
- Inspect the Name, Phone, Email, and Property Address controls with a screen reader.
- Click a visual label.

Wrong output: The inputs have no accessible names from their labels, and clicking a label does not focus its input.

Suggested fix: Add matching `id` and `htmlFor` values, or wrap each input inside its label.

### 6. A zero-line-item quote can be marked sent

Severity: Medium

Files: `src/app/api/quotes/[id]/send/route.ts:149`, `src/app/api/quotes/[id]/send/route.ts:262`, `src/app/portal/[quoteId]/page.tsx:140`, `src/components/quote/QuoteBuilder.tsx:2086`

Failure scenario:

- Analyze a property for which the automated design produces no lighting items.
- Calculate the quote with no manual line items. The saved result has zero line items and a $0 total.
- Click `Send Quote to Customer`.
- Open the returned portal link as the customer.

Wrong output: The send route returns success and stamps the quote sent. The customer receives a link that shows only `Your quote is being finalized`; there is no package, price, or approval action. Production test quote `e3eefe4e-3403-4512-9551-e947d8482943` reproduced this path.

Suggested fix: Reject send when the current portal projection has no effective line items or no positive total. Disable the builder's send button with a clear instruction to add or draw at least one priced item. Keep the portal's finalized guard as defense in depth.

## Regressions

None confirmed.

## Refuted candidates

- CODX-R-001: Partial-selection tax scaling does not underbill while every item uses the same flat tax rate.
- CODX-R-002: The quote PDF is intentionally the original signed snapshot. The missing current amendment experience is part of CODX-F-003.
- CODX-R-003: The pre-discount order minimum is an explicit business rule.
- CODX-R-004: The Valor webhook no longer drops the live `data.invoice_no` order reference. The current parser explicitly accepts `invoice_no`; the older no-order-ref diagnostic comment in the route is stale.

## New suggestions

### Customer amendment comparison and re-sign

Type: Clear defect

Problem solved: Customers need to see the old scope, new scope, price delta, deposit already paid, and new balance before signing an amendment.

Rough size: Large

### Channel-specific delivery receipt

Type: Clear defect

Problem solved: Staff need to know whether SMS and email each succeeded, and retry only the failed channel.

Rough size: Medium

### Durable approval receipt

Type: Opinion, consider it

Problem solved: A customer should be able to see the signer name, signed time, selected items, discount, tax, total, deposit, and current balance in one place.

Rough size: Medium

### Run-scoped QA console

Type: Opinion, consider it

Problem solved: Auditors need a safe list and cleanup action for only the test quotes created by one run. The current global test-data cleanup is too broad for parallel audits.

Rough size: Medium

### Guided first-quote walkthrough

Type: Opinion, consider it

Problem solved: A new operator has to infer the order of contact, photo analysis, design, pricing, recommendation, and send controls from a long builder.

Rough size: Medium

## Deep-dive area

Post-approval change management. The current amendment math and settlement block are careful, but the customer-facing consent step is missing.

## Test quotes created in production

- `e3eefe4e-3403-4512-9551-e947d8482943` — `TEST-CODEX-2026-07-18`; final state `booked`; one $1,200 test-only manual line; typed approval; simulated deposit; no real message or payment.

Cleanup list: delete test quote `e3eefe4e-3403-4512-9551-e947d8482943` and its derived test job/invoice when this audit artifact is no longer needed. It is intentionally retained for review; do not use the global cleanup while other test work may be active.

## How this tool works

An operator signs in and starts a quote in test or real mode. They choose a service vertical, add customer details, load a property image, and use AI analysis to seed a design. The design drives measured line items. The builder prices the scope, applies discounts and fees, calculates tax, splits the deposit, and lets staff choose recommended items.

The operator saves and sends the quote. The customer opens a public portal link. They can compare packages, toggle eligible items, see the live total and deposit, type their name as an e-signature, and approve. A test quote simulates the deposit. A real quote can enter the Valor payment flow. Paid deposits create the booked job. Completed work produces an invoice for the remaining balance.

The approval snapshot is the signed source for the selected package, item ids, total, deposit, and signature. Later amendments append to that snapshot. Invoice and settlement code use the latest agreed total and the actual deposit paid.

## What was confusing on run 1

- The builder says staff should copy and paste the portal link, while the send route also attempts SMS and email.
- The amendment route says the customer must re-approve, but the portal has no way to do it.
- The quote PDF preserves the original signed total, while invoices can move to an amended total.
- Test mode suppresses all outbound email, even though the verifier brief names `TEST_CUSTOMER_EMAIL`.
- The repository README is still the default Next.js text.
- The long quote builder has visual labels that are not connected to their inputs.
- A quote can visibly enter the sent state even though its public portal has no line items and remains in the finalized placeholder.

Passes run: 7. At the user's direction, the five-pass cap was removed after pass 5. Pass 6 reviewed the public/operator authorization boundary and capability-token quote actions; pass 7 reviewed Valor payment parsing, settlement, and booking idempotency. Neither produced a new confirmed static finding, so the loop stopped after two consecutive dry passes. The production verifier subsequently added CODX-F-006 from a directly reproduced customer-flow failure.
