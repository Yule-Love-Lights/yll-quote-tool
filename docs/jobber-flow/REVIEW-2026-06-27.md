# #83 Jobber-flow — adversarial stack review (2026-06-27)

A 7-dimension multi-agent review of the full #83 PR stack (#185, #186, #188, #189, #190, #192, #194), each finding independently verified by an adversarial refuter. **15 raw findings → 7 confirmed, 1 uncertain, 7 refuted.** All 7 confirmed are now fixed (or deliberately deferred with reason) below.

## Confirmed findings & resolution

| # | Sev | Finding | Resolution |
|---|-----|---------|------------|
| 1 | **HIGH** | `jobs.quote_id` / `invoices.quote_id` / `invoices.job_id` FKs had **no `ON DELETE`** → `deleteQuote` + the operator "delete all" bulk-wipe would FK-violation-**fail** once any quote is booked/invoiced (a real regression on master's working delete paths). | **Fixed** — `ON DELETE CASCADE` on all three (P2 jobs migration + schema; P3 invoices migration + schema). Deleting a quote tears down its job + invoice, matching the existing "delete a quote removes its design artifact" intent. *(Tables not yet applied to prod, so a clean migration edit — no constraint surgery.)* |
| 2 | MED | `createJobFromQuote` never set `jobs.customer_id`/`property_id` → customer identity stayed **NULL through the whole job→invoice chain**; the invoice test hand-seeded a `customer_id` the real path never wrote (green test masking a broken flow). | **Fixed** (P2) — `createJobFromQuote` now carries `customer_id`/`property_id` from the quote via a **separate best-effort lookup** (a pre-Phase-5 DB nulls the linkage instead of failing job creation). + tests for the carried and null cases. |
| 3 | MED | Invoice prices off the **live `quote.result`**, not the job's booking-time `line_items` snapshot — apparent contradiction of the snapshot model. | **Doc-clarified** (P3) — this is *intentional and correct*: the invoice is created at install-complete, after any amend, so the live result holds the **final agreed total**. The snapshot is a fulfillment reference, not the pricing source. Comment added; flagged the pre-existing `updateQuote`-no-booked-guard caveat for Jason/#81. |
| 4 | LOW | check-then-insert in `createJobFromQuote`/`createInvoiceFromJob` returns null to the **loser of a concurrent insert race** (no 23505 recovery). Idempotency itself holds (partial unique indexes); only recovery was missing. | **Fixed** (P2 + P3) — both detect a `23505` unique-violation and converge on the existing row. + race-recovery tests. |
| 5 | LOW | `rebook` (P5) omits `status:'draft'` that `saveQuote` sets → a NULL-status row (harmless today via the `deriveStatus` fallback). | **Deferred to integration** — P5 is off master, which lacks the `quotes.status` column until P1 merges; adding `status` now would 500 against a status-less DB. Add `status:'draft'` to `buildRebookInsert` once the status spine lands. |
| 6 | LOW | `db/schema.sql` mirror omits the sequences / `allocate_display_number` RPC / `updated_at` triggers it advertises in comments — a fresh DB built from it alone is incomplete. | **Noted, not changed** — pre-existing house style (master's `FULL-SCHEMA.sql` already lags); the dated `.sql` migrations are authoritative and applied separately, so no runtime impact. Worth a future mirror-completeness pass, out of scope for these fixes. |
| 7 | NIT | `db/schema.sql` merge collisions: P1/P2/P3 (stack) and P5 (off master) all insert new-table blocks at the same anchor → a real (empirically reproduced) conflict when P5 meets the stack on master. | **Expected at merge** — mechanical hand-resolve (keep both blocks), already covered by the AGENTS.md "merge current, never stale" protocol. Resolve in merge order + re-run gates. |
| (unc) | NIT | P3 (`invoices.ts`) and P4 (`amend.ts`) used divergent `round2` (EPSILON-nudged+guarded vs plain) — unreachable through any real money path (deposit rounded once canonically; both only round already-2dp values). | **Aligned** (P4) — `amend.ts round2` now matches `invoices.ts`, so an amend and the invoice it feeds round a balance identically. |

## Refuted (7) — verified NOT defects
Adversarial refuters cleared the rest, chiefly: surfaces correctly deferred to #81 (not live), `attachQuoteToCustomer` intentionally not wired to `saveQuote` yet, the Valor webhook's atomic deposit claim already serializing job creation, and the balance auto-charge being a flagged open decision rather than a bug.

## Still open (decisions, not defects)
- **Balance collection mechanism** — "both" (auto-charge the vault card + pay-link fallback) is the chosen direction, but a server-initiated card-on-file (MIT) charge needs a **confirmed Valor capability + Jason's integration**; the current integration builds "no auto-charge." `balanceCollection.ts` ships only the pure routing. Execution gated on #81 + the Valor confirmation.
- **#81 auth perimeter** — still the hard prerequisite for every money/operator surface across the stack.
