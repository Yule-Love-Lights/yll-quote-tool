---
name: task-ledger
description: Master task ledger — the single clean renumbered list of all tasks (done / planning / backlog / Naldo-pending) with size + status. LIVING DOC — keep current as work lands.
metadata: 
  node_type: memory
  type: project
  originSessionId: e4aa4be2-5f3b-45ef-ba10-c5884f1b5b20
---

# Task ledger (master list)

> **Single source of truth for tasks.** LIVING DOC — update statuses as work lands; this supersedes the old scattered QA punch-list. Renumbered into ONE clean sequence on 2026-06-03; the **Old #** column maps to the messy historical numbers still referenced in `session_log.md` + PR titles. **Size:** S (hours) · M (~a session) · L (multi-session / epic).
>
> **CONVENTION (Jason likes this):** when a task completes, move it to ✅ Completed and ALWAYS record the **session it shipped in** in the `Shipped` column (matching `session_log.md` numbering — S1, S2, S3, …; use a range like S2–S3 if it spanned sessions). Keep this for every task going forward.

> **✅ DESIGN-TOOL RELAY DONE (S14):** the **"Cool White"** bulb color (`cool-white-faceted` · hex `#cee1e7` · glow `#eef5f8`) added to the quote tool (PR #133) — **plus** the existing `cool-white` relabel **"Cool White" → "Pure White"** (id + hex `#e0eaff` + glow unchanged) — was **mirrored into the standalone design tool's palette at `d2389df`** (`client/src/editor/colors.ts` `DEFAULT_COLORS`, byte-identical; the design tool also seeded its **server** `DEFAULT_COLORS`, since its live palette is API-served, which backfills the new builtin into existing palettes), so the two palettes are back in sync (same relay convention as #71/#73; verified against their file on disk). The quote-tool side (`editor-core/colors.ts` `DEFAULT_COLORS` + the portal "Cool White" Light-Color chip in `colorSchemes.ts` + buildable in Build-your-own) shipped in PR #133, LIVE & verified on prod. *(PR #133 also added `withMissingBuiltins` to `getAppSettings` so a newly-added built-in auto-propagates into a stored prod palette — fixed the "new color missing from Settings" bug that forced an earlier revert.)*

> **✅ DESIGN-TOOL RELAY DONE (2026-06-26):** the new **"Stake Lighting"** lighting category (`stake-lighting` surface tag) — an independent sibling of Winter Wonderland (manual ft × difficulty + satellite "Add Stake Run" draw, own portal kind + `Lightbulb` icon, own **$6/$7/$8** per-ft rates) — shipped to the QUOTE TOOL (PR #143, LIVE on prod; spec + plan in `docs/superpowers/`). The only **shared editor-core** change (`editor-core/editor.ts` c9-branch `surfaceOpts` += `["stake-lighting", "Stake Lighting"]` + the `Surface` union `'stake-lighting'` in `sceneTypes.ts`) was **mirrored into the standalone design tool at `c4206e9`** (pushed to main; the design-tool AI confirmed its `surfaceOpts` block matched the diff exactly with no divergence, tsc clean — quote-tool-only bits like pricing/portal/satellite/training intentionally left out). **Cores back in sync** (same relay convention as #71/#73/Cool White). The satellite **purple `#a855f7`** stake channel is app-layer only (portal/builder) — nothing in the shared core, so not relayed.

> **✅ DESIGN-TOOL RELAY DONE (Jason S14, 2026-06-29) — #82 Slice 2b roof-feature tag:** the `RoofFeature` type + optional `roofFeature` on `StrandItem` (`sceneTypes.ts` → design tool's `client/src/api.ts`) + the "Roof feature" dropdown in the editor Quote-binding panel (`editor-core/editor.ts` → `client/src/pages/editor.ts`; 4 hunks — import · options · c9-only Roof-feature `<select>` · change-handler) was **mirrored byte-identical into the standalone design tool at `6f9a775`** (pushed to their main). Verified on disk: the 4 editor.ts hunks byte-identical (incl. the `roofFeatureOpts` labels), the `roofFeature` block correctly placed right before `sSurface`, and the `RoofFeature` type matches semantically (their double-quote style is the pre-existing scene-types divergence); tsc clean their side. Built by Naldo (#203, hunks in `docs/superpowers/specs/2026-06-27-inventory-82-slice2b-relay.md`); relayed by Jason. Cores back in sync (same convention as #63 / Stake / Cool White / #71 / #73).

> **🟢 SECURITY — both prod CRITICALs now CLOSED (verified S15, 2026-06-29; #81 auth LIVE + #90 hardening all shipped):** the full-tool audit (#80) is DONE (**109 verified findings**; report `docs/audit/AUDIT-2026-06-26.md` + raw `_verified-raw.json`), all NON-auth bug-fixes merged + live, **and the 2 CRITICAL anonymous-PII exposures are now FIXED on prod** by **#81 (Supabase Auth, Option B — merged PR #222; `AUTH_GATE_ENABLED=true` flipped LIVE in Vercel Production by Naldo, S12)**: (1) `/customers` list/detail + `GET /api/customers`, and (2) `GET /api/integrations/highlevel/contacts`, all now require an operator session. **Verified by prod probe S15:** `/customers`→`307 /login`, both APIs→`401`, while a customer `/portal/<id>` link stays public (no over-gating). The rest of **#90 hardening** shipped too: **RLS enabled on all 14 tables** (PR #227 — Supabase advisor's CRITICAL "RLS disabled" is **cleared**; the 14 "RLS enabled, no policy" notices are INFO/intentional for the service-role model), **`created_by` actor audit trail** (PR #228), **PII-retention purge cron** (PR #229), and **portal friendly-error boundary** (PR #230). **Remaining = OPTIONAL low/WARN hardening only (none CRITICAL):** Supabase advisor WARNs — mutable `search_path` on trigger fns, `vector` extension in `public`, leaked-password protection off. _(The original "2 CRITICALs live" wording — accurate pre-#81 — is preserved in the #80/#81 rows below + the S11–S13 session logs.)_

> **Completed + shelved tasks -> `task_ledger_archive.md`.** Next free task #: 460 (S57 minted 450-453 on 2026-08-28 — the two customer-facing gaps PR #1051's premerge round found and gated behind `installment_auto_charge_consent_at` (450 the portal/PDF balance that never moves, 451 the missing customer receipt), and 452 the migrated quotes with no frozen deposit rate, and 453 the missing in-app path to a legitimate change on a migrated order, raised independently by the #1053 customer and admin lenses. Checked free against master, the ARCHIVE, every open PR and every remote branch at mint time.) (S74 POST-CLOSE minted 458-459 on 2026-08-29 — the missing void/delete for a bogus manual shift, and the paid-day edit-guard trigger for the Staff payroll build. Checked free against master, the archive, every open PR and the machine-local self-assessment at mint time.) (S74 minted 454-457 on 2026-08-28 — the truck-and-trailer tracker activation steps, the schedule page's silent-empty crew dropdown, the 25-address geocode backlog the repaired fix-list revealed, and the S74 review-residuals bundle. Minted from 454, NOT 450: Jason's OPEN PR #1052 (jason/ledger-450-452) claims 450-453, so S74 claimed above it per the collision rule — the counter is necessary and not sufficient. Checked free against master, the archive, every open PR and the machine-local self-assessment at mint time.) (S56 minted 444-449 on 2026-08-28 — the Homeworks-migration follow-ups: the Calculate money trap on migrated quotes, the unlinked installments page, the uncallable markInstallmentPaid, the lost draft inputs, the monthly runner, and the UTC-vs-ET overdue check. Checked free against master, the ARCHIVE and every open PR at mint time. Jason ruled 2026-08-28 that 448 is built FIRST next session and the rest batched behind it.) (442 = customer prices captured in PostHog autocapture element text, MEASURED at 1004 events over 180 days and PRE-EXISTING; 443 = the QuoteBuilder Clear-all with no confirm, found by the S73 close staff lens in another session's merged PR #916. 440 = the marketing site's sticky-header script throwing on live visitors, 441 = the lead-form drop-off fix once the new per-field events have collected data; both minted 2026-08-28 by S72 after confirming master, the archive and every open PR left 440+ free. S72 is the PostHog automation session: its own shipped work is rows nothing, since the pipeline is recorded in docs/context/posthog_daily_review.md rather than as ledger tasks.) (439 = the S71 wrap-review deferral bundle (fleet/geocoding polish + the estimate-route geocode perf), minted at close after checking 439 free against master, the archive and open PRs. 438 = the dead crew-login path left by row 433's deletion of `/api/ops/v1`, found by the S69 post-close integration lens and minted 2026-08-27 after confirming master, the archive and every open PR left it free. 437 = the S70 close integration lens's code-level leftovers from the staff-panel consolidation, minted 2026-08-24 after confirming no open PR or unmerged branch claimed it.) (S68's verification wrap minted **435-436**: the self-serve estimator's zero telemetry rows, answered at the wrap as a traffic problem rather than a broken insert, and the missing canonical on /estimate which the same PR SHIPPED. **This is the FOURTH mint for the same two findings**, and the history is the point: 404-406 (ceded to Jason's #947), 409-411 (lost to his merged S51/S52 rows), 415-417 (lost when close PR #948 was closed unmerged), 430-431 (lost to the merged S69 close #1014). Every collision was caught at a pre-merge re-check, so the rule held four times running, but the real lesson is the cost: a close PR left open across other sessions' merges renumbers on every re-sync. Land it the same hour, or carry the findings in as few numbered places as possible.) Earlier: Next free task #: 435 (433 = the orphaned Operations Hub routes and contract, 434 = the S69 close customer lens MED on mini-group billed-vs-drawn counts, both minted by S69 2026-08-27 ABOVE the S68 block below, which had claimed 430-432 in a then-unmerged PR #1011 and has since merged) (430/431/432 = the S68 fleet-GPS deferrals, each recorded with the lens that found it: 430 Bouncie connection health + keep-alive, 431 the visit-timeline operability layer, 432 geofence radius and overlap tuning. Checked free against master, the archive and every open PR at mint time. S55 minted 429 on 2026-08-27 - the portal-visibility confirm, shipped same day in PR #1004. Checked free against master, the ARCHIVE and every open PR at mint time. Earlier in S55: minted 423-428 and closed rows 367 and 251.) (S55 minted 427-428 on 2026-08-27 from the PR #1002 lens round: the audit-trail coverage gap on booked-order photo writes, and the crew bot's unguarded extra_photos write. Both deferred with a MEASURED exposure rather than a guess. Checked free against master, the ARCHIVE and every open PR at mint time. Earlier in S55: minted 423-426 and closed rows 367 and 251.) (S55 minted 423-426 on 2026-08-27: the booked-order design audit trail Jason approved, the RULED-open satellite trace, the pre-approval mis-link found live on quote #1304, and the mismatch sweep as a standing guard. Checked free against master, the ARCHIVE and every open PR at mint time - no open PR claimed 423+. S55 also SHIPPED and closed rows 367 and 251.) (415 = the missing PII-retention purge cron, claimed by PR #945 and merging now: the ledger records PR #229 as shipping one, and there is no purge route, module, or cron anywhere in the tree. 415 was re-verified free against master, the archive and every open PR immediately before this merge - 416 and 417 are ALSO still free, because concurrent sessions minted from 418 upward while #945 sat open. 403 = the fleet-GPS/Bouncie integration, decided with Naldo 2026-08-25. S54 minted 422 MID-SESSION 2026-08-26 - the staff-portal analytics pollution deferred from PR #991's admin lens; checked free against master, the archive and every open PR at mint time. Earlier: S54 minted 421 MID-SESSION 2026-08-26 at Jason's ask - the crew-bot is_office gap found by the #983 admin lens, flagged NALDO; checked free against master, the archive and every open PR at mint time. Earlier: S53 also minted 420 at close - the wrap customer lens MED on merged #972, the lifecycle-blind autosave restore. Earlier: S53 minted 418 (clickable customer name on the job page) and 419 (Service Type column on quote/invoice tables), both from Jason's 2026-08-26 device notes. Minted from 418, NOT 415: Naldo's OPEN S68 close PR #948 already claims 415-417 and bumps this counter to 418 - checked master, the archive, and every open PR per the collision pitfall. S53 also closed and archived rows 188/327/334/370/407/409/410/411/412/413/414 in the same close that shipped them. Earlier: 415 (S51 minted 413 (edit-mode autosave, the half of row 406 a warning cannot cover) and 414 (the mark-paid stale-marker dead end, found by two premerge lenses). Checked free against master, the ARCHIVE and every open PR first - the concurrent S52 session had already taken 409-412, so the 409 this session originally planned to claim would have collided. S52 minted 409-412: the NCE-vs-deposit reconcile gap Jason accepted, the ~11 loading morphs outside row 346's scope, and the three-way duplicated quote-audit idiom. Verified free at mint time against the counter, the archive, every open PR and every unmerged branch; S51 holds 404-408. S50 minted 404-408: the unclearable stale-invoice marker, the silent re-analyze override discard, the CONFIRMED typed-value loss on a priced field, the two accepted board-warning papercuts, and the still-owed #921 Recount check. **COLLISION RECORDED:** the counter read 403, but Naldo's OPEN draft PR #945 (`claude/gps-tracker-quote-tool-g2luwq`) already claims **403** for the fleet-GPS decision row, so S50 minted from 404 rather than silently taking a number an open PR holds - the AGENTS.md rule that the counter is necessary and NOT sufficient. S50 also ARCHIVED 18 shipped rows (361/379/380/381/382/386/389/390/392/394-402) in the same close that shipped them.) (S49's wrap minted 402, the pre-existing duplicate-row-number cleanup found by the post-close integration lens. Earlier in S49: 394-401 - the recorded findings from the five parallel builds, held UNFIXED at Jason's instruction 2026-08-25 so the fixes land next session with fresh context. 394/395 block PR #939, 397 blocks PR #940, 401 is process debt on PR #941. S49 also minted 388-393 - the deferred findings from its four pre-merge lens rounds; see the S49 journal fragment. Checked free against master, the ARCHIVE, and every open PR before minting - including Naldo's still-open #923, which holds 367-377. S48 minted 378-387 - the wrap-review deferrals; see the S48 journal fragment. **COLLISION RECORDED:** the counter on master read 367, but Naldo's OPEN PR #923 (`naldo/deferred-findings-367-377`) already claims **367-377** and bumps this counter to 378, so S48 minted from 378 rather than silently taking numbers an open PR holds - the AGENTS.md rule that the counter is necessary and NOT sufficient. 363 remains Naldo's via merged #913.) Earlier: Next free task #: 367 (364 the ScheduleDay staleness-label regression, 365 the follow-up refresh lag, 366 the follow-up resurrection TOCTOU; both from the S47 wrap staff lens, minted 2026-08-24. 363 was SKIPPED, not lost: Naldo's open PR #913 (naldo/unified-staff-panel) already claims it - checked against master, the ARCHIVE, and every open PR before minting, which is the check that failed earlier in S47 when 347 turned out to be held by an archived row.) Earlier: (360 dormant auth gate, 361 editor-core relay divergence, 362 Kristie colour mismatch; minted 2026-08-23 in S47. COLLISION RECORDED: S47 first minted these as 347/348/349 against a counter that read 347, but by the time its ledger PR was raised, master had MERGED 348/349/350 (the S65 light-size rows) and 347 was already held by an ARCHIVED row, while Naldo's unmerged #905 held 351-357. Merged rows win, so S47 renumbered rather than hand-resolving the conflict, and rebuilt its branch on fresh master.) Earlier: (358 = the Ops Hub time-clock API exposure, scoped 2026-08-23 then parked by Naldo to stay on the Quote Tool; 359 = the orphaned-office-pay-row deferral from PR #906's admin lens. Minted from 358 rather than 351 because Naldo's UNMERGED S66 close PR #905 already claims 351-357 and bumps this counter to 358; recorded here per the collision pitfall instead of silently taking a number that an open PR holds. Jason's unmerged #902 separately claims 347-349 and bumps to 350, which collides with the 347-350 block master already records below; that one is his lane to resolve.) (347 = S65 SHIPPED, the per-design light-size slider `scene.lightScale`, PR #883 merged+live — in the archive Completed table; 348-350 = the S65 wrap review's three MED findings on that merged PR — 348 undo/redo slider-display desync [+ the admin LOW no-clamp folded in], 349 the 4x portal oversell [NALDO product call], 350 spritzer tip>centre at high scale; minted 2026-08-23 after confirming no open PR or unmerged branch claimed 347+.) (342-346 = the S46 premerge/delta-verify deferrals — Gmail write-back invisibility (342, claimed in merged #886's body), the identity recovery+audit bundle (343), the portal live-price vs frozen-total drift plus the un-frozen footage population (344), the permanent-side derive clobber (345), the remaining Loading morphs (346); minted 2026-08-23 after confirming no open PR or unmerged branch claimed 342+.) (338-341 = the S45 residual recordings (#839 freeze escape hatches · invoice_basis ms-race · #861 revive-reseed cluster · #867 bistro post-approval freeze gap), minted by S46 2026-08-22; **337 = CLAIMED by Naldo's UNMERGED S64 close PR #876** (the concurrent-send delivery race) — recorded per the collision pitfall so the claim survives whether or not #876 merges as-is; 335 = the `/admin/schedule` UTC-vs-ET day default and 336 = the mis-named crew-parser test, both S61 wrap findings held unnumbered until the 301->318 collision chain settled; minted 2026-08-21 after confirming no open PR or unmerged branch claimed 335+.) (S44 took 324-334 - its own review/deferral rows; see the S44 block in session_log.md. 322/323 are S43's post-close delta, claimed while S44 was still open and confirmed by S43 directly - S44 deliberately claimed ABOVE them rather than racing the counter. Row 318 = PR #818's crew-Telegram row, which renumbered itself 313 -> 318; S44's row 313 (amendment figure parity) is the merged claim on 313 and stands.) (S43 close took 320 = the wrap-residuals bundle + 321 = the pending-colour-request invisibility HIGH, live prod case) (S43 took 316 = the ingested-churn noop fix [PR #826] + 317 = the terminal-quote auto-complete ruling [booked/declined/abandoned leave the inbox] + 319 = colour-request internal notification; 318 CEDED to PR #818's crew-Telegram row per the S43 coordination comment on that PR) (S42 wrap took 313-315: 313 = the master-live amendment card/notice figure mismatch [closes at the amend-decline branch merge], 314 = event-date GHL sync at approval [Jason 1b ruling 2026-08-20], 315 = the S42 polish batch. Row 292 RESOLVED by #799 and archived with the resolution note; S41 independently corroborated.) (S41 took 302-312: 302 = InboxList thrown-fetch silent-drop SHIPPED #806, 307 = Needs-a-look SHIPPED #814 -- both numbers were already cemented in merged code comments before the counter moved, the merged-source rule's first PRE-collision save; 303-306 + 308-310 = the S41 review-debt rows below. S41's row 292 claim was CEDED to S42 -- resolved by S42's #799, S42's wrap records it.) <!-- S50 (Naldo) took 301 = wedding copy left on the two new marketing pages --> (299 = the S58 post-close review fixes PR #812; 300 = scheduling.ts DB-function test coverage. ⚠️ **SESSION-NUMBER CORRECTION 2026-08-19:** rows here previously read "SHIPPED S59" and "SHIPPED S60". Those were WRONG — all of that work happened inside the SINGLE S58 conversation, and this repo's rule is one conversation = one session. Worse, a genuinely separate session had already taken S59 (the 8-day copilot outage, open PR #810), so the labels collided with real work. Corrected to "S58 post-close"; S59 belongs to PR #810.) (S40 wrap took 293: bare-row thread-wide write-back, the sibling-reburial class’s third instance) (S40 took 291-292: inbox single-slot busy/error display state -> 291 [+ its InWorksSection sibling, lens-found pre-build], GHL socket-reset hedge-skip fail-open -> 292; S40 also folded the QuoteBuilder Force-Redeliver zero-confirm-gate finding into existing row 269 — same button, same fix surface) (S39 took 288-290: GML thread-coalescing buried-leads HIGH -> row 288, group-collapse lost-race MED -> row 289, timeout-hedged redeliver-confirm MED -> row 290, all found by the S39 wrap review) (S59 took 279-285: crew auth, the three Flow B routes, midnight auto-close, FULL-SCHEMA regeneration, success metrics, and the two parked professional items. 279 unblocks 280; 283 is the one A6 product question still unanswered; 284/285 are explicit non-blockers) (277 = S58's shipped P4P break-tracking row, in the archive's Completed table; 278 = the open_break exception-queue sweep, below. ⚠️ **S58 NUMBERING HISTORY, kept because this collided repeatedly in one session:** these two rows were written as 259/260, moved to 276/277 when Jason's merged S37 rows 259/260 won under merged-claims-win, then moved again to 277/278 when PR #778 landed rows 272-276. The standing lesson, now an AGENTS.md pitfall: the counter on master is necessary and NOT sufficient — check open PRs and unmerged branches before claiming a number, and re-check immediately before merging.) <!-- S50 post-close took 272-276 = the get-a-quote redirect dead-end, the Elementor mobile header overlap, and three deferred site-forms items (no GHL retry, no admin handled-state, nominee PII in the GHL alert email) --> (S38 truth-up: the "257" here was stale — S37's wrap wrote rows 259-266, and S38 added 267; 216 left for Jason's S34 approved-unpaid-alert candidate; #174 stays RESERVED for S31's winback wrap; 234 taken by the S55 close = unescaped customer text in the internal escalation/digest emails). <!-- S34 wrap: 197 + 211 WRITTEN as real rows (Naldo's #670 content, per Jason's call — reservation cleared); S34 SHIPPED 198/200/204/213 (archived) and the wrap review took 214 (identity-caller seam, #199 blocker) + 215 (builder chip confirm). --> <!-- NUMBER COLLISIONS RESOLVED (Jason S34, 2026-08-05, merged-claims-win rule): 192/193 → #674 (merged code) keeps 192 = per-side track style + approved-sides BOM scoping, 193 = amendment-aware BOM scoping follow-up. 196 → PR #690 (MERGED same day) holds 196 = GHL contact-overwrite risk on the non-lead website forms. Naldo's OPEN #670 (its branch commit 05a63b33 had taken 196/197) must renumber AGAIN during its master-conflict resolution: its rows land as 197 = approved-package desync (buildApproval) + 211 = VALOR_* env docs — both RESERVED here. --> <!-- S34 (Jason) took 198-205 = Jason's S34 feature/fix batch (NCE tags + NCE money, GHL tenure field + push, Jobber backfill campaign, decor S/M/L, trace draw parity, standalone satellite pull, properties tab), 206-210 = the S33-deferred wrap rows, and 212 = discount option for event/permanent/bistro builders. --> <!-- S50 (Naldo) took 194 = marketing-site missing service pages (Diwali, Ramadan/Eid) and 195 = leftover Gravity Forms shortcodes, residue of the #152 custom-embed migration. --> <!-- S33 (Jason) also took 191 = permanent portal facade glow removed (#88 P6b-3 hard-edged wash band, deleted not fixed); S33 (Jason) took 189 = getReopenCounts seq-scan + ingested-flood (closes #185 verify); S33 took 190 = analyze-photo satellite-scale clobber fix; S32 took 187/188 (wrap follow-ups); S30 (Jason) took 172 = GHL abandoned-card resurrect + 173 = stale-balance under-charge race (pre-existing, #640 review) (attach/send/lead deadlock on non-open cards); S29 took 170 (auto-charge arming checklist) + 171 (wrap polish batch); 168 = text-ops (Naldo S46), 169 = colour aftercare (Jason S28 wrap) --> <!-- was stale at 146; S26 consumed 146-150, S31 took 151; S34 created the long-reserved #118; S33 took 152 = website lead-capture forms (custom embed replaces Gravity Forms sitewide, per-service GHL routing) -- MERGED+LIVE (#486/#494/#497/#498/#501/#504/#513); embed UX polish #517 OPEN pending Naldo merge-go; S35 took 153 = PERSONA-SWEEP AUDIT DEFERRED BUNDLE (WT-63 holiday-rates->Settings [money, needs a frozen per-quote rate snapshot like permanent] + portal subset WT-L1/L2/L3/L4/10/11/12/54 [held for #526 core-session reconcile; WT-L1=PS-C1 fix-once-in-adapter] + multi-property epic WT-03/52/53 + WT-08 bistro auto-trace + WT-55 customer-merge UI; 50 of 65 findings already FIXED in PRs #524/#527/#529/#534/#538 awaiting merge-go; full record docs/audit/PERSONA-SWEEP-WHOLETOOL-2026-07-12.md + PR #526); S39 took 155 = legacy Jobber quote migration into the quote tool (rebooking; PR #556); S40 took 156-158 = the #155 send-side buildout (GHL Neighbors pipeline / inbox+stats exclusion / YLL Neighbor tag); Jason S27 took 159-166 (payment money-path cluster); S41 took 167 = referral+booking bug batch (PRs #567/#569/#570 -> DONE+LIVE, in archive) --> <!-- NUMBER COLLISION RESOLVED (Naldo S57, 2026-08-10): Jason's S35 close and Naldo's concurrent session both claimed 222-226 for DIFFERENT tasks. Tiebreaker used: whether the number is referenced in MERGED SOURCE CODE, not merge order. Only 222 was -- Naldo's meaning (fast-sent quote gets no follow-up) is cemented in src/lib/dashboard/inbox/store.ts comments and a store.test.ts describe block on master, so 222 KEEPS Naldo's meaning. 223-227 appear in no code on either side, so Jason's merged 223-227 keep their numbers. Remapped: Jason's 222 (relay #202/#203 into the design tool) -> 228; Naldo's 223 -> 229, 224 -> 230, 225 -> 231, 226 -> 232; Naldo's S57 P4P Track A row -> 233 (in the archive's Completed table). KNOWN DRIFT, not an error: Jason's session_log.md still calls the relay row #222 (his file, not edited by this session) -- read it as 228. --> <!-- RESOLVED (Naldo S58, 2026-08-11): PR #713 (S55 close) had claimed 222 for a DIFFERENT task — unescaped customer text in the internal escalation/digest emails. 222 is definitively the fast-sent-quote follow-up fix (referenced in merged source), so #713's row was renumbered to 234 and this counter bumped to 235 during the master-conflict resolution. Anything that still calls that row 222 (the S55 session-log entry, the PR #713 title and body) is stale wording, not a second claim on 222. --> ⚠️ **RENUMBER HISTORY FOR THE S61 CREW-TELEGRAM ROW (now 318) — it moved THREE times in about fifteen hours while its PR sat open.** Claimed **301** → S50's row 301 (wedding copy on the Diwali/Ramadan pages) merged and won. Moved to **313** → Jason's S42 wrap rows 313-315 merged and won. Moved to **316** → ceded to Jason's S43 lane, which held 316 and 317 in two open PRs pushed earlier that hour (#826 carried the number in its PR title, branch name and commit; #829 wrote both rows) — both have since merged, confirming the cede was right. Landed on **318**, which was and remains free. Its three source-comment references moved with it every time (`src/app/api/admin/crew-accounts/route.ts`, `route.test.ts`, `src/app/settings/accounts/page.tsx`). TWO RULES this paid for: (1) a number cemented in the SOURCE of an UNMERGED branch does not outrank a merged row — source anchoring only breaks a tie between two unmerged claims, which is the case the S57 rule was written for; (2) the third move was NOT a rule outcome (neither side was merged) but a coordination call, accepted because their number was baked into a PR title and branch name while this one was a row plus three comments — do not read it as precedent that unmerged claims outrank each other by seniority. A long-lived close PR should expect to renumber on every resync and keep its row number in as few places as possible. **PR #819 was CLOSED unmerged on 2026-08-20, so its competing claim on 301 is moot and S50's merged row keeps 301.** The TWO S61 wrap findings remain unnumbered by choice — (a) `src/components/ops/ScheduleDay.tsx` defaults its day with `new Date().toISOString().slice(0,10)`, the UTC calendar day rather than the ET business day, so between ~8pm and midnight ET the schedule page opens on TOMORROW (pre-existing; S61 applying the `job_assignments` migration made it reachable); (b) a test named "recognises status, help and done" that asserts only `status` and `complete`. Mint both from 335 onward. (367-377 = the deferred and blocked findings from the 2026-08-24 stale-PR cleanup's lens rounds on PRs 849/866/871/872/874/875/881; minted below 378, so S48/S49's block never collided with them.)

## 🟡 In planning / in-flight
*#83 is DONE — data layer + operator/money SURFACES both MERGED + LIVE (surfaces via PR #251 → master `44a48b9`; auto-charge still gated on Jason/Valor). (#82 inventory epic is complete → archive.)*
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| **453** | **A home.works-migrated order has no in-app path to a legitimate change** — the row 444 guard refuses `/api/quote`, `free-items` and the design writes outright, and the amend flow is structurally a no-op behind it. | S | Found independently by the S57 PR #1053 customer AND admin lenses. `/api/quotes/[id]/amend` derives its delta from `quote.result.total` against the frozen approval total; since nothing can update `result` on a migrated quote, the delta is permanently 0 and every amend 409s `no-change`. The refusal is CORRECT — the engine cannot reproduce these figures, so re-pricing one destroys what the customer agreed — and it fails closed, so no money is at risk. But the only escape hatch is the guard's own message, 'agree the new figures with Jason and record them directly', which is an out-of-app manual process for money a real customer's balance depends on. 20 orders, 3 of them still owing on payment plans. Fix shape when one of them actually adds work: a deliberate staff action that records new agreed figures verbatim, the same way the migration wrote them, rather than anything that re-prices. Not urgent until it happens; recorded so the next person meets a known limit rather than a mystery. | — |
| **450** | 🔴 **A collected installment never moves the balance the CUSTOMER sees** — the portal's approved card and the Quote PDF both compute what is still owed from `approval_snapshot.customerSelection.currentDepositUsd`, frozen at approve time, while `markInstallmentPaid` moves only `quotes.deposit_amount_usd`. | M | Found by the S57 PR #1051 premerge customer lens; verified in `src/lib/pdf/docModels.ts:301-354` (`balanceDue = approval.totalUsd − approval.depositUsd`) and `src/lib/portal/adapter.ts:714-731`. The PDF is one click from `/portal/[quoteId]/approved`. NOT reachable today: nothing collects an installment automatically, and PR #1051's runner refuses every quote whose `installment_auto_charge_consent_at` is NULL, which is all of them. **This row and 451 are named in `migrations/2026-08-28-installment-auto-charge-consent.sql` as preconditions for ever writing a value into that column** — recording consent before they land turns two latent bugs into live ones. Also blocks row 446's manual record button, which creates the identical drift. Fix shape: derive the collected figure from `deposit_amount_usd` when the quote has a plan, rather than from the frozen snapshot; do NOT rewrite the snapshot, which is the record of what the customer signed. | — |
| **451** | 🔴 **An automatic installment charge tells the customer nothing** — no email, no SMS, no portal change; the only notification is a staff Telegram line, and Valor's `receiptUrl` is read and discarded. | S | Found by the S57 PR #1051 premerge customer lens. `chargeOne` receives `result.receiptUrl` (`src/lib/integrations/valorBalance.ts`) and drops it. A homeowner's first signal that $453.13 left their card would be their bank statement — a real chargeback risk, and these three came from Homeworks where Jason collected every payment by hand, so none has ever seen an automatic debit from us. Same gate as 450: unreachable while consent is NULL, and named in the migration comment as a precondition for setting it. Fix shape: a receipt email on `status === 'charged'`, carrying the amount, the date, the payment number and what is left. | — |
| **452** | **All 20 Homeworks-migrated quotes carry no frozen deposit rate**, so every one of them resolves to the 50% default — including five that were paid IN FULL. | XS | Found by the S57 sweep behind Jason's NCE report. `approval_snapshot.customerSelection.depositRate` was never written by the #1049 migration. The three NCE quotes (#1162, #1312, #1313) showed a wrong amber `50% DEP` chip and were CORRECTED in S57 to `0.4`, the rate actually charged (ratios 0.40002 / 0.40002 / 0.39998) — Jason's call, dollar figures untouched. The other 17 show no chip on the list (a non-NCE quote only gets one when it is off 50%), but `/admin/quotes/[id]` still states a 50% deposit for **#1000 Asharib Iqbal, #1141 Michael Green, #1142 Miguel Vias, #1311 Rodney Smith and #1314 Anthony Infranco, who each paid 100%** (FIVE, not the four first reported here — Miguel Vias was missed in the original sweep and the count was corrected 2026-08-29 against a re-run), and for #1138/#1155/#1315 whose figures are running plan totals, not a deposit rate at all. #1316 Ryan Roth sits at 46.90%, which is neither the standard nor the NCE rate. RECOMMENDATION WITHDRAWN AND REPLACED (S57, 2026-08-29). The first recommendation here — stamp the actual collected ratio on the 13 non-plan quotes — was built on a misreading, and Jason corrected it: `deposit_amount_usd / total` is the COLLECTED total, not a deposit rate, so on a finished job it reaches 100% for everybody and stamping it would eventually mark every quote in the tool as a 100% deposit. **The chip exists to flag a genuinely DIFFERENT deposit arrangement: 50% is standard, 40% is NCE, anything else is very rare.** The standard flow is deposit first, remainder after installation. So the only migrated quotes that warrant a stamp are the ones that paid IN FULL BEFORE their install: Jason named **#1142 Miguel Vias and #1141 Michael Green**. OPEN QUESTION back to him: the payments export shows **#1311 Rodney Smith** (one $8,101.87 cash payment, 2026-06-07) and **#1314 Anthony Infranco** (one $1,086.30 payment, 2026-05-11) each settling in a SINGLE full payment too, the same shape as Miguel Vias, and **#1000 Asharib Iqbal** paying two equal halves two days apart (2026-06-29 and 2026-07-01) — so the export finds more candidates than he named, and which of those count as paid-before-install is his call, not something to infer. #1316 Ryan Roth's 46.90% is a single $816.00 check on 2026-08-04 against a $1,740.00 order and is presumably one of the rare cases; also awaiting his word. **RESOLVED 2026-08-29 — Jason ruled, three quotes stamped, row CLOSED.** He confirmed Ryan Roth was a rare case, and that the paid-in-full-before-install pair is Miguel Vias and Michael Green. So exactly three migrated quotes carry a genuinely different deposit arrangement and now say so: **#1316 Ryan Roth `0.469`** (chip reads 47%, a single $816.00 check against a $1,740.00 order), **#1142 Miguel Vias `1.0`** and **#1141 Michael Green `1.0`** (chip reads 100%). Verified through the real `depositChipState`: those three now show a chip, an untouched 50% quote stays hidden, and the NCE trio still reads 40% with no conflict flag. Every dollar figure untouched — all three already carried `currentDepositUsd`, so nothing recomputes off the rate — and the write is guarded on the key's absence, so a re-run touches nothing. #1311 Rodney Smith, #1314 Anthony Infranco and #1000 Asharib Iqbal are deliberately NOT stamped: they reached 100% collected the ordinary way (deposit, then the balance after install), which is the standard arrangement and exactly what the chip should stay quiet about. The three payment-plan quotes are not stamped either, for the reason above. | ✅ CLOSED 2026-08-29 |
| **444** | 🔴 **A staffer can destroy a migrated quote's money by clicking Calculate** — the 20 Homeworks-migrated quotes are `status='booked'`, which the quote builder deliberately exempts from the post-approval freeze (the amend carve-out), so Calculate re-prices through the pricing engine and persists immediately with no confirm. | M | Found by the S56 PR #1049 staff lens. The engine **cannot reproduce the charged tax on 8 of the 14 Homeworks invoices** (per-line rounding, two rates on one invoice, one rate absent from the document) — that is the migration's own founding measurement — so a recompute silently replaces the figures the customer actually agreed to and paid. Nothing in the UI marks these quotes as historical. LIVE IN PROD NOW; the interim rule is do not press Calculate on a quote carrying `approval_snapshot->'homeworks'`. Fix shape: refuse the recompute (or hard-confirm it) when that key is present, and badge the quote as migrated. Jason's call 2026-08-28: fix AFTER row 448, batched with 445-447/449. | — |
| **445** | **`/admin/installments` has no link anywhere in the app** — the payment-plans page ships invisible unless you know the URL. | XS | Found by the S56 PR #1049 staff AND admin lenses independently. Grep-verified against `OperatorShell`, `BillingSubNav` and all of `src/`: zero references. A direct recurrence of the row 296 `CrewLogins` inert-feature class that AGENTS.md already names as a pitfall. Fix: one nav or sub-nav entry. Batched with 444. | — |
| **446** | **`markInstallmentPaid` has no caller**, so when an installment customer pays there is no in-app way to record it. | S | Found by the S56 PR #1049 technical, staff and admin lenses. `src/lib/installments.ts` exports it with a correct compare-and-swap on the installment and (since the S56 fix round) on the quote total too, but `/api/admin/installments` exports only GET and the page is read-only. Deliberate staging — the collection path is the next slice — but it becomes load-bearing the moment Jane pays on 5 September. Fix: a POST route + a button, and wire it to `pay-balance`/`charge-balance` so a real Valor charge records itself. Batched with 444. | — |
| **447** | **The 13 overwritten legacy_rebook drafts lost their original `inputs`/`result`** — the migration's `priorTool` snapshot captured total, deposit, status and service type, not the quote content. | XS | Found by the S56 PR #1049 staff lens. Recorded rather than actionable: the lost content was engine-generated placeholder pricing from the Jobber rebook wave (row 155), not staff work, and the Homeworks figures that replaced it are the real ones. No recovery exists. The lesson is the transferable part — a `prior` snapshot taken before an overwrite must capture what is being overwritten, not a summary of it. | — |
| **448** | **The monthly installment runner** — charge a vaulted card on the due date for the three customers on payment plans, and record it. | M | The remaining build from the S56 Homeworks migration. The primitives already exist and are proven on 18 real quotes (`chargeBalanceOnFile`, the Valor vault, `installments` + `markInstallmentPaid`); what is missing is the runner and its cron. MUST be built dry-run-first with no cron armed until Jason says so — it moves real money. Deadline pressure: Jane's next payment is due 5 September and Raymond's 28 September, and neither has a card on file yet, so the pay links (Jason's own action) come first and vault the card as a side effect. Jason 2026-08-28: build this FIRST next session, lens it, then batch 444-447/449. | — |
| **449** | **`isOverdue` compares against the UTC calendar day, not the ET business day**, so a payment due tomorrow reads Overdue for the last few hours of each evening. | XS | Found by the S56 PR #1049 technical lens, verified live with node. `src/lib/installments.ts` does `asOf.toISOString().slice(0,10)`. Cosmetic today — nothing auto-charges off it — but row 448's runner would inherit the same clock, and row 335 is the same UTC-vs-ET class on `/admin/schedule`. Fix with the same ET helper the payroll and follow-up code already use (`etMidnightAfter` / `etDayKey`). Batched with 444. | — |
| 454 | **Activate the truck-and-trailer's Bouncie tracker and register the vehicle** | S | Second device is paid for but not activated (Naldo's call 2026-08-28: van only for now). Steps when ready: activate the device in the Bouncie account, plug it into the truck's OBD-II port, drive far enough to complete a trip, read the IMEI from `vehicle_events`, then `insert into vehicles (label, imei, vin)` per the runbook (`docs/reference/bouncie-setup.md` step 7). One device per vehicle, never shared (the subscription and history belong to the DEVICE). The poller, map, timers and visit timeline pick it up with zero code changes. | — |
| 455 | **The Schedule page's crew dropdown renders a query failure as "no active crew" — the silent-empty class the geocoding page was just cured of** | S | `src/app/admin/schedule/page.tsx:15` calls `listActiveFieldCrew().catch(() => [])`, and `listActiveFieldCrew` (`src/lib/crewMembers.ts:216-229`) already swallows query errors into `[]` internally (tested as intentional, `crewMembers.test.ts:433-436`). A broken query therefore renders the assign-crew dropdown empty with no indication anything failed, on the page that gates all scheduling — the exact failure shape PR #1036 fixed on /admin/geocoding (error path returned `[]`, page showed the all-clear over 25 real rows). Fix shape: let the page tell a load failure apart from a genuinely empty crew list, as the geocoding page now does. Found by the #1036 admin lens, 2026-08-28. | — |
| 456 | **Work the 25-address geocode backlog: 25 properties cannot be scheduled and were invisible until the fix-list came back to life** | M | /admin/geocoding (repaired in #1036) lists 25 active properties with no verified coordinate; none can be scheduled (`assignCrewToJob` refuses) and none can ever produce a GPS timeline. The list is a mix: obvious test rows (two "123 Main St", "1 Test Lane"), ~9 blank or "(no address)" rows, out-of-area ("7302 E Ave U 3, Littlerock, CA"), and a handful of REAL fixables (wrong-zip "65 Forest Road, Kings Park, NY, 11701", "1 Horse Shoe Path, Lloyd Harbor" which is job #1045 sitting in to_schedule, "2 Seaforth lane Huntington", "3 Kielly Ct, Salisbury Mills"). Staff task through the page (fix one and it re-checks automatically), plus a decision on archiving the junk/test properties so the list reads true. NOT import-only: any new property whose address fails rooftop verification joins this list. | — |
| 459 | **Paid-day edit guard on manual shifts: arm it when the Staff payroll build lands a paid marker** | S | PR #1062 (manual shift entry/edit, admins only) ships WITHOUT a refuse-edits-to-paid-days guard, on record and deliberate: the tool has no paid/approved marker on shifts (payroll approval still happens in Copilot). The moment the Staff payroll build adds one, adminCreateShift/adminUpdateShiftTimes in src/lib/shifts.ts must refuse writes touching a paid day. Until then the protections are the manual_by stamp, the dashboard_activity before/after audit rows, and the crew Telegram note. | — |
| 458 | **No void/delete for a wrongly created manual shift** | S | PR #1062's admin lens: an admin can create a manual shift but only edit its times afterwards — a fully bogus row persists forever (shrinking it to a minute pollutes payroll instead of removing it). Needs a void marker or guarded delete with the same audit posture as the editor. MED, deferred: the audit trail makes a bogus row visible and attributable in the interim. | — |
| 457 | **S74 review residuals: decisions and papercuts from the fleet rounds, recorded not fixed** | S | (a) DECISION on record: non-admin office staff have NO GPS-history surface after the #1046 split — "when did the van arrive at job X yesterday" now requires Naldo or Jason (staff lens MED, accepted by Naldo's access ruling; revisit only if the office actually hits the bottleneck). (b) Old `/admin/fleet?date=...` bookmarks silently show today (the param moved to /admin/fleet/clocks; LOW, feature was hours old). (c) The "van is not the person" caveat now lives only on the admin clocks page while the all-office live page shows at-place timers uncaveated (LOW). (d) `listFleetDays` swallows query errors silently — navigation sugar only, but same silent-[] family as row 455. (e) The non-admin redirect on /admin/fleet/clocks is proven by unit tests and code trace, not by a live non-admin click; first office login to try it is the live test (worst case: they see the live map they are allowed to see). | — |
| 442 | **Customer quote prices are captured in PostHog autocapture element text, outside every masking guard the code sets (MEASURED, pre-existing, NALDO decision)** | M | PostHog `$autocapture` records the clicked element's own text as `$el_text`. That is a DIFFERENT code path from session-replay masking, so `session_recording.maskAllInputs` and `maskTextSelector: '*'` in `src/lib/analytics/posthog.ts` do not touch it, and the comment there about never wanting "an unmasked customer name/address/price on tape" is true of replay and NOT true of autocapture. **Measured 2026-08-28 on prod: 1004 events in 180 days carry a `$` in `$el_text`.** Sampled values are real quote figures, e.g. "Gingerbread $1,080 Off", "Santa's Roofline $440 Off", "16\" Spritzer $85 Off", "$95", plus package tier names and descriptions. No name, email, phone or address appeared in the sample, but the prices attach to a `distinct_id` and a session, so what a given household was quoted is derivable. **This PREDATES this session**: autocapture has been on for months. S73 enabling dead clicks added 7 more events of the same class (`$dead_click` total: 7), and `$rageclick` holds 10, so the session widened a pre-existing gap rather than creating one. Deliberately NOT changed unilaterally: turning autocapture off would break the rage-click and heatmap analysis the whole review pipeline was just built on, and this is customer data, which is Naldo's call. Fix shapes to weigh: mask autocapture text via the posthog-js init options, add a `ph-no-capture` class to the price-bearing elements in the portal and quote UI, or accept it with the exposure written down. Owner: the price elements are Jason's area. Found by the S73 close customer lens, which reasoned it out from the code before the number existed. | — |
| 443 | **"Clear all" wipes every traced satellite line for a roofline with no confirm and no undo, and the next derive zeroes the billed footage on screen** | S | `src/components/quote/QuoteBuilder.tsx` around `clearAllLines` (~:2842) and its two buttons (~:7330, ~:7360), with `src/lib/holidayFootage/reconcileFootage.ts:75-77` doing the zeroing. One unconfirmed click destroys the trace, and the derive pass then overwrites the billed footage shown to staff, including a manually typed override. Thirteen or more other destructive actions in the same file DO confirm first, so this is a sibling-parity gap rather than a deliberate choice. **Not this session's code**: it arrived in PR #916 (roofline-segmentation), merged by a concurrent session during the S73 window, and was itself shipped as the fix for an earlier staff-lens finding. Found by the S73 close staff lens, which read it in code but could not click it live (the dev server needed an operator login it did not have), so **the behaviour is traced, not reproduced** - verify before fixing. Owner: quote builder is Jason's area. | — |
| 440 | **The marketing site's sticky header throws a JavaScript error on live visitors (found the hour error tracking was switched on)** | S | `sticky_head_func` on yulelovelights.com raises `TypeError: Cannot read properties of null (reading 'classList')`, **36 occurrences in a single visitor session**, first seen within an hour of PostHog exception autocapture being enabled on 2026-08-28. It predates this session by an unknown margin: nothing was capturing exceptions before, so the 36 is a floor, not a measurement of how long it has been happening. The script reads an element that is null at the time it runs, which usually means it fires before that element exists or on a page where it never exists. WEBSITE, so report-only for the daily robot and NOT fixable by it (WordPress has no PR gate and no rollback). Needs a human in the Elementor or theme code. What is unknown and worth measuring first: whether a visitor loses anything real (a stuck header, a dead nav) or whether it throws harmlessly after the page is usable. Check the PostHog issue for the affected pages before touching code. | — |
| 441 | **Fix the lead form once the new per-field events say WHERE people quit** | M | Measured 2026-08-28 over 90 days: roughly 886 form views, 165 starts, **23 submits**. View-to-start at 19% is ordinary; start-to-submit at **14%** is not. About 142 people began typing and abandoned, and until now nothing recorded which field they were on when they stopped. PR #1039 shipped `yll_lead_form_field_completed` (field NAME only, never the typed value, once per field per render) precisely to answer that, so this row is deliberately BLOCKED ON DATA rather than on a decision: give it a week of traffic, then read the per-field funnel plus the new heatmaps and dead-click capture before changing anything. Resist redesigning the form first; the whole point of the instrumentation was to stop guessing. Owner: the form is Jason's area (`public/lead-form.js`). | — |
| 439 | **S71 wrap-review deferrals: fleet/geocoding polish + the estimate-route geocode call** | S | The S71 whole-session lens round (4 lenses over the merged GPS stack) came back with no HIGHs on code; these are its dispositioned MEDIUMs/LOWs, none blocking. (a) CUSTOMER MED: `findOrCreateProperty`'s write-site geocode adds an UN-DEFERRED second Google call inside the self-serve `/api/estimate` request path, which already runs tight against its timeout budget — worst case a computed price is lost to a slow geocode. Fix shape: skip the write-site geocode when the caller already passed a gated `geo`, or defer it out of the request path. (b) CUSTOMER MED: a geocode-blocked job (~15 of ~215 properties refuse) is invisible until someone ATTEMPTS to schedule it — no badge on the jobs list, so a paying customer's install can sit unflagged in the ordinary backlog. Fix shape: surface 'address needs fixing' on the job row, not just at the schedule refusal. (c) STAFF MED: /admin/geocoding has no back-to-schedule link after a fix (browser Back works; add the link). (d) STAFF MED: /admin/fleet's open-visit text is not day-scoped — a stale unclosed visit viewed on a PAST day reads as current ('still there'); scope the wording to the viewed day. (e) ADMIN MED: the runbook's READ THIS FIRST block sits at line ~502 of `docs/reference/bouncie-setup.md`; move it to the TOP (backstopped today only by SUPERSEDED banners inside the two dead migration files). (f) LOWs: stale 'Settings -> Accounts' comment atop `BouncieConnectNotice.tsx`; two pre-existing private `addDays` copies (`botTools.ts`, `opsDigest.ts`) now that `opsMidnightClose.addDays` is exported. | - |
| 438 | [✅ SHIPPED S69 2026-08-29 — PR #1045 merged. NALDO RULED: crew logins are no longer a product concept and the Operations Hub is scrapped. Measured prod FIRST: zero crew accounts existed, and all five crew_members rows carrying a login link pointed at operator/admin accounts, so nothing was stranded. REMOVED the minting path, the email/password form, crewAuth.ts and isCrewPath. DELIBERATELY KEPT every guard keyed on isCrewAccount, and made the proxy refusal UNCONDITIONAL rather than deleting it: roleOf collapses non-admin roles to operator, so removing it would silently promote any crew account created later by hand. Field crew unaffected, they clock in via the Telegram bot. Five review passes (4 lenses + delta-verify); the admin lens MED was real and fixed - the amber "No login yet" badge would have invited an owner to re-grant dashboard access.] ~~The admin Staff panel still mints CREW-role logins that can now reach nothing at all, and its own comment states a boundary that no longer exists (S69 post-close integration lens, MED)** | XS | PR #1027 (row 433, same session) deleted the whole `/api/ops/v1/**` namespace. `isCrewPath` (`src/lib/auth/operatorGate.ts:139`) matches ONLY that namespace, and `src/proxy.ts:48` refuses a crew account on every path it does not match, so a minted crew login is now locked out of the entire app. **This fails CLOSED, so it is not a security hole** and the real crew clock-in path is unaffected (that goes through Telegram via `telegram_user_id`, never through this login). What IS wrong: (a) `src/app/api/admin/staff/route.ts` still actively mints these logins and its comment claims the crew role is "confining it to the crew API", which is now a false statement about a live admin feature; (b) `src/lib/auth/crewAuth.ts` (`requireCrew` / `getCrewCaller`) has exactly ONE importer left in the tree, its own test file; (c) `isCrewPath` and its 14 assertions in `operatorGate.test.ts` describe a route namespace that is no longer on disk. Neither PR could see this: #1027 deleted the consumer, the staff route was untouched. Decide whether crew logins are still a product concept at all before deleting anything, since that is a Naldo call and not a cleanup. Sibling of row 437, which is the other half of the same dead-crew-code sweep. | — |~~
| 437 | **Dead `updateCrewMember` and a stale "only reader" comment, both left by the S70 staff-panel consolidation (close integration lens, 2 LOW)** | XS | Two code-level leftovers, neither a behaviour bug, both in `src/lib/crewMembers.ts`. **(1) `updateCrewMember` (~line 261) now has ZERO callers.** Its only live caller was `PATCH /api/admin/crew-accounts`, which PR #913 deleted when it consolidated the two staff panels into `/api/admin/staff`; the new door uses the `patchStaffRow` helpers instead. It sits alongside two other already-dead helpers in the same file, so the cleanup is worth doing as one pass rather than piecemeal. Check for callers again before deleting: this is exactly the kind of function a future PR reaches for. **(2) `setStaffType`'s doc comment (~line 532) and a `crewMembers.test.ts` comment (~line 842) both claim `listActiveFieldCrew` is the flag's "only functional reader".** That was true when #913 was written and false by the time it merged: PR #983 added a second reader, `assignCrewToJob` (`src/lib/scheduling.ts:156`), which refuses an office id server-side. The second reader is a correct backstop, so nothing is broken; the comment is just a stale claim that could mislead someone editing the flag later. The same stale phrasing reached the S70 journal, CLAUDE.md and the archived row 363, and was corrected there in the S70 close; only the two CODE comments remain, because a code edit cannot ride the documents-only close PR. | — |
| 435 | **The self-serve estimator has written ZERO telemetry rows since it launched, and a silent insert failure is indistinguishable from having no customers (S68 verification, prod-measured twice)** | S | Measured in prod 2026-08-25 and again 2026-08-27: `self_serve_estimates` has **0 rows**, while `quotes` went 191 -> 207 in the same window. `/estimate` is live, returns 200 and is `robots: index, follow`. So the dashboard self-serve accuracy tile has nothing to score, and row 815's read-time clamp guards an era that produced no rows at all. `recordSelfServeEstimate` (`src/lib/selfServe/telemetry.ts`) is best-effort BY DESIGN, so a telemetry failure can never break a customer's estimate, which is right, but it only `console.warn`s: **nothing distinguishes "nobody finished an estimate" from "every insert has failed since telemetry landed 2026-07-19"**. Cheapest first step is a Vercel log grep for `[selfServe] recordSelfServeEstimate failed`. If the inserts are fine, the answer is that the funnel has no completions, which is a bigger product finding than the tile. Either way the tile should say WHICH of the two it is instead of rendering an empty state that reads as "no data yet". **ANSWERED 2026-08-27, and it is not a broken insert: there is no traffic.** Production runtime logs for the last 24 hours (Vercel retention refused a wider window on this plan) show **exactly one** `GET /estimate`, at 20:57:39, which was this session's own curl. Zero `/api/estimate` calls, so `recordSelfServeEstimate` never runs. `/estimate` does not appear in the top 25 request paths out of 212 distinct paths. And the marketing site does not point at it: `yulelovelights.com` and `/get-a-quote/` both return 200 with ZERO references to `quote.yulelovelights.com/estimate`, and `/instant-quote/` and `/estimate/` both 404. **So the estimator is built, live, indexable and unreachable.** The work is not a telemetry fix, it is a launch: link or embed it from the marketing site, then re-check this row. The tile should still distinguish "no data yet" from "telemetry failing", but that is now the smaller half. | - |
| 436 | ✅ **SHIPPED S68 — `/estimate` was deliberately indexable but emitted no canonical URL** | XS | `src/app/estimate/page.tsx` set `robots: index, follow` for the standalone page and `noindex` only for `?embed=1`, but exported no `alternates.canonical`. The embed variant being noindex covered the obvious duplicate, but the whole point of PR #624 was to make `/estimate` a rankable landing surface, and any query-string entry (campaign tags, referral params) would have indexed as a separate thin URL. **Fixed:** `alternates: { canonical: '<base>/estimate' }` for every variant, base from `PORTAL_BASE_URL` with the prod host as the literal fallback, matching the idiom the payment routes use. Four tests in `src/app/estimate/page.test.ts`, mutation-probed (removing the line fails 3 of the 4). | - |
| 422 | **Staff driving the customer portal generates CUSTOMER-engagement analytics events with no way to filter them out (PR #991 admin lens MED, deferred)** | S | Row 324 makes "staff open the customer portal and set the opening selection" a ROUTINE, designed workflow. The client trackers - `QuoteViewTracker.tsx`, `InteractiveHero.tsx`, `LightColorPicker.tsx`, `PermanentEffectPicker.tsx` - call `track()` with no staff gate, so a staffer configuring an unsent draft mints real `portal_opened` / `package_selected` / `color_changed` events indistinguishable from customer engagement. PRE-EXISTING (a staff PREVIEW already pollutes today) but row 324 AMPLIFIES it from occasional to systematic, which is why it is recorded rather than ignored. Deliberately NOT bundled into #991: the fix touches the portal page plus four tracker components and has its own analytics blast radius, so it earns its own diff and its own review. FIX SHAPE (traced 2026-08-26): every event funnels through the single `track()` in `src/lib/analytics/posthog.ts`, so ONE gate can cover all of them - but the staff signal is NOT client-readable (`STAFF_DEVICE_COOKIE` is httpOnly, `staffDevice.ts:20`), so the portal Server Component must compute the staff view (cookie OR `getOperator()`, mirroring `isStaffPreview`) and thread it to the trackers, e.g. via the existing provider. NOTE the cookie half matters most: while the auth gate is dormant staff never log in, so an `getOperator()`-only check would miss the common case. | - |
| 421 | 🔴 **NALDO ITEM (his bot area + a product ruling)** - crew Telegram bot's clock-in / arrive path has no is_office gate, so an office staffer's texts can attribute labor time to a field job | S | Found by the #983 admin lens (S54, 2026-08-26): shifts.ts clockIn and jobSegments.ts arriveAtJob insert for ANY crew_member_id, and crewTimeHandler.ts checks only crew.active - never isOffice - before dispatching clock_in/arrive/depart/complete. Since every staff member texts the bot (Naldo ruling 2026-08-24), an office person's "clock in" then "arrive #NNN" writes job_segments rows that per-job labor reports and pay derive from, with no refusal and no signal. DORMANT today, measured in prod 2026-08-26: job_segments 0 rows ever written, office bot shifts 0, job_assignments 0. Questions for Naldo before building: (a) may office staff ever record a job arrival? If no, mirror #983's AssignmentRefusedError at the bot dispatcher; (b) should a bot clock-in from office staff redirect them to the office web clock (crewMembers.ts says that is their intended punch path)? #983 closed the SAME class at the job-assignment write; this is the sibling door (the promoted enumerate-all-consumers pitfall). | — |
| 403 | **Fleet GPS: stream the truck + van position into the quote tool (Bouncie) — DECIDED 2026-08-25, Naldo** | L | Naldo's ask: the office needs to see where the crew is, so it can keep customers informed while jobs are in progress. **Vendor decision: Bouncie**, two devices — the truck (trailer never leaves it, so one dot covers both) and the van. ~$9.65/device/mo, no contract, OBD-II plug-in. Chosen over Zubie (~3x, gated API key), Samsara (multi-year contract, built for 50-truck fleets) and self-hosted Traccar (we would own a Java server + SIMs for two vehicles) on ONE axis: Bouncie is the only one at our size with a genuinely SELF-SERVE developer API — sign up, issue your own key, register your own webhook, no sales call. REST pull + webhook push. **VENDOR FACTS NOW CONFIRMED against the real spec (S68, 2026-08-25), read from `https://docs.bouncie.dev/openapi.json` — the docs site is a Stoplight single-page app that serves an empty shell to a fetch, and the block was the web container's egress proxy, not the vendor. A local session reaches it fine. The spec as read is committed at `docs/reference/bouncie-openapi.json`, distilled at `docs/reference/bouncie-api.md`.** TWO of this row's search-sourced facts were WRONG: (1) auth is **OAuth 2.0 authorization code ONLY — there is no API key.** Per-user tokens from `https://auth.bouncie.com/oauth/token`; refresh tokens ROTATE on every use and expire if unused, so tokens need durable storage, not an env var; and REST calls send `Authorization: <access_token>` with **NO `Bearer` prefix** (the vendor's own FAQ names that prefix as a top cause of 401). (2) **`tripStart` and `tripEnd` carry NO location** — only timestamp/timeZone/odometer. Position comes from `tripData` (batches of `{timestamp,speed,gps{lat,lon,heading}}`, continuous through a trip and the documented bulk of webhook volume), from a geozone event, or from polling `GET /v1/vehicles`, whose `stats.location{lat,lon,heading,address}` IS a current position but is **optional in the schema** — an absent location is the constraint-(c) "no signal" case and must never render as "not at the job". Also confirmed: **no rate limits are stated anywhere** in the spec (the only cap is 100 webhooks per application); duplicate events are expected BY DESIGN (a real-time and a periodic stream overlap, and an offline device buffers then dumps on reconnect), so deduping on `transactionId` is mandatory, not optional; and **a webhook that keeps failing is silently auto-deactivated**, which needs a staleness alarm on our side. Note `lon` vs our `lng` — convert at exactly one seam. **BONUS that shortens phase 4: Bouncie runs server-side Application Geo-Zones** (`POST /v1/locations` → optional `POST /v1/schedules` → `POST /v1/application-geozones`) which are invisible in the vehicle owner's own app, never notify them, and fire an `applicationGeozone` webhook carrying `event: ENTER|EXIT` plus the event location. We may not need to compute geofences ourselves at all. GROUNDWORK MEASURED against prod 2026-08-25: `jobs.property_id` -> `properties`, which already HAS `lat`/`lng` columns and `geocodeAddress()` is already live for the analyzer — but **0 of 211 property rows are populated**, so the backfill is phase 1 and everything else depends on it. `arrive`/`depart`/`complete` already exist at `/api/ops/v1/jobs/[id]/*` with payroll logic behind them — but **0 job_segments have ever been written in prod** (6 shifts), so this is the FIRST real use of the crew-tracking side, not an addition to a working one. 8 active crew, 4 of them field. PHASES: (1) geocode-backfill the 211 property rows [prod data write — needs Naldo's explicitly-named go per the migration/prod-write rule]; (2) webhook receiver + `vehicles` table + vehicle->crew mapping; (3) live map on the dashboard (THE ask); (4) geofenced arrive/depart SUGGESTIONS; (5) optional customer 'on the way' / 'done for today' via the existing inbox. **BUILD-TIME CONSTRAINTS, decided before any code exists — do not relax these in the build:** (a) **GPS NEVER writes payroll.** A geofence may only SUGGEST an arrive/depart to the crew's own device; a human still taps. `/api/ops/v1/jobs/[id]/arrive` stays crew-authenticated and the crew member id keeps coming from the session, never from a GPS payload. A dropped signal or a van parked next door would otherwise become wrong hours in someone's pay. This is why the map (3) ships before the suggestions (4). (b) **The webhook path goes into `PUBLIC_API_EXACT` in `src/lib/auth/operatorGate.ts` in the SAME PR**, method-scoped, with a shared-secret header check inside the route (mirror the Telegram webhook's `x-telegram-bot-api-secret-token` 401), and it is verified on a SIGNED-OUT request — the standing AGENTS.md gate pitfall, which has already produced four prod incidents. **CONFIRMED VIABLE S68 against the real spec: this assumption holds and needs no redesign.** Bouncie authenticates its webhooks with a static shared secret WE choose (`authKey`, set at `POST /v1/webhooks` registration) and echoes it back verbatim on every delivery in BOTH `Authorization` and `X-Bouncie-Authorization` (the second exists for platforms that strip the first — read both). Same shape as Telegram's. What it is NOT: an HMAC over the body. It proves only that the caller knows the secret, with no payload binding and no replay protection — so pair it with HTTPS-only, a CONSTANT-TIME compare, and idempotency on `transactionId`. **FOOTGUN, do not trip it: Bouncie rotates the key when our endpoint returns a new value in an `Authorization` RESPONSE header.** If the receiver ever sets that response header for any reason, Bouncie silently adopts the new value and every later delivery fails our check. The route must never set an `Authorization` response header. (c) **One device per vehicle, never shared.** The Bouncie subscription and trip history belong to the DEVICE, not the vehicle it is plugged into, so moving the truck's tracker into the van files the van's miles under the truck. Naldo confirmed the devices will be unplugged occasionally; an absent device must render as 'no signal', never as 'not at the job' — which is a hard prerequisite for phase 4. (e) **Any future caller that passes `geo` to `findOrCreateProperty` MUST apply the same precision gate the backfill script uses (ROOFTOP/RANGE_INTERPOLATED + street_number&route + not partial_match), or it will silently degrade a geofence anchor.** Found by the S67 self-review of the backfill script, and it is a live trap rather than a theoretical one. `findOrCreateProperty`'s third parameter is NEWEST-WINS: `const nextLat = geo?.lat ?? row.lat`. Today that is harmless because a grep of the whole tree shows NO production caller passes `geo` at all - only `customers.test.ts` does - which is precisely why 0 of 211 rows are populated and why the backfill is the sole writer of these columns. But the moment someone wires a geocode through (the estimate route already has one in hand), an UNGATED town-centroid result would overwrite a rooftop-verified coordinate with no warning and no log, and the only visible symptom would be crews being prompted to clock onto jobs they are nowhere near. Same class as the repo's positive-seam-gate pitfall: the guard must live at the WRITE, not only in the one script that happens to do it correctly today. (d) **Vehicle->crew is a STATIC assignment, not a daily screen.** Naldo confirmed the same crew rides the same vehicle daily, so this is a setting somebody edits when a crew actually changes. A truck carries a CREW (4 field crew across 2 vehicles), so the map label holds multiple names, not one. **(f) OFF-HOURS LOCATION IS NOT OURS TO SEE — enforce it in code, not by promise (S68 staff lens, HIGH).** The truck goes home with someone, so a live map with no time boundary shows an operator a crew member's home address and their evenings. Row 403 previously left this as a verbal "tell them what it's for". It must be structural: the map and any stored position are limited to working hours, and the cleanest lever is the vendor's own — an Application Geo-Zone takes an OPTIONAL `POST /v1/schedules` active window, so the zone simply does not fire outside it. Whatever the mechanism, "can an operator see where a crew member sleeps" must be answerable by reading code, not by trusting a policy. **(g) `closeOpenSegmentForShift` IS THE SEAM WHERE CONSTRAINT (a) WILL GET BROKEN (S68 staff lens, HIGH).** `src/lib/jobSegments.ts` already contains an unauthenticated writer to `job_segments` with `source:'system'` and no crew session, used by the midnight-close cron. It is the obvious, precedent-backed place a future geofence "suggestion" gets wired in to auto-close job time without a human tap — which would satisfy the letter of (a) while breaking exactly what (a) exists to prevent. No GPS-derived event may reach that function or any sibling that writes a segment without a session. Related tightening of (a) itself: "a human still taps" means an AFFIRMATIVE tap. No default-accept, no auto-confirm-on-timeout, no "dismiss to reject" — a crew member who never looks at their phone must end the day with nothing written. OPEN, none blocking: the devices are not bought yet, so phases 2-5 cannot be verified against a real feed. Phase 1 is vendor-independent and can start now. Crew notification is a Naldo action, not a code step: tracking company vehicles in work hours is legal in NY, but an OBD-II plug sits in plain sight and gets found in week one — say what it is for and whether it runs after hours, in writing, before the devices go in. **S71 FINAL-STATE DELTA (2026-08-28) — this row's plan text above is HISTORY; the shipped design differs in three ways it decided.** (1) **No Bouncie geofences.** Naldo's call: customer coordinates never leave the quote tool. The geofence subsystem was built (#1010), then deleted; proximity is computed IN-HOUSE by a 2-minute poller (`src/lib/integrations/vehicleProximity.ts`, cron `/api/ops/vehicle-poll`) against the day's scheduled jobs. The schedule is the watch list. (2) **Constraint (f) is REVERSED, deliberately.** #994 (Naldo 2026-08-27: 'keep all data, we are always open') sets BUSINESS_HOURS to the full day, so nothing is excluded, redacted or expired; the round-the-clock LIVE display was separately put to Naldo and approved (#1028). The (f) text above records the original intention, this delta records the decision that switched it off. (3) **Shipped through phase 3:** backfill RUN (190 coordinates live), webhook receiver live, OAuth grant stored encrypted, visit timeline (the second clock, structurally payroll-isolated), /admin/fleet, /admin/geocoding, Settings->Bouncie health, geocode-at-write with schedule refusal. Remaining phases (ETA texts, duration model) are designed in `docs/reference/scheduling-system-design.md` and NOT built — they wait on the schedule gaining day-ordering. Runbook: `docs/reference/bouncie-setup.md` (read its READ THIS FIRST). | - |
| 415 | **The PII-retention purge cron does not exist in the codebase, despite the ledger recording it as shipped** | M | Found by the S68 customer lens during the row-403 review and independently verified. **RENUMBERED 404 -> 415 on 2026-08-26:** Jason's row 404 (the stale-invoice marker clear, PR #959) merged and archived while this branch was open, and a merged claim wins. Re-checked 415 free against master, the archive, and all 16 open PRs before minting. The finding: `task_ledger_archive.md` records PR #229 (S12-S15 security hardening) as having shipped a "PII-retention purge cron", and the S15 sign-off treats it as done. **It is not in the tree.** There is no `maintenance/retention` route (or any other purge route) anywhere under `src/app/api`, no purge/retention module under `src/lib` (the only `purge`/`retention` grep hits repo-wide are `designs.ts`, `quotes.ts` and their tests, which are unrelated), and `vercel.json`'s `crons` array lists 11 entries, none of them a purge. So customer PII has been retained indefinitely for however long the ledger has claimed otherwise. **Why it surfaced now:** the fleet-GPS backfill (row 403 phase 1) adds a rooftop-precision coordinate for ~190 real homes to `properties`, which is a materially more sensitive artifact than the street-address string already sitting there, in a table nothing purges. That does not block the backfill — the address is already stored, and the coordinate is derived from it — but it means the privacy footprint grows while the control that was supposed to bound it is missing. **TO DO:** (1) establish what actually happened to #229 (reverted? never merged? merged then lost in a later refactor?) before rebuilding anything, since the archive's claim being wrong is itself the finding; (2) decide the retention rule WITH Naldo, including whether `properties.lat/lng` should be nulled on a schedule separate from the row itself; (3) whatever ships, verify it by reading `vercel.json` and hitting the route, not by trusting a PR description — that is precisely the failure mode this row documents. | - |
| 431 | **The visit timeline's remaining operability gaps (partially SHIPPED S68)** | S | UPDATED S68 after the polling redesign and the fleet page. SHIPPED: (b) silent skips became structural — the poller surfaces every read failure in its outcome and logs an error-level summary, and the schedule gate means a scheduled job can no longer silently lack coordinates; the fleet page banners any load error and renders no-signal as "position unknown, not parked". Naldo's rule (no coordinates, no schedule) closed the sharpest version of "a missing timeline reads as a no-show". STILL OPEN: (a) **nothing closes a stale open visit** — a device unplugged mid-visit leaves `exited_at` NULL and the fleet page says "still there" indefinitely; needs a backstop (close-and-flag after a maximum plausible visit, or at day end) that marks the close as SYNTHETIC rather than observed. (c) **crew cannot see their own timeline** — Naldo decided office-only for the compare view (2026-08-27); the crew-notification obligation before the record is used in any hours conversation stands, in the runbook. (d) out-of-order repair no longer applies (polling is inherently ordered). | - |
| 432 | **Proximity radius + dwell threshold: tune from the stored evidence (design updated S68)** | S | UPDATED S68: the geofence design this row described is gone — proximity is computed in-house (`vehicleProximity.ts`), so the radius (`PROXIMITY_RADIUS_METRES = 120`) is a ONE-LINE tune with retroactive context: every visit stores its entry position (`entered_lat/lng`), so "how far from the anchor do real arrivals sit" is a query, not a re-collection. The dwell threshold is Naldo's 15 minutes (`MIN_DWELL_MINUTES`), applied at close and STORED as `below_min_dwell`, never used to discard — the flagged short visits are the drive-by data that tunes the radius. **The overlap rule is DECIDED (Naldo 2026-08-27): the scheduler's day-order picks which job when circles overlap.** Day-ordering is not built yet, so until it exists the nearest anchor wins (`resolvePlace`, one function, commented as the swap point). TO DO once real visits exist: check flagged-visit rate and entry-distance distribution, adjust the two constants, and wire sequence as the tie-break when scheduling ordering ships. | - |
| 384 | **Every revenue/pipeline aggregate reads live `quotes.total` while the portal now reads the FROZEN approved basis (S48 #929 admin lens HIGH, argued-and-deferred; NALDO'S AREA)** | M | `src/lib/dashboard/metrics.ts:73` (homepage Booked Revenue KPI), `workflowBoard.ts:110`, `insights.ts:41,54,91-92`, `serviceMetrics.ts:101,119,139`, `referralMetrics.ts:124`, `botTools.ts:31` all read raw `q.total` gated on `customer_approved_at && !isTerminalStatus`. #929 froze the portal to the approved figure, so the owner's dashboards can disagree with what customers actually approved. Deliberately NOT fixed in #929, with evidence: `amend/route.ts` writes `approval_snapshot.amendments` only and never touches `quotes.total`, so every booked-and-amended quote ALREADY produced identical drift long before #929 - one consistent pre-existing pattern, not an oversight. A real fix means adopting `resolveAgreedTotal` dashboard-wide across 7 files in Naldo's owned area: needs his go, not a unilateral edit to his revenue KPIs. | - |
| 423 | **Design changes on a BOOKED order leave no audit trail** | M | Row 367 deliberately keeps a booked order's design editable - that is the sanctioned amend path - but nothing records who changed the drawing or when, so the owner cannot answer 'who changed this on a job the customer already paid for'. The sibling row-370 code ~30 lines away in the same route DOES audit portal-visibility changes onto approval_snapshot. Measured 2026-08-27: 12 non-test approved quotes have a design write after customer_approved_at, 11 of them booked. Jason approved building this 2026-08-27 and left the shape to me. SHAPE: coarse on purpose - one entry per operator per design per calendar day via the hardened appendQuoteAuditEntry, NOT one per autosave (a 600ms debounce would write an entry per edit and drown the signal). Cheap because readSceneLock already reads the quote row on every scene write. | - |
| 424 | **The satellite trace is NOT frozen post-approval - RULED, do not reopen** | L | The portal renders design.satellite_lines (SatelliteRoofView, WhatsIncluded), and row 367 deliberately does not freeze that write path. The #996 customer lens raised it as a MED. **Jason ruled 2026-08-27: leave it open** - gating it inside Calculate's persistence path would break an ordinary re-Calculate on an approved quote, which re-persists identical lines. Recorded so the next reader does not re-litigate it; the #996 route test pins the current scope so widening it has to be a decision, not a drift. If it is ever revisited, the shape that satisfies Jason's objection is change-DETECTION (refuse only a write that actually differs), not a blanket gate. | - |
| 425 | **A stale HighLevel contact can still mis-link a quote BEFORE approval - live instance found and corrected** | M | Row 251's identity freeze covers POST-approval only, by design. Re-running that row's own detection sweep on 2026-08-27 found a live pre-approval instance: quote **#1304** ($2,985.19, draft) carried patty argento d'aquila's name/email/phone/address while its customer_id, highlevel_contact_id AND property_id all pointed at michael vahling - his ids, from his booked quote #1268. Patty has her own customer row and property; her other quotes #1273/#1277 carry her real hl id. Invisible on every screen, exactly as row 251 predicted. Corrected in prod under Jason's named authorization 2026-08-27 (all three columns, old values pinned in the WHERE clause); the sweep now returns 0 mismatches across 203 linked quotes. OPEN QUESTION, and the actual work: HOW did a stale hl id ride into a NEW quote for a different customer - a prefill that survived, a mis-pick, or a session that kept the previous quote's contact? Reproduce before building. | - |
| 426 | **Make the quote-vs-customer name mismatch sweep a standing guard** | M | Row 251 suggested it; it has now paid twice - it found the original Sharon/Richard incident and, on 2026-08-27, the unrelated live #1304 mis-link (row 425). The query is one join and cheap: quotes joined to customers where the denormalized customer_name disagrees with the linked customer's name, non-test, customer_id not null. Current population 203 linked quotes, 0 mismatches after the #1304 fix, so it starts clean. Put it in the morning digest as a line that stays silent at zero, or as a dashboard check. This class is invisible on every screen by construction, so a periodic sweep is the ONLY thing that surfaces it. | - |
| 427 | **The design-change audit trail covers the scene and the satellite trace, but NOT photo or satellite-image writes on a booked order** | M | Row 423 shipped the trail (one entry per operator per design per ET business day, via appendQuoteAuditEntry) and it is wired at exactly two call sites - updateDesignSceneGuarded and updateDesignSatelliteLines. A BOOKED order stays editable by design (the amend path), so base-photo replace, extra-photo add/remove/rename and satellite-image replace all still land on a signed-off quote with NO record of who or when. Measured 2026-08-27 by the #1002 admin lens: **23 booked+approved designs live, ALL 23 with a base photo, 11 with a satellite image, 3 with extra photos** - a real population, not hypothetical. Deferred deliberately rather than bolted onto a day-old feature at the end of a long session. SHAPE: those routes do not share a writer the way the scene does, so either call a small recordDesignChange-if-auditable helper in each of the 4 routes, or have refuseIfFrozen hand its lock back so the caller can audit off the read it already did. | - |
| 428 | **The crew Telegram bot writes designs.extra_photos with no freeze check at all** | L | `src/lib/integrations/botWriteTools.ts:267` (completeInstall) writes the column directly; grep-confirmed no refuseIfFrozen / isSceneFrozen anywhere in botDispatch.ts or botConfirm.ts. It is the one writer of the four design columns that BYPASSES the freeze mechanism rather than being deliberately exempted by it. Harmless in the common case - a job only exists for a booked quote, which is freeze-exempt anyway - but a booked quote can legally transition to `cancelled` (quoteStatus.ts ALLOWED_TRANSITIONS), and at that point every route-level writer refuses while this path still does not. Exposure measured 2026-08-27: **0** cancelled quotes with a linked job today, and 0 crew-sourced extra photos ever. Dormant. NOTE: src/lib/integrations/** is a SHARED path - heads-up to Naldo before editing. | - |
| 429 | **[SHIPPED S55 - PR #1004 merged 2026-08-27]** Portal-visibility toggles now CONFIRM on an approved quote (deliberately not frozen) | L | The last design surface with no post-approval gate: portalShowStreetView / portalShowSatelliteView decide whether the customer's portal shows the house design image and the satellite plan. Jason's call on my recommendation: **confirm, not freeze**. They are presentational - they can show less or restore what was there, never a different job - and everything the customer agreed to (geometry, photo, satellite trace, price) is already frozen by rows 367/427. Freezing them would strand a booked customer's portal with a bad image whose only remedy is decline -> revive -> edit -> re-send, i.e. destroying a real approval to fix a picture. Row 370 had already made the same call by building an AUDIT TRAIL for these toggles rather than a gate. The confirm is PRE-FLIGHT (before the optimistic state update and before the request), so a decline changes nothing at all. Its predicate is deliberately WIDER than isSceneFrozen - it fires on BOOKED orders too, which the freeze exempts, because a booked customer has approved AND paid. **NOT VERIFIED:** the dialog firing. The repo has no React DOM harness (row 259), a mutation probe confirmed that removing the confirm CALL breaks zero tests, and three automated browser attempts failed to reach React's onChange at all (no confirm AND no fetch, the signature of an event React never processed - the S51 class). The banner copy IS verified live on approved quote #1290, which proves the predicate reaches the component, not that the dialog appears. A human clicking one toggle settles it in ten seconds. | - |
| 360 | **The operator auth gate was DORMANT by default, so any local dev server wrote to PROD unauthenticated and unattributed (S47)** | M | requireOperator() returned null (allow) unless AUTH_GATE_ENABLED === 'true', while .env.local points at the production Supabase. Verified 2026-08-23: curl with zero cookies got 200 from an operator-gated route; the browser held no Supabase auth cookie at all yet the nav rendered Sign out unconditionally; getOperator() (no dormancy carve-out) returned null so every actor/audit field wrote null. Forensic exposure measured, and it is EXACTLY ONE ROW: dashboard_activity holds 1396 'system' + 858 real user ids + 1 NULL, and that null is Kristie Tibbetts' colour apply at 2026-08-22 12:33 ET. The 218 null handled_by inbox rows are the legitimate system path (all 218 carry an activity row, which records 'system'). FIXED by PR #900 (S47): ENGAGED by default, AUTH_GATE_ENABLED=false the only opt-out. Jason ruled 2026-08-23 NOT to add a runtime guard correlating the flag with SUPABASE_URL - the documentation-only warning stands. | - |
| 355 | **Cut GitHub Actions minutes: cancel superseded runs + skip CI on docs-only pushes (S66, offered to Naldo; SHARED `.github/workflows`)** | S | The org is on the free plan (2000 Actions min/mo); an exhausted balance fails every workflow repo-wide with an instant no-runner error — S66 diagnosed exactly this (runs 846-850 passed in minutes, 851+ died in ~5s, runner_id 0). Two low-risk `ci.yml` changes: (1) a `concurrency` group keyed on the ref with `cancel-in-progress: true` so a new push kills the superseded run; (2) `paths-ignore`/a docs-only guard so a pure `docs/context/**` + `CLAUDE.md` push doesn't spend minutes on tsc/lint/vitest. SHARED path — Jason async heads-up (48h) before merge. The real fix Naldo took this session: raise/clear the Actions spending limit in org Settings → Billing. 🔵 **S70: PR #1017 OPEN.** Half of this row was ALREADY DONE before it was actioned — the `concurrency` + `cancel-in-progress` block has been in ci.yml since it was written; only the docs skip was outstanding. #1017 adds `paths-ignore` (docs/**, CLAUDE.md, AGENTS.md, README.md) to both triggers; `.claude/**` deliberately NOT ignored. ⚠️ Needs JASON'S EXPLICIT GO, not the 48h async standard: it changes what "gates green" means for a docs-only PR, which the wrap skill's unattended documents-only auto-merge depends on. Two consequences are written into the workflow itself (a docs-only PR now has NO gates check at all, absent rather than red; and if `gates` ever becomes a required check a skipped run blocks it permanently). | — |
| 358 | ⚰️ **MOOT — the Operations Hub was SCRAPPED (Naldo, 2026-08-27, recorded in S69).** **Expose the Quote Tool time clock to the Operations Hub (read AND write) over signed `/api/ops/v1` machine endpoints** | L | Naldo asked 2026-08-23 whether the Hub could see and drive the QT time clock. Scoped in full, then deliberately PARKED to stay on the Quote Tool; this row exists so the scoping is not redone from scratch. **Decisions already made, do not re-litigate:** read AND write (not read-only first), office AND field staff in one view, and contract-grade SIGNED requests (not a shared-secret header). **The contract already rules the direction:** QT owns all canonical time, the Hub is capture UI plus projection and never holds mutable time or computes pay (`OPERATIONS_HUB_CONTRACT.md` section 0, rulings F2/R8), and the QT assistant is the single author of `/api/ops/v1`. So the Hub calls these endpoints; it never gets its own time schema. **Build order is a safety property, three PRs:** (1) the machine-auth signing layer, HMAC-SHA256 per contract section 1: the `X-YLL-*` headers, the byte-level canonical-target algorithm, the plus/minus five minute timestamp window, constant-time compare, atomic nonce rejection backed by a REAL table (Vercel is serverless, so in-memory replay protection is worthless), key rotation with overlap, and the contract's own conformance vectors green in CI. (2) reads: `GET /api/ops/v1/me/day` and `me/hours`, plus a roster read of who is on the clock right now, joined on `crew_members.hub_employee_id`. (3) writes: `POST /api/ops/v1/clock-in` / `clock-out` / `breaks/start` / `breaks/end`, carrying the contract's `command_id` + `idempotency_key` so a Hub retry cannot double-punch. NOTHING writes until (1) is proven: a payroll write endpoint behind weak auth means anyone can forge punches. **Most of the hard part already exists:** `clockIn`/`clockOut`/`getOpenShift` (`src/lib/shifts.ts`), `startBreak`/`endBreak`/`paidSecondsForShift` (`src/lib/shiftBreaks.ts`), and `crew_members.hub_employee_id` already carries a unique index for the identity join, so this is an auth layer plus a route surface, not new time logic. **Machine auth is genuinely unbuilt though:** the three existing `/api/ops/v1/jobs/{id}/*` routes authenticate with a CREW SESSION (`requireCrew`), which a server-to-server Hub call cannot present. **Clear two blockers before building:** a shared key must be provisioned as a Vercel env var (Naldo does this; the assistant never handles secret values), and the contract is still `v1.5.0-draft` (section 10 makes it final only through paired review), so reconcile with Codex first if the Hub side is mid-change on these sections. ⚰️ **S70 CLOSE: this row is moot.** Naldo scrapped the Operations Hub entirely on 2026-08-27 and replaced it with other tools, so there is no Hub to expose the time clock to. The scoping below is kept only as the record of what was decided and why, and because the signing-layer design (HMAC per contract section 1, nonce table, key rotation) is the reusable part if any future external consumer ever needs a machine door into `/api/ops/v1`. Do NOT build it for the Hub. | — |
| 359 | **Deleting an operator silently orphans their office-staff pay row (`crew_members.auth_user_id` has no foreign key)** | S | Deferred from PR #906's admin lens (the row 354 onboarding UI). `crew_members.auth_user_id` has no foreign key to `auth.users` (`migrations/2026-08-16-crew-members-auth-user-id.sql`), and `DELETE /api/admin/users/[id]` has no awareness of crew linkage, so removing an operator leaves their `is_office` pay row pointing at a user id that no longer exists. **#906 fixed the VISIBILITY half only:** `GET /api/admin/office-staff` now returns `operatorMissing: true` and the Settings panel prints "operator login deleted", so an orphan is visible instead of showing as a normal active staffer. **Still open:** the delete route should refuse, or warn and require deactivating the staff row first, because re-onboarding the same person under a new login creates a SECOND `crew_members` row and splits their pay identity. The same fragility already applies to crew logins and is called out in the DELETE route's own comment; #906 extended the column's use to office staff without deepening the guard. Not urgent today: no live report reads `is_office` rows in aggregate yet, so this bites when payroll reporting lands. | — |
| 349 | 🔴 **BLOCKED ON NALDO (product ruling)** - **Light-size slider can OVERSELL the install on the customer portal at high settings (S65 wrap customer lens, MED — NALDO PRODUCT CALL)** | S | PR #883 (row 347) lets staff scale drawn lights 0.5x-4x. At 4x a c9 core is ~12px against a 15px bulb gap with an opaque saturated core, larger than the space between bulbs — the reviewer's honest line is ~2-2.5x before the render stops matching what a crew installs, and there is NO "illustrative/approximate" disclosure anywhere on the portal (grep-confirmed). Verified live that all 6 real prod sample designs carry `lightScale: null` today, so this is a risk of shipped CAPABILITY, not a live incident. Decision is Naldo's, not the assistant's: lower the slider max (e.g. cap at 2.5x), OR add a small portal disclosure line, OR accept 4x as-is. Flagged to Naldo directly at the S65 close. | — |
| 335 | **`/admin/schedule` opens on the WRONG DAY every evening: it defaults to the UTC calendar day, not the ET business day** | S | `src/components/ops/ScheduleDay.tsx:33` does `new Date().toISOString().slice(0,10)`, so from ~8pm ET (~7pm during EST) until midnight the page opens on TOMORROW and shows tomorrow's jobs, crew and capacity with no visual cue. Pre-existing; became reachable when S61 applied `2026-08-18-job-assignments.sql` to prod. Fix: derive the day in `America/New_York` (the same clock `opsMidnightClose` uses), not from a UTC ISO string. Found by the S61 wrap review; left unnumbered at the time to avoid a second ledger collision. **SHIPPED S54 (#982, merged + live 2026-08-26)** - defaultScheduleDay(now)=etDayKey(now), evening cases unit-tested against exact UTC instants (EDT + EST), midday picker verified in-browser on prod. 🔴 **NALDO ITEM (Jason, 2026-08-26): the one leg tests cannot close is the live EVENING check - open /admin/schedule after ~8pm ET and confirm it shows TODAY, not tomorrow.** Row moves to the archive once that check passes. | — |
| 343 | **Identity freeze: no recovery path for a genuinely mis-linked quote, and refusals are invisible to the owner (S46 #890 customer+admin lens MEDs)** | M | Two halves of the same gap, bundled. (a) Closing the revive escape hatch (row 338) removed the only — even accidental — way to correct a quote whose identity was mis-linked BEFORE approval; the banner is now honest that there is no in-app remedy, which is right, but the real fix is an admin-gated identity-correction action with an audit record, not a dead end that invites decline→duplicate→lose-history workarounds. (b) All five freeze-refusal sites (`src/lib/customers.ts` ~369-372, `src/lib/quotes.ts` ~704-708, three in `src/app/api/integrations/highlevel/attach/route.ts`) only `console.warn`; `identityFrozen` never reaches `src/lib/dashboard/**` (grep-confirmed), so the owner cannot answer "who tried to re-point a sold job's customer, and when". Build (a) on top of (b)'s audit record. | — |





| 295 | **🔴 No staleness alarm on the copilot sync — 8 days dark, nothing noticed (copilot)** | S | The 2026-08-08 outage ran for EIGHT DAYS with zero capture and no signal. `recording_sync_state.last_synced_at` froze at 2026-08-08 01:05Z while every cron 503'd hourly; the only way it surfaced was a whole-session review querying the table by hand. There is no alerting anywhere in the copilot (grep for telegram/notifyOps/alert across its src returns nothing), and `verify:auth-config` exists as an npm script but is wired into no CI job. Add a staleness check that pages when `last_synced_at` falls more than a few hours behind, routed through the QT Telegram bot (the copilot has no notifier of its own — that is the point). Judge it by one question: if capture stops again tomorrow, what tells someone before day eight? | — |
| 294 | **🔴 Production deploys stopped reaching Production (copilot infra)** | S | Found 2026-08-18 while recovering the outage: `yll-call-copilot.vercel.app` was still served by `8dc03a54` (PR #48, Aug 17 12:16Z) more than a day and four merges later. Master merges were landing in a Vercel environment reported by the GitHub deployments API as `precious-alignment / production` — a CUSTOM environment, not Production — so merging stopped shipping AND Production-scoped env vars never reached the running build. That is why setting CRON_SECRET appeared to do nothing across two redeploys. Verified via `get_deployment yll-call-copilot.vercel.app` returning the stale dpl with `target: production`, against newer merges showing the custom env. Find what remapped the git-deployment target, restore master -> Production, and add a post-merge check that the alias actually serves the new SHA — the repo already has a scar for exactly this (a dashboard Redeploy rebuilding the OLD commit while looking live). | — |
| 259 | **Editor + builder UI logic has NO automated coverage — a standing gap, now load-bearing (S37 wrap)** | M | This repo has no React component test infrastructure (no `@testing-library/react`, no jsdom) and no DOM harness for `editor-core`. S37 shipped or queued **five** changes whose core logic is verified by code-tracing alone: #741's photo-delete/undo (money — a resurrected billing group), #241's already-sent notice + force-redeliver wiring, #238's scroll gate, #249's Surface quick-tag + reset notice, #235's Mark-Abandoned button. Jason device-checked #741 and #238; the rest have not been. Each individually matches repo convention; collectively it means the highest-risk surface in the app is the least tested. Decide: build the infra once, or accept and require a device check per UI PR (and say so in the ledger row, not just the PR body). | - |
| 232 | **Call-transcript + commitment PII retention policy (copilot)** | M | Raised by the S54 four-lens review of the #217 plan and NOT addressed by #221. `transcripts` (1,210+ rows of recorded customer phone conversations: names, home addresses, sometimes payment talk), `learnings`, `call_scores`, `call_recordings`, and the planned `call_commitments` all accumulate INDEFINITELY across two databases with no purge, redaction, or anonymization path. #221 fixed WHO can read it (access control); this is HOW LONG it exists, which RLS does not touch. The commitment engine's 7-day item expiry marks an item expired — it deletes nothing underneath. QT already has a PII-retention purge cron to mirror as the pattern. Decide a retention window with Naldo (recordings vs transcripts vs derived summaries may differ), then build the purge. | — |
| 231 | **Copilot security residuals after #221** | S | Two known gaps left open by the #221 lockdown, both verified by the S54 security review, neither currently exploitable. (a) `supabase_admin`'s DEFAULT PRIVILEGES still grant anon/authenticated ALL on tables IT creates in `public` — `ALTER DEFAULT PRIVILEGES` fails 42501 as postgres, the highest role the migration path has, so it needs the Supabase dashboard or a support path. App migrations run as postgres and ARE covered (`0018`), so this only bites if Supabase tooling ever creates a public table holding real data. (b) Leaked-password protection is OFF (advisor WARN) — staff can set a password already in a known breach corpus; one toggle in Auth settings. Also re-run `get_advisors` after any future copilot migration: the project now sits at 30 tables RLS-on-no-policy by design, so a NEW table without RLS would stand out. | — |
| 221 | **Copilot DB RLS lockdown (security)** | M | Naldo GO 2026-08-06 (with 217–220, same session): the `yll-call-copilot` Supabase project has RLS DISABLED on 26 tables incl. `transcripts` (1,210 rows of customer-call PII), `learnings`, `call_scores` — the anon key grants full read/write. Design policies FIRST (the copilot app + its syncs must keep working), then enable + verify; Supabase advisor flags it CRITICAL. First establish which key the copilot app itself uses at runtime, and test the policies against the REAL sync/cron write patterns before enabling (S53 lens finding); needs Codex's sign-off — the copilot/hub schema is his side of the OPERATIONS_HUB_CONTRACT split. (QT-side `inventory_orders` RLS is separate, already on Jason's S33 ping list.) **✅ SHIPPED S57 2026-08-07 — the DATABASE CHANGE IS LIVE AND VERIFIED. My migration files were never merged and PR #39 is now CLOSED as redundant: Codex 0019_existing_tables_default_deny.sql on master does everything mine did and more (RLS with zero policies on every application table, table-level revokes, PLUS per-column grant stripping and function execute privileges, both of which mine missed). Master DOES reproduce prod; no action needed.** `0017_rls_lockdown.sql` enabled RLS on all 26 flagged tables; advisor CRITICAL cleared to zero ERROR findings; row counts unchanged at apply time (transcripts 1,210 · learnings 1,199). Safe because every data path already used the service-role client (66 of 72 non-test API routes; the other 6 touch no Supabase data; both anon-key clients make zero `.from()` calls). Follow-up `0018_revoke_anon_grants.sql` (same day) revoked the redundant ALL-PRIVILEGES grants anon/authenticated still held on all 31 tables PLUS the postgres DEFAULT PRIVILEGES that would have re-granted them on every future table — RLS does not revoke GRANTs, so without it one over-broad future policy reopens full read/write. KNOWN RESIDUAL: `supabase_admin`'s default ACL still grants anon on tables IT creates in public (42501 as postgres; documented in the migration). Security review verified the close independently at pg_class/pg_roles/pg_policies level: no storage bucket, view, or SECURITY DEFINER function routes around it. | — |
| 219 | **Inbound-call rep attribution (copilot)** | S | `call_recordings.ghl_user_id` is NULL on every INBOUND call (outbound populates) → per-employee boards and #217's "who promised what" can't attribute answered calls. Resolve from the GHL payload (answering user) or a lookup; backfill if cheap. BLOCKER for #217's all-reps coverage (until then inbound → an "unclaimed" lane). | — |
| 218 | **Transcription-failure triage (copilot)** | S | 2026-08-06: 3 of 22 call events FAILED transcription silently (all inbound, all 43s — suspicious pattern) and `outcome` is stuck "unknown" on all of today's transcripts. Failed calls are invisible to #217 and to coaching. Add retry + a Telegram alert on failure, surface fail/skip counts, fix outcome classification. | — |
| 217 | **Post-call commitment engine v1 (epic — ops hub × copilot × QT)** | L | Naldo 2026-08-06: every transcribed call → LLM-extract typed commitments (send_quote / send_photos / callback@promised-time / schedule_estimate / other; contact + rep) → per-employee checklist that AUTO-VERIFIES against real events (quote_sent · outbound SMS · later call) + promised-time breach pings + digest sweep. **Naldo's rulings: checklist lives in the OPERATIONS HUB dashboard (NOT the QT dashboard); v1 = checklist+verify ONLY, no auto-actions; ALL reps covered (needs #219).** QT side = a verification event feed — propose as a new OPERATIONS_HUB_CONTRACT flow per its §10 change process (contract = PR #701, in flight); interim surface until the hub ships = the live Telegram bot DM + morning-digest section. Aug-6 evidence: 3 promised quotes unstarted (Sheryl event lead · Rich Dalessio told "right now" 6:51 PM · Chris Hughes callback loop dropped) and a promised 3 PM callback never attempted, while the copilot's existing extraction (~45–75 min latency) already writes summaries naming the commitments. Supersedes the #168 plan's transcript sketch — its "confirm transcription exists" prerequisite is CONFIRMED (copilot DB). Full plan: `docs/superpowers/plans/2026-08-06-post-call-commitment-engine.md`. | — |
| 168 | **Text-ops bot + YLL ops tool layer (Naldo S46 plan → S47 build)** | L | ✅ **SHIPPED + LIVE (Naldo S47, 2026-08-05, master `e7e13d06`): bot #621/#627/#636/#638 + morning digest #675/#684 + ops routing config, all merged + prod-verified.** Telegram staff-only bot over a shared tool layer, extending the dormant #82 bot. Reads (status/schedule/stock/low/jobs); crew `completeInstall` (material ACTUALS + on-hand true-up + photos); Bot-team settings page (`/settings/bot-team`); roles crew<staff<admin (matrix rows 5/17/18 decided: crew=no / staff=yes / staff-approve=admin); confirm-yes + audit on writes; cheap interpreter model; voice+photos. Morning digest: cron un-401'd (#675), routed ops→Naldo+Jason DMs, heartbeat v2 (#684, always-sends, inbox+pipeline counts). MCP-protocol still deferred until a 2nd interactive AI client. Plan `docs/superpowers/plans/2026-07-19-text-ops-bot-and-yll-ops-tool-layer.md`. **S47 POST-CLOSE (2026-08-05): captureLead Phase 3 SHIPPED + LIVE (#693, task #11 DONE)** — crew field-lead capture → GHL contact + SMS-drip tags, behind role + confirm-yes + an ADDRESSED consent affirmation; **digest hardened (#694)** (exhaustive count scan + silent-fail alert); **the two #666 crons restored (#688)**; **#637 salvaged (#692)** + closed. **Still open:** 3 hardening chips (album buffering / prep-deduction baseline / webhook hardening) · `/settings/telegram` admin-gate call · GHL call-transcript→action-items (future). | ✅ S47 |
| 167 | **Training-data flywheel (epic, Naldo)** — self-improving loop for the AI designer/measurer; spec `docs/superpowers/specs/2026-07-20-training-data-flywheel.md`. P1 bulk-ingest Naldo's 4–5yr archive (Drive + camera-roll exports; Jobber docs manual) → classify/OCR markup counts/resolve address → pair each install with fresh Google sat+SV imagery → review queue → `training_houses` + new `plant_examples` · P2 tree/bush estimator in the LIVE quote flow (promote `calcStringsFromBox` + similar-plant few-shot; tree markup numbers = MINI STRINGS per Naldo) · P3 customer tree/bush photos + ballpark-pending portal lines ("final after photo" — never hard-block) · P4 accuracy scorecard + fix the 5 held #109-P2 defects. Decisions locked in 2026-07-20 Q&A; corpus today = 16+2+1 rows (starved, capture works). | XL | **P1 SLICES 1–3 MERGED + LIVE; queue being worked.** | — **S51 (remote CCR, 2026-08-04→05):** slice 2 MERGED+LIVE (#677, prod SHA-verified). Slice 3 BUILT — PR #680 OPEN (trace queue + needs-ID lane + tracer pre-fill w/ locked feetPerPixel×satellite_w scale + corpus isolation: training_houses.source, POSITIVE manual-only few-shot filter, server-derived source). Reviews: four-lens ×2 + wrap review — 2 HIGH + 6 MED + 5 LOW fixed pre-merge, wrap fixed a dead /training/<id> link + 2 staff MED + a failed-prefill-cannot-claim guard; 2 LOW accepted. Migration 2026-08-05-archive-slice3 was the #680 merge blocker at S51 close (unapplied; blast radius = every training-house save). **S52 (2026-08-05): #680 CLOSED OUT.** Migration applied + verified (0 rows with source≠'manual'). Imagery fetch run on prod (75/77; the 2 remaining fail geocoding permanently — a building-name mismatch and a malformed source address, both refuse-to-guess correctly, need a human retype). Night-photo backfill run (141/159 landed; 18 iPhone HEIC files failed — this machine's sharp/libheif build has no HEVC decoder, not a code bug; both needs-identifying rows, deborah sande + tal, are among the 18, so tal's Talonda Thomas candidate could NOT be visually confirmed and was left unapplied). Device-check found a real HIGH: Auto-Analyze silently zeroed a traced/overridden archive gutterline+ridge (the calibration-lock guard covered the scale but not the measurements it feeds) — fixed + live-verified on a redeployed preview. Pre-merge 4-lens review (customer/staff/admin/technical, all Sonnet) found the SAME bug shape still live in the four decoration detection setters (staff + technical lens, independently) — fixed in the same pass, folded under the same guard, gates re-green (tsc 0 · lint 0 · vitest 4613). **PR #680 MERGED (`ea97dd8`) + prod-deploy SHA-verified live at `quote.yulelovelights.com/training/archive`.** Deferred from the review as ledger-candidate follow-ups (none block tonight's actual corpus data): `excludeArchivePhoto` missing the file's own established CAS guard (MED — real race with `identifyArchivePhoto`, can silently revive an excluded row or clobber a fresh identify) · Cancel doesn't route back to `/training/archive` the way Save now does (MED, cosmetic asymmetry this PR itself introduced) · no server-side floor rejects a zero-footage/empty-lines save (HIGH but repo-wide and pre-existing, not archive-specific; the archive instance is inert until a source='archive'→'manual' promotion path exists, which doesn't yet) · backfill script's basename matching has no cross-check against `source_folder` for a FUTURE ingest run pulling from an overlapping folder (MED, tonight's actual run was clean and independently verified). Still open: PII-redaction SQL for the two reviewer_notes rows · ~~the 167 ledger-number duplicate (fold into the 192/193 resolution)~~ **RESOLVED 2026-08-25 (task_ledger row 402 cleanup) — no fold needed, disambiguated directly: a DIFFERENT task also carries the number 167 — the ARCHIVED row (`task_ledger_archive.md`) is Naldo's S41 referral-page + booking bug batch; this one is the training-data flywheel epic. Same number, unrelated tasks; not a duplicate.** · queue pagination before the next manifest drop · 3 address-variant pairs need a human merge call. **NEXT: work the trace queue (~80 properties ≈ 3h).** |
| 151 | **PostHog product analytics -- both properties (Naldo S31)** | L | **v1+Wave1 LIVE** (#479/#480 merged, SHA-verified): masked replay (eyeballed) - funnel events - approve-friction - decline chips - WP form/phone/CTA/gallery snippets - 2 surveys - dashboards Website Metrics 1831708 + Quote Tool 1831713 (PostHog project 506466; key in .env.local + Vercel). **▶ S34: Wave 2 (#481) MERGED + LIVE + VERIFIED** — re-synced/re-gated, SHA-verified deploy, all 5 events proven in PostHog on a seeded is_test permanent quote (color_changed incl. build-your-own adds, custom_colors_opened, effect_changed, daylight_toggled, video_played). **OPEN:** #482 (Wave 4 section-reach/pay-balance) awaits Jason review + Naldo merge-go; post-merge live-verify on a FRESH is_test seed (clone quote 79130960... pattern, stamp quote_sent_at). Parked: video progress milestones (YT iframe API dep) - correlation insights (need data volume). | -- |
| 110 | **Full-tool QUALITY audit (epic, Jason S20)** — whole-tool audit for bugs, inefficiencies, dense-code quality, dead code, and refactor candidates, run in **8 interleaved waves** (audit area → Jason dispositions → fix PRs → next wave): W0 scoping → W1 money core → W2 data layer → W3 dense files (`editor.ts` 5.3kL / `QuoteBuilder.tsx` 3.3kL / training-new 1.5kL) → W4 portal → W5 AI/training → W6 API routes + cross-cutting → W7 Naldo's areas (READ-ONLY → handoff doc, no PRs from us). Complements #80 (bugs/security, 2026-06-26) with the quality dimensions + all post-#80 surface (#104/#13/#83-console/inbox-v2/#53/#54); every wave dedupes against #80 + later review dispositions. **Locked (Jason 2026-07-02):** interleaved waves; `editor-core` REFACTOR-FROZEN (relay-locked — bugs fixed+relayed, structural findings **feed #29 as its spec**); Naldo areas in scope read-only; plan-critic-lite (no full council — council reserved for any L-sized refactor disposition). CRITICAL escape hatch: money-wrong/data-loss/security → immediate own PR; else ledger-then-disposition (fix-now/fix-later/accept/feed-#29/hand-to-Naldo). Characterization-test gate before refactoring thin-coverage files. Fix waves land BEFORE the Aug–Oct Jobber trial. **Full plan: `docs/audit/AUDIT-PLAN-2026-07.md`** · findings → `docs/audit/AUDIT-2026-07.md`. **W0 CLOSED** (manifest 648 files 100% assigned · charter · dedupe ledger 135 rows · handoff scaffold). **W1 money-core CLOSED (S20, 2026-07-04): 79 findings (1 CRIT/4 HIGH/19 MED/55 LOW); 23 of 24 fix-nows MERGED to master `1e65602`** (5 PRs #329/#330/#331/#334/#336, vitest 1695→1756) — CRITICAL W1-001 (invoice billed full `result.total` not the approved selection) + all 4 HIGH + 12 MED + 6 LOW. **W2+W4 FULLY CLOSED (S20, 2026-07-04): every Crit/High/Med + all actionable LOW fixed+live** (master `9a5e443`, vitest 1868) across 3 fix batches (11 PRs #329–#353); W2-001 rebook extra_photos HIGH + W4-003 blank-signature HIGH incl. **⚠️ OPEN ITEMS → `docs/audit/NEEDS-JASON-2026-07.md`** (W2-026 contact-update decision · W4-025 ReferralCard→#41 · W4-003/013 device-verify · Naldo loop). **W7 (Naldo areas, read-only) CLOSED: 96 findings (9 HIGH/38 MED/49 LOW) → `docs/audit/AUDIT-2026-07-NALDO-HANDOFF.md` for Naldo** (trial-critical HIGHs: inbox response-time measures cron-lag not customer-wait · Followed items still escalate · ingest write-amplification · insights+worklist still count cancelled as booked [B7 gap] · inventory clipRules dropped everywhere + PO re-sends cumulative-not-delta + prep claims stock before reading materials · cancel-order UI drops refund flags). **W1 FIX-LATER BACKLOG (do before Aug–Oct trial):** W1-006 2nd-deposit durable record+alert · W1-008 cancel-of-paid refund record (needs design) · W1-010 design-projection route tests · W1-013 approve-branch snapshot tests · W1-019 portal-open sequential DB round-trips · W1-020 GHL stage-config knob drift · W1-068 job line_items selection filter · (known-open: W1-073/#80-076 autoRoofline tie, W1-074 multi-spritzer line). **OUTSTANDING policy step: in-browser prod smoke-test of approve→invoice→pay-balance (unit-tested; browser confirm deferred).** ⚠️ A/B touched Naldo's `invoices.ts`/`jobs.ts` — Jason-authorized, loop Naldo. **W5 AI/training FULLY CLOSED (S20, 2026-07-04): 30 findings all fixed+live** (master `746fbcf`, vitest 1868→1973; 4 PRs #357/#358/#359/#360) — HIGH axis-aware `scenePxPerFoot` (shared `yardstickPpf.ts`) + **token-cost batch** (Anthropic prompt caching on the ~3.4K-token prompt, image downscale 1568px + total-cap 24, max_tokens 8192) + enum validation at BOTH layers (#80-066 model-output + #80-036 operator-submit) + references validation; **live-analyze verified** end-to-end; 2 device-checks → `NEEDS-JASON-2026-07.md` (downscale detection quality [DECISION], cache-savings confirm). **W3 dense files FULLY CLOSED (S21, 2026-07-04): 30 findings (3 HIGH/15 MED/11 LOW, 0 refuted) — all 16 HIGH/MED fix-nows MERGED to master `3ed2a6f`** (3 PRs #369 editor / #370 QuoteBuilder / #371 training, vitest 1973→2009): HIGH W3-001 (address-pull footage no longer zeroed) + W3-002/030 (clone strips groupId/linkedToId → billed) + W3-003 (breakdown filters by stable id, W1-005 sibling); W3-004 **closes #80-105**, W3-006 **closes #80-102 capture half**; + W3-009/010/011/013/014, doSave saveSeq, flip-axis scheduleSave, breakdownLinked useMemo. **editor.ts shared hunks relayed byte-identical to design-tool `main` `8673a68`** (W3-009 miniArea + W3-002 groupId; linkedToId/doSave/flip-axis/twin/stamp are quote-tool-only). **7 refactor findings → #29 spec seed `docs/audit/EDITOR-29-SPEC-SEED.md`** (frozen file). 11 LOW → ledger. W3 device-checks + fix-later (W3-008 server-reorder half · #80-102 non-capture half · training per-photo calibration) → `NEEDS-JASON-2026-07.md`. Composed clean with Naldo's #88 permanent (never-stale re-gate on new master). **W6 routes + cross-cutting AUDITED + FIXED (S21, 2026-07-04): 18 findings (2 HIGH/7 MED/9 LOW, 0 refuted).** The #81 auth perimeter itself is SOUND (no bypass); the security findings are allowlist omissions that FAIL CLOSED. **FIXED → master `ce7632f` (PRs #394 auth / #395 designs+obs, vitest 2009→2146):** HIGH W6-002 (designs/[id]/photos orphaned blob, W2-008 parity) + W6-GAP-1 (login `rateLimitResponse`) · auth allowlist cluster W6-001 (health) / W6-005 (GET quotes-[id] via GET-only `method` param) / W6-008 (**option a** — allowlist + `requireOperator` removed + `is_test===true`→403 pre-mutation boundary; INERT until `AUTH_GATE_ENABLED` flips) · observability W6-003/009/010 (additive log/response surfacing; W6-003 valor + W6-010 jobs/cancel route-level, Naldo-concern → loop Naldo) · test-gaps W6-004/006/011/016/017 + LOW W6-014/015. **HELD:** #396 (W6-012 eslint/vitest `.claude/**` ignore — SHARED files → Naldo reviews first). **DEFERRED:** W6-013 (middleware→proxy Next.js 16 rename, own task). **W6-007 → NALDO: 6 event/permanent money engines (~1007L, #88/event) never audited → money-lens pass in the handoff doc; manifest generator FIXED (regenerated, exit 0).** **✅ ALL 8 WAVES AUDITED (W0–W7); W1–W6 FIXED+LIVE + the W1/W3 fix-later backlogs CLEARED (S21, master `13ba733`, PRs #402/#403/#404):** W1-006/008 durable money-obligation records (approval_snapshot marker + staff email + log, no migration) · W1-010/013 money-path route tests · W1-020 GHL stage-knob drift · #80-102 QuoteBuilder Calculate/Send flush-abort (W1-019 was ALREADY fixed by W2-031/W4-016/W4-033). **#110 EPIC essentially COMPLETE** — residual tail only: **#396** (W6-012 tooling, held for Naldo SHARED review) · **W6-013** (middleware→proxy, own task) · deferred **W1-068** (cosmetic, Naldo's jobs.ts) + **W3-008-remaining/training-calibration** (frozen-file-self-heals / separate change). **Loop Naldo:** #396 · W6-003/010 · W6-007 · W1-068. | L (epic) | 🔨 AUDIT COMPLETE (8/8) · **W1–W6 fixed+live + W1/W3 fix-later tail cleared** · **W7 Naldo-lane: ALL 9 HIGHs FIXED+LIVE (S26)** — W7-001 refund-cue #409 · W7-002 PO-supersedes #419 · W7-003 response-time+`last_inbound_at` migration #415 · W7-004 ingest write-amp #412 · W7-005 Followed-escalation #408 · W7-006/007 cancelled-as-booked insights+worklist #405 · W7-008 prep read-before-claim #411 · W7-009 clips #407 · **residual CLEARED (S26): #396 tooling MERGED · W6-013 middleware→proxy MERGED (#425, `next build`-verified, Jason review)** · Naldo loop | — |
| 83 | **Jobber-flow — Quotes → Jobs → Invoices (mirror Jobber, retire it for ops)** — **✅ ALL 5 PHASES' DATA-LAYER CORES MERGED TO MASTER + LIVE ON PROD (Naldo S9 close-out, 2026-06-27).** Built + adversarially reviewed S8, then merged via integration PR #200 (all 3 migrations applied + FKs verified CASCADE on prod; tsc 0 · lint 0 · 761 tests). Only the **money/operator SURFACES** (jobs/invoices pages, amend route, balance collection) remain gated on #81 — the data layer is in. Each phase a worktree-isolated, TDD'd, centrally-gated PR (Jason-review, PR-not-self-merge): **P1** status spine (#185 Slice A `quoteStatus`/`displayId` + migration) + portal decline/request-changes/e-sign (#186 Slice B); **P2** shared `jobs` table + auto-create-on-booked + dashboard board (#188 core, #190 polish); **P3** invoices engine + balance math core (#194 — `invoices.ts`/`invoiceStatus.ts`/`balanceCollection.ts`, collection gated on #81 + the Valor-MIT decision); **P4** amend-order core (#189 — pure re-price + amendment trail); **P5** Customer/Property identity + rebook-last-season (#192 — `customers.ts`/`rebook.ts` + design clone; **its migration is APPLIED to prod**). A 7-dimension multi-agent adversarial review (`docs/jobber-flow/REVIEW-2026-06-27.md`) → 15 raw → **7 confirmed, all fixed** (HIGH: jobs/invoices→quotes FKs needed `ON DELETE CASCADE` or `deleteQuote`/"delete all" would FK-fail on a booked quote; + customer-linkage carry, 23505 race recovery, doc/round2 fixes). **"Both" balance-collection chosen** (auto-charge vault + pay-link fallback) but auto-charge needs a confirmed **Valor server-side card-on-file (MIT) capability + Jason** (current integration builds "no auto-charge") → only pure routing shipped. **🆕 SURFACES SHIPPED (Naldo S14b, 2026-06-29 → PR #251, MERGED → master `44a48b9`, LIVE):** /admin/jobs + /admin/invoices (list+detail), `jobs/[id]/complete` (advance → auto-create invoice), `quotes/[id]/amend` (server re-priced, immutable trail, invoice re-sync; status stays `booked`), + all 4 gaps (invoice tax-override, Cancel job+invoice+quote, amend → optional TEST-SAFE customer notify, balance PAY-LINK + Valor `bal_<quoteId>` webhook branch). Two adversarial passes → all fixed (CRITICAL: balance webhook verifies the paid amount before settling). **Auto-charge ("Charge remaining balance") STAYS GATED** behind `VALOR_AUTO_CHARGE_ENABLED` — Valor's Direct Sale-Token card-on-file API exists (uses our `appid/appkey/epi`) but needs Jason + a live confirm (deposit-vaulting + MIT); handoff `docs/jobber-flow/VALOR-AUTOCHARGE-FOR-JASON.md`. **NEXT = the only remaining #83 piece: confirm the Valor card-on-file/MIT capability (Jason) to flip on auto-charge.** **🆕 OPERATOR WORKFLOW COMPLETE + VERIFIED (Naldo S20, 2026-07-01 — ALL LIVE, master `1c9a142`):** status-aware operator **Options console** (staff-approve · convert-to-job w/ operator-typed deposit · collect-payment · close · send email/text/both) + read-only `/admin/quotes/[id]` + Quotes|Jobs|Invoices nav (PR #288/#293); a **24-agent pipeline audit → 8 trial-blocker bugs fixed** (PR #300: changes_requested-resend dead-end · /approve status-gate · portal-shows-dead-quotes · tax-override-unpays · cancelled-invoice-webhook-resurrect · cancelled-counts-as-booked-revenue · invoice-breakdown · amend paid_at); **rebook wiring + prod backfill** (PR #306 — 9 quotes → 7 customers/properties); **deposit-amount fix + money-reconciliation line** (PR #309); **Needs-Action dashboard queue** (PR #310). Live-verified create→job→complete→invoice→close end-to-end + **3 LLM council reviews** → **trial-ready for an Aug–Oct parallel-with-Jobber run** (shadow rule: Jobber stays source of truth through Oct; auto-charge stays OFF). — make the tool mimic Jobber's Quote→Job→Invoice pipeline so YLL runs ops on **our tool + home.works** and drops Jobber (kept only for old data). **Spec + plan: `docs/jobber-flow/SPEC.md` (+ `PLAN.md`)** — from a live Jobber walkthrough + 11-agent research (2026-06-26, Naldo). **Locked:** explicit Quote/Job/Invoice statuses + a dashboard **Workflow board** (Naldo's #58 area); portal **Decline-with-reason / Request-changes** + **e-signature** on approve; **auto-create Job** at deposit-paid; **auto-create Invoice** at install-complete; **50% balance via Valor** (auto-charge saved vault card → fallback portal pay-link); **amend a booked order** in-tool (re-open the frozen snapshot, recompute balance, versioned trail); **Customer + Property** identity + **"rebook last season"**; **sequential display IDs** (Quote#/Job#/Invoice#; UUID stays the portal token); tax stays 8.75% + manual override; **Cancelled** state (refunds manual in Valor). **Phases:** P0 = **#81 auth perimeter (BLOCKING)** → P1 statuses+decline/e-sign → P2 Jobs+board → P3 Invoices+Valor balance → P4 amend-order → P5 Customer/Property+rebook. **🔗 SHARED JOB ENTITY with the Inventory epic #82** — both auto-create a "job" on the **deposit-paid (#38) webhook**; Jason confirmed the entity (Job ID ≠ Quote ID, deposit trigger). Build ONE `jobs` object that BOTH the inventory Stages-Kanban (operational) and this Jobber-flow (billing statuses + invoice link) read — do NOT duplicate. ⚠️ Mostly **Jason's area + SHARED data layer** → needs Jason coordination/review. **Ideas (not now):** #87. **TBD:** where Jobs/Invoices are created (our tool vs home.works — see #84). | L (epic) | ✅ **WORKFLOW COMPLETE + LIVE + verified** (S9 data · S14b/#251 surfaces · S20 console #288/#293 + audit-fixes #300 + rebook #306 + deposit-fix/reconciliation #309 + needs-action #310); only **auto-charge** gated on Jason/Valor | — |
| 58 | **Homepage (`/`) → internal staff DASHBOARD + Unified `/inbox` (Naldo's area) — LIVE.** Core dashboard shipped S1 (KPI strip + "needs attention" worklist + per-service sections; plan/records `docs/dashboard/`). **Unified Customer Dashboard `/inbox`** — every UNRESPONDED touch across GHL + Gmail + Quote-Tool + Homeworks in one queue — BUILT S15 (draft #249) → **LIVE S16** (#249 merged + triage v1 #258; migrations applied; Gmail/GHL sources + noise filtering live) → **respond/manage/measure S17:** reply-from-inbox + AI draft (Sonnet, human-in-loop, no-hard-commitments) + layer-3 sender-suppression + Followed snooze (#265, +#262 reopen fix); state lifecycle (In-the-works Awaiting/Handled groups + `completed` terminal status + configurable follow-up days); audit log `/inbox/activity` with per-entry **Reverse** (#276); audit decisions filter (#277); response-time analytics (buckets · time-to-completed · trend · reopen rate · All/90/30d) on inbox+insights+home (#283). 4 migrations applied to prod. Full living context: memory `project_dashboard_inbox.md`. **▶ S23 BUILT (PR #380):** In-the-works rows now fully actionable — inline reply composer (extracted to a shared `ReplyComposer`, also de-duped out of `InboxList`) + AI-draft/send + `Followed` snooze on Handled rows; reuses the existing operator-gated draft/reply/followed routes. Deferred dashboard phases (Ph4 native Insights charts · Ph5 home.works metrics [gated, #16] · Ph6 recurring-client LTV) in `docs/dashboard/PLAN.md`. ⚠️ Clean gates = `npx eslint src` + `npx vitest run src --exclude '**/.claude/**'` (sibling nested worktrees pollute `npm run lint`). | L (epic) | ✅ core + `/inbox` LIVE (S1 / S16 / S17); **In-the-works actionable rows BUILT S23 (PR #380)** | — |

### ↳ Raised 2026-07-17 (Jason) — FIRST REAL PAYMENT incident + follow-ups (quote #1191, david alfaro, holiday)
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 161 | **Deposit card-on-file vaulting (Jason, 2026-07-17) — the #83 auto-charge prerequisite.** Staff expected the customer's card saved to the Valor vault at deposit; it isn't, because `createHostedPageSale` sends NO save-card/vault flag (only the unused `getClientToken` set `save_card=1`), so `quotes.valor_vault_token` stays NULL (the webhook already STORES `event.vaultToken` if one arrives — plumbing exists, nothing sends it). Documented gap: `docs/jobber-flow/VALOR-AUTOCHARGE-FOR-JASON.md`. Fix: add a vault/save-card flag to the hosted-page request + card-on-file consent wording at deposit; verify Valor returns a token in the webhook. Unblocks auto-charging the 50% balance. | M | ⏸️ PARKED — save_card=1 probe FAILED: a live deposit (2026-07-17) confirmed the Valor HOSTED PAGE does NOT vault via `save_card=1` (card never hit the Vault, webhook returned no token); the dead flag was REVERTED. Card-on-file needs a DIFFERENT path — ask Valor support the correct hosted-page vault param, or use the separate valor-vault REST profile API (Valor-App-ID/Key headers + a vault_id we don't persist). Blocked on a Valor answer. **▶ S28 (Jason): self-serve path found — Valor support NOT needed for mechanics.** Docs sweep: NO hosted-page vault param exists (clean negative); Valor documents the TRANSACTION webhook token as `vtToken` — a name parseWebhookEvent never read (the #159 invoice_no class; the S27 "no token" may have been a FALSE NEGATIVE). **#609 MERGED: vtToken/vt_token in the pick-list + hasVaultToken headline log + names-only key-dump on token-less approved txns.** Next real deposit (or a $1 pay-then-void) answers card-on-file in the Vercel logs; if a token lands, #83 needs only a $1 /?saleToken test + the MIT-consent letter (the one remaining Valor-support item). **▶ S29 (Jason, 2026-07-21→23): SOLVED — mechanics COMPLETE + LIVE.** Fadil Cox: the token rides a `redirect_url` S2S callback (never the webhook). Shipped + proven across 7 $1 tests: capture endpoint + URL-secret auth + fill-null persistence (#629/#630), webhook clobber fix (#631) → the token is saved at EVERY deposit and survives booking; $1 MIT charge via /?saleToken **APPROVED** (surchargeIndicator=1 for our cash-discount EPI + shipping_country mandatory; voided-sale tokens still charge); **both-vaults** (#632/#634): a Valor Vault customer is auto-registered per booking (first live entry 4237995, dashboard-verified; prod base online.valorpaytech.com, same creds; addcustomer takes `customer_name` ONLY). Auto-charge fully wired (#633) behind `VALOR_AUTO_CHARGE_ENABLED`=OFF → **remaining work = ledger #170 arming checklist.** | — |
| 164 | **Pass the Valor card-processing fee to the customer — add a fee LINE to the quote total/breakdown on the portal (Jason, 2026-07-17). PARKED — needs the exact Valor rate.** Today YLL EATS the processing fee: `createHostedPageSale` sends `surcharge:0` + `ignore_surcharge_calc:1` (charges exactly the computed amount, no surcharge) on BOTH the deposit and the balance pay-link, and the pricing engine adds NO fee line (grep-verified) — the customer pays only subtotal + 8.75% tax and YLL absorbs Valor's rate on every job. GOAL: bake a VISIBLE card-fee line into the quote total so the customer covers it. DECIDED (Jason): the rate is **Settings-editable + FREEZES per-quote** (new-quotes-only on change, like tax / permanent rates). Baked into the total ⇒ auto-applies to deposit + balance (each = 50% of the new total); the line sits BELOW tax and is itself NOT taxed. NEEDS-INFO before build (why parked): the **exact effective Valor rate** from the merchant agreement (flat % vs % + fixed cents). Un-decided bits (Jason: no preference) default to **all verticals** + **fee base = subtotal+tax** unless he says otherwise. COMPLIANCE before ship: the visible line satisfies disclosure; confirm surcharging is allowed (+ any %-cap) in YLL's operating states; debit-card surcharge is generally barred and the portal can't tell card type — Jason's risk call. SURFACES: pricing engine + rate snapshot, portal breakdown, builder breakdown, deposit/approved amounts, invoice, PDF. | M | ⏸️ PARKED — needs the rate (Fadil 2026-07-23: ask the **Merchant Service Provider**; Valor support won't give it). Mechanism CONFIRMED (Fadil): we set the fee ourselves via `surcharge_amount` on the Sale API — no plugin. | — |

### ↳ Spawned from the S10 booth review
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|

## 🟠 Lower priority — S10 batch (do AFTER the high-prio set; still TODO even if Saturday slips — Jason S10)
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 109 | **Completed-install GROUND TRUTH — pre-fill from stored design + reconcile operator KNOWN numbers (#54/#8 follow-up; LLM-council-flagged S19)** — deferred from #54. For jobs quoted IN the tool, the approved design (#27/#104 — the design IS the master item list, w/ polylines + per-unit line items) is BETTER ground truth than a night-photo re-derivation: on `/training/new`, pre-fill the markup from the stored approved design where one exists, let staff reconcile against the photo, save that. ALSO capture the operator KNOWN install numbers (footage / string / wreath counts) at save time so completed-mode AI detections can be SCORED against operator truth (a truth-vs-vision signal → over time, trust vision where it agrees). Historical uploads with no design record keep the #54 AI-detect path. **Council principle: the AI is a draft-generator; the human-confirmed value is the saved label** — never let unconfirmed night detections become ground truth. Relates to #8 / #54 / #27 / #104 / #13 (extras stay photo-1-only in training capture for now). **▶ S34: PHASE 1 DONE + LIVE (PR #508)** — `/training/new` "Pre-fill From Quote" loads the stored approved design (all 8 markup layers + footage/difficulty) as a reconcilable draft via new operator-gated `GET /api/training/prefill`; historical uploads keep the AI-detect path. **▶ S34: PHASE 2 BUILT + adversarially REVIEWED + HELD (Naldo).** Full build on branch `naldo/109-phase2-ground-truth` (no PR, gates green) → 5-lens verified review found **5 confirmed defects, 4 in the analyzer bias fold-in** (partial-record dilution → "more data = worse calibration"; mini-strand metric structurally zeroed; outlier injection into a 5-pair mean; multi-photo aiSnapshot captures only the last photo; + fractional counts). Because the bias note feeds the analyzer prompt (MONEY path), Naldo HELD the whole phase. Capture UI + readout salvageable; **fold-in needs a per-metric-observation redesign** (don't fabricate "AI was right" zeros). Full findings + fix direction: `docs/superpowers/specs/2026-07-12-109-phase2-ground-truth.md`. | L | 🟡 phase 1 DONE + LIVE (PR #508, S34) · phase 2 built+reviewed+HELD (fold-in broken, redesign needed) |

## 🔜 Backlog — active dev (priority order)
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 301 | **Marketing site: strip leftover wedding copy from the Diwali and Ramadan/Eid pages (Naldo S50)** | S | Both pages were cloned from `/services/event-lighting/`. Every section was rewritten EXCEPT the five "lighting types" cards, whose bodies still read "your wedding or event", "underwater ambiance" and "romantic elegance". Live and customer-visible on `/services/diwali-lighting/` and `/services/ramadan-eid-lighting/`. Replacement copy for all ten cards is written and sits in the S50 scratchpad (`card_copy_fix.md`); applying it needs a logged-in WordPress session. Root cause worth remembering: the card TITLES transferred fine so the BODIES were never read. | - |
| 201 | **Jobber-history backfill: tenure years + PROPERTIES (Jason S34) — prod-data campaign** — Jason uploads Jobber reports of all past jobs; ONE pass yields per-customer worked YEARS (completed/closed + booked jobs count; cancelled/draft excluded) AND PROPERTIES (job addresses). Match EXACTLY as the #155 machinery does (`scripts/match-legacy-contacts.ts`): exact email OR exact last-10-digit phone ONLY — **name is NEVER a match key** (display label only; any name-only candidate and any multi-hit → AMBIGUOUS/review, including for bucket-1 enrichment). Three buckets: (1) existing tool customers → enrich: property rows (normalized-address dedupe vs quote-derived, empty nickname — NOTE the nickname column is #205's migration, a SCHEMA dep, not just visibility) + tenure years + the GHL field; (2) matched-in-GHL, NO tool row → **Jason's IMPORT REVIEW LIST** (Jason locked): he strikes the do-not-work-with-again names, gives the go, and only the approved subset gets customer rows + properties + years + GHL field — EXCLUDED names get nothing written anywhere; (3) no confident match → unmatched review list, never guessed. The per-name exclusion/override column covers ALL buckets (Jason can exclude a bucket-1 existing customer too). GHL field writes go through **#200's own push function** — single writer implementation, never a bespoke second path; a concurrent booking/tenure-edit push racing the campaign means last-write-wins on the mirror, self-healed by the next trigger (disclosed, accepted — tool DB stays source of truth). Dry-run report of all three buckets BEFORE any write; then batched named-consent writes per winback standards. Pre-2022 years = data error → review list, never written. HARD deps: #200 (field + push fn) AND #205's migration (nickname column); #205's tab also makes imports visible. | M–L | 🔜 NEW (S34) · campaign | — |
| 154 | **Wisetack financing on the customer portal (Naldo, 2026-07-14)** — surface **Wisetack** consumer financing on the customer `/portal/<id>` page so customers can finance a lighting project instead of paying in full. Wisetack = point-of-sale financing for home services (customer applies in ~seconds, YLL gets paid upfront, customer pays Wisetack in monthly installments). Placement: near the approve/pay step (a "Finance this project with Wisetack" prequalify link/widget + monthly-payment estimate). **Open scope Qs to lock BEFORE building (prompt-first):** which service types (holiday / permanent / both) · min ticket to show it (Wisetack has a floor) · prequalify-link vs embedded widget · do we still collect the 50% deposit or does Wisetack fund the whole job · where the Wisetack merchant account + API creds come from · does an approved-and-financed quote skip the Valor deposit path. YLL has a Wisetack merchant account. **SCOPED + PLANNED (S38):** spec `docs/superpowers/specs/2026-07-14-wisetack-portal-financing-design.md` + plan `docs/superpowers/plans/2026-07-14-wisetack-portal-financing.md`. Locked: Full API, both verticals, Option B (Valor 50% deposit stays, Wisetack finances the 50% balance), CTA at approve/deposit step, booking trigger unchanged, `WISETACK_FINANCING_ENABLED` kill switch. 5 phases (P0 confirm API/sandbox → P1 backend flag-off → P2 UI → P3 balance reconcile → P4 sandbox E2E + prod flip). **P0 BLOCKS full path:** needs sandbox creds + confirmed API surface from Naldo's Wisetack partner contact. P1 unit-tested parts buildable now (mocks/pure). ⚠️ Jason area (portal/quote/balance) + money path → Jason review + money-review before merge. | M | 🟢 **INTERIM LIVE (S40 post-close, 2026-07-18): flag ON in prod** — flag-gated prequal-link CTA (approve modal + /approved) + "Flexible payments" portal section (order: price → financing → protection → trust; centered amber pill button), PRs #578/#579 merged + deploy-verified + live-checked. Eligibility: holiday/permanent/**bistro**, job total ≥ $1,500 (Naldo floor), balance $500–$25k. Env: WISETACK_FINANCING_ENABLED=true + WISETACK_PREQUAL_URL (Vercel + local). Full API still P0-blocked: **Naldo to send the drafted access request to Wisetack** (in S40 chat). | — |
| 155 | **Legacy Jobber quote migration -> quote tool (Naldo, S39 2026-07-15)** — one-time bulk migration of past clients' Jobber jobs into the quote tool as **draft holiday quotes + analyzer-seeded designs** (REBOOKING play; DISTINCT from #72 Jobber->Insights revenue-history). Pipeline: install roster (2025 Review Check sheet) + "2025 Installs" photos + Jobber **INVOICE** export -> per-property manifest -> `scripts/migrate-legacy-quotes.ts` runs the completed-install analyzer (seeds editable design geometry) + inserts a draft quote carrying the **real invoiced price** (NEVER engine-repriced). **MONEY = invoice "Total paid" is the source of truth** — the quote export proved wildly off both ways (Riza Bueser quote $15.7k vs $3.7k paid; $0-quote customers who paid $2k+). is_test pilot first, --live to scale; resumable. **BUILT + PR #556 (draft, GREEN, awaiting merge-go); live pilot runs on Naldo's LOCAL machine** (cloud has no secrets/photos). Manifest 114 ready, cent-exact; PII data gitignored (`scripts/legacy-migration-data/`). HELD: 5 past-due (4 McNulty + Mirelle's) + 2 $0-comped. | M | **SEND-READY (S40 2026-07-17): 114 real drafts, NONE sent; all 114 GHL contacts matched (111 email / 3 phone, 0 ambiguous, 0 missing) + linked.** Portal variant live (#558/#559). Send machinery #156-158 LIVE + E2E-proven. Remaining: Naldo's Neighbor drip workflows (GHL) + the outreach go. **▶ S28 (Jason) BATCH 2 MIGRATED LIVE: 19 skipped no-photo customers (+$28,001)** — Homes-2024 Drive photos (no-logo JPG preferred; 8 HEIC-only used with-logo jpg twins) + Naldo’s manifest.csv invoice money, dry-run 19/19 → 3-pilot is_test browser-verified → --live 19/19 OK. Pool now **131 send-ready drafts / $207,278**. **⚠️ SEND GATES for batch 2 (S28 wrap review): (1) the 19 have NO GHL contact linked — S40-style match pass owed; (2) the 8 with-logo hero photos are a FULL-WIDTH ad banner (not a small mark) — swap in converted no-logo HEICs before sending those 8 (Bobby Blejec, Brian Emery, Lourdes Mendez, Maggie Gahn, Mark Ahern, Albert Belcher, Nicholas Zebellos, Talonda Thomas); (3) Talonda Thomas bundles 3 properties under one Bellmore address/photo — staff eyeball pre-send.** **▶ S29: send-gates CLEARED — 19/19 GHL-linked** (`scripts/match-legacy-contacts.ts` PR #620, DB-verified still_null=0; reusable for batch 3) **· ad-banner photos accepted AS-IS (Jason) · Talonda PARKED (exclude from the wave).** 18 of 19 send-ready. Still parked: 20 clean-no-photo ($35.5k) + 16 flagged (dispositions; ask Naldo re the 2 comped names). Script PR #556. S40 post-close: manual **"Mark/Remove YLL Neighbor" toggle** on the admin quote detail (PR #572, live) for hand-built returning customers; first use = David Alfaro #1191 (flagged + GHL cards fixed; also noted by the concurrent S41). | — |
| 195 | **Marketing site: non-lead forms (Naldo S50)** — core SHIPPED + LIVE (#678/#682/#685/#686/#689/#697); follow-ups in **PR #782 OPEN** (GHL sync retry, staff handled-marker, nominee CRM contact behind a permission gate; migration already applied to prod). Original shortcode symptom is gone. | S | Residue of #152 (custom lead-capture embed replaced Gravity Forms sitewide). The plugin is deactivated ON PURPOSE, so WordPress prints the shortcode text raw to visitors: the footer 'Join Our Community' block on EVERY page (gravityform id 9), plus `/careers/` and `/intern-program/` (id 6), `/light-up-for-hope/` (id 7), `/newsletter/` (id 8). Fix = delete each shortcode or swap in the #152 embed; do NOT reactivate the plugin. Naldo saw this 2026-08-04 and chose to leave the plugin alone for now. **SHIPPED + LIVE (S50 post-close).** Replaced by a purpose-built non-lead path in the quote tool (PRs #678/#682/#685/#686/#689): own table, own API, five embedded forms, admin view. WordPress swapped over; Gravity Forms deactivated (REST namespace confirmed gone). Wrap-review fixes in #697. | - |
| 272 | **Marketing site: the main GET A QUOTE CTA dead-ends on the homepage (Naldo S50 post-close)** | S | `/get-a-quote/` 301s to `https://www.yulelovelights.com/` and then back to the homepage, so the main nav CTA, the homepage hero buttons and the footer 'Get Quote' link (7 instances on the homepage alone) all land the visitor back where they started with no form and no scroll. Cause is a **Rank Math redirection created 2026-06-26** pointing `get-a-quote/` at `/services/residential-holiday-lighting/`, already triggered 48 times; the observed chain ends at the homepage instead. NOT caused by the S50 work (the page itself is published and intact) and NOT fixed: removing a redirect the owner may have set deliberately is his call. Likely costing quote requests every day it stands. | - |
| 273 | **Marketing site: the header logo clips the hero heading at phone width (Naldo S50 post-close)** | S | At 375px the site logo image sits on top of the first line of the hero heading on Elementor pages with a text heading high in the hero: `/about-us/` renders as "BOUT US" with the A behind the logo, and the same happens on the town pages, `/blog/`, `/gallery/`, `/faqs/`, `/services/`, `/service-areas/`, `/careers/`, `/intern-program/`, `/newsletter/`, `/privacy-policy/`, `/terms-and-conditions/` and the 2026-08 landing pages. **PRE-EXISTING, NOT caused by the flash banner**: proven by dismissing the banner (sessionStorage `yllFbClosed`) and re-rendering, where the clipping is identical, because the banner shifts the header and the hero together. CORRECTION worth keeping: the close-review agent reported a dark navy bar hiding titles entirely, and a DOM occlusion check appeared to confirm it on 43 of 73 pages, but the header is transparent (`rgba(0,0,0,0)`) with no painted ancestor, so that check measured STACKING, not visibility. Only a screenshot settled it. NOT FIXED: moving the logo or the hero text on mobile changes the brand header on every phone visit, which is Naldo's design call. | - |
| 274 | **Site forms: no retry path when the GHL contact sync fails (Naldo S50 post-close)** | S | A `site_submissions` row that lands in `sync_status: 'error'` can never be re-driven. `/api/leads` has both a retry cron and a manual admin action; the non-lead path has neither, and `/api/admin/site-forms` is read-only. A transient GHL outage permanently loses that contact tag. Raised by the pre-merge technical lens, deferred deliberately: low volume, and the row stays visible with its error. | - |
| 275 | **Site forms: admin list has no handled state and no pagination (Naldo S50 post-close)** | S | `/admin/site-forms` renders every row identically with a 500-row cap, and `sync_status` only tracks the GHL sync, not staff follow-up. Nothing marks a job application or a nomination as actioned, so with more than one staffer there is a real chance of double-handling or losing one. Needs a product call on the workflow before building. | - |
| 276 | **Site forms: nominee PII persists in GHL via the staff alert email (Naldo S50 post-close)** | S | A Light Up For Hope nomination carries a THIRD party's home address, email and phone. They never become a GHL contact and never enter automation (tested), but the staff alert is delivered through GHL's conversations API, so those details persist in GHL as message content on the internal alert thread. The Telegram line was deliberately kept free of them; the email was not. Caught by the S50 wrap review, which also corrected the over-strong claim in the code comments (#697). Decision for Naldo: accept it (staff cannot act on a nomination without the address) or route that one alert outside GHL. | - |
| 196 | **Website forms can overwrite an existing GHL contact's name (Naldo S50)** | S | `syncSubmissionToGhl` (`src/lib/siteForms/siteFormService.ts`) calls GHL `upsertContact` with the submitter's name, email and phone. GHL matches an existing contact by phone OR email, so a submission whose phone matches someone already in the CRM MERGES into that record and **overwrites their first/last name**. PROVEN LIVE 2026-08-05: a test nomination submitted with the company's own number (631-517-0186) landed on contact `AhuSDYU81cioAAMWknx0` (Thomas Montalbano, a csv_import lead) and replaced his name and email; the name and the stray `hope-nominator` tag were restored by hand, his email was NOT recoverable and still holds the test address. Harmless for a returning customer submitting their own details; harmful for a mistyped digit, a shared household number, or a job applicant who reuses a relative's phone. OPTIONS: (a) never send firstName/lastName on these forms, so a submission can only ADD the tag to an existing contact, (b) only upsert when no contact matches, otherwise tag-only, (c) accept and document. NOTE the same upsert shape exists on the sales-lead path (`src/lib/leads/leadService.ts`), where overwriting is arguably intended — decide whether this row covers both. | - |
| 197 | **Approved-package desync class in the portal package filters (pre-existing #134 family; S49 wrap follow-up — row written by S34 per the #670 close-out)** — the #134 minimum-gate filter can still hide the package a customer ALREADY approved after a post-approval edit; the approval seed then collapses to an empty selection (the $0 booked portal). S49 hardened the same-price dedupe against this (approved-exempt + holiday-only, adapter.ts) but the gate filter keeps the class. Root fix at `buildApproval`: verify `sel.packageId` still exists in the post-filter packages; if not, seed from the frozen `selectedItemIds` (known-good ids) instead of `pickFallbackApprovalPackageId`. Legacy no-selection snapshots fall back safely today (cosmetic mislabel only). Money-adjacent → money-review on the fix. | M | 🔜 NEW (S49 via S34) | — |

### ↳ Raised 2026-07-28 (Jason S32) — quote/portal UX + ops batch (listed by Jason in-session, no particular order)
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 189 | **[PART (a) VERIFIED LIVE+EFFECTIVE S39 — prod EXPLAIN shows index scan 108ms (was 9-15s seq); part (b) noopReingest widening remains OPEN, Naldo-tagged] Inbox 9–15s residual = `getReopenCounts` seq scan + the reconcile cron's `ingested` flood (S33, closes #185's timing-verify).** The #185 auth fix HELD (7 of 8 timed queries sub-second warm) but `getReopenCounts` alone ran 8.9–14.8s every load and set the page total (S33 [inbox-timing] capture on prod, 3 authenticated loads). Root cause, DB-proven: `dashboard_activity` = 936,927 rows / 255MB, **935,610 of them `action='ingested'` across only 605 distinct items** — the every-minute reconcile cron re-logs 'ingested' for every unchanged OPEN item (~20,335 rows/day; the #110 W7-004 `noopReingest` guard requires `isResolvedStatus`, so open items fall through, `store.ts` planIngest ~line 149), and no index covers `action` → each of the 6 lookups = an 11.6s parallel seq scan (EXPLAIN ANALYZE, all-cache). **(a) SHIPPED S33:** partial index `dashboard_activity_operator_actions_idx (action, created_at DESC) WHERE action NOT IN ('ingested','escalated')` — serves getReopenCounts + listActivity, ~760 rows, flood-immune (migration `2026-08-03-dashboard-activity-action-idx.sql`). **(b) 🔶 NALDO (your ingest, your call):** widen `noopReingest` to unchanged OPEN re-ingests (or stop logging 'ingested' on no-change upserts) — the flood also bloats `inbox_items.updated_at` writes; and decide whether the 935k historical 'ingested' rows get pruned (255MB of backup/vacuum weight; per-touch history may be intentional — comment at store.ts ~1364 calls it "one row per reconcile touch"). | S | (a) ✅ S33 · (b) 🔶 Naldo | — |
| 193 | **Amendment-aware BOM/materials scoping — permanent (#192) + holiday (#160) (S33, #674 review of #192, documented follow-up — NOT built).** Both #192's `includedPermanentSidesFromSnapshot` (permanent BOM/ordering scoping) and the pre-existing #160 `selectedSceneItemIds` (holiday materials scoping) read ONLY the ORIGINAL frozen `approval_snapshot.customerSelection.selectedItemIds` — the signed selection `amend.ts` deliberately never overwrites (an amendment's diff lives in the separate `amendments[]` trail instead, by design — "the ORIGINAL signed snapshot is never overwritten," `amend.ts`). **Concrete gap:** a quote books front-only (`selectedItemIds: ['permanent-front']`), then staff amend the order to add the back side — `amendments[]` gets a `line_item_changes` entry recording the add, but `customerSelection.selectedItemIds` still reads `['permanent-front']` forever, so the scoped BOM/materials keep excluding the newly-sold back side on every future job-work-order/purchase-order/print/email pull until a human notices and hand-adjusts (same shape on a holiday order's amended item selection). **Not a #192 regression** — #192 inherited the exact same read pattern #160 already shipped with; parity, not a new bug. Fix direction: either walk `amendments[].line_item_changes` forward from the original selection to derive the CURRENT included set, or re-derive scoping from the live invoice/line-item state instead of the frozen snapshot. Documented in `includedPermanentSidesFromSnapshot`'s doc comment (`src/lib/permanent/bomFromQuote.ts`). | M | 🔜 queued | — |

### ↳ Raised S13 (2026-06-24, Jason) — code-grounded via a 7-agent investigation workflow
*Two more of Jason's S13 items mapped to EXISTING tasks (both updated above with S13 grounding): **logo watermark = #45** (answers: top-left, shows on portal); **real Google reviews = #22** (needs Naldo's data). The five genuinely-new ones:*
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 84 | **home.works ↔ tool scheduling connection (future placeholder, from #83)** — install **scheduling lives in home.works**, not our tool; the two connect later (our tool creates the Job → home.works schedules it → reports install date/completion back → triggers our invoice). Surfaces the dormant home.works `send`/`signed` routes in the UI. ⚠️ Un-drops/re-scopes **#16**. Boundary (where Jobs/Invoices are created: our tool vs home.works) TBD — see `docs/jobber-flow/SPEC.md` §2. | M | future; gated on #82 + home.works scoping | — |
| 85 | **Permanent / Glow365 recurring billing (deferred, from #83)** — year-round/permanent-install clients (Glow365) billed recurring (monthly/seasonal) vs a fresh seasonal one-off. **Naldo: tackle with the permanent-lighting side of the tool.** The Job object can carry `type=permanent` now; recurring billing is the later build. | L | deferred to perm-lighting work | — |
| 87 | **Customer-doc & notification ideas (from #83, not committed)** — (a) downloadable/emailable **PDF** of the quote/invoice/receipt; (b) **"on my way" / "install complete"** texts (with photos). **▶ S34: PART (a) SHIPPED + LIVE (PR #528).** Generated quote/invoice/receipt PDFs via `@react-pdf/renderer`, styled to MATCH the company's Jobber invoice format (coral header/meta card, RECIPIENT + SERVICE ADDRESS, line-items table, verbatim contract-agreement terms, reconciling totals stack, PAID/CANCELLED watermark). Money-safe: figures verbatim from `loadPortalQuote`/`getInvoiceDetail`; itemized breakdown sourced from the AGREED selection so it reconciles on partial approvals; quote PDF approved-only; adversarial-reviewed (5 findings fixed pre-merge). NO mockup image (Naldo deferred — text/format match only; if revived, the schematic-overlay is the efficient path). **PART (b) texts + email-attach still DEFERRED** (b's "install complete" photos = "text-only v1, no photos" per Naldo). ⚠️ new dep `@react-pdf/renderer` + additive `PortalQuote.customer.phone` mapping (Jason portal area). Device round pending: download the 3 PDFs on a real approved quote. **▶ S34 post-close:** a 5-agent verification pass found a LOW/cosmetic ≤2¢ itemized-stack rounding gap (Total always correct, no mischarge) → **FIXED in PR #543 (OPEN, awaiting Naldo merge-go)** — derives the displayed tax so `Subtotal − Discount + Fees + Tax === Total` to the cent + suppresses noise discounts, +3 regression tests, gates 3246. | S–M | 🟡 (a) PDF DONE + LIVE (PR #528, S34) · reconcile fix #543 pending merge-go · (b) texts deferred | — |
| 88 | **Permanent Lighting vertical (Omni/Ascend RGB puck-on-track, Naldo)** — the second service line. **🟢 LIVE (S23 go-live) — S22 built the ship-gate:** P1 pricing ($40/$35 flat, $2,500 approval gate, rates snapshot-frozen) · P3 Settings-adjustable rates · P4 builder (`PermanentSection` + `projectPermanentDesign` design→footage/corners/front-gaps + route validation) · P5 portal packages A/B/C/D by side · P6a approve (fees off + snapshot gate) · P6b-1 portal variant (lifetime-materials warranty copy, no takedown, holiday add-ons hidden) · **P2 BOM engine** (`lib/permanent/bom.ts`, golden-tested vs Naldo's 3 real jobs ±0.3–3%). **S23 GO-LIVE:** removed the inert `permanentEnabled` flag (#377 — it had ZERO consumers; the real gate is the quote's `service_type`), then hardened via a scoped review + a 7-dim adversarial hunt → **8 bugs fixed & LIVE (#378)**, incl. 2 money (invoice partial-selection tax under-bill [ALSO fixes holiday] + rebook stale rate snapshot) + the whole holiday post-approval confirmation page → permanent variant. **P7 shipped DRAFT (#381)** — operator BOM panel + printable order sheet (`bomFromQuote` adapter + `PermanentBomPanel` + print route, wires the dead-code `bom.ts`). holiday+event byte-identical. Spec: `docs/superpowers/plans/2026-07-02-permanent-lighting-vertical.md` (PR #332, parked for Jason) + `docs/permanent/BOM-DATA.md`. **P6b-2/3/4 SHIPPED LIVE (S24, PR #387 → master `0567a6c`):** the whole "Your Protection" warranty card Settings-editable + version-frozen · permanent scenes ANIMATE (portal-side stepper, editor-core untouched) + a CSS facade glow · **FULL** color picker (reuse holiday `LightColorPicker` + build-your-own = any color, "As Designed" default) + a SEPARATE effect picker (Solid/Chase/Fade, freezes) + Settings-editable `permanentSwatches` colors (own app_settings key; editor = parameterized `PortalSwatchEditor` on Settings→Quotes). Approve color unified w/ holiday; re-synced onto 37-commit-newer master + re-gated (vitest 2227) before merge; 3 adversarial reviews (1 LOW fixed). **REMAINING:** permanent-portal polish next session (Naldo feedback on effects + the facade-glow gradient/colors — tune `.portal-perm-glow` + `sceneColorsAt`/effect speeds + swatch defaults) · twinkle effect · Settings-editable effect list · P8 (#82 inventory, via P7's `costOverrides` hook) · merge #332 (Jason plan doc). Pairs with **#85** (Glow365 recurring billing). **▶ S28 (Naldo) → S22 (Jason) — permanent builder/design bug-batch (#436, MERGED → master `9235c74`):** Naldo's S28 built an all-4-sides projection (per-side `sideSource` auto/manual), 8″ spacing lock + beam toggle, walled the permanent design off from holiday, and an **AI auto-measure analyzer** — then handed #436 to Jason. **Jason REMOVED the permanent AI analyzer** (clean 3-commit revert: `lib/permanent/photoAnalysis.ts` + 29 tests + the analyze-route wiring + `applyPermanentAnalysis`; −904 lines) — KEPT the imagery-load (permanent still pulls Street View + satellite for a MANUAL draw → Refresh from design) + projection/spacing/beam/walling-off. **2 device-check bugs fixed:** hand-typed footage now survives Refresh (`mergeSide` checks `'manual'` before `ft>0`) + a **no-run warning** on all 4 side footage fields (bills footage w/ no matching design run). **Permanent design model CONFIRMED as-is (Jason, S22):** front drawn on the street photo, left/right/back typed from satellite → they bill as line items even with no run (line items come from the footage NUMBERS not the design; the portal is a front photo so sides/back aren't shown). Visual all-4-sides = a later rethink. **The event/highlighting analyzer (wreaths/garland/roofline/spritzers) is still UNBUILT** (taxonomy locked — a SEPARATE analyzer from the removed permanent one). | M→XL | 🟢 LIVE · P6b-2/3/4 (S24) · #436 permanent bug-batch + AI-analyzer-REMOVED (S22) merged; polish/event-analyzer/P8/#332 next | S22 |

### ↳ Raised S16 (2026-06-30, Jason) — task-intake batch (NOT building yet; captured for the ledger)
*Jason's S16 brain-dump, separated into discrete tasks. **#94 is HIGH PRIORITY** (propose-first); the rest are normal priority; **#96** is FUTURE. **#15** was reactivated from Shelved (its old row removed below). **Relay cluster** (must land byte-identical in the design tool, `C:\Users\Jason\Desktop\YuleLoveLights\Claude`): **#98 (likely) · #99 · #100 · #103**.*
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 96 | **Lighting-type customer portals — Christmas / Permanent / Event** (Jason S16 dropped "Bistro"). Same portal + same design, but **type-specific packages/copy** (e.g. an Event/wedding quote needn't show the 4 Christmas packages). Type is chosen **per-quote** — the Christmas/Permanent/Event buttons already exist on `/quote/new` but are inert; wire them. Builds on `quotes.service_type` (#58 Ph2) + permanent lighting (#85/#88). **▶ S22 — REALIZED by the two verticals (NOT separate portals — ONE adaptive portal branched on `service_type`: packages + copy + sections):** **Permanent (#88)** BUILT + LIVE-dormant (behind `permanentEnabled`); **Event (#96 core)** BUILT end-to-end on **PR #344** (`naldo/event-lighting-core`, gated dark behind `eventEnabled`, **UNMERGED** — Jason review + Naldo merge-go). Christmas stays the untouched default. **▶ S23: event WENT LIVE** — re-synced PR #344 onto fresh master (resolved event↔permanent + #377 seam conflicts) + **MERGED**, then **#379 removed the `eventEnabled` flag** (Naldo: always live, no toggle) → the Event service-type button is always on. Logic-layer E2E green (event rates · no accessories · bistro · 3-date schedule · holiday untouched). | L (epic) | ✅ **event LIVE S23** (#344 merged + #379 always-on); permanent BUILT (#377 removed `permanentEnabled`) | S16 |

> **✅ SHIPPED S18 (Jason) → moved to `task_ledger_archive.md`:** **#107** Full Yule ceiling headline + totals reorder + GHL card lifecycle (PR #298) · **#103** side-of-house F/B/L/R tag on c9+permanent strands, editor-core → design-tool relay `dbc42a5` (PR #299) · **#15** Street View move along the street, snap nearest pano (PR #301) · **#46** editor text-tool fonts loaded in the root layout (PR #302) · **#52** edit saved training examples inline — footage/difficulty + all detection types incl. railing/curtain (PR #304) · **#108 (new)** teach the analyzer to detect railings (PR #303; curtains stay editable-only, not AI-taught).

> **✅ SHIPPED S17 (Jason) → moved to `task_ledger_archive.md`:** **#101** editable portal swatches (data-drive `COLOR_SCHEMES` from `app_settings` + Settings→Customer Portal editor; #92 protected via approve-time colorIds freeze; PR #286) · **#102** custom $/ft per item-type (`*CustomRate` on QuoteInputs; PR #273) · **#104** click-to-edit line price — 5-PR epic (stable line-id thread → #90 closed → engine override → click-to-edit → roofline + #102 mutual-exclusion; PRs #289/#290/#291/#292/#296 + review-fix #295; LLM-council'd + adversarially reviewed) · **WW/Stake recommend** checkbox for manual footage (PR #294, #12). **The #90 sceneLinks reorder/swap residual is now CLOSED.**

### ↳ Raised S22 (2026-07-03, Naldo — event thread)
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 118 | **Event vertical — negative-gate audit (migrated from PR #388, S23 32-agent event review; PR #388 closed S34).** Original root cause: holiday-only seams coded `!== 'permanent'` instead of positive `=== 'holiday'`, so the LIVE event vertical inherited holiday behavior. **Partly addressed:** the HIGH roofline double-bill is FIXED (#439, `excludeRooflineId`); S25 event E2E fix-forwarded 3 portal bugs (holiday confirmation page on event, phantom rush/takedown add-ons, cosmetic labels). **STILL OPEN (verify each — intended vs holiday-inherited bug):** master still carries **15 `!== 'permanent'` negative gates** in `src/`; original review also flagged WW/stake footage silently dropped (under-bill), rush/discount shown-then-stripped-at-approve, holiday $1,000 min blocking small events, event dates never reaching the job, unvalidated event input block, holiday color-picker on event, + the never-run in-browser event E2E. Naldo's verify-before-fix call (per PR #388). **▶ S34 AUDIT DONE (5-agent recon workflow, Sonnet seam-classifier):** all 15 `!== 'permanent'` sites classified — **14 INTENDED** (permanent-only feature restrictions on the snowglobe hero animation/glow + the QuoteBuilder satellite/accessories effects; 2 are just code comments; 3 filter on `bulbType` not `service_type` = outside the rule's scope; the serviceMetrics permanent filter has its own event sibling). **1 LIKELY-BUG hardened (PR pending):** `InteractiveHero.tsx:456` package-D label enumerated all 3 non-holiday verticals so a 5th would silently inherit holiday's 'Custom' label → changed to positive `serviceType == null || === 'holiday'` (behavior-identical today). The S23/#388 money/customer bugs (double-bill, WW/stake under-bill, rush-strip, $1k min, color-picker) were ALREADY fixed by #439 + S25 — none reproduce on current master. **#118 RESOLVED.** **NOTE (2026-08-25, task_ledger row 402 cleanup): a DIFFERENT task also carries the number 118 — the ARCHIVED row (`task_ledger_archive.md`) is a booked-page holiday-variant bug fixed by PR #384; this one is the event-vertical negative-gate audit. Same number, unrelated tasks; not a duplicate.** | M | ✅ DONE (S34) — audited, no live bug; 1 latent label hardened | #118 |

### ↳ Raised S25 (2026-07-04, Naldo) — portal per-service-type polish + admin quotes UX (from the /admin/quotes walkthrough)
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 122 | **Add more completed-work photos to the portal galleries (rolling, Naldo supplies).** First batch LANDED S30 via PR #477: 21 photos wired (6 holiday → 17 tiles, 12 event, 3 permanent) + the per-type galleries + the gold serif cross-sell strip (#121). Naldo will supply MORE photos later (all types welcome; permanent especially thin at 3) — drop them in a folder, convert to 1600px webp under public/references/, append to the lists in mockQuote.ts. STAYS BACKLOG until Naldo says the photo set is complete. | S | 🟡 backlog · content (rolling) | — |

### ↳ Raised S21 (2026-07-02, Naldo) — found LIVE on prod during the #83 trial walkthrough; NOT yet built (S22 batch)
*(Numbered #111–#115: Jason concurrently claimed #109/#110 in his S20 audit-plan work — renumbered at the S21 close to avoid the collision.)*
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|

### ↳ Raised S18 (2026-06-30, Naldo) — scoped, NOT building now
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|
| 106 | **PWA (Progressive Web App) — make the tool installable + app-like.** Scoped S18 (Naldo: "not for now"). Add a **manifest** (name/short_name/icons/theme+background color/`display:standalone`/`start_url`), a **maskable icon set** (192/512), and a **service worker** (app-shell precache + offline fallback page). **Phase 1 = installability + shell caching:** home-screen icon · full-screen (no browser chrome) · launch ~1–2s vs ~8–15s cold · near-instant cached repeat-loads · **offline-READ** on flaky job-site signal. **Phase 2 (optional) = Web Push:** customer-viewed / deposit-paid → staff phone even when closed (needs a push-subscription store + VAPID keys; **iOS 16.4+** only). **Wins = field usability** (icon, full-screen, cached loads, offline-read, push). **Out of scope / decisions:** **NOT the App Store** — that's a separate **Capacitor** wrap task (~1–2wk + Apple $99/yr); offline = **read shell only** (live quoting needs Supabase; true offline editing = background-sync, out of scope); heavy compute (the **canvas editor** math) is **unchanged** — PWA speeds launch/loads, not compute. ⚠️ Touches **SHARED** files (`next.config`, `app/layout` metadata/manifest, `public/` icons) → coordinate with **Jason**; Next 16 has native metadata/manifest support (read `node_modules/next/dist/docs` before building). Review icon/splash **branding with the designer**. **Effort ~1 week** (manifest+icons ~1d · SW caching ~2–4d · push ~2–3d optional). | M | 🔵 FUTURE · scoped S18 · SHARED + Jason + designer | S18 |
| 251 | **🔴 A stale HighLevel contact pick re-pointed an APPROVED quote to a different customer (Jason S36) - LIVE INCIDENT** | M | **Real incident, 2026-08-11, corrected by hand the same day.** Staff opened quote #1276 (richard arroyo, permanent, created 13:26), then opened Sharon McDonough's approved quote #1173, tagged it NCE and clicked Calculate at ~14:14. The save **re-pointed #1173's `customer_id` from Sharon's record (`2e125f04`) to Richard's (`3a60236b`) and rewrote its `highlevel_contact_id` from Sharon's `ApjNwz1PrKuLeumSj3BM` to Richard's `5SElcFe2bUZIVU43XH5k`** - while the denormalized `customer_name`/`email`/`phone` on the row stayed Sharon's, so the corruption was invisible on every screen. The NCE tag then propagated onto **Richard's** customer record (`is_nce = true`, updated 14:14:56), wrongly flagging a non-barter customer. Sharon's $7,051.34 approved quote was attributed to the wrong person for ~40 minutes. **Corrected under Jason's named authorization:** #1173 repointed to Sharon + her GHL contact, `deposit_amount_usd` corrected 2276.78 -> 2820.54 (it disagreed with its own frozen approval snapshot), and Richard's `is_nce` set back to false. A tree-wide sweep for the same signature (quote `customer_name` vs linked customer name) found only one other mismatch, benign (a nickname, matching email). **This is the failure class #214 was built to close** ("a stale prefill hl survives a re-pick and can overwrite the WRONG customer") - so either that fix has a hole or this is a different door into it. **Investigate before assuming. DONE = a written answer to 'does #214 have a hole, or was this a different door in?' A conclusion of "no code change needed" closes this row legitimately.** The detection sweep suggested below is a candidate for a FOLLOW-UP row, not required scope here. Not yet checked: whether Richard's GHL contact received a stray NCE tag during the window (no GHL tooling was available in-session). Detection idea: the sweep query above is cheap and would have caught this immediately - consider it as a guard or a digest check.  **[ANSWERED + CLOSED S55, 2026-08-27]** The row asked: does #214 have a hole, or was this a different door? ANSWER: a DIFFERENT DOOR. #214 governs identity RESOLUTION INPUT only and explicitly never writes the `highlevel_contact_id` column (its own doc comment on updateQuote's hlContactId param, merged d34b86a1 on 2026-08-06 - five days BEFORE the incident). The Sharon/Richard write went through two paths that had no approval freeze at all in Aug 2026: updateQuote's customer_* / customer_id reattach, and the attach route's own hl-id write. Both are closed now - #839's CAS identity write, row 338's sticky wasEverApproved signal, and Jason's 2026-08-20 atomic-identity ruling that makes the whole attach endpoint a no-op past approval, with an audit trail (row 326). RESIDUAL SPLIT OUT rather than left here: the freeze covers POST-approval only, and re-running this row's own sweep on 2026-08-27 found a live PRE-approval instance (quote #1304) - row 425. The sweep-as-a-guard idea this row suggested is row 426. The stray-NCE question is moot: the incident's own correction set is_nce back to false the same day. | - |
| 258 | **Prove the call_commitments upsert is actually atomic (copilot)** | S | #217 slice 1 closed a TOCTOU race by moving the resolved-commitment guard into a Postgres function (`0021_call_commitments_upsert_fn.sql`): `select ... for update` over the transcript's rows, decision and upsert in ONE transaction, one RPC. The unit suite CANNOT prove that. It asserts the CONTRACT instead (`persistCommitments` makes exactly one `rpc()` call and zero `.from('call_commitments')` calls, so a regression back to a two-step select/upsert fails loudly) plus the rejection path — stated openly in the test file header rather than letting a contract test masquerade as proof of atomicity. What is missing is an INTEGRATION test against live Postgres: two concurrent sessions, one running the RPC and one running an `update ... set status='cleared'`, asserting the loser is rejected rather than silently applied. Worth doing before slice 3 ships its verify job, because that is the thing that makes the race reachable. Why it matters: the bug being guarded rewrites a RESOLVED commitment's text while its settled status stays put, so a rep's record shows a promise kept that never was. | — |
| 257 | **Wire the commitment extractor to a trigger + surface its refusals (copilot)** | S | #217 slice 1 shipped `src/lib/commitments/` (extractor, time anchor, persistence, backfill) but deliberately wired it to NOTHING — no route, no cron, no caller anywhere in `src`. It is a library waiting for a consumer, so no commitment is being extracted in prod today. Two loose ends land with whoever wires it: (a) `backfillCommitments`' `refused` counter is currently unreachable, because its own candidate selection already filters out any transcript with an existing row — it only fires for a caller that invokes `persistCommitments` directly, which nothing does yet, so it reads 0 forever and nothing consumes it; (b) a transcript with no rep commitments never gets a row, so the cheap-skip optimisation cannot tell 'already processed' from 'never processed' and re-runs extraction (re-billing the model) on every manual backfill. Neither is a correctness bug — persistence is idempotent either way — but both become real the moment this runs on a schedule. Decide the trigger (piggyback the existing hourly learnings batch vs a faster per-transcript hook) as part of the same piece; the plan notes a 6:51 PM promise extracted at 8 PM is fine for a digest and marginal for a same-evening ping. | — |

| 279 | **Crew authentication: ONE shared login store for the Quote Tool AND the Operations Hub (Naldo, 2026-08-16)** — the ruling: create accounts in a shared identity store so the SAME login works in both apps, rather than two separate auth systems. This unblocks the Flow B crew routes (row 280), which have no way to identify a caller today: `requireOperator` is for staff on the dashboard and the only existing `/api/ops` route is a cron endpoint behind `CRON_SECRET`. ⚠️ **SHARED IDENTITY IS NOT SHARED AUTHORIZATION, and this is the whole risk of the design:** a crew account will live in the same store as operator accounts, which hold customer PII. Crew must NOT satisfy `requireOperator`/`requireAdmin` — `roleOf` reads `app_metadata`, so crew accounts need a role that those helpers reject, and that rejection needs a test. Also per the standing AGENTS.md pitfall, any new crew-reachable route must be added to `operatorGate`'s allowlist in the SAME PR and verified while logged OUT, or `AUTH_GATE_ENABLED` serves it the operator login shell instead. | M | 🟢 SHIPPED S58 post-close — crew auth + the role-aware perimeter; migration APPLIED to prod 2026-08-16 and verified by a real query | Naldo |
| 280 | **The three Flow B routes: `POST /api/ops/v1/jobs/{id}/arrive` · `/depart` · `/complete`** — the library behind them shipped in S58 post-close (mislabelled S59 until the 2026-08-19 correction) (`src/lib/jobSegments.ts`, PR #779); these are the HTTP surface. BLOCKED on row 279 (crew auth). **`/complete` ruling (Naldo, 2026-08-16): it sets `jobs.status = 'installed'` using the EXISTING quote-tool machinery — `setJobStatus` + `canTransition` — and introduces NO new "Flow C operational completion" concept or vocabulary. Make it match exactly how the quote tool already works.** `to_schedule → installed` and `scheduled → installed` are both already legal transitions, so no status-model change is needed. Depart still requires its `stoppage_reason`; `/complete` is depart-with-`completed` plus the status move. | M | 🟢 SHIPPED S58 post-close — arrive/depart/complete live behind requireCrew | Naldo |
| 281 | **Midnight auto-close (America/New_York) for forgotten days (Naldo APPROVED 2026-08-16)** — the contract (§4) requires it and it is not built: "Midnight auto-closes forgotten days AND is independent of the Hub's Placement Run midnight reconciliation." Closes a forgotten shift, and any break or job segment still open under it, at the midnight boundary in the BUSINESS timezone (never UTC — the repo has already been bitten by UTC-vs-ET day windows). Must be idempotent (a re-run closes nothing twice) and must flag what it touched for review rather than silently tidying, so a forgotten clock-out cannot inflate hours unseen. Also the natural home for the `open_break`/`open_segment` sweep tracked in row 278 — consider building them together. | S | 🟢 SHIPPED S58 post-close — ET midnight auto-close + cron | Naldo |
| 282 | **Regenerate `FULL-SCHEMA.sql` by its own stated procedure (Naldo APPROVED 2026-08-16)** — found by the S58 post-close six-lens review (mislabelled S59 until the 2026-08-19 correction): the file no longer is what its header claims. Tables are missing from the file entirely, and more are missing from its header roster and table counts, so running it on a fresh project does NOT reproduce prod. Cause is incremental hand-patching, one table per PR (this session did exactly that twice). Fix by reconciling every dated migration in date order and refreshing the header roster, the count, and the RLS-posture bullet in the same pass — NOT another hand-patch, which would make a file that already misleads look more trustworthy. | M | 🟢 SHIPPED S58 post-close — regenerated, 81 migrations / 37 tables, header truthed up | — |
| 283 | **Success metrics for P4P — the one A6 product question with no ruling (A6 item 5)** — skipped rather than deferred when the rest of A6 was answered 2026-08-16. Nothing in the plan defines how anyone knows this worked. Candidates already listed: efficiency trend, floor-top-up frequency, crew retention, hours captured without office correction, payroll time saved. Does not block Phase 2 or 3; does make "did this work" unanswerable at the pay flip. | S | 🔲 open — needs Naldo | Naldo |
| 284 | **Employment attorney: review the comp-plan wording (A6 item 9) — PARKED, explicit NON-BLOCKER (Naldo, 2026-08-16)** — one document, reviewing the wording that makes performance pay conditional (A3 condition 1). Naldo's call: handle it another time, do not treat it as gating Phase 2 or Phase 3. Recorded so it is not lost. Still wanted before the actual pay flip, since A3's forfeiture-not-deduction design is the legal spine of the whole scheme. | S | ⏸️ parked (Naldo) — non-blocker | Naldo |
| 286 | **🔴 CRON_SECRET unset — the copilot ran dark 8+ days (config, OWNER ACTION)** | S | 2026-08-08 01:05Z to at least 2026-08-17: every copilot cron returned 503 and ZERO sales calls were captured. `verifyCronRequest` → `verifyBearerRequest(request, process.env.CRON_SECRET)` returns 'unconfigured' when the secret is absent or under 16 chars, and the route maps 'unconfigured' to 503 — which rejects Vercel Cron itself, not just unauthenticated callers. PROVED against prod rather than inferred: an unauthenticated request AND a request carrying a deliberately wrong bearer token BOTH return 503; a configured secret would answer 401 to the wrong token. **OWNER ACTION: set CRON_SECRET (16+ chars) in Vercel → yll-call-copilot → Production, confirm CRON_ENABLED='true' (routes no-op with {ran:false} without it), then redeploy.** Not a credential this assistant will set. Backfill is safe: `resolveSyncWindowStart` returns the stored `last_synced_at` unclamped, so the first healthy run pulls everything since 2026-08-08. Copilot PR #48 made the 503 name the missing variable (it and the 401 previously returned an identical "Unauthorized" body, which reads as a caller problem — that ambiguity is what let this run dark for eight days). STILL MISSING: a staleness alert on `recording_sync_state.last_synced_at`. | — |
| 285 | **Payroll provider or accountant: the overtime regular-rate formula (A6 item 10) — PARKED, explicit NON-BLOCKER (Naldo, 2026-08-16)** — the exact OT regular-rate treatment including performance pay (weighted-average vs supplemental-premium), how a multi-day job crossing a week allocates, whether the weekly export meets NY pay-stub content rules, and spread-of-hours pay (applies when a workday spans more than 10 hours, which drive time plus multi-job days could trigger). Naldo's call: handle it another time, not gating. Still wanted before the pay flip. | S | ⏸️ parked (Naldo) — non-blocker | Naldo |
| 296 | **Crew login creation + cron misconfiguration is now visible (S58 post-close; the row title said S60 until S61 cleared it)** — `crew_members` had 4 rows and ZERO linked logins, and nothing could create one: `validateNewUser` hard-rejects any role outside admin/operator, so the accounts screen could not make a crew account. Added `POST/GET /api/admin/crew-accounts` (requireAdmin, never dormancy-bypassed) and a "Crew logins" panel on Settings → Accounts. Deliberately a SEPARATE door from `/api/admin/users` rather than loosening that guard — one validator between two populations with different privileges is how someone eventually hands crew the operator surface; a test asserts the operator guard still refuses 'crew'. **Also:** all 11 crons moved to a shared `cronDenial()` that returns **503 naming the variable** when CRON_SECRET is unset vs 401 for a wrong token. Previously identical, which is exactly how the copilot ran dark 8+ days (row 286). ⚠️ NEAR MISS: the first draft also treated any secret under 16 chars as unconfigured, which would have 503'd ALL ELEVEN CRONS if the real prod secret were shorter — caught by 22 failing tests; only absent/empty counts now. | M | 🟢 SHIPPED S58 post-close | Naldo |
| 297 | **Telegram crew time capture — the crew-facing surface the ledger was missing (S58 post-close; the row title said S60 until S61 cleared it)** — until this, the entire time ledger was unreachable by an actual crew member: tables, math and routes existed with nothing a person could touch. Commands: `in` · `out` · `break` · `back` · `arrive 1042` · `done` · `depart <reason>` · `status` · `help`, with the synonyms crew actually type. **Parsed DETERMINISTICALLY and never through the LLM tool layer** — a misread inventory count is recoverable, a misread clock-out is somebody's paycheck. Runs before the addressed-gate too, because in a crew group "in" and "back" are how people talk and requiring an @mention would drop punches silently; safe because the parser only fires on exact matches. `depart` REFUSES to default a reason (a defaulted `completed` marks a rained-out job finished and poisons the learning signal); `completed` is deliberately not a depart reason since `done` is that path and also moves the job status. Identity comes from `crew_members.telegram_user_id`, never the message. 37 tests. | L | 🟢 SHIPPED S58 post-close | Naldo |
| 298 | **P4P Phase 3 scheduling — foundation SHIPPED, drag-drop calendar REMAINS (S58 post-close; the row title said S60 until S61 cleared it)** — new `job_assignments` table (job × crew member × calendar DATE, not a timestamptz: "who is on this job on the 22nd" is a calendar question and an instant reintroduces UTC-vs-ET drift), `src/lib/scheduling.ts` with the capacity derivation the plan insists be stated (job BH divided across the crew assigned to THAT job on THAT day; a job scheduled with nobody assigned is UNASSIGNED LOAD, not zero; jobs with no estimate are counted separately rather than treated as free), `GET/POST/DELETE /api/ops/schedule`, and a dispatch **day view** at `/admin/schedule`. Capacity is the placeholder-rate guardrail's first real consumer: hours are read through `readLaborPlan`, so a placeholder figure arrives tagged and the UI says so. **STILL TO BUILD:** the drag-drop week/month calendar. The day view and the calendar share the same capacity derivation, so it only had to be right once. ⚠️ `migrations/2026-08-18-job-assignments.sql` is **APPLIED S61 (2026-08-20), verified by query — 7 columns, 5 indexes, RLS on with 0 policies, 1 trigger, 0 rows. The note below is the historical reason it lagged** — the Supabase MCP lost its token mid-session; the schedule page errors until it is applied. | L | 🟡 foundation shipped S60 · calendar remains | Naldo |
| 299 | **S58 wrap-review fixes — a crew→admin escalation, a DST payout bug, and a bot help collision (PR #812 — MERGED S61 2026-08-20, after a four-lens review; its DELETE-guard gap is PR #818)** — the four-lens wrap review found three real defects in code merged earlier in the same session. **(a) HIGH privilege escalation:** `PATCH /api/admin/users/[id]` could promote a CREW login to admin — `roleOf` collapses 'crew' to 'operator', so the change read as an ordinary operator promotion and the metadata spread then erased the `role:'crew'` marker permanently; the account gained `/customers` and every PII surface while `crew_members.auth_user_id` still pointed at it as someone's pay identity. PATCH now 403s a crew target. ⚠️ **DELETE shipped UNGUARDED** — #812's body claimed both, and only PATCH had it; three of four S61 lenses converged on the gap independently, and the fix is in PR #818, NOT in #812. **(b) MONEY:** `etMidnightAfter` probed the UTC offset at NOON and applied it to MIDNIGHT, so on the two DST transition days the forgotten-shift cap was an hour early (spring, UNDERPAY) or an hour late (fall, OVERPAY); fixed by converging on the offset actually in force at the boundary. The old test was named "handles the spring-forward day" but computed the midnight AFTER it — an ordinary day — so it passed while the real case was broken. **(c)** the crew parser claimed `help`/`commands`/`?`, the existing bot's own help words, so every user in every chat got crew time-clock help instead of the real help; and unaddressed non-crew chatter in a group drew an unsolicited reply, breaking the bot's "answers only when spoken to" contract. Also removed dead `safeEqual` imports from 10 routes (lint warnings 31→20) and made 17 vacuous `if (out.handled)` test guards assertive. | M | 🟢 SHIPPED S61 — PR #812 merged 2026-08-20 and live; its DELETE-guard gap rides in PR #818 | Naldo |
| 318 | **Nothing in the app can link a crew member's Telegram, so the time clock is unreachable by every crew member (S61)** — `handleCrewTimeMessage` resolves a punch through `getCrewMemberByTelegramUserId`, i.e. `crew_members.telegram_user_id`. That column was written by NOTHING: `insertCrewMember`/`updateCrewMember` had zero callers repo-wide, and the seed migration's backfill joined `bot_users` by display name — `bot_users` is empty, so all 4 crew rows got NULL. Creating a crew login does not help: it sets `auth_user_id`, a SEPARATE identity used only by the `/api/ops/v1/jobs/*` HTTP routes. Shipped: `PATCH /api/admin/crew-accounts` (admin-only, digits-only validation so a pasted @handle cannot be stored as a link that silently never matches, null to unlink, 409 on a duplicate via a new `TelegramUserIdTakenError`), `telegramUserId` added to the GET shape, and a Telegram section in Settings → Accounts → Crew logins that shows “No Telegram — cannot clock in” until linked. Wires the previously-inert `updateCrewMember`. ⚠️ **The four-lens review of the PR then found the panel had NEVER been rendered** — `CrewLogins` was imported by `src/app/settings/accounts/page.tsx` and never used in JSX, so row 296's crew-login creation shipped DEAD too and the S61 brief's “Settings → Accounts → Crew logins” step could not have worked. Master's own lint had been reporting it (`'CrewLogins' is defined but never used`) among 31 untriaged warnings. Now rendered, admin-only. Second HIGH: linking is necessary but NOT sufficient — the webhook drops any chat not in `TELEGRAM_ALLOWED_CHATS` before the dispatcher runs, so a 1:1 DM stays silent until that id is added there; a crew group already on the list works immediately. | S | Found while attempting the S61 brief's “create a crew login, then have that person message the bot ‘in’” step — it would have produced silence. | — |
| 278 | **Build the `open_break` exception-queue sweep — two ways a break can sit open forever with nothing pointing a human at it (S58 wrap review, technical + crew lenses)** — (a) **TOCTOU:** `startBreak` checks "is this shift still open" with a plain SELECT, not a conditional write, so a break inserted in the microseconds after a concurrent `clockOut` is orphaned open permanently — the shift is closed, so nothing will ever call `closeOpenBreakForShift` for it again. (b) **Failed auto-close:** if `closeOpenBreakForShift` throws during clock-out, the error is logged and the clock-out still succeeds (deliberate — never block someone going home), but the break keeps `ended_at = null` AND `auto_closed = false`, so it never enters the very queue `auto_closed` exists to feed. **PAY IS NOT AFFECTED in either case** — an open break clips to the shift's recorded clock-out in `paidSecondsForShift`, so the numbers self-heal; the damage is purely visibility (`getOpenBreak` reports "still on break" indefinitely). The real fix is the sweep the contract already specifies (`OPERATIONS_HUB_CONTRACT.md` §5 exception queues, `time_exception_type = open_break`), which is unbuilt — plus the contract's midnight auto-close, which would catch both cases as a side effect. Both gaps are written into the code's own doc blocks so a reader hits them before this row. | S | 🟢 SHIPPED S58 post-close — GET /api/ops/time-exceptions, 4 exception types, read-only by design | Naldo |

## ⏸️ Pending / needs Naldo (blocked — not active dev)
| # | Task | Size | Notes | Old # |
|---|------|------|-------|-------|

> Note: #8 fully shipped through Stage C/C4 (only **C6** = per-detection confidence deferred); #9/#10/#12/#17/#26/#27/#35 also shipped. **#32 DONE** (Settings-menu epic — all 3 phases shipped S9). **Active dev = the 🔜 Backlog** (#13/#14/#29). **Naldo S4 SHIPPED (all merged + LIVE on prod):** #69 contact-copy · #67-retune→20 · #44 widen+`F` · #51 satellite view · #68 view-receipt · #45 watermark→top-right · #76 Customer-Portal tab · #74 activity feed · #75 "Interested" signal · #77 quote-ID · **#49 Build-your-own** + the portal Light-Color chip iteration (trimmed Red/Green/Purple/Blue solids · "As designed"→"Staff's pick" · added **Cool White #cee1e7** with the `withMissingBuiltins` palette-propagation fix; the interim 3-white attempt #131 was reverted #132). **#63 handed to Jason** (shared core + relay). ✅ **Design-tool relay DONE (S14):** Cool White #cee1e7 + the `cool-white`→"Pure White" relabel mirrored into the design tool's palette (`client/src/editor/colors.ts` at `d2389df`). **S13 SHIPPED (Jason):** #67 auto-dim · #71 yardstick · **#73** multi-select-drag fix *(ex-#72 — renumbered to avoid Naldo's Jobber #72)* · #70 trust section was Naldo-built; the re-raised **#45/#22/#59** + the **#40** discount epic + the **#58** dashboard core all shipped Naldo-side. Sizes are Jason-confirmed (don't silently adjust). No external blockers outstanding (#17 prices all in). **#15** (Street View camera along the road) **shelved S9** → see 🗄️ Shelved. The **#10 operator-default** idea is settled as **won't-do** — the portal always opening "As designed" is the intended behavior (Jason, S9).
