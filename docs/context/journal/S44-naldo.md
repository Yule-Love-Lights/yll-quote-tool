### Naldo S44 — LIVE WORDPRESS ONLY (zero repo code): deleted the legacy Elementor/GHL "Get A Fast Quote" sticky bar site-wide; our `lead-form.js` embed is now the ONLY sticky bar and the ONLY quote form on yulelovelights.com (2026-07-20)

> One conversation, no repo commits beyond this close. All work was on the live WordPress site, driven through the real Chrome (logged-in WP admin). Elementor revision history is the revert lever. master unchanged at `f33d2dd`.

- **What was there (site-wide, not just the homepage):** UAE Header & Footer template **"Sticky Form" (post 2736)**, pulled into every page by `[hfe_template id='2736']` inside the **Footer** template (post **1123**) → container `c1fbbc0` → shortcode widget `f4f86ba`. Template 2736's only content was container **`a0fa6d1` / `#stick_form_section`** (Elementor "Sticky: Top", `position: fixed; top: 0`). Inside it: heading `ae1d5b2` "Get A Fast Quote / Takes Only 5 Seconds" · HTML widget **`91466c1` = a GoHighLevel HOSTED form iframe** (`lights.yulelovelights.com/widget/form/CvW7WYzgx40GpMsEEstk`) · shortcode widget `9f2ae7f` (`#popup_form`) · mobile-only container `f1ab37b` → buttons "Get A Quote" / "Call Us Now".
- **⚠️ The trap that changed the plan (caught in recon, before any edit):** our own sticky bar's placement marker `<div data-yll-lead-form="sticky">` lived **INSIDE** the container slated for deletion (in `9f2ae7f`). `renderSticky()` in `public/lead-form.js` hides that marker and appends the real bar to `<body>` — so the site was showing **two stacked sticky bars**, and deleting the container as briefed would have killed OUR bar too. Surfaced it; Naldo's call: **kill the GHL bar, keep ours**.
- **The edit:** created plain (non-sticky) container `dbdf40c` + shortcode widget `68ad2b0` holding a **byte-identical** copy of the marker (115 chars, both lines verified equal to the original), then deleted `a0fa6d1`. Published; **NitroPack auto-purged** ("Reason: Updated Elementor Header & Footer Builder 'Sticky Form'", 0 stale pages).
- **Verified logged out** (separate browser, no `wpadminbar`) at **1280** and **375** widths: no `#stick_form_section` · **zero** GHL iframes · exactly **one** fixed element (`.yll-lf-sticky-outer`) · mobile row = `631-517-0186` + "Get a Quote" (one-for-one replacement of the deleted mobile buttons) · footer + page body intact · no console errors. Re-checked on `/services/permanent-lighting/` to prove the site-wide path.
- **⚠️ Lead-capture finding worth knowing:** that GHL hosted form was capturing sticky-bar leads **directly into GHL, bypassing `POST /api/leads` → `website_leads`** and the PostHog funnel. So since #152 the site ran two competing quote forms stacked on each other, and an unknown slice of leads never hit our pipeline. Now single-path. **Worth a look in GHL for submissions to form `CvW7WYzgx40GpMsEEstk` that have no matching `website_leads` row.**
- **⚠️ S34's fix did NOT hold — superseded.** The S34 Customizer rule `.elementor-element-a0fa6d1 { display: none !important; }` is **still live** in Additional CSS, yet the bar was rendering today on a fresh, logged-out, NitroPack-purged page (override mechanism unconfirmed). Deleting the element supersedes it. **Cleanup left for the next Customizer pass: that rule is now dead and safe to remove.**
- **Form surface at close (answers the standing "which form is where" question):** homepage = **our embed only** — `data-yll-lead-form` markers `bar` (in `#cta_section_form`), `full`, and `sticky`; **zero** GHL forms. One unrelated `form.elementor-form` (email-only signup) remains further down the homepage. **`/get-a-quote` 301s to the homepage** — it is not a separate page.
- **Gates:** N/A — **zero repo code touched** (the only worktree diff was a pre-existing `.claude/settings.json` change, not mine). Last app-gate baseline stands at S42 vitest **3635**.
- **Revert:** Elementor revision history on template **2736** — restore the revision from before this save.
- **NEXT:** next fresh conversation = **S45**. Optional cleanup: drop the dead `a0fa6d1` Customizer rule; reconcile GHL-form submissions against `website_leads`.

