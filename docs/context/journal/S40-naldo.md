### Naldo S40, Legacy migration #155 END-TO-END: 114 drafts LIVE + portal rebook variant + GHL Neighbors send path (#156-158) SHIPPED + contacts matched — SEND-READY (2026-07-16/17)

> Naldo's LOCAL machine (cloud S39 handed off the branch). One long conversation: pilot -> Naldo's revisions -> 114 real drafts -> the whole send-side buildout (#156/#157/#158, PRs #558-#561) -> E2E send proof -> contact matching + GHL card values. First bullets below were written mid-arc by the observing S39 cloud session; the "S40 CONTINUED" block is the completed record. Next fresh conversation = **S41**.

- **#155 LIVE:** 114 real draft quotes inserted into prod (holiday vertical). NOT sent — held for the send-side buildout below.
- **Pilot-driven revisions (commit `121d981`), all Naldo's calls after seeing real output:**
  - **No analyzer** — auto-drawings on top of real lit installs looked bad; the design is now the CLEAN completed-install photo (the photo IS the display). `ANTHROPIC_API_KEY` no longer needed.
  - **ONE bundled line item** at the pre-tax net (discount folded in, tax verbatim, total = what they paid) — replaces the per-item spread; per-item toggles were inviting $0 recomputes.
  - **`legacy_rebook=true` stamp** on every quote (post-insert UPDATE) -> portal renders a rebooking variant (color-change copy, no daylight toggle, read-only items). Column applied to prod FIRST (S39 flagged the migration-order + orphan/dup risk; handled migration-first, so all 114 stamps succeeded).
- **▶ S40 CONTINUED (same conversation, 2026-07-16/17): the ENTIRE send-side shipped + #155 is SEND-READY.**
  - **Portal rebooking variant LIVE (PRs #558 + #559, merged + deploy-SHA-verified + live-clicked):** legacy quotes show ONE hero tile **"Last Year's Design"** (no tier ladder, no Build-Your-Own), read-only item list ("same as last year"), rebook copy on the Light Color band ("Want to change up your lights this year?"), no daylight toggle; **rush / takedown / Sept-Oct early-install stay LIVE as upsells** (r2 correction after Naldo's review; live-verified repricing $971.14 -> $1,134.26 -> back). Freeze logic = pure seam `frozenMutatorGroups` (items|fees|appearance groups; booked `locked` behavior byte-identical). **Tax decision (Naldo): TODAY'S rate stands** — item price = last year's exact net, tax recomputes at live 8.75% (Bykov $970.02 paid -> $971.14 shown; drift = rate change only).
  - **#156 GHL Neighbors routing LIVE (PR #561):** ALL 6 sync call sites (send / valor deposit webhook / decline / staff-decline / installed / manual attach — the attach site found by grep-reconcile, not recon) gate on `legacy_rebook === true` FIRST -> **Neighbors pipeline** `TIYqklVJ349F5heaSkCs`: sent/entry=**Bid Sent** `9ada8238-...` (stage created this session via Chrome), depositPaid=Booked, installed=Installed, declined=Declined-for-2025. New **"Neighbor Quote Link"** contact field `FIxalRp17zsEZ9zdaTH9` (created via API; env `HIGHLEVEL_CONTACT_FIELD_QUOTE_LINK_NEIGHBOR` in both .env.local + Vercel); when unset the stamp is SKIPPED, never falls back to the holiday field (drip-collision guard, tested both stamp + clear).
  - **#157 inbox exclusion LIVE (PR #560):** `listOpenItems` (single chokepoint: inbox page + /api/inbox + nav counts) drops legacy-quote items at query time; +200 over-fetch buffer so the same-timestamp 114 can't blank page one; totalOpen adjusted. **Inbox ONLY per Naldo — dashboards/stats still count legacy quotes.** Re-enable = flip `EXCLUDE_LEGACY_REBOOK_FROM_INBOX` (inbox/store.ts). Live-verified: inbox "Quote 0".
  - **#158 "YLL Neighbor" badge LIVE (PR #560):** shared `YllNeighborBadge` (sky pill) on /admin/quotes rows, quote detail (renamed from "Legacy rebook"), builder header. Internal-only.
  - **E2E SEND PROVEN on prod** (real non-test quote to Naldo's own contact `pFPrGLxKQ7zk1GiYc4gS`; is_test quotes SIMULATE GHL so a test quote can't prove routing): opportunity landed **Neighbors -> Bid Sent** ($1,305, source Quote Tool), Neighbor field stamped, **holiday field untouched**, Christmas Lights clean. All 3 artifacts cleaned after (quote, GHL card, field stamp).
  - **Contact matching COMPLETE: 114/114 linked** (111 exact-email, 3 phone+name; 0 ambiguous, 0 missing — read-only report first, then Naldo's apply-go). **Neighbors card values set: 114/114** opportunity `monetaryValue` = new quote total (1 transient 401 retried OK); value-only writes, stages untouched (~$184k now visible on the board).
  - **PR #556 MERGED** (script + S39/S40 docs + ledger #156-158 rows) after resolving a 3-way docs collision (concurrent S38-handoff session + the cloud S39's addendum push to the same branch). Ledger: **#156/#157/#158 DONE+LIVE · #155 SEND-READY**.
- **REMAINING for the send wave (all Naldo-side):** (1) Neighbor drip workflows in GHL (his copy/cadence — quote tool routing already keeps regular holiday workflows silent); (2) the outreach decision + per-quote Send (or a batch-send script on request). Later: 56 paid customers with no photo (Homes-2024 fallback staged locally, 113 HEIC need jpg conversion), 6 off-roster payers, 5 past-due + 2 comped held rows.
- **Migration data (manifest / photos / build_manifest.py / match reports) stays gitignored + local** — never in the repo.
- **Cross-dev heads-up (Jason):** #558-#561 touch portal (SelectionContext/WhatsIncluded/InteractiveHero/derivePackages/adapter/loader), send/decline/valor routes, ghlPipelineMap, inbox store, QuoteBuilder header — PR bodies carry the detail; all positively gated on `legacy_rebook === true`, normal-quote behavior pinned by tests (3462 green). Existing `src/lib/rebook.ts` ("Rebook last season") is UNRELATED to `legacy_rebook` despite the name.

- **▶ POST-CLOSE ADDENDUM (same S40 conversation, 2026-07-17/18 — after the first wrap):** (1) **YLL Neighbor toggle** #572 merged + live: Mark/Remove on the admin quote detail (route `POST /api/quotes/[id]/legacy-rebook`, requireOperator, confirm names the 3 effects + a non-draft no-retroactive-GHL warning); first use = **David Alfaro #1191** (flagged; Neighbors card → Booked @ $978.75; stray $0 Christmas Lights card deleted — verified by direct GET, search index lags). (2) **#154 Wisetack INTERIM LIVE + flag ON:** portal "Flexible payments" section + prequal CTAs (PRs #578/#579), eligibility holiday/permanent/bistro + $1,500 job-total floor + $500–$25k balance, section order price→financing→protection→trust, amber pill CTA; live-verified on a real quote (section, order, button, prequal href). Wisetack merchant portal has NO API keys — full API needs a partner grant; **request drafted for Naldo to send**. (3) Ledger housekeeping: 13 done rows archived. Gotchas earned: Vercel MASKED env fields eat blind typing (first flag save was EMPTY — caught because flag-off renders nothing; fix = form_input via DOM ref, which echoes the value back); GHL opportunity search index lags minutes behind writes (verify by direct id GET).

