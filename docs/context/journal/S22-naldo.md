### Naldo S22 (EVENT thread) — EVENT LIGHTING vertical #96 BUILT end-to-end (pricing → builder → portal), 17 commits on `naldo/event-lighting-core` (PR #344, gated dark, UNMERGED) — synced through master `6d96f10` (2026-07-03)

> The parallel EVENT thread to the permanent S22 session below (both Naldo, concurrent — same coordination doc). Scoped event lighting (brainstorm → 3-agent recon → LLM council → coordination with the permanent session) then built the **Event Lighting (`service_type='event'`) vertical end-to-end**, mirroring permanent's "reuse the holiday machine, new pure modules, thin `service_type` seams" pattern. ALL on ONE branch `naldo/event-lighting-core` (PR #344, 17 commits) — **NOT merged** (Jason-area, awaits Naldo's merge-go); gated dark behind `app_settings.eventEnabled` (default false).

- **Pricing** (`lib/event/pricing.ts` `calculateEventQuote`): independent event rate table (roofline **7/8/9** · mini **35/45** · spritzer **65/75/85** · bistro **$12/ft** · **$150** support), accessories EXCLUDED (allow-list), #104 per-line overrides, a **$0-rate guardrail** (a priced item can never silently bill $0 — the council's catch), `QuoteResult.eventRatesSnapshot` (approve/amend rate-drift guard, mirrors permanent). Reads a new `QuoteInputs.event` block (bistro/barrels/dates).
- **Bistro from the design:** `projectScene` auto-projects drawn `bulbType='bistro'` strands → `inputs.event.bistro` (footage from the polyline; keyed on the EXISTING bulbType → **no `sceneTypes` change, no design-tool relay**).
- **Builder:** `EventSection` (3 dates install/event/takedown + barrel/box supports + a takedown≥event≥install order-check), and the Event service-type button is **gated behind `eventEnabled`** (fetched client-side; hidden until Settings flips it on).
- **Settings:** adjustable event rates + the "Enable Event quoting" toggle (Settings → Quotes; #101 app_settings pattern).
- **Portal (ONE adaptive portal, event-branched — same approach as permanent, NOT a separate portal):** event package (single "what's included" card) · approve forces holiday fees off · amend service-agnostic (free) · **ALL copy re-voiced** (3 parallel agents branched RiskReversal / WhatHappensNext / WhatsIncluded; + hero "your event" + event FAQ) · **"Your Event Schedule"** 3-date timeline · **soft add-on suggestions** cards.
- **QA (in-browser, this session):** rendered MOCK_QUOTE as an event quote on the dev portal → every event section + copy present, dates TZ-safe (Jul 11/18/31), **zero Christmas leftovers** (the one "your home," is the correct RiskReversal "your home, venue, or landscaping"). Reverted the mock. Also live-verified `/api/quote` event pricing (roofline $7 vs holiday $8; wreath dropped; bistro 600 + supports 300).
- **Gates:** tsc 0 · `eslint src` 0 · **vitest 2042** GREEN throughout. Re-synced onto master TWICE (P5, then P6) — reconciled the additive `appSettings`/`quoteForm`/`route.test`/`clientSettings`/settings-page collisions (both verticals added their block; kept BOTH; renumbered nothing since the row-keys differ).
- **⚠️ Cross-dev (Jason):** PR #344 touches **Jason's areas** (pricing / builder / portal / settings / projectScene / approve) — flagged for review; Naldo gives the merge-go. Coordinated with the permanent session via `docs/superpowers/plans/permanent-event-parallel-coordination.md` (permanent owned the shared dispatch seams; event added `case 'event'` after each landed).
- **NEXT (S23):** (1) an in-browser E2E with a REAL event quote (create → send → portal → approve) before flipping `eventEnabled`; (2) **merge PR #344** on Naldo's go; (3) go live. The vertical is feature-complete — no critical follow-ups.

