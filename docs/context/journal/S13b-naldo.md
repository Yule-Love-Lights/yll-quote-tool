### Naldo S13b — the #90/#81-hardening + operator-names PRs BUILT (TDD + multi-agent); garland under-bill (#236) FIXED + MERGED; #80 accuracy-bug triage; created_by migration applied to prod (2026-06-29)

> An **ultracode** session, **concurrent with S13** above. This thread BUILT the #90 hardening + names PRs (the S13 entry records their MERGES — Naldo was "pushing to main in another session"), fixed the one genuinely-open #80 accuracy bug, and verified the final prod state.

- **#90 hardening — all 4 items built TDD-first, each its own PR** (merged + live, per S13): **#227 RLS** (enable on all 14 tables; switched the 3 bare-anon data paths — quotes `saveQuote`/`updateQuote`, `training.ts`, `referenceAssets.ts` — to `service ?? anon` FIRST so RLS can't break them; `FULL-SCHEMA` flipped disable→enable); **#228 created_by** (additive nullable `created_by uuid → auth.users`, stamped on create via `getOperator()`); **#229 retention** (CRON_SECRET-guarded weekly `/api/maintenance/retention` purging quote-less designs >90d + their bucket photos); **#230 portal error** (pure `friendlyError`; DepositCheckout no longer leaks raw server text). Multi-agent design+verify per item; RLS flagged council-worthy — the service-role-BYPASSRLS design held.
- **Operator/admin display names (#232, #81 follow-up) — BUILT + merged.** Names live in **`app_metadata`** (admin-only, like role), NOT user_metadata — the design workflow's key catch: the live self-service `/api/account/password` `auth.updateUser` only writes user_metadata, so a name there would be **self-spoofable**. Pure `nameOf()`; required on create, backfillable on PATCH; email fallback everywhere; `seed-admin` optional name arg. Adversarial review → SHIP.
- **#80 accuracy-bug triage (multi-agent diagnose→verify) — 2 of 3 already handled:**
  - **Garland under-bill → FIXED (PR #236, merged + live).** Sections now use an *effective* px/ft = AI roofline calibration **or** the first yardstick's **axis-aware** `pxPerFoot` (was: silent 1-section with no roofline calibration → under-billing long runs). Silent 1-default kept ONLY when no scale exists; new pure `countSeededGarlandUnestimated` → amber builder warning so staff set counts. 3 prior-plan holes designed out (axis-aware ppf; count from the seed step not a scene heuristic; no phantom WW field). Review → SHIP.
  - **Approve trusting client totals → already fixed** in master (server recompute via `priceSelection` from `quote.result`).
  - **Scene-link mis-map → interim count-mismatch guard already shipped** (`sceneLinks.ts`, "audit fix #33" — degrades to under-link). Residual = same-count reorder/swap → needs the **stable-id refactor** (thread a scene id through the pricing result + adapter, Jason's pricing area). Verified the "obvious" fix (drop the per-unit `recommended` carry) is a 3-way regression (staff recommend checkbox + customer "Our Recommendation" + recommended-subtotal) → NOT done. **Flagged for Jason.**
- **Prod migration applied this thread:** `add-created-by` via the Chrome SQL editor (Supabase MCP is read-only) — verified both `created_by` columns nullable uuid. (RLS-all-tables was applied by the concurrent session.)
- **Final verified prod state:** RLS on **14/14** tables; security advisor's CRITICAL "RLS disabled" **cleared** (the 14 "RLS enabled, no policy" notices are INFO + intentional for the service-role model). Left as optional hardening: pre-existing WARNs (mutable `search_path` on trigger fns, `vector` in public, leaked-password protection off) + a new `rls_auto_enable()` SECURITY-DEFINER fn from the concurrent session. Gates throughout: tsc 0 · lint 0 · **vitest 999**.
- **Cross-area:** #227/#232/#236 touch Jason's data-layer/settings/pricing — flagged per PR; Naldo directed all merges. **#90 DONE; the 3 #80 accuracy bugs resolved (2 already-done + garland fixed) except the sceneLinks stable-id residual → Jason.**

