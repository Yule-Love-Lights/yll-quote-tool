### Naldo S6 — gh-not-Chrome · portal "change colors" link · NEW "Stake Lighting" category built (2026-06-26)

> Local session on the main checkout (clean `master`). Two deliverables, each its own branch/PR (Jason-area → Jason-review, merge on my go). Used the brainstorming → spec → writing-plans → build-via-workflows flow under **ultracode** (multi-agent). Gates run LOCALLY this session — restored `node_modules` (`npm install`) after the OneDrive eviction, so tsc · lint · vitest all ran green here (no longer Vercel-build-only).

- **Tooling switch — `gh` CLI, not Chrome.** Naldo installed `gh` on this machine (`gh 2.95.0`, authed `naldoven`, scopes `repo`+`workflow`). From now: PRs via `gh pr create --fill` / `gh pr merge`, NOT the Chrome extension. Recorded in memory `project_github_pr_via_browser` incl. the Bash-tool PATH prefix (`export PATH="/c/Program Files/GitHub CLI:$PATH"`).

- **Portal "Click here to change colors" link (PR #142, OPEN, Jason-review).** Branch `naldo/portal-change-colors-link`. On the 2nd on-portal design render (#50, "Your home, lit up"), replaced the static caption "Updates live as you adjust your selections above" with a gold "Click here to change colors" link that smooth-scrolls to the Light Color picker (#10/#48, new `id="light-color"` anchor) + moves focus there. Re-scoped `aria-hidden` onto just the decorative canvas so the link is reachable. Vercel preview built green. **PENDING: Naldo verify + merge.**

- **🆕 "Stake Lighting" — a brand-new independent lighting category (branch `naldo/stake-lighting`, PR pending).** A parallel sibling of Winter Wonderland / "C9s — Custom Runs": manual ft × difficulty **+ satellite "Add Stake Run" line-drawing** (purple `#a855f7`), billed INDEPENDENTLY at its **own $6/$7/$8** per-ft rates (easy/med/hard), with its **own portal kind `stake-lighting` + `Lightbulb` icon**, a `stake-lighting` surface tag, and training-page capture. Manual-only (no AI auto-detect, like WW). Naldo's locked decisions: own rates 6/7/8 · satellite-draw scope · own icon/kind · training included.
  - **Process:** a mapping workflow (6 finders → completeness critic) produced the verified blast-radius (168 touchpoints, 27 type-lists); brainstorm decisions via AskUserQuestion; spec → `docs/superpowers/specs/2026-06-26-stake-lighting-design.md`, plan → `docs/superpowers/plans/2026-06-26-stake-lighting.md`. Built in 6 commits across the layers (scene types/seeding · pricing · form+API · portal kind/icon/links · satellite draw + manual section · training+editor+migration). A 5-lens adversarial-review workflow (13 agents) → 4 confirmed LOW items (a missing not-projected test case + 3 stale comments/error-string) → all fixed.
  - **Gates:** tsc 0 · lint 0 errors · **vitest 395 pass** (+ new pricing/seed/portal/parse tests).
  - **✅ MIGRATION APPLIED:** `migrations/2026-06-26-add-stake-lighting-training.sql` (3 `training_houses` cols) — **Claude ran it on prod via the Chrome SQL editor** + verified the columns/types (numeric/text/jsonb). Updated [[project-apply-migrations-via-browser]]: the browser path DOES work on this machine (the dashboard just needs ~6s to load past a blank shell; single-line typing + on-screen verify beats the flaky clipboard).
  - **✅ DESIGN-TOOL RELAY DONE:** mirrored into the standalone design tool at **`c4206e9`** (surfaceOpts 4th entry + `Surface += 'stake-lighting'`, matched the diff exactly, tsc clean; quote-tool-only bits left out). Cores back in sync.
  - **Follow-ups this session:** flipped Stake default difficulty → **Easy / $6/ft** (Naldo); merged **#142** (portal "change colors" link) + **#143** (Stake Lighting) — both LIVE on prod. Adopted Jason's **karpathy-guidelines** skill (read + applying manually this session; full activation needs a fresh session).

