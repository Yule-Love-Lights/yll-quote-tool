### Naldo S33 -- WEBSITE LEAD-CAPTURE FORMS: custom embed replaced ALL Gravity quote forms sitewide (per-service GHL routing) + household/spousal handling + WP mobile-header fix; then a long owner-driven UX-polish iteration -- master ->`4c77177`, gates tsc0/eslint0/vitest **3171** (2026-07-11/12)
**Investigate -> build -> cut over -> iterate. One conversation. Replaced the WP site's 3 old GHL-plugin Gravity Forms (all dumped every lead into the Christmas pipeline, dropping service+notes) with a custom embed served from the quote tool.**
- **#486 (MERGED+LIVE) API + embed.** `POST /api/leads` -> Supabase `website_leads` (source-of-truth/retry queue, RLS deny-all, migration applied via browser SQL editor) -> GHL sync: upsert contact, tags `new lead`+`web-lead-<svc>` (NEVER the legacy heroform/stickyform/quotesectionform tags that trigger the old Christmas-pipeline workflows), Service custom field, note, find-or-create opportunity in the RIGHT pipeline (christmas->Christmas · permanent->Permanent · event-wedding->Event · landscape->NEW Landscape pipeline Naldo created). `public/lead-form.js` = dependency-free vanilla-JS embed, 3 variants (full/bar/sticky), no-innerHTML (hook), honeypot+elapsedMs spam, first-touch UTM, PostHog events, TCPA consent. 57 tests. TDD; 2 builder correction rounds pre-merge (clock-skew lead-loss, length caps, strict consent).
- **#494 (MERGED+LIVE) auth-gate carve-out + 14 adversarial-review fixes.** The route shipped behind prod default-deny (proxy.ts) -> 401'd; carve-out allowlists POST+OPTIONS (mirrors referrals/submit). A **4-agent Opus review** (lead-loss/security/GHL-seam/embed) found 14 real issues fixed pre-merge: **HIGH `isTest` bypassed the rate limiter while still firing GHL** (unbounded CRM writes + SMS to arbitrary numbers) · **HIGH the GHL Service field is a CHECKBOX and our scalar-string write never landed** (tests baked the wrong shape) · Christmas leads entered at "Make Quote" not the entry stage (quote-flow env override bled into the lead flow -> new `resolvePipelineStages(..., {envOverrides:false})`) · spoofable leftmost-XFF rate key -> `x-real-ip` · rate-limited/spam leads now leave forensic rows · duplicate-submit dedupe · partial-sync contact-id anchoring · unclamped landingUrl 400-loop · Chrome-autofill-tripped honeypot · PII-in-logs redaction · ES2019 old-Safari parse break.
- **Proof pack (prod, cleaned up):** 4 test leads -> 4 correct pipelines w/ Service field `["X"]` + tags + note; a 5th probe proved the checkbox field UNIONS across services + reuses the open opportunity (no dup).
- **#497 household/spousal:** a 2nd person on a shared email/phone no longer OVERWRITES the contact name -- search-before-upsert, omit name fields, tag `household-second-contact` + note who submitted. (Also fixed a pre-existing `vi.clearAllMocks` test-isolation leak.)
- **WordPress cutover (browser, all LIVE):** swapped all 3 forms -- full form in Contact-Section template 1143 (43 pages) · hero bar on homepage 318 + referral page 3432 · sticky in template 2736 (113 pages). **Exhaustive 115-page sweep caught the referral-page hero that the homepage-only recon had missed.** Old Gravity Forms kept installed = instant rollback (mapped in #510). **Mobile header fix:** Ireland placeholder phones -> 631-517-0186, "Long Island, NY" -> GBP link `share.google/1KackMUQxHt6f2n0A` (both icon-lists in elementor-hf template **39**, not 1121).
- **UX polish PRs #498/#501/#504/#513 (all MERGED+LIVE):** desktop 2-col layout + install photos on cards (#498) · owner's landscape card photo converted from his ProRAW DNG, served from quote-tool public/ (#501) · dropdown->pill picker + sticky contrast (#504) · sticky moved bottom->top slim, auto-detects header offset (#513).
- **#517 (OPEN -- pending Naldo's merge-go):** centered bar (dup heading removed) · bigger centered full heading (32px) · **sticky** = dark `#1B1B1B`, ~50% bigger (older-eyes), **NO dismiss X**, mobile = green **`631-517-0186`** (tel:) + red "Get a Quote" (scrolls to full form -> no fields -> no consent dead-end), desktop = **2x2 service grid** compact panel (262px), dedupe renders one bar even though WP includes the template twice. Iterated live via a **claude.ai Artifact preview** (inlined the new JS + data-URI'd the 4 card photos -- the WP preview page rendered the OLD version because the site-wide prod-script `__yllLeadFormLoaded` guard wins).
- **Confirmed (don't re-ask):** single-select service (not checkboxes) · Wedding->Event, Landscape->new pipeline · custom forms over plugin-Pro · **mobile sticky = Call + Get-a-Quote buttons, NOT a form** (sidesteps TCPA consent) · sticky dark `#1B1B1B` + NO dismiss + 2x2 desktop options + ~50% bigger · header "Long Island, NY" -> GBP link.
- **Cross-dev (Jason):** all lead work touches `src/lib/integrations/highlevel.ts` + `ghlPipelineMap.ts` (his area) -- merged on Naldo's go w/ his review flag. Added `upsertContact`/`createContactNote`; `resolvePipelineStages` gained an optional `{envOverrides}`; `upsertContactCustomField` value type widened `string|string[]`; `findOrCreateOpportunityForContact` gained optional `source`. Behavior for existing callers unchanged.
- **NEXT (S34):** (1) Naldo reviews the #517 Artifact preview -> **merge-go** -> deploy SHA-verify -> refresh live site (embed auto-updates, no WP edit); (2) **retry worker + admin visibility for `website_leads` rows stuck `pending`/`deferred`** (GHL-outage recovery -- NONE exists yet, the accepted launch gap); (3) confirm the 3 Vercel prod env vars are set (`HIGHLEVEL_PIPELINE_ID_LANDSCAPE` / `_STAGE_LANDSCAPE_ENTRY` / `_CONTACT_FIELD_SERVICE`); (4) `docs/website-lead-forms.md` sticky section is STALE (says slides-from-bottom + old desktop/mobile split); (5) bistro option on the website form (from S32 backlog). Cleanup: none outstanding (all test contacts + `website_leads` test rows deleted; temp WP preview page 4045 reverted to private).

