### S88 (Naldo), 2026-09-03, every printed QR code the company owns was dead; now the van decal and the business cards work again on infrastructure we own. 2 PRs (#1186 merged, #1191 open at close), plus one merged for a concurrent session

- **NUMBER:** S88. S87 was a CLOUD session working this same problem and is on master. S89 then yielded S88 to this session after finding its local-only `naldo/s88-close` branch, which carried no commits and had never been pushed. Worth naming because the check that saw it was the local branch list, the only one that sees a session which has started and written nothing. This session was in turn corrected by S87's own fragment: I had checked master, the archive, open PRs and remote branches, found S86 free, and was wrong, because S86's fragment had not merged yet.

- **THE JOB, and why it was a verification job first.** Naldo handed over a cloud session's diagnosis with an instruction to treat every claim as an unverified hypothesis, because that session's network egress had blocked the hosts its conclusions were about. Every claim was re-checked from scratch. Most held. Two did not, and both mattered.

- **CORRECTION 1: the old GoHighLevel account is not dead, and its own redirector is better evidence than its dashboard.** The handoff said the account had lapsed and that the mapping came off its `Sites -> QR Codes` screen. The account still answers, on the former agency's white-label host, and one request each settled the mapping without trusting a screenshot: `link.surgecrm.ai/qr/hbJhlsQLHpFv` 302s to the homepage, `link.surgecrm.ai/qr/DjzJS9mzhTOm` 302s to `/get-a-quote/`, and an unknown slug 404s. That last one is a real difference nobody had noticed: **the old system 404'd unknown codes and ours never does**, so the fail-open behaviour is a deliberate divergence rather than parity, and it is now written down as one.

- **CORRECTION 2: the destination the business card had always pointed at is broken, so shipping it faithfully would have shipped the outage.** `https://yulelovelights.com/get-a-quote/` is a published WordPress page (id 1185) that a redirect rule overrides. Measured: a bare request 301s to the homepage, and only a request carrying a query string reaches `/services/residential-holiday-lighting/`. So the destination depends on whether a query rides along, which is not a property anyone designed. Ledger row 272 had already diagnosed the cause in S50 (a Rank Math redirect created 2026-06-26, 48 hits) and the independent measurement matched it exactly. Naldo's call: **both printed codes land on the homepage**, tagged apart by `utm_campaign` so a card scan stays distinguishable from a van scan. Separately and larger, still open on row 272: 9 links on the marketing homepage and 11 on the residential-lighting page point at that same dead URL, so every "Get a Quote" button on the site dead-ends today.

- **PR #1186, the route.** Both codes to the homepage, 302 not 301 so the destination stays repointable, never a 404 for any slug, allowlisted for signed-out access. Four lenses plus an adversarial delta-verify. **No HIGH findings, and the MEDs earned the round:**
  - **A public route was reading `Object.prototype` as a campaign.** `KNOWN_CAMPAIGNS[slug]` on a plain object walked its prototype, so `/qr/constructor` emitted `utm_campaign=function Object() { [native code] }`. The technical lens reproduced it against unmodified code rather than reasoning about it. Guarded with `Object.hasOwn` and pinned by five tests that fail against a bare lookup.
  - **The gate was opened onto a 404.** A single `[slug]` segment 404'd a bare `/qr` and `/qr/a/b`, while `operatorGate` advertised the whole prefix as public and the module promised it never dead-ends. Now an optional catch-all, so the promise and the allowlist describe the same surface.
  - **The allowlist entry had no test at all**, raised independently by three of the four lenses. Convergence, not severity, was the signal. It has one now, positive and negative, plus a structural guard that fails the moment a second route appears under `src/app/qr` and inherits the public prefix by accident.

- **A CONCURRENT SESSION PUSHED TO THE SAME BRANCH MID-REVIEW, AND IT WAS RIGHT.** The cloud session (S87) landed `ba993926` on top of this session's work, having read it and reset onto it rather than forcing. Its contribution was a finding this session had missed: the route comment claimed `force-dynamic` meant a scan is "never answered from a cache", which is measurably false, since it emits no cache header at all. `Cache-Control: no-store` is what makes the sentence true, plus `X-Robots-Tag: noindex` for an unbounded slug space on a brand subdomain. Neither had a test, which is exactly how the false claim survived, so both are pinned now.

- **THE CUTOVER, and the deviation that prevented an outage.** After the merge and a prod verification on the deployed SHA: `link.yulelovelights.com` added to the Vercel project, then the SiteGround DNS. **Vercel asks for a CNAME. A CNAME would have broken the mail setup.** That subdomain also carries 3 Mailgun MX records, 2 DKIM keys and an SPF including `spf.leadconnectorhq.com`, and a CNAME cannot coexist with other records at the same name. Used Vercel's legacy A `76.76.21.21` instead, which its own panel says still works and which `ops.yulelovelights.com` already uses in that same zone, and left every mail record untouched, verified by query afterwards. Vercel moved from "Invalid Configuration" to "DNS Change Recommended", the expected state for a legacy A.

- **VERIFIED, then verified again by the only test that counts.** Live on the real host, signed out, on a phone user agent: van to `utm_campaign=van`, card to `utm_campaign=business_card`, an unknown slug to the homepage untagged, bare `/qr` not a 404, `/qr/constructor` with no campaign, `Server: Vercel`. Then Naldo scanned the actual van decal and a real business card. Both work.

- **THE CLOSE REVIEW FOUND A CUSTOMER-FACING REGRESSION IN THIS SESSION'S OWN INFRASTRUCTURE CHANGE.** `link.yulelovelights.com` was a GoHighLevel link host, so old booking confirmations and review requests may still sit in customers' texts. Only `/qr` is ours. Every other path on that host now reached the operator gate, which answers a signed-out request by rendering the **staff login page**: an "Operator / Password" form on a Yule Love Lights subdomain, which reads as phishing. Measured both ways, the old address returned a plain 93-byte 404 and the new one 307s to `/login`. **PR #1191** sweeps every non-`/qr` path on that host to the marketing site. Four lenses again: the customer lens confirmed from Next's own bundled docs that `redirects()` run at step 2, before the proxy at step 3, so the fix is not inert, which was the question that would have made it worthless. Admin found two MEDs (the marketing URL had become a literal in two files; the swept traffic carried no attribution while the sibling route tags everything) and technical found the comment claiming a 302 was wrong, because `permanent: false` emits a 307. All three fixed, both new tests mutation-probed, delta-verify PASS.

- **Ledger:** 499 SHIPPED and archived, 500 CLOSED on Naldo's call and archived, 503 and 504 minted (a QR scan is invisible inside this app; `operatorGate.ts` has no ownership row in AGENTS.md). Counter 505. Row 272 unchanged and still open, now with fresh 2026-09-03 measurements behind it.

- **Also merged for another session:** #1189, S87's own close PR, on Naldo's explicit call. Its fragment predates this session's merge and cutover, so it correctly reads "PR #1186 open, not merged"; this entry carries the ending. It was not edited, because it is another session's record.

- **STILL OPEN at close:** PR #1191, awaiting Naldo's merge-go. The scan analytics in the old GoHighLevel account were deliberately let go (row 500).

- Gates at close: tsc 0 · lint 0 errors (23 pre-existing warnings) · vitest **10427 / 596 files**, on the #1191 branch merged with current master. Master at close: `f9517ef4`.

**POST-CLOSE DELTA (2026-09-03, same session).** Naldo gave the merge-go on #1191 after the close, so the fix for this session's own regression is now live and the outage is fully closed rather than half-closed.

- **#1191 merged (`ac368372`) and verified live.** Every non-`/qr` path on `link.yulelovelights.com` now 307s to the marketing site tagged `utm_source=legacy_link`, where before it rendered the operator login page to a signed-out homeowner: `/appointment/abc123`, `/widget/form/xyz`, `/review`, the bare root and a deep path all measured. The six `/qr` cases were re-measured in the same pass and are untouched, including the tagging and the never-404 behaviour, which was the thing this change could most easily have broken.
- **The merge-window guard fired for real, and it was right.** The first merge attempt refused because `#1192` landed on master while this branch was gating, so `git merge-base --is-ancestor` returned false and the chained `gh pr merge` never ran. Re-synced, re-gated, re-pushed, waited for CI on the new SHA, merged. This is the AGENTS.md rule about the BASE moving rather than the head, which `--match-head-commit` alone does not cover, working exactly as written. It cost one extra cycle and prevented a merge onto a master that had never been gated against.
- Gates on the merged head: tsc 0 · lint 0 errors (23 warnings) · vitest **10452 / 597 files**. Master after the merge: `ac368372`.

**POST-CLOSE DELTA 2 (2026-09-03 into 09-04, same session).** A second wrap was run on Naldo's ask, and it was not ceremony: the whole-session review found a live defect that four lenses and an adversarial delta-verify on #1191 had all passed.

- **THE DEFECT: an uppercase `/QR/<slug>` fell between three layers that disagreed about case.** Next's router matches routes case-insensitively by default, and Next compiles a custom route's `source` with `sensitive: false`, so the filesystem route and the `next.config.ts` link-host sweep BOTH treated `/QR/…` as the QR route and declined to touch it. `operatorGate` compared case-sensitively, so it alone refused, and a refusal there routes through `/login`. Two layers stepped aside expecting the third to take it. Fixed in **PR #1199** (`86d4c754`): the gate now agrees with the two around it. Four lenses, all PASS; the technical lens swept every Unicode codepoint to 0x2FFFF for anything folding into `q` or `r` (zero hits) and read Next's installed source rather than trusting the comment.
- **I GOT THE SEVERITY WRONG TWICE, AND SO DID TWO LENSES, THE SAME WAY.** The customer lens reported that `/QR/…` still served the operator LOGIN FORM, and I repeated it to Naldo. All three of us had read the FIRST HOP of a redirect and stopped. Following the chain shows #1191's sweep catches `/login` on that host too, so the real outcome was two hops ending on the marketing site. Nobody was ever shown the login form after #1191. What was actually wrong: the scan lost its attribution (reported `utm_source=legacy_link` instead of a van or card scan), took an extra hop, and dragged a `?from=%2FQR%2F…` parameter onto the destination. A second commit corrected the claim in the code comments, the commit history and the PR body, because a wrong severity written into a comment outlives the conversation that produced it.
- **A TEST WAS MODELLING A MATCHER NEXT DOES NOT USE, WHICH IS WHY THE GAP WAS INVISIBLE.** `qrLinkHost.test.ts` compiled the sweep pattern with path-to-regexp's DEFAULTS, which are case-sensitive, while Next compiles it with `sensitive: false`. Under that fiction the sweep appears to catch `/QR/<slug>`, so anyone reading the test to check this exact question would have been reassured by a fiction. It now compiles with Next's own options, and flipping the flag fails exactly the four assertions that name the case variants.
- **Verified live after the deploy:** `/QR/hbJhlsQLHpFv` and `/Qr/DjzJS9mzhTOm` now resolve in ONE hop with the correct `utm_campaign` (van, business_card); the two real lowercase printed codes are unchanged; the legacy sweep and the lookalikes (`/qrcode`, `/QRCODE`, `/appointment/abc`) still behave.
- **Ledger:** row **505** minted for the one thing this session never verified: the Mailgun MX, DKIM and SPF records on that subdomain were confirmed present in DNS after the A-record change, but no mail was ever sent or received through it. Counter 506.
- Gates on the merged head: tsc 0 · lint 0 errors (23 warnings) · vitest **10504 / 599 files**. Master after the merge: `86d4c754`.

**POST-CLOSE DELTA 3 (2026-09-04, same session).** Naldo asked for row 505 to be closed. It was the one claim left unverified from the DNS cutover: mail on `link.yulelovelights.com` had been confirmed present in DNS and never tested.

- **Answered without any account access, and the answer is that the cutover did not touch mail.** Four measurements, strongest last. Both MX hosts answer SMTP live (`mxa`/`mxb.mailgun.org`, each `220 Mailgun Influx ready`). Mailgun still ACCEPTS mail for the domain rather than merely having DNS pointed at it: an SMTP conversation returned `250 Sender address accepted` and `250 Recipient address accepted`, then RSET and QUIT, with **no message body ever transmitted** - the probe stops before DATA. SPF is intact with both includes, and BOTH DKIM selectors resolve, `k1._domainkey` and `default._domainkey` (the close review's customer lens reported finding only one and was wrong). And the structural point that makes the rest conclusive: **the only record changed was the A record, and SMTP never consults an A record.** Mail routes on MX and authenticates on TXT. The change that WOULD have destroyed the set is the CNAME Vercel recommends, which cannot coexist with other records at the same name, and which was deliberately not used for exactly this reason.
- **Deliberately still not proven, and said so rather than rounded up:** that an outbound SEND through Mailgun succeeds, and whether anything uses this domain for mail at all. Both need Mailgun or GoHighLevel credentials, and neither could have been affected by an A-record change. Row 505 closed and archived on that basis.
- Nothing shipped in code. Gates re-run on master for the close: tsc 0 - lint 0 errors (23 warnings) - vitest **10504 / 599 files**.
