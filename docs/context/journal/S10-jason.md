### Session 10 — King's Park Day beta push: #47 · #40 · #39 · #37 · #57/#48 + payment recon (2026-06-18)

> **⏹️ SESSION 10 CLOSE (2026-06-18, ~80% usage).** One continuous conversation; next fresh session = **S11**. Context: a **booth at King's Park Day on Sat 2026-06-20** to beta-test the tool, so Jason raised a prioritized backlog and we shipped the customer-facing path.
>
> **Big planning intake:** Jason's site review → recorded as a renumbered backlog **#37–#57** in [[task_ledger]] (a 🔴 high-prio Saturday set + a 🟠 S10 lower-prio batch + #56 mobile epic). Don't re-derive — it's all in the ledger.
>
> **Shipped + MERGED to master (PR#):** **#47** toggles count toward the $1,000 gate (PR #49) · **#40** Sep/Oct early-install discount, pre-discount gate, mutually-exclusive-with-rush (PR #50) · **#39** "You're Booked" polish — dynamic install/takedown windows from the approval snapshot, "Text Us Directly" + (631) 517-0186, removed false "receipt emailed", step-2 "no need to be home" (PR #51) · **#37** quote-send→GHL: find-or-update-or-create the Christmas-Lights opp at "Bid Sent" (title=name, value=total, Source="Quote Tool") + text/email the portal link via GHL conversations (PR #52) · **#57+#48** mobile pass slice: color picker → its own band below the packages, hero sky shifted up on phones (pure translateY, no side-crop), 44px Approve tap target (PR #53). Gates green throughout (tsc · lint 0/2 · **253 tests**).
>
> **🔑 GHL verified LIVE (probe):** pipeline **Christmas Lights** `sC6JEcxlGnNDasanlXDN`; stages — Make Quote `14071235…`, 📨Bid Sent `d15bc673…`, Interested `c7f32106…`, ⏰**Approved** `90e7a535…` (currently in the `HIGHLEVEL_STAGE_QUOTE_SIGNED` env var). Jason added the token's **Conversations scope** + 3 new env vars (`HIGHLEVEL_SMS_FROM_NUMBER`=+16315170186, `HIGHLEVEL_EMAIL_FROM`=sales@yulelovelights.com, `PORTAL_BASE_URL`=https://quote.yulelovelights.com) to `.env.local` **and Vercel**. **Conversations API gotchas:** `POST /conversations/messages`, **Version `2021-04-15`**, SMS uses `fromNumber`, Email uses `emailFrom` — **`replyTo` is an enum, NOT an address** (422s if you send an email there); SMS needs the contact to have a phone.
>
> **💳 #38 payment — UNBLOCKED, processor identified = ValorPayTech** (public API `valorapi.readme.io`; reconciles with the Global Payments backend in the PDFs). **Decided:** Approach **A = Passage.js** embedded checkout on the portal; **50% deposit**; **auto-vault EVERY deposit card** (Valor "The Vault") so staff **manually** charge the balance post-install in the Valor portal (NO auto-charge, NO balance feature built); confirm via Valor **webhooks** (HMAC-SHA256, must email Valor to enable + get secret); on payment-confirm move GHL → ⏰Approved (add `HIGHLEVEL_STAGE_QUOTE_APPROVED`=`90e7a535…` or reuse SIGNED) and **drop the approve route's current "Interested" move**. Auth = **APP ID + APP Key + EPI** (Valor portal → Virtual Terminal → Manage → API Keys; the **App Key + EPI are per-device → click ＋ADD under Devices** to generate them; App ID is account-level). **JASON ACTION:** mint App Key+EPI, put APP ID/Key/EPI in `.env.local`+Vercel, email Valor to enable webhooks. Full detail in [[task_ledger]] #38.
>
> **Ending state:** master clean + current (PR #53); prod auto-deploys master (live at quote.yulelovelights.com); nothing uncommitted. **NEXT (S11):** **#38** (Valor Passage.js deposit + auto-vault) — gated on Jason's Valor creds + webhook enablement; then **#42** (payment confirmation email/receipt) + **#43** (post-payment quote-page state), both wired to a single "booked" event (fire on approval now, on payment-confirmed once #38 lands); then **#39 Naldo follow-ups** (premium-takedown window dates · referral $100→**$150** · remove "fully refundable until morning" · remove "Your crew lead" — all need Naldo); then the broader **#56 mobile epic** + the rest of the S10 batch (#13/#14/#29/#41/#44/#45/#46/#49/#50/#51/#52/#53/#54/#55) + Naldo-pending. **Deferred:** #8 C6.
>
> **Process notes:** dev server is preview-managed on :3000 and got **reaped repeatedly** this session — restart on pickup. **Konva surfaces (portal hero + design editor) can't be headless-screenshotted** (the live canvas blocks screenshots) → verify those on a real device / browser device-mode. **Turbopack gotcha:** adding/deleting routes + repeated dev-server restarts corrupted the route registry → nested POST routes (`/approve`, `/send`) 404'd as the page-not-found HTML; fixed by stopping the server, `rm -rf .next`, and restarting clean.

---

