### S61 (Jason), 2026-09-04: the rate history row 506 demanded, then a year of pre-tool timesheet on top of it, then the one thing that made both usable. 3 PRs merged and live, 2 ledger rows closed, a migration and ~150 production payroll rows written. Close PR jason/s61-close

- **NUMBER:** S61, checked against Jason's fragments (S60 was the latest), the machine-local `feedback_self_assessment.md`, `gh pr list --limit 200`, the ledger counter and every remote branch. Naldo ran S87→S91 on the same dates; numbers are PER DEV. Master moved from `964c199f` to `5154151b`.

- **#1214 — pay rates CHANGE, and hours now convert at the rate of the day WORKED.** Row 506, and it was a LIVE defect rather than a future concern: since S60's rollover a payment is DIVIDED by `crew_members.base_rate_cents`, which is one number — the rate right now. Jason's oldest unpaid shift was 21 Aug, inside his $13.00/h window, while his stored rate was $16.00, so paying him marked off about 19% fewer hours than the money bought, silently. New `crew_member_rates` keyed on an ET calendar day; the rate for a shift is the newest row at or before the ET day it STARTED, the same bucketing the hours pages already use.
  - **`allocatePayment` stopped being a single division.** It walks oldest-first buying each shift at its own rate — and walks in **cent-seconds**, not cents, because rounding each shift's cost to whole cents drifts. That is not theoretical: on Khaye's real five-shift $180.00 payment the naive version lost **2 seconds**. Whole-shift takes are now exact and only the final partial boundary rounds.
  - **`base_rate_cents` is now DERIVED**, not kept in step by discipline: `setRateFrom` writes the history then recomputes the column and reads the row back to confirm it moved. A review caught that `insertCrewMember` and `updateCrewMember` had been writing the column directly, falsifying this module's own comment — no callers today, but row 507's import was exactly the caller that would have found it.
  - Migration applied to prod and **verified behaviour-preserving by query: 33 closed shifts, 0 unresolvable, 0 whose value changed** on the day it shipped.

- **#1215 — a year of Jason's pre-tool spreadsheet imported.** Row 507. **142 shifts, 2024-10-10 → 2026-09-03, 801h 22m.** 95 already-paid days settled by one derived $4,422.95 record dated 2026-07-06; **47 days / 359h 05m / $4,102.03 still owed** and visible in the tool for the first time.
  - **THE FILE CONFIRMED THE RATE HISTORY INDEPENDENTLY.** Its two yellow cells are 12 Aug and 1 Sep 2026 — exactly the boundaries Jason had given from memory the day before. Two independent records of one fact agreeing, and the import ASSERTS it rather than assuming.
  - **Doing 506 first was worth $1,643.44**: those hours are $4,102.03 at his real rates and would have read $5,745.45 at a flat $16.00.
  - Read in TWO steps because the meaning lives in CELL COLOUR — green on Hours = paid, no fill = owed, yellow on Date = a rate boundary. `read-timesheet.py` (openpyxl) emits a reviewable JSON; the TS import drives the app's own functions against it. A CSV export drops all three and would have turned a year of settled history into a year of outstanding wages.
  - **The settlement lines are written BY NAME, not through the allocator**, and that divergence was measured rather than assumed: the sheet's fact is *these 95 days were paid*, while an allocator takes an amount and decides for itself where the money runs out. Run first, it overshot by **6 cent-seconds** and would have marked six seconds of 7 July — a genuinely unpaid day — as paid.

- **#1216 — an admin can add a shift for OFFICE staff.** Ann did not clock in on Monday 24 Aug and there was **no way anywhere in the app** to record her hours. `adminCreateShift` refused office staff by name, for a reason that had EXPIRED: an office row "would be invisible on the review page afterward", true when written, false since the per-person page shipped in S60. INACTIVE is still refused. Built by a Sonnet worker in its own worktree; I verified the guard, mutation-probed it both ways, and drove the form in a real browser.

- **THE HEADLINE MISTAKE, and it cost data.** The import's backup captured the deleted SHIFT rows and not the settlements attached to them — while its own comment quoted the rule it was breaking, *the record goes first or the deletion does not happen*. Jason's $1.00 test settlement was destroyed with **no copy anywhere**; confirmed gone by query, not inferred. What is still known of it is written into the backup file under `settlementsReconstructed`, labelled NOT A BACKUP. **Both review lenses found it independently.** The fix round for it then recreated the same loss through a shared filename — `--live` followed by `--live --undo` overwrote the first backup with the second — which the delta-verify caught. Backups are now timestamped, refuse to clobber, capture shifts + settlements + lines, and cross-check the row count against the ids they were asked to delete.

- **REVIEW.** #1214: four lenses (1 HIGH, 7 MED, 3 LOW) + delta-verify (2 MED, 1 LOW). #1215: two lenses (2 HIGH, 3 MED) + delta-verify (2 HIGH, 2 MED, 1 LOW). #1216: no pre-merge lens round — built and verified by me, then given the full session-review treatment at close. **Every HIGH found in the entire session was in my own code**, and both fix rounds introduced new ones.
  - **Three lenses converged** on the "current" rate badge showing the newest row rather than the rate in force today, so a scheduled raise would badge as current while the pay panel correctly used the old one. Convergence made it a class.
  - **Sixteen mutation probes on #1214, six on the import, two on #1216. Nine fired nothing first time and every one was a real result** — a no-op mutation, two tests asserting a proxy rather than the behaviour their name claims, three genuine coverage gaps, one inert guard, and one honest limit no static render could reach.

- **PROVEN BY JASON, ON REAL MONEY, THE SAME DAY.** He added Ann's 24 Aug shift through the new form (09:00–13:04 ET, 4h 04m, stamped with his name) and recorded her payment. It covered 24–27 Aug and **left 27 Aug part paid** — 3h 47m of 4h 09m, remainder rolling over. Every feature from this session and the last, working together on somebody else's wages.

- **PROD WRITES, all with Jason's named consent:** the migration; his three real rates; 3 test shifts deleted (backed up first) plus the settlement that was not; 142 shifts; one $4,422.95 settlement with 95 lines; 3 retroactive audit rows for the deletions, each saying plainly it was written retroactively and why.

- Gates at close: **tsc 0 · lint 0 errors (23 warnings) · vitest 10618 / 605 files**. Master `5154151b`, prod READY on the same SHA.
