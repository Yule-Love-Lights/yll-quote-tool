### Naldo S43 — TOOLING/INFRA (mostly machine-local): parallel-fanout rule (#485 merged) · /insights + /doctor cleanup · claude-mem disabled · psql backup ARMED · Supabase MCP → PAT-stdio (2026-07-19)

> A long tooling/config conversation (started S31-era, ran concurrent with S32–S42; base was 313 commits stale by close). ZERO app code — one merged repo docs change (#485), everything else is Naldo-machine `~/.claude` config + local memory. Numbered S43 to avoid collision (S41/S42 taken by concurrent sessions).

- **Repo (Jason gets on pull): #485 MERGED** — AGENTS.md "Subagent spawns" list gains a **default-to-parallel-fan-out** rule: 2+ independent pieces → one agent per piece in a single message; stay inline for dependent pieces or a one-file edit; explicit model per spawn in Fable sessions. Mirrors the same rule added to Naldo's global working-conventions.
- **Global rules (`~/.claude/rules/working-conventions.md`, machine-local, not git):** added (1) parallel fan-out default, (2) browser-tool routing (real Chrome for logged-in dashboards / Playwright for fresh-user + repeatable E2E / in-app pane for previews / switch-once-and-say-so fallback), (3) money-feature failure-mode enumeration (list integer-cents / rounding / partial-selection / duplicate-retry / tax-discount as the failing-test list BEFORE building, so the adversarial review verifies rather than discovers).
- **`/insights` review → cleanup:** the report flagged the **claude-mem** plugin's observer as the #1 friction (47% of sessions failing 401 + "Prompt is too long", semantic corpus DEAD since 2026-04-23, a per-tool-call LLM hook ≈ 4h of blocking across 50 sessions). **Disabled it** (`~/.claude/settings.json` false + stopped its worker/mcp-server processes; historical data kept). Then a `/doctor` pass: **retired 141 unused skills** (gsd/firecrawl/seo families, 1 use since April) → `~/.claude/skills-retired-2026-07-13/` (≈4.4k est. tokens/session saved, skill-list back under budget); disabled the never-used **code-review** plugin + **nanobanana-mcp** server; **updated Claude Code 2.1.193 → 2.1.207**; set **auto mode as the default permission mode**.
- **Database reliability (the session's biggest thread):** **psql 17.10 armed as the DB backup** for when the Supabase MCP drops — pgpass built for the IPv4 session pooler `aws-1-us-east-1.pooler.supabase.com` (the direct host is IPv6-only and doesn't resolve on this network), tested live (`select count(*) from quotes` = 126), and `PGPASSFILE` set in the settings `env` block so every session finds it with zero setup. Then **swapped the Supabase MCP from the hosted HTTP/OAuth transport to the token-based stdio server** (`@supabase/mcp-server-supabase` + a Personal Access Token) so it connects each session with NO re-auth — the OAuth version's repeated mid-session disconnects were the real "database update issues."
- **⚠️ Security (handled):** a Supabase Personal Access Token (ACCOUNT-level, all projects) got pasted into chat once during MCP setup → flagged immediately, Naldo revoked + regenerated, config re-verified against the exposed value (confirmed differs). Lesson reinforced: never route a token through chat; have Naldo run the `claude mcp add` in his own terminal.
- **Machine-local memory written (NOT synced to the shared repo — Naldo-machine config):** `claude-mem-disabled.md`, `psql-db-backup.md`, `supabase-mcp-config.md` + MEMORY.md index lines.
- **Also explained (no action):** sessions "randomly archiving" = the desktop app's **Auto-archive after PR merge/close** setting firing when a linked PR merges remotely (15 of ~19 archived sessions had a MERGED PR); harmless — transcript stays on disk.
- **Confirmed (don't re-ask):** claude-mem stays disabled (re-enable = one settings flip); psql = the DB fallback, `PGPASSFILE` set globally; Supabase MCP stays PAT-stdio (do NOT revert to OAuth); auto mode = default permission mode.
- **Gates:** N/A — no app code touched. master at close `293bcf4`+ (docs-only PR); last app-gate baseline is S42 vitest 3635.
- **NEXT:** next fresh conversation = S44. Next session, confirm the PAT-stdio Supabase MCP actually connects (hit a `supabase` tool); once confirmed, delete `~/.claude.json.bak-mcp-swap-2026-07-13`.

