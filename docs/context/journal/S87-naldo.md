### S87 (Naldo), 2026-09-03, every printed QR code the company owns was dead, and the routing lived inside a former agency's unpaid GoHighLevel account. Rebuilt on infrastructure we own. 1 PR open (#1186), worked concurrently with a desktop session that superseded half of it

- **NUMBER:** S85 is merged, **S86 is Naldo's and was used 2026-09-02** (it minted ledger rows 495/496), and S84 is a live unmerged close PR. Took **S87**, checked free against master, the archive, every open PR (18) and every remote branch: zero hits. **One check could NOT be run and that is worth knowing:** the machine-local `feedback_self_assessment.md` — the one place git cannot see, and where two collisions in two days actually lived — does not exist in this remote container. *Caught a live mistake doing this:* I first announced **S86**, having checked only the journal-fragment listing, which structurally could not see S86 because its fragment has not merged yet. The ledger counter is what caught it. Listing fragments is not a session-number check.

- **THE PROBLEM, and it was never a DNS typo.** Naldo reported `link.yulelovelights.com/qr/hbJhlsQLHpFv` dead. DNS said the subdomain resolves to `35.212.83.65`, the same IP as the apex and www — the SiteGround marketing site, which has no `/qr/` route. It had been served by a GoHighLevel account **opened by a former agency**, white-labelled as SurgeCRM, and that account shows *"Your payment method is missing"*. So the routing for every QR code the business has ever printed — **van decals and business cards already in circulation, which cannot be recalled** — lived inside a third party's unpaid SaaS account that can be purged without notice. The fix had to be a domain we own.

- **THE MAPPING, recovered before the account dies.** Two codes existed, no more: `hbJhlsQLHpFv` (van) and `DjzJS9mzhTOm` (business card). Found via `Sites → QR Codes`, after `Marketing → Trigger Links` turned out to be a different GHL feature entirely (one 2023 entry, a 20-char key against our 12-char slugs — the shapes did not match, which is what said "wrong screen" rather than "no data").

- **PR #1186** shipped `GET /qr/<slug>` 302ing to the marketing site with the slug carried as `utm_content`, allowlisted for signed-out access in `operatorGate` in the same commit. Verified by RUNNING it with `AUTH_GATE_ENABLED=true` and no session, with three controls: the gate demonstrably blocking `/inventory`, the allowlist line removed to prove it was load-bearing (it serves the operator LOGIN SHELL without it — indistinguishable from the outage), and mutation probes on every guard.

- **THE WRAP'S FOUR-LENS REVIEW FOUND A HIGH IN MY OWN WORK, AND THE EVIDENCE WAS IN THIS REPO THE WHOLE TIME.** Three lenses independently converged: the business card pointed at `/get-a-quote/`, and **ledger row 272 — open on master, never fixed** — records that a Rank Math redirect created 2026-06-26 makes that URL 301 to the homepage, already fired 48 times. I had the S50 memory of this and told Naldo to go check the page by hand; I never grepped our own ledger, where a previous session had already diagnosed it, named the cause and counted the hits. Also found: prototype keys treated as known codes (`/qr/constructor` emitted `utm_campaign=undefined`, reproduced by running it), the allowlist opened as a broad prefix when `#611` had removed exactly that pattern from `/estimate`, and zero test coverage on the allowlist entry — deleting the line left the whole suite green while re-serving the login page to every scan.

- **A DESKTOP SESSION WAS WORKING THE SAME BRANCH, AND ITS WORK SUPERSEDED MINE.** Naldo moved to a desktop session with a handoff prompt written to make it distrust everything this session concluded. It did exactly that. My push was rejected twice; I read rather than forced, and reset onto their work. **They were right and I was wrong on the substance:** I narrowed the public allowlist to a single segment, which would have sent `/qr/a/b` to the operator login; they made the route an OPTIONAL CATCH-ALL so every path under `/qr` lands on the homepage, and pinned the namespace with a test that fails if a second route ever appears there. They also verified `link.surgecrm.ai` still answers both slugs **by request rather than by screenshot**, and corrected three claims I had written: the old account is not detached from everything, the old system DID 404 unknown slugs (so ours never doing so is a deliberate divergence), and no flyer was ever verified — I had written "flyers" with no evidence. Naldo's product call, now in code: **both printed codes land on the homepage**, still tagged apart by `utm_campaign`, because reproducing the card's original dead-end destination would have reproduced the outage. My only surviving contribution was the one finding they had not reached: the route comment claimed `force-dynamic` meant "never answered from a cache", which is measurably false (it emits no cache header at all), so `Cache-Control: no-store` now makes that sentence true, plus `X-Robots-Tag: noindex` for an unbounded slug space on a brand subdomain.

- **STILL OPEN, and one half of it has a deadline nobody here controls.** Rows **499** (the live cutover: add the domain to Vercel, repoint SiteGround DNS, verify a real scan — and decide what the OTHER legacy paths on that subdomain should do, because today they would serve a homeowner the operator login) and **500** (capture the QR scan analytics from the old GHL account before it is purged; unrecoverable afterwards). Row 272 already carries the `/get-a-quote/` redirect and is unchanged.

- MISTAKES: the S86 mis-announcement; telling Naldo to hand-check a page our own ledger had already diagnosed; writing "flyers" without evidence; and shipping an allowlist narrowing that was worse than the alternative. All in the scorecard.

- Gates at close: tsc 0 · lint 0 errors (23 pre-existing warnings) · vitest **10337 / 591 files**, measured on branch head `ba993926`. PR #1186 open, not merged, no merge-go given. **The branch kept moving after this close** — the desktop session pushed `7b9a6c9a` adding a test that pins the two headers this session contributed, which is the right follow-through since they shipped without coverage. So treat that SHA as "where these gate numbers were measured", not as the branch head: #1186 is a live lane owned by the desktop session, and its final state is whatever that session lands.
