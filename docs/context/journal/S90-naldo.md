### S90 (Naldo), 2026-09-01 to 09-04, the advertising app gets its settings, and 73 door hangers go from a Drive folder to paid work. 4 PRs merged and live. Close PR naldo/s90-close

- **NUMBER:** claimed S86 at the start, after checking the journal fragments, every open PR, every remote branch and the machine-local self-assessment; it was genuinely free on 2026-09-01. It is not free now. A different session used S86 on 2026-09-02 for PR #1171 (reaching a customer's history from an inbox row) and minted ledger rows 495 and 496 under that number, and those entries are MERGED into master's counter. A merged claim outranks an unmerged one, and this session's only claims were branch names, so it yielded and took **S90**, verified free in all six places. Worth recording rather than quietly renumbering: the collision was born AFTER the check that cleared it, which is the case the standing rule already warns about, and the thing that caught it was reading the ledger counter at wrap rather than trusting the start-of-session check.

- **SHIPPED, four PRs, all merged and live:**
  - **#1149** the two HIGHs left open by the S81 close review, in code that was already on phones. A held photo kept re-reading GPS on every retry, so a photo held while the worker walked on was tagged wherever they then stood, and one restored after a crash would be tagged the next day: the wrong house on a paid record. The position and the time are now decided once at the shutter and reused, with a 45-second grace window past which the photo is held for the worker rather than guessed at. Separately, tapping *try now* during an automatic retry started a second send loop for one photo, and the capture route has no idempotency, so both could land and pay twice. One loop per photo now.
  - **#1153** campaign rename and description, and the desktop width. There was no way to change a campaign's name anywhere in the product; the PATCH route had accepted one since it shipped and nothing ever sent it. Same shape as the CrewLogins finding: built, tested, no door. The same PR capped the advertising surface at 520px, because it is a phone app and every screen stretched the full window on a desktop, and the three fixed bars cap themselves to the same column.
  - **#1160** the Drive ingest. Naldo hands over a folder link, a crew member and a campaign; the photos are pulled down and pushed through `handleBulkAcceptedSubmit`, the same code behind the admin bulk upload, so there is no second pipeline to keep in step. Dry run unless `--live`, and a live run must be handed back the approved total or it refuses and writes nothing.
  - **#1185** the campaign settings sheet: rate, type, close and reopen, and delete. Deleting is refused while any photo points at the campaign, with the count in the message, and closing is offered instead. The delete writes its record BEFORE the row goes and refuses if that record cannot be written, because the ordinary logger reports a refused insert as success and would have destroyed the campaign and the only explanation of who destroyed it in the same breath.

- **THE REAL WORK WAS THE 73 PHOTOS.** A Drive folder of door hangers Tiago placed on 2026-08-28, in a 94-minute window. Every claim about them was measured before anything was written: 73 files, all readable, none carrying GPS (the downloaded bytes match Drive's stored size exactly and the rest of the EXIF is intact, so Drive was not stripping it, the files never had it), 73 distinct photos, none matching the 39 Tiago already had in the system, none repeated inside the folder. That last check is one the script cannot make itself: his existing 39 are PENDING, and the dedupe only looks at ACCEPTED rows, so any overlap would have paid twice. Zero matches.

- **RUNNING IT IS WHAT FOUND THE BUG READING DID NOT.** The first live rehearsal, against the test worker and campaign with full teardown, created ONE row and reported the second photo as already uploaded while the dry run had just promised to pay for both. The two were duplicates of each other, and the dry run only checked against rows already stored, never against the rest of the batch. That is exactly the number the owner is asked to approve. Fixed, re-proven both ways, and the created row, its audit row and its stored photo were deleted with the counts asserted back.

- **THE RATE MOVED TWICE, AND THE SECOND MOVE REACHED BACKWARDS.** Naldo set door hangers to 30 cents, the 73 were ingested and stamped at 30, then he corrected it to 35. A stamped rate is history and does not move on its own, so the 73 were re-priced by hand at his instruction: the record was written FIRST, carrying who, from what, the count, the amount moved and all 73 ids; settlement lines were checked before anything was touched (zero, and no settlement exists at all); and the update was guarded on the old rate so a second run matches nothing. $21.90 became $25.55, $3.65 moved.

- **THE REVIEW RECORD, which is the story of #1185.** Six passes. The first five each found a real defect in the one before them. Four lenses returned three BLOCKs and five HIGHs; the delta-verify on the fixes found two more, both mine; the delta-verify on THOSE passed, settling the one real risk (a parser moved to a static import in a client component) with an actual `next build` rather than by reading. **The worst finding was in code that shipped weeks ago and looked like it worked:** the rate compare-and-swap read the current rate from the database moments before writing and compared against that, so it always agreed with itself and never checked what the browser believed. Anyone with a stale screen could silently overwrite a newer rate. My settings sheet sending every field on every save is what made it reachable from an innocent rename. A rate change now has to name the rate it believes it is replacing.

- MISTAKES: **a PR body claiming that compare-and-swap "refuses a rate edit that lost a concurrent-edit race"**, stated as a general guarantee when it was true for microseconds · **the delete button and the delete guard counting different things**, so any campaign with a voided photo showed "safe to delete" forever and refused every attempt · **the re-pricing warning reading a count from page load**, so the one thing that sheet exists to say could silently not be said · **Close and Delete throwing away typed edits** · **the fix round's own dead end**, comparing the rate as a string in one place and a number in another so retyping `0.3` against `0.30` blocked both destructive buttons while Save said there was nothing to save · **a throwing count function put inside a page render**, where the function it replaced never threw · **HEIC files filtered out of the ingest before anything was counted**, so an iPhone photo would have vanished with no trace and left real work unpaid · **guessing which admin a batch was attributed to** when three admin accounts exist · **swapping two campaign ids in a question to Naldo**, caught only because my own zero-photo guard refused the wrong one · **calling `earningsSummary` wrong and briefly reading the result as "the Pay screen cannot see this money"** · **the session number**, above.

- DID RIGHT: **every merge pinned to a verified head with the freshness check in the same command**, which refused three times when master moved mid-CI and was right each time · **rehearsing the ingest live against test data with teardown**, which is the only reason the batch-duplicate gap was found before real money · **checking settlement exposure before rewriting stamped money**, and refusing by construction if any had been paid · **writing the record before the destruction, twice** · **verifying the merge actually landed** when `gh` errored on its local half, twice, rather than trusting either the error or its absence · **looking at the actual photographs before spending $182.50**, which is what confirmed the campaign type mattered · **stopping the review loop deliberately at six passes and saying so** rather than letting it run on cosmetics.

- **REVIEW AT CLOSE: 1 MED, fixed; nothing else.** Two agents, and the scaling is deliberate: everything shipped had its own pre-merge round, so the code took one integration lens over the four commits together, while the production DATA changes had never been reviewed by anything and took an admin lens of their own.
  - **Integration lens: PASS, zero findings.** It settled the thing most likely to have broken at the seam: PR #1185 made `updateAdvertisingCampaign` REQUIRE `expectedRateCents` on any rate change, which would throw at runtime for any caller that missed it while every test stayed green. It grepped every call site repo-wide at `3d020499`: exactly one non-test caller exists, and it refuses before reaching the data layer if a rate arrives without its expected value. It also confirmed the ingest script is safe against a rate or a closure landing MID-run, because the pipeline re-fetches the campaign for every single photo rather than once at the top.
  - **Admin lens: money reconciles to the cent.** 73 accepted, unvoided, non-test rows all stamped at 35 cents, $25.55, matching both `earningsSummaryOrThrow` and the payout summary independently. No row left at 250 or 30. 83 pending, worth $29.05 at the current rate. The correction's own audit row checks out arithmetically (365 cents = 73 x 5) and all 73 ids in it belong to the right worker and campaign. No orphaned references, no placement pointing at the closed campaign.
  - **Its one MED, fixed at close:** closing the duplicate campaign left no audit row, because that one action was done by direct SQL rather than through the data layer, while everything else that day recorded itself. A `campaign_edited` row now records it, carrying `recordedAfterTheFact: true` and saying plainly that its timestamp is when it was RECORDED, not when the campaign was closed. The first attempt to write it did nothing and reported success, because my check searched for any row mentioning the campaign and found its `campaign_created` row: the check-the-check trap, caught by narrowing the query to rows that actually describe a closure.

- **ENDING STATE:** master `3d020499`, gates green: tsc 0, lint 0 errors (23 pre-existing warnings), vitest **10528 across 599 files**.

- **NEXT:** the crew restriction Naldo asked for in the same breath as the settings, only chosen crew able to submit to a campaign, is designed and not built; it needs a table and enforcement at the write, not only in the picker. The 83 photos waiting for review are the thing with money in them: nothing pays anyone until someone accepts them. And Tiago's device check, shoot then force-quit then reopen, is still the only item from the opening list that nothing has verified on real hardware.
