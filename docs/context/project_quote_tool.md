---
name: project-quote-tool
description: "AI Quote Tool — current state, confirmed decisions, run commands, gotchas, and what's next. Read first each session."
metadata: 
  node_type: memory
  type: project
  originSessionId: 834b8d13-f89f-476d-bae1-0a9ab5613799
---

# AI Quote Tool — project state (READ FIRST)

> Session-continuity layer — the quick "where are we / what's decided" so a cold session resumes without re-litigating. Deep detail lives in the repo: `docs/ONBOARDING.md` (setup), `docs/CURRENT_STATE.md` (done / half-done / bugs), `docs/CONVENTIONS.md` (how to add code). Pair this with `session_log.md`.

## Current state at a glance
- **Pre-launch.** This tool is NOT customer-facing or used internally yet. The live customer flow today is **home.works** (separate system). So changes here are lower-risk — we're building *toward* launch.
- Deployed on **Vercel** (`yll-quote-tool`, Production tracks `master` → quote.yulelovelights.com), but not yet shown to customers. **Prod is LIVE + current as of S9** — it had silently been frozen at an Apr 23 build for ~7 weeks (Vercel Git was connected to the wrong repo); reconnected to `Yule-Love-Lights/yll-quote-tool` + env vars synced S9. See [[deploy-vercel]] (incl. the "if prod looks stale" playbook).
- Runs locally on **Next 16.2.6** (Turbopack), connected to the **real Supabase** (~55 real/test quotes). `.env.local` is filled on Jason's machine (values: see `project_secrets_access.md`).
- Gates all green: `npx tsc --noEmit`, `npm run lint`, `npm test` (Vitest).
- **#83 Jobber-flow engine BUILT (Naldo S8, 2026-06-27):** all 5 phases' data-layer cores PR'd + adversarially reviewed + fixed (open stack #185–#194; P5's migration applied to prod). Merge + every money/operator surface gated on **#81 (auth)**. See the ledger #83 row + the S8 session-log entry + `docs/jobber-flow/REVIEW-2026-06-27.md`.

## Scope / out of scope (now)
- **IN:** QA-driven bug fixes + feature work on the quote builder, pricing engine, and customer portal (the punch-list below).
- **OUT / deferred:** wiring prod CRM (on hold during testing); **home.works integration DROPPED (Jason, S9 — task #16 shelved):** we're not wiring home.works into this quote tool (the approve route's Zapier hook stays in code but goes unused); the portal package tiers (Classic/Festive/Yule) are Claude-generated placeholders to configure later; auto-send. *(The old c9Lines-render/SSIM/Replicate-pin backlog died with the #36 render teardown.)*

## Decisions confirmed — DON'T re-ask
- **Homepage (`/`) is intentionally untouched boilerplate (Jason, S11):** the root renders the default Next.js "Create Next App" page ON PURPOSE — it's a placeholder reserved for a future **internal staff dashboard** (task #58). Do NOT flag it as a stray/broken page or "fix" it; the real app entry points are `/quote/new`, `/admin/quotes`, `/portal/[id]`, `/settings`, `/training/*`. (Tab title still says "Create Next App" — fix to "Yule Love Lights" when the dashboard is built.)
- **Roofline model (task #17 — Phase 1+1b+2+4 DONE; #17 functionally complete, merged):** 🔴 red = **front roofline = Santa's**; 🔵 blue = **ridge + sides**. A quote **PRESENTS BOTH** a Santa's option (front) AND a Gingerbread option (**front + ridge + sides**) — both shown; **mutually exclusive at SELECTION** (the customer picks one, never both). Operator quotes everything; staff **recommend** one (the portal default); when staff doesn't pick, the engine **auto-selects the option that lands the quote total closest to the $1,000 minimum WITHOUT going under** (else the larger). **Phase 1+1b (S2):** engine prices + exposes both on `QuoteResult.rooflineOptions`, bills only the recommended (`rooflineChoice` input + auto-default); builder breakdown has recommend radios that re-price in place. **Phase 2 (S3):** the customer portal now shows BOTH options as **ordinary "What's Included" line items** (no dedicated selector, no footage on the portal) — only the staff pick is selected by default; clicking the other deselects it (mutually exclusive — never both); clicking the selected one removes it (like any item, no Remove button). Adapter splits the billed roofline into `roofline-santas`/`roofline-gingerbread`; `PortalRoofline = { itemIds, recommendedItemId }` drives the either/or in `SelectionContext.toggleItem`; only the recommended option is bundled into tiers + the $1,000 gate (tierLineItems filter ⇒ no double-count). **Phase 4 (S3, PR `jason/roofline-phase4`):** the AI prompt (`photoAnalysis.ts`) now classifies front-facing gutters/eaves + **front-gable rakes** as red/Santa's, and the **ridge + side-facing runs** as blue/Gingerbread (*gable* = the triangular wall under a peak; *rake* = its two diagonal edges). **FRONT SECTION OF THE ROOF ONLY** — the part facing the road; NEVER the full perimeter or the back/backyard (~90% of jobs; whole-roof/backyard done manually). Must identify the REAL road (not a neighbor's/shared driveway) — the satellite rule was the big fix (it had traced the whole perimeter + mis-picked the front). **Downspouts** (vertical wall gutters) are never traced. Builder/training labels → "Front Gutterline" (red) / "Ridge + Sides" (blue); "Gingerbread Ridge" renamed → **"Gingerbread"** (render-variant KEY `'ridge'` kept). **Validated S3** (PR #14 merged): Jason added 2 correct corrections + analyzed a held-out house — **classification fix confirmed** (street: sides no longer red; satellite: front-section only, no longer wraps the perimeter). Remaining issues are AI **accuracy, not classification**: street lines don't hug the roof (PRE-EXISTING vision limit — drag-to-fix workflow), satellite orientation flips (red on ridge / blue away from road) — **folded into the AI-training-system task** (see Next up). Note: corrections store only the street photo (no satellite image) so they can't teach satellite tracing; only the latest 2 corrections feed the model. C9 / Winter Wonderland left untouched throughout.
- **$1000 minimum (CHANGED in S2, task #18):** NO LONGER an engine floor — `pricingEngine` returns the real total so staff can intentionally send sub-$1000 quotes (the "Minimum quote applied" note is gone). It's now a **customer-portal approval gate**: Approve is disabled until the selected pre-tax subtotal reaches $1000 (with an "Add $X more" nudge). **Waived** (gate off) when the whole quote's items total < $1000 — staff override, else it'd be un-approvable. Portal shows real prices + a Subtotal/Tax/Total/Deposit tie-out (tax now visible). *(Supersedes the old "minimum applies everywhere / floor" rule.)*
- **Multi-image quoting:** extra images are **manual-only** (no AI auto-quote) to avoid double-counting; no cross-image AI dedup. Image 1 covers the roof.
- **Light color/pattern (#10 — SHIPPED S9, PR #38):** a CUSTOMER-facing swatch picker on the portal hero. Picking a whole-house color/pattern **recolors the live design in real time** (`render-readonly` overrides light items' `colorPattern` at render time — strand/spritzer/mini-area only; shallow clone so the scene is never mutated; **"As designed" = no override**, render the operator's authored colors). Choice frozen into the **approval snapshot** (`customerSelection.colorSchemeId`); **no pricing impact, no migration.** The scheme list is one editable array in `src/lib/design/colorSchemes.ts` (12 entries). **Operator-default = WON'T-DO (Jason, S9, confirmed):** the portal ALWAYS opens "As designed" — that's the intended behavior; there is NO builder control and NO data-model field, and we're not adding one.
- **Wreath/garland pricing (#17, S8 — Naldo's numbers all in, NOTHING pending):** TWO tiers only — `bow` = **Non-Decorated**, `fullDecor` = **Decorated** (the old "Labor" tier was removed; internal keys kept to avoid a codebase-wide rename). 36" Oregon wreath dropped; **60"/72" wreaths added**; standalone bow **$35**; **tax 8.75%**. Wreath non-deco 24/30/36/48 = $200/$285/$315/$450 (deco unchanged); 60" = $885/$1130, 72" = $1149/$1455. Garland non-deco 9ft $162 / 4.5ft $135 (deco 250/210). All numbers live in `BUSINESS_RULES` (pricingEngine).
- **Workflow:** never commit without Jason's explicit "yes"; PR-not-master; run gates (tsc + lint + test) before commit. Jason + Naldo are on different machines → `docs/context/` is the canonical shared memory.
- **Pushing:** **Claude runs `git push`** (Jason approved in Session 1). Pushing to the `Yule-Love-Lights` org is NOT blocked here, unlike the design tool's exfil guard. Commits still require Jason's explicit "yes" first.
- **Portal consolidation (next up, task #27):** `/portal/[quoteId]` becomes THE canonical customer URL serving the **Snowglobe** design; the other 3 portal routes (`portal-snowglobe`, `portal-dark`, `portal-concierge`) get retired. ⚠️ KEEP the `components/portal/dark/*` + `snowglobe/*` COMPONENT folders — Snowglobe reuses them; delete only the route folders, and grep-verify before deleting any component.
- **🔗 Design-tool integration — Path B (CONFIRMED, groundwork only — joint initiative):** absorb the **design tool** (a separate Konva canvas app that draws glowing lights on a house photo → produces an editable `scene` JSON) INTO this quote tool — designs stored in **Supabase** alongside quotes, one login/customer (the quote's HighLevel contact), **line-items ⇄ scene-items linked BOTH ways**, and the **static AI portal-hero render replaced by a LIVE editable design** (kills the ~90s + Gemini render cost). ⮕ **Resolved end-state:** the Gemini render pipeline was **fully REMOVED in #36 (S7)** — the live design is the only portal visual now. Full plan: [[project_integration]] (mirrored shared doc); design-tool internals/schema/API: `docs/design-tool-context/`; design-tool repo on disk `C:\Users\Jason\Desktop\YuleLoveLights\Claude`. **Phase 1 DONE (S4, merged PR #18).**
- **🔗 Integration items model — DECIDED (Jason + Naldo, S4):** **Option-1 + Option-2 HYBRID** — the **design is the master list** for standard items (roofline, minis, wreaths, spritzers, garland; one source ⇒ no double-count) **+ a "custom/manual line item"** feature (name/price/description; on quote + portal, NOT on the design) for ~5% niche quotes. **Roofline** stays measurement-driven (the one exception); **mini-lights** = design-driven but priced by a **staff-typed strand count × rate** (not length). **Mini-light editor tool = Approach B:** place a resizable **box** OR **trace an enclosed shape** (auto-closed) → fills with single lights; **density = visual only** (price = strand count). New SHARED-editor item type (coordinate w/ design-tool AI). Railing-as-a-line is an open gut-check. Details in [[project_integration]] (Phase 2 plan + Step-3 decisions). These are **Step 3** (after Steps 1–2).

## How to run locally
- Dev server (repo root). Via Claude Code's **Bash** tool, prefix to dodge the empty-`ANTHROPIC_API_KEY` shell quirk:
  `unset ANTHROPIC_API_KEY; unset ANTHROPIC_BASE_URL; export PATH="/c/Program Files/nodejs:$PATH"; npm.cmd run dev`
  (Normal terminal: just `npm run dev`.) → http://localhost:3000
- Gates: `npx tsc --noEmit` · `npm run lint` · `npm test`
- Key pages: `/quote/new` + `/quote/<id>` (operator) · `/admin/quotes` · `/portal/<quoteId>` (Snowglobe — the one portal) · `/training/*`. *(The `/admin/renders` pages were deleted in #36.)*

## Known gotchas
- Empty `ANTHROPIC_API_KEY` in the Claude-Code shell overrides `.env.local` → Claude routes 503. Unset it before `npm run dev`.
- `react-hooks/set-state-in-effect` is at **error**; the fix pattern for legit effects is a `queueMicrotask`/rAF defer.
- Vercel env vars are all **"Sensitive"** (unreadable) — pull secret values from source accounts (`project_secrets_access.md`).
- Vercel env parity (see [[deploy-vercel]]): S9 added the `HIGHLEVEL_*` (×7) + `NEXT_PUBLIC_PORTAL_*` (×5) vars to prod so it matches dev's `.env.local`. home.works still off (mismatched var names + **dropped, #16**). "Works on dev, 'X not configured' on prod" = a missing Vercel env var; `NEXT_PUBLIC_*` are baked at build (redeploy after adding); never add `SUPABASE_DB_URL` to Vercel.

## Task list → see the master ledger
The full task list (done / planning / backlog / Naldo-pending) lives in **[[task_ledger.md]]** — the single source of truth, renumbered into one clean sequence on 2026-06-05 (now #1–#28) with an Old-# column mapping to the historical numbers in this log + PR titles. **Next BUILD (S4): task #27 — the design-tool integration (Path B)**, starting Phase 1 from the LOCKED [[project_integration_data_contract.md]]. #8 (training-system) planning is DONE (feeds #27 Phase 3). Update the ledger as work lands.

## Next up
**Done in Session 2 (all merged to `master`):** #27 portal consolidation + dead-`dark/StickyBottomBar` cleanup + #25 (discount `20`) + #24 (C9 delete resets footage) + #18 ($1000 min → portal gate) + #4 (rush/premium portal toggles) + **#17 Phase 1+1b** (roofline Santa's/Gingerbread mutually-exclusive engine model + builder recommend radios).
**Done in Session 3 (2026-06-03):** **#17 Phase 2** (merged to `master`, PR #13) — customer-portal roofline switch (both options as mutually-exclusive "What's Included" line items; only the recommended bundled into tiers + the $1,000 gate; backward-compatible). **#17 Phase 4** (PR `jason/roofline-phase4`, merge-all-now) — AI prompt reclassification (front=red/Santa's, ridge+sides=blue/Gingerbread, **front section of the roof only**, downspout exclusion, correct road ID) + builder/training labels ("Front Gutterline" / "Ridge + Sides") + "Gingerbread Ridge"→"Gingerbread" renames. Repo docs sync folded into each PR.

**Also done in Session 3 (planning, no code):** #8 AI training-system refinement — issues + direction captured ([[task-ai-training-refinement]]); planning DONE. The **design-tool integration (#27)** was scoped end-to-end: design-tool context imported (`docs/design-tool-context/`), Path-B plan + resolved decisions in [[project_integration]], and a **LOCKED data contract** in [[project_integration_data_contract.md]]. New tasks raised: #25 (satellite-default pricing + bug), #26 (canvas zoom), #28 (standalone-bow line item).

**Done in Session 4 (2026-06-05) — #27 Phase 1, BACKEND half (branch `jason/integration-phase1`, off `master`):** the Supabase **`designs` table + private Storage bucket** (migration applied by Jason), **`src/lib/designs.ts`**, and the **API routes** (`POST /api/designs`, `GET|PUT /api/designs/[id]`, `POST /api/designs/[id]/photo`). Smoke-tested live; gates green. **Key decision (amends data-contract §3): a design is INDEPENDENT** — its own id + an OPTIONAL quote link set on "Calculate Quote" (which is what saves a quote — no separate Save button) — so designs can be built BEFORE a quote and even with no quote (future standalone site).

**Also done in Session 4 (Day 2, +~23h):** the editor-port **FOUNDATION** — vendored scene **types + guards** → `src/lib/design/sceneTypes.ts` (with the §4 binding fields); `src/lib/designs.ts` uses the real `Scene`. Commit `e79cd34`. Gates green.

**Also done in Session 4 (Day 3) — #27 Phase 1 EDITOR PORT (Option B); PHASE 1 FUNCTIONALLY COMPLETE.** Commit `359b6ab`. Vendored the design tool's Konva editor → `src/components/design/editor-core/` (renderers + ~4097-line controller; imports → `@/lib/design/sceneTypes`; Fastify fetch → `storage.ts` connector over `/api/designs`). `konva@9.3.18`, item PNGs → `public/items/`, `editor-core/**` ESLint-ignored. `DesignEditor.tsx` = client-only React shell (`next/dynamic` ssr:false) mounted as a collapsible "Design" section in `quote/new` (created on open, seeded with the builder photo, linked to the quote on Calculate, autosaves). **Full-screen toggle + Escape-to-exit** (the embedded box was too cramped — Jason's feedback); **Settings button hidden** (deferred); CSS scoped under `.yll-design-host`. Bug fixed: canvas now refits via a `window`-resize handler added to `editor.ts` (the one vendored-core edit — relay to design-tool AI to upstream). Verified in a headless browser.

**Phase 1 MERGED (PR #18 → `master`).** **Phase 2 Step 1 DONE** (branch `jason/integration-phase2-step1`): the linked design renders LIVE on the portal hero (read-only) + a daytime⇄lit toggle, replacing the static placeholder; backward-compatible fallback. Plus 3 fixes from testing (spritzer floor + ray-density, portal cover-fit). Items model DECIDED (Option-1+2 hybrid; mini-light tool = box/shape + density-visual-only + staff strand count; railings grouped; custom line item) — see [[project_integration]].

**Phase 2 Step 1 MERGED (PR #19).** **🔑 Step-2 recon → re-scoped to the FULL PROJECTION (Jason chose Option 2):** the portal toggle→filter can't be done without linking the design's items to the quote's line items (today they're separate id-spaces; the scene is untagged). So the next chunk is the whole binding/projection (the design becomes the master item list — the cemented hybrid model).

**✅ Done in Session 5 — the FULL PROJECTION (A1→D) + A2; design IS the master item list; MERGED to `master`.** Branch `jason/integration-projection` (~16 commits `3791f8d`→`8a19e2f`), gates green (**99 tests**), PR'd + merged. **B** projection lib · **contract v0.3→v0.4** · **C1** engine `customLineItems` + `/api/quote` server-side projection · **C3a** builder custom-items + `designId` · **A1** vendored gated Quote-binding editor · **D** portal toggle-filter · **A2** Scattershot mini-area tool (+ colors) + **railing/column** billing at the standard **$35/string** (no wrap style; only trees vary canopy/trunk) + railing **grouping** (≥2 strands → one `MiniGroupItem` → one "Railing – N strings"). Cores byte-identical with the design tool (hash-verified), zero `[yll]`. **Decisions cemented (don't re-ask):** linkage = "live from design"; per-instance; **drawn size is VISUAL-ONLY → billed spec in staff-set `quote*` fields**; custom items have qty; no-design quotes keep manual entry (2a); railing/column = canopy rate (no Naldo $).

**✅ Done in Session 6 — #31 edit an existing quote (branch `jason/edit-existing-quote`).** `/quote/[id]` reopens a saved quote in the builder: form + saved result + the linked design hydrate (the design section opens the EXISTING design via `getDesignByQuote`); **Calculate UPDATES the row in place** — and `/quote/new` no longer duplicates rows after the first save (Jason-approved behavior change); editing sent/approved quotes allowed with a warning badge; `/admin/quotes` rows have **Edit** links. Builder extracted to `src/components/quote/QuoteBuilder.tsx`; form⇄inputs mapping in `src/lib/quoteForm.ts` (tested, incl. round-trips). Edit-mode caveat (by design): the photo-analysis section starts empty — the street photo isn't stored on the quote row; the design carries its own photo so the design editor is fully functional. Details + landmines fixed (detection-sync mount-wipe guard, updateQuote customer columns): S6 session-log entry.

**✅ Done in Session 6 — #33 roofline picture-toggle + #28 standalone bow (branch `jason/roofline-picture-toggle`).** **#28:** drawn bows project per-instance ("Bow" line item + portal toggle, Ribbon icon, Tier C) + a manual "Bows" builder section; **price $0 placeholder — Naldo to set `standaloneBowPrice`** (Jason approved $0 visible on the portal). **#33:** the S5 hide mechanism was already complete — shipped the missing TAGGING: the builder's street measurement lines auto-seed as pre-tagged C9 strands (at design creation + a "Sync roofline from measurement" button; `seedRoofline.ts`; replacement keys on the surface tag — untagged hand-drawn decor untouched; legacy hand-drawn rooflines need a one-time manual delete after the first sync). Contract → **v0.5** (Jason relays to the design-tool AI; nothing for them to implement). Santa's↔Gingerbread now flips the live PICTURE (front-only vs front+ridge+sides) and Winter Wonderland + drawn bows toggle too.

**✅ Also in Session 6 — #35 raised (Jason) + Phase 1 SHIPPED (branch `jason/design-first-builder`).** **Design-first `/quote/new`:** the measurement box = tabs **Design (Street) | Satellite** — the design tool moved up and IS the street view (eager design creation on photo arrival; camera controls inside; AI roofline lines still seed tagged strands so the portal picture-toggle survives); satellite/manual = the only measurement sources (no more "Pricing uses" radio — solves most of #25); per-unit manual form sections REMOVED entirely (custom items stay — the escape hatch); builder render preview removed; `<form>` wrapper dropped (editor's type-less buttons would implicit-submit; Calculate = plain button, **Enter no longer calculates**); **C3b shipped** (`DesignSummary` — live projected items + prices under the editor). Per-unit items are authored ONLY on the design now. QuoteBuilder 2,963→~1,800 lines.

**✅ Done in Session 6, Day 2 — #35 Phase 2: the BRIDGE AUTO-DESIGN (#35 COMPLETE).** `seedFromAnalysis.ts`: the AI's analysis now BECOMES the design — roofline → tagged strands, bushes/trees → Scattershot areas, columns → strands, wreaths/spritzers/garland → placed items with billed specs, **plus a derived 5 ft scale yardstick** (the AI's roofline-pixel-length ÷ footage = px/ft; without it everything rendered huge — Jason's catch). Staff calibration/items always win (`seed-` prefix + tag replacement rules); seeded garland bills 1 section until staff set the count. Designs open already designed + correctly scaled; staff refine and Calculate.

**✅ Done in Session 7 (2026-06-12) — #36: GEMINI RENDER PIPELINE TEARDOWN (branch `jason/render-teardown`).** Executed from the S6-locked plan + recon journal: 17 files deleted (rendering lib, 4 routes, 3 admin pages, 2 already-orphaned portal files), `photos.ts` → null-URL fallback, `variantPhotos` plumbing purged, `migrations/2026-06-12-drop-renders.sql` written (Jason applies + deletes the `renders` bucket in the UI), FULL-SCHEMA/example-env/docs scrubbed, 7 env vars removed (KEEP `GOOGLE_MAPS_API_KEY`+`ANTHROPIC_API_KEY`; sharp + `@anthropic-ai/sdk` stay), video untouched, #18 closed as moot. Details: S7 session-log entry.

**✅ Done in Session 7 — #8 STAGE A (AI training CAPTURE) — MERGED (PR #30).** Every sent quote auto-captures a self-contained scene-based training example (street+satellite photos + AI's raw analysis + staff's FINAL design); fed back into the few-shot. + manual "Save as training example" button, #9 manual satellite upload, `/training/examples` review page w/ overlays. Adversarially reviewed (15 findings fixed). Editor-core got a `flushSave` (vendored edit — **upstreamed by the design tool at `655b8e8`**; our copy still has `[yll]` comment markers → tiny re-sync follow-up). Detail: [[task_ai_training_refinement]] + S8 session log.

**✅ #8 STAGE B (similarity RETRIEVAL) — BUILT (S7), RE-VERIFIED + SHIPPED (S8).** *(All of #36 + Stage A + Stage B = one Session 7; the fresh session that shipped it = S8.)* Unified pipeline: embed the house photo (Voyage `voyage-multimodal-3.5`, pgvector) → retrieve the most VISUALLY SIMILAR past examples as few-shot, with automatic recency fallback (no toggle/cutover; behaves like newest-8 while the library is small, self-upgrades as it grows). Cap=8. Review caught + fixed 2 criticals (Voyage request/response shapes); PROVEN LIVE (embed→store→cosine-rank). Migration APPLIED to prod. Branch `jason/few-shot-retrieval`. **S8: re-verified live with Jason** (green note flipped to "similar"; prod DB count `{total:2, embedded:1}`; gates 165 tests) → **SHIPPED** in 4 chunks (`e9a09e7` embeddings+migration · `cdfe543` fewShot assembler+capture · `2f2cd3e` routes+builder note · + docs/context-sync commit); pushed; **MERGED to master** (Jason, S8). **`VOYAGE_API_KEY` now in `.env.local` AND Vercel** (Jason added it). ✅

**Done in Session 9 (2026-06-16):** **#10 portal color/pattern picker** (PR #38 → `69cb498`) — customer-facing swatch picker on the portal hero; live whole-house recolor; "As designed" default; choice in the approval snapshot; operator-default DROPPED; editable `colorSchemes.ts`. **#26 measurement-box zoom/pan** (PR #41 → `0c1128b`) — shared `useImageZoomPan` hook (wheel = cursor-anchored zoom 1–4×, drag = pan, Reset); quote/new Satellite tab + /training/new markup; transform on the coordinate element so point-drag stays accurate. **#32 Settings-menu epic — Phase 1** (PR #43) — `app_settings` table (prod) + `/api/settings` + `lib/appSettings`; shared `editor-core/renderSettings.ts` seam + `spritzer.ts` reads the configurable density (7–36 clamp kept); `/settings` page (Palette + Rendering tabs); applied in editor shell + portal; relay sent + **CLOSED** (cores byte-identical). **+ Phase 2** (PR #44) — per-type seed defaults: data-driven `toolDefaults.ts` + `SettingsField`/`DefaultsTabPanel`, wired as the Lights/Decor/Text/Poles/Custom tabs; NO editor-core change. **+ Phase 3** (PR #46 → `734a634`) — custom graphic library: public `custom-uploads` bucket + `custom_uploads` table + `lib/customUploads` + `/api/uploads` + `/photos/[...path]` resolver (so vendored `custom.ts` renders custom items in editor + portal, NO editor-core change) + Custom-tab library UI. **#32 COMPLETE — full Settings menu (7 tabs: Palette · Lights · Decor · Text · Poles · Custom · Rendering).** Only editor-core change across all of #32 = the `renderSettings.ts` seam (relayed + closed). Plus ledger housekeeping (PR #40): #15/#16 shelved, #10 operator-default won't-do. Gates green (231 tests). See [[session_log]]. **⚠️ docs/context NOT synced since PR #42 (#26 close)** — batch the S9-tail + #32 into the next docs PR.

**Done in Session 10 (2026-06-18) — King's Park Day beta push (booth Sat 06-20):** Jason's site review → renumbered backlog **#37–#57** in [[task_ledger]]. Shipped + MERGED: **#47** toggles count toward the $1,000 portal gate (PR #49); **#40** Sep/Oct early-install discount — `BUSINESS_RULES.earlyInstallDiscounts` (15%/10%), whole-order pre-tax, mutually exclusive with rush, gate measured PRE-discount, captured in the approval snapshot (PR #50); **#39** "You're Booked" polish — install/takedown windows dynamic from `approval.installTiming`/`takedownSelected`, "Text Us Directly" + business number, removed the false "receipt emailed" line, "no need to be home" (PR #51); **#37** quote-send→GHL — find-or-update-or-create the Christmas-Lights opportunity at Bid Sent (title=name, value=total, Source="Quote Tool") + text+email the portal link via GHL conversations (PR #52); **#48 + #57 slice** — light-color picker moved out of the hero into a band below the packages, mobile hero sky shift (pure translateY, no side-crop), 44px Approve tap target (PR #53). Gates green (tsc · lint 0/2 · **253 tests**). **GHL is LIVE** (probe-verified) + token Conversations scope added + new env `HIGHLEVEL_SMS_FROM_NUMBER`/`HIGHLEVEL_EMAIL_FROM`/`PORTAL_BASE_URL` (local+Vercel); prod portal phone → (631) 517-0186. **#38 payment processor identified = ValorPayTech** (recon done — Passage.js embed + auto-vault "The Vault" + HMAC webhooks; full spec in [[task_ledger]] #38), gated on Jason's Valor creds. Detail: [[session_log]] S10.

**Done in Session 11 (2026-06-22):** ValorPay API access slipped to Mon → shipped a **pre-Valor PLACEHOLDER deposit flow** (#38a, PR #55): Approve → snapshot → text+email the customer ("we'll reach out for your 50% deposit") + email the internal `sales@` GHL contact; NO GHL move; booked-page reframe; removed the home.works/"Interested" bits from the approve route. **#39 Naldo answers** done (premium takedown "Starting Jan 1", referral **$150**, dropped the refund-policy line + "Your crew lead" + the broken referral link box → #41). **#43** post-approval booked/read-only portal — sticky `BookedBanner` + `SelectionContext.locked` (PR #56). **#55** removed the admin Video link (PR #57). **#60** walkthrough video fixed on prod (added `NEXT_PUBLIC_PORTAL_WALKTHROUGH_VIDEO_ID` to Vercel; no code). **#61** hero header restructure — heading→top, removed location/walkthrough, daylight toggle lifted to `SelectionContext` + moved to the Light Color band (PR #58). **#62** Street View pulled rectangular `640x400` (PR #59). New env (local+Vercel): `HIGHLEVEL_INTERNAL_CONTACT_ID`, `NEXT_PUBLIC_PORTAL_WALKTHROUGH_VIDEO_ID`. New tasks raised: **#58** (homepage→staff dashboard; boilerplate is intentional, don't "fix" it), **#59** (staff per-quote waive-$1k override), **#63** (draw strand over strand — shared-core, Option A). **+ late S11 (after a Claude outage that 500'd "Analyze from Address" on prod):** shipped **#64 analyzer FAIL-SAFE** — the analyze routes split image-fetch from the Claude call, so on a Claude outage they return the Street View/satellite imagery with `result: null` and the client loads the photos for MANUAL design + an amber "auto-design unavailable" notice (no more raw 500); **analysis model bumped to `claude-sonnet-4-6`** (was 4-5). Also ran `/graphify` on `src/` (code knowledge graph; outputs in gitignored `graphify-out/`). Detail: [[session_log]] S11.

**Done in Session 12 (2026-06-23) — #65 real portal packages + WW recommend checkbox (PR `jason/portal-packages-real`, pushed, Jason to merge):** the four portal tiers are now auto-derived per quote (were placeholders). **Tier 1 "Classic Glow"** = Santa's roofline + cheapest spritzers/extras to clear $1k (trims to closest-above when over); **Tier 2 "Full Festive"** = Tier 1's exact set with Santa's→Gingerbread; **Tier 3 "The Full Yule"** = everything on Gingerbread (never both rooflines); **Custom "Our Recommendation"** = staff #12-recommended items + roofline, reverting to "Build Your Own" + default Tier 1 when nothing recommended. Cards display "Tier 1/2/3" + "Custom" by POSITION; internal ids stay A/B/C/D (snapshots safe). `derivePackages(lineItems, result, roofline?)` takes both roofline options + omits Tier 2 on front-only quotes (no distinct Gingerbread). `applyOurRecommendation` runs in the loader after `attachSceneLinks`. **WW now recommendable** in the builder (kind `'ridge'` added to RECOMMENDABLE_KINDS; flag rides scene strands → `attachSceneLinks` carries it). 3-dimension adversarial review (11 agents) → fixed 2 med + 4 low. Gates: tsc · lint 0/2 · 270 tests. Live-verified on 2 quotes. **Jason-confirmed decisions:** Tier 2 inherit+swap · Tier 3 Gingerbread · no-rec→Build-Your-Own+Tier1 · front-only omits Tier 2 · fill=fewest-cheapest · **a Gingerbread-only quote will never happen** (copy residual moot). Also shipped + MERGED in S12: **#66** mobile hero — whole house, no side-crop on phones (PR #64); **#50** second on-portal design render between line items and add-ons (PR #65). Master `3e6e657`. (Note: S12 = this whole conversation incl. the 2026-06-24 continuation — one-conversation-one-session; the next fresh conversation is S13.) See [[session_log]] S12. docs/context synced 2026-06-24.

**🆕 As of S12 close (read for S13):** (1) **Two devs now** — Naldo joins on the **dashboard (#58)**, Jason on other areas; S13 must set up a no-conflict workflow + Naldo's machine onboarding before building (Jason's call: don't build it in the session that raised it). (2) **graphify rule in `AGENTS.md`** (merged): query the local graph for architecture/cross-cutting Qs (grep/read for targeted), with staleness guards; auto-refresh hooks installed on Jason's machine. (3) **Pending from S12 close** (Bash-classifier outage blocked git): verify master + sync local memory → `docs/context` at S13 start. Full detail: [[session_log]] S12 FINAL CLOSE.

**Immediate next (after S11):**
1. **#38 — Valor payment, PROPER (Monday).** Replaces the #38a placeholder: Passage.js 50% deposit + auto-vault ("The Vault") + HMAC webhook; on payment-confirm mark booked + move GHL → ⏰Approved + send the receipt. **GATED on Jason:** APP ID/Key/EPI in `.env.local`+Vercel + Valor webhooks enabled. When it lands, move the messaging/GHL trigger from approve → **payment-confirmed** (this completes **#42** receipt + #43's "paid" branch). Full spec: [[task_ledger]] #38.
2. Then the remaining backlog: **#56** mobile epic · **#44** widen-embed+hotkey · **#45** logo watermark · **#46** font bug (shared-core) · **#49** custom pattern · **#50** second house image · **#51** satellite roof view · **#52** edit training · **#53/#54** completed-job · **#41** referral link · older **#13/#14/#29** · **#58/#59/#63** · Naldo-pending. **Deferred:** #8 C6.

**FUTURE (when building portal packages):** ✅ DONE in S12 (#65) — the A/B/C/D tiers are now REAL (Classic Glow / Full Festive / The Full Yule / Custom "Our Recommendation"), and the "Our Recommendation" card IS the #12 staff-recommended selection. No longer placeholders.

**Loose threads:** S10 left test quotes stamped "sent" + a test card (`1BZreaoZj…`) at Bid Sent in GHL + a few test emails to info@yulelovelights.com — all harmless test data (wiping before launch). S11 also deleted test quote `12160f46` from the shared Supabase (it 404'd on both dev + prod). **docs/context synced through S11** (this session).
