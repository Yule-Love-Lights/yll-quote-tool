---
name: project-quote-tool
description: "AI Quote Tool — current state, confirmed decisions, run commands, gotchas, and what's next. Read first each session."
metadata: 
  node_type: memory
  type: project
  originSessionId: 834b8d13-f89f-476d-bae1-0a9ab5613799
---

# AI Quote Tool — project state (READ FIRST)

> Session-continuity layer — the quick "where are we / what's decided" so a cold session resumes without re-litigating. Deep detail lives in the repo: `docs/ONBOARDING.md` (setup), `docs/CURRENT_STATE.md` (done / half-done / bugs), `docs/CONVENTIONS.md` (how to add code). Pair this with `session_log.md`.

## Current state at a glance
- **THE 2026 HOMEWORKS REVENUE IS NOW IN THIS TOOL (S56, ledger 444-449, PR #1049 merged+live).** Homeworks (the CRM used Apr-Aug 2026) is being retired before it bills again on **16 September**. All of 2026 was migrated: **20 quotes, 18 customers, $41,162.88 contracted / $29,890.70 collected / $11,272.18 outstanding**, reconciled to the penny against three independent Homeworks exports. Before this the tool held nothing earlier than 2026-06-23, so half the year's revenue existed in no report. **MONEY RULE, do not re-litigate:** the figures are Homeworks' verbatim and are NEVER recomputed — the pricing engine cannot reproduce the charged tax on 8 of the 14 invoices (Homeworks taxes PER LINE and rounds each, three invoices carry two rates, one rate is not in the document). Migrated quotes carry `approval_snapshot->'homeworks'`, which is how migrated revenue stays separable from money taken in this tool. ~~⚠️ LIVE TRAP (row 444)~~ **CLOSED S57 (PR #1053, merged+live):** `/api/quote` now refuses to re-price a migrated quote outright, and so do `free-items` and every design write; the builder shows a Migrated badge and locks its controls at load rather than after a failed save. Calculate is safe again.
- **PAYMENT DATES RECONCILED AGAINST THE HOMEWORKS `payments.csv` EXPORT (S57, 2026-08-29).** Every migrated customer's collected total matches the export to the cent, and the export's own total reproduces the tool's `$29,890.70` exactly. Reading that export needs one rule: **money in = invoice payments + `added to credit` rows − `decreased from credit` rows, and an invoice row whose Method is `Customer Credit Balance` is a credit being APPLIED, not new money.** Without it several customers appear to have paid twice (Jane Laguerre reads $6,811.89 instead of $4,093.14). **8 of 20 deposit dates were wrong and 5 were corrected**: the five estimate-sourced quotes (#1079, #1098, #1148, #1187, #1318) carried the ESTIMATE's date because S56 had no payment record for them — #1318 Steve Herman was out by 78 days. The other three (#1000, #1212, #1315) are a credit-timing artifact, where the customer's money entered their Homeworks account credit days before it was applied to the invoice; those are LEFT ALONE on purpose, because moving `deposit_paid_at` earlier than `customer_approved_at` would create a quote that was paid before it was approved. Mary O'Connor's 2nd installment was recorded on its due date and actually cleared 2026-08-17. Every correction is stamped in `approval_snapshot->'homeworks'->'depositPaidAtCorrected'` with its previous value, and both statements are guarded so a re-run touches nothing. **Raymond Brown's 4th installment is correctly `paid_source: 'homeworks'`** — Jason collected it through Homeworks on 2026-08-28, after the export was taken, so it is absent from `payments.csv` by design and is NOT a discrepancy.
- **First payment-plan tracking in the tool (S56).** `installments` + `/admin/installments` (read-only) cover the three customers who pay monthly: Raymond Brown, Mary O'Connor, Jane Laguerre. Card-on-file vaulting was already live and populated (18 of 22 paid quotes carry a Valor vault token), so the monthly runner (row 448) is a schedule on top of proven primitives, not a from-scratch build. **Jason's own dated actions:** a Valor pay link to Jane before **5 Sep** and Raymond before **28 Sep** — each vaults the card as a side effect of the payment. Mary needs neither.
- **The self-serve estimator `/estimate` IS customer-reachable and indexable, and it has recorded ZERO completions (measured in prod 2026-08-25 and again 2026-08-27, S68).** The page returns 200 with `robots: index, follow`, but `self_serve_estimates` holds 0 rows while `quotes` went 191 to 207 across those two days, so the dashboard's self-serve accuracy tile has nothing to score. `recordSelfServeEstimate` is best-effort and only `console.warn`s, so an empty table cannot be distinguished from an insert failing silently since telemetry landed 2026-07-19. That is **ledger row 435**, and it was settled at the S68 wrap by reading prod logs: **there is no traffic.** One `GET /estimate` in 24 hours (this session's own curl), zero `/api/estimate` calls, and the marketing site references the estimator nowhere (`yulelovelights.com` and `/get-a-quote/` 200 with zero refs, `/instant-quote/` and `/estimate/` 404). Built, live, indexable and unreachable, so the open work is a LAUNCH, not a telemetry fix. Note this sits in tension with the "pre-launch, not customer-facing" line below: that line is true of the quote and portal flow, not of `/estimate`.
- **Pre-launch.** This tool is NOT customer-facing or used internally yet. The live customer flow today is **home.works** (separate system). So changes here are lower-risk — we're building *toward* launch.
- Deployed on **Vercel** (`yll-quote-tool`, Production tracks `master` → quote.yulelovelights.com), but not yet shown to customers. **Prod is LIVE + current as of S9** — it had silently been frozen at an Apr 23 build for ~7 weeks (Vercel Git was connected to the wrong repo); reconnected to `Yule-Love-Lights/yll-quote-tool` + env vars synced S9. See [[deploy-vercel]] (incl. the "if prod looks stale" playbook).
- Runs locally on **Next 16.2.6** (Turbopack), connected to the **real Supabase** (~55 real/test quotes). `.env.local` is filled on Jason's machine (values: see `project_secrets_access.md`).
- Gates all green: `npx tsc --noEmit`, `npm run lint`, `npm test` (Vitest).
- **FLEET GPS LIVE (S71, ledger row 403 — phases 1-3 shipped and switched ON).** The van's Bouncie tracker is connected in prod: OAuth grant stored encrypted, a */2-min poller (`/api/ops/vehicle-poll`) reads positions and writes the VISIT TIMELINE (the second clock — crew still clock manually for payroll; GPS is a structurally-isolated cross-check, no FK into shifts/job_segments). Customer coordinates NEVER leave the tool (Naldo's call — the built Bouncie-geofence design was deleted for in-house proximity against the day's schedule). Surfaces: `/admin/fleet` (live positions + two clocks), `/admin/geocoding` (fix-list; a job cannot be scheduled until its property geocodes — ~15 addresses need fixing), Settings→Bouncie (connection health). NOT built: ETA texts + the duration model (designed in `docs/reference/scheduling-system-design.md`, wait on schedule day-ordering). Runbook: `docs/reference/bouncie-setup.md` — its READ THIS FIRST names the 3 live migrations and the 2 SUPERSEDED ones that must never be applied. Truck's tracker: activate + authorize later, one row in `vehicles`.
- **S64 draft batch, NOT SHIPPED:** eight reviewed PRs remain open from Naldo's quote-tool punch list: #849 Permanent satellite persistence, #858 Permanent colour labels, stacked #859 staff colour entry, #866 portal image visibility, #871 per-photo brightness, #872 mini-group editing, #874 internal staff notes, and #875 quote-build timing. The additive private schemas for #866/#874/#875 are already applied and inert until code ships. #858 must precede #859. Before any merge, resolve and re-gate the known #874/#875 schema overlap, #849/#875 QuoteBuilder overlap, and #866/#871 portal overlap. Browser verification is still outstanding because no Vercel preview was created and no dev server was allowed. Full handoff: `docs/context/journal/S64-naldo.md`.
- **S32 (Jason, 2026-07-28→30) shipped 13 tasks in one day (#175–#186, PRs #645–#656):** VIEW-ONLY browse portals (staff flag; colour picker live, approve/pay 409-blocked on 7 routes) · per-quote DEPOSIT % (1–100, default 50, frozen into the approval snapshot) · customer TENURE ("N years with YLL" auto ∪ manual) · Neighbor 2+-item toggling + universal 1-item lock + zero-selection · inbox Neighbor-noise silenced (the #157 exclusion had been dead in prod — a `:color-request` id poisoned the lookup) + follow-up resurrect/orphan fixes · manual Mark-as-sent (DB-only) · What's-Included grammar + stable "Build Your Own" naming · client-side photo downscale everywhere (kills the 4.5MB 413 class, both JSON + multipart legs) · declined-deposit staff email + admin notice (proven against Valor's real decline webhooks) · operator-page 60s hangs root-caused to the un-timeouted auth gate (5s timeout + dedup + a per-query `[inbox-timing]` prod log line — read it next session). Ledger #187/#188 hold the wrap-review follow-ups.
- **#83 Jobber operator workflow COMPLETE + LIVE + verified (Naldo S20, 2026-07-01):** the full **create → job → complete → invoice → close** flow is operator-drivable on prod — status-aware **Options console** (#288/#293), a 24-agent audit → **8 trial-blocker fixes** (#300), rebook wiring + prod backfill (#306), a deposit-amount fix + money-reconciliation line (#309), and a Needs-Action dashboard queue (#310). Live-verified end-to-end + **3 LLM council reviews** → **trial-ready for an Aug–Oct parallel-with-Jobber run** (shadow rule: Jobber stays source of truth through Oct; auto-charge stays OFF). Only **auto-charge** (Valor card-on-file/MIT) remains gated on Jason. See the ledger #83 row + the S20 session-log entry. _(History: data-layer cores S8–S9 #185–#200; surfaces S14b #251.)_
- **Lighting verticals LIVE — FOUR service types now on prod (Naldo S22–S32):** the portal + builder are ONE adaptive machine branched on `quotes.service_type` = `holiday` (Christmas, default) · `permanent` (#88, year-round puck-on-track) · `event` (#96, temporary event/wedding lighting) · `permanent_bistro` (#117 S32, long-term café string lights — $30/ft satellite-measured + $100/pole, own Landscape GHL pipeline + Bistro Quote Link field + BOM/order-sheet + single-tile portal). **Permanent + Event both went LIVE always-on** — their dark-launch flags were deleted (#377 `permanentEnabled`, #379 `eventEnabled`) since the real customer gate is the `service_type` column, not a flag. Each vertical = a pure pricing module (`lib/{permanent,event}/pricing.ts`) + a builder section + adaptive portal copy/packages + Settings rates + a frozen `*RatesSnapshot` (approve/amend rate-drift guard). ⚠️ **Event was verified only at the logic layer (pricing→adapter); the in-browser create→send→portal→approve E2E is still OUTSTANDING** (S23 container had no backend creds) — do it before relying on event for a paying customer; revert-#379 re-adds a kill switch. **#117 (permanent BISTRO) SHIPPED + LIVE as the 4th vertical (S32) — see the Next-up entry + the archived #117 ledger row.**

- **Marketing site (yulelovelights.com, WordPress - separate from this app).** S50 (2026-08-04) put a flash-sale announcement bar on every page (Elementor Custom Code post 4092; trash that snippet to kill it) and repaired a header-overlap break on ~40 classic pages via a second snippet (post 4104). Audit marketing-site work from the **WordPress REST API**, not the sitemap: the sitemap omits published pages. #195 is SHIPPED: the four non-lead forms (newsletter, careers, intern, Light Up For Hope) now run on the quote tool at `/forms/<type>`, embedded by WordPress, with their own `site_submissions` table that structurally cannot hold an opportunity; Gravity Forms is deactivated. **#194 is DONE:** both pages are built on the event-lighting Elementor template and PUBLISHED at their canonical slugs, so the 11 inbound links resolve. **Standing rule from Naldo (2026-08-17): never build a page natively in WordPress — always Elementor from an existing page's template, and copy the Astra layout meta (`ast-site-content-layout: full-width-container`) with the Elementor data or the hero renders boxed at 1200px instead of full bleed.** The 375px header-overlap on Elementor pages is FIXED by a third snippet, "Mobile Header Clearance" (post 4400), which measures the real overlap and adds only the shortfall. One verified customer-facing defect is still OPEN and was not introduced by any of this work: the main GET A QUOTE CTA 301s to the homepage (Rank Math rule from 2026-06-26, 48 hits). #195 follow-ups (sync retry, handled-marker, nominee contact) sit in PR #782, migration already applied to prod.
- **Marketing-site offer terms, confirmed by Naldo 2026-08-17 (do not re-ask):** permanent project minimum **$4,000**, deposit **$1,300** (may change), cap **100 homes**, financing offered but **never required**. A permanent-lighting landing page carrying these is built as a mockup in the S50 scratchpad and is NOT published; open questions are where it lives and real roofline pricing. Live defect outstanding: ledger #301, wedding copy still on the two new service pages.
- **S43 (Jason, 2026-08-20) — the #252 INBOX EPIC IS COMPLETE, plus the review-debt backlog cleared: EIGHT PRs merged + live in one day** (#825 reconcile hardening / #826 ingested-churn noop / #827 Reverse integrity + CAS / **#828 slice C+H — `bucketOf()` is now the single source of bucket truth, with an exhaustive drift test proving the SQL predicates and the pure function agree on every (status x followed_up_at) combination** / #829 + #833 colour-change requests finally raise an immediate internal email / #835 rows 311+306+308 action-path integrity / #838 row 317 terminal-quote auto-complete). Every PR took the standing four-lens round, a fix round, and an adversarial delta-verify; three hand-resolved merge conflicts were re-verified at the final SHA. **Bucket semantics are now Jason's ruling in code:** Open leads = they spoke last and we owe a reply on any channel; Awaiting their reply = we spoke last and something is outstanding from them; Handled/completed = nothing outstanding, leaves the screen, a new inbound reopens it. **New standing rule (row 317, Jason 2026-08-20):** a quote deriving booked / declined / abandoned auto-completes its inbox items — with a hard carve-out that an item currently holding an unanswered inbound is NEVER auto-completed (verified live: Susan Pace-Burke's colour request stayed open while her booked quote's bare item completed). 'cancelled' is deliberately NOT in that set. **TWO CONSENTED PROD OPS:** Jason swept 84 stale Handled rows by hand (42 completed / 41 followed), and the `dashboard_activity` audit table was purged of **1,136,438 dead `ingested` rows** (99.8% of it; 2,123 real audit rows preserved exactly, VACUUM ANALYZE run) after #826 proved the churn had stopped — it had been writing ~4k junk rows/day since 2026-06-30. Jason's offline device check passed 5/5 on both inbox surfaces.

- **S46 (Jason, 2026-08-22→23) — the S45 handoff cleared plus an eight-task batch: TEN PRs merged + live, two prod migrations applied.** #867 (row 244 editable bistro footage) · #870 (row 260 design-scene CAS guard) · #885 (row 323 `markFollowUpDone` CAS) · #886 (row 303 ordinary Gmail threads carry `source_message_id`, pinned to the CUSTOMER's last inbound) · #887 (row 333 holiday footage overrides survive unrelated redraws) · #888 (rows 331+341 post-approval freeze on price/label/bistro-footage, server-enforced, booked-amend path preserved) · #890 (row 338 identity freeze made STICKY through decline→revive, CAS centralized inside `attachQuoteToCustomer`) · #892 (row 332 operator loading skeletons) · #889 (row 314 event date reconciles to GHL at approval, inside `after()`, gated on a new `ghl_event_date_pushed` marker so it can never revert a staff CRM edit) · #891 (row 340 declined-era portal selection surfaced + guarded clear on revive). **Migrations applied + query-verified: `designs.version`** (integer default 1 NOT NULL, 295 rows backfilled) **and `quotes.ghl_event_date_pushed`** (nullable text). Every PR took a risk-tier lens round and every fix round an adversarial delta-verify — which caught a freeze that broke the sanctioned amend path (3 lenses converging), a StrictMode ref hazard that would have silently no-op'd redraws in `npm run dev`, a second-round defect where a transient GHL read failure could still stomp a staff edit, two vacuously-passing tests, and a comment overclaiming its own guard. Whole-session integration + customer lenses both CLEAN. **Still owed: a browser leg on this session's customer-facing changes** (no dev server was reachable during the close).

## Scope / out of scope (now)
- **IN:** QA-driven bug fixes + feature work on the quote builder, pricing engine, and customer portal (the punch-list below).
- **OUT / deferred:** wiring prod CRM (on hold during testing); **home.works integration DROPPED (Jason, S9 — task #16 shelved):** we're not wiring home.works into this quote tool (the approve route's Zapier hook stays in code but goes unused); the portal package tiers (Classic/Festive/Yule) are Claude-generated placeholders to configure later; auto-send. *(The old c9Lines-render/SSIM/Replicate-pin backlog died with the #36 render teardown.)*

## Decisions confirmed — DON'T re-ask
- **Homepage (`/`) is intentionally untouched boilerplate (Jason, S11):** the root renders the default Next.js "Create Next App" page ON PURPOSE — it's a placeholder reserved for a future **internal staff dashboard** (task #58). Do NOT flag it as a stray/broken page or "fix" it; the real app entry points are `/quote/new`, `/admin/quotes`, `/portal/[id]`, `/settings`, `/training/*`. (Tab title still says "Create Next App" — fix to "Yule Love Lights" when the dashboard is built.)
- **Roofline model (task #17 — Phase 1+1b+2+4 DONE; #17 functionally complete, merged):** 🔴 red = **front roofline = Santa's**; 🔵 blue = **ridge + sides**. A quote **PRESENTS BOTH** a Santa's option (front) AND a Gingerbread option (**front + ridge + sides**) — both shown; **mutually exclusive at SELECTION** (the customer picks one, never both). Operator quotes everything; staff **recommend** one (the portal default); when staff doesn't pick, the engine **auto-selects the option that lands the quote total closest to the $1,000 minimum WITHOUT going under** (else the larger). **Phase 1+1b (S2):** engine prices + exposes both on `QuoteResult.rooflineOptions`, bills only the recommended (`rooflineChoice` input + auto-default); builder breakdown has recommend radios that re-price in place. **Phase 2 (S3):** the customer portal now shows BOTH options as **ordinary "What's Included" line items** (no dedicated selector, no footage on the portal) — only the staff pick is selected by default; clicking the other deselects it (mutually exclusive — never both); clicking the selected one removes it (like any item, no Remove button). Adapter splits the billed roofline into `roofline-santas`/`roofline-gingerbread`; `PortalRoofline = { itemIds, recommendedItemId }` drives the either/or in `SelectionContext.toggleItem`; only the recommended option is bundled into tiers + the $1,000 gate (tierLineItems filter ⇒ no double-count). **Phase 4 (S3, PR `jason/roofline-phase4`):** the AI prompt (`photoAnalysis.ts`) now classifies front-facing gutters/eaves + **front-gable rakes** as red/Santa's, and the **ridge + side-facing runs** as blue/Gingerbread (*gable* = the triangular wall under a peak; *rake* = its two diagonal edges). **FRONT SECTION OF THE ROOF ONLY** — the part facing the road; NEVER the full perimeter or the back/backyard (~90% of jobs; whole-roof/backyard done manually). Must identify the REAL road (not a neighbor's/shared driveway) — the satellite rule was the big fix (it had traced the whole perimeter + mis-picked the front). **Downspouts** (vertical wall gutters) are never traced. Builder/training labels → "Front Gutterline" (red) / "Ridge + Sides" (blue); "Gingerbread Ridge" renamed → **"Gingerbread"** (render-variant KEY `'ridge'` kept). **Validated S3** (PR #14 merged): Jason added 2 correct corrections + analyzed a held-out house — **classification fix confirmed** (street: sides no longer red; satellite: front-section only, no longer wraps the perimeter). Remaining issues are AI **accuracy, not classification**: street lines don't hug the roof (PRE-EXISTING vision limit — drag-to-fix workflow), satellite orientation flips (red on ridge / blue away from road) — **folded into the AI-training-system task** (see Next up). Note: corrections store only the street photo (no satellite image) so they can't teach satellite tracing; only the latest 2 corrections feed the model. C9 / Winter Wonderland left untouched throughout.
- **$1000 minimum (CHANGED in S2, task #18):** NO LONGER an engine floor — `pricingEngine` returns the real total so staff can intentionally send sub-$1000 quotes (the "Minimum quote applied" note is gone). It's now a **customer-portal approval gate**: Approve is disabled until the selected pre-tax subtotal reaches $1000 (with an "Add $X more" nudge). **Waived** (gate off) when the whole quote's items total < $1000 — staff override, else it'd be un-approvable. Portal shows real prices + a Subtotal/Tax/Total/Deposit tie-out (tax now visible). *(Supersedes the old "minimum applies everywhere / floor" rule.)*
- **Multi-image quoting:** extra images are **manual-only** (no AI auto-quote) to avoid double-counting; no cross-image AI dedup. Image 1 covers the roof.
- **Light color/pattern (#10 — SHIPPED S9, PR #38):** a CUSTOMER-facing swatch picker on the portal hero. Picking a whole-house color/pattern **recolors the live design in real time** (`render-readonly` overrides light items' `colorPattern` at render time — strand/spritzer/mini-area only; shallow clone so the scene is never mutated; **"As designed" = no override**, render the operator's authored colors). Choice frozen into the **approval snapshot** (`customerSelection.colorSchemeId`); **no pricing impact, no migration.** The scheme list is one editable array in `src/lib/design/colorSchemes.ts` (12 entries). **Operator-default = WON'T-DO (Jason, S9, confirmed):** the portal ALWAYS opens "As designed" — that's the intended behavior; there is NO builder control and NO data-model field, and we're not adding one.
- **Wreath/garland pricing (#17, S8 — Naldo's numbers all in, NOTHING pending):** TWO tiers only — `bow` = **Non-Decorated**, `fullDecor` = **Decorated** (the old "Labor" tier was removed; internal keys kept to avoid a codebase-wide rename). 36" Oregon wreath dropped; **60"/72" wreaths added**; standalone bow **$35**; **tax 8.75%**. Wreath non-deco 24/30/36/48 = $200/$285/$315/$450 (deco unchanged); 60" = $885/$1130, 72" = $1149/$1455. Garland non-deco 9ft $162 / 4.5ft $135 (deco 250/210). All numbers live in `BUSINESS_RULES` (pricingEngine).
- **Workflow:** never commit without Jason's explicit "yes"; PR-not-master; run gates (tsc + lint + test) before commit. Jason + Naldo are on different machines → `docs/context/` is the canonical shared memory.
- **Pushing:** **Claude runs `git push`** (Jason approved in Session 1). Pushing to the `Yule-Love-Lights` org is NOT blocked here, unlike the design tool's exfil guard. Commits still require Jason's explicit "yes" first.
- **Portal consolidation (next up, task #27):** `/portal/[quoteId]` becomes THE canonical customer URL serving the **Snowglobe** design; the other 3 portal routes (`portal-snowglobe`, `portal-dark`, `portal-concierge`) get retired. ⚠️ KEEP the `components/portal/dark/*` + `snowglobe/*` COMPONENT folders — Snowglobe reuses them; delete only the route folders, and grep-verify before deleting any component.
- **🔗 Design-tool integration — Path B (CONFIRMED, groundwork only — joint initiative):** absorb the **design tool** (a separate Konva canvas app that draws glowing lights on a house photo → produces an editable `scene` JSON) INTO this quote tool — designs stored in **Supabase** alongside quotes, one login/customer (the quote's HighLevel contact), **line-items ⇄ scene-items linked BOTH ways**, and the **static AI portal-hero render replaced by a LIVE editable design** (kills the ~90s + Gemini render cost). ⮕ **Resolved end-state:** the Gemini render pipeline was **fully REMOVED in #36 (S7)** — the live design is the only portal visual now. Full plan: [[project_integration]] (mirrored shared doc); design-tool internals/schema/API: `docs/design-tool-context/`; design-tool repo on disk `C:\Users\Jason\Desktop\YuleLoveLights\Claude`. **Phase 1 DONE (S4, merged PR #18).**
- **🔗 Integration items model — DECIDED (Jason + Naldo, S4):** **Option-1 + Option-2 HYBRID** — the **design is the master list** for standard items (roofline, minis, wreaths, spritzers, garland; one source ⇒ no double-count) **+ a "custom/manual line item"** feature (name/price/description; on quote + portal, NOT on the design) for ~5% niche quotes. **Roofline** stays measurement-driven (the one exception); **mini-lights** = design-driven but priced by a **staff-typed strand count × rate** (not length). **Mini-light editor tool = Approach B:** place a resizable **box** OR **trace an enclosed shape** (auto-closed) → fills with single lights; **density = visual only** (price = strand count). New SHARED-editor item type (coordinate w/ design-tool AI). Railing-as-a-line is an open gut-check. Details in [[project_integration]] (Phase 2 plan + Step-3 decisions). These are **Step 3** (after Steps 1–2).

## How to run locally
- Dev server (repo root). Via Claude Code's **Bash** tool, prefix to dodge the empty-`ANTHROPIC_API_KEY` shell quirk:
  `unset ANTHROPIC_API_KEY; unset ANTHROPIC_BASE_URL; export PATH="/c/Program Files/nodejs:$PATH"; npm.cmd run dev`
  (Normal terminal: just `npm run dev`.) → http://localhost:3000
- Gates: `npx tsc --noEmit` · `npm run lint` · `npm test`
- Key pages: `/quote/new` + `/quote/<id>` (operator) · `/admin/quotes` · `/portal/<quoteId>` (Snowglobe — the one portal) · `/training/*`. *(The `/admin/renders` pages were deleted in #36.)*

## Known gotchas
- Empty `ANTHROPIC_API_KEY` in the Claude-Code shell overrides `.env.local` → Claude routes 503. Unset it before `npm run dev`.
- `react-hooks/set-state-in-effect` is at **error**; the fix pattern for legit effects is a `queueMicrotask`/rAF defer.
- Vercel env vars are all **"Sensitive"** (unreadable) — pull secret values from source accounts (`project_secrets_access.md`).
- Vercel env parity (see [[deploy-vercel]]): S9 added the `HIGHLEVEL_*` (×7) + `NEXT_PUBLIC_PORTAL_*` (×5) vars to prod so it matches dev's `.env.local`. home.works still off (mismatched var names + **dropped, #16**). "Works on dev, 'X not configured' on prod" = a missing Vercel env var; `NEXT_PUBLIC_*` are baked at build (redeploy after adding); never add `SUPABASE_DB_URL` to Vercel.

## Task list → see the master ledger
The full task list (done / planning / backlog / Naldo-pending) lives in **[[task_ledger.md]]** — the single source of truth, renumbered into one clean sequence on 2026-06-05 (now #1–#28) with an Old-# column mapping to the historical numbers in this log + PR titles. **Next BUILD (S4): task #27 — the design-tool integration (Path B)**, starting Phase 1 from the LOCKED [[project_integration_data_contract.md]]. #8 (training-system) planning is DONE (feeds #27 Phase 3). Update the ledger as work lands.

## Next up

**The advertising subsystem is COMPLETE and LIVE (Naldo S80, 2026-08-29).** Six merged PRs: schema + data layer (#1057), worker capture and admin review surfaces (#1061), the yard-sign SKU with manual stock reconciliation (#1067), **pay per accepted PHOTO** (#1077), sign allotments plus the photo-similarity engine (#1086), and placement **void** (#1096). MONEY RULES, all Naldo's and all enforced as CHECK constraints plus data-layer guards: the campaign rate is paid per accepted photo of ANY kind (the campaign NAME says whether it is signs or hangers; this SUPERSEDED the earlier door-hangers-never-pay rule on 2026-08-29), the rate is stamped at acceptance so later rate changes never move history, pending and rejected never pay, and reject→resubmit→accept pays exactly once. ALLOTMENTS: an admin hands a worker N signs; remaining is DERIVED (issued minus yard-sign photos taken, voided excluded), never stored, and never gates a submission. Issuing draws the warehouse pile down under a CAS, audited. VOID: an admin voids a placement with a required reason and it counts for NOTHING anywhere (pay, estimates, allotments, stock, campaign badges, duplicate flags) while its status, stamped rate and photo survive as the record; there is no un-void by design. DUPLICATES stay review-time hints, never blocks: nearby GPS, same suggested address, same worker same ET day, plus a perceptual photo hash (identical photo scores 0, the two sample photos 27 of 64). Proven live against the real database with generated sample photos, including per-photo pay and void. STILL OPEN, ledger 474-477: void's allotment give-back needs Naldo's ruling (474), the issuance idempotency lock (475), **payout settlement tracking — the tool says what is EARNED, nothing records that it was paid (476)**, and two voided-exclusion guards needing real test assertions (477). Presentation belongs to the concurrent Simple Crew UI session; wiring left for it is a Void surface on its screens and the allotment balance in Manage Crew. First device check: real phone, real GPS, real camera.

**Ops hub workstreams A + B are LIVE (Naldo S76, 2026-08-29).** Admins get a "View as" menu in the top header (Office / Advertising live, Crew disabled until the door ruling — row 466); `/admin/time-tracking` holds the time-exception queue with the digest line and the manual-shift-editor fix path; the invoices list carries the Unreconciled chip; the advertising lane (worker capture → admin review → pay) was proven end to end against the live database with generated sample photos (`npx tsx scripts/advertising-e2e-smoke.ts`, is_test, full teardown). ⚠️ Post-#1077 drift: that smoke's door-hanger-earns-0 checks WILL FAIL next run (row 468) — fix its expectations before trusting a red result. One-session-per-lane convention drafted for Jason (row 467).

**Calls-to-tasks is LIVE (Naldo S75, 2026-08-29).** Every completed HighLevel call becomes a transcript (HighLevel's own transcription, Deepgram never needed) and every promise a rep makes becomes a shared office task on the dashboard, auto-assigned by email match, hourly (sync :12, extract :27). 7-day backfill done: 79 calls, 19 tasks. The build plan of record is `calls_merge_plan_2026-08.md`; remaining slices are ledger rows **460-464** (S3 playbooks, S4 scoring, S5 coaching+practice, S7 rollups, S8 retirement DOUBLE-GATED). The Ops Hub audit (`ops_hub_audit_2026-08.md`) carries every role/advertising ruling; advertising role lockout is live (#1043). Watch item: first unattended cron ticks, and the /admin/calls page is the debug surface.

**THE HOMEWORKS MIGRATION IS FINISHED (Jason S57, 2026-08-29).** All of ledger 444-450 and 452 shipped and live; 451 is dormant, 453 open and fail-closed. Verified complete three ways against Homeworks' own exports — `payments.csv` → collected **$29,890.70**, the A/R aging report → outstanding **$11,272.18**, revenue-by-customer → contracted **$41,162.88**, each to the cent. No customer exists in Homeworks that is not in the tool, and it holds **$0** of expense data, so cancelling it before **16 September** loses nothing (keep the export zip for the source PDFs). ⛔ **THE INSTALLMENT RUNNER IS BUILT AND DELIBERATELY UNARMED, indefinitely** — Jason's ruling 2026-08-29: he collects the three plan customers by hand and sends each a pay link on the day. Four independent gates hold it off; read `src/lib/installmentRunner.ts`'s header before touching any of them. HIS DATED ACTIONS: Jane before **5 Sep** ($453.13), Raymond before **28 Sep** ($278.40); Mary needs neither. No install dates exist for any of the 20 yet and that is expected — dates come first, then scheduling.

- **NEXT SESSION: no work is queued on purpose.** Jason has his own list and will direct it (his instruction at the S57 close). Start by asking him, not by picking a row.

**Next up (after Naldo S70, 2026-08-24) — the staff-admin surface is rebuilt; one PR waits on Jason.**
Settings → Accounts now has ONE Staff panel (`/api/admin/staff`) covering office and field together, replacing the two panels and two routes that did the same job differently. Every row has the same actions: edit rate, link/change/unlink Telegram, reset password, move office<->field, activate/deactivate, remove. Setting up an office person no longer needs hand-written SQL, crew passwords can be reset at all (they never could), and field-crew rows can be created in-app (`insertCrewMember` had zero callers before).
- **Waiting on Jason:** PR #1017 (row 355, skip CI on docs-only pushes). Needs his EXPLICIT go rather than the 48h async standard, because it changes what "gates green" means for a docs-only PR and the wrap skill's unattended auto-merge depends on that reading.
- **Owed:** a live admin pass on the new panel. It replaced a screen Naldo had just tested, and two of the session's own copy claims turned out false under review.
- **Open:** row 359 (deleting an operator still orphans their pay row; the panel now SHOWS it as "login deleted" but cannot repair it).
- **Moot:** row 358 (expose the time clock to the Operations Hub) — the Hub was scrapped 2026-08-27.
- **Do not re-litigate:** `is_office` controls only whether someone is offered when assigning crew to a job; both clocks ignore it. Jason is an admin on a field row and should stay that way. Admin shows as a badge, not a third group.

**Next up (after Naldo S68, 2026-08-27) - a verification pass plus one small fix.** The three self-serve estimator PRs (#624/#800/#815) are confirmed merged, present and unreverted, re-checked at master `27df2103`. The #800 keystroke flash is measurably gone on live prod (DOM instrumentation with a control) and the #815 read-time clamp is correct with 12/12 tests green.

- **DO FIRST: row 435 is already ANSWERED - the work is a LAUNCH.** The estimator has no traffic and the marketing site links it nowhere, so the next move is to link or embed it, then re-check. Superseded detail: Grep Vercel logs for `[selfServe] recordSelfServeEstimate failed` and settle whether the estimator has genuinely had no completed submissions or the telemetry insert has been failing silently. Cheap, and it decides whether any estimator metric means anything.
- **Row 436 SHIPPED in the same PR:** `/estimate` now emits `alternates.canonical`, four tests, mutation-probed.
- **The six closed-unmerged branches are DEAD, verified at content level, so prune rather than ship.** `naldo/223-named-morning-brief` holds row 229, which shipped via merged PR #780; the other five are close notes for sessions already recorded as journal fragments, and all five write to the FROZEN `session_log_naldo.md`.
- **S68's first close PR #948 was closed unmerged by Jason** on 2026-08-26 with green checks and no comment. Nothing from it landed, which is why these rows are the third mint of the same findings. Worth asking him why before the next close.
**▶▶ Next up (after Naldo S69, 2026-08-27) — READ THIS FIRST.** Master `27df2103`, gates tsc 0 · lint 0 errors (19 warnings) · vitest **8137 / 450 files**. S69 was an open-PR cleanup: 12 PRs merged, 7 closed, the seven-deep stale-close pile finally cleared (they could never merge because each was written against the frozen `session_log_naldo.md`).

- **THE OPERATIONS HUB IS SCRAPPED** (Naldo, 2026-08-27), replaced by other tools. PR #881 closed unmerged and nothing was ever applied to prod. But earlier Hub work is already ON master with no consumer: `src/app/api/ops/v1/jobs/[id]/arrive|depart|complete`, `docs/context/OPERATIONS_HUB_CONTRACT.md`, the AGENTS.md "Operations Hub contract" ownership row, and row 358. **Ledger row 433** flags it; whether to strip them is Naldo's call. Do not read that contract as live.
- **Portal image visibility stays PORTAL-ONLY and is not extended** (row 368, ruled). What Naldo actually wanted was hiding a customer's photo on their OWN referral page, and that already exists and is live: `customers.referral_photo_optout`, toggled from `CustomerReferralPanel`. He does NOT want a photo hidden from a customer's quote. Prod at close: 311 designs, ZERO hidden. Rows 370/429/367/427, built on that feature by later sessions, are KEPT.
- **Quote build timing:** the idle cap shipped (row 374, PR #925), sessions over two hours are excluded and tallied separately. The disclosure pill is in **PR #1009, OPEN, needs a merge-go**. Per-person numbers stay visible to any operator by Naldo's choice (row 375).
- **Deliberately untouched, other sessions' lanes:** #869, #817, #782, #679, #669 (older) and #914-918, #922 (the AI measurement set), plus the fleet-GPS work claiming ledger 430-432 in PR #1011.

**▶▶ Next up (after Jason S54, 2026-08-27) — READ THIS FIRST (quote-tool lane).** Master `c40f92ad`, gates green (tsc 0 · lint 0 errors / 19 pre-existing warnings · vitest **7978** across 441 files). S54 merged **fourteen** PRs (#979-#991, #993) and shipped twelve rows: 418/419 (customer-name links + Service Type columns), 420 (edit-autosave bound to the quote lifecycle), 335 (schedule opens on the ET business day), 356/300 (office/field boundary enforced at the assignment WRITE + the scheduling DB functions finally tested), 336, 376 (Calculate no longer waits on build-timer bookkeeping), 206 (a stale draft no longer restores over a lead prefill), 209 (drawn satellite footage beats an AI street re-analyze — money-adjacent), 388 (stale-payment markers on the job page + a standalone invoice resync that refuses settled invoices and stamps who ran it), 324 (staff can save a customer's opening portal selection). Plus Jason's ad-hoc asks: the Leads nav item removed, "+ New quote" added to the top nav, "Pull satellite" moved beside the satellite-photo slot.

**Open / next:** rows **367** (design post-approval freeze — scoped with Jason in S54: the AMEND path stays open, only silent drift is refused, so an approved design can still be changed deliberately with a reason, trail and re-consent), **251** (the live wrong-customer incident), **384**, **343**, **259**. **Blocked on NALDO** (flagged 🔴 on the rows themselves): **368** (should "hide from portal" hide from the referral hero + public gallery too?), **421** (the crew bot's clock-in/arrive path has no `is_office` gate — his bot area; measured DORMANT in prod: `job_segments` 0 rows ever, office bot shifts 0), **349**, **374**, **375**. Row **335** stays ACTIVE despite shipping: its one untestable leg is a live evening check (open `/admin/schedule` after ~8pm ET, confirm it shows TODAY) — archive it once that passes. Next free ledger #: **423**.

**⚠️ NOT VERIFIED IN S54:** the live-browser check on the two UI asks (nav CTA, relocated Pull-satellite button) never ran — the Chrome extension dropped mid-session. Both are confirmed by deploy SHA (READY on the exact merged commit) and by gates, NOT by anyone seeing them on screen. Worth a two-second glance.

**▶▶ Next up (after Jason S53, 2026-08-26) — READ THIS FIRST (quote-tool lane).** Master `63a0c7c1`, gates green (tsc 0 · lint 0 errors / 19 pre-existing warnings · vitest **7795 tests**). **Thirteen PRs merged + live**, every shipped surface device-verified by Jason the same day — rows **409** (#968 deposit chip beside the NCE badge), **410** (#969 loading skeletons + shared component), **411** (#970 one hardened `appendQuoteAuditEntry`, three call sites migrated), **413** (#972 edit-mode autosave with reopen recovery), **414** (#973 Mark-reconciled override, figures shown before clearing), **370** (#976 portal-image visibility audit), **188** (#975 schema true-up + the four canon indexes applied to prod on Jason's go, verified in pg_indexes), the #971 write-findings-early pitfall promotion, **#977** (quote search now matches the #number the list displays — Jason's device find), **#974** (WorkflowBoard folds Approved into Sent — Jason's go, Naldo informed, revert-the-squash is the rollback lever), and #964 (S52's row-412 docs). Rows 412/334 closed on evidence; 407 archived as accepted-at-merge. **Both S51 carry-overs closed**: #966's colour chips verified on Jason's screen (Orange on #1303, Champagne on #1129), and the prod deploy SHA verified against master via the NEW `VERCEL_TOKEN` in `.env.local` (standing wrap step now — see memory `project_vercel_token.md`). Ten rows archived; active 92 → 84 (+2 minted). **NEXT (S54):** rows **418** (clickable customer name on the job page) and **419** (Service Type column on quote/invoice tables) — both Jason device notes, both small — then 368 (gallery vs visibility flags; row 370's audit now feeds it), 335 (schedule UTC day), 356 (office/field write gate), 300 (scheduling.ts coverage). Ledger counter **420** (Naldo's open #948 holds 415-417). Cross-dev flag: the archive holds a pre-existing DUPLICATE S52 (Naldo) block (twice, different tails, both on master) — Naldo's to dedupe.

**▶▶ Next up (after Jason S51, 2026-08-25) — READ THIS FIRST (quote-tool lane).** Master `a57da4d6`, gates green (tsc 0 · lint 0 errors / 19 pre-existing warnings · vitest **425 files / 7728 tests**). **Five PRs merged + live** — rows **406** (#949 unsaved-edit guard), **405** (#951 re-analyze confirm), **404** (#959 stale-invoice marker clear), **362** (#966 approved colour on job/invoice/customer profile), plus **#965** (a Windows path bug that made the flow-q contract test unpassable on this machine). Row **408**'s device check, owed three sessions, RAN and PASSED. Rows 362/404/405/406/408 archived; active 95 → 92.

**Do first:** row **413** (edit-mode autosave for the quote builder). Row 406 shipped the warning, but a warning cannot cover the two doors that remain: IN-APP navigation (every OperatorNav link is a `next/link` client transition that never fires `beforeunload`, and clicking Inbox mid-edit is ordinary) and a silent Chrome Memory Saver tab discard — which is the trigger Jason could not rule out for the original prod loss. A sticky banner was tried and REVERTED in the same PR. Then row **414** (the `mark-paid` stale-marker dead end: the cash/check path every non-card customer settles through never clears the markers, and is provably terminal for them post-#959).

**Two things a future reader should not re-derive:**
- **Row 362 is fully closed, including the prod data.** Kristie Tibbetts's booked quote **#1129** was corrected `as-designed` → `champagne` (verified by re-query), applied SILENTLY on Jason's explicit call because she had already been told by a hand-typed SMS on 2026-08-22. Susan Pace-Burke was already correct. The colour now also DISPLAYS on the job page, both invoice pages and the customer profile.
- **The colour-change REQUEST → APPROVE → NOTIFY flow already existed and was not rebuilt.** `color-change-request` writes `pendingColorRequest` and emails staff; `ColorRequestPanel` on `/admin/quotes/[id]` applies or dismisses (dismiss requires a written reason); `apply-color-request` texts AND emails the customer and reports which send failed. What was missing was only the DISPLAY, which is what #966 added.

**Cross-dev heads-up:** Naldo's row **374 appears already shipped** — his PR #925 merged 2026-08-25 12:59Z with a body saying "Closes ledger row 374", while the ACTIVE row still reads as an open NALDO PRODUCT CALL. S51 flagged it in the ledger but deliberately did NOT archive it: his lane, his call.

**Next free ledger #: 415** — S52 holds 409-412, S51 minted 413/414.

**▶▶ Next up (after Jason S52, 2026-08-25) — READ THIS FIRST (quote-tool lane).** Master `1b836692`+, gates green (tsc 0 · lint 0 errors / 19 pre-existing warnings · vitest **7670 tests**). **Seven PRs merged + live** — rows **391** (#952 follow-up page cap), **373** (#953 staff-notes paging), **371** (#950 brightness-orphan prune + the post-delete CAS version), **350** (#954 spritzer hub), **346** (#956 last loading morph), **328** (#955 NCE deposit provenance + suppression), **372** (#957 staff-note withdrawal, migration applied by Jason first). Rows **348 · 342 · 326** were found ALREADY SHIPPED in earlier sessions and marked off; **369** closed as decided-in-code; **370** recorded with a design and deliberately NOT built (it needs row 411 first). Eleven rows archived; active 105 → 94.

**Do first:** rows **409** (surface the deposit % beside the NCE badge — a sent quote can now carry `is_nce` with a non-40 deposit, which Jason accepted for edge cases, but nothing reconciles them), **410** (~11 bare loading morphs outside row 346's scope), **411** (unify the THREE hand-copied `approval_snapshot` audit implementations — one had a data-loss bug; row 370 is blocked behind it). Two browser checks are still owed and neither is blocking: a spritzer at max light-size, and the NCE held-back notice on a sent quote. **The Supabase MCP is now configured** (`~/.claude.json`, project-pinned, token in a user env var) so migrations can be applied directly. Next free ledger #: **412**.

**▶▶ Next up (after Jason S50, 2026-08-25) — READ THIS FIRST (quote-tool lane).** Master `c476313b`, gates green (tsc 0 · lint 0 errors / 19 pre-existing warnings · vitest **419 files / 7590 tests**). **Six PRs merged + live** (#938 #939 #940 #941 #943 #944) — the entire S49 draft-PR queue cleared, plus the editor-core relay to the standalone design tool (`6bfeab3`).

- 🔴 **DO FIRST: row 406 — a hand-typed value on a PRICED field can be silently lost, and Jason CONFIRMED it in prod.** He changed a Front footage 95 → 100, tabbed out, came back, clicked Calculate, refreshed, and got 95. Verified by direct query that the edit was LOST, not delayed. Three explanations were killed with evidence (the ceil-to-5 rounding can't produce it; the derive fires once at mount and that quote has no lines; a save race was ruled out by re-querying much later). What survives: the builder seeds `form` ONCE from a server prop with no edit-mode autosave, no `beforeunload` guard and no dirty indicator, so any reload between the edit and the click silently restores the server value. The trigger is unconfirmed — Jason couldn't recall whether the page reloaded — but the exposure doesn't depend on it. Fix: warn on unload while dirty + show unsaved changes.
- **Then rows 404, 405, 407** — the unclearable stale-invoice marker (`/amend` 409s when there's no price delta, so the ⚠ can't be cleared in-app), the silent re-analyze override discard (no confirm, unlike both sibling paths), and the two accepted board-⚠ papercuts.
- ✅ **BOTH MIGRATIONS ARE APPLIED AND QUERY-VERIFIED**: `follow_ups.re_chase_since` (nullable timestamptz) and `job_stock_movements` (8 columns, RLS on / 0 policies, 0 rows). No Supabase MCP was connected this session — psql via the URL already in `.env.local` was the fallback. `information_schema` confirms no `stock_movements` table was ever created, so row 397's rename landed while the migration was still unapplied, which is exactly why S49 held it.
- **Row 408 — the #921 Recount device check is finally RUNNABLE and still owed.** The three-session blocker was concrete: the only permanent test quote loses its `satellite_lines` on save, so no Recount control renders on reopen. **Test quote #1302** (`2b950119-f563-4c32-8973-0b8d6383005c`) now exists WITH lines on all four sides plus a scale. Hand-typing an accessory count reveals the button; per-side `× Run 1` controls make the line-edit step possible with no canvas dragging. Needs Jason's login.
- **Row 362 — Kristie Tibbetts.** Jason said he'd deal with it once the builds were done. They are. Seven sessions unanswered.
- **Ledger: next free # is 409.** **COLLISION RECORDED** — the counter read 403, but Naldo's open draft PR #945 already claims 403 for the fleet-GPS row, so S50 minted 404-408 instead. S50 also archived **18** shipped rows in the same close that shipped them (the AGENTS.md pitfall promoted this session, applied to itself).
- **Process note:** `git checkout <branch> -- .` on master will stage that branch's files into the shared tree — use `git grep <ref>` to answer read-only questions about another branch. And `grep -o X | grep -v prefixX` can never filter anything, because `-o` strips the prefix before the exclusion runs.


**▶▶ Next up (after Jason S49, 2026-08-25) — READ THIS FIRST (quote-tool lane).** Master `90cb1c97`, gates green (tsc 0 · lint 0 errors / 19 pre-existing warnings · vitest **417 files / 7518 tests**). S49 shipped rows **378 · 385 · 387 · 383 · 348** plus a ledger truth-up, across PRs #931/#932/#933/#934/#937/#935/#936 — all merged + live.

- 🔴 **DO FIRST: the four DRAFT PRs are built, gated green, and deliberately UNFIXED.** Jason's call 2026-08-25 was record-now-fix-next-session, so every review finding is already written up as a ledger row. Fix in this order: **#939** (rows 381/389 — **2 HIGH, blocked**: rows 394 + 395), then **#940** (rows 382/386 — 1 HIGH, row 397), then **#941** (rows 390/392 — shipped UNREVIEWED, row 401: run its lens round FIRST), then **#938** (rows 379/380 — 2 MED, rows 399 + 400).
- **Two migrations are UNAPPLIED, on purpose.** `stock-movements` (PR #940) and `follow-ups-re-chase-since` (PR #941). Row 397 may RENAME the stock table, and a rename is only free while unapplied — settle 397 before applying either. Both are additive/safe under AGENTS.md; a column-add ships migration-FIRST.
- **The two HIGHs on #939 were each confirmed by more than one independent reviewer**, which is what makes them classes rather than opinions: the stale-invoice markers are written but cleared NOWHERE (row 394), and the "verify in Valor" note fires on every settled invoice — all four real prod invoices would show it permanently (row 395). Jason ruled 2026-08-25: "go with their read" — relocate it to the amend panel.
- **Ledger is now TRUE.** S49 archived 12 rows that were already done but still reading as open, and resolved row 393 as superseded. One of them (191) was on Jason's own build list — the stale ledger was one step from causing already-finished work to be rebuilt. The sweep now checks EVERY cell of a row, not just the Task column. **Next free ledger #: 402.**
- **Rows minted in S49: 388-401.** 388-393 the S49 close deferrals · 394-401 the recorded findings from the five parallel builds.
- **Still owed and needing Jason:** row **362 Kristie Tibbetts** (told 'Champagne', booked job builds as-designed — parked to next session by Jason) and the **#921 Recount device check** (needs his login; the control only appears once an accessory count is hand-typed, which is why S48 could not find it).
- **Four worktrees remain on disk** under `.claude/worktrees/` holding the four unmerged draft branches. Everything is pushed, so they are safe to delete once those PRs land.
- **Cross-dev:** PR #939 touches four of Naldo's dashboard files, all additive (`workflowBoard.ts`, `needsAction.ts`, `queries.ts`, `WorkflowBoard.tsx`). Jason confirmed 2026-08-25 that he told Naldo and Naldo is fine with it.

**▶▶ Next up (after Jason S48, 2026-08-24) — READ THIS FIRST (quote-tool lane).** Master `da31c034`, gates green (tsc 0 · lint 0 errors / 19 pre-existing warnings · vitest **416 files / 7460 tests**). Prod verified serving that exact SHA via the GitHub deployments API, state `success`. **Six PRs merged+live** (#920 #921 #926 #927 #928 #929) covering rows 287/320/325/329/337/339/344/345/364/365/366. Two prod migrations applied and verified by query: `jobs.stock_deductions` (jsonb, nullable) and RLS on `public.inventory_orders` (0 policies, 0 rows, all 5 consumers already service-role). Zero `jason/*` PRs open at close.

- 🔴 **DO FIRST: row 378 — the CUSTOMER pay-balance route has no staleness guard, while its STAFF sibling got one this session.** `src/app/api/quotes/[id]/pay-balance/route.ts` (~158-167) hands `amountUsd: invoice.balance` straight to Valor with no reconciliation; `src/app/api/invoices/[id]/charge-balance/route.ts` (~460-501) now recomputes the expected figure and 409s `invoice-stale`. After an amendment whose invoice re-sync loses its CAS race, staff are refused and the customer is not. Measured in prod at close: 23 approved non-test quotes, 3 with no frozen pricing, exactly 1 in the exposed state. Sixth instance this session of the sibling-guard class, and this one we created ourselves.
- **The delta-verify on every fix round is now empirically load-bearing: 27 defects caught pre-merge, 13 of them INTRODUCED by the fix rounds.** Headline: #927's retry produced a ~$500 double-charge (balance derived from `newTotal − deposit` while a settled balance payment was subtracted nowhere), with a shipped test asserting the wrong figure as correct. Budget the delta-verify into every money/identity fix round; skipping it is exactly how these ship.
- **Rows minted this session: 378-387.** 378 (above) · 379 permanent reconcile cross-service baseline · 380 `sideSource` written-never-read · 381 `priorBalanceCollectedUsd` blind to refunds · 382 stuck `PENDING_STOCK_SNAPSHOT` invisible · 383 `materialActuals` live-recompute (now fixable with 325's snapshot) · 384 dashboards vs portal basis (**NALDO'S AREA**) · 385 the 7-day handled-but-quiet re-chase (**Jason ruled option (b) 2026-08-24**) · 386 no durable stock-movement audit · 387 `dismissItem` still on a negative CAS.
- **Device checks:** #896 PASSES both halves, verified in the DATABASE (members 1+1 → bills **1 not 2**; 1+3 → **4**). #921 passes 2 of 3 — a hand-typed Front override survived a new Left roofline, and deleting Left's last run reset it to 0 with Front intact. **STILL OWED: the #921 Recount → redraw sequence**, which is the one with zero test coverage; it is not runnable on quote `5ca53030` (no Recount control rendered) and needs a quote whose accessory card exposes it.
- **Row 362 — Kristie Tibbetts still needs a human.** Asked four times during S48, still unanswered. She was told by hand-typed SMS her colour change was 'Champagne'; her booked order builds as-designed, which is what she actually asked for. Money untouched. The install crew builds whatever is frozen, so doing nothing is the one wrong option.
- **Ledger numbering:** Naldo's OPEN PR #923 (`naldo/deferred-findings-367-377`) claims **367-377**; S48 minted from 378 and recorded the collision in the counter. **Next free ledger #: 388.**
- **Process note for the next session:** four transient DNS failures on this machine silently no-op'd a fetch, a pull and four branch deletions — one produced GREEN GATES ON A STALE TREE. Verify the OUTCOME of any git/gh command you depend on; a failed command returns no information, not good news.

**▶▶ Next up (after Jason S47, 2026-08-24) — READ THIS FIRST (quote-tool lane).** Master `e23f1d98`, gates green (tsc 0 · lint 0/19 pre-existing warnings · vitest **396 files / 7138 tests**). Prod verified serving that exact SHA via the GitHub deployments API. **Eight PRs merged+live** (#895 #896 #897 #898 #899 #900 #901 #902) plus a cross-repo relay commit `3884e4b` to the standalone `design-tool`. Zero `jason/*` PRs open.

- 🔴 **The operator auth gate is now ENGAGED BY DEFAULT (#900, row 360).** It was dormant unless `AUTH_GATE_ENABLED === 'true'`, so any local dev server pointed at `.env.local` (which holds PROD credentials) ran operator-only mutations unauthenticated, writing `null` attribution. Measured exposure: exactly ONE row ever (Kristie's colour apply, 2026-08-22 12:33 ET). **On any machine that does not set `AUTH_GATE_ENABLED=false` you must now log in locally at `/login`** — and never set it false while `SUPABASE_URL` points at prod. Jason ruled 2026-08-23 NOT to add a runtime guard for that; the warning is documentation-only by decision.
- **Fix first in S48: row 366** — `/api/dashboard/followup` can silently RESURRECT a completed/dismissed inbox item back to `handled`. The positive gate is read-then-write with a negative `.neq` CAS underneath; `markItemHandledLocal` has three callers so it needs an expected-status parameter, not a global CAS change.
- **Then rows 364 and 365** — both from the S47 wrap staff lens. 364 is a regression S47's own #897 fix round introduced (ScheduleDay's staleness label is gated on `loading` instead of `loadedDate !== date`, so it vanishes on a FAILED refetch while stale crew data stays on screen). 365 is a missing `router.refresh()` after follow-up Done.
- **A DEVICE CHECK IS OWED on #896** and nothing drove a browser in S47 — both review rounds disclosed it. The test: draw two mini strands, set ONE to String count 3, group them, expect **4**; then group two fresh default strands and expect **1, NOT 2** (that second half is the guard against the over-bill).
- **Row 362 — Kristie Tibbetts still needs a human.** She was told by hand-typed SMS her colour change was 'Champagne'; her booked order builds `as-designed`, which is what she actually asked for. Money untouched. Parked by Jason 2026-08-23.
- **Row 361 — the `editor-core` relay is DONE** (`3884e4b` on `design-tool`), but that repo has TWO `groupSelectedMini` call sites to our FOUR, so the cores are behaviourally relayed, not byte-identical. Do not "harmonise" the per-call-site fallbacks in either repo.
- **Next free ledger #: 367.** 363 is Naldo's (open PR #913) — recorded, not taken.

**▶▶ Next up (after Naldo S66, 2026-08-23) — READ THIS FIRST (office-staff lane). Three PRs merged + live; the office-onboarding UI is the next build.** The three real office logins (Kelly, Khaye, **Ann = ariane@**) are linked to their `crew_members` pay rows and flagged `is_office=true` (Naldo too), done by manual SQL and verified by query. Shipped: **#864** (Telegram webhook logs sender id+name for crew linking), **#873** (office web time clock in the dashboard header — `source:'office'`, resolved from the operator session, fails closed, idempotent), **#893** (`crew_members.is_office` + `listActiveFieldCrew()` so office staff no longer appear as assignable field crew; office removed from the Crew-logins panel). **Architecture ruling (recorded in `project_p4p_labor.md` A2):** the office clock can live in BOTH tools — the Quote Tool is the system of record for time, a Hub-side office clock can be built later reading the same canonical `shifts` ledger; office staff never enter the P4P pools. **Wrap integration lens: CONCERNS — 1 MED (row 356: the office/field boundary is enforced at the schedule dropdown but NOT the `assignCrewToJob` write) + 1 LOW (row 357: ClockCard collapses three 403 reasons into one message; latent), both on merged work, both deferred.** **DO FIRST (S67): build the office-onboarding UI (row 354)** — a Settings → Accounts flow to pick/create an operator login → link a `crew_members` pay row → set `is_office=true`, replacing the manual SQL; its own /premerge + Naldo's merge-go. Then row 355 (CI Actions cost, SHARED `.github/workflows`, Jason async heads-up), row 356, row 357. Next free ledger #: **358**. ⚠️ **This session is S66, not S64/S65** — S64 is a concurrent session's open close PR #876 (eight-draft session) and S65 is a different concurrent session's merged close #904 (the light-size slider). Next fresh Naldo conversation = **S67**.

**▶▶ Next up (after Jason S46, 2026-08-23) — READ THIS FIRST. The S44 and S45 backlogs are both CLEARED; this supersedes the S44 entry below.** Master `ba3173ae`, gates green (tsc 0 · lint 0/19 warnings · vitest **385 files / 6935 tests**), CI + Vercel both green at that exact SHA. Ten PRs merged this session (see the Current-state bullet above); zero `jason/*` PRs remain open.

- 🔴 **DO FIRST, and it is not code: two customers are waiting on a HUMAN colour-change reply.** **Kristie Tibbetts** ($761.25, requested 2026-07-29 — the oldest open item in this project) and **Susan Pace-Burke** ("Champagne", $1,680.19). The tool surfaces them now (row 321/#847); nothing shipped since answers them.
- **A browser leg is owed** on this session's customer-facing changes (post-approval freeze, revive/selection-clear, identity freeze). Both close-review lenses were code-trace only — no dev server was reachable. Drive the portal + quote builder before treating those as device-verified.
- **Rows 342-346** are this session's deferrals, all with the evidence that produced them: 342 Gmail write-back failures invisible to staff (a population #886 multiplied ~100x) · 343 identity recovery-path + refusal audit · 344 portal live-price vs frozen-total drift + the still-unfrozen footage population · 345 the permanent-side derive sharing row 333's clobber shape · 346 the six remaining Loading morphs. **Row 322** (the colour-request APPLY panel has never once been used successfully) is Jason-approved and pairs naturally with the two customers above.
- **Next free ledger #: 347.** Row 337 belongs to Naldo's unmerged #876 — recorded, not taken.
- **Worktrees NOT pruned** (14 exist, all clean, 12 on merged branches) — a named-consent prune list is in the S46 handoff.

**▶▶ Next up (after Jason S44, 2026-08-20) — READ THIS FIRST. CONCURRENCY ENDS: S45 is a SINGLE session covering both lanes.** S44 shipped **15 PRs** (master `25c15df8` → `9451dc1e`): rows 243/239/237/313/171/242/315/240/248/236 all SHIPPED+LIVE, plus the C9 materials 12″ pin, the dead NCE deposit-revert fix, the portal colour-restore race, and item numbering + staff rename. ⚠️ **CI IS DOWN REPO-WIDE since ~18:55Z** (every Actions run fails in seconds, including on master; workflow unmodified — likely exhausted minutes). S44's close rests on LOCAL gates: tsc 0 · lint 0 · vitest **6689 / 379**. Prod alias-verified serving `9451dc1e`. ⚠️ **The four-lens WRAP review did NOT run** (subagent budget exhausted) — every individual PR got its own four-lens round + delta-verify, and the combined master gates green, but **S45 should run a wrap-style review over the S44 span before treating it as reviewed.** **Jason's locked rulings:** identity is ATOMIC past approval (customer_id + HighLevel ids + displayed contact fields freeze together); drawn spacing is design-only (materials pin 12″); NO Approved stage (sent → booked or declined); duplicate items number Tree 1/2 + staff rename; operator portal pre-select = always-allowed-with-confirm (row 324); AI-seeded c9 draws at 15″. **DO FIRST (S45):** the missing wrap review → **#839** (atomic identity; needs the independent delta-verify its last round never got) → **#846/#847** (S43's, merge-ready on a CI confirm alone) → PRs for rows **244** + **260** (260's additive migration applies at merge) → rows 324-334. Next free ledger #: **335** (322/323 are S43's post-close delta). Worktrees NOT pruned — S43 is live and `wt-s43/w-303` holds uncommitted red tests.

**▶▶ Next up (after Naldo S61, 2026-08-19→20) — the two blocking PRs are MERGED and LIVE, the migration is APPLIED, and the crew panel turned out never to have been rendered.** Supersedes the S58 post-close entry below, which is now stale in three places (it says #812 is open, the migration is unapplied, and that you can create a crew login at Settings → Accounts → Crew logins — that screen did not exist).

- ✅ **PR #812 MERGED + LIVE** (`a307ab3f`, prod-verified): the crew→admin privilege escalation is closed, and the DST payout bug is fixed. Verified independently by sweeping all 365 days of 2026 — old code wrong on exactly 2 (Mar 8 an hour early = underpay, Nov 1 an hour late = overpay), new code correct on all 365. **No real payroll was ever damaged**: `opsMidnightClose` only shipped 2026-08-17, after spring-forward and before fall-back, so the buggy formula never crossed a live transition.
- ✅ **PR #813 MERGED + LIVE** (`42284c9e`): the S58 close, after resolving a CLAUDE.md conflict with #811 by keeping both additive sides.
- ✅ **`2026-08-18-job-assignments.sql` APPLIED to prod and query-verified** (7 columns, 5 indexes, RLS on with 0 policies, 1 trigger, 0 rows). `/admin/schedule` should no longer error. **The Supabase MCP token works fine from a cloud container** — the S58 outage was local to Naldo's machine.
- ✅ **PR #818 MERGED + LIVE 2026-08-21** (`9d556444`, prod deploy SHA-verified, aliased to quote.yulelovelights.com). Carries the DELETE-side crew guard that #812's body wrongly claimed to have (three lenses converged), plus **ledger row 318** (claimed as 301, then 313, then 316; renumbered three times — see the ledger counter): an admin surface to link `crew_members.telegram_user_id`.
- 🔴 **The reason the S58 handoff's step 4 could never have worked, in two parts.** First, `<CrewLogins />` was imported by `src/app/settings/accounts/page.tsx` and **never rendered** — so Settings → Accounts → Crew logins has never existed as a screen and S58's row 296 crew-login feature shipped dead. Second, a crew LOGIN is not the bot identity: the Telegram time clock resolves a punch only through `crew_members.telegram_user_id`, which **nothing in the app could write** (`updateCrewMember` had zero callers; the seed backfill joined the empty `bot_users`). Both fixed in #818.
- ⚠️ **Linking Telegram is necessary but NOT sufficient.** The webhook drops any chat not in `TELEGRAM_ALLOWED_CHATS` before the dispatcher runs. A linked crew member texting in an already-allowlisted crew group works immediately; a 1:1 DM stays silent until their id is added to that env var too.
- ✅ **The ledger collision is RESOLVED.** The crew-Telegram row renumbered 301 → 313 → 316 → **318** (each move ceded to a claim that merged first); PR #819, which had also claimed 301, was closed unmerged on 2026-08-20. Row 301 stays with S50's merged wedding-copy row.
- ⚠️ **Every `budgeted_hours` is STILL a placeholder** and the seed-rates session is still undated (Naldo's standing call). The guard that stops a placeholder reaching pay math is intact.
- ✅ **The crew panel has now been seen rendering, on prod, in a real browser** (2026-08-21, admin session). All four crew list (Big James, Jason Balroop, Little James, SonSon), every one reads "No login yet" and "No Telegram, cannot clock in", and both the create-login form and the Telegram-link form render. `GET /api/admin/crew-accounts` returns the same four with `hasLogin: false` and `telegramUserId: null`. NOT run, by Naldo's call: the adversarial @handle rejection test (a prod form submit) and the end-to-end clock-in.
- **DO FIRST (next session):** link one crew member's numeric Telegram id (they get it from `@userinfobot`) in Settings > Accounts > Telegram time clock, then have them text "in" **from a chat already on `TELEGRAM_ALLOWED_CHATS`**; a one-to-one DM stays silent until that id is added to the env var and the app redeployed. Still open and reported, not fixed: no crew password-reset or unlink path exists anywhere; crew rows render as "operator" in the accounts table while ALL FOUR row actions 403 (Edit name, Reset password, Make admin and Remove: three PATCH plus the DELETE that #818 guarded), with no visual cue that the row is crew; the time-clock parser is exact-match, so "Done!", "back." and "I'm in" do not register.
- **Still open, reported not fixed:** no crew password-reset or unlink path exists anywhere in the app; crew rows render as "operator" in the accounts table while ALL FOUR row actions 403 (Edit name, Reset password, Make admin and Remove: three PATCH plus the DELETE that #818 guarded), with no visual cue that the row is crew; the time-clock parser is exact-match and drops `"Done!"`, `"back."` and `"I'm in"` (a real unpaid-time risk once crew are live).
- Gates at close: tsc 0 · lint 0 errors (19 warnings) · vitest **6094 / 367 files**. Master at close: `92262475`. Re-gated on the 2026-08-21 resync of #818 against master `365d07a2`: tsc 0 · lint 0 errors (19 warnings) · vitest **6701 / 379 files**. Re-gated again on the final resync before merge: tsc 0 · lint 0 errors (19 warnings) · vitest **6762 / 381 files**, CI green on the exact head SHA. S62 has since run (PR #848); the next fresh conversation is **S63 or later** — read both session logs before claiming a number.

**▶▶ Next up (after Jason S42, 2026-08-20) — READ THIS FIRST (quote/portal lane; S43 = inbox lane runs concurrent):** S42 shipped **row 269 (#799)** — alreadySent redeliver scoped to per-channel `quote_deliveries` outcomes on BOTH send surfaces, typed-YES/confirm gates, the socket-reset class hedged outcome-unknown — and **#805** — amendment notices in exact cents on the INVOICE basis, "after installation" conditional on job status, cased greeting, amend-reason customer-facing warning. A LIVE amendment ran mid-session (Susan Pace-Burke: +$342.56 → phone decline → −$342.56 reversal, signed in 41s). **FOUR staged branches await the ruled merge queue #809→#808→#807→amend-decline** (re-merge master + re-gate BEFORE each; the #808×amend conflict is 3 files, resolve KEEP-BOTH — especially `operatorGate.test.ts`, where dropping either route's entry still gates green): #809 NCE/Neighbor holiday-only (money gate + deposit-40 reset + approval-freeze parity) · #808 portal selection memory (**migration ALREADY APPLIED to prod** — merge-ready) · #807 event date→GHL (SHARED `quotes.ts` additive; Jason waived review — leave Naldo a heads-up comment at merge) · amend-decline (NO PR yet — **round 5 FIRST** per Jason: pre-stamp invoice re-read, rename the "byte-identical" test, add the decline-during-amend concurrency test). ⚠️ **Row 313 is LIVE on master** (amendment card vs notice figure mismatch on tax-overridden invoiced jobs — no customer hit; the amend-decline merge closes it). Rows 314 (event-date at approval, Jason's 1b) + 315 (polish batch) queued. Gates at close: tsc 0 · lint 0/20 · vitest **6122/366**. Next free ledger #: **316**. Worktrees on disk for the four branches: `yll-amend-notice`, `yll-row237`, `yll-row239`, `yll-row243` (S43 must NOT touch). Next fresh conversations: **S43 (inbox) + S44 (this lane)** — Jason numbers at launch.

**▶▶ Next up (after Naldo S58 POST-CLOSE, 2026-08-19) — P4P Track A Phase 2 COMPLETE, Phase 3 foundation shipped:** crew auth into a shared login store, the three Flow B routes, ET midnight auto-close, the exception queue (row 278), Telegram crew time capture, and scheduling (rows 279-282, 296-298 — all now labelled **S58 post-close**, see the correction below). ⚠️ **SESSION NUMBERING:** rows previously said "S59"/"S60"; that was wrong — all of it happened in the single S58 conversation, and **S59 belongs to a different session** (the 8-day copilot outage, open PR #810). Next fresh conversation = **S61**. ⚠️ **PR #812 is OPEN and contains a PRIVILEGE-ESCALATION FIX** (a crew login could be promoted to admin via `PATCH /api/admin/users/[id]`) plus a DST payout bug (forgotten shifts capped an hour early/late on both transition days) and a bot-help collision — **merge it first**. ⚠️ **`2026-08-18-job-assignments.sql` NOT APPLIED** — the Supabase MCP lost its token; the schedule page errors until it runs. ⚠️ Every `budgeted_hours` is STILL a placeholder; the seed-rates session is deliberately undated (Naldo's call). **DO FIRST:** merge-go #812 → apply the migration → create a crew login at Settings → Accounts → Crew logins and test the bot end to end. Gates at close: tsc 0 · lint 0 errors · vitest **6081** / 366 files.

**▶▶ Next up (after Jason S41, 2026-08-20) -- INBOX LANE (concurrent-session structure live: S41=inbox ran beside S42=misc; next pair S43=inbox + S44=misc, Jason numbers at launch):** S41 shipped FIVE PRs (#796 write-back message-level-or-nothing / #797 per-item busy+error+Dismiss / #798 follow-up auto-close + churn gate + audit / #806 thrown-fetch restore+lock [found by Jason's device check] / #814 Needs-a-look split + confirm gate) and THREE consented prod data ops (26 unanswered-quote rows re-filed to Awaiting [$101,887] · 3 buried completed-with-live-quote rows reversed [1193/1230/1246] · Aurea Herod x2 handled). **Jason's bucket ruling is LAW now (row 252): open = they spoke last / awaiting = we spoke last + something outstanding / handled = nothing outstanding, off-screen, inbound reopens. Naldo handed INBOX ownership to Jason (AGENTS.md, this close).** **S43 DO IN ORDER:** verify #814 live (Needs-a-look 13 / Handled 71) -> hand Jason the sweep instructions (in the S43 handoff) -> Jason sweeps ~63 collapsed-Handled rows -> slice C (bucketOf() single source of truth) -> slice H renames -> Jason's device-check checklist. Review debt: rows 303-306 + 308-310 (306 = markItemFollowed guard, Jason: leave queued). Row 292 CEDED to S42 (its #799 fixed it). S42's ledger floor after this close: 313 (wrap lenses took 311-312). Gates at close: master CI 366 files / 6122 tests green at `01e3fe9b`; prod alias-verified. Naldo's next = S61 (per-dev numbering).

**▶▶ Next up (after Naldo S59, 2026-08-16) — P4P Track A PHASE 2 IS COMPLETE:** `job_segments` (arrive/depart, breaks-pause-job-time, missed-tap backstop), the three Flow B routes (`/api/ops/v1/jobs/{id}/arrive|depart|complete`) behind crew auth, and the ET midnight auto-close — all in **PR #779**. Naldo's 2026-08-16 rulings are all recorded in `project_p4p_labor.md` A6: rain day = day off · clock override = self-claim then Jason approves · no backup approver · takedown starts **Jan 4** · crew auth = ONE shared login store for the Quote Tool AND the Operations Hub · `/complete` = `jobs.status='installed'` via the existing machinery, no new vocabulary. ⚠️ **SECURITY, worth knowing:** shared identity is NOT shared authorization — `roleOf` would have classified a crew login as an *operator* and the proxy admitted any authenticated user, so the first crew account would have reached `/customers` and its PII. Closed at both layers (`isCrewAccount` + a role-aware `src/proxy.ts`), both tested. ✅ **`2026-08-16-crew-members-auth-user-id.sql` IS APPLIED to prod** (2026-08-16, verified by a real query: nullable `uuid`, partial unique index with its predicate, comment attached, 4 crew rows / 0 linked). It landed a few hours after the PR merged because the Supabase MCP had dropped mid-session. ⚠️ **Every `budgeted_hours`/`labor_revenue_cents` is STILL a placeholder** — the seed-rates session is deliberately undated and unworked (Naldo's call). **STILL OPEN:** A6 item 5 (success metrics, row 283) is the one product question with no ruling; off-season Feb-Aug and the labor-% dial are deferred; rows 284/285 (attorney, payroll provider) are parked non-blockers. **NEXT:** Phase 3 = scheduling calendar, or the row 278 exception-queue sweep. Gates at close: tsc 0 · lint 0 errors · vitest **5707** / 353 files. Next fresh conversation = **S60**.

**▶▶ Next up (after Jason S39, 2026-08-17) — READ THIS FIRST:** S39 shipped **THIRTEEN ledger rows across 12 PRs** (master `d8c9937b` → `c99b76ae`, prod alias-verified after every wave): row 270 send-honesty (#767, 3 rounds) · 252 slice D per-contact inbox rollup (#769) · row 268 GML lead-forwards classifier fix (#770) · rows 210/211 docs (#772) · row 234 email escaping (#773) · row 271 502 stage fields (#774) · rows 245/207 bistro colour-gate + route skeletons (#775) · rows 224/230 completion guard + suppression visibility (#776). **PROD DATA OP (named consent):** an 8-row GML backfill — 5 contacts created, 3 matched, all repointed off the forwarder contact; #770 then confirmed working unaided on 4 new prod threads. **FIVE stale ledger rows closed with evidence** (173, 114, 220, 222, 189a — all previously shipped, the ledger just hadn't caught up). Gates: tsc 0 · lint 0 errors/19 warnings · vitest **5722 / 353**. ⚠️ **The wrap found 1 HIGH → row 288 (pre-existing, still live): GML thread-coalescing** — Gmail folds distinct customers' forwards into one thread under Zapier's reused subject, identity resolves only for the FIRST customer, so **~20 real leads all-time have no row/contact and 2 live rows display one customer while linking another's contact** (wrong-call risk: staff working the row as displayed reach a different real person). Two MEDs: row 289 (group-collapse hides a lost-race error note) and row 290 (timeout-hedged retries keep the low-friction redeliver confirm despite a GHL outcome-unknown failure). **Decisions locked (don't re-ask):** permanent "Whole Home" tile does NOT auto-scroll — confirmed wanted (Jason 2026-08-17). Device checks 1-5 (typed-YES send path, Mark-Abandoned, bistro gate, route skeletons, inbox rollup) ALL passed on Jason's device. **Jason NEXT:** **row 288 FIRST** (live customer path, wrong-call risk) → rows 289/290 (small) → **#252 slices E/F** (now unblocked — #734 superseded) + C/H (still needs Naldo's bucket-semantics) → row 269. Next free ledger #: **291**. Next fresh conversation = **S40**.
**▶▶ Next up (after Naldo S58, 2026-08-11) — P4P Track A Phase 2 slice 1 SHIPPED+LIVE:** `shift_breaks`, the unpaid-break ledger on top of S57's `shifts` clock (**PR #732 MERGED**, migration APPLIED to prod 2026-08-11 and verified by real queries). Breaks are unpaid, so paid time = clock envelope − break time; `src/lib/shiftBreaks.ts` owns that arithmetic, merges overlapping breaks so an office correction cannot subtract the same minutes twice, and clock-out auto-closes a running break at punch time flagged `auto_closed` for the `open_break` queue — all three behaviors taken from `OPERATIONS_HUB_CONTRACT.md` Flow B rather than invented. **PR #713 (S55 close) finally merged** after a renumber of its ledger row 222 → 234. **`rates_are_placeholder` is no longer write-only:** `src/lib/laborPlan.ts` + an ESLint rule now stop a placeholder figure reaching pay math silently (A6 item 12 updated to match). **A6 item 13 CLOSED** — Naldo's ruling: the plaintext crew-wage exposure is an accepted risk, documented, with one named reopen condition. ⚠️ **Every `budgeted_hours`/`labor_revenue_cents` in prod is STILL a placeholder** — the Naldo+Jason seed-rates session still has NO DATE and is the top Track A blocker. **PR #743 MERGED** (`41508405`): the S58 wrap review's own fixes (the paid/break decomposition was not exact on millisecond inputs; the lint rule missed bracket notation; error branches were untested). **NEXT:** seed-rates session → Phase 2 slice 2 = job arrive/depart/complete segments, then travel, then Telegram wiring LAST so the bot is not rebuilt twice. `shift_breaks` is live with ZERO HTTP readers by design — the Flow B routes are unbuilt. Next fresh conversation = **S59**.

**▶▶ Next up (after Jason S36, 2026-08-12) — READ THIS FIRST:** S36 shipped **FIVE ledger rows, all merged + live** (#226 NCE-false deposit reset · #227 orphaned-miniGroup billing · #212 discount for event/permanent/bistro · #225 `invoices.settled_by` · #223 toolDefaults trim), captured **19 backlog rows (235–256)** from a dictated dump, and pushed **three design-tool relays** (#202/#203/#227 → `main` `20900ed`). Master `693d112e`, gates tsc 0 · lint 0 errors · **5310 tests / 340 files**, prod deploy SHA-verified.

**▶▶ Next up (after Naldo S57, 2026-08-07→10) — P4P labor-tracking Track A foundation SHIPPED+LIVE:** `crew_members` (PR #712), `jobs.budgeted_hours`/`labor_revenue_cents`/`rates_are_placeholder` (PR #720), `shifts` clock-in/out ledger (PR #724) — all MERGED, master past `69a4a65d`. Built via the new `codex-mode` skill (Claude orchestrates, OpenAI Codex CLI writes code from `C:\dev\yll-quote-tool`). The cross-repo `docs/context/OPERATIONS_HUB_CONTRACT.md` (mirrored in `yll-call-copilot`) reached **v1.3.0-draft** with real rulings from Naldo (piece-rate ad pay $2.50/sign, sign inventory in scope, four daily digests, vendor-neutral payroll CSV, door-hanger pay OFF, no-required completion photos w/ a 3-prompt cadence). ⚠️ **Every `budgeted_hours`/`labor_revenue_cents` number in prod right now is a PLACEHOLDER production rate** (loudly flagged via `rates_are_placeholder`) — real numbers need the **Naldo+Jason seed-rates session (still no date)**. **PR #726 MERGED 2026-08-10** (this line read "OPEN, needs Naldo's explicit merge-go" until the S58 wrap review's docs lens caught it still saying so a day after the merge; gates at the time were tsc 0 · lint 121 warnings/0 errors · vitest 5187) (touches AGENTS.md — the S57 wrap's own 4-lens review found + fixed 4 real cross-file gaps: `FULL-SCHEMA.sql` missing both new tables, a self-contradicting migration-application rule, a sibling-guard parity gap on `crewMembers.ts`, and a `{}` vs `undefined` contract break on empty bistro geometry). **Deferred to `project_p4p_labor.md` A6 #12/#13:** no recompute mechanism yet for placeholder-rate jobs once real rates land; real crew wage data sits in plaintext in a git-tracked migration file (**DECIDED S58, 2026-08-11: accepted risk, closed — A6 item 13 records exactly what that accepts and the one condition that reopens it**). **NEXT:** merge-go #726 → seed-rates session → Track A Phase 2 continues (breaks, arrive/depart/complete segments, Telegram wiring — none built yet). Full detail: `session_log_naldo.md` S57, `project_p4p_labor.md`. Next fresh conversation = **S58**.

**▶▶ Next up (after Naldo S55, 2026-08-07) — nothing pending; one bug opened and closed.** The escalation + end-of-day digest emails had a relative `href="/inbox"` that mail clients resolved as a HOSTNAME (`DNS_PROBE_FINISHED_NXDOMAIN`), so the "Open the inbox →" link had NEVER worked from email since the feature shipped. **PR #704 MERGED + LIVE** (master `f5e4be4e`, prod deploy SHA-verified): both builders in `src/lib/dashboard/inbox/notify.ts` now take an absolute `baseUrl` from the server caller (`runEscalation` → `appBaseUrl()` → `PORTAL_BASE_URL`). Gates on the combined tree after a 26-commit master resync: tsc 0 · lint 0 · vitest **4924**; CI `gates` green. Four-lens wrap review: all four lenses CLEAN on the diff; one PRE-EXISTING finding ticketed as **ledger 234** (customer `name`/`preview` interpolated into the internal escalation/digest email HTML unescaped — same issue the July audit raised as W7-025). Note for whoever reads an old alert: escalation emails already sitting in the inbox still carry the broken link; only newly-sent ones are fixed. Next fresh conversation = **S56** (S54 was taken by a concurrent session).

**▶▶ Next up (after Naldo S53, 2026-08-07) — post-call commitment engine:** recon + plan DONE, zero code, zero prod writes. **PR #707** (draft, docs-only, CI gates green, 4-lens reviewed at wrap — 13 findings fixed in-doc, 2 accepted) = the v1 plan (`docs/superpowers/plans/2026-08-06-post-call-commitment-engine.md`) + ledger rows **217–221** (engine epic + 4 quick wins; **216 left for Jason's S34 candidate**; next-free 222). Naldo's rulings in row 217: hub-hosted checklist · v1 = checklist+auto-verify only · all reps. Aug-6 evidence: 3 promised quotes unstarted (Sheryl M. event lead / Rich D. / Chris H.) — morning-triage list delivered in-chat. The ops hub is REAL (contract via PR #701, Codex side, mirror in yll-call-copilot) — #217's event feed goes through its §10 change process. ⚠️ Copilot DB: RLS OFF on 26 tables incl. all 1,210 transcripts (row 221, advisor CRITICAL). **DO FIRST:** morning triage → merge-go #707 → who builds copilot-side rows 218/219/221 (this assistant vs Codex)? → #217 slice 1 after #701 lands. Desktop continuation = **S54**.

**▶▶ Next up (after Jason S35, 2026-08-10) — READ THIS FIRST:** S35 shipped **SEVEN ledger rows, all merged + live + SHA-verified** (master `950dfa56`, vitest **5155**): **#214** identity-caller seam (callers re-verify the customers link; booked-freeze) · **#199** NCE money (40% deposit default, balance blocked on all 3 paths, Mark-paid-NCE + required trade reference) · **#205** Properties tab (nickname/manual add/Maps pin/archive-not-delete) · **#208** `handled_by` uuid (a display string was making Postgres reject the WHOLE inbox update, silently) · **#203** trace draw-over-strand parity · **#202** decor sizes → Small/Medium/Large · **#215** builder chip confirms. **Two prod migrations applied + verified** (`invoices.paid_method`/`payment_reference`; `properties.nickname`/`archived_at`). **CI `gates` is triggering again** after being dead since S34. Jason device-verified #199/#202/#203/#205/#215 live.

⚠️ **The wrap review found 2 HIGHs sharing one root cause → ledger #226, MONEY, LIVE ON PROD NOW: nothing treats "NCE resolves to false" as a signal to reset `inputs.depositPercent`.** (a) the admin NceToggle OFF path DELETES the key instead of writing an explicit blank, so `chargesFromResult` falls back to the stale `result.depositRate` and the portal + approval snapshot + Valor charge stay at 40% on an untagged quote (its own confirm copy promises 50%); (b) `rebookLastSeason` resolves `is_nce` from the customer's current tag but only ever ADDS the default, so someone who left the barter network is rebooked non-NCE and still charged 40%. Both UNDER-collect (~10 deposit points) — no customer is harmed financially, YLL collects less. The builder chip is correct by contrast (writes `0`). **Fix #226 before anything else.** Other wrap rows: **#224** inbox cross-action race (pre-existing, newly reachable via #208, Naldo's area) · **#225** no WHO on a manual settle · **#222** relay #202+#203 into the design tool (durable capture files committed; NEITHER has been relayed) · **#223** `toolDefaults` still offers the pre-#202 inch supersets.

**Jason NEXT:** #226 (money) → #222 relay → #201 (needs his Jobber reports) · #212 discount · #206/#207/#209/#210 · owed: clear the ONE stuck `:color-request` inbox row (verified: only 2 exist ever, 1 stuck), decide whether the approved-unpaid alert takes the still-free row **216**. **Valor CLOSED:** Sharon approved+signed then closed the tab without attempting payment — no Valor fault, no Fadil letter; Jane's Aug-3 hourglass stays a separate environment-side case. Next free ledger #: **227**.

**▶▶ Next up (after Jason S34, 2026-08-07) — READ THIS FIRST:** S34 captured Jason's batch as ledger rows 198–215 then shipped FIVE of them live (master 0674e1c4, vitest 4883, every deploy SHA-verified): **#198 NCE + YLL Neighbor tags** (quote+customer tags, builder chips, propagation, profile UI) · **#200 GHL "Years with YLL" mirror** (field id 0FDCH8wSbalqhAKJeTpH created + E2E-proven on Melissa Wohr; tenure floor now 2022) · **#700 hl-link heal + 153-row backfill** (166/168 customers rows had NULL hl_contact_id — the device round caught it) · **#204 Pull satellite** (no-Street-View quotes unblocked; analyze partial-success) · **#213 identity-merge gate** (single-shared-field adoption dead; dup rows + candidate-merge logs instead). ⚠️ **The S34 wrap review found the seam BETWEEN the PRs: ledger #214 (identity-resolution callers bypass the #213 gate — 3 HIGHs, one fix shape) is THE remaining #199 blocker — build it before #199 (NCE money behaviors).** Valor hourglass: diagnosed environment-side (Jane, Aug 3 — failed on Valor's own emailed link too); Sharon McDonough #1173 = the Aug 6 case (her quote reverted to sent for add-on edits — she re-approves fresh; her GHL stage needs a manual drag back). **Jason NEXT:** #214 → #199 · #201 needs his Jobber reports (after #205) · #215 chip confirm · Fadil letter (two timestamped cases) + Sharon callback + the approved-unpaid alert row (candidate, 216) · chip pre-light eyeball. Next free ledger #: **216**.

**▶▶ Next up (after Naldo S51 → S52, 2026-08-05) — #167 P1 endgame:** ✅ **DONE tonight (S52):** migration applied + verified · night-photo backfill run (141/159 landed; 18 iPhone HEIC files failed — no HEVC decoder on the operator's machine, parked) · imagery fetch run (75/77; 2 addresses fail geocoding permanently, refuse-to-guess is correct) · device-check found a real HIGH (Auto-Analyze silently zeroed a traced/overridden archive gutterline+ridge) · fixed + live-verified · pre-merge 4-lens review found a second instance of the same bug in the detection setters (staff + technical lens, independently) · fixed + gates green (tsc 0 · lint 0 · vitest 4613) · **PR #680 MERGED + prod-deploy SHA-verified** (`quote.yulelovelights.com/training/archive` live). **NEXT:** work the trace queue (~80 properties ≈ 3h, grind-down visible) · deferred from the 4-lens review as ledger candidates: `excludeArchivePhoto` missing the file's own CAS guard (MED, real race with `identifyArchivePhoto`) · Cancel doesn't route back to `/training/archive` like Save now does (MED) · no server-side floor on zero-footage saves, repo-wide not just archive (HIGH but pre-existing, inert until a promotion path exists) · backfill script's basename matching has no `source_folder` cross-check for a future overlapping-folder ingest (MED). Still open from S51: PII-redaction SQL for the two reviewer_notes rows; the 167 ledger-number duplicate (fold into the 192/193 resolution); queue pagination before the next manifest drop. Archive examples stay OUT of ground-photo few-shot by design (source='manual' positive filter, verified by the review — traced through to the actual retrieval function); wiring them into a satellite-mode path is a separate design pass.

**▶▶ Next up (after Naldo S47, 2026-08-05) — READ THIS FIRST:** **Text-ops bot #168 + the morning digest are BUILT + LIVE in prod** (master `e7e13d06`, combined gates vitest **4584**). Bot: reads (status/schedule/stock/low/jobs) + crew `completeInstall` (material actuals + on-hand true-up + photos) + Bot-team settings page, role/confirm/audit gated, live on 2 groups + both DMs. Digest: cron un-blocked (#675), routed to Naldo+Jason DMs, heartbeat v2 (#684) with rebook/awaiting-reply/changes-requested/inbox counts, always-sends (silence now means broken). **POST-CLOSE (same conversation):** **captureLead Phase 3 SHIPPED + LIVE (#693, task #11 DONE)** — crew field-lead → GHL contact + SMS-drip tags, behind role + confirm-yes + an ADDRESSED consent affirmation. **Digest hardened (#694)** (exhaustive count scan + silent-fail alert). **The two #666 crons restored (#688)** (inventory-prep + jobs-completing, dead since Aug 3). **#637 salvaged (#692)** + closed. A 5-agent "review all your code" pass fixed 6 MEDIUMs. master `23b6e915`+, gates vitest **4676**. **Still open:** 3 bot hardening chips (album buffering / prep-deduction baseline / webhook hardening) · `/settings/telegram` admin-gate call · GHL transcript pipeline. Next fresh = **S52**.

**▶▶ Next up (after Jason S33, 2026-08-05) — READ THIS FIRST:** S33 shipped TEN ledger items live (PRs #659–#665/#667/#674, master 05b83ea8, vitest 4578): #189 inbox index (9–15s → 0.4s; prod-applied; #189b = Naldo's cron-flood lanes) · #187 view-only/send hardening · #188 FULL-SCHEMA regen (live-verified 30/30 tables; 4 canon indexes MISSING on prod — paste-able with Jason, UNAPPLIED; drift since regen already 4 new dated files) · #171 a–h polish (only (d) dup-label decision open) · #173 charge-balance stale-balance race (refute-lens-hardened) · #190 analyze-photo satellite-scale clobber (stake-0ft live bug, same-day fix) · #191 permanent facade glow REMOVED · #192 per-side track style + approved-sides BOM scoping (+#193 amendment-aware follow-up queued). ⚠️ **192/193 NUMBER COLLISION with Naldo's open S49 close PR #670** — proposed: #670 (docs-only) renumbers to 196/197; Jason to ping Naldo; next-free # frozen at 196 until resolved. **Jason NEXT:** ping Naldo (collision + branch-name `jason/187-*` misattribution + notifyTelegram/S46-plan staleness + #189b + inventory_orders RLS + time-to-completed KPI note) · trey's-portal colour-pick eyeball (#191) + per-side-rows eyeball (#192, now live) · #171d · 4-index paste · orphan-design cleanup call (5772e96d + 0e176303) · #582 free-items prod round · batch-3 dispositions · YLL Neighbor tag re-apply · #164 (MSP rate). **Rows to open next session (post-collision):** stale-draft-vs-lead-prefill clobber (wrap staff MED) · Leads loading skeleton · handled_by-uuid write bug · street-re-analyze-vs-satellite-footage product call · #189 migration-comment CONCURRENTLY truth-up.


**▶▶ Next up (after Jason S30, 2026-07-26) — READ THIS FIRST:** **AUTO-CHARGE IS LIVE (#83 COMPLETE).** `VALOR_AUTO_CHARGE_ENABLED` is ON in prod; the full chain is proven end-to-end (deposit saves the card → staff one-click "Charge saved card" → $1.09 live test charged txn 10450479359, invoice settled, job auto-closed). The #170 arming checklist all shipped (PR #640): atomic settle + double-charge/charged-cancelled alerts, `valor_txn_log` audit column, capture-equality + $25k guards, **`payment_preference`** (cash/check customers are never one-click charged; explicit override only), deposit consent line. ALSO live: **#172** GHL abandoned-card RESURRECT (attach/send/website-leads reopen a returning customer's non-open card; OPEN cards refused — diana lopez-smith live-proven), **#641** portal custom-label fix + clickable admin list #s + "Awaiting payment" job label, **#169** colour-apply notify visibility. Master `ba6c5bde`, gates tsc 0 / eslint 0 / vitest **4079**, all deploys SHA-verified. **OPEN: PR #643** (S30 wrap-review fixes — critical tone for settle-failed/amount-mismatch, the missed queueAttach call site, job→invoice links, resurrect chip visibility) awaiting Jason's merge-go. **Naldo, two flags:** (1) your WorkflowBoard still says "Requires invoicing" for the job status while using "Awaiting payment" for invoices — my jobs-list rename made the same status read differently across surfaces (your area); (2) **before the Neighbor send wave, confirm the Neighbors drip triggers on stage-entered, NOT opportunity-created** — resurrected cards skip created-triggers. **Jason NEXT (S31):** merge #643 · #171 polish (+ dup-label call) · #582 free-items round · batch-3 dispositions (Naldo: the 2 comped names) · #164 (needs the MSP rate) · #173 stale-balance race (new row, small). Next free ledger #: **174**.


**▶▶ Next up (after Jason S29, 2026-07-23) — READ THIS FIRST:** **#161 card-on-file = SOLVED + LIVE.** Every deposit now automatically banks its payment token (`quotes.valor_vault_token`, redirect_url capture chain #629–#631) AND registers the card in Valor's own Vault (#632/#634 — first live entry 4237995, dashboard-verified). **#83's merchant-initiated charge is PROVEN** ($1 tokenized sale APPROVED + voided; recipe: `surchargeIndicator=1` + `shipping_country` — even voided-sale tokens charge). **Auto-charge is fully WIRED (#633: real chargeBalanceOnFile + charge-slot idempotency) but `VALOR_AUTO_CHARGE_ENABLED` stays OFF until ledger #170** (arming checklist: cross-path double-charge race vs the pay-link · amend-reopen 409 · chargedUsd sanity cap · staff visibility/override UI · consent line at deposit). Batch-2 send-prep cleared (19/19 GHL-linked, photos accepted, **Talonda parked**) → 18 send-ready; wave on Naldo's drips. Also live: app-wide route loading skeletons (#626) + the portal label detail strip (#628). Polish backlog = #171. Fadil (Valor) answers in writing: no MIT tag needed, no required consent language, #164 fee via `surcharge_amount`, rates via the MSP. Next free ledger #: **172**.

**▶▶ Next up (after Naldo S48, 2026-07-26) — READ THIS FIRST:** **Self-serve estimator REDESIGN — PR #624 OPEN, review-clean, NOT merged.** The whole public `/estimate` was rebuilt into the dark "Option A" mockup: "Let's make your life bright" hero, a **real Christmas-only completed-job gallery** (before/after slider, DesignCanvas render at dusk, gold trim, YLL logo, full quote-tool colors), how-it-works, **photo upload** (→ hand-designed follow-up), **recolor-your-own-house** result, "what happens next" close. Also folds in the S47 range-low fix + `/estimate` made indexable. Branch up to date with master (`ba6c5bde`, no conflicts), gates green **tsc 0 / eslint 0 / vitest 4088**. Four-lens pre-merge review done: **2 HIGH + 3 MED + 2 LOW fixed** (Anonymous-upload-lead bug; a gallery PRIVACY leak now gated to **staff-sent** quotes only — never raw visitor drafts; upload spend cap; orphaned swatches; measuring timeout). **Flag-gated (`SELF_SERVE_ESTIMATE_ENABLED`, ON in prod) + UNLINKED** — merging updates the live-but-unlinked `/estimate`; no customers until the WordPress embed is pasted. **DO FIRST next session:** (1) Naldo's **merge-go on #624** + the **gallery-consent** decision (feature staff-sent homes as-is, or add an opt-in "OK to feature" flag first); (2) after merge, **paste the embed into a WordPress page** (Elementor create-don't-edit; `docs/self-serve-estimate-embed.md`) to LINK it; (3) deferred: Cloudflare Turnstile (needs Naldo's CF account); (4) +10% pricing buffer stays on HOLD until ~20–30 real quotes fill the accuracy tile. **Cross-dev (Jason):** SHARED `designs.ts` gained `listSampleDesigns`/`SampleDesign` (additive) + `operatorGate.ts` gained 3 allowlist entries (additive) — no existing shared behavior changed.

**▶▶ Next up (after Naldo S46, 2026-07-20) — READ THIS FIRST:** **Text-ops bot + YLL "ops tool layer" — PLAN drafted, PR #596 DRAFT (not merged), zero code shipped.** A Telegram staff-only bot that drives the quote tool via a shared tool layer, EXTENDING the dormant #82 bot (webhooks/allowlist/`notifyTelegram` already built). Full plan `docs/superpowers/plans/2026-07-19-text-ops-bot-and-yll-ops-tool-layer.md` (ledger #168): tool-layer-vs-MCP model + "when is an MCP warranted" rule (MCP only when a 2nd interactive AI client — a voice agent — appears), 26-row crew/staff/admin permission matrix (settings/config + bot-admin = admin-only; staff gets money writes), confirm-yes gate + audit, cheap interpreter model, the `completeInstall` flagship (photos + material ACTUALS = new; only estimates deducted today), a risk-ordered 5-phase build, and the GHL call-transcript→action-items pipeline (event extractor, NOT MCP; needs GHL transcription confirmed). **To start:** paste the S46 kickoff prompt into a fresh session (Phase 0 zero-code Telegram → Phase 1 read+digest). **Open (Naldo):** matrix rows 5/17/18. Phase 4 (quote/price writes) touches Jason's area. **Nothing built/enabled yet.**

**▶▶ Next up (after Naldo S42, 2026-07-19) — READ THIS FIRST:** **#163 customer post-booking colour change is DONE + LIVE, both halves** (notify PR #585 + staff apply/dismiss PR #588). The first-real-payment cluster (#159–166) is essentially clear: #159/#160/#165 fixed, **#162** (staff free-items) + **#166** (the `/approved` post-deposit race, fixed by `BookingConfirmingOverlay` polling until booked) shipped by Jason's concurrent S28 (#582/#583), **#163** done this session. Only **#161 (deposit vaulting) + #164 (customer card fee) stay PARKED on Valor answers Jason must get** — both consolidated for him in `docs/jobber-flow/VALOR-AUTOCHARGE-FOR-JASON.md` (PR #589): #161's `save_card=1` hosted-page probe failed live 2026-07-17 (needs the correct vault param or the REST chain), #164 needs the exact effective Valor rate. Master `e6185d3`, gates tsc 0 / eslint 0 / vitest **3635**; all prod deploys VERIFIED READY via the Vercel API. **Still owed by Naldo (unchanged, top priority):** (1) send the Wisetack API-access request (unblocks #154's full in-portal flow); (2) build the Neighbor drip workflows in GHL, then the **#155 send wave** (114 drafts matched + linked, none sent). ⚠️ Naldo-lane rule held again: classifier outages block prod writes AND often the Bash gate/commit itself — keep paste-able PowerShell-proof terminal sequences ready. **▶ S42 POST-CLOSE ADDENDUM (2026-07-19/20) — an adversarial review of the just-merged #163 found 7 REAL defects, all now fixed + live:** **PR #597** CAS-guards both colour writes — a customer colour write racing a staff **amend** did a blind read-modify-write of `approval_snapshot` that ERASED the amendment trail entry, silently reverting the agreed total and dropping the re-consent gate (the F-014 class). **PR #598** fixes a separate **PRE-EXISTING HIGH**: `jobs/[id]/close` read `latestAmendment` instead of `latestConsentAmendment`, so a cosmetic $0 amendment (a #163 colour apply **or a #162 free-item add**) masked a pending price increase and let close settle the **un-consented** total, while mark-paid/charge-balance correctly blocked it. ⚠️ **#598 touches Jason's settlement code** (merged on Naldo's go, worth his eyes). Master later advanced to `24a6118` via the concurrent S43/S44 closes; gates there tsc 0 / eslint 0 / vitest **3687** green. **Owed next quiet cycle:** archive the finished payment-cluster rows (#162/#163/#166); and PRs **#590/#592/#593** (self-serve estimate Phase A) plus the merged **#584/#595** partial-lead capture have **NO ledger rows** (next free # **168**) — deliberately left for the owning session to number.

**▶▶ Next up (after the Naldo S40 post-close addendum, 2026-07-18) — READ THIS FIRST:** **Wisetack financing is LIVE on the portal** (interim prequal-link slice, #154): eligible quotes (holiday/permanent/bistro, total ≥ $1,500, balance $500–$25k) show the "Flexible payments" section + CTAs; kill switch = WISETACK_FINANCING_ENABLED in Vercel. **Naldo owes: send the Wisetack API-access request** (drafted in the S40 chat) — unblocks the full in-portal flow (#154 P2+). **#155 send wave still pending**: Naldo's Neighbor drip workflows in GHL, then send (contacts all matched + linked; manual YLL Neighbor toggle #572 live for stragglers). Then the items below.

**▶▶ Next up (after Naldo S41, 2026-07-18) — READ THIS FIRST:** referral + booking bug batch (#167) is DONE + LIVE (master `810e1dc`, gates tsc 0 / eslint 0 / vitest 3462 my tree, 3503 combined). Four owner-reported bugs fixed and prod-smoked: the `/refer/<code>` page now renders the referrer's lit design as the hero (was the raw no-lights photo), the trust logos as a proper marquee (was giant stacked images), and all the missing sections in Naldo's order (contact directly under the form + above Trust, then trust, reviews, install photos, steps, protection, FAQ, about); the `/approved` confirmation page has a 5th step ("We schedule your install") at position 2; the quote PDF prints "Deposit paid (date)" + "Balance due" once `deposit_paid_at` is stamped; and the staff Convert-to-job path now moves the GHL pipeline card (root cause of a stuck card on a real booking), Neighbors routing honored. David's cards were cleaned manually (Neighbors → Booked $978.75, quote relinked, dup gone). Q4 neighbor toggle was already shipped by a concurrent session (#572). **Nothing pending from this batch.** Then resume the S40 send wave below. ⚠️ **Naldo-lane rule confirmed again S41:** prod writes (GHL opportunity moves, quote-row updates) are blocked by the auto-mode classifier even with consent — hand Naldo a PowerShell-proof paste-able script for his own terminal, ending with a `verify` step he pastes back.

**▶▶ Prior (after Naldo S40, 2026-07-17):** the legacy REBOOKING machine (#155) is fully built and **SEND-READY — nothing sent**: 114 real draft quotes live ($183,969.69, invoice-net items + live 8.75% tax per Naldo's call), each linked to its GHL contact (114/114 matched, 0 ambiguous), portal shows the "Last Year's Design" rebook variant, and every send/deposit/decline/installed sync routes to the **YLL Neighbors pipeline -> Bid Sent** (never Christmas Lights; dedicated Neighbor Quote Link field so holiday drips stay silent — E2E-proven live). Neighbors board cards carry the new quote values (~$184k). Legacy quotes are HIDDEN from the operator inbox (re-enable: `EXCLUDE_LEGACY_REBOOK_FROM_INBOX`, inbox/store.ts) and badged "YLL Neighbor" on all internal surfaces. **BEFORE the send wave (Naldo):** build the Neighbor drip workflows in GHL (his copy), then send per-quote from the builder or ask for a batch-send script. Backlog: 56 paid-no-photo customers (Homes-2024 fallback photos staged locally, HEIC->jpg conversion needed), 6 off-roster payers, 5 past-due + 2 comped held. Master `2876245`+ · gates tsc 0 / eslint 0 / vitest **3462** · next free ledger # **159**.

**▶▶▶ Naldo S38 (handoff / merge-queue track) — READ FIRST.** My own track ended at master `13e5b6f` (vitest 3423 on the #553 tree). **Master then advanced to `cfda4f0`** via a concurrent session's legacy-rebook work (#558/#559/#560/#561). **Gates verified green by me at close on `cfda4f0`, the whole tree including that concurrent work: tsc 0 · eslint 0 errors (54 warnings) · vitest 3462 passed (259 files).** Two concurrent S38 conversations ran (this one + the Wisetack #154 planning session); both logged as sibling S38 entries. **S39 (cloud) + S40 (local) are also taken** by the legacy Jobber migration sessions. **Next fresh conversation = S41** (per the S37 final close, #562).
- **MERGED + LIVE + deploy-SHA-verified:** **#553** portal fallback-deposit rounds to CENTS not whole dollars (`buildApproval` used `Math.round(total*0.5)`, so a legacy row or staff approval with no frozen `currentDepositUsd` could display a deposit ~49¢ off). Display-only, the real charge was always cents-accurate. Prod verified READY at `c044abf`.
- **CLOSED (no action needed, do not reopen):** **#541** (settings-parity, stays held on the `task_09ddeb99` money gap) · **#542** (its WT-53 headline already shipped via #548; remainder is chip `task_f2cd605d`, rebuild fresh) · **#519** (Jobber CSV scaffold, superseded). All three were stacked on the CLOSED duplicate waves A to D, so none could merge to master as-is.
- **#72 ledger row DROPPED (PR #557).** The Jobber import is being rebuilt on a **different approach**: see still-open PR **#556** "Legacy Jobber quote migration: batch script (quote + analyzer-seeded design per past install)". The old CSV-to-`historical_jobs`-to-Insights plan is dead.
- **🔴 LEDGER DRIFT (#155-#158) — already flagged by the S37 final close (#562); this is the corroborating detail, not a competing claim.** The legacy Jobber sessions (S39 cloud / S40 local) merged **four PRs against task numbers with no ledger rows**: #558/#559 = `#155` (legacy-rebook portal variant + r2 live fee upsells) · #560 = `#157` (inbox exclusion) + `#158` (YLL Neighbor badge) · #561 = `#156` (route `legacy_rebook` to the Neighbors pipeline). Verified still absent at master `562989f`. **The agreed plan (#562, follow it, do not front-run it): let #556 land FIRST, then reconcile the "next free ledger #" marker and backfill the #155-158 rows.** I deliberately did not write those rows: #556 was in flight, so I would have guessed the scope and collided with its close.
- **Leads worker (#525/#530) confirmed LIVE, do not rebuild:** `leadRetry.ts` + `/api/leads/retry` + `/admin/leads`, cron `*/10 * * * *` in `vercel.json`.
- **⚠️ LEDGER ARCHIVE STILL OWED (now 3 closes deferred: S34, S37, S38).** The active ledger holds **25 rows but only ~8 are genuinely open** (#154 · #109 p2 · #85 · #87b · #122 · #88 polish · #84 · #106). **Move to `task_ledger_archive.md` byte-verbatim:** #145 · #149 · #150 · #83 · #58 · #41 · #96 · #116 · #118 · #121 · #19 · #21 · #23 · #24. **Also correct 4 rows that claim open but are DONE:** **#151** (row says "#482 awaits Jason"; S37's addendum merged it and declares #151 fully done, so archive it) · **#87(a)** (says "#543 pending merge-go"; merged, keep the row for part (b)) · **#114** (says "Naldo verify prod"; S34 confirmed code + prod, so archive it) · **#110** (reads complete: audit 8/8, W1-W6 live, W7's 9 HIGHs fixed, residual cleared S26. **Ask Naldo to confirm before closing.**). S38 did not run this pass: the Bash tool was mid safety-classifier outage for most of the wrap (the S25 rule requires memory-doc surgery go through a runnable script, not hand edits), and the ledger was being actively written by concurrent sessions, so a 14-row move would likely have collided with their closes. **Do it once #556 lands and the #155-158 backfill is done, in the same pass: that is the true quiet cycle.**
- **Tooling win worth reusing:** the **Vercel MCP is connected to Naldo's account** (team `Naldoven Seizeme's projects`, project `yll-quote-tool` = `prj_o70qX9Q5Qf0tiWA2x2HtCIXIOoSi`). `list_deployments` verifies a deploy's commit SHA + READY state directly, so "deploy verify owed to Naldo's dashboard" no longer needs his login.
- **Still Naldo-gated / owed:** **#85** Glow365 recurring (permanent live, clock ticking) · **#109 p2** HELD (bias fold-in needs the per-metric-observation redesign) · `task_09ddeb99` (portal charge wiring, MONEY) · `task_f2cd605d` (commercial surfacing + cosmetic portal copy) · #87(b) texts · #83 Valor auto-charge (Jason) · **Supabase MCP is UP** (PAT-stdio since Naldo S43; Jason S28 used it heavily for read-only prod verifies — stays PAT-stdio, do NOT revert to OAuth).

**▶▶ [RESOLVED by S37 — historical, no action] Naldo S36 CORE-journey audit + 6-wave fix campaign (the reconciliation this entry demanded is DONE: S35's set was kept, S36-unique was salvaged into #548, the duplicate waves #531/#533/#536/#537 were closed, and #541/#542 were closed in S38). Kept for the lessons + finding IDs only:** ran the **core-journey** 3-persona audit (report #520) + a **consolidated dedup plan** of both audits (#526), then a **6-wave fix campaign A-M** (PRs **#531** money/security · **#533** dashboard/inbox · **#536** portal/admin/builder · **#537** back-office · **#541** settings-parity · **#542** commercial Phase 1), all gated green (final vitest **3359**), all **AWAITING Naldo merge-go**, STACKED A→B→C→D→S→M (merge bottom-up). **⚠️⚠️ THIS DUPLICATES the S35 whole-tool campaign (#524/#527/#529/#534/#538):** the WT findings were fixed by BOTH sessions, so **DO NOT merge both sets** (conflicts + double-apply). **S37 FIRST TASK = reconcile:** for each finding, keep ONE fix. UNIQUE to S36 (not in S35's set, so must come from here): all **PS-\*** core-journey findings; the **settings-parity WT-63(partial, per-item only — 6 global fee/tax/min fields deferred to task_09ddeb99)/56/65** (S35 HELD these); **commercial Phase 1 WT-52/53/12/40** (S35 deferred); **PS-F4** (send-gate bug found via a live create walkthrough). My PRs bundle PS+WT together, so the reconciliation likely means: rebuild the PS-only / held-only slices, or pick S36's superset per cluster and CLOSE the overlapping S35 PRs. Recommend Naldo decide which campaign is canonical, then one session lands ONE coherent set. **2 money reviews inside S36** caught + fixed a WT-31 on-hand over-credit, an unfixed event-engine WT-01, and a WT-63 mis-charge trap (panel trimmed to per-item rates). **Follow-ups:** task_09ddeb99 (WT-63 portal-charge wiring, MONEY) · task_bbc6490a (inbox activity actorName UUID). Env note: a mid-session OneDrive/memory event WIPED node_modules (recovered via `npm ci`). Next free ledger # unchanged (S36 opened no new ledger rows — audit+fix only).

**▶▶ Naldo S37 — S35/S36 audit campaigns RECONCILED + MERGED, then a live backlog blitz — READ FIRST (master `ad7e3b4`, gates tsc0/eslint0/vitest 3422):** the two duplicate 2026-07-12 audit-fix campaigns are resolved. Kept the review-hardened **S35 whole-tool** set, salvaged **S36-unique** into **#548** (PS-C1/WT-L1 portal-$0 **money ship-blocker** + PS core-journey inbox/referral/portal-action-bar/admin/builder + WT-53 rebook **live bug**), dropped the WT-dups. **MERGED (Naldo's go, in order):** S35 #524/#527/#529/#534/#538 → #548 → #543 PDF ≤2¢ fix → docs #518/#520/#526. Co-merge proven on a throwaway preview first (3396), stale stacked branches (#534/#538/#548) re-synced + re-gated at merge, final master **`35ff140`** green (3399 = 3396 + 3 from #543). **Closed superseded:** #531/#533/#536/#537 (branches kept as reference). **HELD #541** (ships unmade business decisions — holiday rates/warranty editable, bistro maintenance — + a money-safety gap: Settings edits rates the portal still charges hardcoded → `task_09ddeb99`). **DEFERRED** commercial surfacing WT-52/12/40 + cosmetic portal copy PS-B2/B3/B4/WT-10/L2 → chip `task_f2cd605d` (rebuild fresh on merged base; #542 kept as reference, do-not-merge-as-is). **▶ POST-CLOSE BLITZ (same conversation, all SHIPPED + LIVE + deploy-verified):** prod deploy of the reconcile SHA-verified live (`35ff140`, Vercel Ready) · **#482 PostHog Wave 4** merged (all PostHog waves now live → #151 DONE) · **#550 inbox activity-log actor names** (`getOperatorLabels` reuse, `task_bbc6490a` DONE) · **#551 customer PDFs group wrapped mini-light items** into one "Bushes ×N" line (PDFs-only, money-safe, +4 tests) · **device round DONE** (drove Chrome, rendered all 3 #87a PDFs inline on booked quote #1050 — Jobber format + cent-exact reconcile + PAID watermark confirmed live). **STILL OPEN / OWED:** #519 Jobber CSV dump (your export) · #109 p2 HELD (bias fold-in redesign) · **#85 Glow365 recurring** (Naldo-gated) · **WT-63/#541 + `task_09ddeb99` DEFERRED** (Naldo's call, money-engine, not worth rushing) · `task_f2cd605d` (commercial surfacing + cosmetic portal copy) · #87(b) customer texts/notifications · #83 Valor auto-charge (Jason+live confirm) · buildable-now: **#149** (analyze JSON retry), **#145** (power-injection wire SKUs). **⚠️ Jason:** the merged set touches heavy Jason-area files (portal/pricing/inventory/GHL/auth/analyzer) — Naldo's directed campaign, PR bodies carry detail. **Ledger archive owed:** stale ✅ rows (#151/#150/#116/#118/#114/#21/#23/#24/#19) to move → `task_ledger_archive.md` next quiet cycle. **▶ S37 CLOSED after an independent 3-agent review (money / reconcile-completeness / integration): ALL CLEAN** — reconcile verified every finding fixed exactly once; gates green; no money bug in the #551 grouping or #548 portal-$0 fallback. Its one find was a PRE-EXISTING quirk, now fixed + merged: **#553** — the fallback approval deposit rounded to WHOLE dollars (`Math.round(total*0.5)`), now `roundMoney` round-to-cents (+1 test); display-only, normal customer-approved quotes unaffected. All 5 prod deploys SHA-verified **via the Vercel API** (`list_deployments` — authoritative; prefer it over the dashboard). **⚠️ LEDGER NUMBER DRIFT:** the marker still says "Next free task #: 155" but merged legacy-rebook code consumes **#155/#156/#157/#158** with no rows for them → next truly-free is **159**. Deliberately NOT edited here: PR **#556** (legacy Jobber migration = ledger **#72**) is IN FLIGHT and touches `task_ledger.md` — let it land, then reconcile the marker + backfill #155–158. **S38/S39/S40 are ALL taken** — S38 Wisetack #154; S39 (cloud) + S40 (local) ran the legacy Jobber migration per in-flight PR **#556**, which reports **114 legacy rebook drafts LIVE in prod (none sent)** and queues the send-side buildout (#156–158) → next fresh conversation = **S41**.

**▶▶ Naldo S35 whole-tool PERSONA-SWEEP fix campaign — READ FIRST:** ran a whole-tool 4-persona audit (report MERGED, PR #522: `docs/audit/PERSONA-SWEEP-WHOLETOOL-2026-07-12.md`, 65 findings) and fixed **50 of 65 across 5 gated PRs, ALL AWAITING Naldo merge-go**: **#524** Wave 1 money/security · **#527** Wave 2 owner/GHL · **#529** Wave 3 analyzer/training · **#534** Wave 4 inventory/jobs (STACKED on #524) · **#538** Wave 5a parity/cleanup (STACKED on #524). **MERGE #524 FIRST** (Waves 4+5a stacked on it → auto-clean once it lands); #527/#529 independent; #524/#527 re-synced onto current master. **DEFERRED (ledger #153):** WT-63 holiday-rates→Settings (money-engine change, needs a frozen per-quote rate snapshot like permanent) · portal subset WT-L1/L2/L3/L4/10/11/12/54 (HELD for the parallel core-journey session's consolidated plan **#526**; WT-L1=PS-C1 is the one true dup, fix once in `adapter.ts`) · multi-property epic (WT-03/52/53) · WT-08 · WT-55. **COORDINATION:** posted a done-list handshake on #526 so the 50 shipped items are excluded from its waves. **NEXT:** merge the 5 PRs (#524 first) → reconcile the portal subset with the core session via #526.

**▶▶ Naldo S34 backlog batch — READ FIRST (master `0538218`, gates tsc0/eslint0/vitest 3243):** shipped PostHog Wave 2 verified (#481); **#116 re-send** LIVE (#506); **#109 phase 1** LIVE (#508); **#21/#19/#118** closed (#516, #118 audit found NO live bug); ledger cleanup (#514); **#87(a) customer PDFs LIVE (#528)** — quote/invoice/receipt as generated `@react-pdf/renderer` files **matching the company's Jobber invoice format** (Naldo supplied a real Jobber PDF), two adversarial reviews + rendered-and-compared. **NOTE: session was mislabeled "S32" mid-work then corrected to S34** (bistro=S32, lead-capture=S33 taken concurrently); **next session = S35**. **NEXT / owed:** (1) **device round** — download the 3 #87a PDFs on a real approved quote to eyeball; (2) **#72 Jobber import** — Naldo provides the FULL Jobber data dump → fill the column map (PR #519 scaffold ready, dry-run first) + settle 6 go-live decisions; (3) **#109 phase 2 HELD** — the analyzer bias fold-in needs a per-metric-observation redesign before revival (findings in `docs/superpowers/specs/2026-07-12-109-phase2-ground-truth.md`); (4) **#482 Wave 4** awaits Jason review → Naldo merge-go; (5) still Naldo-gated: **#85 Glow365 recurring** (permanent live, clock ticking), #87 part (b) texts. **⚠️ Jason:** #87a added `@react-pdf/renderer` (SHARED package.json) + additive `PortalQuote.customer.phone` mapping (portal area); merged with his FYI. Jason ran a concurrent whole-tool audit-fix campaign (PRs #524–#536) — master churned fast. Ledger archiving deferred this close (concurrent churn). Next free ledger # **153**.

**▶▶ WEBSITE LEAD-CAPTURE FORMS LIVE (Naldo S33, #152) — READ FIRST:** the WP site's 3 old GHL-plugin Gravity quote forms are replaced sitewide by a custom embed (`public/lead-form.js`) posting to `POST /api/leads` → `website_leads` (Supabase, RLS deny-all) → per-service GHL pipeline (christmas→Christmas · permanent→Permanent · event-wedding→Event · landscape→NEW Landscape pipeline) with the Service custom field + note + **household/spousal** name-preservation. MERGED+LIVE: **#486** (API + vanilla-JS embed, 3 variants) · **#494** (auth-gate carve-out + a 4-agent adversarial review's 14 fixes incl. 2 HIGHs: `isTest` rate-limit bypass firing GHL; the GHL Service field is a CHECKBOX so the scalar write never landed) · **#497** (household) · **#498/#501/#504/#513** (desktop layout+photos, owner's landscape card photo, pill picker, sticky→top). WordPress cutover done + **115-page swept clean** (caught the referral-page hero the homepage-only recon missed); old Gravity Forms kept installed = instant rollback (mapped in **#510**). Mobile header fixed (Ireland placeholders → 631-517-0186 · "Long Island, NY" → GBP link; elementor-hf template **39**). **▶ OPEN, pending Naldo merge-go — PR #517:** final embed UX polish — centered bar (dup heading removed) · bigger centered full heading · **sticky** dark `#1B1B1B`, ~50% bigger, **NO dismiss X**, mobile = Call `631-517-0186` + "Get a Quote" buttons (no fields → no consent dead-end), desktop = **2×2 service grid** panel. Iterated live via a **claude.ai Artifact preview** (WP preview page shows the OLD version — the site-wide prod-script `__yllLeadFormLoaded` guard wins). **NEXT (S34): (1)** Naldo eyeballs the #517 preview → **merge** → deploy-verify → refresh live (embed auto-updates, no WP edit); **(2) build a retry worker + admin visibility for `website_leads` rows stuck `pending`/`deferred`** (GHL-outage recovery — the one accepted launch gap, NOTHING exists yet); **(3)** confirm the 3 Vercel prod env vars (`HIGHLEVEL_PIPELINE_ID_LANDSCAPE` / `_STAGE_LANDSCAPE_ENTRY` / `_CONTACT_FIELD_SERVICE`); **(4)** `docs/website-lead-forms.md` sticky section is STALE (says slides-from-bottom); **(5)** bistro option on the website form (S32 backlog). Cleanup: none outstanding (all test contacts + rows deleted; temp WP preview page 4045 reverted to private). **⚠️ Jason:** lead work touches `src/lib/integrations/highlevel.ts` + `ghlPipelineMap.ts` (his area) — merged on Naldo's go w/ his review flag; existing-caller behavior unchanged. Gates at close: tsc 0 / eslint 0 / vitest **3171** · master `4c77177` · next free ledger # **153**. **▶ S44 UPDATE — the site is now SINGLE-PATH.** A legacy Elementor sticky container (`a0fa6d1` / `#stick_form_section`, in UAE template **"Sticky Form" post 2736**, pulled site-wide by the Footer template 1123) was still rendering a **second** sticky bar above ours, containing a **GoHighLevel HOSTED form iframe** (`lights.yulelovelights.com/widget/form/CvW7WYzgx40GpMsEEstk`) that captured leads **straight into GHL, bypassing `/api/leads` → `website_leads`** and the PostHog funnel. S34's Customizer `display:none` hide was still in the CSS but was NOT holding. **DELETED it** (live, Elementor-revision-revertible); our `data-yll-lead-form="sticky"` marker was nested inside it, so it was re-created byte-identical in a plain container first. Verified logged out at 1280 + 375: zero GHL iframes, one fixed bar (ours), site-wide. **Homepage forms = our embed only** (`bar` / `full` / `sticky`); `/get-a-quote` 301s to the homepage. **Follow-ups:** the dead `.elementor-element-a0fa6d1` Customizer rule is safe to delete; reconcile GHL submissions to that form against `website_leads` rows.

**▶▶ PERMANENT BISTRO #117 SHIPPED + LIVE (Naldo S32) — READ FIRST:** the 4th service type (`permanent_bistro`) is fully on prod (bistro merges took master `df20ec2`→`adb7010`; other sessions → `b1a0b7d`), built from scratch this session across **12 PRs** + a prod CHECK-constraint migration. What's live: pricing ($30/ft + $100/pole, Settings-editable, frozen snapshot); builder w/ **satellite-measured footage** (draw freeform runs on the Satellite tab at true ft/px, round-up-5; Design tab is VISUAL-ONLY, projection exempted server-side; full #142 reopen-freeze); single-tile portal (bistro copy/gallery/FAQ, Permanent-first cross-sell); approve/book path; **GHL = Landscape Lighting pipeline + its own Bistro Quote Link contact field** (live send stamps `contact.bistro_quote_link`, verified); **BOM engine + supplier-grouped order sheet + Inventory Bistro tab** (Thunder lines auto-pool into the supplier PO, Home Depot + Amazon manual); dashboard Bistro card; job label "Bistro". Two adversarial reviews caught + fixed **2 real HIGHs pre-merge** (the recommendation-clobber; the design-PUT-validator missing the `bistro` channel → persistence silently broke — the builder's own green tests missed it). **NEXT / bistro backlog (suggestions surfaced this session):** set the real job **minimum** (Settings, defaults $0); add the Landscape **Bid-Sent drip** merging `{{contact.bistro_quote_link}}`; a **yardstick seed/nudge** so a scale-less satellite draw can't under-measure; bistro on the **website lead form**; cost-check the Thunder **Bistro Hardware Kit (80001)** vs individual pieces; warranty-copy Settings parity; reshoot the 2 backyard gallery tiles. **Accepted-as-parity (with permanent, not fixed):** deleting ALL satellite runs then recalc leaves stale portal geometry (money correct, visual-only). **Cleanup:** test quotes #1044/#1047/#1048 (`is_test`) + their GHL cards / the Bistro/Perm link values on Naldo's own contact. **⚠️ Jason:** all bistro code is his area (pricing/portal/builder/editor-core/inventory/jobs + SHARED `serviceType.ts`/`quoteForm.ts`/`projectScene.ts`/`adapter.ts`), merged on Naldo's owner-go; he gets it on pull. Gates at close: tsc 0 / eslint 0 / vitest **3171** (bistro tree; master then moved w/ other sessions).

**▶▶ REFERRAL SYSTEM #41 LIVE (Naldo S30) — READ FIRST:** the full referral program is on prod (master `4483868`): personal `/refer/<code>` link + landing page + staff "Referred by" picker + $125-per-booked-friend credit (stackable, **2-yr expiry**) + friend gets 2 free 16" spritzers + one-click Apply/Remove redemption + customer-profile panel. Both migrations applied; GHL Option-B wired (our link in the `referral_link` field `ZWKHfFRuFUraltOmE8dq`, delivered by the install-complete email; Naldo did the GHL side). A 5-lens Opus adversarial review found + fixed 2 HIGH money bugs (Apply-then-no-Calculate silent loss; rebook re-discount), both live-verified. **⚠️ 3 approved enhancement branches BUILT + pushed to origin, NOT integrated:** `naldo/41-expiry-notify` (2yr expiry + referrer-earned SMS/email), `naldo/41-growth` (Share + QR + dashboard Referrals tile), `naldo/41-auto-analyze` (dormant-flag instant house preview). All branched off pre-#493 master, so **NEXT SESSION FIRST TASK: integrate them onto clean master** — reconcile the duplicate `listReferralsFor`/`CustomerReferralPanel` (now on master from #493), wire expiry's `isReferralSpendable` into the merged `consumeCredits`/`listReferralsFor`, resolve growth-tile-vs-Jason's-bistro-card, add the `qrcode` dep (SHARED lockfile), apply the expiry migration → 3 PRs, Naldo merge-go each. Money-review the expiry integration. Also open: rotate the Supabase access token; decide the Supabase-MCP `--read-only` flag flip. Other Naldo-gated: #85 Glow365 recurring (permanent live, clock ticking), #72 Jobber CSV import. #117 bistro + #121/#122/#114/#150/#23/#24/#145 all now DONE/LIVE/closed. Gates at close: tsc 0 / eslint 0 / vitest **2954** · next free ledger # **152**.

**PostHog analytics (Naldo S31, #151):** v1+Wave1 LIVE on prod + yulelovelights.com (project 506466; dashboards: Website Metrics 1831708 / Quote Tool 1831713; 2 surveys live). **Jason: review #481 (Wave 2) + #482 (Wave 4)** -> Naldo merge-go -> prod verify on a fresh is_test seed. Both PRs add the same optional SelectionContext.serviceType (second merge = trivial conflict). Website optimization waits ~2 weeks for data; weekly ritual = dashboards + 5 form-abandon replays + 5 portal-abandon replays.
> **Per-session shipped-work history (the "Done in Session N" / "Naldo SN" narrative) moved to [[project_quote_tool_archive.md]]** — it duplicated `session_log.md` / `session_log_naldo.md`. Only the current forward-looking items stay below.

**▶▶ Next up (after Jason S28) — READ THIS FIRST:** S28 reconciled the weekend merge-marathon and shipped 4 arcs: **#580 codex audit ACCEPTED** after seat money-review (one flag → **#586** signature cap 200KB→1KB, merged+deployed — the CAS-by-serialized-snapshot URL writes made a big drawn signature a quote-bricker) + Jason live-proved the amend-consent CAS on prod · **#163 FULLY DONE across a duplicate-build collision** (my #606 closed unmerged per the S37 playbook; Naldo’s merged #588/#597/#598 kept canonical; **#607 salvaged the unique delta**: notify-on-apply [is_test-suppressed] + the portal now OPENS on the frozen colour — resolveInitialColorState) · **#161 vtToken trap SET (#609)**: the docs sweep proved NO hosted-page vault param exists, but Valor documents the TRANSACTION webhook token as `vtToken` — a name our parser never read (the #159 class; S27’s “no token” may have been a false negative) — pick-list + hasVaultToken log + key-dump shipped; **the next real deposit (or a $1 pay-then-void) answers card-on-file in the Vercel logs**, Valor support only still owed the MIT-consent letter · **#155 BATCH 2 MIGRATED LIVE: 19 skipped no-photo customers** (+$28,001) from Homes-2024 Drive photos + Naldo’s manifest.csv — pool now **131 send-ready legacy drafts / $207,278**. **⚠️ S29 FIRST: the 19 have NO GHL contact linked (0/19) — run the S40-style match pass before any send.** Then: the $1 Valor probe · 20 no-photo ($35.5k) + 16 flagged batch-2 candidates need dispositions (ask Naldo re the 2 comped names) · #164 still needs the merchant-agreement rate · #582 free-items prod round nominally owed · #609 deploy READY-verify owed. Naldo’s review-gates policy #601 post-dates my PRs — adopt the four-lens pass from S29. Master `cb05106`+ · my last full gates tsc 0 / eslint 0 / vitest **3712** · next free ledger # **168**.

**▶▶ Next up (after Jason S27) — READ THIS FIRST:** S27 = YLL's **FIRST REAL CUSTOMER PAYMENT**, which exposed a money-path cluster — all root-caused via Vercel logs + fixed LIVE: **#159** deposits now AUTO-BOOK (webhook reads Valor's `data.invoice_no`; PR #568) · **#165** deposit CENTS bug fixed (Valor sends cents, /100 at parse; PR #574 — corrects stored amount + balance + all automated SMS/emails) · **#160** job BOM respects the approved selection (PR #571). **PARKED/BLOCKED:** **#161** card-on-file vault — the Valor hosted page does NOT vault via `save_card=1` (probed live, reverted PR #575); needs a Valor answer (correct hosted-page vault param OR the vault REST API) — this also blocks #83 balance auto-charge. **#164** pass the card fee to the customer — parked (eat it for now; needs the exact Valor rate; will be Settings-editable + freeze-per-quote). **OPEN follow-up:** **#166** the /approved page shows the pre-booked variant for a beat (async-webhook timing race; a refresh resolves it — small "confirming payment..." state fixes it). Session-open also: #149 analyze-retry DONE (PR #478); #150 scrapped; #19/#145 verified-done; #109 shelved (seasonal). **Reviewed Naldo's audit PRs:** pushed 3 fixes to #524 (WT-17/WT-31/WT-61) + posted #527 verdict — both await Naldo's merge. Master `405f491` · gates tsc 0 / eslint 0 / vitest **3481** · next free ledger # **167**.

**▶▶ Next up (after Jason S26) — READ THIS FIRST:** S26 was a backlog blitz (Jason fed 9 items; all shipped or dispositioned): **#146 GHL pipeline-sync ARC LIVE end-to-end** (per-service-type card moves on send/decline/deposit/installed + per-type Quote Link contact fields feeding the drip automations + per-type ✨ quote-ready messages + decline blanks the link; Jason hand-edited the 3 Bid Sent drips with the merge tags; live-verified on all 3 pipelines; ⚠️ SHARED `quotes.ts` gained an additive updateQuote guard — **Naldo FYI**) · **#147 customer color-picker FIXED** (2-layer anonymous-401; every customer affected since auth-gate go-live; prod-probed 200) · **#148 wave-1 UI batch** (event pole card · full-width quotes table · gold selected borders). Naldo's #474 (wire SKU confirm, closes the S25 provisional flag) + #475 landed concurrently. **OPEN (Jason lane): #149** analyze transient-JSON retry (S, backlog). **⏸️ #150 event/highlighting analyzer PARKED, likely scrap — waits on Jason's Naldo confirm** (if revived: roofline = one bucket, no rigid rules). Still parked: inventory ON-HAND counts (P8) · #141 v2 (corpus). Naldo-gated: #117/#41/#85/#72/#121/#122/#114. Master `4101f6c` · gates tsc 0 / eslint 0 / vitest **2566** · next free ledger # **151**.

**▶▶ Next up (after Jason S25) — READ THIS FIRST:** S25 finished the permanent-analyzer arc and opened the inventory lane: **#140 stack MERGED + LIVE** (after 3 accuracy fixes: default-yardstick seed, deterministic orientation from the Street View pano bearing, and the L/R convention flip — satellite left/right = the HOMEOWNER'S, standing at the house facing the street, SUPERSEDES the S24 road-viewer wording) · **#141 permanent TRAINING LOOP v1 LIVE** (`permanent_training_examples` table + pgvector RPC applied via **psql** — Jason's machine now applies prod DDL directly, see `project_psql_ddl_migrations.md`; every permanent SEND banks the corrected traces; Voyage key refreshed local+Vercel, similarity live for both loops; v2 corrections/bias PARKED for corpus) · **#142 satellite rehydrate on reopen LIVE** (derive-freeze protects saved numbers; SHARED `designs.ts` gained `satelliteFeetPerPixel` — Naldo FYI) · **#144 BOM reconciled against the real estimator** (wire line w/ PROVISIONAL SKU — **Naldo: confirm the Ascend wire item/price**; controller-feed extensions; per-side strip packing; injectors +1 spare netted vs stock at PO time; 6% waste on lights too; 85% transformer derate validated) · **#143 spinner fixed** (dev-only strict-mode) · #125/#111/#112 verified ALREADY-DONE (ledger drift) · #14/#29 SHELVED (Option B editor styling = FINAL, don't re-raise). **⏸️ PARKED on Jason's word: inventory ON-HAND counts + everything touching them** (`inventory_on_hand` = 0 rows tool-wide; catalog holds all 37 APL SKUs; machinery ready, waits on a warehouse count). **NEXT BUILD (Jason's lane): the event/highlighting analyzer** (wreaths/garland/roofline/spritzers — taxonomy locked S28; run a #140-style domain Q&A BEFORE the plan). Master `296942c` · gates tsc 0 / eslint 0 / vitest 2477 · next free ledger # **145**.

**🟢 WHOLE-TOOL AUDIT FIXES (Naldo S29) — PR #439 MERGED (master `6d24816`, pre-S24).** Residuals that still need a human: the 4 browser E2E checks from the PR body (event double-bill · amend end-to-end · reply double-submit · live-analyze) + reviewer flags (`recommendRoofline` sibling re-price unwired · inventory over-decrement edge · amend clamp-to-0 edge). Original scope: A 39-agent adversarial audit (report `docs/audit/FULL-AUDIT-2026-07-06.md`) found 23 confirmed bugs (1 CRIT/4 HIGH/10 MED/8 LOW); `naldo/fix-full-audit` (28 commits) fixes all of them + suggestions + dead-code, each Opus-reviewed + combined-tree gated (tsc0/eslint0/**vitest 2382**). Headliners: CRITICAL **event double-bill** (both mutually-exclusive rooflines bundled → front billed twice into the snapshot+deposit) · HIGH invoice tax-override CAS · homeworks resend agreed-total + atomic claim · approve hide-early-install server-enforce · approved-portal shows the SIGNED selection (+ checkout-off cancelled-as-booked). Plus inventory `requireOperator`×14 + atomic on-hand (both halves) · amend-reprice carve-out + client-wire + multi-amend math · +15 more · 5 dead dark components deleted. **Reply-guard migration APPLIED to prod** (`inbox_items.reply_claimed_at`; route fails open till present). **Almost ALL Jason-area** → his review, then Naldo's go; the AI did not self-merge. **STILL NEEDS a human:** browser E2E on the deployed code (event double-bill · amend end-to-end · reply double-submit · live-analyze) — no app creds in the branch. Reviewer flags in the PR body: `recommendRoofline` sibling re-price unwired · inventory over-decrement edge · amend clamp-to-0 edge. **Note:** the positive-gate-seams fix overlaps the open **#119** event add-on decision.

**🟢 PERMANENT LIGHTING #88 (Naldo) — LIVE (S23 go-live), customer-facing on prod (master `d679b58`).** **✅ S28 (Naldo) → S22 (Jason) — permanent BUILDER/DESIGN bug-batch #436 MERGED (master `9235c74`), the permanent AI analyzer REMOVED per Jason (see `docs/FOR-JASON-permanent-2026-07-07.md`):** walled the permanent design off from holiday (permanent-only editor palette; no holiday analyzer/seed; ALL ridge verbiage + the satellite holiday-measurement UI + the "From your design" summary gated off permanent) · all-4-sides "Refresh from design" w/ per-side `sideSource` provenance (deleted run clears its side, satellite-typed side preserved) · 8-inch-only spacing + a defaults clamp · beam max 12→20 + a per-strand beam toggle (`showBeam`) · the permanent AI analyzer (roofline front/left/right/back + gaps, no ridges/peaks) DRAFTED then WIRED to auto-fill the Permanent section (front from Street View, sides/back from satellite). 2 adversarial reviews → all findings fixed (a stale-side over-bill, a blob-URL design-never-created blocker, a gap double-count, a silent-failure banner). Gates: tsc0/eslint0/vitest2335. **S22 OUTCOME (Jason):** REMOVED the permanent AI analyzer (clean 3-commit revert, −904 lines; `photoAnalysis.ts` + tests + the analyze-route wiring + `applyPermanentAnalysis`) — **KEPT** the imagery-load (permanent still pulls Street View + satellite for a MANUAL draw → Refresh from design) + projection/spacing/beam/walling-off. Fixed 2 device-check bugs: hand-typed footage now survives Refresh (`applyPermanentProjection.mergeSide` checks `'manual'` before `ft>0`) + a **no-run warning** on the 4 side footage fields (bills footage w/ no matching design run). **#436 MERGED → master `9235c74`.** **Permanent design model CONFIRMED as-is (Jason): front is drawn on the street photo; left/right/back are typed from satellite → they BILL as line items even with no run** (the breakdown line items come from the footage NUMBERS, not the design; the portal is a front photo so sides/back aren't visually shown) — **visual all-4-sides = a later rethink, model (a) kept for now.** **STILL UNBUILT: the event/highlighting analyzer** (wreaths/garland/roofline/spritzers — a SEPARATE analyzer from the removed permanent one; taxonomy locked). The full P1–P6 sales path + P2 BOM engine are live. **S23:** removed the inert `permanentEnabled` flag (#377 — it had ZERO consumers; the real customer-facing gate is the quote's `service_type`), then hardened via a scoped review + a **7-dim adversarial hunt → 8 bugs fixed & LIVE (#378)** — incl. 2 money (HIGH invoice partial-selection tax under-bill [ALSO fixes holiday partial selections] · rebook stale rate snapshot) + the entire holiday post-approval confirmation page → permanent variant. **P7 merged** (BOM panel + print route on master). **P6b-2/3/4 SHIPPED LIVE (S24, PR #387 → master `0567a6c`):** the "Your Protection" warranty card Settings-editable + version-frozen · permanent scenes ANIMATE (portal-side rAF stepper via the render's `setColorOverride` — editor-core untouched, no design-tool relay) + a CSS **facade glow** (`.portal-perm-glow`) · **FULL** color picker (reuse the holiday `LightColorPicker` + build-your-own = any color, "As Designed" default) + a SEPARATE **effect picker** (Solid/Chase/Fade, held in `SelectionContext.permanentEffect`, freezes) + Settings-editable **`permanentSwatches`** colors (own app_settings key; editor = the holiday `PortalSwatchEditor` parameterized w/ a `settingsKey` prop, on Settings→Quotes "Permanent light colors"). Approve permanent-color handling unified w/ holiday. **NEXT (next session):** Naldo has feedback on the **effects + facade-glow gradient/colors + how the colors work** → tune `.portal-perm-glow` (in `portal-snowglobe.css`) + `sceneColorsAt`/effect speeds (`permanentScenes.ts`) + the permanent swatch defaults; then twinkle effect · Settings-editable effect list · P8 (#82 inventory, via P7's `costOverrides` hook) · merge #332 (plan doc) once Jason reviews. **Confirmed (don't re-ask):** permanent picker = FULL color + build-your-own (not a fixed preset set) · effect = SEPARATE pick (Solid/Chase/Fade, default Chase; a solid color stays still) · permanent colors = own Settings-editable `permanentSwatches` list · color+effect both freeze at approval · portal render = stylized dots + glow wash (NOT photoreal, Naldo signed off) · twinkle deferred (needs per-bulb on/off) · $40/$35 flat $/ft (Settings-adjustable) · materials never touch the customer price (BOM cost/margin only) · $2,500 = approval gate not a floor · packages A/B/C/D by side · transformer sizing binds on PUCKS (350=300/600=510, one hub/system) · warranty = lifetime MATERIALS only (labor billed, non-transferable). Data + formulas: `docs/permanent/BOM-DATA.md`. **Seeded test quote** `1fc0eec0-23f5-4101-8287-c4b464dd2be0` (`is_test`) sits in prod for Naldo's live-portal eyeball — delete after looking.

**🟢 EVENT LIGHTING #96 (Naldo) — LIVE + prod-E2E-VERIFIED (S23 go-live, S25 verification).** The full event vertical (`service_type='event'`) is **LIVE on prod, always-on** (S23: PR #344 merged → #379 deleted the `eventEnabled` flag; the customer-facing gate is the quote's `service_type` column, no toggle). Mirrors permanent's pattern (reuse the holiday machine · pure `lib/event/*` modules · thin `service_type` seams · **ONE adaptive portal branched on type**). Pricing: `calculateEventQuote` — roofline **7/8/9** ($8/ft medium vs holiday $10) · mini 35/45 · spritzer 65/75/85 · bistro $12/ft · $150 support · accessories excluded · #104 overrides · $0-guardrail · `eventRatesSnapshot`. Portal: event package · **approve force-strips holiday fees + server-recomputes** (money-safe) · 3-date "Your Event Schedule" · soft suggestions · re-voiced copy/FAQ. **S25 in-browser prod E2E CLEARED the council's #1 risk** — seeded a real event quote (PostgREST + service key), drove portal→approve on prod: rates/copy/schedule correct; approve force-off proven even when the customer toggled rush+takedown ON and signed $2,544.75 (booked $2,218.50, `serverRecomputed`). **Open (fix-forward):** **#118 confirmation-page holiday-variant = FIXED by PR #384** (verify live); **#119** event portal still *offers* a Rush/Early-takedown add-on that the approve route strips → **design decision** (hide for event vs let it carry the fee); cosmetic "CUSTOM" eyebrow + "Your Your event lighting" heading. **Confirmed (don't re-ask):** event = always-live, no toggle · independent rates · 3 staff-entered dates on portal · keep the skin, rewrite copy · **permanent bistro = separate future section (#117, deferred)**. Test quotes in prod for eyeball: `2a372960` (event, sent) + `40e5c357` (event, booked), both `is_test`.

**▶ Next up (after Jason S24):** S24 shipped the **#131–#139 permanent/portal batch LIVE** (sides split into Left/Right line items + Tier 2 "Front & Sides" + below-minimum tiles hidden [3 adversarial-review fixes] + per-side Recommended checkboxes + settings new-quotes-only disclaimer + WW label/card fixes + round-up-to-5 footage — 13 PRs, master `3f5f536`, prod-verified) **and BUILT the #140 permanent AI analyzer epic — ⏳ NOT MERGED: 4 stacked PRs #457 (BOM sizes/overrides) → #458 (Extensions/Splitters card + geometry derive) → #459 (satellite auto-trace) → #460 (street runs + jump/splitter detection), live-tested end-to-end on a real address.** **S25 FIRST TASK (Jason's explicit ask): produce step-by-step verification instructions WITH LINKS for the #140 stack, then Jason device-checks trace accuracy on real houses, then merge bottom-up** (re-sync + re-gate each; ledger #140 row has the full detail + locked domain rules — extension sizes 3/5/10/25, the sizing ladder, splitter = level-branch, card = editable counts that win). Follow-up candidate: rehydrate satellite lines/preview on quote reopen. Ledger #141 free. Other open Jason-lane: event/highlighting analyzer (unbuilt) · #29 · #14 · #109.

**▶ Next up (after Jason S23):** S23 was a **permanent-lights refinement epic** — 7 PRs, all browser-verified + merged. **Shipped LIVE:** white builder cards (#441); permanent **auto-yardstick 10 ft** on house load (#442); permanent **measures its 4 sides by DRAWING on the Satellite tab** (footage + corners from the draw; **"Refresh from design" removed** — design projection was inaccurate) with the traces persisted + shown on the portal side-by-side like holiday (#443); permanent roof-light runs **twin across photos** → portal package toggles show/hide a side's lights on **every angle** (#444, Jason device-verified); 3 portal fixes (#445) — strip footage/$/ft from customer labels, restore the legacy-quote satellite (a #443 regression), and **THE ROOT-CAUSE FIX**: permanent never persisted the satellite **image** (`satellite_path` null) so the portal always hid the satellite → now parked like holiday (verified end-to-end: pull → draw → Calculate → portal shows it). **#56 fully closed** (Jason device-verified finger-draw + pinch). Deleted 2 approved TEST-Walkthrough prod quotes (#1014/#1016). **State:** master **`9ca049b`** (Naldo → `6d24816`) · tsc 0 · eslint 0 · vitest 2399. **NEXT (S24 — Jason's stated direction: keep refining PERMANENT lights):** more portal/builder polish from Jason's device rounds; **note existing pre-#443 permanent quotes need an address re-pull to get a satellite image**; the **event/highlighting analyzer** is still UNBUILT (wreaths/garland/roofline/spritzers); Jason's other lane (**#29** editor restyle · **#14** corner-house · **#109**) untouched. Next free ledger #: **131**.

**🎄 #83 Jobber operator workflow (Naldo S20) — COMPLETE + LIVE + trial-ready.** Full **create → job → complete → invoice → close** is operator-drivable on prod (Options console #288/#293 · audit-fixes #300 · rebook+backfill #306 · deposit-fix + reconciliation #309 · Needs-Action queue #310); live-verified + 3 council reviews. **PLAN: run the Aug–Oct parallel-with-Jobber trial** (council-endorsed). **DO FIRST:** pin the shadow rule in the operator runbook — *"Jobber is source of truth through October; the tool only shadows."* **Council backlog (optional polish, not trial-blocking):** input-anchoring on operator-typed money fields · a scheduled tool-vs-Jobber diff report · one-tap balance pay-link · approval-provenance labels · undo + idempotency guards on the money buttons. **Shadow rule pinned S21** → `docs/jobber-flow/OPERATOR-RUNBOOK.md`. **Balance collection = the customer PAY-LINK** (shipped, doc-confirmed, compliant). **Auto-charge researched S21 (9-agent workflow) → verdict `yes-needs-one-live-test`, NOT doc-confirmable** — Valor's Vault guide says stored cards are "Virtual Terminal only" (API charging may be disabled for our EPI) + the vault-token→`/?saleToken` linkage is inferred; needs ONE Valor staging test + 4 written confirmations + card-on-file consent at deposit. **Decision (Naldo S21): ship the pay-link, keep auto-charge as a Jason-gated follow-up (don't build blind).** The dead disabled "Charge remaining balance" button was removed from the trial UI. **Balance-collection surface SHIPPED S21 (PR #316, LIVE):** (A) **auto-send balance link** — `POST /api/quotes/[id]/send-balance` texts/emails the customer their pay-balance link via GHL + a "Text / email balance link" button on the invoice; (B) **operator card-on-file charge** — `POST /api/invoices/[id]/charge-balance` charges the saved card for the exact balance via the `valorBalance.ts` seam, **gated behind `VALOR_AUTO_CHARGE_ENABLED`** (button hidden till on; money-safe: short-capture guard refuses to settle a partial auth). **Jason's remaining #83 piece:** fill `chargeBalanceOnFile` (`/?saleToken`, populate `chargedUsd`) + deposit-vaulting + consent + one staging test → flip the flag. Full spec + staging recipe: `docs/jobber-flow/VALOR-AUTOCHARGE-FOR-JASON.md` (2026-07-02 update). **Only gated #83 piece:** auto-charge — Jason.

**#58 inbox (Naldo) — FULLY SHIPPED + LIVE through S17:** v1 triage (#258) → **v2** reply-from-inbox + AI draft (Sonnet) + layer-3 suppression + Followed snooze (#265, +#262 reopen fix) → **state lifecycle** In-the-works (Awaiting/Handled groups) + new `completed` terminal status + configurable follow-up days + **audit log `/inbox/activity` with per-entry Reverse** (#276) → audit **decisions filter** (#277) → **response-time analytics** (buckets · time-to-completed · trend · reopen rate · All/90/30d) on inbox+insights+home (#283). 2 migrations applied to prod (`followed_up_at`, `completed`-status CHECK). **Open #58 follow-up:** make the In-the-works rows fully actionable (reply composer + mark-followed inline; today only "Mark completed"). ⚠️ Clean gates = `npx eslint src` + `npx vitest run src --exclude '**/.claude/**'` (sibling nested worktrees pollute `npm run lint` / bare `vitest run src`).

**FUTURE (when building portal packages):** ✅ DONE in S12 (#65) — the A/B/C/D tiers are now REAL (Classic Glow / Full Festive / The Full Yule / Custom "Our Recommendation"), and the "Our Recommendation" card IS the #12 staff-recommended selection. No longer placeholders.

**Loose threads:** S10 left test quotes stamped "sent" + a test card (`1BZreaoZj…`) at Bid Sent in GHL + a few test emails to info@yulelovelights.com — all harmless test data (wiping before launch). S11 also deleted test quote `12160f46` from the shared Supabase (it 404'd on both dev + prod). S18 bumped a #93 TEST quote's `view_count` 7→8 during a live verification (harmless — test quotes are metrics-excluded). **docs/context synced through S18** (the S19 reconcile PR folded in the previously-unmerged S14b–S18 Naldo docs; **S17 was a concurrent Naldo inbox session** — #265/#276/#277/#283).
