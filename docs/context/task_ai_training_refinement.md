---
name: task-ai-training-refinement
description: Scoping + plan for the AI training/correction system refinement (the photoAnalysis few-shot loop). LIVING DOC — Stage A (capture) MERGED + Stage B (similarity retrieval) SHIPPED; Stage C (designer brain) is next.
metadata: 
  node_type: memory
  type: task
  originSessionId: e4aa4be2-5f3b-45ef-ba10-c5884f1b5b20
---

# AI training/correction system — refinement task (SCOPING / PLANNING)

## 📍 STATUS (S8, 2026-06-15): Stage A MERGED (PR #30) · Stage B SHIPPED · **Stage C = NEXT**
Stage B (similarity retrieval) committed on `jason/few-shot-retrieval` (`e9a09e7`/`cdfe543`/`2f2cd3e` + docs sync), pushed, **MERGED to master (S8)**. It's the unified pipeline: embed the house photo (Voyage `voyage-multimodal-3.5`) → pgvector cosine `match_training_examples` (cap 8) → few-shot, with automatic recency fallback. Re-verified live S8 (green note flipped to "similar"; prod DB `{total:2, embedded:1}`; 165 tests). The **"STAGE B — BUILD-READY PLAN"** section below is now HISTORICAL (built as described). **Stage C** = the designer-brain prompt work: feed AI-seed→staff-final scene pairs as examples; teach placement/count rules; maybe garland-section estimate + per-detection confidence.

## 🧠 STAGE C — AGREED PLAN (S8, 2026-06-15): teach the AI from its own mistakes
**✅ C1+C2+C3 BUILT + committed (S8)** on `jason/stage-c-designer-brain` (1 commit, PR off master, merge pending) — all prompt/few-shot, no schema; gates green (184 tests, +18). C1 = `seedFinalDiff.ts` (per-example seed→final diff in the few-shot user turn). C2 = `seedFinalStats.ts` + `getCorpusBiasNote` (corpus-wide bias block in the system prompt, ≥5 seeded pairs, folded into `assembleFewShot`). C3 = satellite orientation self-check in the dynamic satellite note. **C4 (garland sections) BUILT (S8) — reframed: NOT a schema change.** A scale already exists at seed time (`derivePxPerFoot`), so C4 = a compute fix in `seedFromAnalysis.ts`: seeded garland `quoteSections = ceil((boxWidthPx/ppf)/sectionLen)`, fallback 1 with no scale (Approach B; chosen over A=AI-estimates-length). Committed on `jason/stage-c-c4-garland`, PR off master. **Only C6 (per-detection confidence) still deferred.** All Stage C teaching no-ops on an empty corpus (built ahead of the post-wipe repopulation).

**Core insight (Plan-agent, code-grounded):** Stage A captures BOTH the AI's first guess (`original_analysis`/`seed_analysis`) AND the staff's final scene, but the few-shot only ever shows the model the FINAL answer — `exampleToFewShot` never reads `original_analysis`. The captured **seed→final PAIR is the unused teaching signal.** Stage C = make the model learn from "here's what you guessed, here's the correction."

**Jason's calls (S8 — confirmed):** build **C1 → C2 → C3 NOW** (all prompt/few-shot, NO schema change); **teach from the DIFF text** (not the raw wrong seed — cheap tokens, directive, avoids the model imitating the wrong answer); **C4 (garland schema) = LATER**; **C6 (per-detection confidence) = DEFERRED**.

**Sub-steps:**
- **C1 (M, no schema) — teach from the seed→final pair.** Read `row.original_analysis` in `exampleToFewShot` (trainingExamples.ts), thread it through `FewShotExample`, and in `buildFewShotMessages` (photoAnalysis.ts) inject a compact DIFF line into the example's user turn ("your first pass overcounted bush strings 8→5, missed the left return, set garland 1→3 sections; corrected JSON below"). Assistant turn stays = the final answer. **MUST normalize both seed + final to the same coords/vocabulary before diffing** (else the diff reflects converter lossiness, not model error — agent risk #3).
- **C2 (M, no schema) — systematic-bias prompt block.** New pure lib (e.g. `seedFinalStats.ts`) computes corpus-wide deltas from stored seed/final pairs (AI overcounts minis ~Nx; defaults garland to 1 section; flips satellite red/blue ~X%), injected as a short dynamic note in photoAnalysis.ts. Self-improves as data grows; skip the block when N small.
- **C3 (M, no schema) — satellite teaching lane.** Add an explicit satellite orientation self-check (cross-ref the street image to fix red/blue) to the satellite note; get more satellite-bearing examples into the feed (today only examples with confirmed satellite lines carry the satellite image). Satellite is the weakest area.
- **C4 (M–L, SCHEMA) — LATER:** garland length/section estimate on `GarlandDetection` + prompt schema; carry through `seedFromAnalysis` (kills hardcoded `quoteSections:1`) + `sceneToFewShot`.
- **C6 — DEFERRED:** per-detection/line confidence (invasive; revisit after C1/C2).

**Timing:** like Stage B, the signal is near-empty until the post-wipe corpus grows (Jason wipes before Christmas, won't build in busy season). Jason chose BUILD NOW anyway (same "degenerate-case-while-small, self-upgrades with data" logic) → every sub-step MUST degrade gracefully on a tiny/empty corpus (fall back to current behavior; skip stats when N small).

**Key files (Stage B did NOT touch these — agent analysis valid):** `photoAnalysis.ts` (SYSTEM_PROMPT + buildFewShotMessages + dynamic notes + FewShotExample type), `trainingExamples.ts` (`exampleToFewShot` — currently ignores `original_analysis`), `sceneToFewShot.ts`, `seedFromAnalysis.ts` (hardcodes `quoteSections:1`). NOTE: route-level example SELECTION is now similarity cap-8 via `fewShot.ts` (Stage B) — the agent's "recency cap 2 / fewShot.ts doesn't exist / build Stage B" notes were STALE (it read a pre-Stage-B branch by mistake). Full agent report (with its Stage-B blind spot) is in the S8 workflow task output.

## ⚡ S7 RECONCILIATION (2026-06-12) — code-grounded re-map after #35 + #36 (4-agent sweep; supersedes stale S3 claims below where they conflict)
- **🔴 BIGGEST CHANGE: the correction CAPTURE UI no longer exists.** #35 Phase 1 removed it from /quote/new along with the per-unit forms (known/accepted at the time — "corrections capture pauses until #8"). `/api/save-correction` still exists but nothing calls it. The few-shot loop is READ-ONLY today: only pre-#35 corrections feed it; staff refinement of seeded designs (the richest training signal we've ever had) is captured nowhere.
- **NEW OPPORTUNITY unlocked by #35:** the staff-refined SCENE is perfect ground truth. A scene-based correction = {street photo + satellite image + the AI's raw analysis + the staff's FINAL scene + final footages}. One capture covers roofline tracing, placement, counts, sizes — the modern replacement for the old form-based correction. This is what "scene-based corrections/training" in the S6 close meant.
- **Satellite is still blind end-to-end (confirmed in code):** corrections store satellite LINE coords but no satellite IMAGE (orphaned); training-house photo ordering puts `satellite` 6th of 7 then `slice(0,4)` → effectively always dropped. The model has ~zero satellite trace examples while satellite is THE pricing source (#35 made it satellite-or-manual). Highest-leverage data fix, unchanged from S3 but now more urgent.
- **Issue #8 (stale "Gutterline ft / Ridgeline ft" labels): ALREADY FIXED** — current /training/new says Front Gutterline / Ridge + Sides. Drop from the punch list.
- **Style-ranking confirmed dead** (no `houseStyle` hint ever sent — builder has zero style logic); superseded anyway by the DECIDED image-embedding retrieval.
- **Current few-shot caps (verified):** 2 corrections (recency) + 2 training houses (recency; pool of 10) + 2×3 reference assets; ≤4 photos/house; `claude-sonnet-4-5`, max_tokens 2048. Coordinate rescue (0–1000→0–1) + 5ft rounding on parse.
- **Render-era items moot** (#36): no render pipeline anywhere; the analysis output's ONLY consumer is `seedFromAnalysis.ts` → the design. "Make the AI better" now literally = "make the seeded design need less staff cleanup."
- **Where staff cleanup actually goes today (per the delivery-path map):** phantom/missed mini detections + string-count overrides; wreath placement hit-rate; garland `quoteSections` always defaults 1; roofline footage accuracy; satellite orientation flips.
- **PROPOSED STAGED PLAN (pending Jason's calls, S7):** **Stage A** = rebuild capture, scene-based (correction = photo pair + raw analysis + final scene; SAVE THE SATELLITE IMAGE; + #9 manual front/satellite split uploads; + overlay lines on the corrections View page). **Stage B** = retrieval few-shot (image-embedding match per the S3 decision; raise example count past 2; include satellite-image examples in satellite calls). **Stage C** = designer-brain prompt work (feed AI-seed→staff-final scene pairs as examples; teach placement/count rules; maybe garland-section estimate + per-detection confidence). Open calls for Jason: capture TRIGGER (auto at Calculate/Send vs explicit button vs hybrid), Stage A scope, embedding-provider research timing.

## ✅ STAGE A BUILT (S8, 2026-06-14) — branch `jason/training-capture`, review-hardened, awaiting Jason verify + commit
Scene-based capture fully implemented + adversarially reviewed (4 reviewers → per-finding skeptic verify; 1 critical + 1 high + 7 medium + 6 low — ALL fixed). Key files: `trainingExamples.ts` (capture + `exampleToFewShot`), `design/sceneToFewShot.ts` (pure inverse of seedFromAnalysis), `/api/training-examples/*`, `/api/designs/[id]/analysis-context`, `designs.ts` (satellite up/download + provenance), `/training/examples` page + `components/training/AnnotatedPhoto.tsx`, migration `2026-06-12-training-examples.sql` (APPLIED to prod). Few-shot now: design examples take the correction slots first, legacy corrections fill the rest (sunset at the planned wipe). **⚠️ Touched the vendored `editor-core/editor.ts`** (flushSave for the autosave-before-capture race — marked `[yll #8 Stage A]`, RELAY to design-tool AI; cores no longer byte-identical). Gates: tsc · lint 0/2 · 151 tests. Detail in the S8 session-log entry. **NEXT after merge: Stage B (embedding retrieval) → Stage C (designer brain).**

> **Task #8 in [[task_ledger.md]]** (companion: #9 = manual satellite upload on `/quote/new`).
> **Status: PLANNING DONE (S3, 2026-06-05).** Jason confirmed he's listed everything; issues + direction captured. Build deferred — feeds #27 (integration) Phase 3 auto-design. Detailed design happens at build time. Open items carried to build: `front_takedown` photo meaning (ask Naldo), fine-tuning research (later). Jason wants to stay in "list the issues + plan" mode and is still adding feedback; do NOT start building until he says go. This doc is a running list — keep appending as more comes up. Started Session 3 (2026-06-03), Jason driving (Naldo originally built this system; Jason is learning it).

## 🔑 S7 DECISIONS (Jason, 2026-06-12) — Stage A kickoff
- **Capture trigger = BOTH:** auto-capture at Send-to-customer (+ an "exclude" badge for oddballs) AND an explicit "Save as training example" button.
- **Build order: Stage A NOW** (capture rebuild) → B (embedding retrieval) → C (designer brain). Branch `jason/training-capture`.
- **🧹 FULL DATA WIPE PLANNED:** once the training overhaul is done, Jason will WIPE all existing DB data (quotes/corrections/training houses — ALL of it is pre-production test data) and repopulate fresh with correct houses/designs. Implications: NO migration of old corrections into the new format (they feed few-shot only until wipe-day); new schema designed clean-slate; current data hygiene irrelevant — the mechanism is what matters. Training examples must be SELF-CONTAINED snapshots (own photo/scene copies, quote_id/design_id nullable links only) so they survive quote/design deletion.

## ✅ STAGE B — BUILT + REVIEW-HARDENED + PROVEN LIVE (Session 7, 2026-06-15). UNCOMMITTED on `jason/few-shot-retrieval`; holding for Jason's self-test. *(NUMBERING: any "S8"/"S9" in this doc = Session 7 — one continuous conversation; next fresh session = S8.)*
Shipped exactly the plan below, with review fixes. Files: `src/lib/embeddings.ts` (Voyage client), `src/lib/fewShot.ts` (assembleFewShot + selectFewShot), `migrations/2026-06-15-training-example-embeddings.sql` (APPLIED to prod), trainingExamples.ts (embed-at-capture + getSimilarTrainingExamples), both analyze routes, builder note (similar-vs-recent), embeddings.test.ts + fewShot.test.ts. **Adversarial review caught 2 CRITICALS** (my WebFetch had the WRONG Voyage shapes): request content type is **`image_base64`** (not `image`); response vector is at **`data[0].embedding`** (not `embeddings[0]`) — both settled EMPIRICALLY against the live API. Also pinned `output_dimension:1024` + fixed the fallback gate to decide on PROJECTED count. **PROVEN LIVE:** real Voyage key + prod DB → embed → store `number[]` straight into `vector(1024)` via supabase-js (no string conversion) → RPC ranked the matching image first by cosine. Gates: tsc · lint 0/2 · **165 tests**. Voyage key is in Jason's `.env.local` (still needs adding to **Vercel** for prod). The plan that was executed:

## 🟢 STAGE B — BUILD-READY PLAN (UNIFIED retrieval: similarity-primary + recency-fallback). Rewritten S8 2026-06-14 after Jason+Naldo changed direction; BUILT S9 (see above).

**🔑 DIRECTION CHANGE (Jason + Naldo, S8): B2 is NO LONGER deferred — build it NOW, as ONE pipeline (not two switchable systems, not a future cutover).** The realization: B1 (newest-N) and B2 (most-similar-N) are the SAME pipeline with a different ranking step, and **"most-similar 8" ≡ "all of them" when the library ≤ 8** — so B1 is just the degenerate case of B2. Therefore:
- **Similarity (Voyage) is the PRIMARY ranker.** **Recency is the AUTOMATIC FALLBACK** (used when: no `VOYAGE_API_KEY`, the query-image embed fails, or there are no embedded rows yet). So analyze NEVER breaks, the system behaves like B1 while the library is small, and **self-upgrades to B2 the moment the Voyage key + embeddings exist — no toggle, no cutover, no dead code to remove.**
- **CONFIRMED CALLS (don't re-ask):** unified pipeline ✓ · **image-only embedding first** ✓ (add a Claude-generated architectural-caption hybrid later ONLY if a precision@k eval on real data shows image-only retrieval is weak — documented future optimization, not v1) · cap = **8** (tunable) · model **`voyage-multimodal-3.5`** · **`vector(1024)`** · Supabase **pgvector**.
- **Why now is low-risk:** while the library ≤ 8, similarity returns the same SET as feed-all, so even mediocre early similarity is harmless; by the time it matters (library > 8) there's real data to validate with. Example QUALITY still matters (no similarity magic fixes a bad capture) → keep using the Stage A "exclude" toggle.

**⚠️ JASON ACTION (gating):** create a **Voyage account + API key** at voyageai.com (free tier: 200M tokens + 150B pixels — effectively never pay at this volume), add **`VOYAGE_API_KEY`** to `.env.local` (and Vercel for prod). The build can land BEFORE the key exists — it just runs on the recency fallback until the key is present, then lights up as similarity. So not strictly blocking the build, but needed to actually exercise B2.

**Why a refactor underneath:** the few-shot assembly is DUPLICATED verbatim in BOTH `src/app/api/analyze-photo/route.ts` (≈L54–106) and `src/app/api/analyze-address/route.ts` (≈L81–130). Extract into ONE shared `assembleFewShot()` so ranking + cap live in one place.

**BUILD STEPS (in order):**
1. **NEW `src/lib/embeddings.ts`** — the Voyage client. `isEmbeddingConfigured()` (checks `VOYAGE_API_KEY`, graceful-skip like other integrations); `embedImage(base64, mediaType): Promise<number[] | null>` → `voyage-multimodal-3.5`, returns the 1024-dim vector, **null on any failure/unconfigured** (null is the signal to fall back to recency). ⚠️ **VERIFY the multimodal call shape at build:** the research flagged the `voyageai` npm README mostly documents TEXT `embed()`; the multimodal path is the REST endpoint `POST https://api.voyageai.com/v1/multimodalembeddings` — if the SDK's multimodal method is unclear, just raw-`fetch` that endpoint. Add `VOYAGE_API_KEY` to `.env.local.example` + ONBOARDING + [[project-secrets-access]].
2. **Migration `migrations/2026-…-embeddings.sql`:** `create extension if not exists vector;` · `alter table training_examples add column if not exists embedding vector(1024);` · RPC `match_training_examples(query_embedding vector(1024), match_count int) returns setof training_examples` → `select * from training_examples where excluded = false and embedding is not null order by embedding <=> query_embedding limit match_count`. NO ANN index needed at a few-hundred rows (exact scan is instant; 1024 < pgvector's 2000 index cap if one's added later). Apply via the direct DB connection (the S7 `SUPABASE_DB_URL` pattern; show SQL + get Jason's yes) or Jason pastes it.
3. **Embed at capture** — in `captureTrainingExample` (`trainingExamples.ts`): after assembling the row, `embedImage(street photo)` → store in `embedding`. Graceful — null on failure, row still saves (just recency-eligible). Voyage down NEVER breaks capture.
4. **Similarity fetch** — `getSimilarTrainingExamples(queryEmbedding, limit)` in `trainingExamples.ts` via `.rpc('match_training_examples', { query_embedding, match_count })`.
5. **NEW `src/lib/fewShot.ts`** — `export const FEW_SHOT_LIMIT = 8;` + `assembleFewShot(houseStyleHint, queryImage)`: embed the query image; **if** a vector came back AND the RPC returns rows → similarity path; **else** recency fallback (`getRecentTrainingExamples`). Then merge with training houses + corrections via a PURE, unit-testable `selectFewShot({...}, limit)`. **Order most-relevant LAST** (model weights the end of context most + "lost in the middle"); design/training are ground truth, corrections legacy-fallback. Returns `{ examples, references, breakdown }`.
6. **Wire both routes** to call `assembleFewShot(houseStyleHint, streetBase64)` — pass the query image so it can embed it. Replace the duplicated inline blocks. analyze-address keeps its `satellite` analyzePhoto option; analyze-photo doesn't.
7. **Scope v1 to `training_examples`** for similarity (the going-forward primary, auto-captured). Keep `training_houses` + legacy corrections as recency-included fill (DON'T embed training_houses in v1 — fast-follow if Jason uses that manual flow heavily). Image-budget lever still applies: training houses send up to 4 photos each (`slice(0,4)`) → consider trimming to 2 to bound latency.
8. **photoAnalysis.ts:** no change — the corrNote already enumerates sources (Stage A M3 fix).
9. **TESTS `src/lib/fewShot.test.ts`:** pure `selectFewShot` — under-cap returns all; over-cap returns exactly `limit`; ordering (best last); empty → []. Plus the fallback DECISION (vector present → similarity; null → recency). Embed + RPC verified live.

**GATES + VERIFY:** tsc · lint · test. Live (two passes): (a) **with** `VOYAGE_API_KEY` set — capture a few examples (confirm `embedding` populates), analyze a house, confirm similarity ordering + `fewShotBreakdown` caps at 8; (b) **without** the key — confirm analyze still succeeds via recency fallback. Final analyze verify is Jason's (needs his Anthropic key; the empty-`ANTHROPIC_API_KEY` shell gotcha).

**LOCKED:** unified pipeline · image-only · cap 8 · voyage-multimodal-3.5 · vector(1024). **Open/judgment at build:** training_houses embedding (deferred fast-follow) + exactly how recency-included training_houses merge with similarity-ranked training_examples; the hybrid caption (future, eval-gated). Full B2 provider research = the S8 workflow output `…/tasks/w32aqqn9o.output`.

**LOGISTICS:** branch off an up-to-date **master** — S8 `git pull origin master` was BLOCKED by the github.com DNS flake (local master stale at `962de90`; Stage A merged code == the `jason/training-capture` branch). FIRST retry the pull (or Jason sets DNS to 1.1.1.1/8.8.8.8); if still blocked, branch off `jason/training-capture` (identical content), reconcile when DNS clears. Branch e.g. `jason/few-shot-retrieval`. Bigger than B1-alone — ~a session: embeddings lib + migration → capture wiring → fewShot.ts + route wiring → tests. Dev server gets reaped (exit 127 = external kill, not a crash) + an orphan may hold :3000 — clean-restart before verifying.

## Why this task exists
Phase 4 (#17) fixed the roofline classification in the prompt, but validating it exposed how little the "training" system actually does. Jason wants to understand it fully, then refine it so the tool is solid for years. Core realization driving everything:

**This is few-shot PROMPTING, not learning/fine-tuning.** Every Analyze is a fresh, stateless Claude API call; saved corrections/training-houses are just a few worked examples pasted into that one prompt for it to imitate. The model never permanently learns. Only the **latest 2** corrections (+ up to 2 training houses + 2 reference assets) are injected per call — a context/cost cap, not a bug. So "more data → smarter over time" (a fine-tuning mental model) is NOT how it currently works.

## Strategic direction (CONFIRMED with Jason)
Build toward "accumulate + improve," staged:
1. **Fix data capture / hygiene first** — every saved example clean + complete (correct red/blue, captured satellite image, records the ACTUAL install). The clean dataset is the durable asset for any path.
2. **Near-term: retrieval-based few-shot** — keep the WHOLE library; at quote time retrieve the most relevant N examples instead of "latest 2." **Match signal = image-embedding similarity (DECIDED, Jason): match on the actual house image, NOT style words.** Gives "gets better as we add data" today, with Claude, no fine-tuning.
3. **Future option: fine-tuning** — a step-change once the dataset is large + clean. Likely needs a vision-tunable model (Claude vision fine-tuning may not be generally available — RESEARCH needed, see below). Keep as a later decision, not now.

## Issues found (current state)
1. Few-shot, not learning; only latest 2 corrections used (cost/context cap).
2. **Corrections don't save the satellite IMAGE** — only the street photo. Satellite line coords ARE saved but orphaned (replayed with no image for the model to see → teach nothing). Jason wants corrections to save satellite image + tracings too, like street view.
3. **Training-house satellite photos get dropped** — priority #6, and the analyzer only sends the first 4 photos (`slice(0,4)` in `api/analyze-photo`). No dedicated satellite upload button either.
4. **Corrections "View" page (`/training/corrections`) doesn't draw the saved lines** on the photo — shows them only as a text list. Visualization gap (data is there).
5. **`/training/new` Analyze uses the QUOTING AI** (`/api/analyze-photo`) — it SUGGESTS where lights could go (hallucinated bush boxes on a roofline-only install) instead of READING what was actually installed. Wrong tool for the job. Compounded by lit-install-photo vs daytime-bare-photo mismatch (the quote AI expects bare daytime houses).
6. **Training-house fields stored but NEVER sent to the AI** (decide keep-vs-remove later; some maybe kept as business records): address, year_completed, general notes, C9/Winter-Wonderland footage+difficulty+c9_lines, the spritzers/wreaths/garland PRICING arrays (separate from the detection boxes), scale_anchor, "didnt_install", cost_materials, cost_labor_hours, revenue.
7. **House-style ranking is dead in practice** — the quote builder never sends a `houseStyle` hint, so `getTrainingFewShot` always falls back to recency (latest 2). The style field is stored but unused at quote time; the AI does NOT auto-detect style.
8. **Stale labels** — `/training/new` footage inputs still say "Gutterline ft" / "Ridgeline ft" (a Phase-4 leftover; canvas labels were updated but not this section). Should be Santa's/Front + Gingerbread (Ridge + Sides).
9. **Photo-tag system over-built** — front_install / front_takedown / side / back / detail / satellite. `front_takedown` purpose unclear (confirm with Naldo — possibly the bare/no-lights front).

## Planned changes / direction (EVOLVING — not final)
- **Photo uploads (both /training/new and the correction flow): simplify** to → "Install photo" (completed install, usually 1, sometimes more) + **dedicated Satellite** (Google pull for convenience OR manual upload fallback; canvas comes up to trace red/blue ON the satellite = "this is what we actually did") + optional "extra photos" catch-all. Drop the other tags.
- **Corrections (`/quote/new` save-correction): capture + save the satellite IMAGE + tracings** too (mirror the street-view save). Add a manual **"upload satellite image"** option alongside the Google pull + the existing manual front-of-house upload (so we can upload both a front image AND a satellite image when needed).
- **Satellite, end-to-end:** make sure the satellite image + lines are actually captured AND reach the model as training (today they're dropped/orphaned).
- **Retrieval-based few-shot** (the core upgrade): use the full dataset, retrieve best matches. Decide the match signal here.
- **`/training/new` Analyze:** consider a separate "detect what's installed in this photo" mode instead of the quoting AI.
- **Corrections View:** overlay the saved red/blue lines on the photo.
- **Fix the stale "Gutterline/Ridgeline" training labels** (small; could even be a quick pre-task fix).
- **Decide keep-vs-remove** on the AI-unused fields (#6) — cost/revenue/labor maybe kept as business records.
- **Google satellite pull cost:** negligible (~fractions of a cent/image, monthly free credit; quote flow already pulls it). Use Google pull + manual fallback.

## Adjacent change — manual satellite upload on `/quote/new` (may be its own small task)
Jason (clarified): on the **quote builder** (`/quote/new`), today you can upload ONE manual photo (any photo). He wants to **split that into two manual uploads — a front-of-home photo AND a satellite photo** — with Analyze working the same for each as it does when pulling from Google Maps. **Why:** so a manually-uploaded house can also carry a satellite view → more/better training data. **Coupling:** this is interdependent with the training work — corrections can only "save the satellite image" if `/quote/new` actually captures one. So do it as part of (or immediately alongside) this task's satellite-capture workstream, even though it's technically a quote-builder UI change. Jason left the task-placement to us.
**⚠️ SCALE CAVEAT (Jason realized this):** the Google-pulled satellite's accuracy comes from a KNOWN feet-per-pixel scale computed from the Static Maps zoom-20 + latitude formula (`analyze-address` route ~L71-77). A **manually uploaded** satellite has **no known scale** — the AI could still trace the layout (normalized coords) but couldn't compute reliable FOOTAGE without a scale reference. So a manual satellite is good for capturing the **tracing/layout (training)** but is **not pricing-accurate** unless we add a scale step. **Jason's resolution (don't over-build):** when staff manually upload a satellite, they'll already have the footage on hand from their own Google Maps and will just **enter it manually** — so NO auto-scale/reference mechanism is needed; the existing manual footage fields cover it. Manual satellite uploads are expected to be **rare** anyway (Google pull is reliable and far better than street view). Google-pull stays the auto-measured source. (Also see task #25 — making satellite the default pricing source.)

## Open questions / decisions deferred
- ~~House-style matching~~ **DECIDED (Jason): use image-embedding similarity as the retrieval match signal, NOT style words / a style-guesser.** Keep `house_style` as captured metadata only (not the match key).
- **`front_takedown` photo purpose** — confirm with Naldo.
- **Fine-tuning** — keep as a future option, gated on dataset size/quality + availability.

## RESEARCH TODO (later — do NOT do yet, Jason will say when)
- Current state of **Claude / vision-model fine-tuning** availability + options (Anthropic API limits; vision fine-tuning at other providers e.g. OpenAI GPT-4o, Google Gemini), so the "do we fine-tune eventually" decision is grounded in what's actually offered today.
